# Editor Panelのソースモード統合（2026-09-20）

対象：RFN01-17 / RFN01-20 / RFN01-27、PR #4。
会話で「今回、ソースモードへ統合する」と合意。

## 実装

- Panel専用の本文TextInputを撤去し、通常Editorの横書きソースモードと同じEditorPane、TextEngine、入力・選択・IMEのコールバックを使用する。
- 上段のPane配置と独立した表示IDで、Panel文書と選択・スクロール状態を保持する。画面を切り替えても上段の文書を差し替えない。閉じたPanelの状態と描画キャッシュを解放する。
- ログ追記、履歴保持行数、ReadOnly、Ctrl+Enter送信、上段Terminal TABごとの所有関係は維持する。保存は既存のPanel保存処理へ接続し、ログ用の既定ファイル名も維持する。
- 新規Panelの保存後のTAB名、背景・文字色、Terminal表示中のPane背景メニュー、ログ出力をしていないPanelの終了確認を修正。
- ログ開始前の画面再描画と、カーソル位置移動だけの空行を取り込みに混ぜない。実際の空行は維持する。
- フォント一覧はEditor設定の実際の選択ボタンの直下に表示する。

## 検証の観点

- 通常Editorと同じ文書・状態を参照した日本語追記、選択解除、ReadOnly中の入力抑止。
- 3,000行のPanelでクリック・末尾移動・描画。入力とログ追記後の背景をピクセルで確認。
- 上段TAB切替・非表示中のログ継続と取り込み先維持、停止先の独立、保存・名前変更、シェル切替のキャンセル。
- 設定画面を開いた際のRefCell競合を回帰テストで検出し修正。
- 実画面で日本語入力、全選択からEscape解除、新規保存後のTAB名更新、ログ追記・停止・保存、TerminalのPane背景メニューを確認。

最終テスト件数、確認したコミットと実行ファイルの対応は確認用フォルダのBUILD.txtに記録する。
長時間・大容量ログとすべてのシェルの制御シーケンスを網羅したものではない。
ユーザーAccept後にPRをマージし、Issue Closeはユーザーが行う。

## 0fb8b16のレビューへの追加対応

- ログ追従でスクロール位置を変えた直後に、同じソース描画処理で表示タイルを更新する。
- Terminalのサイズ変更・再描画に伴う先頭空行を取り込みから除外する。通常の先頭空行と出力間の空行を残す回帰テストを追加。
- 非アクティブTerminal TABにも保持している背景色と、それに対応する文字色を渡す。
- Editor設定のフォント一覧の座標で、親レイアウトの位置を重複加算しない。
- 修正版の実画面で、ログ追記・停止後の先頭表示、非アクティブTABの背景と文字色、フォント一覧の左揃えを確認。
- 設定のホイールは「フォント一覧を開く→余白をクリックして閉じる→その余白上でスクロール」で正常動作した。ユーザー報告は未再現であり、修正済みとは扱わない。
- 全体テスト1,044件、明示実行するTerminal実接続テスト10件が成功。
