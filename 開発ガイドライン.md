# RFN Edit 開発ガイドライン

追加機能・不具合修正・新しい画面の設計前に読む。仕様と進行は[AGENTS.md](AGENTS.md)、機能要件は[要件定義.md](要件定義.md)・[追加要件.md](追加要件.md)に従う。本書は実装の責務と再利用・検証の判断基準を扱う。部品化の経緯と検証記録は[Editor部品化_実装計画.md](Editor部品化_実装計画.md)に残す。

## 1. 機能追加前に決めること

対象の操作、期待結果、変更する範囲、既存動作への影響、完了条件を整理する。Issueの登録だけで着手しない。新規画面や外観変更では、入口・配置・表示名・初期状態・フォーカス・確定／キャンセル・閉じた後の状態を合意する。

実装計画には、再利用する部品と接続先、状態の所有者、通知先、保存方針、終了時の解放、影響する利用先とテストを明記する。似た処理を追加する前に既存コードを検索し、既存部品の拡張で実現できるか判断する。共通化できない場合は、異なる契約とその理由を計画に残す。

## 2. Editorの責務と参照先

| 責務 | 参照する実装 | 利用時の原則 |
| --- | --- | --- |
| 文書・変更履歴・通知 | [open_document.rs](src/open_document.rs) | `OpenDocument::with_events`で文書と通知を作る。文書にWindowを直接持たせない |
| 画面向けの文書通知 | [document_ui.rs](src/document_ui.rs) | Windowに関する通知は利用先のアダプターで接続する |
| 編集セッション | [editor_session.rs](src/editor_session.rs) | `EditorSession`の入力確定・範囲置換・Undo/Redoを使う |
| 選択・ReadOnly・追従などの表示状態 | [editor_state.rs](src/editor_state.rs) | 同じ文書を別表示する場合も、表示ごとの状態を区別する |
| キャレット移動・クリック・ドラッグ | [editor_interaction.rs](src/editor_interaction.rs) | 既存の選択・移動処理を使う。利用先の操作方針の違いはアダプターに置く |
| 組版・タイル描画 | [editor_render.rs](src/editor_render.rs) | `LayoutOptions`・`TileViewport`などの値で条件を渡す |
| 本文表示・入力・選択描画・IME・本文メニュー | [EditorSurface](ui/editor-surface.slint) | 本文編集用の独自TextInputや別の描画経路を作らない |
| TAB・検索欄・フッターなどの外枠 | [EditorPane](ui/editor-pane.slint) | 本文部品と外枠の操作を分ける |
| 主Windowへの埋込み・解放 | [editor_host.rs](src/editor_host.rs)・[panel_source.rs](src/panel_source.rs) | 状態・画面行・描画キャッシュをまとめて登録／解放する |
| 別Windowの接続例 | [draft_editor.rs](src/draft_editor.rs)・[quick-draft.slint](ui/quick-draft.slint) | `draft_editor::install`を参照。AppWindowや代理TABを生成しない |
| OS入力・IME候補配置 | [input_platform.rs](src/input_platform.rs)・[ime.rs](src/ime.rs) | OS固有処理を利用先ごとに複製しない |

現在の部品はアプリ内で共有するモジュールであり、独立した公開ライブラリではない。描画用の型などには`main.rs`側の定義を参照する部分も残る。外部crateとして利用できることを前提にしない。

## 3. Editorを新しい場所へ組み込む

1. 文書を新規作成するか、既存の`Rc<OpenDocument>`を共有するか決める。文書の履歴と、各表示の選択・スクロールの所有を分ける。
2. `EditorSession::new`、または既存状態を引き継ぐ`with_state`を使う。Panelで同じ状態を参照する場合に、コピーと同期処理を追加しない。
3. `EditorSurface`へ表示状態を渡し、入力・選択・移動を共有処理へ接続する。本文機能のために偽のTABや隠れたAppWindowを作らない。
4. 書式・表示範囲・向き・ズームを描画処理へ渡す。余白や外枠は利用先の契約を確認する。Quick Draftへ通常Editorの紙面枠を持ち込むなど、再利用に伴う外観変更を避ける。
5. 変更結果を利用先の通知・保存・画面更新へ反映する。編集APIが保存や全表示の更新まで自動実行するとは考えず、既存の接続例で戻り値と通知の流れを確認する。
6. フォーカス移動、非表示、閉じる、非同期処理の終了まで接続する。別Windowの場合は主Windowなしで入力・描画・終了できることを検証する。

## 4. 編集・IME・寿命で守ること

文字位置はUTF-8のバイト境界を確認する。古い選択範囲、不正な範囲、日本語を含む置換を扱い、範囲外アクセスでPanicにしない。通常の編集で文書を直接書き換えて履歴やReadOnly判定を迂回しない。ログ追記・復元など編集と異なる操作は、その専用の契約に従う。

IME未確定文字を保存本文やUndo履歴へ混入させない。確定・キャンセル・Enter改行・Shift選択を既存経路で処理する。フォーカス維持用の見えない入力欄と、候補が避ける実際の文字範囲は別物である。`EditorSurface`の`ime-area`を利用先のWindowの`ime::candidate_area`へ接続し、文字サイズ・縦横・スクロールを反映する。最下行、縦書き見出し、別Windowで候補一覧まで表示して確認する。

`RefCell`の借用を保持したまま、同期通知や画面コールバックを呼んで再入しないか確認する。文書通知は汎用の再入防止機構ではない。再描画を保留する場合は次回の更新で回収し、要求を失わない。

タイマー・非同期通知は弱参照と対象の有効性を確認し、閉じた表示を保持・更新しない。埋込みIDを再利用して古い通知を新しい面へ届けない。キャッシュと状態の登録には対応する解放を用意する。

## 5. 周辺機能も既存部品を使う

- メニューは[controls.slint](ui/controls.slint)の`MenuFrame`・`MenuRow`などを使う。メニュー・キー・ボタンは同じ操作へ接続する。条件付き部品を閉じると通知元が消える場合、操作を通知してから閉じる。
  - A案の主タイトルメニューは`menu_commands.rs`のWindows Popupを使い、既存コマンドへ接続する。ネイティブメニューの項目・子メニュー操作と、主ウィンドウの枠／Snapを扱うための例外。本文やPanel内の既存メニューは変更しない。
  - 挿入の項目は`menu_commands.rs`の`INSERT_MENU`が1つだけ持つ。タイトルバーの挿入メニュー、本文の右クリックメニュー、キー設定の名前、書式のツールバーとパレット（[format-bar.slint](ui/format-bar.slint)）はここから引き、押せる条件は`context_insert_state`に揃える。操作は`InsertShape`（形）で運び、番号は右クリックメニュー・書式のボタン（Slint）との境目でだけ使う。項目を足すときは表に1行足し、1段目に足したら`format_face`に絵と束を足す（試験が捕まえる）。
  - 選択範囲（無ければキャレット）の読み出しは`editor_state::chosen_source_range`を使い、同じ処理を利用先に書かない。
- タグ（`#タグ`・フロントマターの`tags:`）の読み方は[tags.rs](src/tags.rs)の1か所に置く。本文の色（`document.rs`）、索引（`Entry::tags`）、`tag:`検索、Ctrl+クリック、`#`補完はここを呼ぶ。規則を利用先で書き直さない（色の付いた語が検索に出ない、が起きる）。
  - 主メニューからの貼り付けは`EditorSurface.paste-from-clipboard()`、フォーカス復帰は既存の`take-focus()`を使う。`menu-key`は未確定文字がない場合にホストへ通知し、EditorPaneがF10／Escを扱う。Quick Draftはこの通知を接続せず、本文やIME処理を複製しない。
- ポップアップは対象要素と可視範囲の座標を渡して配置する。画面端・分割・スクロール・DPIを検証し、利用先ごとに固定座標を足して直さない。
- 外観は既存の書式部品と[appearance.rs](src/appearance.rs)を使う。Editor／Terminalの独立設定、Pane背景、個別背景の適用順位を保つ。名前が似ていても異なる設定契約を一つにしない。
- 保存は[saving.rs](src/saving.rs)、[writer.rs](src/writer.rs)、[file_io.rs](src/file_io.rs)、[app_data.rs](src/app_data.rs)の既存経路を調べる。明示保存・Workspaceの自動上書き・退避・ログファイル・下書き内部保存を区別する。
- ログ取り込み・保持行数・追従・停止後保存はPanel側、履歴・復元・コピー・TAB送信はQuick Draft側が担う。利用先固有の機能を本文部品に持ち込まない。

## 6. 検証とレビュー

| 変更対象 | 必要な確認の例 |
| --- | --- |
| 編集・選択・履歴 | 日本語・空文書・先頭末尾・選択置換・Undo/Redo・ReadOnly・古い範囲。EditorSession等のテスト |
| 本文表示・IME・操作 | 通常Editor／Panel／Quick Draftへの影響。縦横・Live Preview・候補一覧・フォーカス・最下行・サイズ変更。`memo_ui_tests.rs`等と実操作 |
| Panel・ログ | 非表示中の追記、保持上限、追従、取り込み中ReadOnly、停止後保存、出力先維持。Terminal関連変更なら必要な実ConPTYテスト |
| 通知・寿命 | 面を閉じた後の遅延通知、状態とキャッシュの解放、文書共有と表示状態の独立 |
| 外観・新規画面 | 合意した入口・配置・初期値・遷移、通常サイズと縮小時、各利用先での実画面 |

共有部品の修正では、報告された利用先だけでなく他の利用先も影響を確認する。変更箇所に応じた回帰テストを行い、作業単位がまとまった段階で全体テストと一つの確認版を用意する。文書のみの変更ではリンク・参照先・内容の整合を確認し、再ビルドを目的化しない。

報告はソース確認、自動テスト、実画面確認、ユーザーAcceptを区別する。件数や自己申告だけで完了とせず、報告された操作を再現する。確認版の実装コミットと場所、短い操作手順、未確認事項を示す。ユーザーAccept後にPRをマージし、IssueのCloseはユーザーが行う。

共通API・責務・接続方法を変更したときは、本書と利用例、影響するテストを同じPRで更新する。実装が本書と異なる場合は、実装と合意事項を照合して更新し、古い説明を理由に新しい独立実装を増やさない。
