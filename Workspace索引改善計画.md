# Workspace索引・リンク解決の現状調査と改善計画

2026-09-20 / RFN01-24。以下は着手前の現状調査と合意した改善計画。合意範囲の実装と自動検証を実施済み。結果・測定条件・未確認事項は [RFN01-24_検証結果.md](RFN01-24_検証結果.md) を参照。ユーザーAcceptは未実施。

## 1. 依頼された目的と範囲

- Workspace間で同じフォルダの索引を共有し、親子フォルダの重複走査を整理する。
- 背景で複数スレッドを使って走査する。途中終了してもキャッシュを壊さない。
- 開いた文書の最新状態を優先し、背景走査の完了待ちや古い結果による巻き戻りを防ぐ。
- リンク作成は現在の相対パス表記を維持する。読み取り時、索引内の `.md` ファイルに対して拡張子省略を補う。
- 画像は相対パスに加え、索引による名前・パスの解決を可能にする。
- 走査の時間・内訳・処理件数を測定し、改善前後を比較できるようにする。

### 索引対象の合意（2026-09-20追記）

- 索引対象は `.md` とエディタが表示に対応する画像ファイルに限定する。
- `.md` はファイル情報と見出し、画像はファイル情報のみを保持する。索引作成時に画像をデコードしない。
- `.log`・`.txt` は単独で開く用途を維持するが、索引によるリンク候補・名前解決の対象にはしない。他の対象外形式も索引へ登録しない。
- 明示された相対パス・絶対パスを開く既存動作は維持する。フォルダ表示・全文検索・単独ファイル編集の対象を、この索引制限で変更しない。
- 対象を探すためのディレクトリ列挙は必要。列挙件数と実際の索引登録件数を分けて測定する。

保存方式、原稿・退避本文の管理、Workspaceの選択規則は維持する。画像拡張子の推測、ノート埋込み、新形式の画像デコード、外部画像の取得は含めない。新規の設定画面・候補選択画面は提案していない。

## 2. 調査の根拠と限界

- ローカルHEAD: `bf666f5581faa71899cb56ff5cf1f094d6bc03b5`。未コミット変更を含む現在の作業ツリーを閲覧した。特に `pictures.rs`、`diag.rs`、`file_io.rs`、`wiring.rs` などに既存変更があり、実装時は再確認して保持する。
- Linear RFN01-24の本文とコメントを読み直した。現在はDiscuss。背景走査、並列化、途中終了の安全性、開いたファイルの優先反映が記載されている。
- Workspace設計.md、Workspace実装状況.mdと、以下の実装を照合した。
- この調査ではソース変更・テスト実行・実GUI再現・性能測定を行っていない。画像症状の説明はコード上の根拠であり、ユーザーの原稿による再現確認は実装前の検証に含める。所要時間や高速化率の数値は未取得。

## 3. 現在の構造と不足

| 領域 | 接続点 | 現状と不足 |
|---|---|---|
| 索引データ | workspace_index.rs: Entry / Snapshot | ファイルパス、root、相対パス、更新日時・サイズ、見出し、解析完了状態。ファイル一覧に画像も含まれる。root所属がEntryに埋め込まれ、共有情報とWorkspace表示情報が混在する |
| 背景走査 | Indexer / run_scan | 1本の常駐workerが走査・削除を直列処理。キャッシュ読込→ファイル列挙→変更文書の見出し解析→キャッシュ保存。ファイル情報を先に分割通知するが、見出し解析は列挙後 |
| 制限 | ScanOptions / cache読込 | 10万件、128件単位通知、見出し解析2MiB、キャッシュ1ファイル32MiB。`.git`、`target`、リンク・reparse pointを除外。制限・読取失敗を正常な完全走査と区別する |
| 重複 | dedupe_roots | 同一走査内は親rootを残して子rootを除く。同じファイルのWorkspace間共有はない |
| 永続化 | cache_dir_for / write_cache | AppDataの `workspace-index/<Workspace ID>/` にrootごとのキャッシュ。原子的置換を利用。AppData不可時はメモリのみ |
| 更新・切替 | WorkspaceLinks / main.rs: workspace_links_tick | 前回走査終了後、開始から30秒以上なら再走査。切替時はスナップショットを破棄して再走査。世代番号で古い通知を拒否 |
| リセット・整理 | clear_cache / workspace_links_clear_all_caches | Workspace専用キャッシュを消す前提。共有化時は通常リセット、Workspace解除、全キャッシュ整理の責務を分ける必要がある |
| 開いた文書 | main.rs: dirty_headings / 補完生成 | 編集済み文書の見出しを既存Entryに上書き。未索引の文書を追加せず、保存後はこの上書き対象から外れる。クリックは索引を直接参照する |
| リンク解決 | workspace_links.rs: resolve_link | 裸のWiki名は完全なファイル名で索引検索。明示パスとMarkdownリンクは元文書から解決。`.md` 補完なし |
| 見出し補完 | link_completion.rs: heading_candidates | 生成する相対パスとの比較が別実装。クリックの名前解決と一本化されていない |
| リンク切れ | ValidityChecker / validation_roots | 別workerで確認。Workspace全体の正常走査終了を条件とする。現在の本文を使う箇所はあるが、局所的に確認済みでも全体待ちになる |
| 画像表示 | pictures.rs: resolve / main.rs: PreviewSlot::size_images | 索引を使わず文書フォルダと結合。画像が索引にあるだけでは表示されない。本文等が変わらない場合の画像再評価に索引世代が入っていない |
| 画像の印刷 | print_view.rs: lay_outの画像読込箇所 | 画面とは別にpictures::resolveを呼ぶ。共通の解決結果を渡す必要がある |
| 構文 | document.rs: image_of_line / link_target_ranges | 画像単独行のMarkdown/Wikiと幅指定に対応。通常リンクの列挙は画像を除外する |
| 改名・移動 | link_rewrite.rs / link_move.rs | 通常リンクはresolve_linkを利用。画像は書換え対象外。移動後に作る移動前Entryはroot/relativeが一般索引と異なるため、root基準解決を追加する際に修正が必要 |
| 診断 | diag.rs / main.rs: PerfLog | ローテーションとperfモードあり。索引処理には本番用の工程別計測がない |

## 4. 改善方針案

### 4.1 共有データとWorkspaceの見せ方を分ける

- 索引専用の対象形式判定を共通化し、走査、旧キャッシュ読込、開いた文書からの追加、リンク候補に適用する。既存の全文検索用 `is_searchable` を狭めない。旧キャッシュの対象外Entryも除外し、件数上限は対象形式のEntryに適用する。
- ファイルの実体情報は正規化パスをキーとして共有する。Workspaceは登録rootから見える範囲を切り出す。別Workspaceのファイルを候補に漏らさない。
- root/relativeは共有ファイル自体の属性から切り離し、表示・リンク解決用のビューで付ける。親と子のどちらで発見したかによってリンク先が変わらないようにする。
- ディレクトリ単位の走査済み範囲と状態を管理し、重なる要求をまとめる。親の結果から子の範囲を再利用し、子から親へ広げるときは未確認部分を走査する。単にWorkspace名を外したキャッシュだけでは親子再利用は解決しない。**列挙キャッシュの寿命（2026-09-21合意）**：スコープが変わったときは重複再利用のため保持し、同じスコープの再走査では破棄する（再走査は前回開始から30秒後で、保持しても寿命の終わりに近いため）。この非対称は意図であり、比較の向きを反転させない。
- キャッシュの互換条件にスキーマ・解析方式・除外規則を含める。親側で除外された範囲を子側の完全走査と誤認しない。
- キャッシュがあることと、現在のディスク確認が済んだことを分ける。候補は先に出せるが、不存在・一意性の断定には対象範囲の確認状況を使う。

### 4.2 開いている文書を優先する

- **合意した追加要件（2026-09-20）**：全体走査を並列化し、それとは別の優先処理スレッドで、既に開いているファイル、新しく開いたファイル、その本文に含まれるリンク先を優先更新する。全体走査のキューが空くまで待たせない。
- 優先処理にはUIから文書改訂付きの本文スナップショットを渡し、未保存本文の見出しとリンクを解析する。リンク先が開いていればその最新本文を優先し、未オープンならファイル情報とmd見出しを更新する。画像はファイル情報と解決先を更新し、索引処理ではデコードしない。
- 実装案：新規オープン・編集・保存・外部更新・Workspace切替で優先要求を発行する。同じ文書の古い要求を最新改訂へ集約し、全体走査と同じ対象ファイルを処理する場合も旧結果で巻き戻さない。索引対象形式とWorkspaceの検索範囲は維持する。
- 優先対象は開いた文書から直接参照されるリンク先とする案。リンク先のリンクを無制限に再帰追跡しない。明示パスは直接確認し、未解決の裸名は共有の名前一覧更新に追従して再解決する。不完全な一覧から一意性を推測せず、優先要求ごとに独立した全走査を増やさない。
- 共通の参照ビューを「共有ディスク索引＋開いている文書の最新情報」から作り、補完・クリック・見出し・リンク判定に渡す。
- dirtyだけを条件にしない。開く・編集・保存・Save As・改名・閉じる・外部変更読込を反映し、未走査の文書も保存先が確定していれば追加できるようにする。
- 文書改訂とディスク確認世代を分け、遅い走査結果が現在の本文を上書きしない。閉じた直後も保存済みの新情報を保持し、古いキャッシュへ戻さない。破棄時は未保存見出しを取り除く。
- 名前のない新規文書は自文書の見出しのみ利用し、架空のファイルパスを共有索引に作らない。
- 確認済みの文書・明示パスは全体走査を待たず利用する。一方、不完全な名前一覧で同名候補が一つ見えただけでは一意と断定しない。「未確認」と「見つからない」を区別する。

### 4.3 並列走査と寿命

- 管理役1つが結果の統合・世代・キャッシュ書込を管理し、上限付きworker群がディレクトリ列挙・見出し解析を担当する。単一の大きなrootにも並列化が効く単位で分割する。
- 全体走査worker群とは別に優先処理用スレッドを確保する。全体走査の全workerが使用中でも優先処理が開始できる構成とする。worker数・I/O上限は優先スレッド分を含めて測定し、全体走査の進捗も維持する。
- キュー、同時読込量、結果通知量、共有キャッシュ総量に上限を設ける。開いた文書に関係する処理を優先し、全走査も進むようにする。
- 切替では新Workspaceと重なる処理を再利用し、不要な処理を中止する。非選択Workspaceを新たに全走査する機能は追加しない。
- リセットは対象範囲の再確認・再解析を要求する。他Workspaceが参照中の正常な索引は、新結果を置き換えるまで維持する案を推奨する。
- Workspace解除は参照を外す。どのWorkspaceも使わず実行中ジョブも参照しないキャッシュだけを整理する。全キャッシュ整理は別操作として維持する。
- workerはキャッシュを書かず管理役が直列保存する。中止世代からの保存を防ぎ、キャッシュ本体の原子的置換と複数ファイルにまたがる世代確定を設計する。同時アプリ起動の書込競合も検証対象とする。
- 旧キャッシュは検証済みのものを暫定データとして読む案。重複・矛盾は再走査で確定し、新形式の保存成功前に旧形式を削除しない。破損時は再構築する。

### 4.4 共通のリンク解決

候補列挙・パス比較はI/Oなしの共通処理とし、実在確認が必要な処理は背景処理または既存の開く処理で行う。描画のたびにWorkspace全体を検索しない。

優先順の提案：

1. 元文書からの既存相対パス・絶対パスを優先する。明示された `./`、`../`、絶対パスは別の場所へ勝手に振り替えない。
2. 拡張子省略の文書リンクは、その場所に対応する `.md` を索引で探す。完全一致の拡張子なし実ファイルがある場合はそちらを優先する。
3. 裸のWiki名と画像ファイル名は、現在のWorkspaceの範囲で完全名を探す。文書リンクだけ `.md` 補完を行う。
4. `設定/人物` や `画像/風景.png` のようなroot基準候補は、登録rootからの相対パスで照合する。相対パスで見つからなかった場合の補助とする。複数rootで一致したら曖昧とする。
5. 不明・曖昧・未確認・解決済みを呼出側へ返す。通常Markdownの裸名をWorkspace全体から探すかは下記の未決事項。

- 見出し指定・別名・幅指定と、パーセント符号を保持する。デコードは一度だけ行う。
- 画像では `.png` 等の拡張子を必要とし、現在対応する形式と画像単独行の範囲を維持する。
- 画面・印刷に同じ画像解決結果を渡す。Workspace切替、索引更新、画像の追加・改名で解決先とレイアウトを再評価し、本文を編集しなくても表示が更新されるようにする。無関係な索引更新で全画像を再デコードしない。
- 通常文書リンクの改名追従は新しい解決規則に合わせる。画像の自動書換えは現状対象外なので、今回含めるか別途確定する。自動保存対象のみという既存の書換え条件は維持する。

### 4.5 測定

- 計測を構造変更より先に導入し、現行の1worker方式で基準値を取得する。
- 記録：要求から完了までの経過時間、キュー待ち、キャッシュ読込、列挙・変更確認、見出し読込・解析、キャッシュ保存、最初の候補公開、UIへの反映待ち。
- 処理量：ディレクトリ数、確認ファイル数、新規・変更・削除・再利用・再解析数、読込バイト数、失敗・上限除外数、worker数。
- 並列時は全体経過時間とworkerの累積処理時間を別項目にする。並行する工程の時間を単純合算して全体時間としない。
- 開いた文書の改訂から、補完・リンク・画像に反映されるまでの時間を別に測る。
- 優先要求の待ち時間、処理時間、リンク先更新・公開までの時間を全体走査と分けて記録する。全体走査workerが稼働中にファイルを開くケースで、優先処理が独立して開始・公開されることと全体時間への影響を測る。
- 正常、部分失敗、中止、エラーを記録する。古い世代を捨てても計測要約は回収できるようにする。強制終了で終端ログがない実行は未完了と分類し、ゼロ秒や成功にしない。
- 計測イベントを既存のDiag/Perfへ渡す。通常は走査単位の要約、診断時は工程内訳を残す案。本文・ファイル名・フルパスは記録せず実行内IDと件数を使う。ログ失敗で編集や走査を止めない。
- 比較条件：同一PC・release・同一データセットで、初回、キャッシュあり変更なし、一部変更、同一rootのWorkspace切替、親子切替、中断・再開。OSキャッシュ状態と索引キャッシュ状態を区別し、各条件を複数回測り中央値と範囲を示す。worker数1/2/4を比較し、既定値は結果で決める。
- 目標秒数・高速化率は基準値取得後に設定する。大量ファイル、少数の大文書、画像混在を分け、編集応答性とメモリ増加も確認する。

## 5. 実施順序と成果物

| 段階 | 作業 | 確認・成果物 |
|---|---|---|
| 0：今回 | 現行コードとIssueの調査、方針整理 | 本文書。実装・実測は未実施 |
| 1 | 下記の仕様判断を確定し、再現用の小さいWorkspaceを用意 | リンク解決優先順、キャッシュ寿命、再現操作と期待結果 |
| 2 | 現行構造への計測導入と基準値取得 | 計測単位のテスト、条件別の時間・件数表 |
| 3 | 共通の解決処理と開いた文書の参照ビューを導入 | md省略・画像・補完・クリック・保存直後の整合性テスト |
| 4 | 共有索引・対象範囲・新キャッシュを導入。まず1workerで検証 | Workspace間共有、親子、切替、リセット、解除、移行のテスト |
| 5 | 上限付き並列処理と優先処理を導入 | 旧結果混入、中止、書込競合、負荷と時間の比較 |
| 6 | 画面・印刷・改名追従との統合、全体確認 | 全体テスト、一つの確認用ビルド、実画面確認、短いAccept手順 |

実装はCodexが担当する。関連する変更を一つの作業単位として扱い、途中の技術検証と最後のユーザー確認を区別する。PRがある場合はAccept了承後にマージし、Issue Closeはユーザーが行う。

## 6. 完了条件と重点ケース

- 全体走査workerを意図的に待機させた状態でも、既存TAB・新規オープン文書と直接リンク先の優先更新が別スレッドで開始・公開される。未保存本文、連続編集、大量リンク、相互参照、重複リンク、途中Close、Workspace切替を検証し、古い要求の公開・無限追跡・キュー肥大化を防ぐ。
- mdと対応画像だけが索引・候補に入り、txt/log等は旧キャッシュや開いたTAB経由でも混入しない。拡張子の大小文字を確認する。画像の見出し解析・デコードを走査で行わず、対象外ファイルの単独編集と明示パスの既存動作を維持する。
- 同じrootを使うWorkspace間で索引を再利用し、親子の重複部分は同一世代に二重走査しない。別Workspaceの候補は表示しない。
- 列挙中・解析中・保存中・切替直後に中止しても原稿を変更せず、再起動後にキャッシュを利用または安全に再構築できる。
- 開く・編集・保存・Save As・閉じる／破棄・外部更新・改名の各直後に古い見出しへ戻らない。遅い走査を制御したテストで確認する。
- `[[人物]]`、`[[人物#経歴|表示名]]`、相対パスの拡張子省略、通常Markdown、同名md、拡張子なし実ファイル、ドット入りの名前、大小文字、日本語・空白・%・#を検証する。
- `![[風景.png]]`、`![[画像/風景.png|300]]`、Markdown画像、同名画像、索引なし→到着、Workspace切替、追加・改名、削除・読込失敗で正しい絵を表示するか未解決に留まる。別の画像へ誤解決しない。
- 明示相対パス、Workspace外への既存リンク、コード内文字列、URLを巻き込まない。画像の縦横表示・幅・印刷結果を確認する。
- キャッシュ破損・書込失敗・容量上限・root不在・権限エラー・ジャンクション・除外root・同時アプリ起動を確認する。未走査と不存在を混同しない。
- リンク書換えは一意に確定したもののみで、未保存文書・Auto Save条件を維持する。
- 基準値と変更後を同一条件で比較でき、計測ログから本文・ファイル名が漏れない。性能値だけで機能完了としない。

## 7. 実装前に確定する判断

ユーザーからの目的は1節。以下はCodexの提案であり、まだ採用済みではない。

1. **リンク優先順**：既存相対パスを優先し、不成立時だけ索引で補う。`./`・`../`は別rootへ振り替えない。通常Markdownの裸名も全rootを探すか、相対パス＋md補完に留めるか。推奨は後者で、画像は索引検索を含める。
2. **拡張子省略の定義**：`人物.v2` → `人物.v2.md` のようなドット入りノート名も末尾`.md`省略として扱うか。推奨は完全一致優先で対応し、画像や既知の別形式をmdへ誤補完しない条件を固定する。
3. **画像の改名追従**：今回の表示解決に加えて原稿内の画像リンク書換えも含めるか。推奨は今回の必須範囲を表示解決と更新反映に限定する。
4. **共有索引のリセット**：共有データを直ちに削除するのでなく、現在利用中の情報を維持して再構築・置換する案。
5. **計測の提示先**：既存ログに記録し調査結果を会話・文書で示す案。常時表示や新画面は現時点では含めない。

これらを確定後、具体的なAPI・データ形式と変更ファイルを各段階の実装前に固定する。性能については測定結果を受けて並列数と容量上限を調整する。
