# Workspaceとリンク補完（RFN01-11 / RFN01-5）

2026-09-18の初期設計記録。初期版の実装・検証結果はWorkspace実装状況.mdを参照。2026-09-19のレビューで画面・TAB切替・範囲制限を見直したため、以下と食い違う点は[Workspaceレビュー対応](Workspaceレビュー対応.md)を優先する。

## 合意した仕様

- 複数のWorkspaceを管理し、各Workspaceに離れた複数フォルダを登録できる。原稿フォルダへ独自データを作らず、設定・状態・索引はAppDataに保存する。
- 同じフォルダを複数Workspaceから参照できる。保存方式はフォルダ単位の共通設定とし、Workspaceを切り替えても変わらない。
- 保存方式は既定が自動退避。明示的に指定したフォルダ配下の保存先がある文書だけ元ファイルへ自動保存する。新規の未保存文書・未登録領域は従来の自動退避。ファイル単独で開いた場合も適用する。
- 文書に実際の保存方式を表示する。入力が止まった時に自動保存し、外部変更や書込失敗時は上書きを止め、本文を退避して通知する。
- デフォルトWorkspaceを一つ設定できる。通常起動・ファイル単独起動はデフォルトを使い、所属Workspaceの選択を求めない。未設定時はWorkspaceなし。起動後は手動で切り替えたWorkspaceをリンクの範囲に使う。
- 左側のWorkspace名から切り替えられる。切り替えで既存TAB・未保存本文を閉じない。
- 管理画面に作成・複製・名前変更・フォルダ追加／取り外し／並べ替え・デフォルト指定・状態のリセット・登録解除を用意する。
- リセットは表示状態を初期化し、索引を再構築する。フォルダ構成・保存方式は維持する。登録解除はWorkspace専用データを削除するが原稿・退避本文は削除しない。デフォルト解除時は未設定に戻す。
- フォルダ設定には利用中のWorkspaceを表示する。未使用設定を整理でき、設定解除後は自動退避に戻る。見つからないフォルダはその状態を示し、場所変更・取り外しで整理できる。
- リンク補完・解決は現在のWorkspaceの全登録フォルダが対象。同名ファイルはフォルダとパスで区別する。
- `[[` でファイル候補、ファイル指定後の `#` で見出し候補を表示する。通常のMarkdownリンクのリンク先でも補完する。入力で絞り込み、上下で選択、Enterで確定、Escで閉じる。
- **画像の記法の中では画像だけを候補にする**（2026-09-21、RFN01-48）。`![[` と `![説明](` は画像の拡張子（`workspace_index::is_image`）のファイルだけを出し、`![[画像.png#` のような見出しの問いには何も出さない。普通のリンクは画像も候補のまま（`[[絵.png]]`は開く先として使える）。
- 保存済み索引を使って先に表示し、全走査を待たず編集を開始できる。初回は取得済み候補を順次利用する。「索引更新中」と候補なしを区別する。
- 表示対象と開く文書を優先し、別スレッドで全登録フォルダを走査する。ファイル索引を先に更新し、変更された文書の見出しを取得する。開いている文書は未保存本文を優先する。
- 更新結果はまとめて画面へ渡す。切り替え・リセット時は古い走査を中止し、遅れて届いた結果を適用しない。リンクを開く際に存在を確認する。

## Codexによる影響範囲調査

調査対象：`c0970f1` のソース（PR #1でmainへマージ済み）。運用ルールの未コミット変更は保持する。

| 現在の接続点 | 今回必要な対応 |
|---|---|
| `src/app_data.rs` の `Session.folder` / `expanded` / `search_folder` | 旧セッション読込を保持し、Workspace台帳・共通フォルダ設定・専用状態を別管理。退避領域と削除責務を分離する |
| `src/main.rs` の `Live`、`open_work_folder`、`publish_tree`、フォルダ検索 | 単一root前提を複数root表示に接続。新規作成・移動・検索・現在文書を表示する処理も対象rootを明示する |
| `src/file_tree.rs` の `rows` / `read_folder` | rootごとの既存行生成を再利用。通常表示で全階層を同期走査しない |
| `src/main.rs` の起動処理 / `open_startup_paths` | TAB復元、デフォルト選択、明示的フォルダ引数の順序を整理。相対パスの起動元基準を保持 |
| `src/saving.rs` の `write_work_copy_if_due` / `save_document` / `write_document_in` | 既存の自動退避と元ファイル保存を区別。自動保存は文書を明示指定し、フォーカス中の文書を誤って保存しない |
| `src/saving.rs` の `check_external_change` / `restore_tabs`、`src/writer.rs` | 外部変更・復元・退避削除と保存完了の順序を維持。古い保存結果で新しい編集を保存済みにしない |
| `src/document.rs` の `link_target_at` / `link_path` / 見出し解析 | 構文解析からI/Oを分離したまま、索引による名前解決と見出し位置への移動を追加 |
| `src/main.rs` の `open_link_at`、`src/link_ui_tests.rs` | 既存TAB再利用・未保存本文保持を維持し、曖昧な候補を勝手に選ばない |
| `ui/app-window.slint` / `ui/editor-pane.slint`、`src/wiring.rs` | 管理画面と候補UI、キー処理、IME・Undo・読み取り専用・縦横両表示との接続 |

## 実装依頼の分割と順序

Codexが接続先を確認してから、以下の単位でSonnetへ依頼する。全体調査や仕様決定は委任しない。各単位の変更許可ファイルとインターフェースを固定して渡し、範囲外が必要なら先に報告させる。

1. **台帳・保存方式判定**：新規 `src/workspace.rs` と永続化テスト。Workspace ID、フォルダ参照、デフォルト、設定の読書き、文書パスから保存方式の判定。管理操作はまず純粋なデータ処理として用意する。元ファイル書込・画面・索引走査は対象外。
2. **索引・補完候補モデル**：新規 `src/workspace_index.rs` / `src/link_completion.rs` と単体テスト。キャンセル可能な走査、キャッシュ、世代番号、候補の識別、ファイル／見出しの絞り込み。Slintや原稿への書込は対象外。
3. **管理画面・起動・ツリーの接続**：`main.rs` / `wiring.rs` / `app_data.rs` / `file_tree.rs` / `ui/app-window.slint` とUIテスト。台帳の操作を管理画面に接続し、複数rootと起動順序、整理操作を確認する。保存の意味は変更しない。
4. **文書別の自動保存**：`saving.rs` / 必要最小限の `writer.rs`・文書状態・タイマー接続と回帰テスト。台帳の判定を使い、安全な書込・失敗時退避・表示へ接続。手動保存と復元の動作を維持する。
5. **リンク操作の接続**：候補UI、編集入力とキー、`open_link_at`、見出し移動、未保存本文の索引上書き。IME・Undo・読み取り専用・両ペインの回帰テストを含める。
6. **統合確認**：Codexが差分と周辺機能を確認。gpt-ossに境界条件と結果分析を依頼し、実際のテストはCodexが実行する。最後に一つの確認用ビルドとIssue別のAccept手順を用意する。

## 完了条件

- 複数Workspaceと複数rootの登録・複製・編集・並べ替え・解除・再起動で整合する。デフォルトなし／削除済み／見つからないrootでも原稿を失わない。
- 同じフォルダを参照するWorkspace間で保存方式が一致し、単独起動・切り替え・Save As後も保存先で判定する。領域外と新規文書には自動上書きしない。
- 自動保存と外部変更、読み取り専用、書込失敗、終了、復元、保存中の追記が競合しても新しい本文を失わない。成功前にdirtyを落とさない。
- 索引なし／古い索引／破損／追加・削除・改名／読込失敗／大きい文書／切替中の結果到着を検証する。遅い走査をテストで制御し、完了前に編集・取得済み補完が利用可能であることを確認する。
- 日本語・空白・同名・大小文字差・親子フォルダ・リンク先見出し変更を検証する。同名候補が混ざらず、既存リンクとTAB再利用を維持する。
- 補完の確定・取消・Undo・IME・読み取り専用・縦横表示・別ペインを確認する。Escで本文を変えない。
- リセットと削除はアプリ管理の対象データだけに限定し、原稿・未保存退避を削除しない。他Workspaceの共有設定も失わない。
- 全体テスト、確認用ビルド、実画面確認の結果を個別に記録。ユーザーのAccept了承後にまとめてPRをマージする。

## 実装前に固定する技術上の判断

gpt-ossのテスト観点レビューを実施済み（`20260918T061527Z-e9b0d129`）。共有フォルダの保存方式やデフォルト起動に反する前提は不採用。外部改名の直後に保存タイマーが発火するケース、索引走査中のCtrl+S、候補表示中の状態変化をテストに含める。退避はAppDataのみ、Ctrl+Sは元ファイル保存、Workspace未設定は所属から自動選択しない、という合意を維持する。

以下は合意済みの操作を実現するための設計検討であり、追加のユーザー仕様として確定したものではない。

- 親子の登録フォルダで保存方式が異なる場合の優先順位、Windowsの大文字小文字・別名・ジャンクションの扱い。
- デフォルトWorkspaceと明示的フォルダ引数の関係、Workspaceなし起動で旧セッションのフォルダを復元する範囲。
- 複数rootで生成するリンクの表記、同名見出しの識別、未保存文書からリンクを作る場合の表記。
- 自動保存の待機時間・保存中の編集の世代管理・自動退避Offとの関係。元ファイル保存への切替だけで既存のdirty本文を直ちに上書きしないかを含めて決める。
- 索引の対象拡張子・除外フォルダ・サイズ／件数上限・キャッシュ読込量・差分検出方式。UIスレッドの大量キャッシュ読込も起動待ちの原因にしない。

今回の対象外：RFN01-7の部品化、RFN01-8の起動高速化全般、RFN01-6の文書上限引上げ、RFN01-10のWSL起動対応、意味が未定義の任意ファイルステータス機能。

## 統合時の実装判断（2026-09-18）

- 保存方式は実在する保存先を正規化して判定し、親子の登録では最も深いフォルダを優先する。判定不能時は元ファイルを自動上書きしない。
- 明示的な起動フォルダは一時的な単一フォルダ表示とし、保存済みのデフォルトWorkspaceを書き換えない。
- 自動保存は入力停止2秒後を基準にする。設定を有効にした時点の未保存本文は直ちに上書きせず、以後の編集から開始する。外部変更等で停止中は、自動退避Offでも保護用退避を行う。
- 索引は一つの作業スレッドが世代付きの走査・キャッシュ消去を順に処理する。件数上限はキャッシュと新規候補を合わせて適用し、上限到達や読み取り失敗を完全な走査結果として扱わない。
- リンクは原則として元文書からの相対パスを使い、別ドライブ等では絶対パスとする。構文と衝突する文字はパーセント表記にする。同名見出しは実際の見出し文字列に出現順を添えた `#見出し#2` の表記で識別する（RFN Edit内の拡張）。見出し文字列に含まれる `#` は `%23` と区別し、移動時には現在の本文で位置を確認する。

これらは実装・検証の基準であり、実装済みや実操作確認済みを意味しない。
