# 挿入メニュー詳細案（RFN01-12／34）

**2026-09-20の範囲確定：今回は既存機能のメニュー化のみ。以下の新規ひな形・行注記・改ページ挿入は実装対象外として保持する。既存の箇条書き・番号付きリスト・振り直しだけを今回のInsertへ接続する。**

2026-09-20、ひな形挿入方式への改訂。仕様の検討資料であり、実装未着手。先のファイル／見出し／Web別の入力画面案を置き換える。

項目ごとの実行対象・接続先・検証条件は[メニュー操作接続仕様](メニュー操作接続仕様.md)第4節を参照。追加調査で確認した太字・斜体・取消線・コード・引用・タスクリスト・水平罫線等の未配置記法は同書第11節に列挙した。今回採用するかは未決であり、この一覧だけで全記法の採否が確定したとは扱わない。

## 合意した前提

- メニューを選ぶと本文へ記法のひな形を直接挿入し、次に文字を入れる場所へキャレットを移す。入力ダイアログ・プレビュー画面・確定ボタンを新設しない。
- ルビは選択ありなら選択文字を親文字にし、読みの空欄へ置く。選択なしなら親文字から入力する。
- 以下の `│` はキャレットを説明する印で、本文には入れない。ルビ開始の全角縦線 `｜` とは別。
- 合意済みはこの基本方式とルビの選択あり／なしの位置。その他の細部・追加記法はレビュー用提案。

## メニューの配置案

```text
Insert
  Link →
    Markdown Link
    Wiki Link
    Wiki Link with Alias
  Ruby
  ---
  Annotation →
    Left-side Annotation
    Ruby and Left-side Annotation
  Emphasis Marks → 傍点／ゴマ傍点／丸傍点／白丸傍点／二重丸傍点／×傍点
  Emphasis Lines → 傍線／二重傍線／波線／鎖線／破線
  Text Annotation → 縦中横／割り注／小さな文字／大きな文字
  ---
  Heading → H1 / H2 / H3 / H4 / H5 / H6 / 見出しを解除
  Paragraph Annotation →
    字下げ → 1字 / 2字 / 3字 / 4字 / 字下げを解除
    地付き → 地付き / 地から1字上げ / 2字上げ / 3字上げ / 4字上げ / 地付きを解除
  Page Break（改ページ）
  Bullet List → - / * / +
  Numbered List
  Renumber
```

画面を開かずその場で挿入するため項目名に `…` は付けない。リンク先がファイル・見出し・Webであってもひな形は同じなので、用途別メニューは作らない。Wiki Link with AliasとAnnotation以下の採用範囲は提案。

## リンク・ルビの挿入結果

選択ありの例は本文の「東京」を選択した場合。

| 操作 | 選択なし | 選択あり | 次に入力するもの |
| --- | --- | --- | --- |
| Markdown Link | `[│]()` | `[東京](│)` | なし：表示文字、あり：リンク先 |
| Wiki Link | `[[│]]` | `[[東京│]]` | リンク先の名前。選択文字をリンク先として扱う案 |
| Wiki Link with Alias | `[[│\|]]` | `[[│\|東京]]` | リンク先。選択文字は表示名として扱う案 |
| Ruby | `｜│《》` | `｜東京《│》` | なし：親文字、あり：読み（ユーザー確認済み） |

表内の `\|` はMarkdown表のエスケープ。実際のWiki記法には半角縦線1つだけを入れる。

- Wiki Linkは選択文字を括弧で囲むだけ。別のリンク先へつなぐ表示名として選択を使いたい場合はWiki Link with Aliasを使う案。
- Rubyの初期ひな形は親文字の範囲を明示する全角縦線形式に統一する案。半角縦線・縦線省略の読取り対応は維持するが、それらを選ぶ入力画面は作らない。
- Markdown Linkの未選択時は表示名から書き、リンク先欄へは通常のキャレット移動で移る。Tabで空欄を順送りする新しいスニペット機能は今回追加しない。
- Wiki Linkの選択あり時に末尾へ置くのは、続けて `#` や別名を入力できるようにするための提案。選択の向きによらず同じ結果にする。

## 既存の補完との接続

- `[[` 内でリンク先を入力し、`#` で見出しを指定する現在の補完を再利用する。
- メニューでひな形を入れた直後も、本文とキャレットから通常の補完更新処理を呼ぶ案。手入力のキーイベントを偽装しない。
- 閉じ括弧を先に置くため、候補確定で `]]` や `)` が重複しないことを確認する。既存補完には後ろの閉じ括弧を扱う処理がある。
- 別名付きWikiでは補完が半角縦線以降の表示名を壊さないことを実装時に確認する。今回、その条件を実操作確認したわけではない。
- 補完候補をEscで閉じても、既に入れたひな形は消さない。挿入を取り消す操作はUndo。
- リンク先の自動選択、URL判定、ファイル選択画面、パス方式の質問、ブラウザ起動をひな形挿入に伴わせない。選択文字の自動エンコード・書換えもしない。候補を選んだ場合の既存のパス生成は維持する。

## その他の本文注記も同じ方式にする案

### 一度だけ選択文字を囲む形式

| 操作 | 選択なし | 選択あり（「東京」） |
| --- | --- | --- |
| 傍点 | `《《│》》` | `《《東京》》│` |
| 縦中横 | `［＃縦中横］│［＃縦中横終わり］` | `［＃縦中横］東京［＃縦中横終わり］│` |
| 割り注 | `［＃割り注］│［＃割り注終わり］` | `［＃割り注］東京［＃割り注終わり］│` |
| 小さな文字 | `［＃小さな文字］│［＃小さな文字終わり］` | `［＃小さな文字］東京［＃小さな文字終わり］│` |
| 大きな文字 | `［＃大きな文字］│［＃大きな文字終わり］` | `［＃大きな文字］東京［＃大きな文字終わり］│` |

追加入力のない選択済み装飾は、生成した記法の後ろへキャレットを置く案。選択なしは空の中身から入力する。空のひな形がまだ表示用記法として解釈されなくても、挿入を拒否しない。

### 選択文字を本文と注記内で参照する形式

| 操作 | 選択あり（「東京」） | 選択なしの提案 |
| --- | --- | --- |
| 左側の注記 | `東京［＃「東京」の左に「│」の注記］` | 無効。本文を選択してから使う |
| 両側の注記 | `｜東京《│》［＃「東京」の左に「」の注記］` | 無効。本文を選択してから使う |
| 丸傍点等 | `東京［＃「東京」に丸傍点］│` | 無効。本文を選択してから使う |
| 傍線等 | `東京［＃「東京」に傍線］│` | 無効。本文を選択してから使う |

- 同じ親文字を2か所へ書く記法は、選択ありに限定する案。未選択用の入力ダイアログや、入力を別の空欄へ自動複製する機構を追加しない。
- 両側の注記は読みを先に入力し、左側の注記の空欄へは通常のキャレット移動で移る。自動ジャンプは含めない。
- 丸傍点等は「ゴマ傍点・丸傍点・白丸傍点・二重丸傍点・×傍点」に、傍線等は「傍線・二重傍線・波線・鎖線・破線」に置換して同じ形を使う。
- 通常の傍点は囲む形式 `《《…》》` とするため、未選択でも利用できる。
- これらは採用範囲を含め未確定。選択なしでも使いたい場合は、ひな形だけを置く方式を別案として検討し、入力画面の新設には戻らない。

## 行単位の操作：見出し・字下げ・地付き

選択がかかる行全体へ適用し、選択なしなら現在行を対象にする方向をユーザーが了承。字数候補や変更・解除の細部は以下の提案とする。

### 対象行

- 表示上の折返しではなく、原文の改行で区切った行が単位。行途中の一部分を選択しても行全体へ適用する。
- 複数行なら各行へ同じ指定を適用。選択末尾が次行の先頭ぴったりなら、その次行は含めない。逆向き選択でも同じ対象にする。
- 選択中の空行は維持する案。未選択の空行はひな形を挿入して、その記号の後ろへキャレットを置く。
- コード・表・注記専用行など未定義の構造が混ざる場合は、一部だけ変更せず理由を表示する案。初回は通常本文と同種の既存指定を対象にし、矩形選択は別途検討する。

### 見出し

| 操作 | 実行前 | 実行後 |
| --- | --- | --- |
| H2 | `第一章` | `## 第一章` |
| H3へ変更 | `## 第一章` | `### 第一章` |
| 見出しを解除 | `### 第一章` | `第一章` |
| 空行でH2 | `│` | `## │` |

H1〜H6を直接選び、同じレベルの再選択は変更なし。解除は独立項目。複数行なら各行を見出しにし、行を結合しない。既存の見出し開始記号だけを変更・除去し、本文中の`#`や手入力の空白を削らない。引用・箇条書き等との組合せは初回に自動変換しない。

### 字下げ

| 操作 | 実行前 | 実行後 |
| --- | --- | --- |
| 2字下げ | `本文` | `［＃2字下げ］本文` |
| 3字へ変更 | `［＃2字下げ］本文` | `［＃3字下げ］本文` |
| 字下げを解除 | `［＃3字下げ］本文` | `本文` |
| 空行で2字下げ | `│` | `［＃2字下げ］│` |

- 各行の頭へ注記を付ける。空白・Tabを増減する操作や、箇条書きの階層変更とは分ける。
- メニューは1〜4字を初期候補とする案。現行の解析は半角・全角の1〜2桁を受け付けるため、この候補数は解析上限ではない。
- 複数行でも開始・終了の注記行は追加せず、各行へ指定する。既存の行頭注記は値を置換し、重複させない。解除はその注記だけを取り除く。
- 既存の`［＃ここからN字下げ］`〜`［＃ここで字下げ終わり］`に囲まれた範囲の変更・解除は、範囲分割が必要になり得る。初回は対象外として理由を示す案。範囲外の注記を勝手に変更せず、解除できないのに成功扱いにしない。

### 地付き・地からの字上げ

| 操作 | 実行前 | 実行後 |
| --- | --- | --- |
| 地付き | `署名` | `［＃地付き］署名` |
| 地から2字上げ | `署名` | `［＃地から2字上げ］署名` |
| 地付きから変更 | `［＃地付き］署名` | `［＃地から2字上げ］署名` |
| 地付きを解除 | `［＃地から2字上げ］署名` | `署名` |
| 空行で地付き | `│` | `［＃地付き］│` |

- 地付き（行末から0字）と地から1〜4字上げを候補とする案。縦書きでは下側、横書きでは右側の行末へ寄せる既存表示を使う。
- 複数行でも各行の頭へ付ける。未対応の`［＃ここから地付き］`等の範囲構文は生成しない。
- 地付きと地からN字上げは同種指定として置換する。解除は注記だけを除去し、本文の空白で位置を調整しない。
- 字下げと地付きの重ね指定、見出し・引用・箇条書きとの混在は、現行解析で意図どおり効くことを確認するまで自動的に併用しない。異なる指定を黙って消して置き換えない。

### 選択・キャレット・Undo

- 選択ありなら変更後も対象本文の選択を維持し、続けて別レベル・字数を指定できるようにする案。追加・除去した記号の長さで両端を補正し、対象行がずれないようにする。
- 未選択なら本文内の相対的なキャレット位置を維持。空行だけは追加した記号の後ろへ置く。
- 複数行まとめてUndo一回。変更のない再指定は不要なUndo履歴を作らない。
- 対象行の指定が一律なら現在値に印、混在なら単一値に印を付けない。解除は同種指定があるとき有効。
- 文字範囲のひな形操作と異なり、行頭の同種指定は認識して変更・解除する。新しい入力画面は作らない。
- 根拠は`src/document.rs`の`heading_level`、`note_indent_head`、`note_indent_of`、`note_tail_head`、`note_count`。表示・解析は既存だが、これらのメニュー編集処理は未実装。

強調・脚注・画像・表は引き続き別候補。改ページは既存の対応記法なのに一覧から漏れていたため、以下に補う。

## 改ページ：独立した注記行

### ソースで確認した現行動作

- `［＃改ページ］` は既に対応している。`src/document.rs` の `line_style` は、前後の空白を除いた行全体がこの注記と一致すると `LineKind::PageBreak` にする。`本文［＃改ページ］` のような本文との同居は改ページにならない。
- 認識には `reading.ruby` が有効で、コードフェンス内でないことが必要。挿入に伴って解釈設定を勝手に変更しない。
- 画面は連続した紙面のまま、表示用の破線で区切りを示す。原文表示と注記を解釈した表示は区別する。根拠：`src/text_blocks.rs` の `LineOrnament::PageBreak` と `src/directwrite_render.rs` の描画処理。
- 印刷・印刷プレビューでは `src/directwrite_render/print.rs` の `page_ranges` がこの行でページを終え、後続を次ページへ送る。`page_tasks` は改ページの破線を印刷対象から除く。
- 縦書き・横書きのページ分割と、破線を印刷しない既存テストがある。今回確認したのはテストのソースであり、実行や実画面確認はしていない。
- `src/shortcuts.rs`、`src/main.rs`、`src/wiring.rs`、`ui` の探索では改ページ専用の挿入コマンドを確認できない。解析・表示・印刷への対応と、メニューからの挿入は別。

### メニュー案と未決事項

- `Insert → Page Break（改ページ）` を追加する。入力画面を開かないため `…` は付けない。見出し・字下げのように選択した各行へ適用する操作ではなく、独立した注記行を一つ挿入する案。
- 前の回答で示した「現在行／選択開始行の前へ置く」は、現行動作ではなく未合意の提案。独立行が必要という解析条件だけでは、挿入位置は決まらない。
- 行途中のキャレットについて「その位置で本文を前後の行へ分ける」か「現在行の前に置き本文を分割しない」かを確定する必要がある。選択ありの場合の位置、選択維持、実行後のキャレットも未決。選択文字列を注記で消してしまう共通の置換処理は使わない。
- 実装時は先頭・末尾・連続する改ページ、自然なページ境界との重なり、既存の改行形式、コードフェンス内、解釈OFFを確認する。現行ページ分割は注記行を含めて区切るため、空ページが生じないと未検証で断定しない。
- この追記は調査・設計の補正であり、挿入処理の実装着手ではない。

## 編集動作の共通ルール案

- メニューを開く前の対象文書・選択・キャレットを保持する。文字範囲を包むひな形操作は選択範囲を生成文字列で一度に置き換え、指定位置に選択なしのキャレットを置く。行単位操作と改ページは各節の規則に従う。
- メニュー項目を選ぶまでは本文を変えない。メニューを閉じたら元の対象へフォーカスを戻す。選んだ後は即時挿入なので確定・キャンセル画面はない。
- 挿入全体をUndo一回で戻せる単位にする。続けて入力する読みやリンク先とはUndoの区切りを設ける案。
- 絵文字・結合文字・日本語を含む選択でも文字境界を壊さない。逆向き選択、縦書き・横書き、Source・Live Previewで同じ原文を生成する。
- 文字範囲のひな形操作は初回は単一行の連続選択を対象とする案。行単位の見出し・字下げ・地付きと既存の箇条書きは複数行を対象にできる。矩形選択の扱いは別途確定する。
- 既存リンク・ルビを解析して編集・解除する機能は今回含めない。本文を自動変換・正規化しない。区切り記号を含む選択の扱いは記法ごとに確認するが、空ひな形に読みや宛先がないことをエラーにしない。
- IME変換中は確定前の文字列を包まない。無効にして先に通常の確定操作を行ってもらう案。新しいEnter処理は作らない。
- 読み取り専用・Terminal・設定・空TAB・検索入力欄では本文挿入不可。Editor Panelへの適用範囲は別途確定する。
- 同じ操作をメニューとショートカットから呼ぶ。新しい既定キーは自動的に割り当てない。
- 記法を解釈する設定がOFFでも勝手にONにしない。ひな形はそのまま原文へ挿入する。全体の文字・組版・背景設定をメニューへ追加しない。

## 調査済みの記法・対応範囲（挿入操作とは別）

- 通常Markdownリンク、Wikiリンク（別名あり／なし）、同じ文書・別文書の見出しリンクの解析・補完・移動処理がある。同名見出しは出現順指定がある。
- Web URLのリンク記法は解析できるが、文書内リンクを開く現行処理ではURLをファイルとして解決しない。ひな形挿入はブラウザ起動対応の追加ではない。
- ブロックIDリンク・参照形式リンク・HTMLリンクの専用移動対応は今回の探索では確認できない。画像・埋め込みは通常リンクと別扱い。
- 通常ルビは全角・半角縦線と漢字の省略形を解析する。左注記と通常ルビを組み合わせられる。HTML rubyは要件7.8で非採用、専用解析も確認できない。
- 傍点6種類・傍線5種類、縦中横・割り注・小さな文字・大きな文字の範囲注記を解析する。
- 読み取り・表示への対応と、メニューによるひな形挿入の実装済みは別。今回の改訂は設計のみ。

根拠：`src/document.rs`の`link_here`、`ruby_here`、`side_note_here`、`dots_note_here`、`RANGE_NOTES`。`src/link_completion.rs`の検出・候補生成・閉じ括弧の処理。`src/workspace_links.rs`のリンク解決。`src/main.rs`の`open_link_at`。`要件定義.md` 7.8。

## レビューで残る点

Wikiリンクの選択文字をリンク先とする案と別名付き項目の採用、通常ルビの標準形式、注記・傍点等の初回範囲、参照型注記の選択必須、複数行・矩形選択・区切り記号の扱い、Editor Panelへの適用。ファイル／見出し／Web別の画面、記法プレビュー、親文字・読み入力ダイアログは検討対象から外した。
