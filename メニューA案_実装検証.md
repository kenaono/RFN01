# メニューA案：実装・検証記録

2026-09-20。RFN01-34。タイトル行に開閉可能な File / Edit / Insert / Layout / View / Run / Help を設け、既存処理へ接続した。初回Acceptでの指摘を反映した修正版。ユーザーAccept・マージ・Issue Closeは別の段階とする。

## 実装範囲

- タイトルバー最左端のアプリアイコンとF10で分類を表示する。分類幅は48pxで横並び。ホバーで展開し、項目実行後は分類も閉じる。本文、TAB列、サイドバーの矩形は開閉で変えない。WindowsのネイティブPopupで項目と子メニューを選ぶ。
- New File / New Terminalを直接選ぶ。New Tabは＋専用。既存ショートカットID 18は予約として残し、割当・実行を無効化した。他のIDは変更していない。
- 対象の文書・TAB identity・Panel・Terminal session・入力欄を保持する。対象が消えた場合や別TABに変わった場合は実行しない。
- 入力欄のUndo/Redo/Cut/Copy/Paste/Select Allはその欄へ接続する。本文のPasteは共通EditorSurfaceの既存入力経路を利用する。
- 全体の文字・組版・背景設定は既存Settingsに置く。メニューの背景操作は既存のTAB／Pane操作へ接続する。行番号と空白・改行記号は既存の共有設定を切り替え、作用範囲を項目名に表示する。
- Runに保存版／Git最終Commit／ファイル比較と、既存のTerminal出力保存・ログ取り込み開始／停止を配置した。
- Insertは既存の箇条書き・番号付きリスト・番号振り直しを実行できる。新規挿入I01〜40も階層ごと表示し、末端を「未実装」として無効化する。Helpも使い方／Aboutを無効項目として表示する。新しい編集機能・Help画面は追加しない。
- R10（Panel側から親Terminalのログを新規Panelへ取り込む追加入口）は採用未決のため表示しない。

## 統合・自動検証

作業中にmainへ入ったEditorSurface共通化を取り込み、`237d00c`を基点に統合した。旧本文TextInputの複製を戻さず、ホストのメニュー入力・貼り付けアダプタを共通部品に追加した。開発ガイドラインにも境界を記録した。

- 修正版の全体：1,059成功、0失敗、24 ignored（明示実行用の試験を含む）。未実装挿入40項目を再帰的に調べ、すべて無効で実行IDがないことを確認する試験を追加。
- Terminal UI試験を `--include-ignored` で追加実行：17成功、0失敗。実ConPTYによるファイル出力・Panelログ・対象保持・キャンセル・検索・シェル切替・開始フォルダを含む。
- 新規試験：同じ文書を共有する別TABへの誤実行防止、閉じたPanelの親への誤実行防止、共有表示設定の書込先、Kill Ring有効判定の副作用防止、旧New TabキーIDの互換、タイトル領域のヒットテスト。
- 日本語カタログとRustメッセージ対の検証対象に新規メニュー／共通EditorSurfaceを追加した。
- 新規worktreeでUTF-8 BOM試験データがCRLFになり1件失敗した。Git原本のバイト列（LF）を復元して成功。試験の期待値・製品の改行処理は変更していない。
- ビルド時の既存dead-code警告と、共有target内の古いincrementalディレクトリへのアクセス警告は残る。ビルド自体は成功。

## 実画面検証

追加修正`d757dcc`：旧Menu位置の空白をなくすため、Workspace／Explorer／検索／履歴／OutlineのY座標だけを36px上へ移動。確認用exeでWorkspaceが最上段に並び、空白がなくなることを実画面確認した。ビルド成功。配置だけの変更のため全体テストの再実行は省略（直前の`6166df2`で1,059成功）。

修正版で確認：アプリアイコンから横並び7分類を表示、スクロール量0のポインタ移動によるクリックなしのホバー展開、リンク3種類の無効表示、Helpの無効表示、File→新規文書を選ぶと分類も閉じてタイトルへ戻ること、Popup中のアプリアイコン再クリックで全体を閉じること。以下は初回版での確認記録も含む。

既存ユーザー設定から分離したLOCALAPPDATAと、確認用実行ファイルのコピーを使用した。確認済みと未確認を分けて追記する。

- 確認済み：通常サイズでMenu開閉による本文位置の維持、New Fileへの直接接続、約650px幅への端リサイズ、日本語表示、クリックによるPopup表示、マウスを止めたまま左右キーで分類移動、メニューから全選択→選択行の箇条書き適用→Undo、行番号ON、未保存文書における保存版／Git比較の無効表示。
- 先行ビルドで確認済み：F10、Esc、選択後Copy、タイトル余白のダブルクリック最大化／復元、最大化ボタン、Snap候補表示。
- マウス操作で透明なFocusScopeが項目に重なる問題と、分類切替直後に動いていないマウス位置で分類が戻る問題を修正し、実画面で再確認した。
- 最終ソース`b1a2dd4`の確認用exeで、F10→先頭のFile→下矢印でPopup→Escでバー→下矢印で同じPopupを再表示、の一連を確認した。最終ソースの全体テストも1,058成功。
- 未確認：別DPIモニター間の移動と100/125/150/200%の全組合せ、Snapへの実際の配置、IME変換途中と透過・Drag & Dropの全組合せ、すべてのメニュー項目の実画面操作。

## Accept手順

1. タイトルバー最左端のアプリアイコンを押し、間隔を詰めた7分類が同じ行に並ぶこと。File等へホバーすると開き、新規文書などを選ぶと分類も消えて文書タイトルへ戻ること。本文は開閉で動かない。
2. File→New File、File→New Terminal、＋によるNew Tabを確認。
3. 二つの文書／Editor Panelで選択し、Edit→Copy／UndoとFile→Saveが選んだ対象へ作用することを確認。
4. Insertのリンク・ルビ・傍点・見出し・段落注記・改ページとHelpが存在し、未実装の末端が無効であること。既存リスト操作、LayoutのTAB／Pane背景、Viewの行番号／空白表示は実行できること。
5. Runの比較、主／下段Terminalの出力保存とログ開始／停止を確認。
6. 最大化／復元・最小化・リサイズ・Snap、未保存文書のClose／Exit取消を確認。

## 確認用ビルド

- 製品ソースの検証コミット：`d757dcc`。後続コミットは本検証記録のみ。
- 実行ファイル：`D:\Projects\10_Creation\50_Dev\10_Editor_menu\target\accept\basic-menus-d757dcc\editor_spike.exe`
- SHA-256：`F6CF5A57AB7D480CCA80D5A3E62613F255C692732E0EA6BA264F061DAC36998F`
- Debugビルド。実画面試験でもこのファイルを起動した。手動起動では通常の既存プロファイルを使う。試験時のみLOCALAPPDATAを分離した。
- ユーザーAccept待ち。PRのマージおよびIssue Closeは実施していない。
