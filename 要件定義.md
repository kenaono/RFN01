# Windows Editor 要件定義

## 1. 文書の位置づけ

本書は、`10_Editor`で開発するWindows向けEditorの初期要件を定義する。

このEditorは、一般的なテキスト編集機能に加え、既存のObsidian Vaultを壊さず編集できるMarkdown編集機能と、日本語の縦書きライブプレビュー編集を中核機能とする。

## 2. 対象環境と技術

- 対象OSはWindows 11のみとする。
- 実装にはRustとSlintを使用する。
- ローカル環境だけで動作し、アカウントやクラウド接続を必須としない。
- テレメトリーや利用状況の外部送信は行わない。
- 日本語IME、高DPI、Windowsの表示倍率に対応する。

### 2.1 実装上の責務分担

- Slintは、ウィンドウ、タブ、ファイルツリー、左右Split、ステータスバー、設定画面など、アプリ全体のUI基盤として使用する。
- Rustは、文書モデル、Markdown解析、検索、保存、復元、ファイル監視などを担当する。
- Slint標準部品だけでは要件を満たせないライブプレビュー編集と縦書き編集については、Rustによる独自編集コンポーネントを実装する。
- 独自編集コンポーネントでは、必要に応じてWindowsの文字描画・入力関連APIを利用する。
- 独自編集コンポーネントとSlint UIの境界を分離し、文書モデルを共有する。

### 2.2 本体実装前の技術検証

本体実装へ進む前に、小規模な検証実装で次を確認する。

1. 日本語縦書きの描画
2. 日本語IME入力と変換候補の表示位置
3. 縦書き時のカーソル移動と範囲選択
4. 縦書き文書の横方向スクロール
5. Markdownソース位置とライブプレビュー表示位置の対応

これらの成立性を確認した後、検証結果に基づいて編集コンポーネントの方式を確定する。

## 3. 製品の基本方針

- Markdownと文章編集を中心とする汎用テキストEditorとする。
- VS Codeのような開発用IDEやコードEditorは目指さない。
- Markdownテキストを唯一の原本とし、表示用の独自文書形式へ変換しない。
- 既存のObsidian資産について、表示の完全再現よりも、内容を壊さず編集・保存できることを優先する。
- アプリ固有の状態や表示設定をMarkdown本文やVault内へ書き込まない。

## 4. 編集対象

### 4.1 Markdownファイル

`.md`ファイルでは、ソース編集とライブプレビュー編集を利用できる。

### 4.2 その他のテキストファイル

- `.txt`などのテキストファイルは横書きのソース編集で扱う。
- プログラムコード向けの補完、デバッグ、高度なシンタックスハイライトは提供しない。
- 画像などのバイナリファイルは編集対象外とする。

## 5. Vaultとファイル管理

### 5.1 Vault

- Obsidian Vaultをフォルダ単位で開く。
- 初期版では、1つのウィンドウにつき1つのVaultを開く。
- 別のVaultを開く場合は現在のセッションを保存して切り替える。
- 最近使用したVaultの履歴を提供する。
- 複数Vaultを同じウィンドウ内へ混在させない。
- `.obsidian`フォルダは原則として参照・変更しない。
- Vault内にEditor独自の管理ファイルを作成しない。

### 5.2 ファイルツリー

- 左サイドバーにVaultのファイルツリーを表示する。
- ファイルツリーは表示・非表示を切り替えられる。
- Markdownファイル、その他のテキストファイル、添付ファイルを表示する。
- ファイルおよびフォルダの新規作成、名前変更、移動、複製に対応する。
- ファイルツリー内のドラッグ＆ドロップに対応する。
- ファイルをWindowsエクスプローラーで表示できる。
- 削除時は完全削除せず、Windowsのごみ箱へ移動する。
- ファイル名を変更しても、開いているタブの状態を維持する。

### 5.3 内部リンクの更新

- ノートの名前変更または移動時に、影響するMarkdownリンクとObsidian内部リンクを検出する。
- 更新対象となるファイル数を事前に表示する。
- リンク更新方法として「常に確認」「自動更新」「更新しない」を選択できる。
- 初期値は「常に確認」とする。
- 更新処理に失敗した場合、Vaultを途中状態のまま残さない。

## 6. 基本画面

- 左側にファイルツリーを配置する。
- 中央にタブ式の編集領域を配置する。
- 下部に文書情報を示すステータスバーを配置する。
- タブごとに対象ファイルと表示モードを管理する。

## 7. Markdownの編集モード

各Markdownタブは、次の3モードを切り替えられる。

1. 横書きソース編集
2. 横書きライブプレビュー編集
3. 縦書きライブプレビュー編集

モード切り替え時は、次の状態を可能な限り維持する。

- 編集内容
- Undo／Redo履歴
- 未保存状態
- カーソル位置
- スクロール位置

タブごとに最後に使用した表示モードを記憶する。

## 8. ライブプレビュー編集

### 8.1 基本動作

- 整形済みのMarkdown表示上で直接編集できる。
- 通常時はMarkdown記号を隠し、編集位置に応じて必要な記号を表示する。
- 未対応の記法はソース表現のまま表示し、保存時にも削除・変形しない。
- 通常クリックはカーソル移動に使用する。
- `Ctrl+クリック`で通常リンクまたは内部リンクを開く。
- タスクリストのチェックボックスをクリックすると、元の`[ ]`または`[x]`を更新する。

### 8.2 整形表示する要素

- H1～H6見出し
- 太字、斜体、取り消し線
- 箇条書き、番号付きリスト、タスクリスト
- 引用
- 水平線
- 通常リンクとObsidian内部リンク
- インラインコードとコードブロック
- 表
- 脚注
- Callout

### 8.3 画像とリッチコンテンツ

- `![[画像.png]]`や`![説明](画像.png)`などの記法を保持する。
- ライブプレビュー内に画像そのものは表示しない。
- 画像はファイル名またはリンクとして簡潔に表示する。
- 数式とMermaidは初期版では描画せず、記法を保持して編集可能にする。

## 9. Obsidian互換

- Obsidian標準のMarkdown記法、内部リンク、埋め込み記法、YAMLフロントマター、タグ、脚注、Calloutを保持する。
- `[[ノート名]]`および`[[ノート名|表示名]]`形式の内部リンクを扱う。
- 見出しおよびブロックへの内部リンクを扱う。
- プラグイン固有機能は実行・再現しない。
- Obsidianのテーマ、独自CSS、プラグインによる表示は再現しない。
- 未対応の記法が存在しても、保存時に内容を失わない。

## 10. 縦書き

- 縦書きはライブプレビュー編集で提供し、ソース編集は横書きとする。
- 文章は画面右上から開始する。
- 文字は上から下へ進み、列は右から左へ増える。
- 文書はページ分割せず、横方向へ連続してスクロールする。
- マウスホイール操作を横方向の文書移動へ割り当てる。
- 縦書きに対応したカーソル移動、範囲選択、日本語IME入力を提供する。
- 句読点と括弧を縦書き用の字形・位置で表示する。
- 全角英数字は、縦書きの各文字として正立表示する。
- 連続する半角英数字は横書きの文字列として組み、縦書き内で90度回転して表示する。
- 縦中横を適切に扱う。
- 表とコードブロックは、縦書き内でも横書きブロックとして表示する。
- 印刷用の用紙サイズや改ページは扱わない。

## 11. 左右Split

- 編集領域を左ペインと右ペインに分割できる。
- 上下Splitは初期版の対象外とする。
- 境界線をドラッグして左右の幅を変更できる。
- 各ペインは独立したタブを持つ。
- 左右で異なるファイルを開ける。
- 同じMarkdownファイルを左右両方へ表示できる。
- 同一ファイルをソース編集とライブプレビュー編集で同時表示できる。
- 横書きライブプレビューと縦書きライブプレビューを同時表示できる。
- 同じファイルへの編集内容は左右へ即時反映する。
- Undo／Redo履歴はファイル単位で共有する。
- カーソル位置とスクロール位置はペインごとに保持する。
- タブを左右のペイン間で移動できる。
- Splitの解除とペインの入れ替えに対応する。
- Split状態をセッション情報として復元する。

## 12. 検索とナビゲーション

- 現在のファイル内を検索・置換できる。
- Vault全体を全文検索できる。
- ファイル名からクイックオープンできる。
- Markdown見出しからアウトラインを自動生成する。
- アウトラインから該当セクションへ移動できる。
- 内部リンクからリンク先ノートへ移動できる。
- リンク先を反対側のSplitで開ける。
- 最近開いたファイルの履歴を提供する。

## 13. 保存、復元、外部変更

### 13.1 自動保存

- 既存ファイルは、入力停止から一定時間後に元のファイルへ自動保存する。
- `Ctrl+S`による即時保存にも対応する。
- 自動保存は設定で無効化できる。
- 保存処理は一時ファイルを利用し、失敗時に元ファイルを壊さない。
- UTF-8を基本とし、BOMや既存の改行コードを不用意に変更しない。

### 13.2 外部変更

- Obsidianなど別のアプリによるファイル変更を監視する。
- 未編集のファイルが外部変更された場合は、安全に再読み込みする。
- 編集中のファイルが外部変更された場合は、勝手に上書きせず通知する。

### 13.3 未保存バッファ

- 新規文書は、名前を付けるまでアプリ専用領域へ自動保存する。
- 複数の新規バッファを「無題1」「無題2」のように保持できる。
- アプリを終了しても、新規文書を破棄しない。
- 新規文書を正式に保存できた後、一時バッファを安全に削除する。
- 新規文書のタブを明示的に閉じる場合は、保存または破棄を確認する。

### 13.4 セッション復元

次回起動時に、以下を復元する。

- 最後に開いていたVault
- 開いていたタブとアクティブなタブ
- 左右Splitの状態と幅
- 編集内容と未保存バッファ
- カーソル位置、選択範囲、スクロール位置
- 各タブと各ペインの表示モード
- ファイルツリーの展開状態

## 14. 表示設定

- H1～H6のフォントサイズを個別に指定できる。
- 本文のフォントと文字サイズを指定できる。
- 見出しのフォントを指定できる。
- 横書き時の行間を指定できる。
- 縦書き時の文字送りを指定できる。
- 縦書き時の列間隔を指定できる。
- 文字色、背景色、編集領域の余白を指定できる。
- 設定変更を編集中の画面へ即時反映する。
- 設定を初期値へ戻せる。
- `Ctrl+マウスホイール`で、マウスカーソルがある編集ペインの表示を拡大・縮小できる。
- 拡大・縮小はソース編集、横書きライブプレビュー、縦書きライブプレビューのすべてに適用する。
- 拡大・縮小は表示倍率だけを変更し、Markdown本文や基準となるフォントサイズ設定は変更しない。
- `Ctrl+0`で対象ペインの表示倍率を100%へ戻せる。
- 各ペインの表示倍率をセッション情報として復元する。
- 初期版では表示設定をアプリ全体で共通とし、Vaultごとの設定は持たない。
- 表示設定はアプリ専用領域へ保存し、Markdown本文には書き込まない。

## 15. 行数と文字数

ステータスバーへ、次の情報をリアルタイムに表示する。

- 文書全体の論理行数
- Markdown記号を除いた本文文字数
- Markdown記号を含むソース文字数
- 選択範囲の文字数
- ソース編集時の現在行と桁位置
- 現在アクティブな編集ペインの表示倍率（例：`100%`、`125%`）

行数は画面上の折り返しや縦書きの表示列ではなく、ファイル内の改行を基準に数える。文字数は、日本語や絵文字を画面上の一文字として自然に数える。表示倍率は、`Ctrl+マウスホイール`または`Ctrl+0`による変更を即時反映する。

## 16. クイック下書き

### 16.1 目的

チャットなどへ送信する短い文章を、`Enter`による誤送信を気にせず推敲するための簡易編集機能を提供する。

### 16.2 ウィンドウ

- メインEditorとは独立した小型ウィンドウとして提供する。
- Vaultを開いていなくても利用できる。
- グローバルショートカットで表示・非表示を切り替えられる。
- 「常に手前に表示」を切り替えられる。
- ウィンドウの位置とサイズを記憶する。
- 横書きのプレーンテキスト編集とし、`Enter`は常に改行とする。
- Undo／Redo、切り取り、コピー、貼り付け、全選択に対応する。

### 16.3 Clipboard

- `Ctrl+C`は選択範囲をClipboardへコピーする。
- `Ctrl+Shift+C`は下書き全文をClipboardへコピーする。
- マウス操作用に「全文コピー」ボタンを提供する。
- コピー成功を非侵襲的な表示で通知する。
- コピー後も下書き内容を保持する。
- 必要に応じて「コピーして閉じる」操作を提供する。

### 16.4 保存と復元

- 現在の下書き1件をアプリ専用領域へ自動保存する。
- ウィンドウを閉じても下書きを破棄しない。
- 次回表示時に直前の内容と編集位置を復元する。
- 内容の削除は明示的な「クリア」操作で行う。
- 必要に応じて通常のMarkdown文書へ昇格して保存できる。
- 下書き履歴は初期版では提供しない。

## 17. 将来拡張

### 17.1 Terminal連携

- 別途開発予定のTerminalへ、クイック下書きの全文または選択範囲を直接送信できるようにする。
- Terminal側で選択した文章をクイック下書きへ取り込めるようにする。
- Clipboardを必須とせず、Windows上のローカルなアプリ間通信を利用できる構造にする。
- 複数Terminalが存在する場合は送信先を選択できるようにする。
- Terminalへ送信した内容は入力欄へ貼り付けるだけとし、自動実行しない。
- EditorとTerminalは独立して起動・更新できる構成とする。
- 初期設計から、クイック下書きの送信先をClipboard以外へ拡張できるよう責務を分離する。

### 17.2 その他の候補

- 文字色と背景色の組み合わせをEditor独自の表示テーマとして名前を付けて保存する機能
- 保存した表示テーマを編集画面から切り替える機能
- 表示テーマはObsidianテーマや独自CSSの再現とは分離し、アプリ専用領域で管理する
- Vaultごとの表示設定
- 上下Split
- 複数ウィンドウまたは複数Vaultの同時利用
- 縦書きの印刷プレビューや書き出し

## 18. 初期版の対象外

- Windows 10以前への対応
- プログラムコード向けのIDE機能
- Obsidianプラグインの実行
- Obsidianテーマや独自CSSの再現
- 画像のインライン表示
- 数式およびMermaidの描画
- `.canvas`の編集
- Obsidian Syncとの直接連携
- グラフ表示や高度なバックリンク分析
- 印刷、PDF、EPUB、Wordなどへの書き出し
- ページ単位の縦書きレイアウト
- クイック下書きの履歴管理
- Terminalとの実際の連携実装

## 19. 未決定事項

- アプリの正式名称
- クイック下書きを呼び出すグローバルショートカット
- 自動保存までの待機時間
- 初期フォント、配色、各見出しサイズの既定値
- 想定する最大ファイルサイズと性能目標
- 配布方式（インストーラーまたはポータブル版）
- ショートカットのカスタマイズ範囲
