# Windows Editor 要件定義

## 1. 文書の位置づけ

本書は、`10_Editor`で開発するWindows向けEditorの初期要件を定義する。

製品名は「RFN Edit」、開発コードは「RFN-01」とする。

このEditorは、Markdownと文章の編集機能、および日本語の縦書き編集を中核機能とする。

## 2. 対象環境と技術

- 対象OSはWindows 11のみとする。
- 実装にはRustとSlintを使用する。
- ローカル環境だけで動作し、アカウントやクラウド接続を必須としない。
- テレメトリーや利用状況の外部送信は行わない。
- 日本語IME、高DPI、Windowsの表示倍率に対応する。
- 時間のかかる処理がUI操作を妨げないよう、マルチスレッドでの実装を基本とする。

### 2.1 実装上の責務分担

- Slintは、ウィンドウ、タブ、ファイルツリー、編集ペイン、ステータスバー、設定画面など、アプリ全体のUI基盤として使用する。
- Rustは、文書モデル、Markdown解析、検索、保存、復元、ファイル監視などを担当する。
- Slint標準部品だけでは要件を満たせないライブプレビュー編集と縦書き編集については、Rustによる独自編集コンポーネントを実装する。
- 独自編集コンポーネントでは、必要に応じてWindowsの文字描画・入力関連APIを利用する。
- 独自編集コンポーネントとSlint UIの境界を分離し、文書モデルを共有する。

### 2.2 本体実装前の技術検証

本体実装へ進む前に、小規模な検証実装で次を確認する。

1. 日本語縦書きの描画
2. 日本語IME入力と変換候補の表示位置
3. 縦書き時のカーソル移動と範囲選択
4. 縦書き文書の横方向スクロール
5. Markdownソース位置とライブプレビュー表示位置の対応

これらの成立性を確認した後、検証結果に基づいて編集コンポーネントの方式を確定する。

### 2.3 ファイルサイズと性能方針

- 想定する最大ファイルサイズは3MBとし、文書の上限を100万文字とする。
- 上限を超えるファイルの読み込み、貼り付け、入力は受け取らず、その理由をステータスバーへ表示する。黙って破棄しない。
- 一段落が32,000文字を超える場合は、ステータスバーで知らせる。ただし自動で改行を挿入するなど、本文を書き換える処理は行わない。
- 具体的な性能目標は、主要機能の実装後に、測定対象となる操作、テストデータ、測定条件を定めたうえで決定する。
- 具体的な性能目標が未決定であることを、設計および実装開始を止める条件にしない。
- 実装中は、時間のかかる処理でUI操作を妨げないことと、後から性能を測定できる構造にすることを優先する。

#### 長い段落の入力応答（E17・2026-09-14）

- 折り返しのある長い段落は、変更前の確定済み組版を再利用し、表示範囲・入力位置の周辺を先に組む。画面外の続きの折り返しとブロック計測は、UIを待機させずにバックグラウンドで確定する。
- 未確定範囲は全体寸法の推定にだけ使い、文字の描画・選択・ヒットテストには使わない。確定結果へ切り替える際は表示中の文字の画面内位置を保つ。
- 連続入力では古い仕事を中止し、待機仕事と結果をそれぞれ最新1件に制限する。本文、書式、表示条件が一致する結果だけを反映し、Pane終了後や別の表示条件の結果を適用しない。
- 未確定範囲へのスクロールでは追加の限定範囲を先に計算する。大量貼り付け・大きな書式変更など、入力位置までの前方組版が未確定の場合もUIを長時間占有せず、確定した部分から表示する。未確定位置のカーソルは計算完了後に表示する。
- 保存対象は常に本文であり、組版完了を待たない。全体の正確な配置が必要な処理用に同期組版経路を保持する。折り返しなしの長大行と文書上限の引き上げは本項の高速化対象に含めない。
- 組版単体の時間と、入力処理・解析・画像化・画面描画を含む時間を区別して記録する。組版単体の測定値を実操作の表示遅延と呼ばない。

### 2.4 配布方針

- 初期の配布形式はMSIとする。
- **売り物として配るための道具を用意する**（2026-09-08追加）。どれも小さいが、
  **無いと「怪しいソフト」として扱われる**もので、機能ではなく信用の問題である。
  - **実行ファイルへのコード署名。**署名が無いとSmartScreenが警告を出し、
    初めての人はそこで止まる。**最初の5分で失うものが一番大きい。**
  - **更新の道。**直したものが書き手へ届かなければ、直したことにならない。
  - **試用期間。**縦書きの書き味は説明では伝わらない——**自分の原稿を入れて確かめて
    もらう**以外に伝える方法が無い。
  - **落ちたときの報告。**診断ログ（README）は既にあるので、それを送る道を作る。
- **アイコンは紫地にアイボリーの`RE`**（RFN Edit、2026-09-06追加）。タイトルバーと
  タスクバーはこれを出す。実行ファイル自体のアイコン（エクスプローラーで見えるもの）は
  Windowsのリソースを埋め込む必要があり、まだ入れていない。絵は同じものを使う。

## 3. 製品の基本方針

**この編集器は、縦書きで文章を書く人のための道具である**（2026-09-08決定）。中でも
**小説を書く人**を先頭に置く。それまでは「書き手ひとりのための道具」として作ってきたが、
売り物として育てる以上、**誰の何を助けるのかを先に決めておかないと、機能ごとに別の人を
想像することになる**。

- **迷ったときは縦書きで書く人に寄せる。**横書きも同格に扱う（7.2の4モード）が、
  どちらを取るか決められない設計上の選択が出たときは、縦書き側を通す。
- **軽く作業を進めるための道具であって、製品を仕上げるための道具ではない**
  （2026-09-08、書き手の指摘で書き直した）。DTPではない。**Markdownとはそういうもの**
  ——原稿を軽く書き、軽く直し、仕上げは専用の道具に任せる。
  - **だから、いちばん大事なのは「編集と校正が軽いこと」**である。新しい機能を足すか
    決めるとき、最初に訊くのは「これは書く手を軽くするか」で、「これで仕事が終わるか」
    ではない。
  - **納品の形式（EPUB・Word・入稿）は追わない。**印刷（PDFもそこから出る）だけは持つが、
    それは納品の道ではなく**自分の原稿を紙の形で確かめるため**である（7.10）。
  - この項目は**足すものより外すものを決める**ために書いてある。7.9・7.10のどちらも、
    この一行から出た。
- **端末（6.8）は「書きながら相談する場所」である**（2026-09-08、書き手の指摘で書き直した）。
  最初は「書き手自身が欲しくて入れた開発者向けの機能」と書いたが、それは見立てが浅かった。
  **作業フォルダが原稿のフォルダで、そこで開く端末は原稿を文脈に持っている**——AIと筋や
  設定の矛盾を相談しながら書く、という使い方に対して、下段の端末（6.8）と下書きから送る
  仕組み（11.7）は既にその形をしている。**執筆と相談を並べて置けること**は、この編集器を
  選ぶ理由になりうる。
  - ただし**端末そのものを売りにするのではない**。売りは「書く手が軽いこと」で、端末は
    その一つの現れである。初めて触る人へ最初に見せるものではない。
- **ペイン分割（6.4）は残すが、前に出さない。**書き手自身が欲しくて入れたもので、実際に
  使われている。要件から外す理由は無く、宣伝する理由も無い。
- Markdownと文章編集を中心とする汎用テキストEditorとする。
- VS Codeのような開発用IDEやコードEditorは目指さない。
- Markdownテキストを唯一の原本とし、表示用の独自文書形式へ変換しない。
- 未対応のMarkdown記法が含まれていても、内容を壊さず編集・保存できることを優先する。
- アプリ固有の状態や表示設定を、編集対象のファイルや作業フォルダ内へ書き込まない。

## 4. 編集対象

### 4.1 Markdownファイル

`.md`ファイルでは、ソース編集とライブプレビュー編集を利用できる。

### 4.2 その他のテキストファイル

- `.txt`などのテキストファイルは横書きのソース編集で扱う。
  - **開くときの話である**（2026-09-08、書き手の報告）。縦書きのペインで設定ファイルを
    開いたら設定ファイルまで縦書きになっていた——記法の無いテキストに整形表示は無く、
    `key: value`の並びを縦に組んでも読めない。**そのあと縦書きにするのは書き手の自由**で、
    縦書きで書く人は`.txt`の原稿も縦で読みたい（3）。
  - **Markdownかどうかは拡張子で判じる**（`.md` `.markdown` `.mdown`）。中身を見て決めると、
    `#`で始まる設定ファイルがMarkdownになり、見出しの無い原稿がそうでなくなる
    ——どちらも書き手には理由が見えない。
- プログラムコード向けの補完、デバッグ、高度なシンタックスハイライトは提供しない。
  - **Markdownの記法に色を付けることは、これに当たらない**（2026-09-08、
    [単語チェックモード要件](単語チェックモード要件.md) 8.5）。字句解析はブロック分割のために
    既にあり、外してあるのはその先の意味解析——補完・定義へ移動・診断——である。
    **字句解析があるからといって、意味解析へ進む理由にはならない。**
- 画像などのバイナリファイルは編集対象外とする。

### 4.3 文字コードと改行コード（2026-09-11追加、追加要件 E2 から転記）

- **読めるのは UTF-8（BOM有無）・UTF-16 LE／BE・CP932**（Windowsの日本語文書）。
  判別は「印（BOM）→UTF-8→CP932」の順で、**どれでも読めなければ断る**——文字化けした
  内容を確定しない。
- **判別が決まらないファイルは推定で開き、何で読んだかを画面に出す**（6.6）。
  **候補を並べて選ばせる画面は作らない**（2026-09-10、書き手が削った）。
- **「指定文字コードで開き直す」と「指定文字コードで保存する」は別の操作**として示す。
  開き直しは未保存の本文があれば確認を出す。
- **保存先の文字コードで表せない字は、黙って置き換えない。**断って、UTF-8で保存するよう
  言う——**数えない・替えない・訊かない**（2026-09-10、書き手の決定）。
- **改行コード（LF／CRLF）は読んだ形のまま保つ。**混在していればそう表示する。
  **変えるのは「名前を付けて保存」の欄**で、文字コードと同じ場所にある。

## 5. 作業フォルダとファイル管理

### 5.1 作業フォルダ

> 2026-09-19：RFN01-11 / RFN01-5の初期実装後、Workspaceの画面・TAB切替・ファイル範囲を見直した。現行の変更仕様は[Workspaceレビュー対応](Workspaceレビュー対応.md)。通常Explorerは単一フォルダ、Workspaceは複数フォルダを独立して管理する。ユーザーの再確認は未完了。

- 任意のフォルダを作業フォルダとして開く。
- 初期版では、1つのウィンドウにつき1つの作業フォルダを開く。
- 別の作業フォルダを開く場合は現在のセッションを保存して切り替える。
- 最近使用した作業フォルダの履歴を提供する。
- 複数の作業フォルダを同じウィンドウ内へ混在させない。
- 作業フォルダ内にEditor独自の管理ファイルを作成しない。

#### 起動引数（RFN01-9、2026-09-18）

- 引数のファイルはTABで開き、既存フォルダは作業フォルダとして開く。`.` は起動時のカレントフォルダを指す。
- ファイルとフォルダは併用でき、引数順に処理する。複数フォルダを指定した場合は最後のフォルダを作業フォルダとする。
- 相対パスはすべて起動時のカレントフォルダを基準に解釈する。フォルダ指定によって後続の相対パスの基準を変えない。
- 前回のセッションを復元してから引数を処理する。フォルダ指定で復元済みのTABや未保存本文を閉じない。引数なしでは従来どおり復元する。
- ファイルの重複指定・読込失敗は既存のファイルを開く処理に従う。存在しないパスは作成せず通知し、後続の引数も処理する。
- 診断引数（16章）と併用できる。`--` 以後はオプションとして解釈せず、ハイフンで始まる名前もパスとして扱う。
- 縦横・ソース／プレビューの起動時指定は追加候補として保留する。WSL固有の起動対応は別課題（RFN01-10）で扱う。

### 5.2 ファイルツリー

- **Left Pane の書式**（2026-09-15、書き手：「文字サイズなどを変更可能に。合わせて背景色と文字色も。フォルダとファイルで色を分ける。フォントの変更までは不要」）。
  左のExplorer・検索結果・最近使ったファイル・見出しの面を**Left Pane**と呼ぶ。Settings → Left Pane（Terminalの次の群）で、
  文字の大きさ（Size、9〜28px、既定12px）・背景色（Background）・文字色（Text Color）・フォルダの色（Folder Color）・
  ファイルの色（File Color）を決める。行の高さは字の大きさに合わせる（12pxで24px）。書体は変えない。
  フォルダの色は4つの面のフォルダの行に、ファイルの色はExplorerのファイルの行に効き、**選ぶまでは文字色と同じ**（パレットの
  「既定の色」で文字色に戻る）。見出しの行（作業フォルダの名前）にも字の大きさが効く。設定ファイルに残り、面のResetとReset Allで戻る。

- ExplorerのNew File／New Folderは、選択フォルダ内（ファイル選択時はその親、選択なしは作業フォルダ）に仮の行を表示し、名前を編集する。Enterで作成し、Escは仮の行を取り消す。確定前はディスクに作成しない。同名・不正な名前・作成失敗は理由を示し、入力を保持して修正可能にする。作成したファイルはActive Paneに開く。入力中はツリーの自動更新と行操作を抑止する。

- Explorerの見出しに「現在の文書をツリー内に表示」「すべて折り畳む」「更新」を置く。現在文書の表示はActive Paneの保存先まで親フォルダを展開し、その行を選択・スクロール表示する。文書やTABを開き直さず、Active Paneを維持する。保存先なし・ファイル不明・作業フォルダ外は案内し、作業フォルダを勝手に切り替えない。すべて折り畳む操作は展開を解除する。手動更新は選択・展開状態を可能な範囲で保持し、自動更新も継続する。

- 左ペインに作業フォルダのファイルツリーを表示する。
- ファイルツリーは表示・非表示を切り替えられる。
- Explorer表示中は、作業フォルダと展開中のフォルダをバックグラウンドで定期確認し、外部での追加・削除・名前変更・移動を数秒程度で反映する。全階層の走査は行わない。
- 変更がある場合だけ一覧を更新し、残っている選択項目・展開状態・スクロール・Active Paneを維持する。削除された選択項目は解除する。ドラッグ・メニュー・確認画面の操作中は自動反映を待つ。
- 一覧の更新は、開いている文書の参照パスや未保存本文を書き換えない。別アプリによるファイル移動への文書の追随とは別の処理とする。
- Markdownファイル、その他のテキストファイル、添付ファイルを表示する。
- ファイルおよびフォルダの新規作成、名前変更、移動、複製に対応する。
- ファイルツリー内のドラッグ＆ドロップに対応する。
  - 移動先は、フォルダの行のほか、**そのフォルダに属する行（開いているフォルダの中身の一覧）**を指しても指定できる。
  - 移動先に同じ名前のものがある場合は、**上書きの確認を求める**。上書きするときは、いまあるものをWindowsのごみ箱へ移す。
- ファイルをWindowsエクスプローラーで表示できる。
- 削除時は完全削除せず、Windowsのごみ箱へ移動する。Explorerからの削除が成功したら対象ファイルを開く全TABを閉じる。フォルダ削除では配下の開いている文書も対象にする。
- 対象に未保存編集があれば「保存してごみ箱へ移動」「保存せずごみ箱へ移動」「キャンセル」を選ぶ。保存して削除する場合は最新の本文を保存してから移動する。外部変更・保存失敗では削除を止める。確認中に未保存本文が変わった場合は再確認する。移動失敗・キャンセルではTABを保持する。削除成功後は作業コピーと戻る／進むの対象からも外し、自動復元による再表示を防ぐ。外部アプリからの削除は従来の参照先不明の警告と本文保持を維持する。
- ファイル名を変更しても、開いているタブの状態を維持する。

## 6. 基本画面

### 6.1 画面構成

メインウィンドウは、次の領域で構成する。

- 左ペイン（アイコンの縦列と、その隣の面）
- 編集領域
- ステータスバー

**トップバーは置かない**（2026-09-06改訂）。当初この節はトップバーを挙げていなかったが、
実装が持っていた2段のツールバーはどの節にも根拠が無かったため、6.7を書いて位置づけた
（2026-08-22）。その後アイコン1段まで縮んで残っていたのはメニューと左ペインの開閉の2つで、
**どちらもアイコンの縦列が担える**——メニューは列の先頭、開閉は「いま見ている面をもう一度押す」。
横一行を2つのボタンのために残すのは、その行のぶんだけ文書を狭くすることである。

### 6.2 左ペイン

- 画面左側へ配置し、表示・非表示を切り替えられる。
- **幅をマウスのドラッグで変更でき、セッション情報として復元する**（2026-09-06追加）。
  下限はファイル名が読める幅、上限は編集領域が残る幅とする。
- **アイコンの列の一番下に設定ボタンを置く**（2026-09-06追加）。編集器全体に効く設定は
  すべてここから開く（9、および6.8の既定の接続先）。並びの上にある4つは隣の面に何を出すかを選ぶもので、
  これはそれとは別のものを開くため、列の端に置いて区別する。
- 次の機能を切り替えて表示する。
  - Explorer
  - 検索
  - 履歴（最近使用したファイル）
  - アウトライン（2026-08-19追加）
- Explorerには、現在の作業フォルダのファイルツリーを表示する。
- 検索には、作業フォルダ全体の全文検索の結果を表示する（7.7）。
- アウトラインには、フォーカス中の編集ペインの文書の見出しを表示する（7.7）。

アウトラインの置き場所は当初この節に無かった。7.7が「見出しからアウトラインを自動生成し、
そこから該当セクションへ移動できる」と求めているのに対し、どこに出すかを決めていなかったため、
左ペインの4つ目として追加した。

### 6.3 編集領域

- ファイルの内容を表示・編集するメイン領域とする。
- 編集領域は、1つ以上の編集ペインで構成する。
- 各編集ペインは独立したタブ列を持つ。
- 各タブは、1つのファイル・未保存バッファ・**シェル**のいずれかを表示する（6.8）。
- タブ内の具体的な編集機能は「7. Editor機能要件」で定義する。
- **「キーボードはここ」を言う印はすべて同じ事実から出す**（2026-09-08追加）。
  アクセント色のタブも、紙の縁の線も、**いまキーボードを持っている編集ペイン**を指す。
  画面の焦点そのもの（メニューや設定画面が一時的に持っていく）で判じてはならない
  ——**メニューを開くたびに印が消えて戻る**ことになり、指定幅で折り返しているとき（9）は
  その線が幅の境界線として見えているので、いちばん目につく場所で点滅する。

### 6.4 編集ペインの分割

- ステータスバー右下は「Active Pane: N」で現在選択しているペイン番号（1始まり）を表示する。本文・TABの選択やペインの増減に追随する。**紙の周りの余白や紙の外を押しても、そのペインがActive Paneになる**（キャレットは動かさない。2026-09-15、書き手の求め）。クリックで「右に分割」「下に分割」を表示し、現在操作中のペインを対象に既存の分割処理を実行する（E15）。分割後は編集へフォーカスを戻す。

- ペインを閉じた後は、残った本文へキーボードフォーカスを戻す。クリックを挟まず次の`Ctrl+W`を実行できる（R4、2026-09-13実操作確認済み）。

- フォーカス中の編集ペインを、右方向または下方向へ分割できる。
- 分割は編集ペイン単位で行う。例えば、左右に分割した後で左側のペインだけを上下に分割できる。
- 分割時、新しい編集ペインにはフォーカス中のタブと同じファイルを表示する。
- 分割境界をドラッグし、各編集ペインの幅または高さを変更できる。
- 複数ペインがある状態で、あるペインのタブをすべて閉じると、そのペインの分割を解除する。
- 編集ペインの入れ替えに対応する。**入れ替える相手は方向で選ぶ**（左・右・上・下、2026-09-06改訂）。
  その向きに隣のペインが無い場合、その入れ替えは提示しない。隣を決める規則は11.3のペイン間移動と同じとする。
- 分割構成と各境界の位置をセッション情報として復元する。

### 6.5 タブ

- 同じ編集ペイン内のタブは、マウスドラッグで並べ替えられる。
- タブを別の編集ペインへドラッグして移動できる。
- ファイル名を変更しても、開いているタブの状態を維持する。
- **タブを少し長く押すと、そのタブのファイル名を変更できる**（2026-09-06追加）。
  **名前はタブの中で直に書き換える**——画面に出ている名前を変えるのに、別の場所を見せない。
  変更の規則と結果は5.2のリネームと同じものとする——入口が増えるだけで、
  名前の検査もタブの追従も二重に持たない。保存先の無いバッファには名前が無いので提示しない。
- **新しいタブは「New Tab」として開く**（2026-09-07追加）。何も表示せず、面の中央に
  **New File / New Terminal / Open File** の3つを行のように並べ、押されたものにその場で変わる。
  New Terminalは既定の接続先を起こす（6.8）。
  **タブを作ってから何になるかを決める**——先に訊いてから作ると、決める場所がまだ無い状態で
  決めることになる。
  - **左パネルからファイルを開いても、New Tabがその開き先になる**。新しいタブは増えない。
    これは仮のタブより強い規則で、開き方（一覧のクリック・ダイアログ・起動時の引数）を問わない
    ——まだ何でもないタブは、何にでも譲る。
  - **1文字でも入力すれば、そのタブは文書になる**（変換中の文字も含む）。下にあるのは
    §8.4の未保存バッファそのもので、New Fileを押したときと同じものである。
  - あるペインのタブをすべて閉じたときも、New Tabが1枚だけ残る。
- **左パネルの一覧から1回のクリックで開いたタブは「仮のタブ」とする**（2026-09-07追加）。
  仮のタブは1つの編集ペインにつき1つで、次に一覧から開いたファイルがその場所を引き継ぐ。
  タブ名は斜体で表示する。**次のいずれかで仮ではなくなる**——そのタブに1文字でも入力する、
  同じ行をもう一度クリックする、ファイルを開くダイアログや起動時の引数など名前を指して開く。
  フォルダを見て回るのにタブが増え続けるのを避けるための仕組みで、
  編集を始めたものは決して置き換えない。
- タブおよび編集ペイン間のキーボード移動は「11. ショートカット」で定義する。

#### 閉じたTABを開き直す（E6）

Paneメニュー「閉じたTABを開き直す」とCtrl+Shift+Tで、直近の文書TABを現在Paneに開く。現在の起動中に閉じた20件まで保持し、キャレット・スクロール・表示モード・未保存内容を戻す。同じ文書が既に開いている場合は共有する。明示的な破棄・Explorerでの削除は履歴からも除外する。存在しないファイル、空のNew Tab、Terminalは復元しない。下段Terminalのプロセスも復元対象外。再起動後の文書復元は既存の作業コピーの仕組みを使う。

### 6.6 ステータスバー

- 画面の最下部へ配置する。
- フォーカス中のタブと編集ペインに関する文書情報を表示する。
- 表示内容の詳細は「10. 文書情報」で定義する。
- **読み方の帯**（2026-09-11追加）：何の文字コードで読んだか、改行コード、
  **現在の論理行と行内位置**、検索中は`「語」3 / 12`。
  - **書き方はここに置かない**（2026-09-10、書き手の問い）。保存の文字コードは
    「名前を付けて保存」の欄にある——**ここが言うのは、いま開いている原稿をどう読んだか**である。
  - **知らせは窓に1つの場所へ。**断ったこと（保存できない字がある等）もここに出す
    ——縦書きの紙の上にだけ出すと、横書きで書いている書き手には一言も届かない。
  - **知らせは時間で消さない**（読んでいる最中に消えるのは、出ていないのと同じ）。
    **書き手が次へ動いたら畳む**：打つ・別のタブへ移る・別のものを開く・新しいタブを開く。
  - **知らせは誰の話かを分ける**（2026-09-15、書き手の判断：Pane1で保存した「Saved」が、Active Paneを
    Pane2へ切り替えても残っていた）。出す場所はステータスバー1か所のまま、頭に対象の名前を付ける。
    - **TAB（文書）の話**：保存・読み直し・外部変更・Viewer／ReadOnly・上限・リンク・比較できない理由など。
      「原稿.md：保存しました」。
    - **Paneの話**：分割できない・組版や描画の失敗・選択の印・前後の移動・端末など。「Pane 2：分割: ペインが多すぎます」。
    - **窓全体の話**：Explorerの操作・単語帳・設定のReset・壁紙・すべて保存・作業コピーの退避など。名前を付けない。
    - TAB・Paneの話は、**別のPaneで操作が始まったら畳み、戻っても出さない**。窓全体の話はPaneを移っても残る。

### 6.7 メニューとボタン

#### 基本メニュー（2026-09-20合意・実装検証中）

タイトルバーの同じ一段を、通常タイトルとFile / Edit / Insert / Layout / View / Run / Helpで切り替える。入口はタイトルバー最左端のアプリアイコン。分類は幅48pxで横並びにし、ホバーで展開、項目選択後に分類も閉じる。開閉で本文・TAB・サイドバーを動かさず、Windowsの移動・サイズ変更・最大化・終了を維持する。以下の旧配置規定より本項を優先する。

- 2026-09-21（RFN01-38）: **挿入の末端項目は「未実装」の無効表示ではなく、本文へ置く処理へ接続した。**リンク（Markdown・Wiki・別名付き）、ルビ、注記（左側の注記・ルビと左側の注記）、傍点6種、傍線5種、文字注記4種、見出し1〜6と解除、字下げ1〜4と解除、地付きと地から1〜4字上げと解除、改ページの40項目である。選んだ字を指す注記は字を選んでいる間だけ、行の体裁は触れる行のときだけ押せる。行の体裁は**引用・箇条書き・コード・表・空白で始まる行には置かない**——記号も注記も行のいちばん頭に置くので、そこ以外では読まれないためである。字下げと地付きは併用でき、地付きを先に置く。
- Helpの2項目（使い方・RFN Editについて）は、内容を用意するまで「未実装」の無効表示のままにする（2026-09-21、書き手の判断）。
- New File / New Terminalは直接選び、New Tabは＋専用。旧キーID 18は予約扱いとし、以後のIDを詰め直さない。
- 文字・組版・背景の全体設定はSettingsに置く。Layoutの背景と外観は対象Pane／TAB／Panelに作用するもの。行番号と空白・改行記号はViewで切り替え、縦横共有時を含む作用範囲を表示する。
- 文書・Editor Panel・主／下段Terminal・入力欄を識別し、メニューを開く前の対象を保持する。対象が消えた場合は中止し、別の文書へ適用しない。
- F10でバーへ、左右で分類、下／Enterで項目を開く。Escで子メニュー、項目、バーの順に閉じる。Terminal本文のF10とIME変換中のキーは横取りしない。
- 詳細は[画面仕様と実装計画](メニューA案_画面仕様と実装計画.md)、[操作接続仕様](メニュー操作接続仕様.md)を参照。実画面検証・ユーザーAcceptの完了は別途記録する。

2026-08-22追加。機能を呼ぶ口をどこに置くかを、それまで定義していなかった。

- 機能を呼ぶボタンはアイコンで表し、名称はマウスオーバーで示す。
- 名称は英語表記とする。文書の内容はこの限りではない。
- **アプリ全体に関わる操作**は、アイコンの縦列の先頭のメニューへ集める（2026-09-06改訂。
  もとはトップバー左端）。ファイルとフォルダを開く、全体の保存、検索。
  **表示設定だけは同じ列の一番下の設定ボタンへ分けた**（6.2）——設定は「呼ぶ操作」ではなく
  「変えておくもの」で、一覧の中に混ぜると探す対象が2種類になる。
- **編集ペインとタブに関わる操作**は、そのペインのタブ列右端のメニューへ集める。
  保存、名前を付けて保存、タブを閉じる、分割・解除・入れ替え、表示倍率。
  **どのペインに効くかは、そのメニューがどのペインにあるかで決まる**——
  操作の前に対象のペインを選ばせない。
- **表示モードの切り替え（7.2）はメニューへ入れない。**タブ列に出したままにする。
  いまどのモードかは常に見えている必要があり、閉じたメニューは状態を表さない。
- 左ペインの4つの機能（6.2）は、アイコンの縦列で切り替える。
  **列は左ペインを閉じても残す**——どの機能を見ていたかを失わないため。
  **いま見ている面のボタンをもう一度押すと面を閉じる**（2026-09-06追加）。
  開閉専用のボタンは置かない。
- 配色・余白・文字サイズは1か所で定義し、画面のどこにも直接書かない。
- **右ボタンは、押した場所にできることを出す**（2026-09-06追加）。場所ごとに次のとおり。
  ショートカットのあるものは行に併記する。
  - 編集面で**何か選んでいるときは、`単語帳へ追加`**（2026-09-08追加、要件 7.9）。
    **いまこの文書に効いているモードの語群**が並ぶ。**書きながら語を足すいちばん普通の道**
    なので、選択があるときだけ、選択に関わる行のそばに出す。
  - タブ列の空き：New File / New Tab / New Terminal / Open File、Close / Close All / Close Clean。
  - 編集面：Undo / Redo、Cut / Copy / Paste、本文だけをコピー、箇条書き、
    選択開始・矩形選択開始（11.4）、戻る・進む（7.7）、下段の開閉（6.8）・クイック下書き（12）。
    Split Right / Split Down / Close Other PanesとZoom In / Zoom Out / Reset Zoomはペイン上部のメニューへまとめる（U2・U3）。
  - タブ（文書）：Close / Close Others / Close Clean、Rename / Copy Path、Save / Save As。
    **`Close Clean`は未編集のタブを閉じる**が、シェルのタブは閉じない——保存待ちは無くても、
    閉じれば走っているものが終わる。
  - タブ（端末）：Close 3行と、接続先の切り替え（6.8）。**名前も道も保存も出さない**
    （2026-09-07追加）——シェルには変える名前が無く、指すファイルも無い。
    押せる行が何もしないより、無いほうがよい。
  - Explorerの行：Rename / New File / New Folder / Duplicate / Reveal / Delete /
    Search in This Folder（5.2、7.7）。
  - **下段**（6.8）：帯にも右ボタンのメニューを置く（2026-09-08追加）。**上下で別のもの**になる
    ——シェルの帯は Copy / Paste と、上の面へ戻る・帯を閉じる。下書きの帯は Cut / Copy /
    Paste / Select All と、上の端末へ送る・上の端末へ戻る・帯を閉じる。倍率はどちらにも無い。
- **その場所でできないことは、出さない**（2026-09-08追加）。押せる行が何もしないのは、
  壊れている行と画面の上で見分けがつかない。前に出ているタブがシェルのとき、次は出さない。
  - 編集面と`⋮`：Cut、選択開始・矩形選択開始——**見えていない文書を切り、見えていない
    文書に印を置いていた**（タブの裏には空の文書がいる）。
  - 編集面と`⋮`：Zoom In / Zoom Out / Reset Zoom——**倍率は文書のもの**で、升目には
    効かない（9）。
  - `⋮`：Save / Save As / Reveal in Explorer——タブのメニューが2026-09-07に落とした3行と
    同じ理由（シェルには名前も道も保存するものも無い）。
  - ☰：Find in File——帯は要件7.7の文書を探すもので、シェルの中は探せない。
  - **Copyは残す。**シェルにも選択があり（11.2）、選ばれているものを写すのがこの行の意味
    である。写す先が本文か升目かは編集器が判じ、綴じ目だけ`Ctrl+Shift+C`と書き替える。
  - タブ：Rename / Copy Path は、**ディスクの上に何かあるタブだけ**に出す。無題の文書は
    `Save`で名前をもらう側なので、そちらの2行は残る。
- **メニューは、それを開いたものの真下に出す**（2026-09-07追加）。タブのメニューはそのタブの
  下、`＋`のメニューはその釦の下。**位置は開いたものと同じ座標系で数える**——窓の座標へ
  直してから戻すと、変換の片方が古いだけで手から離れた場所に出る。
- **タブ列の右端の`＋`は New Tab を1枚開く**（2026-09-07改訂。もとは New File /
  New Terminal / Open File のメニューだった）。**何になるかはタブの中で訊く**（6.5）ので、
  押す前に決めさせない。タブ列の空きの右クリックには、直に答えを選ぶ道も残す
  （New File / New Tab / New Terminal / Open File）。
- Explorerの`＋`は New File / New Terminal / New Folder（2026-09-06追加）。こちらは
  作業フォルダに対する操作なので、New File／New FolderはExplorer内に仮の行を追加して名前を入力する。
  その隣のフォルダのボタンは Open Folder（5.1）とする。

### 6.8 Terminal

2026-09-07追加。13.1に「別途開発予定のTerminalと連携する」とだけ書いていたものを、
**この編集器自身が持つもの**として書き直した。用途は限定しない汎用のシェルとする。

- **タブ1つがTerminalになる。**打鍵はシェルへ送り、出力は升目へ描く。
- 接続先は **WSL2・PowerShell・PowerShell 7** から選ぶ。WSL2が起動していなければ起動させる。
- **接続先の一覧は設定として持ち、書き手が足せる**（2026-09-08追加。もとはこの3つの決め打ち）。
  1件は**名前とコマンド行の組**で、設定ファイルへ`名前 | コマンド行`の形で並べる。
  上の3つはその初期値であって、`cmd.exe`も`wsl -d Ubuntu`も同じ資格で並べられる。
  **並び順が意味を持つ**——先頭が既定の初期値であり、`＋`のメニューもこの順に出す。
  **この機械に無いものは出さない**（`PATH`に実行ファイルが無いもの）。ただし1つも見つから
  なければ全部を出す——空のメニューよりは、開けなかったと言えるほうがよい。
  **既定は名前で覚える**（同日改訂。もとは番号）——一覧が書き手のものになった以上、
  番号は明日には別のシェルを指している。
- **既定の接続先を設定として持つ**（2026-09-07追加。初期値はWSL2）。設定画面のいちばん上
  ——書字方向のシートを選ぶより前——に置く。シェルに書字方向は無いためである。
  **接続先を訊かない道はすべてこれを起こす**：`＋`のNew Tab、タブ列のメニュー、Explorerの`＋`、
  そして編集ペインの下段。**訊くのは☰だけ**とする。
- **端末のタブを押し続けると、接続先を選び直せる**（2026-09-07追加）。タブはそのままで、
  中で走っているものだけが替わる（走っていたものは終わる）。文書のタブで同じ press が
  ファイル名の書き換えになるのと同じ手つきで、**端末のタブには替える名前が無い**。
- **接続先を選べる場所はすべて同じ道を通る**（2026-09-07改訂）——前に出ているタブが
  まだ何でもない（New Tab、6.5）ならそのタブがその端末になり、そうでなければ新しいタブが
  開く。どのメニューから頼んだかで結果は変わらない。
- **端末の見た目は、紙とは別の設定として持つ**（2026-09-08追加、書き手の求め）。
  背景色・文字色・書体・大きさ。**端末を黒地で使う人が多い**のに対し、要件9のシートは
  原稿の紙の設定で、**同じ値で決めるものではない**。設定画面もシートを選ぶ面とは分ける
  （9）——端末に書字方向は無い。
  - **16色（SGR）は書き手に選ばせない。**背景の明るさから、明るい側と暗い側の2組を
    自動で選ぶ。**選ばせると16回選ぶことになり、しかも選び終えるまで読めない**——
    黒地にした瞬間に暗い色が消えるのが、この設定でいちばん起きやすい壊れ方である。
- **表示文字コードの設定は持たない。**ConPTYが出入りともUTF-8へ変換し、シェル側の
  コードページはconhostが吸収するため、設定を作れば正しく動いているものを壊せる欄が
  増えるだけである（技術検証9.4）。
- スクロールバックをホイールで遡れる。選択してコピーできる（11.7）。
- **編集ペインの足元に、もう一段置ける。**文書のタブの下にはTerminalを、Terminalのタブの
  下にはEditor（下書き）を置く。
  - **下段はタブごとに持つ**——開いているかどうかも、高さも、下書きの中身も、そのタブのもの。
    閉じても中のシェルは走り続ける。開閉と高さはセッションへ入れる（8.5）。
    **シェルそのものは覚えない**——編集器と一緒に終わるので、戻ってきたタブが前に出たときに
    改めて1本だけ起こす。
  - 境界のドラッグで高さを変える。
  - 本文と下段の行き来は11.3のペイン間移動と同じ手つきとする。
  - **下段のEditorは、書いたものを上の端末へ送れる**（11.7）。**送るのは打鍵としてであって、
    貼り付けではない**——打ったとおりに端末へ入る。末尾に改行を置けば走り、置かなければ
    プロンプトに残る。**それを決めるのは書き手**である。送った下書きは空にする。
  - **送られる範囲を背景色で示し、改行のあるところに罫を引く**（2026-09-07追加）。
    下書き全部が送られるのだから色も全部に付き、`ls`と`ls`＋改行は**色の付く行数**で
    見分ける。改行そのものを字（`¶`など）で出すことはしない——折り返した行のどこで
    折れたかを編集器が知らない以上、行によって印が出たり出なかったりする。
    **一部にしか置けない印は置かない。**

## 7. Editor機能要件

### 7.1 共通編集機能

- WindowsのExplorerなどからメインウィンドウへファイルをドロップすると、落とした編集ペインへ保持タブとして開く。複数ファイルを受け付ける。本文とタブ領域を対象とし、編集ペイン以外へのドロップでは開かない。
- 読み込み、同一ファイルの再利用、未保存本文の保護は通常のファイルを開く処理に従う。フォルダのドロップは開かず案内する。読めないファイルは既存の読み込みエラーを通知する。

- 文字入力、カーソル移動、範囲選択、切り取り、コピー、貼り付けに対応する。
- 矩形選択に対応する。横書きと縦書き、ソース編集とライブプレビュー編集のいずれでも利用できる。
- 矩形選択の範囲は、**表示行**の範囲と、行を横切る位置の範囲で定める。画面上の向きは書字方向に従う。
- 2026-08-28改訂。**もとは「論理行の範囲と行内の桁範囲」と定めていたが、それではこのEditorが書くものの上で使えなかった。**Markdownの散文は1段落＝1論理行であり、折り返して画面上は何十行に見えていても、その下の論理行は段落を終わらせる空行、その次は見出しである。書き手が矩形で囲もうとしているのは**見えている行**であって、ファイルの中の行ではない。
- 上記にともない、ライブプレビュー編集について「選択範囲にかかる論理行をソース表示（編集扱い）とする」という定めを削除する。表示行で定めるなら、整形表示の矩形は整形表示のまま成り立ち、原文を立ち上げる必要がない。選択文字数は、両端が指す位置のあいだにあるソース文字で数える（Markdown記号を含む）。
- F1：設定のLayoutに「空白・改行を表示」を設け、初期値は非表示。縦書き・横書きの設定ごとに保存する。半角空白は点、全角空白は四角、タブは書字方向の矢印、実改行は折れた矢印で描く。自動折り返しには改行印を付けない。印は描画だけに追加し、本文・保存・コピー・Undo履歴・文字位置を変えない。Markdownの整形で隠れる記法は表示対象外とし、原文の空白確認にはソース表示を使う。
- Undo／Redoに対応する。
  - 本文の右クリックメニュー上段にもUndo／Redoを置く。履歴がないとき、Viewerモード、外部版の読み取り専用表示では無効表示とする。
  - 本文の右クリックは編集操作を上段にまとめ、分割・他ペインを閉じる・倍率操作はペイン上部のメニューにまとめる（U2・U3）。
  - Undo／Redoで保存済み本文と一致したら未保存の印を消し、再び異なれば付ける。保存成功時は連続入力のUndo単位を区切る（R2、2026-09-13）。
- 日本語IMEによる入力と変換に対応する。
- キーボード操作の詳細は「11. ショートカット」で定義する。

#### 語と行を選ぶ・動かす（2026-09-11追加、追加要件 E3 から転記）

- **ダブルクリックで語、行番号を押して論理行**を選ぶ。語の切れ目は`Alt+F`／`Alt+B`と
  同じ規則にそろえる——**規則を2つ持たないことのほうが、語の切り方の精度より大事**である。
- **選んだ論理行を、文書順の前／後へ動かす・写す・消す。**縦書きでも`Alt+↑`が「前の行」で
  ある（画面の上下ではない）。
- **複数行の`Tab`／`Shift+Tab`で字下げ。**箇条書きの行なら**入れ子の深さ**が変わる。
  字下げは空白で書き、一段は本文でも箇条書きでも同じ幅である。
- **`Enter`が引用・箇条書きを継ぐ。**字下げを継ぐのは**箇条書き・引用と、項目の
  続きの段落だけ**である（2026-09-12の書き手の決定）——**本文の行の字下げは継がない。**
  行頭の空白はいちど出来ると次の行へ写り、その行がまた次へ写して文書じゅうに広がる。
  広がった先の行は**見出しにならない**（行頭に空白のある行は本文として読む）ので、
  「`#`を打っても見出しにならない」までついてくる。項目の字下げは書き手が打った
  ものではなく項目の形そのものなので、そちらは継ぐ。番号は1つ進め、**下の行は振り直さない**。
  タスクは空の箱で継ぐ。**中身の無い項目の`Enter`は、印を消して継続を終える**
  （改行は入らない）。**`Shift+Enter`はその項目の続きの段落**で、印を継がず本文の列に残る。
- **選ばれるのは行の字まで**で、後ろの改行は入れない——入れるとカーソルが次の行の頭へ出る。
- **選択の端がちょうど行末なら、その行は入らない**（終わりが行頭ならその行が入らないのと
  同じ話の裏側）。行の頭の近くを押すと当たり判定は前の行の末尾を返すからである。

#### 後から箇条書きにする（2026-09-11追加、追加要件 E10 から転記）

- **選んだ行に箇条書きの印を付ける／外す。**印の無い行が1つでも混じっていれば付け、
  そろっていれば外す。**触るのは本文の行と、既に箇条書きの行だけ**——見出し・区切り線・
  表・コード・空行・タスクは、行そのものが別の意味を持つので触らない。
- **番号は選んだ範囲の中で1から**、字下げの桁ごとに数える。範囲の外の番号は書き換えない。
- **`Renumber`は、カーソルの行の番号から下を数え直す。**開始番号を変えるのは、先頭を手で
  打ち直してこれを頼むことである——**数を訊く画面は作らない**（2026-09-11、書き手の選択）。
- **番号の鍵は「そろえる→外れる」の階段。**そろっていなければ数え直し、もう変わらなければ
  外す。印の鍵も同じで、**頼まれた記号にそろえてから外す。**
- **連なりは空行では切れない**（Markdownの緩いリスト）。**記号が変われば切れる**
  （CommonMark §5.3）——`1. 甲 / 2. 乙 / 1) 丙`の丙は3つめではない。

#### 本文だけをコピー（E13）

Wordなどへ草稿を渡すため、本文の右クリックメニューに「本文だけをコピー」を設ける。選択中はその範囲、未選択なら全文を対象にする。通常のCopy／Cutは原文のまま維持する。
見出し・強調・引用・箇条書きの記号、ルビの読みと傍点の記号を外し、リンクは表示名だけ残す。コードの中身はそのまま残してフェンスを外す。区切り線・表の区切り行を除き、表の列はタブで区切る。未対応の記法や画像の記法は、内容を失わないよう保持する。横書き／縦書き・ソース／プレビュー・記法の表示設定によらず、対応する記法を除去する。
原稿・選択・Undo履歴は変更しない。クリップボードに渡せなかった場合は失敗を表示する。Markdown以外の文書は文字列をそのままコピーする。

### 7.2 Markdownの編集モード

各Markdownタブは、次の4モードを切り替えられる。

1. 横書きソース編集
2. 縦書きソース編集
3. 横書きライブプレビュー編集
4. 縦書きライブプレビュー編集

モード切り替え時は、次の状態を可能な限り維持する。

- 編集内容
- Undo／Redo履歴
- 未保存状態
- カーソル位置
- スクロール位置

タブごとに最後に使用した表示モードを記憶する。

### 7.3 ライブプレビュー編集

#### 7.3.1 基本動作

- Explorerはシングルクリックで既存TABへの移動または仮表示を行い、ダブルクリックで別TABとして保持する。最初のクリックで選択中のNew Tabへ開いた場合、または未表示のファイルを仮TABへ表示した場合は、そのTABを保持して重複追加しない。ダブルクリック前から開いていたファイルは、通常TAB・仮TABのどちらでも別TABを追加する。本文・未保存内容は共有する。

- 保存先のある文書TABの右クリックメニュー「同じ文書を別TABで開く」で、同一ペイン内にも別TABを追加できる。本文・未保存内容・変更履歴は共有し、Viewer状態・カーソル・スクロールはTABごとに保持する。通常のファイル選択は既存TABへ移動するが、選択中のTABがNew Tabなら、そのTABで開くことを優先する。ファイルダイアログ・Explorerのどちらも同じ規則とし、New Tabへ開いた文書は仮TABにしない。複数のNew Tabがあっても選択中のものを使う。

- E14②：TAB単位のViewerモード。ペイン上部の本のボタンで切り替え、Viewerではクリック・選択位置にかかわらずMarkdownの完成形を表示する。入力・削除・貼り付け・置換・Undo／Redo・IME編集は行えず、選択・コピー・検索・スクロール・リンク移動は利用できる。同じ文書でも別TABは独立して編集でき、変更はViewerにも反映する。TAB切り替えではモードを保持し、再起動時は復元しない。Viewerを終了するとライブプレビュー編集へ戻る。文書の保存・自動退避・OSのファイル属性には影響しない。
  - ソース表示のReadOnlyモード（追加要件 2026-09-15）：ソース表示で本のボタンを押すとReadOnly、プレビュー表示で押すとViewerとする。ReadOnlyは原文を横書きで表示し、縦書きで押された場合は横書きへ切り替える。編集の制限はViewerと同じで、縦書き・プレビューへの切り替えもできない（ボタン・ショートカットとも）。
  - ReadOnlyで前に出ている文書は0.5秒ごとに外部変更を見回り（ほかの文書は従来どおり2秒）、変われば読み直す。入った時点とTABを戻した時点では最下行を表示し、書き足しに合わせて自動スクロールする。書き手が上へスクロールすると自動スクロールを止めて表示位置を保ち、最下行まで戻すと再開する。面の大きさが変わっただけでは止めない。書き足しだけの読み直しではカーソル・選択を保つ。
  - ReadOnlyでは上書き保存しない。名前を付けて保存は可能で、保存は断面となり文書は別ファイルになるため、ReadOnlyを解除する。再起動時は復元しない。
  - Viewer・ReadOnlyのあいだに外部変更を読み直しても、モードは解除しない。

- 整形済みのMarkdown表示上で直接編集できる。
- 通常時はMarkdown記号を隠し、編集位置に応じて必要な記号を表示する。
- 編集中の見出しもH1〜H6の文字サイズ・書式を維持する（E14①）。見出し記号はその見出しと同じ文字サイズ・書体で、実測した長さに基づき直前の余白へ配置する（横書きは右寄せ、縦書きは下寄せ、間隔は見出しサイズの1/4）。H1の`#`からH6の`######`まで各設定で測り、間隔を加えた最大値を共通余白とする（最小16px、端数切り上げ）。設定変更時に測り直して再描画し、通常の編集・カーソル移動では余白を維持する。横書き・縦書きは独立して計算する。原稿の記号は保持し、ソースモードでは原文を表示する。
- RFN01-22：編集中の見出し記号と本文は、横書きでは基線、縦書きでは同じ列の基準位置を揃える。行間・ルビ用余白の設定があってもずらさず、記号上のカーソルも本文と同じ位置・高さ（縦書きでは幅）にする。編集開始・終了で本文の行頭位置を動かさない。
- **太字などの記号は、閉じたときだけ記法である**（2026-09-15、書き手の判断：Markdownの標準に合わせる）。閉じない`**`は字のまま出し、行末や文書の終わりまで太字にはしない。**同じ段落の中なら改行をまたいで閉じられる**（空行・見出し・箇条書き・引用・表・コードで段落が切れる）。閉じる記号を打った時点で、前の行まで太字になる。ステータスバーの本文の字数も、またいだ記号を数えない。`*`と`**`が同じ字で並ぶ入れ子（`***`）をまたぐ場合の読み分けは、1行のときと同じ簡略な読み方による。
- **編集中の行も、太字・斜体・取消線・インラインコード・リンク・傍点の書式を保つ**（2026-09-15、書き手：「記号は隠さず、ただし太字として見える方がいい」）。記号（`**`など）は見えたままで、その内側の字が整形表示と同じ書式で組まれる。読み方は整形表示と同じものを使い、ルビの読みなど字を覆う箱は編集中には付けない。
- **表は表のまま編集する**（2026-09-15、書き手の選択）。カーソルのある行も升目のまま組み、各セルの中身を原文（太字の記号なども見える）で出す。`|`と前後の余白は描かず、列の位置も動かない。区切り行（`| --- |`）だけは、カーソルが来ると原文の1行で出る（揃えの記号を書き換えるため）。整形表示の面では：
  - `←`／`→`はセルの端から隣のセルへ移る（`|`と余白に止まらない）。行の端からは隣の行へ。
  - `Tab`は次のセルの頭、行の最後のセルからは次の表の行の最初のセル（区切り行は跨ぐ）、表の最後のセルでは動かない。`Shift+Tab`はセルの途中ならそのセルの頭、頭なら前のセル。
  - セルに打った`|`は区切りではなく字として、原稿に`\|`で書く（1行の入力のとき）。
  - 途中の空のセルにもカーソルを置いて書ける。行・列の追加と削除は、使ってみてから決める。
- 未対応の記法はソース表現のまま表示し、保存時にも削除・変形しない。
- 通常クリックはカーソル移動に使用する。
- `Ctrl+クリック`で通常リンクの相対パス・フルパス、およびフルパスを指定した内部リンクを、同じペインの別タブで開く（E12）。相対パスは原稿の保存先を基準にする。既存タブの未保存本文を維持し、開けない場合は理由を表示して元の原稿を保つ。名前だけの内部リンク、未保存メモの相対パス、Web URL・見出し／ブロック指定は移動対象外。リンク先の確認はクリック時だけ行い、フォルダの再帰探索はしない。
- タスクリストのチェックボックスをクリックすると、元の`[ ]`または`[x]`を更新する。

#### 7.3.2 整形表示する要素

**記法そのものに色を付ける話は[単語チェックモード要件](単語チェックモード要件.md) 8.5 に
ある**（2026-09-08）。ここは「何を整形して見せるか」、あちらは「何を何色で見せるか」——
記法の色は単語の色と同じ層に乗るので、順位ごとあちらに置いてある。

- H1～H6見出し
- 太字、斜体、取り消し線
- 箇条書き、番号付きリスト、タスクリスト
  - **印は`-`・`*`・`+`の3つ**（CommonMarkがそう認めている）。**同格に扱う**
    （2026-09-11、書き手の決定：「同格に認めているなら、同格に扱ってあげるべき」）。
  - **どれを印として読むかは、1つずつ入切できる**（設定は 9 の「読み方」）。`-`も切れる
    ——扱いは3つとも同じである。切った記号の行はただの本文になり、記号もそのまま字として
    出る（**原稿のバイト列は変えない**）。
  - **記号ごとに、画面へ出す印を選べる**（`•`／`・`／`○`。設定は 9 の「紙」）。
    **読まないと決めた記号は、見せ方も設定できない。**
  - **番号の印は`1.`と`1)`の2つだけ。**`(1)`は仕様に無いので箇条書きにしない
    （2026-09-11、書き手の決定：「そのままにしましょう」）。
- 引用
- 水平線
- 通常リンクと内部リンク
  - E12（2026-09-13）：所在を解決していない内部リンクは赤字と下線で表示する。これはファイルの不存在を断定する印ではなく、移動先が未解決であることを示す。別名や内部の強調は維持し、画像・コード・未完の記法には適用しない。単語チェックの指定色がある場合はそちらを優先する。構文解析中にフォルダを探索しない。フルパスの内部リンクは通常の下線表示とし、Ctrl+クリックで開ける。存在確認はクリック時に行う（7.3.1）。
- インラインコードとコードブロック
- 表
- 脚注
- Callout

#### 7.3.3 画像とリッチコンテンツ

- `![[画像.png]]`や`![説明](画像.png)`などの記法を保持する。
- **画像だけの行**（2026-09-15、書き手）は、ライブプレビューで画像そのものをブロックとして表示する。対象は`![説明](画像.png)`・`![説明|300](画像.png)`・`![[画像.png]]`・`![[画像.png|300]]`で、行き先が画像の拡張子（png・jpg・gif・bmp・webp・tif・ico・jxr・heicなど、Windowsが読める形式）で終わるもの。引用・箇条書きの中、文中の画像、URL（`://`を含む行き先）は記法のまま表示する。
  - 行き先は文書のフォルダからの相対パス（`%20`などは戻す）か絶対パス。名前の無い文書では相対パスを読まない。読めない画像は記法のまま表示する。
  - 大きさは元の画素数を表示倍率ぶん。`|300`があれば幅300に合わせて縦横比を保つ。行の長さに入らなければ、縦横比を保って行の長さへ縮める。縦書きでも画像は回さず、行（列）の上下の長さに合わせる。
  - カーソルのある行は記法をすべて表示し、画像も表示したままにする。記法が行の頭の側、画像がその先（横書きは記法の下、縦書きは記法の左。2026-09-16、書き手）。
  - **大きさはマウスで変えられる**（2026-09-16、書き手）。画像の上にポインタを置くと角につまみが出る（横書きは右下、縦書きは左下——組み直しても動かない角の対角）。引いているあいだは枠だけを表示し、縦横比を保つ。離したら記法の幅（`|300`、無ければ足す、`300x200`は幅だけにする）を書き換える。行に沿う長さは行の長さまで、幅は16px以上。表示倍率を外した画素数で書く。1回の編集として取り消せ、カーソルは動かさない。閲覧モードではつまみを出さない。
  - ソース表示と閲覧用の書き出しでは記法のまま扱う。
- 数式とMermaidは初期版では描画せず、記法を保持して編集可能にする。

### 7.4 Markdown記法の互換性

- 標準的なMarkdown記法に加え、内部リンク、埋め込み記法、YAMLフロントマター、タグ、脚注、Calloutを保持する。
- `[[ノート名]]`および`[[ノート名|表示名]]`形式の内部リンクを扱う。
- 見出しおよびブロックへの内部リンクを扱う。
- Obsidianで使用される記法は保持するが、プラグイン固有機能は実行・再現しない。
- Obsidianのテーマ、独自CSS、プラグインによる表示は再現しない。
- 未対応の記法が存在しても、保存時に内容を失わない。

### 7.5 縦書き編集

- 縦書きは、ソース編集とライブプレビュー編集の両方で提供する。
- 文章は画面右上から開始する。
- 文字は上から下へ進み、列は右から左へ増える。
- 文書はページ分割せず、横方向へ連続してスクロールする。
- マウスホイール操作を横方向の文書移動へ割り当てる。
- 縦書きに対応したカーソル移動、範囲選択、日本語IME入力を提供する。
  - **縦書きなのは編集面だけである**（2026-09-08、書き手の報告）。設定画面の名前の欄も、
    検索欄も、タブの名前も**横書きで打つ**——IMEの変換候補の並びは合成フォントで決まり、
    それは窓に1つしか無い（技術検証 7.2）ので、**最後に言ったのがペインなら欄まで縦になる**。
    **組版の向きはペインのものだが、欄はペインではない。**
- 句読点と括弧を縦書き用の字形・位置で表示する。
- 全角英数字は、縦書きの各文字として正立表示する。
- 連続する半角英数字は横書きの文字列として組み、縦書き内で90度回転して表示する。
- 縦中横を適切に扱う。
- 縦書きライブプレビュー内の表とコードブロックも、初期版では縦書きとして組む。
- **禁則はWindows（DirectWrite）の組版に任せ、独自の行分割は持たない**（2026-09-15、書き手の判断：「Windows標準が一般的であれば、独自に持つ必要はない」）。縦書き・横書きとも同じで、設定では変えない。効いている規則（実測、試験`line_breaking_keeps_the_kinsoku_rules`で確かめ続ける）：
  - 行頭に来ない字：`、。，．・：；？！ー…‥`、小書きの仮名`ぁぃぅぇぉっゃゅょゎゕゖァィゥェォッャュョヮヵヶㇰ`、`々〻ゝゞヽヾ`、閉じ括弧`）」』】〕〉》］｝〟’”`、`〜‐゠〵`。前の字ごと次の行へ送る（追い出し）。
  - 行末に来ない字：開き括弧`（「『【〔〈《［｛〝‘“`。
  - 分けない並び：`……`・`――`・`‥‥`・`！？`・半角の英数字の並び。
  - 行頭に来てよい字：`―`（ダッシュ1字）と`〳〴`。ぶら下げ（句読点を行の外へ出す）はしない。
- 印刷用の用紙サイズや改ページは扱わない。

### 7.6 同一ファイルの複数表示

- 異なる編集ペインで、異なるファイルを表示できる。
- 同じファイルを複数の編集ペインへ表示できる。
- 同一ファイルを、ソース編集とライブプレビュー編集で同時に表示できる。
- 横書きと縦書きを同時に表示できる。
- 同じファイルへの編集内容は、すべての表示へ即時反映する。
- Undo／Redo履歴はファイル単位で共有する。
- **開く入口がどれでも、同じファイルの本文は1つ**（2026-09-08追加）。ツリーもコマンドラインも
  1つの道を通っていたが、「ファイルを開く」ダイアログだけが毎回別の本文を作っていて、
  そのタブどうしは互いの編集を知らず、退避先（8.1）まで取り合っていた。
- **保存も入口である**（2026-09-11追加、書き手のレビュー P1）。名前を付けて保存の宛先を別の
  タブが開いていたら、**書いたあとに1つの文書へ合流させる**——相手のタブは残り、保存した本文を
  見るようになる。**相手に未保存があれば、捨てる前に訊く**（保存する／別の名前で／やめる）。
  合流しないと、**同じ保存先と同じ退避先を、別々の本文が使う**ことになり、次にどちらかが
  保存した拍子に、もう一方の原稿が消える。
- カーソル位置、選択範囲、スクロール位置、表示倍率は編集ペイン内のタブ表示ごとに保持する。

### 7.7 検索とナビゲーション

- 現在のファイル内を検索・置換できる。
- 検索バーは、その文書を開いている編集ペインの中に表示する（2026-08-22追加）。
  対象が1つの文書である以上、どの文書に効くのかが置き場所で分かるようにする。
- 検索バーを開いたショートカットをもう一度押すと閉じる（2026-08-22追加）。
  置換の入力欄は`Ctrl+H`のときだけ表示する。
- **全置換も文書の上限（2.3）の内側にいる**（2026-09-08追加）。打鍵も貼り付けも上限を見て
  いるのに、全置換だけが結果をそのまま本文にしていた——短い語を長い語へ一括で置き換えれば
  上限を越え、**保存はできて開き直せないファイル**になる（読み込み側は同じ上限で断る）。
  確定の前に測り、越えるなら何も起こさずに理由を言う。
- 検索は英字の大文字・小文字を区別しない（2026-08-22追加）。文書内の検索・置換と
  作業フォルダの全文検索の両方に適用する。全角と半角、カタカナとひらがなは区別する。
- 作業フォルダ全体を全文検索できる。
- **検索するフォルダを指定できる**（2026-09-07追加）。既定は作業フォルダで、
  Explorerの行の「Search in This Folder」か、検索パネルのフォルダ名を押して選ぶ。
  **絞っていることは常に見えていなければならない**——見つからない言葉の理由が
  画面のどこにも無いことになる。指定はセッションへ入れ（8.5）、**作業フォルダを
  開き直したら外す**（前のフォルダの中を指したままにしない）。
- Markdown見出しからアウトラインを自動生成する。
- アウトラインから該当セクションへ移動できる。
- 通常リンクのリンク先を別の編集ペインで開ける。
- 最近開いたファイルの履歴を提供する。
- **編集ペインごとに、見てきた文書を戻る・進むで辿れる**（2026-09-07追加）。マウスの戻る／進むボタンは、そのペインの中なら紙の外の余白でも効く（2026-09-16）。
  - 同じファイルでも別TABは別の移動先として記録する。開いているTABへ戻るときは、そのTABのViewer状態・表示位置を維持する。
  - 本文上ではマウスの戻る／進むボタンでも操作できる。ポインタがある編集ペインの履歴を使い、Alt+←／→と同じ動作とする。
  ブラウザと同じ手つきで、覚えるのは直近16か所。**記憶するのは文書であってタブ番号ではない**
  ——仮のタブは次のファイルに置き換えられるので、戻る先のタブはたいてい残っていない。
  タブが無ければ仮のタブとして開き直す。端末のタブは場所として数えない。

#### 探すときの手つき（2026-09-11追加、追加要件 E1・E4 から転記）

- **打っているあいだ、件数と現在位置を出す**（`「語」3 / 12`。場所は 6.6）。
  **打っているあいだはカーソルに触らない**——変換中に本文が飛ばない。
- **一致箇所に色を付ける。**現在の一致は強く、ほかは薄く。
- **`F3`／`Shift+F3`は本文からも効く**（帯は開いていなくてよい。最後に探した語で動く）。
  帯の中では`Enter`／`Shift+Enter`が同じ操作。
- **探し方は3つ**——大文字・小文字を区別／単語単位／選択範囲内。既定は区別しない。
- **正規表現**（2026-09-10、書き手の求めで前倒し）。
- **検索語・置換語の履歴**を`↑`／`↓`で辿れる。
- **語・置換語・帯の言うことは、面ごとに持つ**——帯はその文書を開いている面の中に出るの
  だから、語もそこにある（2つの面で別の語を探せる）。
- **`Ctrl+G`で行へ移動する**（追加要件 E4）。ペイン上部のメニュー「Go to Line…」にも入口を置き、そのペインの文書を対象にする。Terminalには表示しない。**無い行では動かない**。横書きと縦書きで
  同じソース位置へ着地し、折り返しの表示行と論理行を混同しない。

#### 検索対象の除外と結果の表示（E5）

検索パネルに名前パターンを入力する。`;`区切り、`*`は任意文字列、`?`は1文字。大小文字を区別しない。各階層のファイル・フォルダ名に適用し、末尾`/`はフォルダだけを対象とする。例：`*.bak;backup/`。フォルダは中を探索せず、除外対象は検索ファイル数上限に数えない。Explorerは非表示にしない。条件はアプリのセッションに保存し、検索範囲変更後にも適用する。編集時は古い結果を破棄し、新しい検索操作で反映する。

検索パネルの右端をドラッグして最大1200pxまで広げられる。結果は行の全文を保持し、表示幅を超える部分だけ省略する。マウスオーバーで行の全文を表示する。

### 7.8 ルビ・傍点・縦中横（2026-09-08追加）

縦書きで日本語の文章を書く人にとって、**これは「あると嬉しい機能」ではなく、無いと原稿に
ならない機能**である。小説を書く人を先頭に置くと決めた（3）以上、初期版の対象に入る。

- **記法は青空文庫／なろう式とする。**ルビは`｜漢字《かんじ》`、親文字が漢字の連なりで
  明らかなときは`漢字《かんじ》`。**採る理由は互換性である**——投稿サイト（カクヨム・なろう）と
  青空文庫がこの形をそのまま読むので、**書き手が別の道具へ移っても原稿が生きている**。
  §3の「Markdownテキストを唯一の原本とする」がそれを要求している：独自形式へ逃げない。
  HTMLの`<ruby>`は採らない——原稿がタグだらけになり、投稿サイトが読めない。
- **傍点**（圏点）も同じ理由で青空文庫式（`《《強調》》`、または注記形式）とする。
  **どちらの綴りを正とするかは実装時に決める**（15）——投稿サイトごとに揺れがあるため、
  実際に使われている形を調べてから決める。
- **縦中横の注記で置けるのは4字まで**（2026-09-21、RFN01-38）。1マスに収めるので、
  字が増えるほど1字が小さくなる——読めなくなる前に止める。上限は**自動の規則が1マスに
  収める最大**（`12`と`!?`で4字）に合わせた。5字以上の選択では挿入の行を出さない。
  **原稿に既にある長い注記はこれまでどおり読む**——書き手が書いた指示を黙って捨てない。
- **印の種類を注記が名指す**（2026-09-16、書き手「組版の表現拡大」）。`［＃「…」に傍点］`は点、
  `丸傍点`は●、`白丸傍点`は○、`二重丸傍点`は◎、`ゴマ傍点`はゴマ点、`×傍点`は×。`《《…》》`は点。
  **`［＃「…」に傍線］`は線**——Markdownに下線が無いので縦書きの原稿では傍線が使われる。
  縦書きは字の右、横書きは字の下（下線）に引く。知らない注記はこれまでどおり本文のまま残す。
- **体裁の注記のうち、字下げを読む**（2026-09-16、書き手「組版の表現拡大」）。
  `［＃ここから2字下げ］`から`［＃ここで字下げ終わり］`までの行と、行の頭に置く`［＃2字下げ］`
  （その行だけ）。手紙文・詩・引用のために、小説の原稿で実際に使われる形である。
  **字下げは引用や箇条書きと同じ仕組み**（ブロックの字下げ）に載せ、数える単位は**字**にした
  ——引用と箇条書きの1段は2字、注記は書いてある字数そのもの。指示だけの行は`---`やフェンスと
  同じく字を隠すが、**行そのものは残る**（1行ぶんの空きになる）。ルビと同じ旗で切れる。
- **地付きと改ページも読む**（2026-09-16）。`［＃地付き］`と`［＃地から2字上げ］`は、その行を
  行末へ寄せる（署名・結び・詩の行末ぞろえ）。**寄せ方はブロックの性質**なので、変わるところで
  ブロックが切れ、行の箱をそのぶん短くして終わりへ寄せる。`［＃改ページ］`は**画面では紙を
  切らない**（編集面は続いた1枚、要件 7.10）ので、そこに切れ目があることを破線で見せるだけ。
  紙にするとき（印刷）はこの行でページを改める。
- **行と行のあいだは、ルビの帯が入る広さにする**（2026-09-16、書き手の決定）。行送りの下限を
  ルビの厚みぶんまで上げ、広げたぶんは帯の側（横書きは上、縦書きは右）へ寄せる。**帯が行の箱に
  収まる**ので、段落の先頭の行でも読みが切れない。既定の設定では列の送りが約12%広がる。
  ルビを読まない設定では、この空きも取らない。
- **縦中横**：縦書きの中で半角数字2桁などを正立・横並びにする。**書き手が指示しなくても
  2桁までは自動で縦中横にする**——「20歳」が「二〇歳」でも「2
0歳」でもなく「20歳」と
  見えるのが、縦書きの原稿として当たり前の姿である。3桁以上は縦に並べる。
  **ただし既定は切**（2026-09-15、書き手の決定）。Layoutの縦書きシートの`Tate-chu-yoko`で入れる。
- **半角記号の縦中横**（2026-09-17、書き手の決定）。`!?`・`!!`・`⁉`・`‼`などを1マスに正立・
  横並びにする。**小説の原稿にいちばん多く出る形**がこれで、横倒しのままでは原稿にならない。
  **旗は半角数字と分ける**——「半角数字が縦に並ぶ違和感」と「`!?`が縦に並ぶ違和感」は別のもの
  なので別々に入切する。置き場所は`Tate-chu-yoko`の隣、既定は数字と同じく切。
- **縦中横を名指せる**（2026-09-17）。`［＃縦中横］…［＃縦中横終わり］`。自動の規則が拾わない
  ものを立てるための口で、**旗の入切に関わらず効く**（書いてあるものは書いてあるとおりに組む）。
  1マスに入らない字数は、枡目からはみ出さないよう縮めて収める。
- **傍線の種類**（2026-09-17、書き手「組版の表現拡大」）。`二重傍線`・`波線`・`鎖線`・`破線`を
  傍点の種類と同じ作りで読む。**線は範囲ごとに1本引く**——刻みのある線を字ごとに引き直すと、
  字の境目で必ず模様が切れる。
- **左の注記**（2026-09-17）。`［＃「…」の左に「…」の注記］`で、**右にルビ、左に注**の二重ルビに
  する。帯の仕組みは1つで、出る側だけが逆（縦書きは字の左、横書きは字の下）。**親文字の字間を
  動かすのは右のルビだけ**——両側から押し合えば字送りの持ち主が2人になるので、長い左の注は
  前後へかけるにとどめる。
- **割注**（2026-09-17）。`［＃割り注］…［＃割り注終わり］`で、1行の中に**半分の大きさで2行**。
  箱が場所を取り、中の字は箱が隠す（縦中横と同じ作り）。割れ目は枡目の数で半分、余りは前の行へ。
  箱が取る長さは長いほうの行が決める。
- **文字の大きさの注記**（2026-09-17）。`［＃小さな文字］`・`［＃大きな文字］`は本文の3/4と3/2。
  **その走りの大きさに掛かる**ので、見出しの中なら見出しの字に対しての比率である。箱ではなく
  太字と同じ旗なので、中は普通の本文としてそのまま入れ子になる。
- **整形表示ではルビとして組み、原文にはそのまま出す。**7.3.1の規則（原文には飾りを付けない）
  をそのまま適用する——原文では`｜漢字《かんじ》`がそこにあり、それが本文である。
- **本文文字数（10）がルビを数えるかどうかは、設定で選べる**（2026-09-08、書き手の指摘で
  改めた。初期値は数えない）。**どちらが正しいかは書き手の用事で決まる**——投稿サイトへ出す
  ための字数はルビを含めず、打ち込んだ量を知りたいときは含む。編集器がどちらかに決めると、
  もう一方の用事の人が毎回数え直すことになる。**選べる設定は増やさない方針だが、これは
  「正解が二つある」たぐいなので選ばせる。**
- **長い読みは前後の仮名へ1字までかけ、それでも余るぶんは親文字の字間を広げる**（2026-09-16、書き手「組版の表現拡大」）。かけてよいのは仮名まで——漢字や記号の上にかけると、その字が何の読みを持つのか分からなくなる。かける先に別のルビが立っていればその側へはかけない（読みどうしが重なる）。読みは親文字の中央に置く。
- **ルビの字の大きさと位置は表示設定（9）に入れる。**親文字に対する比率で持つ——シートごとに
  持つ（縦書きと横書きでルビの置き場所そのものが違う）。
- **この記法を読むかどうかは、切れる**（2026-09-11追加、追加要件 E9 から転記）。
  **Markdownの標準ではない**ので、切れなければ「この編集器でしか正しく見えない書き方」を
  書き手に強いることになる。切ると`｜漢字《かんじ》`も`《《傍点》》`も`［＃…］`も
  **そのまま字として出る**（原稿のバイト列は変えない——読むのをやめるだけである）。
  **ルビと傍点は1つの旗で切る**（同じ記法の一族）。既定は読むほうで、設定は 9 の「読み方」。
  **縦中横は組み方なので紙のシートにある**（9）。

### 7.9 軽い校正（2026-09-08追加）

**この編集器がいちばん力を入れるところ**（3）。書くことと直すことは同じ手つきの中にあり、
**直すために別のモードへ入ったり、別の窓を開いたり、原稿を別の形式へ出したりしない。**
校正の道具がそれを要求した時点で、それはもう軽くない。

- **単語チェックモード**（書き手の求め、2026-09-08）。**語の一覧を作っておくと、本文の中の
  その語が指定した色で出る。**考え方はソースの予約語の色分けと同じで、**モードが語群を持ち、
  語群が色を持つ**。モードは文書ごとに選ぶ。
  - **要件は[単語チェックモード要件](単語チェックモード要件.md)にある**（2026-09-08に切り
    出した）。**辞書の構造・保ち方・出し入れの形式・切り替え・色の順位**まで噛み合っている
    ので、この章の1項目では収まらない。**あちらが正**である。
  - **これは検索の言い換えである。**校正とは同じ語を何度も探すことで（7.7）、
    **単語帳は「毎回打ち込んでいた語を、置いておく場所」**にあたる。だから新しい概念では
    なく、既にやっていることの手数を減らすものである。
- **表記ゆれの自動検出は持たない。**辞書と正規化の規則が要り、外したときに「当たらない
  指摘を消す」作業が生まれる。**単語帳で足りる**——書き手は自分が何を揺らすか知っている。
  **編集器はどれが正しいかを判定しない。**
- 文書情報（10）は書きながら見えているもので、これも校正の道具である。

### 7.10 紙の形で見る——印刷（2026-09-08追加、2026-09-16に「縦書きPDF」から書き直した）

**納品のためではなく、自分の原稿を紙の形で確かめるため**である（3）。画面で読むのと紙で
読むのとでは気づくものが違う、という一点のためだけに持つ。

- **縦書きのままWindowsの印刷へ出す**（2026-09-16、書き手の決定）。**PDFはその中の一つの
  プリンタ**（「Microsoft Print to PDF」）であって、**PDF専用の入口は持たない**——道が同じ
  ものに入口を二つ作らない。はじめ「縦書きPDF」と書いていたのは、PDFを自分で書くつもりで
  いたころの名前である。**PDFを自分で書かない**：字の埋め込みも縦書きの字形の差し替えも
  Windowsの側に既にあり、そこを自作すれば画面と紙で違う原稿になる。
- **画面の組版と同じ結果を出す。**DirectWriteが既に縦組みをしているので、**別の組版器を
  持ち込まない**。二つ持てば、画面と紙で違う原稿になる。**描く先が紙になるだけで、描く処理は
  画面と同じものを使う。**
- **ページという概念がここで初めて入る**（用紙の大きさ、四辺の余白、ノンブル）。
  編集画面は無限に続く1枚として組んでおり（14「ページ単位の縦書きレイアウト」は対象外の
  まま）、**紙にするときだけページに切る**。この切り方は表示設定（9）とは別に持つ。
  `［＃改ページ］`（7.8）がここで効く。
- **印刷プレビューを持つ**（2026-09-16、書き手の指摘）。プリンタは刷る前に何も見せないので、
  プレビューが無ければ**用紙と余白が合っているかを刷ってから知る**ことになる。
  **プレビューはページに切る仕事そのもの**であり、そこに映る1ページと、プリンタへ送る
  1ページは、同じ処理が描いた同じ絵である。
- **紙は繰って見る。**ホイールでも矢印でもボタンでも動く。**Ctrl+ホイールで縮めれば、並ぶ
  枚数が増える**（2026-09-17、書き手の求め）。**並ぶ向きは本文の流れと同じ**——縦書きは右から
  左へ横に、横書きは上から下へ縦に並ぶ。**枚数は大きさに合わせる**（枚数で頭打ちにすると、
  入るのに出さない紙が出て並びが欠けて見える）ので、決めるのは**縮められる下限**のほうで、
  6枚が並ぶ大きさで止める。**大きくするほうは紙がはみ出してよい**（2026-09-17、
  書き手の決定：「ルビなど大きく見たい場合もある」）——**はみ出したぶんは掴んで動かす**。
- **天地の余白に字を入れられる**（2026-09-17、書き手の求め）。天と地それぞれの**左・中・右**、
  6つの欄に**自由に書ける**——決まったものを選ばせる形では、1か所に1つしか入れられない。
  `{name}`・`{date}`・`{page}`・`{pages}`と書けばその紙の値に置き換わるので、
  「第一稿　{page} / {pages}」のように**並べて書ける**（大小も日本語も通る）。
  既定は**地の真ん中にノンブル**（「{page} / {pages}」）——何枚目かは**総数も出す**。あと何枚かが分からないと、紙の束を
  見失う。
- **用紙・向き・部数・プリンタはWindowsに訊く**（9の色と同じ理由——Windowsが訊けば、他の
  Windowsのプログラムと同じやり方で選べる）。**余白とノンブルはこの編集器が持つ**
  ——Windowsの設定と二重に持たない。**用紙の大きさと向きは、プレビューの「用紙…」から
  Windowsのプリンタ設定画面で決める**（2026-09-16、書き手の問い「A4などとLandscapeの設定が
  どこで反映されるかわかりません」）。この編集器に用紙の一覧は持たない。
- **紙の行の長さは紙の寸法である**（2026-09-16、書き手の指摘）。用紙から四辺の余白を
  引いたぶんがそれで、**字数では決めない**——「字数は英数字だとかなりちがいますし、禁則文字も
  あるため一意に決められません」。半角の字は送りが違い、禁則で追い込み・追い出しも起きるので、
  「1行◯字」という長さはそもそも存在しない。**字体と大きさを決め、その幅で組ませる**のが組版で
  あり、1行に何字入るかはその結果である。
  - **画面の行の長さを紙へ持ち込まない。**画面の幅はペインの幅であって、原稿の寸法ではない。
- **画面が見せているものを刷る**（2026-09-16、書き手の指摘「ソースで印刷を選択したら、
  ソースのまま印刷されるのが正しいと思います」）。整形表示のTABは組まれた姿で、**ソースの
  TABは原文のまま**——記号が字としてそこにあり、見出しも本文と同じ大きさ・同じ字体で出る
  （画面のソース表示と同じ規則）。**紙にするときに整形し直さない。**
- **画面で設定したとおりに刷る**（2026-09-16、書き手の決定「まず、画面で設定したフォントが
  印刷できるのが一番いいです。そうすることで、画面のイメージに近く出力できるからです」）。
  本文・コード・見出し6段の**字体はそれぞれのものがそのまま**紙へ行き、**見出しの色も行く**
  ——紙のために1つの字体へまとめない。行送り・字間・見出しの倍率・ルビ・縦中横も同じで、
  どれも「その原稿をどう組むか」という書き手の決めごとである。
  - **落とすのは、紙が自分で持っているものと、編むための印だけ**：紙の色（紙は白い）、
    行番号、空白の印。
  - **本文の大きさだけは、紙のものを持てる**（任意。既定は画面のまま）。**見出しは倍率で
    付いてくる**ので、動かすのは本文の1つで足りる。画面の拡大（Ctrl+ホイール）は紙には
    効かない——拡大は読むためのもので、刷る大きさではない。
  - **本文の大きさの既定は10.5pt**（Wordが11pt、Excelが10.5pt）。**紙に置いて普通に見える
    大きさ**を既定にし、画面で読む大きさは拡大の既定（150%）が持つ。
- **組版の細かさは追わない。**禁則・追い込み・追い出しは画面の組版がしているぶんだけで、
  紙のためにそれ以上を足さない。**入稿できる品質を目指さない**——そこはDTPの仕事である。
- EPUBとWordは対象外のまま（14）。

### 7.11 比較とMerge

#### 比較の入口

| 入口 | 本文（左） | 比較対象（右） | 採用・反映 |
| --- | --- | --- | --- |
| Paneメニュー「別のファイルと比較…」 | 起点TABの未保存変更を含む本文 | 選択ファイルの保存内容 | 編集可能なTABで利用可能 |
| 外部変更の「外部版と比べる」（8.3） | 編集中の本文 | 同じファイルの外部版 | 編集可能なTABで利用可能 |
| Paneメニュー「保存版と比較」（E7） | 起点TABの本文 | 同じファイルのディスク上の保存内容 | 比較専用 |
| Paneメニュー「前回のCommitと比較」（2026-09-15追加） | 起点TABの本文 | 同じファイルのGitのHEADの内容 | 比較専用 |

- 左右とも比較開始時点の内容を保持する。右は過去の保存履歴ではなく、取得時点の内容。外部で変更されていれば、その変更も比較対象になる。
- 別ファイルの文字コードは通常の読み込みに従い、同じファイルの保存版・外部版は文書の文字コード設定で読み込む。
- 保存版との比較では対象選択を省略する。保存先がない場合は保存後の比較を案内する。
- 前回のCommitとの比較も対象選択を省略する。書き手のPCの`git`を呼び、Gitが無い・管理下にない・Commitが無い・そのファイルがCommitに無い場合はステータスに理由を出し、本文とTABを変えない。内容は文書の文字コード設定で読む。
- Viewerからも比較できる。Terminalには比較の入口を表示しない。
- 対象選択のキャンセル・読み込み失敗では本文やTABを変更しない。

#### 共通の比較表示

- 同じウィンドウ内を左右比較へ切り替え、元の編集画面を保持する。「比較を終了」またはEscで元のPane配置・TAB・表示位置へ戻る。比較表示内の本文は直接編集しない。
- 横書きの原文を行単位で揃える。左のみを赤、右のみを緑、変更を黄で表示し、対応行がない側は空欄にする。各側の元の行番号を示す。
- 次／前の差分と、左右どちらかの差分行のクリックで選択する。選択した差分の先頭行は、背景より手前に描く左右共通の枠と左端の目印で示す。同じ内容の行をクリックしても選択は変えない。
- 移動ボタンの有効時のマウスオーバーは薄紫の背景と紫の枠。移動先がない方向は無効化して淡く表示する。差分なしでは両方無効にする。
- 上下・左右のスクロールは共通。表示内の差分選択ではスクロール位置を維持する。末尾改行の有無、差分なし、広い変更をまとめて計算した場合も分かる表示にする。
- 左右の全文をコピーできる。表示用の行番号・改行印は含めない。比較・コピーだけでは本文・保存・Undo・自動退避・保存基準・外部変更警告を変更しない。

#### 差分の採用と反映

- 採用可能な比較では、差分の先頭行に左右一組のチェックボックスを置く。初期状態は左をチェックし、左右どちらか一方を採用する。選択済み側の再クリックでは維持する。
- ヘッダの「左を残す」「右を採用」は現在の差分へ作用し、チェック状態と同期する。複数行の差分はまとめて扱う。
- 「選んだ差分を本文（左）に反映」でまとめて取り込む。未選択は左のまま。反映前の終了・Escでは選択を破棄し、本文を変えない。
- 反映全体を一回のUndoで戻せる。反映はファイル保存・保存基準の更新を伴わず、外部変更警告も保持する。
- Viewer・読み取り専用文書では取り込めない。比較開始後に元本文や対象TABが変わった場合は反映を拒否し、比較の開き直しを案内する。文字数上限を超える反映も拒否する。

## 8. 保存、復元、外部変更

### 参照先が見つからない場合

- 元のファイルが見つからない場合は警告し、外部版とは比較できない旨を表示する。移動先の推測や、手動指定による参照先の付け替えは行わない。
- 未保存本文・Undo・TAB・元の参照先を保持する。必要な本文は「別名で保存…」で残せる。「キャンセル」では何も変更しない。
- 通常の保存時も警告し、元の場所へ無断でファイルを再作成しない。すべて保存では該当文書を保留する。

### 8.1 作業内容の自動退避

- 設定Generalでの表示名は「自動退避」。On／Offの直下に退避の説明を置き、「元ファイルへの反映は Ctrl+S」と明示する（U1、2026-09-13）。

- 編集中の内容は、既存ファイルか新規文書かを問わず、アプリ専用領域へ作業コピーとして自動退避する。
- 作業コピーの自動退避では、作業フォルダ内の元ファイルを上書きしない。
- アプリを終了または再起動しても、明示的に保存していない編集内容を復元できるようにする。
- アプリやOSが異常終了した場合も、最後に退避できた作業内容から復元できるようにする。
- 作業コピーは作業フォルダの外にあるアプリ専用領域で管理し、Editor独自の管理ファイルを作業フォルダへ作成しない。
- 入力停止から2秒後に作業内容を退避する。
- 入力が継続している場合も、最後の退避から最大5秒以内に再度退避する。
- **終了は退避のあと**（2026-09-08追加）。窓を閉じるとき、セッション（8.5）と下書き（12.4）だけ
  でなく**本文の退避も実行し、書き込みが終わるのを待ってから終わる**。退避は入力停止から2秒・
  継続中も5秒ごとで、その時計は窓が閉じれば止まる——打った直後に`×`を押したぶんが、
  どこにも書かれないまま消えていた。**別スレッドへ渡しただけでは足りない**（要件2）：
  乗せたことと書き上がったことは別である。
- **退避に失敗したら、知らせて、間を置いて試し直す**（2026-09-08追加）。渡した時点で
  「退避済み」にしていたので、書けなかった一回はそのまま忘れられ、次の打鍵が無ければ
  その文書は二度と退避されなかった。失敗は画面に出す——要件8.1は書き手への約束なので、
  守れていないことは書き手が知っていなければならない。
- **自動退避を切れる**（2026-09-08追加）。設定のGeneralに置く（9）。既定はOn——
  何も言っていない書き手は、未保存の中身を捨ててくれとは言っていない。
  - **Offにすると、置いてある作業コピーも消える。**「切っている間は書かない」だけだと、
    切る前に退避した古い姿が次の起動で戻ってきて、書き手の言ったこととちょうど逆になる。
  - Offの間は復元もしない（8.5）。**セッションそのものは残す**——どのファイルを開いて
    いたか、ペインをどう分けていたかは「未保存の中身」ではない。戻ってきたタブは
    保存済みの中身で開く。
  - **未保存のタブを閉じるときの問い（8.4）から「作業コピーを残して閉じる」が消える。**
    退避していないのだから、残すものが無い。
  - **窓を閉じるときも訊く**（2026-09-08追加）。すべて保存して閉じる／破棄して閉じる／
    キャンセル。**Onのときは訊かない**——閉じても失われないので、問いは邪魔でしかない。

### 8.2 明示的な保存

- 元ファイルへの反映は、ショートカットまたは画面操作によって利用者が明示的に保存した場合だけ行う。
- 既存ファイルで外部変更との競合がない場合、`Ctrl+S`は確認を表示せず元ファイルへ即時上書きする。
- 「名前を付けて保存」は、通常の上書き保存とは別の操作として提供する。
- 新規文書で`Ctrl+S`を実行した場合は、保存先とファイル名を指定する画面を表示する。
- 保存前の確認は、元ファイルに外部変更があり、作業コピーと競合する場合だけ表示する。
- 保存処理は一時ファイルを利用し、失敗時に元ファイルを壊さない。
- UTF-8を基本とし、BOMや既存の改行コードを不用意に変更しない。
- 元ファイルへの保存が完了した後、その内容を新しい保存基準として扱う。
- 保存済みの内容と同一になった不要な作業コピーは、安全に削除する。
- Undoで保存済みの本文まで戻った場合も、書き込み待ちの退避と同じ順序で古い作業コピーを削除する。元ファイルが外部変更・参照先不明の場合は現在の本文を退避して保持する。
- すべて保存で無題文書の保存先を別のTABが開いている場合は、その保存を保留して知らせる。個別の「名前を付けて保存」で確認・文書の統合を行う。

### 8.3 外部変更

- 別のアプリによるファイル変更を監視する。
- 未編集のファイルが外部変更された場合、読むだけの面（ReadOnly・Viewer）で開いていれば、安全に再読み込みする。
- **編集モードでは、未編集でも自動で取り込まない**（2026-09-15、書き手の判断）。知らせを1度出して「⚠ 外で変更」の印を立て、書き手が読み直しを指示するまで取り込まない。書き足しが続いても知らせは繰り返さない。未保存の変更が無い文書で印を押すと「外部の変更を読み込む」「ReadOnlyモードで読む」「キャンセル」を選べる（比較はPaneメニュー「保存版と比較」で同じことができるため出さない）。未保存の変更がある文書は従来の問いのままで、ReadOnlyは出さない。
  - 書き足されただけの読み直し（新しい内容が元の内容で始まる）では、どの表示モードでもカーソル・選択・表示位置を保つ。内容が丸ごと替わった場合は従来どおり先頭へ戻す。
- 編集中のファイルが外部変更された場合は、勝手に上書きせず通知する。
- 作業コピーがある状態で元ファイルも変更された場合は、作業コピーによる上書き、外部変更の読み込み、別名保存、キャンセルを選択できる。
- **作業コピーは「どの版に対して書いていたか」を持つ**（2026-09-08追加）。復元は元ファイルを
  読み直すので、それだけでは*その時の*ファイルが「合意した姿」になり、**閉じているあいだの
  外部変更が無かったことになる**（未保存のまま終了→別のアプリで元ファイルを変更→再起動→
  保存、で確認が出ない）。退避した時点の更新時刻と長さを作業コピーへ入れ、復元時に戻す。
- **外部変更は、保存のときではなく気づいたときに知らせる**（2026-09-11追加、書き手のレビュー
  S2。**①通知・②外部版タブ・③差分表示・④手動取り込みまで実装**）。長く書き続けているあいだに外の原稿も変わっていると、保存時に「上書きか
  読み込みか」を選ぶだけでは、**どちらかの変更を必ず捨てる**ことになる。
  - 編集中の文書で外部変更を検知したら、**保存を待たず、邪魔にならない常設の表示で知らせる。**
  - **本文と自動退避は保持し、執筆は続けられる。**外部版を無断で本文へ適用しない。
  - **「外部版と比較する」入口を設け**、双方の内容・差分を確認して、必要な変更を手で取り込める
    ようにする。**比較だけではどちらも上書きしない。**
  - 競合が解決するまで、元ファイルへの**無確認の上書きを防ぐ**。
  - **最初の範囲は「双方を保持した比較と、手での取り込み」**。自動マージは必須にしない。
    編集開始時の本文・現在の本文・外部の本文を使う三者比較は将来の候補である。
  - E7（保存版との差分）と対象が違う——**保存基準と、現在の外部版を区別して表示する。**
  - **左右比較への統合（2026-09-13）**：未保存の変更がある文書の「⚠ 外で変更」または保存時の問いで「外部版と比べる」を選ぶと、要件7.11の比較画面を表示する。
    左は未保存変更を含む現在の本文、右は「外部版・取得時点」。元文書の読み方（指定文字コード・BOMの優先）を引き継ぐ。
    内容は比較開始時点で固定し、再度比較すると最新の外部版を取得する。比較用TABは追加しない。
    行を揃えた差分、差分移動、連動スクロール、左右の全文コピーを使う。比較表示では本文を編集しない。
    「比較を終了」またはEscで元のTAB・Pane配置・表示位置に戻る。本文・保存基準・外部変更の警告は変更しない。
    読み込み失敗ではエラーを表示し、元の本文と表示を維持する。差分単位の取り込みは7.11に従う。反映後も外部変更の警告を保持する。
- **答えを待つあいだも、頼まれた保存は形を持っている**（2026-09-11追加、書き手のレビュー）。
  外部変更の確認をはさんでも、**名前を付けて保存で選んだ文字コードと改行コードは落ちない。**
  どの文書へ書くかも、訊く前に決まっている（答えが来るころには、書き手は別のタブにいるかも
  しれない）。
- **書き手が言った文字コードは、読み直しても引き継ぐ**（2026-09-11追加、書き手のレビュー。
  4.3）。**ただし印（BOM）があればそちらが正**——外のアプリが別の形で書き直したのなら、
  言われた読み方はもう古い。

### 8.4 未保存バッファ

- 新規文書は、名前を付けるまでアプリ専用領域へ作業コピーとして自動退避する。
- 複数の新規バッファを「無題1」「無題2」のように保持できる。
- アプリを終了しても、新規文書を破棄しない。
- 新規文書を正式に保存できた後、一時バッファを安全に削除する。
- **新規文書のタブを明示的に閉じる場合は、「名前を付けて保存／破棄／キャンセル」から選ぶ**
  （2026-09-12実装、書き手のレビュー S1）。**「作業コピーを保持して閉じる」は設けない**——閉じるたびに見えない作業コピーが
  残ると、**残した文書を把握できなくなる。**
  - **空の新規タブは、確認なしで破棄する。**
  - 破棄したメモは「戻る／進む」の履歴からも外す。作業コピーを削除できなければ、本文とタブを残して知らせる。
  - **アプリ全体の終了時は、開いている新規文書を退避して、次回起動時に同じ文書として復元する**
    （自動退避Onの場合）。**アプリを終わることと、文書を閉じることは別の操作**である。
  - 同じ文書の自動退避は**同じ作業コピーを更新する**（退避のたびに別のメモを作らない）。
  - 正式なファイルへの保存が成功したら、不要になった退避コピーを削除する。**保存失敗・
    キャンセルでは本文とタブを保持する。**
  - **本文のあるメモを、期限や件数だけで自動削除しない。**書きかけを残すなら、タブとして
    見える状態にする。
  - 同一文書を複数ペインで開いている場合は、**最後の表示を閉じるときに**文書を閉じる判断をする。
  - **自動退避を切ってあるときの終了確認は、これまでどおり**（8.1）。

### 8.5 セッション復元

- 作業コピーの本文を保存基準にしない。元ファイルが退避時と同じ版なら、そのファイルの本文を基準にする。外部変更・参照先不明などで元の保存本文を確定できなければ、保存基準は不明として未保存状態を維持する。
- 復元時に元ファイルを開けなくても参照先を保持し、文書ごとの退避先を維持する。複数の文書を同じ無題番号へ変換しない。

次回起動時に、以下を復元する。

- 最後に開いていた作業フォルダ
- **ウィンドウの位置と大きさ**（2026-08-27追加）。最大化されていたかどうかも含める。
  位置と大きさは**最大化・最小化されていない最後の姿**を覚える——最大化した大きさで
  復元された窓は、元に戻せない
- 開いていたタブとアクティブなタブ
- 編集ペインの分割構成と各境界の位置
- 元ファイルへ未反映の作業コピーと未保存バッファ
- カーソル位置、選択範囲、スクロール位置。**スクロール位置は画素ではなく、
  表示の先頭にあった本文位置で覚える**（2026-08-27改訂）——幅・拡大率・表示モードが
  変われば画素は別の場所を指すが、本文位置は同じ箇所のままである
- 各タブと各編集ペインの表示モード
- ファイルツリーの展開状態

## 9. 表示設定

- **設定はTABで開く**（2026-09-14、書き手：「設定を押すと、新たなダイアログを出さずに設定Paneとして
  TABで開く。設定カテゴリーも左側に縦に並べる」）。ダイアログや画面内パネルは出さない。
  - フォーカス中のペインに「Settings」のTABとして開く。**窓に1つ**で、既に開いていればそのTABへ移る。
    前に出ているのがNew Tabなら、そのTABが設定になる。
  - 群（General〜Keys）は左に縦に並べ、選んだ群の中身を右に出す。どの群を見ていたかは窓が覚える。
  - 設定のTABに本文は無い。字は入らず、保存・置換・Undoなど本文に効く操作は効かない。
    TABを閉じる・移る、ペインを移る、ファイルを開く、Quick Draftは効く。
  - **設定を探す**（2026-09-15、書き手：「Settingで検索欄が開いていても検索が効きません。可能なら、検索した文字の
    含まれる設定のみがリストされると良い」）。設定のTABで`Ctrl+F`を押すと検索欄が「設定を探す」になり、
    打った語を**全部の群から**探して、名前か見出しに語を含む行だけを出す（見出しに一致すれば、その見出しの行は全部）。
    大文字小文字・全角半角は問わず、表示している言語の名前で探す。探しているあいだは見出しに群の名前を付け
    （「Text — HORIZONTAL」）、群ごとのResetは出さない。Wordsの群は丸ごと出すか出さないか。語の数え方・前後の移動・
    置換の欄は出さない。欄を空にするか閉じれば、選んでいた群の表示に戻る。
  - セッションには残さない（Terminalと同じ）。閉じたTABの履歴にも入れない。
- **横書きと縦書きのシートは切り替えず、上下に並べて全部出す**（2026-09-15、書き手）。Text・Layout・Pageの各面にHORIZONTAL、VERTICALの見出しで並ぶ。
- **文字の大きさは打って決められる**（2026-09-15、書き手）。本文はpx、見出しは本文に対する%の欄に数を直接打つ。隣にptの欄を置き、どちらを打っても片方が追従する（1pt = 96/72px）。確定はEnterか欄を離れたとき。範囲外は範囲内へ収め、全角数字も読む。
- **Resetはその面の値だけを戻す**（2026-09-15、書き手）。Text（本文・見出しの大きさ・色・書体・装飾、コードの書体）、Layout、Page（紙の色）、Terminal、Keysがそれぞれ持つ。**Generalの「Reset All」**は確認のうえ、General・Terminal・Text・Layout・Page・Keysを既定に戻し、文書ごとに選んだ単語チェックモードを「なし」にする。単語帳とモードそのもの、文字色のセットは残す。
- **コードの書体（Code Font）はTextの面に置く**（2026-09-15、書き手：Pageにあるのはおかしい）。
- 背景は、ボタンから開くパレットの一番上の「Paperに合わせる」か、パレットの色から選び、適用する背景色と「Paper／指定色」を一つのボタン内に表示する。Paperに合わせる場合はPaperの変更に追従する（2026-09-13追記、2026-09-15改訂）。
- **色はアプリ内のパレットで選ぶ**（C5、2026-09-15、書き手：「Windows標準は小さくわかりにくいので、MS-Officeの形式がいい」）。文字色・背景色・紙の色・Terminalの地と字・語群の色が同じパレットを使い、Windowsの色ダイアログは使わない。
  - 並びはOfficeと同じ：一番上に「既定の色」（背景は「Paperに合わせる」、語群には無い）、テーマの色（Office既定テーマの10色と明るい3段・暗い2段）、標準の色10色、最近使用した色（新しい順に10色、設定ファイルに残す）、「その他の色…」。色を押せば決まって閉じる。
  - 「その他の色…」は色面と色相の帯、`#`16進とR・G・Bの欄で色を作る。新規と現在を上下に並べ、OKで初めて反映し、キャンセル・Escでは変えない。
- **TAB毎・Pane毎の紙の色**（2026-09-15、書き手）。全体の紙の色（Page）とは別に、**本文の紙の上の右クリック「Background…」でそのTABの紙**、Paneメニュー「Pane Background…」でPaneの紙をパレットから選ぶ（書き手の求めで、TABの右クリックはTABの色に譲った）。**優先はTAB > Pane > 全体**。**横書き・縦書きの両方に同じ色**が付く（書き手の判断 2026-09-15。向きで別々に持っていたときは、横書きのTABでPaneの色を変えても縦書きのTABが塗り変わらなかった）。**上の段の色を変えると下に付けた色は外れる**：Paneの色を変えればそのPaneのTABの色が、全体の紙の色（Page、既定に戻すを含む）を変えればすべてのTAB・Pane（閉じたTABも）の色が外れる。パレットの「既定の色」で外すと下の段（TABならPane、Paneなら全体）の色になる。**ランダムは全体設定**（Settings → Page「Random Background」：Off／Light／Dark、書き手の求め「おまけなので全体設定でON/OFF」）。Onなら**新しく開いたTAB**に淡色または濃色のランダムな紙を縦横同じ色で付ける。既に開いているTAB、Terminal・Settingsには付けない（同じ文書の別TABは新しいTABとして付く）。TAB・Paneのメニューからは選ばない。TABの色はTABが残っている限り（別のPaneへ運んでも付いて行く。**同じ文書の別TABは新しいTABなので引き継がない**）、Paneの色はPaneが残っている限り付き、セッションで再起動後も戻る。TerminalとSettingsのTABには出さない。
- **表示の言語**（2026-09-15、書き手）。Settings → General「LANGUAGE」の**プルダウン**で「System」「日本語」「English」を選ぶ（書き手の求め 2026-09-15：言語が増えても行を足すだけで済む）。既定はSystemで、Windowsの表示言語が日本語なら日本語、対応する言語が無ければ英語。**日本語表示ではメニュー・機能名も日本語**（書き手の判断。2026-08-22の「メニューと機能名は英語表記」は英語表示の姿として残る）。言語の名前（日本語／English）はどちらの表示でもその言語で書き、読めない表示から戻れるようにする。選べばすぐ切り替わり、設定に残る。
  - **段階**（書き手の選択）：①仕組み・設定と画面の文言（メニュー・設定画面・ツールチップ・Keysの操作名・比較画面・Quick Draft）。②ステータスバーの知らせ・問いの文とボタン・ファイルダイアログの題と絞り込み・ステータスバーの字数表示（日本語では「12行」「3,456字」「原文 4,000字」「選択 20字」「5行、3列」）・TABの名前（無題／Untitled、新しいTAB、設定）・モードの「なし／None」などRustの文言。①②とも実装済み。
  - 切り替えたときは、Rustから出ている文言（TABの名前・モードの名前・左の欄・ステータスバー）を置き直し、前の言語で出ていた知らせは畳む。モードの名前には「なし」「None」のどちらも使えない。
  - 画面の文言は英語を元の文として`@tr`で書き、日本語の訳を実行ファイルに埋め込む。Rustの文言は日本語と英語を対にして書く。訳の漏れは画面・Rustとも自動試験で検出する。
- **本文背景の実透過**（2026-09-17、書き手）。設定 → ページで背景の透過度を0〜100%（既定0%）に調整する。背後にある他アプリの現在の内容が見え、文字・メニュー・タイトルバーの濃さは変わらない。メインウィンドウの縦書き・横書き・分割した本文に共通適用し、設定・Terminal・別の下書き窓は対象外。即時反映し、設定を保存・復元する。ページのリセットとすべてリセットは0%に戻す。透過中は壁紙を表示せず、選択とパスを保持して0%で復帰する。
- **背景の壁紙**（2026-09-15、書き手）。Settings → Page「BACKGROUND IMAGE」の全体設定で、本文の紙の後ろに画像を敷き、紙を「Image Strength」（壁紙の濃さ、0〜100%、既定30%）ぶん透かす。字は濃いまま。種類は「None」「Windows Wallpaper」「Image File」。
  - **Windowsの壁紙を画面上と同じ位置に敷く表示**も選べる（書き手の案）。実透過とは別の背景表示である。画面ごとの壁紙・配置（中央・並べる・拡大・合わせる・ページ幅・スパン）・地の色をWindowsから読み、窓の移動・拡大率の変更に追従する。壁紙が替われば見回り（2秒）で置き直す。
  - 「Image File」は画像を選び、「Tile」（元の大きさで並べる）「Fit Height」「Fit Width」（縦横比を保って縦／横に合わせ、中央に置く）。Windowsが読める画像形式。
  - 壁紙はスクロールで動かない。TAB・Paneの紙の色はそのまま透ける。メニュー・Explorer・Terminal・Settings・New Tabには敷かない。読めなければステータスバーに理由を出し、紙だけで表示する。PageのResetで外し（パスは覚える）、Reset Allでパスも忘れる。
  - **TABの色**（書き手の求め、同日）：TAB（見出し）は既定でそのTABの紙と同じ色（TAB > Pane > 全体）。**全体の紙が既定の色のままで、TAB・Paneにも色が付いていないときは今までの見た目**（前のTABだけが紙の色）。全体の縦書き・横書きの紙を既定から変えると、その向きのTABは選ばれていなくてもその色になる（書き手の判断 2026-09-15）。TABの右クリック「Tab Color…」で個別に選べ、選んだ色はそのTABの紙を変えても追随しない。**Paneの紙の色を変える（既定に戻すを含む）と、そのPaneのすべてのTABに付けた紙の色と見出しの色が外れ、TABは全部Paneの色に塗り変わる**（書き手の判断 2026-09-15）。全体の紙の色を変えたとき・PageのReset・Reset Allでは、すべてのTAB・Paneの色が外れる。「既定の色」で紙に合わせる状態へ戻る。暗い色では名前を明るい字で書く。セッションに残る。
  - **縦書きを横書きに合わせる**（書き手の求め、同日）：Settings → Page の VERTICAL に「横書きに合わせる」（既定はOff）。Onの間は縦書きの全体の紙も横書きの色を使う（TAB・Paneの色は元から縦横共通なので、この設定に関わらない）。縦書きの Paper Color の欄は隠す。Offに戻すと縦書きの全体の紙の色が戻る。PageのReset・Reset AllでOffに戻る。
  - **Text・Layout の面にも「横書きに合わせる」**（書き手の求め、同日。面ごとに1つ）。Onなら縦書きはその面の値（Text：文字の大きさ・装飾・文字色・背景色・書体・Code Font、Layout：行送り・文字送り・余白・折り返し・行番号・空白表示・ルビ・箇条書きの印）を横書きから読み、縦書きの欄を隠す。**縦書きにしか無い設定（縦中横）は共通にせず残す。**縦書きの値は消さずに保存し、Offで戻る。その面のReset・Reset AllでOffに戻る。
- **文字色のセット**（2026-09-15、書き手：背景が明るい・暗いたびに文字色を設定するのは面倒）。Textの面の先頭「COLOR SETS」で、一覧からSet 1〜10を**選ぶとその場で適用**し、下の個別の色見本も替わる（空のセットは選ぶだけ）。「Save to…」は**保存先を別の一覧で選ぶ**（上の一覧で選ぶと先に適用されて、整えた色が失われるため）。Deleteは選んでいるセットを空にする。**一覧の表示は今の文字色から決める**：同じ色のセットがあればその番号、なければ「Custom」（開いた直後も、個別に色を変えたあとも、表示と色が食い違わない）。1セットは**Body・H1〜H6の文字色を横書き・縦書きまとめて14色**。紙の色・文字の背景色は含めない。原文（ソース表示）は一様な色のまま、その色をBodyの文字色とする（既定の墨に固定しない。紙の色は原文にも効くため）。設定ファイルに`ink.set.N`として残し、面のReset・Reset Allでは消さない。削除・上書きの確認は出さない。

**E11（2026-09-13実装）**：Settings → Textで、本文・H1〜H6ごとに太字・斜体・取り消し線・背景色を指定できる。見出しには罫線のOn／Offもある。横書き・縦書きで別々に保持し、既存設定に項目がなければ背景はPaperに合わせ、他の装飾はOffとする。従来の背景OffはPaperに合わせる、Onは指定色として引き継ぐ。

背景は文字だけでなく行の領域に塗る。見出し罫線は文字のアンダーラインではなく、折り返した見出し全体の後ろ側（横書きは下、縦書きは左）へ、行幅いっぱいに見出しの文字色で引く。Markdown内の強調は設定と併用できる。表示設定で原稿を書き換えず、ソース表示には装飾を適用しない。

**表示設定は横書きと縦書きで別に持つ**（2026-08-22改訂）。設定画面は書字方向ごとのシートに分け、
**シートに載っている値はすべてその向きのもの**とする。同じ文書を両方の向きで開く（要件7.2の4モード）
以上、向きによって読みやすい書体も字の大きさも違う——一部だけを向きごとにすると、
どの設定がどちらに効くのかを利用者が覚えることになる。

各シートは次を持つ。

- 本文と見出しH1～H6のそれぞれについて、**文字サイズ・文字色・フォントを指定できる**
  （2026-08-22改訂。もとは「H1～H6のサイズを個別に」「本文のフォント」「見出しのフォント」と
  分かれていたが、**設定するのは「その見出し」であって、サイズと色とフォントはその3つの側面**である）。
- 行の送り（横書きなら行間、縦書きなら列間隔）を指定できる。
- 文字送りを指定できる。
- コードブロックとインラインコードの等幅フォントを指定できる（2026-08-22追加）。
  等幅への切り替えは既に整形表示（7.3.2）が行っており、その書体名を設定へ出すもの。
- 背景色と編集領域の余白を指定できる。
- **折り返し幅を指定できる**（2026-09-05追加）。次の3つから選ぶ。
  - 指定幅で折り返す。**幅は本文の文字数で指定する**——画素で持つと、字の大きさを変えるたびに
    1行の長さの意味が変わってしまう。
  - 編集ペインの幅で折り返す（既定）。
  - 折り返さない。**上の2つがどちらも選ばれていない状態**として表す（2026-09-05改訂）——
    「折り返さない」ための釦を別に置くと、3つのうち1つだけが入るという同じことを2通りに書くことになる。
  設定は**ソース編集とライブプレビューの両方に効く**。どちらも同じ面の読みやすさの話であり、
  要件9が原文から取り上げるのは文字の飾りだけである。
  **指定幅はペインの幅に縛られない**（2026-09-05改訂）。ペインより広い幅を指定したときは、
  入りきらないぶんを**流れと交差する向きへスクロールして見る**——ペイン幅で折り返してしまうと、
  25字しか入らないペインでは25字の設定と40字の設定が同じ絵になり、**設定が効いているかどうかを
  画面から知りようがない**。キャレットはその向きにも追いかける。
  指定幅がペインより狭いときは、**本文はペインの中央に置く**。
- **原文（ソース編集）には文字の設定を適用せず、一様な大きさ・色・書体で表示する**（2026-08-22追加）。
  原文は記法そのものを読み書きする面であり、見出しが大きく色づいて見えることは編集の助けにならない。
  行の送り・文字送り・余白・背景色はシートのものを使う——それらは面の読みやすさであって、
  文字の飾りではないため。
- **行番号の表示・非表示を切り替えられる**（2026-09-07追加）。番号は**ファイルの行**に
  つけ、折り返した段には出さない（10の行数の数え方と同じ）。
  **番号は編集ペインの端に置き、本文との間に境界線を引く**（同日改訂）。本文のすぐ脇に
  置くと文の一部のように読めてしまう——番号は本文ではないので、本文と同じ紙の上で
  離れた場所に立てる。余白はその列と境界線を入れるぶんだけ広がる。
  **横書きでも縦書きでも同じ形とする**（同日追記）。行の頭の余白は、横書きなら面の左端、
  縦書きなら面の上端である。**数字はどちらでも正立させる**——横倒しの数字は数字として
  読めない。縦書きでは数字が列を横切るので、桁が増えたら小さく組んで隣の列に掛からない
  ようにする。
- 設定変更を編集中の画面へ即時反映する。
- **設定画面は群ごとの面に分ける**（2026-09-08追加）。General / Terminal / Left Pane / Words / Text /
  Layout / Page / Keys の8つ（同日、Terminalを分けた＝6.8、Wordsを足した＝7.9。Keysは11.8。Left Paneは2026-09-15＝5.2）。
  設定は増える一方で、一枚に積むと下のほうが窓から落ちる。
  **切れ目は「書字方向を持つかどうか」**：Generalだけが方向を持たない設定（既定の接続先
  6.8、自動退避8.1）で、残りの3面はどれもシートの上の値である。だから**シートを選ぶ釦は
  Generalには無く、他の3面にある**。
  「初期値へ戻す」は書式の両シートを戻すもの（そう書いてある）なので、書式の3面にだけ置く。
- **表示設定はアプリ全体で共通とし、表示倍率だけを編集ペインごとに持つ**（2026-08-22明記）。
  そのため設定を開く口はトップバーのメニューにもペインのメニューにも置くが、開く先は同じ1つとする。
- 設定を初期値へ戻せる。
- `Ctrl+マウスホイール`で、マウスカーソルがある編集ペインの表示を拡大・縮小できる。
- 拡大・縮小は4つのMarkdown編集モードすべてに適用する。
- 拡大・縮小は表示倍率だけを変更し、Markdown本文や基準となるフォントサイズ設定は変更しない。
- `Ctrl+0`でフォーカス中の編集ペインの表示倍率を100%へ戻せる。
- **ステータスバーの倍率表示（10）をクリックすると、その真上に縦のスライダーが立つ**
  （2026-09-06追加／2026-09-07改訂）。50〜240%を10刻みで、キーとホイールが動かすのと
  同じ刻みとする。**大きい数は上**とし、100%へ戻す口を添える。
  **押した数の真上に出す**——押した場所と出る場所が離れていると、何を動かしているのかが
  読めない。
- 各編集ペインの表示倍率をセッション情報として復元する。
- 初期版では表示設定をアプリ全体で共通とし、作業フォルダごとの設定は持たない。
- 表示設定はアプリ専用領域へ保存し、Markdown本文には書き込まない。
- 初期フォント、配色、見出しサイズは、一般的なWindows向けテキストEditorの範囲から暫定値を設定する。
- 初期表示の厳密な数値は開発開始の前提条件とせず、実際の表示を確認しながら調整する。

### 9.1 読み方と組み方は、別の場所にある（2026-09-11追加）

**設定には2種類ある。**混ぜると、同じ文書が2つのペインで別の本文になる。

- **読み方**——原稿の字をどう解釈するか。**窓に1つ**（設定画面のGeneral）。
  紙のシートに置けば、縦書きと横書きで本文そのものが変わり、**字数まで食い違う**。
  - ルビと傍点の記法を読むか（7.8）
  - どの記号を箇条書きの印として読むか（7.3.2）
  - ルビを本文文字数に数えるか（7.8・10）
- **組み方**——読んだものをどう見せるか。**紙のシートごと**（設定画面のLayout／Page）。
  同じ原稿を別の紙で開けば違って見えてよい。
  - 書体・大きさ・色・行送り・折り返し幅・余白
  - ルビの大きさと位置、縦中横（7.8）
  - **箇条書きの記号ごとの印**（7.3.2）。**読まないと決めた記号は、ここでも設定できない。**

### 9.2 画面の名前と、ソースの名前をそろえる（2026-09-11追加、書き手の求め）

**設定画面の群はGeneral／Terminal／Words／Text／Layout／Page／Keys**であり、文書・コメント・
ソースの識別子も**その名前で呼ぶ**。**ソースと画面が食い違えば、書き手と作り手が別のものを
指して話すことになる**——名前が合わないと気づいたら、**画面のほうを直す**（呼び方を
言い替えて済ませない）。

## 10. 文書情報

ステータスバーへ、次の情報をリアルタイムに表示する。

- 文書全体の論理行数
- Markdown記号を除いた本文文字数
- Markdown記号を含むソース文字数
- 選択範囲の文字数
- ソース編集時の現在行と桁位置
- フォーカス中の編集ペインの表示倍率（例：`100%`、`125%`）
- **単語チェックモード（要件 7.9、2026-09-08追加）。**前に出ている文書のもの。
  **押すと切り替わる**——コードエディタが言語モードを出しているのと同じ場所、同じ手つき。
- **原稿用紙の枚数（400字詰め、2026-09-08追加）。**公募の規定がこの単位で書かれていて、
  **書きながら「あとどれくらいか」を知りたい**ため。本文文字数から求める（ルビを数えるか
  どうかは7.8の設定に従う）

行数は画面上の折り返しや縦書きの表示列ではなく、ファイル内の改行を基準に数える。文字数は、日本語や絵文字を画面上の一文字として自然に数える。表示倍率は、`Ctrl+マウスホイール`または`Ctrl+0`による変更を即時反映する。

## 11. ショートカット

### 11.1 基本方針

- Windowsの標準的なショートカットを踏襲する。
- 可能な範囲でEmacsに似た編集ショートカットを提供するが、完全互換は目指さない。
- ショートカットが競合する場合は、フォーカス中のUI要素と編集状態に応じて動作を決定する。
- Ctrl+C／X／V／Aなどの基本編集キーは固定する。変更可能な操作と有効な場面は11.8で定める。
- 以下のキー一覧は既定値を示す。変更対象の操作は11.8の設定を優先する。

### 11.2 Windows標準に準じる操作

| ショートカット | 操作 |
|---|---|
| `Ctrl+A` | 全選択 |
| `Ctrl+C` | コピー |
| `Ctrl+V` | 貼り付け |
| `Ctrl+X` | 切り取り |
| `Ctrl+Z` | Undo |
| `Ctrl+O` | 横書き・縦書きの本文からファイルを開く。キャンセルで元の本文を保持する |
| `Ctrl+Y` | Redo |
| `Ctrl+F` | 検索 |
| `Ctrl+H` | 置換 |
| `Ctrl+S` | 既存ファイルを確認なしで即時上書き。新規文書では保存先を指定 |
| `Ctrl+Shift+S` | 名前を付けて保存（文字コードと改行コードを選ぶ欄がある。4.3） |
| `Ctrl+W` | タブを閉じる |
| `F3`／`Shift+F3` | 次／前の一致へ（本文からも効く。7.7） |
| `Ctrl+G` | 行を指定して移動（7.7） |
| `Escape` | 開いているメニューを閉じる。開いていなければ、選んでいる途中から戻る（11.4） |

設定はTABで開くので（9）、設定の上に出る検索欄は設定を探す欄である。設定の入力欄の`Escape`は、
キーボードをそのペインへ戻す（R3の順序は2026-09-14のTAB化で不要になった）。

**行と箇条書きの操作**（2026-09-11追加、追加要件 E3・E10 から転記）

| ショートカット | 操作 |
|---|---|
| `Alt+↑`／`Alt+↓` | 選んだ行を文書順の前／後へ動かす（縦書きでも同じ） |
| `Shift+Alt+↑`／`Shift+Alt+↓` | 選んだ行を前／後へ写す |
| `Ctrl+Shift+K` | 選んだ行を消す |
| `Tab`／`Shift+Tab` | 選んだ行の字下げ（箇条書きなら入れ子の深さ） |
| `Ctrl+Shift+8` | 選んだ行を箇条書きにする／外す（記号は右クリックから選ぶ） |
| `Ctrl+Shift+7` | 選んだ行を番号付きにする／そろえる／外す |

**`Ctrl+Shift`を押した数字が何の字で届くかは鍵盤で違う**（JISでは`(`と`'`、USでは`*`と`&`）。
修飾を押したまま打った字は本文へ入らないので、**どれで届いても同じ意味に取る。**

### 11.3 タブと編集ペインの操作

| ショートカット | 操作 |
|---|---|
| `Ctrl+Tab` | 次のタブへ移動 |
| `Ctrl+Shift+Tab` | 前のタブへ移動 |
| `Ctrl+Alt+←` | 左の編集ペインへ移動 |
| `Ctrl+Alt+→` | 右の編集ペインへ移動 |
| `Ctrl+Alt+↑` | 上の編集ペインへ移動 |
| `Ctrl+Alt+↓` | 下の編集ペインへ移動 |
| `Alt+←` | 前に見ていた文書へ戻る（7.7） |
| `Alt+→` | 戻る前の文書へ進む（7.7） |

### 11.4 Emacsに似た編集操作

| ショートカット            | 操作                                         |     |
| ------------------ | ------------------------------------------ | --- |
| `Ctrl+D`           | 次の1文字を削除                                   |     |
| `Alt+F`            | 1単語後へ移動。日本語では句読点または空白まで移動                  |     |
| `Alt+B`            | 1単語前へ移動。日本語では句読点または空白まで移動                  |     |
| `Ctrl+K`           | カーソル位置から行末までを切り取り、Kill Ringへ追加             |     |
| `Ctrl+Space`       | 選択を開始                                      |     |
| `Ctrl+Shift+Space` | 矩形選択を開始                                    |     |
| `Alt+W`            | 選択範囲をKill Ringへコピー                         |     |
| `Alt+X`            | 選択範囲を切り取り、Kill Ringへ追加                     |     |
| `Alt+Y`            | 最新のKillを貼り付け                               |     |
| `Alt+Shift+Y`      | 直前に貼り付けた範囲を、1つ前のKillへ置換。続けて押すとさらに前のKillへ置換 |     |

矩形選択が対象とする表示モードと、範囲の定め方は「7.1 共通編集機能」で定義する。

| `Escape` | 選んでいる途中から戻る（2026-09-12追加） |


**選択は`Escape`で必ず解ける**（2026-09-12追記、書き手の報告「選択になって戻れなくなる」）。
選択が始まる道はいくつもある（`Ctrl+Space`の印・Shiftを押した移動・マウスの引きずり・
語の二度押し・検索）が、**降りる道は1つでよい**——押した覚えのない書き手には、
「もう一度同じ鍵を押す」という戻り方が無いためである。`Escape`はメニューと検索の帯を
先に畳み、どちらも開いていなければ選択を解く。**カーソルは動かさず、本文も変えない。**
変換の途中の`Escape`はIMEのもので、ここには来ない。

**選択の開始は、押しっぱなしにしなくてよいShiftである**（2026-09-07追記）。開始したあとは
矢印キーだけでなく**マウスのクリックでも範囲が決まる**——押した場所までが選ばれ、
指を離したところで選択の開始は終わる。したがってもう一度クリックすれば、
ふつうのクリックとして選択が解除される。

### 11.5 表示倍率

| ショートカット | 操作 |
|---|---|
| `Ctrl+マウスホイール` | マウスカーソルがある編集ペインの表示を拡大・縮小 |
| `Ctrl+=` | フォーカス中の編集ペインを1段階拡大 |
| `Ctrl+-` | フォーカス中の編集ペインを1段階縮小 |
| `Ctrl+0` | フォーカス中の編集ペインの表示倍率を100%へ戻す |

### 11.6 Kill Ring

- Kill Ringは、このアプリ内だけで使用する一時的なテキスト履歴とする。
- Windows Clipboardとは別に管理する。
- Kill Ringへ追加する操作とClipboardを使用する操作を区別する。

### 11.7 Terminal（2026-09-07追加）

2026-09-19合意：Terminal内の検索は `Ctrl+Shift+F`。`Ctrl+F` と `Ctrl+C` はシェルへ渡す。`Shift+PageUp` / `Shift+PageDown` は保持履歴のページ移動。Editor Panelの `Ctrl+S` / `Ctrl+Shift+S` はそのPanel TABを保存する。

今回のTerminal作業単位の現行仕様と確認条件は `Terminal実装計画.md` を参照する。複数行送信・動作中シェルの終了確認は既定ON。確認キャンセルでは送信・終了を行わない。下段の名称はEditor Panel／Terminal Panelとし、隠す操作とTABを閉じる操作を分ける。

| ショートカット | 操作 |
|---|---|
| `Ctrl+\`` | 編集ペインの下段を開閉する（6.8） |
| `Ctrl+Alt+↑` / `Ctrl+Alt+↓` | 本文と下段を行き来する。下段が無ければ11.3の隣のペインへ |
| `Ctrl+Enter` | 下段のEditorに書いたものを、上の端末へ打鍵として送る（6.8） |
| `Ctrl+Shift+C` | Terminalで選択した範囲をコピーする |
| `Ctrl+V` / `Ctrl+Shift+V` | Terminalへ貼り付ける（シェルが求めるときは括弧付き貼り付けで送る） |

**Terminalのタブでは、これら以外のキーはすべてシェルのものとする。**同じ組み合わせが
パイプの向こうでは別の意味を持つ。上の5つだけは編集器のものとして先に取る——
どのタブで押しても同じ意味でなければならないものだからである。

### 11.8 ショートカット設定（C1）

設定画面の「Keys」の群に、操作と適用する場面を分類表示する（2026-09-15、書き手の求めで改訂）。
- **分類で切り替えず、全部を1つの一覧に並べる。**分類の見出しを押すと、その分類を畳める。固定のキー（基本編集・Terminal・入力欄）も同じ一覧に「固定」と示して並べる。
- 操作名は設定を探す欄（`Ctrl+F`、9）で探し、Keysの面には**キーを押して探す欄**を置く。キーは綴らずに押して取り込む（Backspaceで消す）。探している間は、一致のある分類だけを開いて出す。
- 変更欄は普段は出さない。**操作を選ぶと、その行のすぐ下に開く**（キーの取り込み・適用して保存・割り当て解除・既定に戻す・閉じる）。
- 面の下の「Reset」は、キーの割り当てだけを既定に戻す。

| 分類 | 変更対象 | 有効な場面 |
| --- | --- | --- |
| 本文の操作 | 15操作。保存・検索・Undo／Redo・行の移動／複製など | 本文にフォーカスがあり、IME変換中でないとき |
| Pane・TAB | 13操作。TAB・Pane移動、下段、表示モードなど | 本文にフォーカスがあるとき |
| Emacs | 12操作。単語移動・選択、削除、Kill Ringなど | 本文で常時有効。専用モード切り替えはない |
| Quick Draft | 5操作。起動、全文コピー、コピーして閉じる、クリア、TABへ貼り付け | 起動は本文側、その他は独立したQuick Draft内 |
| 基本編集・Terminal・入力欄 | 固定キーの一覧 | 各分類に記載した場面 |

計45操作を変更できる。操作名・キーで検索し、操作を選んで実際の組み合わせを押すとキーを表示する。「適用して保存」で確定し、割り当て解除と既定への復帰もできる。修飾キーだけでは取り込み・操作を実行しない。

- 指定できるキーは一覧の既定キー、またはCtrl+Alt+A〜Z。Ctrl+C／X／V／Aは変更対象に割り当てない。
- 同じ場面での重複を拒否する。本文の操作・Pane／TAB・Emacs・Quick Draft起動は同じ場面として判定する。本文とQuick Draft内の間では同じキーを併用できる。
- 変更・解除した旧キーは元の操作を実行しない。設定は保存し、再起動後も維持する。開いているQuick Draftにも変更を反映する。
- 操作は現在のTAB・Paneを対象とし、メニューや表示切り替えボタンの説明も割り当てに合わせる。Viewerでの編集制限は維持する。
- 縦書き／横書き、ソース／Preview、Viewer開始／終了を設定できる。ソース／PreviewのキーはViewerを終了してから切り替える。
- Quick Draft起動、新しいTAB、Quick Draft内のコピーして閉じる・クリア・TABへ貼り付け、3つの表示切り替えは初期状態で未割り当て。その他は従来のキーを既定とする。
- Terminal・入力欄、F3の検索移動、箇条書きなど、設定対象外のキーは従来どおり。対象の追加や操作検索からの実行は追加候補として扱う。

## 12. クイック下書き

### 12.1 目的

チャットなどへ送信する短い文章を、`Enter`による誤送信を気にせず推敲するための簡易編集機能を提供する。

### 12.2 ウィンドウ

- メインEditorとは独立した小型ウィンドウとして提供する。
- 作業フォルダを開いていなくても利用できる。
- 初期版では、クイック下書きを呼び出すグローバルショートカットを既定で割り当てない。
- 呼び出し処理をUI操作から分離し、将来グローバルショートカットを割り当てられる構造にする。
- 「常に手前に表示」を切り替えられる。
- ウィンドウの位置とサイズを記憶する。
- 横書きのプレーンテキスト編集とし、`Enter`は常に改行とする。
- Undo／Redo、切り取り、コピー、貼り付け、全選択に対応する。

### 12.3 Clipboard

- `Ctrl+C`は選択範囲をClipboardへコピーする。
- `Ctrl+Shift+C`は下書き全文をClipboardへコピーする。
- マウス操作用に「全文コピー」ボタンを提供する。
- コピー成功を非侵襲的な表示で通知する。
- コピー後も下書き内容を保持する。
- 必要に応じて「コピーして閉じる」操作を提供する。

### 12.4 送り先と保存（2026-08-27改訂）

**改訂の理由**：下書きは「送るために書くもの」であり、送ったあとも残っているのは
書き終えた合図にならない。**出ていく道を増やし、出ていったら空にし、失わないために
履歴を持つ**——この3つが1組で、どれか1つだけでは成り立たない。

- 現在の下書き1件をアプリ専用領域へ自動保存する。
- **Editorで開いているタブへ貼り付けられる。**貼り付けたら下書きは空にし、ウィンドウを閉じ、
  そのタブを前に出す。
- **送信ボタンには送り先のタブ名を出す。**クリックでそのタブへ貼り付け、押したままにすると
  タブ一覧が出て、選んで手を離すとそのタブへ貼り付ける。
- **送り先は前回選んだタブを覚える。**一度も選んでいなければ先頭のタブ。
  **覚えているタブが閉じられていたら、クリックで一覧を出す**——別のタブを勝手に選ばない。
- タブ一覧では、ペインが複数あるときにペイン番号を添える（左右・上下といった配置では言わない。
  分割は今後増える）。
- **ウィンドウを閉じたら下書きは空にする。**（改訂前は「破棄しない」。空にする代わりに、
  下記の履歴へ必ず入る。）
- 次回表示時に編集位置を復元する。
- 内容の削除は明示的な「クリア」操作、対象タブへの貼り付け、ウィンドウを閉じること、
  のいずれかで行う。**いずれの場合も履歴へ入れてから空にする。**
- **下書き履歴を自動で10件保持する**（改訂前は「初期版では提供しない」）。
  新しいものが先頭、同じ本文は1件、古いものから落ちる。一覧から選ぶと下書きへ戻る。
- Markdown文書への昇格保存は行わない（改訂前は「必要に応じて」）。
  対象タブへ貼り付けてから、Editorの保存で行えばよい。

## 13. 将来拡張

### 13.1 クイック下書きとTerminalの連携

**Terminal そのものは将来拡張ではなくなった**（2026-09-07改訂）。この節は「別途開発予定の
Terminalと連携する」ことを書いていたが、編集器が自分のタブとしてシェルを持つことにしたため、
Terminalの要件は6.8へ移した。**別プロセスのTerminalとのアプリ間通信も要らなくなった**
——送り先は同じウィンドウの中にある。

残るのは、独立した小型ウィンドウであるクイック下書き（12）と、そのTerminalとのつながりである。

- クイック下書きの全文または選択範囲を、開いているTerminalのタブへ送れるようにする。
- 送り先が複数あるときは選べるようにする。送り先のタブを覚える規則は12と同じとする。
- 送った内容をどう扱うかは6.8と同じ——打鍵として入れ、走らせるかどうかは末尾の改行で決める。
- Terminal側で選択した文章をクイック下書きへ取り込めるようにする。

### 13.2 その他の候補

- 文字色と背景色の組み合わせをEditor独自の表示テーマとして名前を付けて保存する機能
- 保存した表示テーマを編集画面から切り替える機能
- 表示テーマはObsidianテーマや独自CSSの再現とは分離し、アプリ専用領域で管理する
- 作業フォルダごとの表示設定
- 複数ウィンドウまたは複数作業フォルダの同時利用
- 縦書きの印刷（PDFを含む）は採用済みの7.10に従う。ここでは重複した将来候補にしない。

## 14. 初期版の対象外

- Windows 10以前への対応
- プログラムコード向けのIDE機能
- Obsidianプラグインの実行
- Obsidianテーマや独自CSSの再現
- 文中（行の途中）の画像のインライン表示
- 数式およびMermaidの描画
- 縦書きライブプレビュー内で、表とコードブロックを横書きブロックとして表示すること
- `.canvas`の編集
- Obsidian Syncとの直接連携
- グラフ表示や高度なバックリンク分析
- 名前だけの `[[ノート名]]` 形式の内部リンクからリンク先へ移動すること（2026-08-19追加）。フルパスを明示した内部リンクはE12で対象に追加（7.3.1）。
- 名前変更・移動にともなうリンクの書き換え（旧5.3、2026-08-19に対象外へ移動）
- ファイル名で開くクイックオープン（旧7.7、2026-08-19に対象外へ移動）

Vaultを対象外としたため、`[[ノート名]]`が何を指すかを決める名前空間が定まらない。同じ名前の
ノートが複数あるとき、どれを開くかを決める規則はVaultの側にあり、作業フォルダ（5.1）はそれを
持たない。**記法としては引き続き保持し（7.4）、ライブプレビューでも整形して表示する（7.3.2）**
——壊さないことと、移動先を決められることは別である。

相対パスの通常リンク（`[説明](章/第一章.md)`）は移動の対象外にしない。指す先はそのファイルの
位置から一意に決まり、Vaultの名前空間を必要としない。

クイックオープンは作って動かしたうえで外した。**ファイル名を打ってファイルを開くこと自体が、
この編集器の書き手には要らなかった。**左ペインのExplorer・履歴・検索の3つで、ファイルへ行く道は
足りている。要件7.7が「クイックオープン」とだけ書いていて、何を速くするための機能なのかを
書いていなかったのが、作ってみるまで分からなかった理由である。**機能の名前だけを書いた項目は、
それが誰の何を助けるのかを書くまで着手しない。**

なお12「クイック下書き」は別の機能で、対象外にしていない。名前が似ているだけで、あちらは
メインとは独立した小型ウィンドウである。11.4のKill Ringも別で、こちらは文字列の履歴であって
ファイルとは関係がない。

**リンクの書き換え（旧5.3）は、通常リンクだけに絞っても対象外とする。**名前変更のたびに他の
ファイルの中身を書き換える機能であり、失敗したときに作業フォルダを途中状態で残さないことまで
求めていた。名前変更そのものは5.2で、開いているタブが追従するところまで動いている。書き換えは
そこから先の、壊しうる範囲がはるかに広い操作である。**名前を変えることと、他人の文章を書き直す
ことは別の決定**なので、後者は書き手の手元に残す。
- **EPUB、Wordなどへの書き出しと、入稿できる品質の組版**（2026-09-08改訂。
  **印刷だけはここから外して7.10にした**——ただし納品の道としてではなく、
  **自分の原稿を紙の形で確かめるため**である。仕上げは専用の道具に任せる、というのが
  この編集器の思想である＝3）
- **編集画面のページ単位の縦書きレイアウト。**編集中は無限に続く1枚として組む。
  紙にするときだけページに切る（7.10）——**書いている最中に改ページが動くのは、
  書く邪魔にしかならない**
- クイック下書きとTerminalの連携（13.1。**Terminal自体は6.8で実装した**）

## 15. 実装後に決定する事項

次の項目は、主要機能の実装後に実測結果をもとに決定する。これらが未決定であることを理由に開発を止めない。

- 性能を測定する具体的な操作とテストデータ
- 各測定対象の性能目標値
- **傍点をどの綴りで正とするか**（7.8、2026-09-08追加）。投稿サイトごとに揺れがあるので、
  実際に使われている形を調べてから決める。
- **紙の既定値**（7.10、同日）：用紙の大きさ、四辺の余白、1ページの行数と字数。
  **実際に組んで目で見てから決める**——数字を先に決めても、読める紙になるとは限らない。
- **語群をいくつまで持てるか、1つに何語まで入れられるか**（7.9、同日）。
  いまは1モードに8語群・1万語で、**どちらも「足りないと言われてから増やす」側の数**である。
  語群の数は色を用意するぶんの上限、語数は木の大きさの上限。実測してから決める。

## 16. 診断モード

- 通常時も起動・終了、保存・退避・復元と外部変更、設定・印刷の結果、エラー、確認の種類・選択結果を記録する。詳細診断のために別ビルドへ入れ替える必要はない。
- 起動引数`--diagnostics`で全分野、`--diagnostics=render,input,terminal,ui,search,perf`で必要な分野だけを有効にする。カンマ指定・複数回指定に対応し、不明な分野では通常モードに戻して知らせる。引数なしはdebug／releaseとも通常モード。設定には永続化しない。
- 性能ログと起動時の組版診断プローブ・フレーム計測は対応する診断分野を指定したときのみ有効にする。
- 診断・性能ログは同じ`diag/`へ出力し、`Diag_<実行ID>.log`と`Perf_<実行ID>.log`を対応させる。実行ファイル横に診断ログを作成できない場合は利用者データの`diag/`へ両方の出力先を切り替える。性能ログは対応する診断分野を指定したときだけ作成する。
- 両方とも共通処理で1ファイル8 MiBに達する前に切り替え、直前の区間を`.prev.log`として1つ保持して記録を続ける。性能ログにも行数による停止制限を設けない。
- 世代は実行単位で最新20回を保持する。性能診断を指定しない回も数え、古い実行の診断・性能・直前区間をまとめて整理する。複数起動は異なる実行IDを使い、使用中・削除できない実行は保持する。旧形式の性能ログは更新・移動しない。
- 本文・検索語・Terminalへの入力文字・引数一覧は記録しない。削除確認などが持つ本文やpanicのメッセージをそのまま出力しない。パス・設定・エラー情報は調査用に残す。
- ログの失敗で編集・保存を妨げない。レコードの長さと改行を制限する。
