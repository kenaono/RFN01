# 基本メニューの整理案（RFN01-34）

2026-09-20。調査・議論用。メニュー実装は未着手。

**最新合意：A案（タイトルバー統合）を採用し、今回は既存機能のメニュー化のみを行う。不足機能の追加は対象外。** 新規の記法挿入・Help画面・機能拡張は今回の一覧から除外する。Insertは既存リスト操作を配置し、項目のないHelpは表示しない。詳細な採否は[メニュー操作接続仕様の確定範囲](メニュー操作接続仕様.md)を優先する。

末端操作の現行調査・接続仕様は[メニュー操作接続仕様](メニュー操作接続仕様.md)にまとめ直した。本書は分類と議論の経緯として参照し、対象・副作用・接続先の差異は接続仕様を優先する。[現行MenuRow入口149件の索引](メニュー現行入口索引.md)も作成した。

追加調査による訂正：Save Allは現行ではEditor Panelを列挙しない。Close Savedは保存先ありではなく未編集かつ非Terminalを判定する。PanelのRenameも実ファイルの改名である。TAB本文背景とTAB見出し色は別であり、Pane背景変更は配下の個別色を解除する。Open in Another Tab／Terminal in Document Folder／Tab Backgroundは一覧漏れとして接続仕様へ追加案を記載した。以下の旧説明をそのまま実装仕様として使わない。

## 調査対象と確度

- 対象：ローカルの現在の作業ツリー。HEADは `069dfedab8886de2a54326964a1c43776b679cb2`。多数の未コミット変更があり、HEAD単独の調査結果ではない。
- `ui/app-window.slint`、`ui/editor-pane.slint`、`ui/controls.slint`、`ui/quick-draft.slint`、`src/shortcuts.rs`、`src/main.rs`、`src/wiring.rs` と要件定義を照合した。
- 以下の「既存」はソースで入口・接続を確認したもの。今回、アプリの実画面・実操作確認やテスト実行はしていない。
- 合意済み：File・Edit・Insert・Layout・View・Run・Helpの7分類。Referencesは独立させず、Outline・ブックマークの表示はViewへ。左上アイコンでメニューバーを開閉し、普段のシンプルな画面を維持する。
- 追加合意：New Tabは＋ボタン専用。メインメニューにはNew FileとNew Terminalを置き、種類選択を挟まず直接作成する。詳細なメニューリストを確認してから実装する。
- 第5稿の整理軸：メニューは現在の文書・TAB・Pane・Terminal・Panelへの操作。全体の文字・組版・背景設定はメニューに置かず、既存の設定ボタンから変更する。行番号と空白・改行記号の表示は、ユーザー合意によりViewからON/OFFできるようにする。ファイル作成・画面分割など作業を始める操作もメニューに含む。Terminal固有操作を右クリック専用として除外しない。
- 本文の「詳細メニュー案」が現在の具体案。「配置案と既存入口」は調査対応表として参照する。
- 以下の具体的な項目・開閉動作・機能追加は提案であり、実装承認ではない。

## 調査で分かった注意点

1. **New TabとNew Fileは別操作。** New TabはNew File / New Terminal / Open Fileを選ぶ空のTABを作る。New Fileは編集できる無題文書を作る。追加合意により、New Tabは＋ボタン専用、メニューからはNew File／New Terminalへ直接進む。
2. **最上部のファイル名はWindowのtitle。** `app-window.slint:130`で設定し、アプリ内の最上部は既にアイコン列・ペイン領域。下部にもファイル名・未保存印がある。タイトル行へメニューを組み込む案は、単なる既存行の内容差し替えにはならない。
3. **ルビ等を表示する機能と、記法を挿入する機能は別。** リンク補完・ルビ等の解釈はあるが、汎用のリンク／ルビ挿入メニュー・入力ダイアログは今回の探索では見つからない。RFN01-12の新規操作として扱う。
4. **Outlineは既存、ブックマークは追加候補。** `src`・`ui`内にブックマークの実装入口は見つからない。RFN01-28と合わせて検討し、既存機能の配置替えに含めない。
5. **Helpの内容も新たに決める。** 操作説明・バージョン情報のメニュー／画面は今回の探索では見つからない。ショートカット一覧は既存の設定内にある。
6. **設定の現行分類はGeneral / Editor / Terminal / Keys。** 要件定義9.2の古い分類と一致していない。メニューの設計は現行画面定義を基準にし、採用時に関連要件を更新する。
7. **Alt+文字に既存割当がある。** Alt+F/B/W/X/YなどはEmacs風操作で使用済み。Windows風アクセスキーをそのまま追加すると競合する。

## 配置案と既存入口

「接続追加」は既存処理を新しいメニューから呼ぶための作業。「新規」は内容・動作の設計が必要な操作。

### ファイル

| 項目案 | 現在の入口・状態 | 備考 |
| --- | --- | --- |
| 新規文書 | TAB列右クリックのNew File、空TABのNew File | 直接、編集できる無題文書へ |
| 新しいTerminal → シェル | 左上メニュー、TAB列右クリック、空TAB | 既存シェル一覧を再利用 |
| ファイルを開く… | 左上、空TAB、Ctrl+O | 接続追加 |
| フォルダを開く…／最近のフォルダ → | 左上、Explorer | 接続追加 |
| Workspaces… | 左上メニュー | 管理画面へ。登録・複製・解除等は管理画面に集約 |
| 保存／名前を付けて保存…／すべて保存 | ペイン・TABメニュー、Ctrl+S／Ctrl+Shift+S、左上 | Editor Panelを含む対象の扱いを決める |
| 閉じる → 現在／他のTAB／すべて／保存済み | ペイン・TABメニュー、Ctrl+W | 現行の未保存確認・Terminal終了確認を再利用 |
| 閉じたTABを開き直す | ペインメニュー、Ctrl+Shift+T | 接続追加 |
| 文字コードを指定して開き直す → | 下部の文字コード表示 | UTF-8／UTF-16 LE・BE／CP932。保存時の文字コード指定と区別 |
| ファイル情報 → 名前変更／パスをコピー／Explorerで表示 | TAB・ペインメニュー | 実体のあるファイルのみ。無題文書の名前付けは保存 |
| 印刷… | ペインメニュー、Ctrl+P | 既存の印刷プレビューを開く |
| 終了 | ウィンドウの閉じるボタン | メニュー入口は接続追加。既存終了確認・退避を必ず通す |

### 編集

| 項目案 | 現在の入口・状態 | 備考 |
| --- | --- | --- |
| 元に戻す／やり直す | 本文右クリック、Ctrl+Z／Ctrl+Y | 編集可能な対象に限定 |
| 切り取り／コピー／貼り付け／全選択 | 本文右クリックの前3操作、Ctrl+X/C/V/A | 入力欄・Terminal・Editor Panelへの振り分けが必要 |
| 本文だけをコピー | 本文右クリック | Markdown記号等を除く既存コピー |
| 検索…／置換…／行へ移動… | 左上・ペイン・Ctrl+F/H/G | Terminal検索・設定検索は対象別に明示 |
| フォルダ内を検索… | 左上、左の検索、Explorer右クリック | 検索パネルを開く |
| 行操作 → 削除／上下へ移動／上下へ複製 | 変更可能なショートカット | 現行処理にメニュー入口を追加 |
| 選択 → 選択開始・解除／矩形選択開始・解除 | 本文右クリック、ショートカット | 1文字移動など通常のキー操作は列挙しない |
| Kill Ring → コピー／切り取り／最新を貼り付け／前へ置換 | ショートカット | 高度な操作としてサブメニューにまとめる |
| 単語チェック → モード選択 | 下部・TABメニュー | 現在文書への適用。共有語群の追加・管理はメインメニューから外す |


「次の1文字を削除」「単語単位の移動・選択」「行末まで切り取り」は既存キー操作。必要なら行操作／選択／Kill Ringに追加できるが、第一階層には並べない。

### 挿入

| 項目案 | 現状 | 扱い |
| --- | --- | --- |
| リンク → Markdown／Wiki／別名付きWiki | 入力済みリンク記法の補完は既存 | ひな形挿入とキャレット移動。詳細はRFN01-12 |
| ルビ | 記法の解釈・表示は既存 | 選択を親文字にし、読みへキャレット。未選択なら親文字へ |
| 傍点／縦中横 | 解釈・表示機能はある | 挿入候補。採用・対象書式は未決 |
| 箇条書き → 記号付き／番号付き／番号を振り直す | 本文右クリックに既存 | 既存の選択範囲／行への適用を再利用 |
| 見出し → H1〜H6／解除 | 表示・解析は既存 | 選択行全体、未選択なら現在行へ適用する案 |
| 段落注記 → 字下げ／地付き | 行頭注記の表示・解析は既存 | 字数変更・解除を含む行単位の操作として整理 |
| 強調・画像・表など | 描画対応と作成操作は別 | 自動的に実装対象へ追加しない |

挿入には本文を変える操作を置く。ルビの表示サイズ・縦中横の自動表示条件は設定画面で扱い、メニューには置かない。

### Layout

| 項目案 | 現在の入口・状態 | 備考 |
| --- | --- | --- |
| 横書き／縦書き | TAB列ボタン、変更可能なキー | 現在の向きをチェック表示。既存ボタンも維持する案 |
| このPaneの背景…／このTABの色… | ペイン・TABメニュー | 適用範囲の異なる既存操作として区別 |
| このTerminal／Panelの外観… | 個別のAppearance | 現在のTABだけを対象にする既存処理を利用 |

共通設定へのショートカットを各メニューに分散させない。既存右クリックにある共通Layout設定への入口の扱いは、同じ原則に合わせて整理する候補。

### View

| 項目案 | 現在の入口・状態 | 備考 |
| --- | --- | --- |
| 左パネル → Workspace／Explorer／検索／履歴／Outline | 左アイコン列 | 同じ項目を再選択したとき閉じる現行動作を揃える |
| ブックマーク | 今回の探索では実装入口なし | 将来の配置先のみ確保。RFN01-28で検討 |
| 表示モード → Source／Live Preview／Viewer・ReadOnly | TAB列ボタン、変更可能なキー | ViewerとReadOnlyの既存差を維持。常設ボタンも残す案 |
| ペイン → 右へ分割／下へ分割／他のペインを閉じる／上下左右と入替 | ペインメニュー、下部の分割ボタン | 最大数など現行制約を維持 |
| 移動 → 次／前のTAB、上下左右のPane、戻る／進む | キー、本文右クリック | 戻る／進むは文書内の移動履歴 |
| Terminal Panel／Editor Panelの表示・非表示 | ペイン・本文メニュー、Ctrl+` | 親TABの種類によって表示名を変える |
| Quick Draft… | 左上・本文メニュー、設定可能なキー | 独立ウィンドウへの既存入口 |
| 倍率 → 拡大／縮小／100%／倍率一覧 | ペインメニュー・下部倍率 | Terminal等へ文書倍率を誤適用しない |
| 行番号／空白・改行記号の表示 | 現在はSettings → EditorのLayout | Viewにチェック式のON/OFFを置く（合意済み） |

### Run

| 項目 | 既存入口・対象 | 配置の合意 |
| --- | --- | --- |
| Compare → 保存済み／Git最終コミット／別ファイル | ペインメニュー、通常文書 | Fileから移動 |
| Terminal Output → 出力保存・ログ開始／停止 | Terminalと取り込み先Panel | Fileから移動 |
| Send to Editor Panel → 選択／保持中の履歴 | 主Terminal | Editから移動 |
| Switch Shell → 登録シェル | Terminal TAB／Terminal Panel | Editから移動 |

### Help

- ショートカットの設定・一覧は設定ボタン → Keysに集約。Helpに同じ設定画面への入口を重複配置しない案。
- 操作説明…：表示する文書・言語・開き方を決める新規項目。
- RFN Editについて…：バージョン情報の表示内容・画面を決める新規項目。
- 操作説明・バージョン画面を今回作らない場合、存在しない画面に向けた空の項目を置かない。

## 詳細メニュー案（レビュー用・第6稿：ひな形挿入）

以下は表示順を含む具体案。7分類とNew Tabの扱いは合意済み、それ以外の細部はこのリストで確認する。英語UIでの表示名案を示し、日本語UIでは既存の翻訳に合わせる。`…`は入力・設定・確認画面を開く操作、`→`は子メニュー、`---`は区切り線。キーは現行の既定値であり、ユーザーが変更した場合は変更後の割当を表示する。記載のないキーは勝手に新設しない。

### File：作る・開く・保存する

```text
New File
New Terminal →
    （登録済みシェルを設定順で表示。既定のシェルに印）
Open File…                         Ctrl+O
---
Open Folder…
Recent Folders →
    （最近のフォルダを既存の順序で表示）
Workspaces…
---
Save                               Ctrl+S
Save As…                           Ctrl+Shift+S
Save All
---
Close Tab                          Ctrl+W
Close Other Tabs
Close All Tabs
Close Saved Tabs
Reopen Closed Tab                  Ctrl+Shift+T
---
File Operations →
    Rename…
    Rename Panel Tab…              （Editor Panelの場合）
    Copy Path
    Reveal in Explorer
    Reopen with Encoding →
        UTF-8
        UTF-16 LE
        UTF-16 BE
        CP932
---
Print…                             Ctrl+P
---
Exit
```

- **New File**：操作対象の編集領域に無題文書を作り、本文へフォーカス。空TABを選んでいた場合はそのTABを文書にする。Editor Panel内から開いた場合はそのPanelへ新規文書を作る案。保存ダイアログは出さない。
- **New Terminal**：シェルを選ぶと、対象の親PaneにTerminal TABを作る。Panel内や検索欄から選んだ場合も、その所属Paneを使い、Editor PanelをTerminalへ変換しない。
- **Save / Save As**：直前の文書またはEditor Panel文書が対象。検索欄にフォーカスがあった場合はその欄が属する文書を対象とする。無題は既存の保存先指定へ。ReadOnlyで上書き保存は禁止、Save Asは既存どおり許可し、成功後にReadOnly解除。
- **Save All**：既存の一括保存処理の対象範囲をそのまま使う。新しいメニューだけで対象を増減しない。実装時にPanelを含む実際の範囲を照合して文言・確認手順を確定する。
- Terminalの出力保存・継続ログはRun → Terminal Outputに置き、文書のSaveと区別する。
- **Close Tab**：Panel内からはそのPanel文書、主領域からは主TABを閉じる案。**Close Other / All / Saved Tabs**は主Pane内のTAB列を対象とし、Panelを操作中は無効にする案。全ウィンドウのTABを一括で閉じる意味にはしない。
- **Close Saved Tabs**：既存の「未編集の文書TABを閉じる」。Terminalは含めない。無題の扱いなど既存判定を再利用する。
- **Reopen Closed Tab**：現行の閉じたTAB履歴を使う。Panelの履歴を新規に追加しない。Panel操作中は無効にする案。
- **File Operations**：実体のある通常文書に対する操作。Panelへの対応が既存で確認できない操作は有効化しない。Renameはファイル名変更であり、Save Asとは分ける。
- **Print**：通常の文書TABを対象。Panel、Terminal、設定、空TABでは無効。比較はRun → Compareへ移す。
- **Exit**：ウィンドウの×と同じ終了処理を通す。未保存確認・退避失敗・Terminal終了確認・キャンセルを省略しない。
- 多数の項目になるため、Close系を子メニューにまとめる代案は残す。まず主要操作を直接見つけられる上記案をレビューする。高さ不足時はメニュー内をスクロールできるようにする。

### Edit：本文操作と検索

```text
Undo                               Ctrl+Z
Redo                               Ctrl+Y
---
Cut                                Ctrl+X
Copy                               Ctrl+C
Paste                              Ctrl+V
Select All                         Ctrl+A
Copy Text Only
---
Find…                              Ctrl+F
Replace…                           Ctrl+H
Go to Line…                        Ctrl+G
Find in Folder…
---
Terminal →
    Clear Screen
    Clear History
---
Lines →
    Delete Line                    Ctrl+Shift+K
    Move Line Up                   Alt+Up
    Move Line Down                 Alt+Down
    Duplicate Line Up              Alt+Shift+Up
    Duplicate Line Down            Alt+Shift+Down
Selection →
    Start Selection / Cancel Selection
    Start Rectangle Selection / Cancel Rectangle Selection
Kill Ring →
    Cut to End of Line             Ctrl+K
    Copy to Kill Ring              Alt+W
    Cut to Kill Ring               Alt+X
    Paste Latest Kill              Alt+Y
    Replace with Previous Kill     Alt+Shift+Y
---
Word Check →
    Mode → None / （登録済みモード）
```

- Undo～Select Allは「メニューを開く直前に入力していた場所」に作用する。検索欄を編集中ならその欄、PanelならPanel。対象が対応していない操作は無効にし、背後の本文には送らない。
- TerminalではCopyのキー表記をCtrl+Shift+Cにする。Pasteは既存の複数行貼り付け確認を通す。Undo・Redo・Cut・Select Allは対応が確認できるまで無効。シェルへ編集用キーを送る代用実装にはしない。
- Findは通常文書なら本文検索、Terminalなら既存Terminal検索、設定TABなら設定検索へ。Terminalでは表示名をFind in Terminal…、設定ではFind Settings…にし、実際に同じ操作ができるキーだけ併記する。Panel内検索の既存対応は実装前に照合し、未対応なら無効。
- Replace・Go to Line・Lines・Selection・Kill Ringは通常文書で既存処理を再利用。Panelへどこまで適用できるかは機能ごとに照合し、まとめて有効扱いにしない。Viewer／ReadOnlyでは本文を書き換える項目を無効にする。
- Copy Text Onlyは現行の選択範囲の扱いを維持する。通常入力欄・Terminalには表示上の変換をかけない。
- Word CheckのModeは対象文書の現在値に印を付ける。Add Selectionは本文ではなく共有語群を変更する操作なので、第4稿ではメインメニューから外す。既存の選択語右クリック入口を残すかは別途整理し、語群・モードの管理は設定 → EditorのWordsへ集約する。
- 全体設定・ショートカット設定は設定ボタンに集約。EditにSettingsの入口を置かない。
- EditにはTerminalの画面・履歴クリアを残す。転送・ログ・シェル切替はRunへ移す。

### Insert：記法のひな形を本文へ入れる

入力ダイアログを開かず、その場でひな形を挿入し、次の入力位置へキャレットを置く。詳細は[挿入メニュー詳細案](挿入メニュー詳細案.md)を参照。リンク先の用途別画面や親文字・読みの入力画面を作る旧案は置き換える。

```text
Link →
    Markdown Link
    Wiki Link
    Wiki Link with Alias
Ruby
---
Annotation → Left-side Annotation / Ruby and Left-side Annotation
Emphasis Marks → 傍点／ゴマ／丸／白丸／二重丸／×
Emphasis Lines → 傍線／二重傍線／波線／鎖線／破線
Text Annotation → 縦中横／割り注／小さな文字／大きな文字
---
Heading → H1 / H2 / H3 / H4 / H5 / H6 / 見出しを解除
Paragraph Annotation →
    字下げ → 1字 / 2字 / 3字 / 4字 / 解除
    地付き → 地付き / 地から1〜4字上げ / 解除
Page Break（改ページ）
Bullet List → - / * / +
Numbered List
Renumber
```

- `│`をキャレットとして、Wikiは未選択なら`[[│]]`、Markdownは`[│]()`を挿入する。
- ルビは選択「東京」に対して`｜東京《│》`、未選択なら`｜│《》`（合意済み）。
- 直接実行する項目名に`…`は付けない。リンクはファイル・見出し・Webではなく記法別に選び、既存補完を利用する。
- 別名付きWiki、左注記、傍点等の範囲と細部は提案。詳細案に選択あり／なしの挿入文字列・キャレット位置を明記した。
- 親文字を注記内にも繰り返すものは選択必須とする案。入力ダイアログや文字列の自動複製機能は追加しない。
- メニューを閉じるだけなら本文は変えない。選択して実行した後の取消しはUndo。挿入全体をUndo一回で戻す。
- 箇条書きは既存の選択行への適用・解除・番号振り直しを再利用する。本文注記の挿入と全体の組版設定は区別する。
- 見出し・字下げ・地付きも選択行全体へ適用し、未選択なら現在行。原文の改行が単位で、折返し行は別行としない。複数行は各行を変更する。
- 同種の行頭指定は置換し、解除は記号だけを取り除く。字下げ・地付きは行頭注記を使い、開始／終了の注記行を新設しない。初期字数候補は1〜4字、地付きは0字相当という案。
- 選択末尾が次行先頭なら次行を含めない。選択／キャレットは本文上の位置を維持し、複数行でもUndo一回。既存の範囲字下げや異種の指定との混在は別途扱いを確定する。詳細は挿入メニュー詳細案の行単位操作を参照。

- 改ページの記法 `［＃改ページ］` は独立行として既に解析・画面表示・印刷に対応している。Insertの一覧漏れを補正した。選択した各行に付ける操作ではなく独立した注記行を一つ挿入する案。挿入位置・選択／キャレットの扱いは未決であり、先に示した現在行の前へ置く案を現行仕様とは扱わない。解釈設定・コードフェンスの条件、印刷との違いは詳細案の改ページ節を参照。

### Layout：文書の見せ方・紙面設定

```text
Horizontal                         （現在値に印）
Vertical                           （現在値に印）
---
Pane Background…
Tab Color…
Terminal Appearance… / Panel Appearance…
```

- Horizontal／Verticalはトグル名にせず、選んだ方向にする。既にその方向なら変更しない。既存の切替キーは「反対の方向」の項目だけに表示する案。
- Viewerは現行の方向切替条件に従う。ReadOnlyは現行どおり横書き固定なのでVerticalを無効にする。Terminal・Panel・設定・空TABにも文書方向切替を誤適用しない。
- Text / Layout / Background Settingsは削除。書体・文字サイズ・見出し書式・行間・余白・ルビの組み方、背景画像・透過・新規TABの既定色は設定画面に置く。
- Pane Backgroundは対象Pane、Tab Colorは対象文書TABへ適用。Terminal Appearanceは現在のTerminal TAB、Panel Appearanceは現在のPanel TABの個別外観を変更する。既定値編集用のAppearanceへ接続しない。
- 個別Appearance内に文字設定があっても、そのTABだけへの変更なら対象操作として扱う案。全体の既定フォント設定とは区別する。既定へ戻す操作もその対象の上書きを解除する意味であり、全体既定値は変更しない。
- 記法の解釈条件は設定画面に置く。行番号・空白／改行記号の表示ON/OFFはViewに置く。

### View：画面の構成と移動

```text
Sidebar →
    Workspace
    Explorer
    Search
    History
    Outline
---
Live Preview                       （チェック）
Viewer Mode / ReadOnly Mode / Exit Viewer / Exit ReadOnly
ReadOnly                           （Editor Panelの場合、チェック）
Show Line Numbers                  （チェック）
Show Spaces and Line Breaks         （チェック）
---
Panes →
    Split Right
    Split Down
    Close Other Panes
    ---
    Swap Left
    Swap Right
    Swap Up
    Swap Down
Go To →
    Next Tab                       Ctrl+Tab
    Previous Tab                   Ctrl+Shift+Tab
    ---
    Left Pane                      Ctrl+Alt+Left
    Right Pane                     Ctrl+Alt+Right
    Upper Pane                     Ctrl+Alt+Up
    Panel Below / Lower Pane       Ctrl+Alt+Down
    ---
    Back                           Alt+Left
    Forward                        Alt+Right
---
Terminal Panel / Editor Panel      （チェック）
Latest Terminal Output
Focus Editor Above / Focus Terminal Above
Quick Draft…
---
Zoom →
    Zoom In
    Zoom Out
    Reset Zoom (100%)
    ---
    （現行の倍率一覧。現在値に印）
```

- Sidebarは現行アイコン列と同期し、現在表示中の項目にチェック。同じ項目を選ぶと閉じる。ブックマークは実装後の追加先をここに予定し、今回は行を置かない。
- 表示モードは3つの独立モードを新設せず、現行の2操作を表現する。Live PreviewのチェックなしがSource。ソースからの閲覧開始はReadOnly、PreviewからはViewer。閲覧中は解除用の表示名に変え、Live Previewの変更は無効にする案。常設ボタンも維持する。
- Show Line Numbers／Show Spaces and Line Breaksは選択するたびにON/OFFし、現在状態をチェックで示す。Viewに置くことは合意済み。現行は方向別の共有設定なので、対象文書だけの切替にするか既存の共有値を切り替えるかは実装前に確定する。今回の合意だけで保存単位・適用範囲を変更しない。Terminal等へ文書用設定を誤適用しない。
- Editor PanelではReadOnlyを独立したチェック項目として表示する案。ログ取り込み中に解除すると取り込みも止まる既存動作を説明し、停止項目からも明示的に止められるようにする。
- Panesは主Paneの分割・入替。Panelを操作中ならその親Paneを対象とする。隣がない方向へのSwapや、分割上限時のSplitは無効。
- Go ToのTAB移動は主PaneのTAB列を対象。PanelからのTAB巡回を新設しない。Panel Below / Lower Paneは現行キーと同じく、下段があれば下段、なければ下のPaneへ移る。移動先・履歴がなければ無効。
- Terminal Panel / Editor Panelは親TABの種類から表示名を選び、表示状態にチェック。同じ項目で開閉。ログやシェルの終了とは区別する。
- Quick Draftは既存ウィンドウを開く。履歴・コピー・送り先等の操作はその窓内で行う。

### Run：比較・記録・転送を実行する

Runの追加と、以下の4グループへの集約をユーザーが了承。New Terminalは作成操作としてFileに残す。

```text
Compare →
    With Saved
    With Git Last Commit
    With Another File…
---
Terminal Output →
    Save Output…
    Capture Log to File… / Stop File Log
    Capture Log to Editor Panel / Stop Panel Log
Send to Editor Panel →
    Selection
    Retained History
---
Switch Shell → （登録済みシェル）
```

- Compareは現在の通常文書と、保存済みファイル／Gitの最終コミット／別ファイルとの比較。Gitであることを表示名に明記する。既存の比較処理・適用条件を使い、Panel・Terminal・設定・空TABは対象にしない。
- Terminal Outputは現在のTerminalの一度の出力保存と継続ログ。ファイル／Editor Panelの開始・停止を別々に扱う。取り込み先Editor Panelからの停止等は後述の対応表に従う。
- Send to Editor Panelは選択内容または保持中の履歴を転送する。ログとは別の一度の操作。転送先の既存ルールを維持する。
- Switch Shellは現在のTerminalのシェルを切り替える。既存セッションの終了確認を通す。シェルプロファイルの登録・編集は設定画面で行う。
- 対象別の表示・無効条件は共通案に従う。Run追加を新しいGit操作や任意コマンド実行の追加承認とは扱わない。

### Help：操作を調べる

```text
User Guide…                        （新規内容の確認後に採用）
---
About RFN Edit…                    （新規画面の確認後に採用）
```

- キー一覧・割当変更は設定 → Keysへ。Helpは説明を読む／版情報を確認する役割に絞る。
- User Guideは同梱の操作説明を読む入口とする案。開発用READMEをそのままユーザー向け説明として出さない。内容・表示方法は別途作成が必要で、未提供なら項目を置かない。
- Aboutはアプリ名・製品バージョン・ビルド識別子とCloseボタンの小さな画面を提案。×／Esc／Closeで元の画面へ戻る。診断ログやローカルパスは載せない。採用時に取得する版情報と画面案を確認する。

### 操作対象と無効条件の共通案

メニューを開いた時点で「主Pane」「その中の文書／Panel／Terminal」「直前の入力欄と選択」を保持する。選択範囲がメニュー操作中に消えても別の場所へ実行しない。対象TABがなくなったり文書が変更された場合は、古い選択位置での書換えを実行せず、選び直すよう通知する。

| 直前の対象 | 基本編集 | 保存 | 挿入 | 文書Layout／表示モード | 新規・開く |
| --- | --- | --- | --- | --- | --- |
| 通常文書 | 選択・Undo履歴等に応じて有効 | 既存保存条件 | 編集可なら有効 | 既存条件 | そのPane |
| Viewer／ReadOnly | コピー等は可、書換え不可 | 現行条件。ReadOnlyの上書き不可 | 無効 | 閲覧モードごとの制限 | そのPane |
| Editor Panel | 対応済み操作だけPanelへ | Panel文書へ | 初回案は無効 | 文書用は無効 | FileはPanel、Terminalは親Pane |
| Terminal／Terminal Panel | コピー・貼り付け・検索・クリア等。転送・ログは下記対応表 | 文書保存は無効。Terminal Outputで対象の出力を扱う | 無効 | 個別Appearanceは可。文書方向切替は無効 | 親Pane |
| 設定TAB | 入力欄内操作と設定検索のみ | 無効 | 無効 | 文書用は無効 | そのPane |
| 空TAB | 無効 | 無効 | 無効 | 無効 | 選択中の空TABを既存処理で利用 |
| 検索等の入力欄 | その欄の文字列へ | 所属文書に対する既存条件 | 無効 | 所属文書の条件 | 所属する領域 |

- **共通項目は無効表示、対象固有の項目群は切替表示とする案。** Copy等は位置を固定し、選択なしならグレー。Terminal OutputやEdit → Terminalは対象がTerminalのとき表示し、取り込み先Panelでは関連する停止操作を表示する。対象となる種類でも未対応の操作は無効化し、親Terminalへの誤転送で代用しない。既存右クリックの非表示方針は維持する。未実装・今回不採用の機能は並べない。
- 最近のフォルダ、シェル、語群、倍率等の動的一覧は開くたびに現状へ同期。空一覧は親項目を無効にする。移動・表示項目のチェックと、実行可能かどうかは別に扱う。
- コピーは選択あり、Undo／Redoは履歴あり、貼り付けは対応データあり等を既存処理と揃える。メニューを有効にしても実行時に再確認する。
- ショートカットで既存動作が異なる場所には、誤ったキーを表示しない。Panelや入力欄への共通操作の接続は実装前に確認する。

### Terminal／Panelの操作を漏らさないための対応表

既存実装は `terminal_workflow.rs`、`terminal_panels.rs`、`terminal_appearance.rs` と画面の入口を照合。以下の「既存」はソース確認であり、今回の実操作確認ではない。

| 操作 | 配置 | 主Terminal TAB | 下段Terminal Panel | Editor Panel |
| --- | --- | --- | --- | --- |
| 出力を一度保存 | Run → Terminal Output → Save Output… | 既存 | 既存。下段のセッションを指定 | Save／Save Asで本文保存 |
| ファイルへ継続ログ開始／停止 | Run → Terminal Output | 既存 | 現行入口は主Terminalを取得。下段対応を仮定せず無効 | 親Terminalのログを暗黙に操作しない |
| Editor Panelへ継続ログ開始／停止 | Run → Terminal Output | 既存。開始時に新しい取り込み先文書 | 現行の取り込み元は主Terminal。今回は無効 | 自分への取り込み停止は既存。親から新規ログを始める入口は下記参照 |
| 選択内容をEditor Panelへ送る | Run → Send to Editor Panel | 既存。現在のPanel文書のキャレット位置へ挿入 | 現行は主Terminalの選択を取得。今回は無効 | 転送先。ReadOnly時に勝手に解除しない |
| 保持中の履歴をEditor Panelへ送る | Run → Send to Editor Panel | 既存。新しいPanel文書へ | 現行は主Terminalを取得。今回は無効 | 転送先。既存本文を上書きしない |
| コピー／貼り付け | Edit | 既存 | 既存 | 既存の本文操作 |
| 出力検索 | Edit → Find in Terminal… | 既存 | 既存 | Panel本文検索は対応を確認してから有効化 |
| 画面／履歴クリア | Edit → Terminal | 既存 | 既存 | 文書のクリアと混同しない |
| シェル切替 | Run → Switch Shell | TABの既存切替 | Panelの既存切替 | 対象外 |
| 最新出力へ移動 | View → Latest Terminal Output | 既存 | 既存 | 文書キャレットを移動する操作には転用しない |
| 下段を表示・隠す／上へ戻る | View | Editor Panelを開閉 | 親Editorへ戻る／下段を隠す | 親Terminalへ戻る／下段を隠す |
| 個別外観 | Layout → Terminal／Panel Appearance… | 現在のTerminal TAB | 現在のTerminal Panel TAB | 現在のEditor Panel TAB |
| New File／Open File／Save／Save As／Close | File | 文書保存は対象外 | 文書保存は対象外 | 既存Panel文書操作に接続 |
| 文書名変更 | File → File Operations → Rename Panel Tab… | 対象外 | 対象外 | 既存Panelの名前編集と同じ意味。通常ファイルのRenameとは区別 |
| ReadOnly | View → ReadOnly（チェック） | 対象外 | 対象外 | 既存。取り込み中の解除は取り込み停止も伴う |

**ログの開始・停止・対象の表示**

- Save Outputは保持中の出力のスナップショット保存。Capture Logは以後の出力を継続記録する別操作。保持上限を越えた過去の全出力まで保存できるような文言にしない。
- ファイルログとPanelログは独立した開始／停止項目とする。一方を停止しても他方を止めない。両方同時に記録できる現行の仕組みを維持する。
- 未記録時はCapture Log to File…、記録中はStop File Logへ切り替える。Panelログも同様。ファイル選択のキャンセル・開始失敗では記録中表示にしない。
- Run → Terminal Outputの先頭に、対象のTAB名と主領域／下段の区別を補助表示する案。記録中は保存先ファイル名／Panel文書名を添え、長いパスはツールチップ等で確認できるようにする。
- ファイルログの開始・停止だけでEditor Panelを開かない。停止は記録先を消さず、シェルも終了しない。
- Editor Panelへのログは現行どおり新しい取り込み先を作り、記録中はReadOnly。選択転送は現在のPanel文書へ、履歴転送は新しいPanel文書へ、という違いを操作説明に明記する。
- Editor Panelを操作中のStop Panel Logはその文書が受けている取り込みを止める。隣のPanel文書や無関係なTerminalの記録を止めない。
- Editor Panelの既存メニューには親Terminalからログを開始する入口もある。メイン側では `Capture Parent Terminal Log to New Panel` と明示する案。これは現在のPanelに追記する操作ではなく、新しい取り込み先を作る操作。取り込み中は自分へのStopを優先して表示する。
- Clear Screen / Clear Historyは対象セッションの既存処理を使う。保存済みログファイル・取り込み済みPanel本文の削除やログ停止を兼ねる操作にはしない。
- 終了済みセッションでは開始・シェルへの貼り付けなど実行できない操作を無効化。保持出力のコピー・検索・保存は引き続き可能な範囲で利用する。
- 下段Terminal Panelの継続ログ・Editor Panelへの転送を可能にすることは、単なるメニュー接続ではない。必要性が決まった場合は対象セッションの扱いと転送先を別途設計する。

## 設定画面で扱う項目（メニュー一覧には含めない）

| 設定画面で扱うもの | メニューに残す対になる操作 |
| --- | --- |
| 本文・見出しのフォント、サイズ、装飾・色の既定設定 | 個別TABの色／Paneの背景 |
| 行間、余白、折返し、ルビ・縦中横の共通組版条件 | 現在文書の縦横切替 |
| 全体背景画像・透過・新規TABの背景 | 個別Terminal／Panelの外観 |
| Terminal／Editor Panel／Terminal Panelの外観既定値 | 同上の個別上書き |
| シェルプロファイルの登録・編集・既定値、履歴保持数、確認設定 | 登録シェルで起動／現在セッションのシェル切替 |
| 語群・モードの共有管理 | 現在の文書に適用する単語チェックモード |
| キー割当、言語、全体リセット、自動退避等 | 該当設定の入口はメインメニューに追加しない |

全体の文字・組版・背景設定への入口はメニューに置かない。個別Appearanceでフォントを変えることと、全体の既定フォントを変えることを区別する。行番号・空白／改行記号は今回の合意によりViewに置き、この設定専用一覧には含めない。設定検索は設定TAB内の既存操作として残す。

### このリストの確定に残る項目

1. 上記の表示順・子メニューと、FileのClose系を直接並べる構成。
2. 共通項目は無効表示、Terminal等の対象固有項目群は切替表示とする案。右クリックは従来どおり非表示。
3. PanelからのNew File／Save／Closeの対象、Print・Compare等の対象制限。
4. ひな形挿入の対象記法・選択あり／なしの結果、注記等の初回範囲。入力画面は作らない。
5. HelpにUser GuideとAboutを今回含めるか。
6. タイトル行への配置方法と、以下の開閉動作案・キー操作。

まずこのリストを会話で調整し、残る技術確認・画面案をまとめてから実装に進む。未決項目を実装側で勝手に確定しない。

## 個別の場所に残す操作

メインメニューを作っても既存の右クリック・ボタンは原則維持する案。ただしNew Tabは＋ボタン専用とするため、TAB列右クリックのNew Tabと、KeysのNew Tab割当項目は整理対象。＋で開いた空TAB内のNew File / New Terminal / Open Fileは維持する。既存の保存済みキー設定を壊さない扱いは実装時に確認する。

- Explorer：ファイル／フォルダ作成、名前変更、複製、削除、そのフォルダでTerminal起動・検索、Explorerで表示。
- Workspace：切替、新規・複製・名前変更・既定化・解除・表示リセット、フォルダ追加・並替・移転・解除、Clone。メイン側は管理画面への入口を置く。
- Terminal：右クリック入口も維持し、出力保存・ログ・転送・検索・クリア・シェル変更・最新出力・個別外観をメインメニューにも配置する。右クリックに残すことを、メイン側から除外する理由にしない。
- Editor Panel：文書追加・開く・保存・名前を付けて保存・閉じる・ReadOnly・個別外観・取り込み停止をメイン側にも接続する。Panel名の変更もFile Operationsの対象別項目として整理する。
- Quick Draft：履歴、クリア、全文コピー、コピーして閉じる、TABへの貼り付け。独立窓内に維持。
- 比較画面：差分への移動、左右のコピー・採用、反映、比較終了。比較中の画面に維持。
- 印刷画面：用紙・方向・余白・倍率等。印刷プレビュー内に維持。
- 検索バー：大文字小文字、単語、選択範囲、正規表現、前後の結果、履歴、置換・全置換。検索画面内に維持。

## メニュー開閉の動作案

### 配置は2案を比較する必要がある

| 案 | 表示 | 調査上の含意 |
| --- | --- | --- |
| A：タイトル行へ統合 | 最上部のファイル名の場所に7分類。閉じると通常の簡素なタイトル行 | ユーザーの希望に近い。標準タイトルバーへの統合方法を技術検証し、移動・最大化・最小化・閉じる・Snap・DPIを維持する必要がある |
| B：タイトルバー直下 | 開いたときだけアプリ内の上端に1行追加 | 現行UI構造を利用しやすいが、タイトルバーは残り、開閉で本文領域の高さが変わる |

A案の採用はユーザー確認済み。Bへ勝手に変更せず、Aの具体的な実現方法と画面仕様を詰める。ファイル名を消す場合も、TAB／下部の未保存表示は残す。ウィンドウタイトル文字列まで消すかは別に判断する。

### 共通の提案

1. 初回は閉じた状態。左上のMenuボタンで表示・非表示を切り替える。
2. メニューバーの開閉状態は実行中維持し、再起動時は閉じる案。永続保持は未決。
3. 項目を実行したらドロップダウンを閉じ、バーは表示したまま。元の編集対象へフォーカスを戻す。別画面を開く操作では、その画面へ移す。
4. 外側クリックではドロップダウンを閉じる。バーはMenuボタンで閉じる。
5. Escは子メニュー→ドロップダウン→バーの順で閉じ、本文へ戻す案。IME変換中のEscを奪わない。
6. キーボードではF10などの専用入口を候補にし、矢印・Enter・Escで操作。Alt+文字の既存Emacsキーと衝突させない。最終キーは既存入力処理と照合して決める。
7. 設定済みショートカットを項目に併記し、変更後も同期。新規文書へCtrl+Nなどを無断で追加しない。
8. 最小幅640px、日本語・英語、高DPIでも7分類とウィンドウ操作が重ならない配置を確認する。

## 対象・状態の扱い

- メニューを開く直前の文書／Pane／Panel／入力欄を操作対象として保持し、メニューにフォーカスが移ったことを理由に対象を変えない。
- 設定TAB、空TAB、Terminal、Editor Panel、Viewer、ReadOnly、比較表示、IME変換中を分ける。
- 保存・挿入・Undo等を、Terminalや設定TABの裏にある代用文書へ送らない。
- コピー・貼り付け等は検索欄も含め実際の対象に従う。TerminalのコピーはCtrl+Shift+Cであり、文書のCtrl+Cと表記・処理が異なる。
- できない操作の扱いは既存要件6.7では「非表示」。第4稿では共通項目は無効表示、対象固有項目群は切替表示、既存右クリックは非表示を維持する案。採用は未確定であり、既存要件を黙って変更しない。
- 新規挿入はメニュー直前の選択範囲を保持し、項目選択でひな形を即時挿入する。メニューを閉じるだけなら変更しない。実行後はUndo一回で戻す。入力画面は設けない。

## 作業の切り分けと確認事項

- RFN01-34：既存操作のメニュー入口、分類、開閉、操作対象、キーボード操作。Helpの新規画面の範囲も合意する。
- RFN01-12：リンク・ルビ等のひな形挿入。選択あり／なしの生成文字列、キャレット位置、既存補完への接続、Undoを具体化する。
- RFN01-18：メニューとキーの対応、変更可能な操作の追加範囲。Terminalや入力欄のキー設定拡張を自動的に含めない。
- RFN01-28：ブックマーク本体は別途検討。
- 採用時に更新する仕様：要件定義6.7（操作の配置、表示モードをメニューへ入れない旧規定）、9.2（設定分類）、11.8（キー追加時）。今回の調査では既存仕様を書き換えない。
- 実装後の確認：入口から操作できること、複数Pane・Panelで対象が変わらないこと、キャンセルで選択・本文・フォーカスが保たれること、Viewer／ReadOnly等で誤編集しないこと、キー競合・IME・タイトルバー操作・狭い幅とDPIを確認する。

## 主な根拠箇所

- `ui/app-window.slint:130`：ウィンドウタイトル。`:1192`付近：左上アイコン。`:1282`付近：既存アプリメニュー。`:1478`付近：設定入口。`:2887`付近：設定の現行4分類。`:5150`付近：下部ファイル名・文字コード等。
- `ui/editor-pane.slint:1254`付近：TAB列ボタン。`:1394`付近：ペインメニュー。`:3033`付近：本文メニュー。`:3442`付近：空TAB。`:3562`付近：Panelメニュー。`:3931`付近：TAB右クリック。
- `src/shortcuts.rs:4`：変更可能な46操作（Quick Draft内の4操作を含む）。`:160`付近：固定キー。`:543`付近：対象判定とディスパッチ。
- `src/main.rs:2360`・`:2380`：New TabとNew Fileの別コールバック。`:2595`：箇条書き。`:3069`：本文コピー。`:3650`：終了処理。`:10942`・`:10947`：新規TAB／新規文書。
- `src/wiring.rs:1506`：印刷入口。`ui/controls.slint:1141`：共通MenuRow（新しいバーのキーボード動作は別途設計が必要）。
- `src/terminal_panels.rs`：action、import、start_file、stop_source。主Terminalを取得するログ・転送と、Panel文書操作の区別。`src/terminal_workflow.rs`：TerminalSpotで主／下段を分ける検索・クリア・出力保存。`src/terminal_appearance.rs`：Default／Front／Panelの適用先の区別。
