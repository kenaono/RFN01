# 書式のツールバーとパレット

2026-09-23、書き手と会話で合意。画面案：https://claude.ai/artifact/CeuGTSiciNSeJcKLM83EzV

## 合意した仕様

- **中身**：Insertメニューの全項目。ツールバーとパレットは同じ並び・同じ絵で、どちらを使ってもよい（片方だけでも両方でも出せる）。
- **入口**：View →「Toolbar」「Format Palette」（チェック付き、別々）。パレットの×で閉じるとチェックも外れる。初期状態はどちらも非表示。表示状態とパレットの位置は次回起動へ引き継ぐ。
- **ボタン**：Insertメニューの1段目1つにつき1つ（16個）。組（▸の項目）は右下に小さな三角があり、押すと右クリックメニューと同じ文字の行が開く（ツールバーは下へ、パレットは横へ。窓の右端では左へ）。箇条書きはInsertメニューでは3行だが、右クリックメニューと同じく1つのボタンにまとめ、- * + を子の行にする。
- **束**：リンク・ルビ・注記／傍点・傍線・文字注記／ハイライト・コメント・脚注・Callout／見出し・段落注記・改ページ／箇条書き・番号付き・振り直し。ツールバーでは縦線、パレットでは行で区切る。
- **ツールバー**：メニューバーの直下に窓の幅いっぱいで1本（34px）。並びきらない分は末尾の「⋯」にまとめる。「⋯」の中の組を押すと、同じ欄が子の行に替わる。
- **パレット**：アプリの窓の中で最前面（ペイン・サイドバー・Terminalの上）。メニュー・右クリックメニュー・確認の問い（とその幕）はパレットより上。窓の外には出せず、タイトル行にも入らない。窓を縮めると中へ押し戻す。置いたことが無ければ右上。
- **挿入先**：最後に字を打っていたペイン（キーで走らせるメニュー操作と同じ対象）。押してもキャレットと入力のフォーカスは本文のまま。
- **押せる条件**：Insertメニューと同じ判定（`context_insert_state`、箇条書きは記号を読むか）。キャレット・選択・ペインを動かすと追いかけて変わる（120msごとに組み直し、変わったときだけ画面へ渡す）。
- **アイコン**：線画（16×16・線幅1.5）。ルビ・注記・傍点・傍線は既存の絵が無いので新しく描いた。形は後から差し替えられる（書き手：いったん進めてよい）。

## 実装

- `ui/format-bar.slint`：`FormatButtons`（ツールバー／パレット共通の並び、組の行と「⋯」の欄）。
- `ui/app-window.slint`：ツールバー（タイトル行の下、本文側を34px下げる）とパレット（問いの幕の直前＝最前面）、表示状態と位置のプロパティ。
- `src/menu_commands.rs`：`format_face`（絵と束）、`publish_format`（組み直し）、`run_format`（押された番号→メニューと同じ`Command`）、Viewメニューの2行。`text_state`をタイトルバーのメニューと共用。
- `src/app_data.rs`・`src/session.rs`・`src/main.rs`：セッションの`format-toolbar`・`format-palette`・`palette-at`、タイマーと押下の接続。

## 確認手順（Accept用）

1. View → Toolbar：メニューの下にアイコンの帯が出る。もう一度選ぶと消える。
2. View → Format Palette：右上に「Format」の小窓が出る。×で消え、Viewのチェックも外れている。
3. 本文で「東京」を選び、パレットのルビ（2つめ）を押す：`｜東京《》`になり、キャレットは《》の中。続けて読みを打てる。
4. 何も選ばずに傍点（▸付き）を押す：行が開き、選択が要る項目は灰色。「傍点」を押すと本文に入る。
5. 見出しの行にキャレットを置き、見出しボタンを開く：今のレベルにチェックが付いている。
6. 窓を狭くする：ツールバーの末尾が「⋯」になり、押すと入らなかったボタンが出る。そこで箇条書きを押すと - * + の行に替わる。
7. パレットをつかんで左下へ動かし、終了→起動：同じ場所に出る。ツールバー・パレットの表示状態も同じ。
8. 右クリックメニューをパレットの上で開く：メニューがパレットより上に出る。
9. Terminal・設定画面・ReadOnlyに字を入れている間：ボタンはすべて灰色。

## 未確認

- 実機（Windows）での表示・DPI・ドラッグの手触りは未確認。画面は試験用の描画（ソフトウェアレンダラー、1000×740と420×740）で確かめた。
- 縦書きペインでの挿入結果は、既存のInsertメニューと同じ処理なので個別には確かめていない。
