# 縦書き編集 技術検証

## 1. 目的

本体実装へ進む前に、RustとSlintを使ったWindows 11向けEditorで、次の構成が成立するかを確認する。

- Slintをアプリ全体のUI基盤として使用する。
- Rust側にMarkdown原文を保持する文書モデルを置く。
- 縦書きライブ編集面を、Slint標準の`TextEdit`から分離した独自コンポーネントとして実装する。
- 縦書きの文字レイアウトとヒットテストにDirectWriteを使用する。
- 長文でも、操作コストが文書長ではなく表示領域に比例する構造にする。

## 2. 検証環境

- 検証日: 2026-08-14（縦書きペインとブロック分割）、2026-08-15（書字方向の一般化、横書きペインの置き換え、任意の文字送り・列間・見出しサイズ、論理行の内側での分割、走査の差分化）、2026-08-16（IME、選択操作、3モード目）
- OS: Windows 11
- Rust: 1.97.1 (`x86_64-pc-windows-msvc`)
- Cargo: 1.97.1
- Slint: 1.17.1
- windows-rs: 0.62.2

## 3. 実装した検証項目

### 3.1 Slint UI

- 標準は横書きまたは縦書きの単一編集面とし、ツールバーから切り替える。
- 非選択側は`visible`の切り替えではなく条件付きUIから外し、レイアウトと再描画の対象にしない。
- Splitを有効にした場合だけ、横書きと縦書きを左右へ同時表示する。
- 左右ペインの希望幅は内容から切り離し、分割比だけで幅を決める。内容に応じた幅要求を許すと、入力した側のペインが広がって境界が動く。
- 境界線はドラッグで動かせる（要件11）。両ペインの希望幅が0なので、行の全幅が「余り」として分割比のとおりに配られる。ペインをレイアウトから外して幅を直接指定するより、Slintの仕組みに乗ったままで済む。分割比は0.15〜0.85で頭打ちにして、片側が潰れないようにする。
- 境界のドラッグは「押した位置からの差」ではなく「前回からの差を足し込む」形で計算する。境界は分割比に従って動くので、レイアウトがこの要素をポインタの下へ動かした時点で`mouse-x`は`pressed-x`へ戻る。押した位置を基準にすると同じ移動量を二重に数える。
- ウィンドウの初期サイズは`preferred-width`/`preferred-height`で与える。`width`/`height`を直接指定するとウィンドウマネージャに強制される固定サイズになり、リサイズできなくなる。
- 縦書きの列の高さはペインの実高さに追従する。列に入る文字数が変わるため、高さはレイアウト全体を決める入力である。
- ウィンドウのリサイズは全ブロックの再計測を伴うので、サイズ変更が150ms止まってから再レイアウトする。ドラッグ中は毎フレーム再計測しない。
- 横書きモードの複数行ソース編集
- 縦書きモードの横方向スクロール領域
- 長い縦書き描画を画像タイルへ分割し、可視領域と交差するタイルだけを遅延生成・保持（幅はペイン高さから決まり、既定の高さでは1024px）
- 選択背景とキャレットを画像からSlintオーバーレイへ分離し、ドラッグ中は画像タイルを再生成しない
- 拡大・縮小操作とステータスバーの倍率表示
- 行数、本文文字数、ソース文字数の表示
- 縦書きキャレット位置へ追従する透明なSlint `TextInput`によるWindows IME入力ブリッジ
- 縦書き編集面では、横棒と両端の短い縦線からなる専用マウスポインターを表示する。

### 3.2 文書モデルと縦列モデル

- Rust側の共有文書バッファをMarkdown原文の正本とする。
- 単一表示では選択中の編集面だけを更新し、Split表示の場合だけ両方を即時同期する。
- 表示モード切り替え時に、共有文書バッファから切り替え先の編集面を構築する。
- 右上から開始し、列が右から左へ進む順序でデータを生成する。
- 各列に対応するUTF-8原文のバイト範囲を保持する。
- 改行で新しい縦列を開始する。
- Unicode書記素クラスタ単位で文字数と表示セルを扱う。
- 絵文字の結合シーケンスを1文字・1セルとして保持する。
- 整形後プレビューのUTF-16位置からMarkdown原文のUTF-8バイト位置への対応を保持する。対応表は単調非減少なので、位置の変換は二分探索で行う。
- キャレット移動と削除はUnicode書記素クラスタ単位で扱う。

### 3.3 DirectWrite

DirectWriteの`IDWriteTextFormat`へ次を設定した。

- Reading direction: `TOP_TO_BOTTOM`
- Flow direction: `RIGHT_TO_LEFT`
- Locale: `ja-JP`
- Font: `Yu Mincho`

`IDWriteTextLayout`の作成、レイアウトメトリクス取得、`HitTestTextPosition`によるキャレット座標取得に成功した。

加えて、DirectWriteのレイアウトをDirect2DのWICビットマップ描画ターゲットへ描画し、BGRAからRGBAへ変換してSlintの`Image`へ表示した。これにより、Slintの文字部品による代替表示ではなく、DirectWriteが選択した縦書き字形と配置を画面上で確認できる。

検証文字列`日本語ABC123`では、起動時の検証結果として次を得た。

- レイアウト: 約39×162 px
- 先頭から次の文字位置へのY方向移動量: 24 px

この結果から、Windows 11上でDirectWriteによる「上から下へ読み、列が右から左へ進む」テキストレイアウトと、原文位置からキャレット座標への変換が利用できることを確認した。

### 3.4 ブロック分割レイアウト

文書全体を1つの`IDWriteTextLayout`にすると、描画・ヒットテスト・キャレット移動・選択のすべてが毎回そのレイアウトを作り直すことになり、操作コストが文書長に比例する。これを避けるため、文書を論理行境界で分割し、ブロックごとに独立したレイアウトを持たせた。

**軸の語彙。**分割・配置・タイルの幾何は`text_blocks.rs`に隔離してあり、縦横どちらの書字方向でも同じものを使う。座標は**流れ軸（flow axis）**、すなわち行が積み上がり文書が際限なく伸びる方向で表す。直交する**行軸（line axis）**は1行が走る方向で、その長さはペインが決める。

| | 流れ軸 | 行軸 |
| --- | --- | --- |
| 縦書き | 画面x、右から左 | 画面y、ペイン高さ |
| 横書き | 画面y、上から下 | 画面x、ペイン幅 |

流れ軸を画面のどちらへ割り当てるかは呼び出し側の責任で、`text_blocks.rs`はそれを知らない。座標は常に「対象の小さい側の辺＋大きさ」で表すので、向きに関係なく`flow_start + flow_size`が大きい側の辺になる。ただし**読み順が流れ軸のどちらへ進むか**だけは知る必要がある。縦書きは先頭ブロックが右端に来て、読み進むほど座標が小さくなるからである。これを`FlowOrder`（`Ascending`＝横書き、`Descending`＝縦書き）として持たせた。方向で分岐するのは、積み始める端（`place_blocks`）、順序に依存する2つの探索（`block_at_flow`、`blocks_in_flow_range`）、タイルをブロックのどちらの端から切るか、の4箇所だけである。行の移動やUTF-16位置の索引は読み順そのものなので分岐しない。

なお「行」は常に視覚上の行（DirectWriteが行メトリクスを返す単位）を指し、改行から改行までは「論理行」と呼び分ける。縦書きでいう「列」が「行」にあたる。

**分割の単位**

- DirectWriteは改行ごとに新しい列を開始するため、論理行の境界で切れば列の位置は変わらない。分割は必ず`\n`の直後で行い、論理行の途中では分割しない。
- ブロックの大きさは文字数ではなく、行が占める空間で数える。各行を`ceil(文字数 ÷ 1行のセル数) × セル数`で課金し、最小256セル・最大768セルの範囲で切る。空行は1文字だが1行ぶんをまるごと使うので、文字数で数えるとブロックの大きさに上限が無くなる（4.4）。
- 1つの論理行が最大を超える場合は、その行だけで1ブロックとする。
- 切れ目は行の内容そのもので決める。行のFNVハッシュが`hash % 256 < その行のセル数`を満たす行末だけが境界候補になる。長い行ほど候補になりやすいので、行長がまちまちでも平均のブロックサイズは一定に保たれる。文書先頭からの累積で決めると、挿入のたびに以降の境界がずれて全ブロックが書き換わる（4.6）。
- ブロックは文書が始まる側の端から積む。縦書きなら右端からなので、あるブロックのx座標はそれより後ろのブロックの幅の合計になる。横書きでは上端から積み、それより前のブロックの高さの合計になる。積む向きが違うだけで、ブロックの並び（配列の順序）は常に読み順である。
- ブロックの幅は自分の計測値だけを丸めて整数にする。累積した座標を丸めてはいけない。左方のブロックが変わるだけで丸め結果が1px変わり、本文が同じブロックのタイルが無効になる。丸める前の値は`exact_width`に残し、単一レイアウトとの一致検証はそちらで行う。
- 計測結果は「ブロック本文＋最終ブロックか」で、レイアウトオブジェクトは「ブロック本文」でキャッシュする。編集時に再計測されるのは、変更のあったブロックだけである。

**タイル**

- タイルはブロックを横に切ったものであり、識別子は「何番目のブロックの何枚目か」である。**座標を含まない。**キャッシュのキーは署名（ブロック本文・スライス番号・幅・高さ・フォントサイズ・ブロック内の変換中範囲）で、これも座標を含まない。本文が変わらないブロックのタイルは、レイアウトがどれだけ横へ滑っても再利用される。
- 画面をn等分する格子ではこれが成立しない。列が1つ増えると文書は右端側へ広がるので、右端基準にすれば編集位置より前は保持されるが後ろが無効化され、左端基準ではその逆になる。単一の格子である限りどちらか片側は必ずずれる。
- スライスの幅は「幅×高さが一定」になるよう高さから決める。タイルは文書と同じ高さなので、幅を固定すると全画面で1枚あたりの描画量が数倍になる。
- ブロック幅はまちまちなので、Direct2Dの描画先はスライスの最大幅で1つだけ確保し、`CopyPixels`に`WICRect`を渡して実幅ぶんだけ読み出す。タイルごとに確保すると`CreateWicBitmapRenderTarget`が毎回走る。ここが単体で最も重い呼び出しである。
- 署名が一致したタイルは、描き直さずに配置座標だけ与える。配置はキャッシュしない。
- 文書の左右の余白はどのタイルにも属さない。`app-window.slint`の用紙矩形が同じ`#fffdf7`なので見た目は変わらない。背景色を変えるときは両方を合わせること。

**表示領域への限定**

- 描画、ヒットテスト、選択矩形は、表示領域と交差するブロックだけを対象にする。選択矩形はさらに、表示領域と交差する列まで絞る。
- キャレットの左右移動と`Home`/`End`は、計測時に作った列テーブルから求める。文書全体の走査は行わない。
- Markdownを展開する行は、キャレットが120ms止まってから切り替える。展開はその行を含むブロックの本文を書き換えるため、ブロック1つ分の再描画を伴う。カーソルキーの連続移動でこれを毎回払うと引っかかる。

### 3.5 日本語IME

DirectWriteが返す縦書きキャレットの矩形へ、文字色とキャレットを透明にしたSlint `TextInput`を配置した。Windows IMEの変換中文字列は原文へ確定せず、同じキャレット位置へ一時的に挿入してDirectWriteで再描画する。変換中の範囲にはDirectWriteの下線を設定する。

IME確定時だけMarkdown原文へ文字列を挿入する。IME確定に使ったEnterと通常の改行Enterを分離するため、変換確定直後の`accepted`を一度だけ抑止し、短時間後に通常状態へ戻す。

### 3.6 書字方向の一般化

engineは`WritingMode`（`Vertical` / `Horizontal`）を1つ持ち、生涯それを変えない。方向に依存する判断はこの型のメソッドに集約し、engine本体は流れ軸と行軸だけで書かれていて、画面のxやyを自分で名指す箇所は無い。

| メソッド | 役割 |
| --- | --- |
| `flow_order` | `text_blocks`の`FlowOrder`へ翻訳する。縦書きは`Descending` |
| `to_screen(flow, line)` | 2軸の点を画面座標へ。レイアウト箱やタイル画像の縦横も同じ入れ替えで決まる |
| `to_axes(x, y)` | その逆 |
| `flow_of(metrics)` | ヒットテスト結果の流れ座標。縦書きは`left`、横書きは`top` |
| `apply_to(format)` | DirectWriteの`SetReadingDirection`と`SetFlowDirection` |

具体的に変わるのは次の3種類だけだった。**DirectWriteへ渡すレイアウト箱**（縦はブロックの流れ方向上限×ペイン高さ、横はペイン幅×流れ方向上限）、**行メトリクスから流れ座標を読む位置**（`left`か`top`か）、**描画とヒットテストの原点**（ブロックのオフセットと余白のどちらを画面xへ割り当てるか）。行の送り量が`DWRITE_LINE_METRICS.height`であること、ブロックの大きさがその合計であること、末尾の空行を最終ブロック以外が手放すこと——計測の中身はどちらの方向でも同じ式のままである。

engineの`height`フィールドは`line_extent`へ改めた。縦書きではペイン高さ、横書きではペイン幅を指すためである。`Default`は`Vertical`なので、検証済みの縦書き経路は無変更で動く。

**この一般化で最も確かめたかったのは、分割したブロックの合計が単一レイアウトと一致するという不変条件が横書きでも成り立つことである。**縦書きと同じ4通りの段落数で成立した。行の送り方向が変わっても、ブロックが自分の行メトリクスだけから大きさを決められるという前提そのものは方向に依存しない。

### 3.7 横書きペインの置き換え

SlintのTextEditを撤去し、横書きペインを縦書きと同じ構造——engine・ブロック分割・タイル画像・DirectWriteのヒットテスト——へ載せた。Slint側に残るテキスト部品は、透明なIMEブリッジ用`TextInput`だけである。

**このペインはMarkdown原文をそのまま描く。**縦書き側は`PreviewDocument`で整形後テキストを作り、原文位置との対応表を引くが、横書きのソース編集面ではその対応が恒等写像になる。結果として、プレビューの再構築も、キャレット行のMarkdown展開待ちも要らない。8章に書いた「ソース編集では位置の対応表が恒等写像になる」という見通しの、実装による確認にあたる。

構成は次のとおり。

- ペインごとに`TextEngine`（`WritingMode::Horizontal`）とタイルキャッシュを持つ。文書は1つを共有し、**キャレットと選択はペインごとに別**である。片方の編集はもう片方のキャレットを動かさず、再レイアウトだけを起こす。
- タイルは画面yへ積む。`x`は常に0で幅はペイン幅、高さがタイルの流れ方向の長さになる。`PreviewTile`に`y`と`height`を足し、両ペインが同じ構造体を使う。
- 文書は上端から始まるので、**行が増えてもスクロール補正は要らない。**縦書きは右端から始まるため補正が必要だった（4.3）。向きの違いがそのまま出る箇所である。
- ペインの**幅**が1行の文字数を決めるので、幅の変更は縦書きの高さ変更と同じく150ms待ってから再レイアウトする。Split境界のドラッグもここを通る。
- キー割り当ては画面方向で決める。横書きは左右で1文字、上下で1行。縦書きは上下で1文字、左右で1列。engineへ渡す値は同じ規約（±1が文字、±2が行）で、「次に読む行」がどちらかは`WritingMode`が決める。

`EditorState`（キャレット・選択・IME変換中文字列）は両ペインで同じ型を使う。行軸方向の保持位置は縦書きではy、横書きではxなので、`preferred_y`を`preferred_line`へ改めた。

### 3.8 任意の文字送り・列間・見出しサイズ

7章に残していた「任意に指定した縦方向の文字送りと横方向の列間隔」「フォントサイズや見出しサイズが異なる場合の文字送り・列間隔」を実装した。engineはそれまで**文書全体で1つのフォントサイズと1つの送り量**を前提にしており、そこを崩したときに3.4の構造が保つかどうかが検証の対象である。

**3つの量を`Typography`にまとめた。**`font_size`以外はすべて比なので、1つの指定が倍率によらず同じ見た目になる。

| 量 | 型 | DirectWrite | 既定 |
| --- | --- | --- | --- |
| 字送り | `character_spacing`（自分のサイズに対する比） | `IDWriteTextLayout1::SetCharacterSpacing` | 0.0 |
| 列間 | `line_spacing`（行の送りの倍率） | `IDWriteTextFormat3::SetLineSpacing`（PROPORTIONAL） | 1.0 |
| 見出しサイズ | `heading_scale: [f32; 6]`（レベルごと） | `IDWriteTextLayout::SetFontSize`（範囲指定） | 1.6〜1.1 |

見出しは**レベルごとに任意の値**を持つ配列とした。UIの1ノブは`with_heading_ramp`が配列を埋めるだけで、engineは6つのサイズを読むだけであり、その並びに何も仮定しない。

**行ごとの属性はテキストから読み戻せない。**縦書き面が組むのはプレビューであり、そこでは見出しマーカーが既に除去されている。そのためブロックは自分の文字から「この行は見出しだ」と知ることができない。属性は`StyledText`——本文と**論理行ごとの見出しレベルの配列**——として横に並べて渡す。分割は論理行境界でしか起きないので、**あるブロックの属性は必ずこの配列の連続部分列になる。**3.4の「文書先頭からの累積を分割規則に持ち込まない」は属性にもそのまま効いており、見出しレベルはその行だけで決まるので、前の行が変わってもブロックの属性は動かない。

DirectWriteへ渡す範囲は`style_runs`が返す**ブロックローカルなUTF-16範囲**である。IME変換中の下線と同じ扱いで、ブロックの位置には依存しない。キャッシュの鍵（レイアウト・計測・タイル署名）にはこの範囲と、**レベルではなく実際のサイズ**を入れた。あるレベルに同じサイズを与えた2つの指定は同じ画素を描くので、タイルを共有してよい。

見積り式も一般化した。`cells_per_line`はサイズではなく**送り量**（`font_size × (1 + character_spacing)`）で割る。1行の課金は見出しを**二重に数える**——1行に入る字数が`size_scale`ぶん減るので折り返しが増え、さらにその1行1行の送りが`size_scale × line_spacing`ぶん伸びる。片方だけでは、見出しばかりの頁が同じブロック予算で流れ方向へ静かに伸びる。

原文（横書きペイン）とプレビュー（縦書きペイン）は論理行が1対1に対応するので、レベルの配列は**両ペインが同じものを読む**。`HeadingSlot`が原文をキーに1つ持ち、1打鍵につき1回だけ走査する。

### 3.9 論理行の内側での分割

6.9で「許容できる段落長はブロック予算そのもの」と分かったので、あとは論理行の内側でその予算を適用する手段だけが要る。分割は`\n`の直後でしか行えないという規則を外した。

**ブロックの境界は、行が始まる位置でなければならない。**そうでなければ次のブロックが行の途中から始まり、その中の行がすべて違う場所に描かれる。改行は常にそういう位置なので、これまでこの条件は問われなかった。論理行の内側で切るには**組版エンジンが選んだ折り返し位置**が要り、それはエンジンしか知らない。フォント、幾何、その言語の行分割規則で決まるからである。

そこで`text_blocks.rs`に`WrapPoints`を置き、答えを引数として受け取る形にした。この module は算術だけでDirectWriteを知らないという性質（Windows以外でも検証できる）を保つためである。

| 実装 | 置き場所 | 役割 |
| --- | --- | --- |
| `LayoutWraps` | `directwrite_render.rs` | 段落を組版して`GetLineMetrics`から行頭を読む |
| `EveryNCharacters` | `text_blocks.rs`のテスト | N文字ごとに折り返すことにする代役 |
| `NeverWraps` | 同上 | 「切るな」。この機能を入れる前の挙動 |

`split_blocks`は、1行で1ブロックを超える論理行に出会ったときだけ`WrapPoints`に訊き、`BLOCK_MAX_CELLS`ぶんごとの折り返し位置で切る。**普通の段落は`WrapPoints`に到達しない。**答えが空なら「切るな」の意味で、従来どおり1つの巨大ブロックになる。組版を作れないことは、そのテキストに触らない理由ではあっても、文書全体の組版を止める理由ではない。

**論理行カーソルの数え方を直した。**切られた断片は改行を含まないので、「覆う行数」ではなく「含む改行数」で進めなければならない。断片は論理行を覆うが終わらせないからである。ここを間違えると、以降の見出しレベルが1つずつずれる。

#### 折り返し位置は、そのままでは毎回探し直しになる

折り返し位置は組版の産物なので、探すには組版が要る。**素朴に実装すると、文書中のすべての長大段落を毎打鍵組版することになる。**そうなっていた（6.10）。段落ごとに位置を憶え、本文が変わっていない段落は訊き直さない。

さらに、**編集より前の折り返し位置は動かない。**行はその前にあるテキストだけで決まるからである。前回の結果のうち編集位置より前を残し、そこから先だけ組版し直す。再開位置は前回と今回のテキストの共通接頭辞から求める。

ただし`WRAP_REUSE_MARGIN`（2行）だけ戻ってから再開する。**「前は動かない」は行の前を見る限り正しいが、日本語の行分割は行の後ろも見る。**閉じ括弧や句点は行頭に置けないので、編集の直前にある折り返しは動きうる。40字ほど余分に組版するだけでこの問いが消える。

### 3.10 大きさの上限

#### 文書の大きさのハードリミット

**この製品が対象とする原稿の性質を大きく超えたファイルは、開けない方がよい。**うっかり開いて壊れ方の分からない状態になるくらいなら断る、という判断で`MAX_DOCUMENT_CHARACTERS`を置いた。上限を超える入力は拒否し、ステータスバーに理由を出す。黙って捨てると打鍵が落ちたのと区別が付かないので、必ず言う。

数字の根拠は、**文書の大きさに比例する唯一のコスト**である。1打鍵のコストは編集している段落の長さで決まるが（6.10）、`preview`と`stats`だけは毎回文書全体を走査する。

当初は7万字で3.6ms、1字あたり0.052µsだったので、50万字では26msが常に先に乗る計算になり、上限を50万字とした。**その後6.12でこの走査を行単位の差分にしたので、1字あたり0.006µsになった。**100万字でも6msである。

| 文書 | 差分化前 | 差分化後 |
| ---: | ---: | ---: |
| 70,000字 | 3.6ms | 0.4ms |
| 500,000字 | 26ms | 3ms |
| 1,000,000字 | 52ms | **6ms** |

これを受けて上限を**100万字**へ上げた。**動いたのは数字であって、上限が要るという判断ではない。**差分化で得たのは余地であって、何でも開いてよいという許可ではない。

エンジン側の防護（`MAX_LAYOUT_FLOW`）とは目的が違う。あちらは**どんな文書が来ても未知の挙動に触れない**ようにするもので、こちらは**そもそも受け取らない範囲を宣言する**ものである。前者があるので後者を超えても壊れはしないが、壊れないことと、対象としていないものを半端に開くことは別である。

なおファイル読み込みは未実装なので、現状この上限が効くのは入力と貼り付けだけである。読み込みを実装するときに同じ`fits_document_limit`を通す。

#### 一段落の長さに上限を設けるか（決定済み）

上限を設ける案が出ている。判断に要る数字を先に置く。

**262144pxは32,000字ではない。**1行の送りが約30.8px（本文22px）なので、上限は約8,500行にあたる。1行に何字入るかはペインの大きさで決まるため、字数に直すと**17万字〜70万字**の幅がある（全画面の縦書きペインで1行83字なら約70万字、小さいペインで1行20字なら約17万字）。**上限が使用中のペインの大きさで動くので、これをそのまま文書の規則にはできない。**

したがって32,000字は技術的な限界ではなく、**編集の快適さから選ぶ数字**になる。6.9と6.10の実測から、段落の先頭で1文字打つコストはSplitで1字あたり約4.2µs である。

| 一段落の長さ | 段落先頭での1打鍵 |
| ---: | ---: |
| 4,000字 | 約17ms（1フレーム） |
| 8,000字 | 約34ms |
| 32,000字 | 約114ms（実測） |

なお段落の**末尾**で打つ限り、32,000字でも10.3msである（6.10）。重いのは先頭付近だけで、そこも「打てない」ではなく「引っかかる」水準である。

**強制改行には反対する。**書き手が打っていない改行を、打っている最中に文書へ入れることになる。エディタが原稿を書き換える動作であり、上限に達したことを伝える手段としては強すぎる。上限を設けるなら、正規化が期待される場所——ファイル読み込みや貼り付け——で行うのが筋である。入力の拒否も、書き手が直せないという指摘のとおりで採れない。

現実的な組み合わせは次のとおり。**打っている最中は何もしない**（段落は切られているので破綻はしない）。**上限を超えた段落があることをステータスバーで知らせる**。**ファイル読み込みを実装するときに、読み込み側で分割するかを決める**。この方針で合意し、実装した。

**知らせる閾値は32,000字とした**（`PARAGRAPH_WARNING_CHARACTERS`）。一段落が32,000字というのは編集作業として起こりえない長さであり、そこまで行く前に伝えるべきだからである。強制はしない。`DocumentStats`が最長論理行の字数を持ち、超えていればステータスバーへ書く。

**この32,000字と`MAX_LAYOUT_FLOW`は別物であり、揃えてはならない。**前者は文書についての規則で、字数で言えるし、ウィンドウを変えても動かない。後者はレイアウトについての防護で、字数では言えず、ペインの大きさで動く。切ることは常に速さの側なので、防護の側を下げても得るものは無い。

#### DirectWriteの限界には、意図的に届かないようにした

**最大値に達したときに何が起きるか分からない状態を作るくらいなら、自分で決めた手前で止めるべきである。**その判断に従い、1回のレイアウトが流れ方向に届く距離を`MAX_LAYOUT_FLOW`（200,000px）で頭打ちにした。DirectWriteの262144pxには決して届かないので、そこで何が起きるかを知る必要が無くなる。

**最初は「収まる部分だけ切り、その先は1つのまま残す」形にしたが、これでは足りなかった。**切られずに残った部分は1つのブロックになり、**そのブロック自身のレイアウト箱は段落の残り全部の長さになる。**つまり上限を超えるレイアウトが、探索から測定へ移るだけだった。問題を動かしただけである。

**折り返しの探索を窓に分けて繰り返す形にした。**各窓は1つ前の窓の最後の行頭から始まる——行頭から先の組版はそこから先の文字だけで決まる（4.10）という、ブロックを切ってよい理由とまったく同じ理由で正当である。窓の終わり際の位置は、後ろの文字が来ると動きうるので数個落として次の窓で取り直す（`WRAP_REUSE_MARGIN`と同じ理屈）。

これで**段落がどれだけ長くても最後まで切れる**ので、**編集中に作られるレイアウトはすべて小さい。**未知の挙動に触れる経路が無くなった。

なお`MAX_LAYOUT_FLOW`は字数ではなく、字数に直せない。1行に何字入るかはペインの大きさで決まるからである。**これはレイアウトについての防護であって、文書についての規則ではない。**文書についての規則（32,000字）は別に置いてある。


### 3.11 段落先頭の字下げは実装しない

7.1に「段落ごとの量なので、行ごとの属性の形がそのまま使えるとは限らない」と挙げていた項目である。**作って動かしたうえで、機能としては入れないことにした。**

**字下げは書き手が入れるものである。**プレビューがしてきたのはマークアップを取り除くことだけで、原文に無いものを足したことは一度もない。字下げは足す側にあたる。書き手が打った空白を見せるのがEditorの仕事で、打っていない空白を足すのはワープロの仕事である、という線引きにした。

作ってみて分かったことは残す。

**構造の問いには答えが出た。**段落ごとの量も、行ごとの属性の枠に収まる。ただし収まり方が見出しとは違う。

| | 何で決まるか |
| --- | --- |
| 見出しレベル | **その行だけ**。マーカーが行の中にある |
| 段落の先頭か | **前の行が空行か見出しか**。その行だけでは決まらない |

**行だけでは決まらない属性も、文書全体で一度決めてしまえば、そのあとは行ごとの事実として同じ枠に乗る。**ブロックが自分の外に依存しないという性質は、*属性をどう決めるか*ではなく*ブロックがどう読むか*で保たれている。7.1に残る「見出し以外の行属性」（引用・リスト・コード）にも同じことが言えるはずである。属性を`&[u8]`から`&[LineStyle]`へ広げた形はそのまま残してある。

**組み合わせで破れる場所も見つかった。**長大段落を切った断片は、それぞれ「改行を持たない1行目」に見える。3.9（論理行の内側での分割）と字下げは単体では正しいのに、重ねると**切り口ごとに字下げが入った。**エンジンが「このブロックは論理行の途中から始まるか」を持てば直るが、それはキャッシュの鍵3つ（レイアウト・計測・タイル署名）すべてに入れる必要がある——同じ本文でも字下げの有無で画素が違うからである。

### 3.12 横書きのライブプレビュー（3モード目）

横書きペインが、Markdown原文と整形後表示のどちらでも組めるようにした。ツールバーの「横書き: 原文 / 整形後」で切り替える。**3つ目の*モード*を足すのではなく、横書きペインが何を組むかの切り替え**にした——モードにするとSplitとの組み合わせが増えるが、こちらなら増えない。

**中身は`refresh_horizontal`の数行である。**

```rust
let shown = if window.get_horizontal_preview() {
    PaneText::Preview(horizontal_preview_slot.get(source, active_line_start))
} else {
    PaneText::Source(source)
};
```

これが3モード目の全部で、下流は`PaneText`に対して書かれていて、どちらを渡されたかを訊かない。3.7に書いた見通し——**「engineへ渡す文字列と方向、そして位置の対応表が恒等写像かどうかだけ」**——が、そのまま型になった。

| | ペインが組む本文 | 位置の対応 |
| --- | --- | --- |
| `PaneText::Source` | Markdown原文 | 恒等写像 |
| `PaneText::Preview` | 整形後テキスト | `PreviewDocument` |

「1文字」が何かも、この対応の一部として扱う。**書記素クラスタの区切りは、表示している本文の側で決まる**——原文のMarkdown記号ではなく——ので、キャレットの左右移動も`PaneText`を通る。

#### プレビューはペインごとに要る

プレビューは**キャレットのある行だけMarkdown原文を見せる**。キャレットはペインごとに別（3.7）なので、**見せる行も別になる。**したがって`PreviewDocument`も別に持つ。共有すると、互いに相手の行を開いてしまう。

代償は1打鍵あたり`preview`ぶんの重複で、6.12で0.41msまで下げてあるので許容した。原文モードではプレビューを作らないので0.00msのままである。

## 4. DirectWriteの挙動で判明した注意点

ブロック分割の実装中に、単一レイアウトでは表面化しない挙動を確認した。いずれも分割したことで初めて誤差として蓄積し、テストで検出された。4.5と4.6は、タイルをブロック単位へ移したあとで性能ログから見つかったものである。

### 4.1 末尾の改行が空の列を1つ増やす

末尾が改行で終わるテキストに対し、DirectWriteは空の最終行を1つ追加で返す。文書全体で見ると、その位置は次のブロックの先頭列にあたる。各ブロックがこの列を自分のものとして数えると、ブロック境界ごとに列が1つ増える。

40段落・4ブロックの検証文書では、分割後の幅の合計が単一レイアウトより105.7px大きくなった。列ピッチ約35pxの3列分であり、境界の数と一致する。

対策として、最終ブロック以外は末尾の空行を捨てる。文書末尾の空列はキャレットが置かれる位置なので、最終ブロックでは保持する。

### 4.2 列の幅は行ごとの送り量であり、単一のピッチではない

正解に至るまでに2度外した。順に書く。どちらも「ブロック全体で1つのピッチを求める」という前提が誤りだった。

`HitTestTextPosition`が返す`width`は、その位置にある文字のインク幅であり、列の送り量ではない。両者には全角セルで0.6px程度の差がある。ブロック幅を「左端の列の左」から「右端の列の右」までで測ると、ブロックごとにこの差だけ不足する。40段落・4ブロックの検証文書で、分割後の合計が1.87px小さくなった。境界3か所で割ると0.62px/境界であり、差の正体と一致する。

**1度目の誤り。**隣接する列の`left`の差の**最大値**をピッチとし、ブロック幅を「列数×ピッチ」とした。最大値は標本が増えるほど大きく出る統計量なので、40列のブロックは、同じ文章が160列の文書の中にあるときより小さいピッチを報告する。ブロックは細くなり、不足は境界の数に比例する。ブロックが3つのうちは0.6pxで許容内に収まり、分割が細かくなって5つになった時点で1.25pxとして現れた。

**2度目の誤り。**ピッチを「両端の距離 ÷ 間隔の数」の平均に替えた。標本数の偏りは消えたが、なお0.396px/境界が残り、しかも**単一レイアウト側の総幅まで50px動いた**。これは「単一のピッチが存在する」という前提そのものが誤っている証拠である。回転した半角英数字ランを含む列は、漢字だけの列より実際に広い。

**正解。**`DWRITE_LINE_METRICS.height`は、DirectWriteがその行を配置するのに使った送り量であり、縦書きではその列の幅そのものである。各列の幅にこれを入れ、ブロック幅はその合計とする。推定も隣接列も要らない。文書全体の列は各ブロックの列そのものなので、分割後の合計は原理的に単一レイアウトと一致する。

この前提は`a_column_advances_by_the_height_of_its_own_line`で直接検証している。隣り合う列の`left`の差が、次の行の`height`と一致することを確かめるものである。合計から推し量るのではなく前提を名指しで見るので、崩れたときにどこが崩れたかが分かる。

なお、キャレットを隣列へ移すときの列中心と、選択範囲の可視列判定は、インクではなくセルを基準にすべきものである。この置き換えは同時に正しさの修正でもある。

### 4.3 文書幅が変わったときのスクロール

縦書きでは列が増えると文書は右端側へ広がる。文書の座標系は左端が0なので、幅が増えると編集位置より前のテキストの座標が増える。ビューを動かさないと、書き終えた部分が右へ滑って見える。

正しくは、ビューを同じ量だけ右へ動かして文書の先頭（右端）を画面上で固定する。動くのは編集位置より後ろ（左側）だけになり、日本語の縦書きが伸びる方向と一致する。

補正関数は当初から存在したが、「キャレットが無いとき」という条件が付いており、編集中は一度も実行されていなかった。

### 4.4 ブロックの大きさを文字数で数えると幅に上限が無くなる

Enterが足す空行は、文字としては1つだが列としては1本まるごとである。文字数を予算にすると、同じ場所でEnterを押し続けたブロックは予算に引っかからないまま横に伸び続ける。ブロックは無効化の単位なので、太ったブロックはスライス数が増え、本文が変わるたびにその全部が描き直される。**押した回数だけ重くなる。**

実測では、`blocks`が75のまま文書幅だけが1148px増え、再描画枚数が2枚から7枚へ、タイル描画が3.2msから8.5msへ単調に増えた。

対策は3.4に書いたとおり、予算を「行が占める空間」で数えることである。空行が1行ぶんの代金を払うようになり、ブロックの大きさに上限がつく。

### 4.5 ブロック境界は挿入で動き、以降のブロックを巻き添えにする

境界を「文書先頭からの累積が予算を超えた最初の行末」で決めると、その累積は挿入のたびに変わる。ある行末の累積が予算のすぐ手前にあった場合、切れ目が1行手前へ移り、**以降のブロックの本文がすべて別物になる**。計測キャッシュもタイルキャッシュもそこから先で全滅する。

`技術検証.md`を4回つないだ文書の154か所へ空行を1つ挿入して数えたところ、本文が変わるブロック数は最悪13、1〜2で収まったのは154回中84回だった。予算を列の空間へ変えた直後は、空行1つが予算を20セル進めるため確率がさらに上がり、実機のログでもEnterのきっかり半分が16〜20ブロックの再計測を引き起こしていた（10.6ms）。

対策は、境界を行の内容そのもので決めることである（3.4）。同じ文書での再測定では、154回中153回が1ブロック、残り1回が2ブロックになった。

**この方式の限界。**同じ行が並ぶ区間には内容の違いが無いため、境界候補が1つも現れず、最大サイズだけが頼りになる。Enterを押し続けてできる空行の連続がこれにあたり、その区間だけは連鎖する。ただし空行が途切れた時点で内容による境界が復帰するので、連鎖は区間の長さで頭打ちになる。実機のログでも、再計測10ブロックが179回中19回残っている。

### 4.6 折り返し位置の空白は前の列に属する

折り返しの起きた位置にある空白は、DirectWriteでは「前の行の末尾の空白」として扱われ、次の列の先頭へは送られない（`DWRITE_LINE_METRICS.trailingWhitespaceLength`）。したがって、改行後の行頭に置いた空白は表示されるが、折り返した列の先頭には空白が現れない。原文からは失われていない。

段落先頭は字下げし、折り返した行は字下げしないという日本語組版の約物処理と一致するため、現状のままとする。

### 4.7 列間はPROPORTIONALでなければならない

`SetLineSpacing`には`UNIFORM`と`PROPORTIONAL`がある。**`UNIFORM`は全行の送りを1つの数値で置き換える。**これは4.2で「単一のピッチを推定してはならない」と結論した当のものであり、ブロックの大きさが自分の行の送りの合計であるという前提そのものを壊す。行の高さが全部同じになれば計算は合ってしまうので、**壊れたことがテストに出ない。**出るのは見出しの行が本文と同じ高さで描かれるときである。

`PROPORTIONAL`（`IDWriteTextFormat3`、Windows 10 1607以降）は各行が計算した高さに倍率を掛ける。行ごとの差は比のまま残り、ブロックの合計もそのまま倍率ぶん伸びる。1.5倍を指定したブロックの`exact_flow_size`は実測で1.5倍になった。

なお`IDWriteTextFormat3`へのキャストは倍率が1.0のときは行わない。既定の経路はDirectWrite 3を要求しない。

### 4.8 ブロックの流れ方向の上限は、そのブロックの見出しサイズから決めなければならない

`block_flow_bound`はDirectWriteへ渡すレイアウト箱の大きさであり、**下回ると行が切り落とされる。**従来は1行あたり`font_size × 2.2`で見積もっていた。この2.2は本文の行高さに対する余裕であって、**見出しが本文の2.2倍を超えた瞬間に足りなくなる。**見出しは2.4倍程度まで普通に使う値なので、実用域のすぐ外側にあった。

ブロック自身の見出しレベルから見積もるようにした。40行すべてが2.4倍の見出しで列間1.4倍という条件で、全ブロックの実測値が上限の内側に収まることを確認している。

### 4.9 2行のサンプルでは行ごとの送りの差が出ない

4.2で「回転した半角英数字ランを含む行は実際に広い」と書いた差を、2行のサンプル（`日本語ABC123を含む行です` と `あいうえおかきくけこ`）で測ると**0.1px以内で同じ**だった。4.2の0.4px/境界は文書全体の平均から出た値であり、**任意の2行がその差を示すとは限らない。**

行ごとの送りが違うことを検証したい場合は、見出し行と本文行のように**差が設計上保証されるペア**を使う必要がある。4.2の主張（単一のピッチは存在しない）は変わらない——存在しないことと、任意の2行で差が観測できることは別である。

### 4.10 折り返し位置から組版し直すと、同じ行が出る

**この検証で最も確かめたかったこと。**折り返し位置で切ったブロックは、その位置から独立に組版される。それが正当なのは、その位置から先の行が、前にあるものに依存しない場合だけである。

自明ではなかった。日本語の行分割には禁則処理があり、行頭・行末に置けない文字が決まっている。**破り方によっては、前に何があるかを見て判断が変わってもおかしくない。**閉じ括弧・句読点・分割できない半角ランを含む段落で、縦横それぞれ「分割したブロックの合計＝単一レイアウト」が成立することを確認した。字送り・列間を既定から外した版でも成立する。

DirectWriteの行分割は貪欲であり、**ある折り返し位置から先の組版は、そこから先の文字だけで決まる。**この性質がこの設計全体の前提になった。

### 4.11 行頭の`leadingSpacing`は字形を動かすが、キャレットの前縁は動かさない

字下げは入れないことにした（3.11）が、`SetCharacterSpacing`の挙動そのものは字送り（3.8）でも使っているので残す。

字下げが効いているかを、段落の1文字目の位置——`HitTestTextPosition(0)`——で測ったところ**0pxだった。**行頭の空きが捨てられていると読み、方式が成立しないと判断しかけた。

**測るものを間違えていた。**`leadingSpacing`は文字の**手前**の空きなので、その文字の**前縁は行頭のまま**で、字形だけが送られる。1文字目の**後ろ**（`HitTestTextPosition(1)`）で測れば、指定どおり1字ぶん動いている。

見た目の字下げは成立している。**残るのは、字下げされた段落の先頭にキャレットを置いたとき、キャレットが空きの手前——行頭——に立つことである。**文字のすぐ手前ではない。

### 4.12 インライン オブジェクトは、字下げとキャレットの両方を動かす

要件7.3.2の行の属性（箇条書き・引用）には字下げが要るが、`IDWriteTextLayout`は段落ごとの字下げを持たない。4.11の`leadingSpacing`は字形だけを送り、キャレットの前縁を行頭に置き去りにする——**キャレットが文字の無いところに立つ。**

そこで`IDWriteInlineObject`を行頭のマーカー範囲（`- `＝2単位）に張って測った。`directwrite_probe.rs`の`MarkerBox`で、**何も描かず幅だけを答える箱**である。本文は24ptの`Yu Mincho`、箱は`width: 48.0, height: 24.0, baseline: 19.0, supportsSideways: true`。

| 測ったもの | 横書き | 縦書き |
| --- | --- | --- |
| 行頭のキャレット | 0.0 | 0.0 |
| 箱の内側を指したとき | 48.0 | 48.0 |
| 箱の次の文字 | **48.0** | **48.0** |
| その次の文字 | 72.0 | 72.0 |
| 箱の範囲の矩形の数／流れ軸の大きさ | 1／48.0 | 1／48.0 |
| `GetMetrics`が呼ばれた回数 | 2 | 2 |

- **箱は実在する箱である。**本文が箱の幅ぶん字下げされ、**キャレットもそこに来る**。4.11の問題を踏まない。
- **`width`が行の軸に沿った送りで、それは縦書きでも変わらない。**縦のコラムでも`height`（24.0）ではなく`width`（48.0）が送りになった。**`width`は「行の軸に沿った長さ」であって画面の横幅ではない**——`text_blocks`の流れ軸・行軸の言い方と同じで、ここは読み替えが要らない。
- **ヒットテストは箱を1領域として、箱の幅で返す。**行頭のクリックも、マーカーにかかる選択矩形も正しい形になる。
- **箱の中に位置は無い。**内側を指すと向こう側（48.0）へ寄る。1つのクラスタなので、`- `の`-`と空白のあいだにキャレットは立たない。**プレビューでは問題にならない**——キャレットのある行は原文のまま出す（要件7.3.1）ので、その行に箱を張らなければよい。マーカーを隠す条件とまったく同じ条件である。

副次的に、**マーカーの長さが違っても字下げは同じ**になる（`- `と`10. `で本文の開始位置が一致する）。これが「送り幅を広げる」ではなく「箱を張る」を選ぶ理由である。

`GetMetrics`は1レイアウトにつき2回呼ばれるので、**箱は答えるのが安くなければならない。**定数を返すだけにしてある。

**描画はこの箱にさせない。**Direct2Dの`DrawTextLayout`から呼ばれる`Draw`にはレンダーターゲットが渡らないので、箱がターゲットを抱えることになる。ターゲットは作り直され、箱を参照するレイアウトはまさにそこをまたいでキャッシュされている。飾り（`•`、`☐`、引用の縦棒、水平線）は**タイル描画側で、`HitTestTextRange`が返す矩形に描く**——`selection_rects`と同じ道具である。

**`#[implement]`を使うには`windows-core`への直接の依存が要る。**マクロが絶対パス`::windows_core::…`へ展開するので、`windows::core`の再エクスポート経由では届かない。バージョンは`windows`が使っているものと同じにすること（違えば`windows_core`が2つになり、片方のCOMトレイトがもう片方を満たさない）。ロックにもキャッシュにも元からあるので`--offline`は成立する。

### 4.13 その他

- 選択範囲が空行をまたぐと、`HitTestTextRange`は面積ゼロの矩形を返す。描画対象から除外する。
- テキストのアンチエイリアスはグレースケールとする。ClearTypeはサブピクセル単位で計算量が増えるうえ、特定のRGB配列を前提とした結果を画像へ焼き込む。この画像はSlintへ渡され拡大縮小されるため、ClearTypeの前提は成立しない。
- 字送りは前後へ半分ずつ配る（`SetCharacterSpacing`の`leadingSpacing`と`trailingSpacing`）。片側へ寄せると行全体が一方向へずれる。見出しの範囲には見出しのサイズで計算した値を改めて設定する。字送りは自分のサイズに対する比なので、サイズが違えば絶対量も違う。

### 4.14 縦書きでは、箱が「横に寝られる」と言うかどうかで送りの意味が変わる

**`supportsSideways`を`TRUE`にしたインライン オブジェクトは、縦書きで正立する字の上に載ると、
`width`ではなく`height`ぶん送る。**2026-08-26に`probe_box_advance`で測って確定した。

| 箱が覆うものの先頭 | `supportsSideways` | 横書きの送り | 縦書きの送り |
| --- | --- | ---: | ---: |
| `|`・空白・`a`（寝る字） | TRUE | `width` | `width` |
| `央`・`あ`（正立する字） | TRUE | `width` | **`height`** |
| どれでも | FALSE | `width` | `width` |

`height`が効いていることは値で分かる——`height`を0にすれば送りは0、44にすれば44になる。
**emへの丸めではない**（幅11も65も100も、同じ22になった）。

理屈は素直で、**縦書きの行はまず「正立する字の連なり」と「寝かせる字の連なり」に割れる**。
寝かせる連なりの中では箱も寝かされるので、箱の`width`が行に沿う。正立する連なりの中では
箱も正立するので、**行に沿うのは`height`のほう**である。`supportsSideways: FALSE`は
「寝た姿を持っていない」という申告で、そう言えばどちらの連なりでも正立扱いになり、
**`width`の意味が1つになる**。

**この箱は何も描かないので、`FALSE`は嘘ではない。**寝た姿を持っていないのは事実であり、
その事実を言うと2つの書字方向が一致する。

**これが7.7の「規則は突き止めていない」の正体で、当時の観測はすべてこれ1つで説明できる。**

- 表の箱のうち、**セルの尾を切ったときの箱だけが正立する字から始まる**（切った字の上に載るので）。
  だから**縮めた表だけ**が縦書きでずれた。11や380.5が通ったのは、その箱がバーか空白から始まっていたから。
- 17.5と28.5が22になったのは、`across`＝フォントサイズが22だったから。丸めではなく`height`そのもの。
- **「行の途中に置いた幅0の箱が縦書きで1行ぶんの場所を取る」も同じ現象**（`Ornament::Cut`を
  廃止した理由）。幅0でも`height`は22なので22送っていた。
- **行の頭の箱（箇条書き・引用）が無事だったのも同じ規則**。マーカーは`- `や`10. `で、
  どれも寝かせる字から始まる。

**測りかたの教訓：現象を、それが起きた仕組み（表）の中で測ると規則は出ない。**
7.7では「箱の幅→送り」の対応表を表から読み出したので、覆っているものが箱ごとに違うことが
変数から落ちていた。**箱1つ・中身1つ・答え1つに切り分けたら1回で出た。**

## 5. 自動テスト

次の124件を実装し、すべて成功している。DirectWriteに依存しない幾何計算とインデックスは`text_blocks.rs`と`document.rs`へ分離してあり、Windows以外でも検証できる。

### 5.1 ブロック分割と配置（`text_blocks.rs` 36件）

論理行境界でのみ分割すること、文書を隙間なく覆うこと、長すぎる論理行を分割しないこと、空文書で1ブロックになること、**空行の連続が1つのブロックを無制限に太らせないこと**、**挿入された行が、それが落ちたブロックとその次までしか書き換えないこと**、先頭ブロックが流れの始点に来ること、グローバルな流れ座標からブロックを特定できること、表示領域およびUTF-16範囲と交差するブロックだけを選ぶこと、ブロック境界と空ブロックをまたいで行を移動できること、位置から行を特定できること、行末尾が改行の手前になること、グローバル座標とブロック座標を相互変換できること、表示領域が映す行だけを報告すること、**ブロックが整数画素に置かれ、その大きさが隣のブロックに依存しないこと**、長文で可視タイルだけを保持すること、両側に1枚ずつ先読みしても文書外へ出ないこと、可視範囲を文書内へ丸めること、ブロックの流れ方向の上限見積もりが実際の必要量を上回ること、**タイルが各ブロックを隙間なく1回ずつ覆うこと**、**1つのブロックを広げても他のブロックのタイルの識別子と大きさが変わらないこと**。

組版の指定を自由にしたことで6件を追加した。**送り量を広げると1行に入る文字数が減ること**、**見出しが「1行に入る字数」と「1行の送り」の両方で課金されること**、**流れ方向の上限見積もりが見出しサイズと列間の両方に追随すること**（下回るとDirectWriteが行を切り落とす）、**DirectWriteへ渡す範囲がブロックローカルで、改行の手前で止まること**、指定の無いレベルが本文として扱われること。

論理行の内側での分割で3件を追加した。代役の`WrapPoints`（N文字ごとに折り返す）を使い、**長大な論理行が1つの無制限なブロックでなくなること**と、切り口がすべて代役の報告した位置であること、**切った後も被覆とUTF-16の通し番号が狂わないこと**（この経路は通し番号を手で数えているので、間違えても他のどこにも出ず、キャレット位置だけが全部ずれる）、**段落の後ろでの編集が前の断片を残すこと**。

このうち向きに依存する6件は両方向で実行している。加えて横書き向き（`Ascending`）専用に3件——先頭ブロックが上端に来ること、流れ座標からのブロック特定、表示領域との交差——と、**両方向が互いに正確な鏡像になること**（配置・タイルの切り出し・探索の3つを、期待座標をもう一組書かずに突き合わせる）を追加した。1つのブロックが太ったときに動くのが編集より前か後ろかは、向きによって入れ替わる唯一の性質である。

### 5.2 文書モデル（`document.rs` 12件）

論理行数の集計、基本的なMarkdown記号を除いた本文抽出、インデント後の記号を文字として扱うこと、編集中の行だけMarkdown原文を表示すること、絵文字の書記素クラスタ単位の文字数集計、Markdown記号をまたいだプレビュー位置の対応、書記素クラスタ単位のキャレット移動、UTF-16位置からプレビューバイト位置への変換、**二分探索の結果が置き換え前の線形走査と全位置で一致すること**、全位置で原文の文字境界へ着地すること、最長論理行の字数（7.4の警告が使う）、**行単位に数えた統計が文書全体を数え直した値と一致すること**、**編集を重ねたあとのプレビューの引き当てが全位置で線形走査と一致すること**（6.12）、**見出しレベルの判定と、プレビューがマーカーを除去する行とが1行ずつ一致すること**（食い違うと、縦書き面が表示している文字と違うサイズで組む）。

### 5.3 DirectWrite（`directwrite_render.rs` 35件 ＋ `directwrite_probe.rs` 1件）

このうち6件は横書きモードのengineをDirectWriteで実際に動かすものである。**分割したブロックの高さの合計が単一レイアウトの高さと一致すること（段落数37/40/41/53の4通り）**、行送りが自分の行の`height`ぶんで下向きに積まれること、ブロックが上端から隙間なく並ぶこと、タイル画像がペイン幅×タイルの流れ方向長で字形を含むこと、ヒットテストとキャレット座標が往復すること、下キーが次の行へ・上キーが前の行へ移動すること。

論理行の内側での分割で7件を追加した。**折り返し位置で切ったブロックの合計が単一レイアウトと一致すること（縦横、および組版を既定から外した版）**——これがこの方式の成否そのものである。**接頭辞を再利用したengineでも一致すること**（編集位置を19/20から3/20まで前へずらし、各回が前回の残した折り返しから出発する）。段落の外の編集がその段落を組版し直さないこと、中の編集は組版し直すこと、末尾での編集が末尾しか組版し直さないこと、**長大段落が複数ある文書で正しい段落の折り返しを引き継ぐこと**（長大段落1つでは、205msの床があった版でも通ってしまった）。

組版の指定を自由にしたことで5件を追加した。**3つの量をすべて既定から外し、見出しを混ぜた文書で、分割したブロックの合計が単一レイアウトと一致すること（縦横×段落数4通り）**、見出しの行が自分のサイズぶん送られ、**隣の本文行は動かないこと**、字送りを広げると同じ論理行がより多くの視覚行へ折り返すこと、**列間の倍率が行ごとの差を潰さず比のまま残し、ブロックの合計が倍率どおりに伸びること**、見出しばかりのブロックが流れ方向の上限を超えないこと、**1つの窓に収まらない長さの段落でも最後まで切られ、隙間なく覆われること**（7.4）。

以下は縦書きモードの15件である。論理行群ごとのブロック計測、**列が自分の行の`height`ぶんだけ送られること（ブロック計測が拠って立つ前提の直接検証）**、**分割したブロックの幅の合計が単一レイアウトの幅と一致すること（段落数37/40/41/53の4通り）**、ブロックをまたいでヒットテストとキャレット座標が往復すること、指定した可視タイルだけを描画すること、タイルへ実際に字形が描かれること、**編集が自分のタイルだけを描き直し他へ波及しないこと**、**列が1つ増えて文書幅が変わっても、本文が変わらないブロックのタイルが編集位置の左右どちらでも保持されること**、選択矩形が表示領域内に収まること、左右キーが隣接する表示列へ移動すること、`Home`/`End`が表示列の端へ移動すること、空列をまたいでも表示高さを保つこと、**編集で再計測されるブロックが2つ以下であること**、同一内容の更新で再計測が発生しないこと、グローバル範囲とブロックの交差計算、縦書きで次の文字が下へ配置されること。

### 5.4 アプリ統合（`main.rs` 34件）

横書きペインの置き換えで7件を追加した。**全位置でソースバイトとUTF-16位置が往復すること（このペインの位置対応はこれだけである）**、書記素クラスタ単位のキャレット移動、IME変換中文字列を描画用テキストだけへ入れること、ペイン幅未報告時に既定値へ落ちること、横書きキャレットが表示領域内に留まること、**貼り付けた改行が保たれCRLFが1つに畳まれること**、**他ペインの編集で文字の内側へ取り残されたキャレットが落ちないこと**、**文書の上限を超える入力が字数で拒否されること**（7.4）。後の2件は6.7で見つかった不具合の再現である。


単一表示とSplitの表示切り替え、IME変換中文字列を描画用プレビューだけへ挿入すること、変換中でない入力で文書を複製しないこと、横書き選択範囲へのTab挿入、Tab幅の統一、IME候補ウィンドウのアンカー位置、キャレットの横方向追従、文書幅変更後の表示距離の維持、タイルの退避と可視タイルの保持、逆方向ドラッグの選択範囲正規化、`Shift`移動による選択拡張と通常移動による解除、選択範囲の置換、見出し行頭でのTabインデント、`技術検証.md`が切り詰められないこと、折り返し列をまたぐ左移動、回転した半角英数字ランをまたぐ左移動での表示高さ維持、**3万文字で常駐タイルが3枚以下であること**、**長文のスクロールで再計測が発生しないこと**、**文書全体の選択で可視ブロックだけを計測すること**、**1列増えたとき文書先頭が画面上の同じ位置に留まること**、ペイン未報告時に既定の列高さへ落ちること、**高さ3倍のペインでタイルが細くなり1枚あたりの画素数がほぼ一定に保たれること**、可視タイルが上限を超えても要求中のタイルを退避しないこと。

### 5.5 計測用文書（`testdata/09_段落長の計測.md`）

生成した文書が毎回同じであること、**振り分けた各段落が改行を1つも含まない1論理行であること**（紛れ込んだ改行が1つあれば、その節はもう長大な段落ではなくなり、計測が計測でなくなる）、対照区間の段落がブロックを共有できる短さに留まること。

### 5.6 旧縦列モデル（`vertical_layout.rs` 3件）

DirectWrite導入前の仮モデルに対するテスト。現在は本体で使用していない。

## 6. 画面上で確認した結果

### 6.1 表示と入力

以下のうち横書きペインに関する項目は、そのペインがまだSlintの`TextEdit`だった時点での確認である。置き換え後の確認はこの節の末尾にまとめた（3.7、6.6、6.7）。

- Slint `TextEdit`へ日本語文字列を入力できた。
- 入力内容が右側の縦列モデルへ即時反映された。
- 入力後に本文文字数、ソース文字数、列数が更新された。
- 100%から110%への倍率変更が、編集面とステータスバーへ反映された。
- DirectWrite検証結果とDirectWrite/Direct2Dの実描画をアプリ画面へ表示できた。
- 全角英数字`ＡＢＣ１２３`が一字ずつ正立して表示された。
- 半角英数字`ABC123`と`Version2.0`が横組みの連続ランとして90度回転して表示された。
- 句読点、括弧、かぎ括弧がDirectWriteの縦書き字形・位置で表示された。
- 100%から110%への変更時に、実描画のフォントサイズが22.0ptから24.2ptへ変化した。
- 縦書き本文をクリックし、DirectWriteのヒットテスト結果へ青い横キャレットを表示できた。
- キャレットの横範囲を文字セルの`left`と`width`から描画し、対象列の右隣へ一列ずれる問題を解消した。
- 縦書き面へ半角文字を入力し、左側のMarkdown原文と再描画結果へ反映できた。
- 矢印キーでキャレットを移動し、Backspaceで対応する原文文字を削除できた。
- 見出しの`# `を表示から除いた状態でも、本文位置へ入力してMarkdown記号を保持できた。
- 縦書きプレビューで見出し行をクリックすると`#`が表示され、別の行へ移ると見出し表示へ戻った。強調行では編集中だけ`**`が表示された。
- 日本語IMEの変換中文字列を、原文の行数・文字数を変えずに縦書きキャレット位置へ下線付きで表示できた。
- IME候補ウィンドウ用アンカーを縦書きキャレットから右へ30px、下へ32px移し、変換中文字セルと候補ウィンドウが重ならないことをMicrosoft IMEで確認した。
- IME確定時は確定文字だけが原文へ入り、確定に使ったEnterによる余分な改行は入らなかった。
- IME確定後の通常Enterでは、改行が1つだけ原文へ入った。
- 縦書き面のマウスポインターを、横向きのテキスト選択形状へ変更した。
- 上下キーは同じ列の前後文字へ、左右キーは現在位置に最も近い高さの隣接列へ移動した。
- 半角英数字ランをまたぐ左右移動では、隣列のX座標と現在のY座標の交点をDirectWriteで再ヒットテストし、ラン内部の表示位置を維持できた。
- 縦書き面を同じ列内および複数列にまたがってドラッグし、選択範囲を青い文字セル背景で表示して、ステータスバーの選択文字数を更新できた。
- `Shift+↓`で同じ列の次の文字を、`Shift+←`で隣の縦列まで選択範囲を拡張し、通常のカーソルキーで選択を解除できた。
- 縦書きの`Home/End`で現在列の上端・下端へ移動し、`Shift+Home`で列端まで選択、`Ctrl+Home/End`で文書先頭・末尾へ移動できた。
- 32段落・1078文字・描画幅2460pxの文書で、マウスホイールの下方向操作により表示が右側の第1段落付近から左側の第13段落付近へ進み、逆方向操作で戻ることを確認した。
- 横方向へ手動スクロールした後も、`Ctrl+Home/End`で移動先のキャレットを左右24pxの余白内へ追従表示できた。
- 明示改行ごとの折り返し列数を合算することで、長い複数段落でも第1段落から第32段落まで描画領域へ収まった。
- 描画幅2460pxの文書を1024px幅の画像タイルへ分割し、Slintでは可視領域と交差するタイルだけを表示した。分割後もホイール移動とクリック位置が一致することを確認した。
- `技術検証.md`全文を貼り付け、4096pxを超える幅で描画した。縦書き編集面の`Ctrl+Home/End`で文書先頭と末尾の双方を表示できた。

#### 自前描画へ移した後の横書きペイン

6.7の修正を入れたうえで、次を確認した。

- クリックでキャレットを置き、直接入力と日本語IME変換ができた。
- 左右キーで1文字、上下キーで1行のキャレット移動、`Home`/`End`、Backspace/Deleteが動作した。
- ドラッグ選択の矩形が表示され、ホイールで縦方向にスクロールできた。
- 片方のペインの編集がもう片方へ即時に反映され、かつ2つのキャレットは互いに独立していた。
- 改行を含む長文を貼り付けて、改行が保たれた。

### 6.2 リサイズ

- ウィンドウを可変サイズにし、縦書きがペインの高さいっぱいに伸びることを確認した。
- ペイン高さ1190pxでタイル幅447px、1910pxで278px、724pxで735pxとなり、1枚あたりの画素数がほぼ一定に保たれた。
- リサイズ確定後の再レイアウトは75ブロックの文書で125〜135msだった。ドラッグ中は発生しない。
- Split表示で左右どちらへ入力しても、ペインの幅が変化しないことを確認した。
- 文書の途中で改行したとき、書き終えた側（右）が動かず、後続（左）が流れることを確認した。

### 6.3 性能

最適化済みReleaseビルドで、`refresh_preview`の内訳を1操作1行で`perf_log.txt`へ記録して計測した。

単一レイアウト方式では、3万文字・描画幅86386pxの文書で次の値だった。

| 操作 | 所要時間 |
| --- | --- |
| 初回描画 | 90.1ms |
| 1文字追加後の再描画 | 88.0ms |
| 横スクロール | 35ms |
| 範囲選択 | 75ms |

ブロック分割方式では、75ブロック・全画面（ペイン高さ1910px、可視タイル10枚）の、より大きい文書で次の値になった。

| 操作 | 合計 | 再描画タイル | 内訳の中心 |
| --- | --- | --- | --- |
| 文字入力 | 7.2ms | 10枚中2枚 | tiles 2.9 / stats 1.7 / layout 1.3 / preview 1.3 |
| カーソル移動 | 0.03ms | 0枚 | 展開行を切り替えないため再描画なし |
| ドラッグ範囲選択・横スクロール | 0.03ms | 0枚 | 可視ブロックのヒットテストのみ |
| Enter（列が増える） | 17.8ms | 10枚中10枚 | tiles 12.6 |

- 1文字入力で再計測されるブロックは1つである。
- 横書きペインへの反映（Split時のみ発生）は0.06msであり、Splitの体感差の主因ではない。Splitで重く感じる分は、Slintが横書きペインを描画するコストである。
- 大きな文書を開いた直後の初回は、全ブロックの計測に117〜135msを要する。以後はキャッシュにより発生しない。
- 計測を進める過程で、ボトルネックの予想は3回続けて外れた（`preview`/`stats`だと思ったら`tiles`、Slintの`TextInput`だと思ったら0.06ms、ブロック幅の変動だと思ったら幅は不変）。内訳をログへ出してから初めて正しい箇所へ手が入った。

タイルをブロック単位へ移し、分割予算を列の空間に替え、境界を内容で決めるようにした後は、260ブロックの文書で次の値になった。Enterを179回押した記録である。

| 操作 | 合計（中央値） | 合計（最大） | 再描画タイル | 再計測ブロック |
| --- | --- | --- | --- | --- |
| Enter（列が増える） | 5.1ms | 16.6ms | 中央値1枚、最大4枚 | 4分の3が1〜3 |
| カーソル移動・ドラッグ選択・横スクロール | 0.03ms | — | 0枚 | 0 |

- 内訳の中央値は tiles 1.09 / stats 1.63 / layout 1.19 / preview 1.25 である。**タイル描画はもう最大の項目ではない。**残るコストは4つにほぼ均等に散っており、`stats`と`preview`は文書全体を走査しているので、次に削るならそこである。
- 最大16.6msは、4.5に書いた空行の連続で境界候補が出ない区間である。179回中19回で再計測が10ブロックになっている。
- タイルの再描画枚数が押した回数とともに増える現象（4.4）は消えた。

### 6.4 フレーム側の実測

`refresh_preview`の計測は、タイル画像をSlintへ渡した時点で終わる。その先（画像のGPUへの転送とシーンの描画）を見るため、`set_rendering_notifier`で`BeforeRendering`と`AfterRendering`を挟んだ。`AfterRendering`はバッファ提示の前に呼ばれるので、この区間に画面同期の待ちは含まれない。

**集計の仕方で1度失敗した。**最初は区間の合計と最大を記録したが、これは操作の合間に手を止めた時間ぶんのフレームまで数えるため、転送0KBの操作でも1フレーム98ms、最大1.5秒という値になった。作業ではなく待ちを測っていた。**その区間で最も速かったフレーム**を見るように変えて、初めて意味のある値になった。最速のフレームは待たされなかったフレームなので、実際の作業量に一番近い。

#### タイルの転送は測定にかからない

| 直前の操作が渡した画像 | 最速フレーム（中央値） | 最遅フレーム（中央値） |
| --- | --- | --- |
| 0KB | 0.26ms | 7.11ms |
| 2〜6MB | 0.26ms | 1.71ms |
| 6MB超 | 0.26ms | 2.78ms |

6MBの画像を渡した直後のフレームが、何も渡していないフレームと同じである。Slintへ渡すのは画像ハンドルだけで、転送はGPU側に吸収される。**タイルの転送量を根拠にGPUラスタライズを検討する理由は無い。**

#### 横書きペインが1フレーム90ms使っている

| 表示 | ブロック数 | 最速フレーム（中央値） | 標本数 |
| --- | --- | --- | --- |
| 縦書きのみ | 50未満 | 0.28ms | 142 |
| 縦書きのみ | 150以上 | 0.29ms | 82 |
| Split（横書きペインあり） | 118 | 89.98ms | 37 |

縦書き単独のフレームは0.29msで、**文書が5倍になっても変わらない**。タイルは表示領域ぶんしか存在しないので、シーンの規模が文書長に依存しないためである。

一方、横書きペインが出ている状態では最速のフレームでも88.5msを下回らなかった。しかもこちらはより小さい文書での値なので、文書サイズでは説明がつかない。SlintのTextEditはMarkdown原文全体を保持しており、表示領域による間引きが効いていないと考えられる。**約10fpsであり、1打鍵5.1msという入力側の数字より2桁大きい。**

6.3で「Splitで重く感じる分は、Slintが横書きペインを描画するコストである」と推測していた項目の実測にあたる。推測は当たっていたが、桁は想像より大きかった。

### 6.5 タイルをブロックの分割単位へ移した

以前のタイルは画面座標をn等分する格子で、Enterのたびに可視タイル10枚すべてが再描画されていた（17.8ms）。右端基準にすると編集位置より前は保持されるが後ろが無効化され、左端基準ではその逆になる。単一の格子である限り、どちらか片側は必ずずれる。

タイルをブロックの分割単位に変え、識別子から座標を外した。詳細は3.4に書いたとおりである。この変更だけで、通常の位置でのEnterは再描画1枚・タイル描画1.3msになった。

ただし、この変更は同時に2つの別の問題を表に出した。どちらもタイルではなくブロックの側の問題である。

1. ブロックの大きさを文字数で数えていたため、Enterを押し続けるとブロックが際限なく太り、その全スライスが毎回描き直された（4.4）。
2. ブロック境界が文書先頭からの累積で決まっていたため、挿入のたびに以降のブロックが書き換わり、再計測が10.6msかかっていた（4.5）。

いずれもブロックの分割規則を変えて解消した。**タイルの粒度を上げたことで、それまでブロックの側に隠れていたコストが見えるようになった**というのが、この一連の変更で得た主な知見である。

### 6.6 横書きペインを自前描画へ移した後の実測

6.4で「1フレーム90msであり、本番ではこのままにできない」と結論した項目の、置き換え後の測定である。

#### Splitのフレームコストは縦書き単独と同じになった

| 表示 | 最速フレーム | 標本 |
| --- | --- | --- |
| Split（Slint `TextEdit`） | 89.98ms（最速でも88.5msを下回らず） | 37 |
| 縦書き単独 | 0.29ms | 82 |
| **Split（両ペインとも自前描画）** | **0.33ms（0.25〜0.46ms）** | 18 |
| **横書き単独** | **0.33ms** | 後日 |

横書き単独の値は長く空欄だった。**走らせていなかったからではなく、測る場所が無かったからである。**フレーム統計は縦書きの`refresh`行にしか出しておらず、縦書きペインが動かない横書き単独では、どれだけ操作しても1行も残らなかった。横書き側の行にも同じ統計を出して初めて埋まった。**3条件とも0.3ms台で並ぶ。**

**270分の1になり、縦書き単独と区別がつかない。**横書きペインが持っていたテキスト部品はIMEブリッジ用の透明な`TextInput`だけになり、Slintが描くのは画像とごく少数の矩形だけになった。6.4で立てた「同じブロック＋タイル構造に載せればこの項目は0.3ms側へ移る」という見通しは、そのとおりだった。

#### 通常の大きさの文書

サンプル文書（3ブロック、流れ方向418px）での横書きペインの1操作は、合計2.5〜5.5ms（layout 0.25〜0.56 / tiles 1.8〜5.0）である。縦書き側と同じ桁に収まっている。

#### 改行の少ない長文で1打鍵130msになる

**ここで新しい問題が出た。**改行の少ない長文を貼り付けると、1打鍵あたり次の値になった。

| 項目 | 値 |
| --- | --- |
| 文書の流れ方向 | 36,000px（約1,000行） |
| ブロック数 | 7 |
| 再計測ブロック | 1 |
| 縦書きの`layout` | 125〜135ms |
| 横書きの`layout` | 35〜39ms |
| Splitでの1打鍵合計 | 170〜220ms |

**ブロック単位の無効化そのものは正しく効いている**（`measured=1`）。壊れているのはブロックの大きさのほうで、1,000行の文書がわずか7ブロックにしかなっていない。原因は3.4に書いた分割規則そのものである。**分割は`\n`の直後でしか行わないので、改行を含まない長大な段落は、どれだけ長くても1ブロックになる。**6.3の計測で使ったサンプルは段落が短く、この性質が表に出なかった。

同じブロックの計測が縦書きで125ms、横書きで35msと3.5倍違う。縦書きの組版（縦組みの字形選択と半角ランの回転）がそれだけ重いということであり、方向によらず「1ブロックが巨大なら1打鍵が重い」という構造は同じである。

なお横書きのタイルは、ペイン幅1883pxのとき高さ282pxに切られた（面積一定の規則どおり）。1枚2.1MB、全面再描画で8枚17MBを転送しているが、6.4のとおりこれは測定にかからない。

#### 改行のある文書では従来どおり

同じセッションで、改行を含む232ブロックの文書（流れ方向40,800px）では1打鍵2.5〜6.7msだった。**engineを2つ同時に動かしても、6.3で得た水準は保たれている。**重いのは長大な段落という一点だけである。

### 6.7 画面で確認して見つかった不具合

置き換えた直後の版には、テストが1つも落ちないまま4つの不具合があった。自動テストは engine の幾何と文書モデルを守っていたが、**4つのうち3つはengineの外——2ペイン構造そのもの——にあった。**本体実装でも同じ形で現れるので、原因ごと残す。

#### 貼り付けると改行が消える（両ペイン）

IMEブリッジの`TextInput`が`single-line: true`だった。Slintの単一行`TextInput`は挿入されるテキストから改行を落とす。両ペインの入力はすべてこのブリッジを通るので、症状も両方に出た。

`single-line: true`はEnterの扱いと一体だった（`accepted`で改行を挿入し、IME確定のEnterと区別する）。複数行にすると、変換中でないEnterを`key-pressed`で受けて`accept`する形へ変える必要がある。**これでEnterの挙動が`single-line`の意味論から独立した**ので、結果として前より確かになった。

この修正で初めて別の不具合が表に出た。`normalize_typed_input`が`'\r'`を`'\n'`へ写像していたため、**Windowsのクリップボードが渡すCRLFが改行2つになる。**単一行フィールドが改行を落としていた間は発現しようがなかった。

#### 他ペインの編集でキャレットが文字の内側に取り残されて落ちる

貼り付け後に縦書き側を編集すると`'る'`の途中を指すバイト位置でpanicした。**2つのペインがそれぞれキャレットをソースのバイト位置で持ち、片方が編集すると、もう片方の保持位置が多バイト文字の内側を指しうる。**これは2ペイン構造に固有の問題で、単一ペインの間は起こりようがなかった。

さらに悪いことに、それを直すためのガードがこうなっていた。

```rust
if source.is_char_boundary(byte) { byte } else { byte_at_utf16(source, utf16_at_byte(source, byte)) }
//                                              ^^^^ この関数が、まさにその位置で切って落ちる
```

**境界でない位置を直すための関数が、その位置で切っていた。**境界処理を`floor_char_boundary`1箇所へ集約し、ペインが非表示のときもクランプするようにした（従来は早期returnで飛ばしていたので、縦書き単独で編集してからSplitへ戻すと同じ経路が残っていた）。文字数表示のような**読むだけの経路は境界へ丸め、文書を変更する経路は厳格なまま**にしてある。後者で黙って丸めると、落ちる代わりに文書が静かに壊れる。

#### 横書きペインの下半分が描かれない

ペインは可視になった時点で生成されるので、**生まれたときの高さがそのまま続くと`changed visible-height`が一度も発火しない。**表示領域の高さが既定値520pxのままになり、タイルはその範囲ぶんしか作られなかった。幅のほうは（スクロールバーの出入りなどで）変化イベントが起きていたため更新されており、片側だけ古いという分かりにくい状態になっていた。

原因の特定は画面ではなく性能ログの算術からできた。ブロック平均175px・`tiles_shown=8`から逆算した可視高さが、既定値と一致した。修正は`init`での報告と、**ウィンドウ高さを下限に使うこと**である。ペインがウィンドウより高くなることはないので下限は常に安全で、多く見積もる誤差はタイル1〜2枚、少なく見積もる誤差は白紙になる。ログには申告値と実際に使った値を並べて出すようにした。

#### 縦書きの操作に横書きが連動する

ドラッグ用のフラグが、そのTouchAreaがpointer-upを受け取ったときにしか下りなかった。**ドラッグが別の場所で終わるとフラグが立ったままになり、以後そのペイン上でマウスを動かすだけでキャレットがマウス位置へ飛ぶ。**「連動しているが同じ場所ではない」という見え方になる。手製フラグへの依存をやめ、ツールキットが管理する`TouchArea`の`pressed`と併用した。縦書き側も同じ作りだったので両方直した。

### 6.8 見出しサイズを効かせた状態での実測

Split、7万文字級の文書、倍率140%（本文30.8px）、見出し1.6倍で計測した。**組版を行ごとに変えても、1打鍵のコストは6.6の水準のままである。**

| 項目 | 値 |
| --- | --- |
| ブロック数 | 212〜215 |
| 再計測ブロック | 1〜3 |
| 縦書きの1操作 | 1.9〜5.5ms（`layout` 0.7〜1.6 / `tiles` 0〜2.8） |
| 横書きの1操作 | 4.5〜7.3ms（`layout` 0.6〜0.8 / `tiles` 2.2〜4.8） |
| フレーム | 最速0.38ms、中央値6.7ms |

**行ごとにサイズが違ってもブロック単位の無効化は効いている**（`measured=1`）。見出しは分割規則から見ると「課金の重い行」でしかないので、境界候補になりやすくなるだけで、ブロック数も1ブロックあたりの再計測コストも変わらなかった。

**見出しレベルの走査は測定に出ない。**論理行ごとに1バイトを作る1パスを1打鍵につき1回追加したが、`preview`は1.23〜1.34msで、追加前（1.26〜1.30ms）と区別がつかない。同じ経路にある`visible_markdown_text`が文字列を作り直しているのに対し、こちらは行頭を見るだけである。

`stats`が横書き側で1.65〜1.74ms、`preview`が縦書き側で1.23〜1.34ms——**7章に挙げた「文書全体を走査する2箇所で合計2.9ms」は、文書が7万文字でもそのままの値である。**入力側で残っているコストの過半がここであることは変わっていない。

#### 組版を変える操作は1回200msかかる

別のセッションで、3つの量を画面上で動かした（字送り -20〜40%、列間 70〜150%、見出し 120〜210%、文書121KB・約4万字、倍率100%）。診断ログの`relayout`行73件は次のとおりである。

| 項目 | 値 |
| --- | --- |
| 1回の押下 | **中央値202ms**（178〜234ms） |
| 縦書き | 142ms（`layout` 119 / `tiles` 22、`measured=257` ＝全ブロック） |
| 横書き | 61ms（`layout` 38 / `tiles` 22、`measured=261` ＝全ブロック） |
| 1ブロックあたりの計測 | 縦書き0.46ms、横書き0.15ms |

**設計どおりである。**組版の指定は文書全体にかかるので、`measured`はブロック数と一致し、可視タイルも全部無効になる。ズームとまったく同じ性質で、実際コストもズームと同じである。縦書きの計測が横書きの3倍というのも6.6（125ms対35ms）と一致している。

**それでも押しづらい。**ボタン1回が200msで、100%から150%へ動かすには5回押す。1回ずつなら気にならないが、連打すると1秒間なにも返らず、しかも途中の4回ぶんの計測はすべて捨てられる。リサイズに150msの待ちを入れたのと同じ理由がここにも当てはまる（3.1）。**7章に「要るかもしれない」と書いた待ちは、要った。**

**150msの待ち（`SPEC_SETTLE`）を入れた。**値の表示は押した瞬間に更新し、再レイアウトだけを遅らせる。連打は1回にまとまり、途中の結果は捨てられずに済む。倍率も同じタイマーを共有する——**倍率のコストは前からこれと同じで、組版の操作がそれを目立たせただけである。**気に入る値を探して範囲を往復する操作だからこそ表に出た。

字送りを変えるとブロック数が257→253→278と動く。`cells_per_line`が変わって分割位置が変わるためで、これも設計どおりである。**組版を変えた後は、計測もタイルも1つも残らない。**

#### 上限に当たった押下は0.3msで返る

列間を70%まで下げてからさらに押した9回が、`total=0.21〜0.37ms`で記録されている。値がクランプされて変わらなかったので`TextEngine::matches`が一致し、`update`が何もせずに戻っている。**早期returnの経路が実際に働いていることの確認**になっているが、同時に、押しても何も起きなかったことが画面上は分からないということでもある。

### 6.9 段落長と1打鍵のコスト

7章が「まずどこまでの段落長なら許容できるかを測るところから」と書いた計測である。段落長を倍々に振った文書（`testdata/09_段落長の計測.md`、7万字）を開き、各節で入力した。Split・倍率100%・本文22px。

`measured_utf16`（その打鍵で測り直した文字数）ごとの平均は次のとおり。

| 測った文字数 | 縦書き`layout` | 縦書き`tiles` | 縦書き合計 | 横書き合計 | Split合計 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 206 | 1.2 | 1.6 | 4.6 | 5.4 | 10.0 |
| 548 | 2.2 | 2.1 | 6.1 | 4.1 | 10.2 |
| 1,027 | 3.7 | 3.0 | 8.5 | 4.7 | **13.2** |
| 2,050 | 6.9 | 6.6 | 15.4 | 6.2 | 21.6 |
| 4,044 | 13.2 | 6.8 | 21.9 | 9.2 | 31.1 |
| 8,076 | 26.0 | 9.7 | 37.5 | 19.8 | 57.3 |
| 16,038 | 52.1 | 13.9 | 67.8 | 32.0 | 99.8 |
| 32,084 | 105.1 | 53.0 | 160.3 | 51.3 | 211.6 |
| 69,628 | 233.7 | 13.1 | 251.8 | 85.6 | 337.4 |

**組版のコストは、測り直した文字数にきれいに比例する。**縦書きで1文字あたり3.26µs（525標本の中央値、3.16〜4.67µs）、横書きで0.96µs。200字から7万字まで、3桁近い範囲のどこにも超線形な項が無い。6.6で見た「1ブロックが巨大なら1打鍵が重い」は、**隠れた二乗項ではなく、単に文字数がそのまま出ていただけ**である。縦書きが横書きの3.4倍なのも6.6（3.5倍）と一致する。

**そしてコストが従うのはブロック数ではなく文字数である。**最下行は40ブロック合計69,630字を測り直した全体再計測だが、1ブロック32,000字の打鍵ときっちり2倍の関係にある。分けても、測る文字数が同じなら同じだけかかる。

#### 許容できる段落長は、いま使っているブロック予算そのものだった

Split合計が1フレーム（60Hzで16.7ms）に収まるのは**1,000字あたりまで**である。`BLOCK_MAX_CELLS`は768セルで、既定の配置ではおよそ768字にあたる——**分割規則が狙っている大きさは、最初から正しい大きさだった。**新しい閾値は要らない。欠けているのは基準ではなく、論理行の内側でその基準を適用する手段だけである。

タイルも同じことを言っている。1,000字のブロックで`tiles`は3.0msだが、32,000字では53.0msになる。**巨大なブロックは多数のタイルにまたがり、その全部が一度に無効になる。**切れれば描画側も一緒に直る。

#### この計測が次の設計に課すこと

コストが文字数に比例し、ブロック数に依存しないということは、**長大な段落を分割しても、1打鍵がその段落全体を測り直すのであれば1msも速くならない**ということである。得になるのは、打鍵が分割後のブロックの一部だけを無効にできるときに限られる。

DirectWriteの折り返しは貪欲なので、ある折り返し位置から後ろの組版はそこから先の文字だけで決まる。したがって実際の折り返し位置で切れば、後続は同じに組まれる。問題は編集がその位置を動かすことで、**段落の末尾で打てば無効になるのは最後の1ブロックだが、先頭で打てば以降のすべてが動く。**段落先頭での入力は今と変わらない。この非対称は分割方式を選ぶ前に受け入れておく必要がある。

### 6.10 論理行の内側で切った結果

まず**遅くなった。**最初の版は、文書中のすべての長大段落を毎打鍵組版していた。計測用文書の対象段落は合計63,000字あり、6.9の3.26µs/字を掛けると205ms——観測された床とぴったり一致した。編集の大きさに関わらず毎回これを払っていた。

一方で切ること自体は最初から効いていた。

| | 切る前 | 切った後 |
| --- | ---: | ---: |
| 最大ブロック | 32,000字 | **770字** |
| 1打鍵の再計測 | 32,000字 | **750字** |
| `tiles` | 53.0ms | **1〜5ms** |

**つまり切り口を使うコストではなく、切り口を探すコストだけが青天井だった。**段落ごとに折り返し位置を憶え、変わっていない段落を訊き直さないようにして床は消えた。

#### 接頭辞の再利用と、その限界

編集より前の折り返し位置は動かないので、前回の結果を接頭辞まで残し、そこから先だけ組版し直す。1,000字の段落（ペイン高さの都合で1行83字・12行）で測った。

| 入力位置 | 共通接頭辞 | 縦書き | 横書き | Split合計 |
| --- | ---: | ---: | ---: | ---: |
| 段落の末尾 | 2,843バイト（≒948字） | 5.0ms | 5.0ms | **10.0ms** |
| 段落の先頭付近 | 61バイト（≒20字） | 9.6ms | 8.0ms | 17.6ms |

32,000字の段落の先頭付近（共通接頭辞94バイト≒31字）では、縦書き`layout`が109.8ms、合計114.0msだった（89標本）。6.9の160.3msから下がっているのは、ほぼ`tiles`の削減ぶんである。

**先頭付近での入力に、残せる折り返し位置は存在しない。**5行目がどこで折り返すかは1〜4行目を組んでみないと分からない——貪欲な行分割の性質そのもので、回避できない。6.9で「分割方式を選ぶ前に受け入れておく必要がある」と書いた非対称が、そのままの形で出た。

| 32,000字の段落 | 切る前 | 切った後 |
| --- | ---: | ---: |
| 末尾で入力 | 160ms | **10.3ms**（縦4.8 ＋ 横5.5） |
| 先頭で入力 | 160ms | 114ms |

末尾の行は実測である。共通接頭辞91,731バイト（≒30,577字）・行数380という`miss`の値がその段落を指しており、`wrapped`は228——**32,000字のうち228字ぶんしか組版し直していない。**再利用の効き方が「再開位置から段落末尾までの長さ」で決まり段落全体には依存しない、という見込みはそのとおりだった。1,000字の段落の10.0msとも一致する。

#### 原因を3回続けて読み違えた

`measured=1`（1ブロックしか測り直していない）を「編集は段落の末尾寄りのはずだ」と読み、そこから外れた推論を3周した。**その段落はブロックが2つしかなく、`measured=1`は位置について何も言っていなかった。**42ブロックある32,000字の段落を頭に置いたままの読みだった。

診断値を足して確定させた。`wrap=asked/exact/resumed`（各長大段落の折り返し位置を何個訊いて、何個がそのまま返り、何個が途中から続けられたか）と、`miss=shared/starts`（再利用を断った段落が前回と何バイト共有していたか、前回の折り返し位置が何個あったか）である。**`shared`が61バイトと出た時点で、実装のバグではなく入力位置の問題だと1行で分かった。**

「ボトルネックの推定は繰り返し外れた」（README）は、原因の推定にも同じだけ当てはまる。

### 6.11 Splitで縦書きのキャレットが右端へ行けない

**6.7とまったく同じ失敗が、直していないほうのペインで起きた。**

症状は「Splitのとき、縦書きペインの右端へキャレットを移動できない」。縦書き単独では起きない。原因は`preview-visible-width`が古いままだったことである。縦書きペインは条件付きUIの中にあるのでSplitへ切り替えると作り直され、**生まれた時点で正しい幅なので`changed visible-width`が一度も発火せず**、プロパティは切り替え前の全幅を保持し続ける。

そこから2つ同時に壊れる。`caret_visible_scroll`は「全幅ぶん見えている」と思っているので、**実際には右端の外にいるキャレットを可視と判断してスクロールしない。**そしてスクロールの下限が`visible - content`なので、**実幅との差だけ、文書の右端（＝先頭）に到達できない。**症状の文言そのままである。

前者は**文書のどこでも起きる**（キャレットが実幅を越えた時点で画面外に出て、以後追従しない）。後者は文書の端でだけ起きる。報告では「特定の節のあたりで」となっていたが、それはそこで操作していたからで、位置に意味は無かった。

`init`での報告を足して直した。6.7で横書きペインの高さについて出した結論——「ペインは可視になった時点で生成されるので、生まれたときの寸法は変化イベントでは取れない」——を、**見つけた場所にしか当てていなかった。**8章にも書いてあったのに、もう一方のペインの、もう一方の軸には及ぼしていない。

#### 答えは最初からログにあった

診断のために`pane`・`scroll`・`caret`をログへ足したが、**それらが無くても分かった。**`tiles_shown`を単独表示とSplitで比べるだけでよかった。

| | 単独表示 | Split |
| --- | ---: | ---: |
| 症状が出た版 | 26.5枚 | **26.9枚** |
| 直した版 | 25.4枚 | **16.3枚** |

ペイン幅が半分なら見えるタイルも半分になる。**症状が出た版では変わっていない。**これはこの項目が挙がるずっと前からログに書かれていた。

**無い値を足す前に、ある値を全部見ること。**なお足した3つは残す。`pane`は今回まさに確認に使ったし、幅が古いという状態は一度起きれば二度起きる。

### 6.12 文書全体の走査を行単位にした

7.1に「入力側の残るコストの過半」として挙げていた項目である。`stats`と`preview`と見出しレベルの3つが、1打鍵ごとに文書全体を走り直していた。

**コストの正体は書記素クラスタの走査だった。**1打鍵で文書全体に対して3回走っていた——原文の書記素、整形後テキストの構築と書記素、プレビューの書記素境界。7万字の文書で合計3.6msである。

3つとも**論理行ごとに独立して決まる**。プレビューは行単位でマーカーを除去し、統計は行ごとの数の和と最大値であり、見出しレベルはその行だけで決まる。ブロック分割で使ったのと同じ性質（3.4）がここにも当てはまる。

| 1打鍵あたり（7万字・121ブロック） | 着手前 | `stats`のみ | 両方 |
| --- | ---: | ---: | ---: |
| `stats` | 2.03ms | 0.02ms | 0.02ms |
| `preview` | 1.59ms | 1.94ms | **0.41ms** |
| 合計 | **3.62ms** | 1.96ms | **0.43ms** |

#### 索引表は行内相対で持つ

`preview`のほうが厄介だった。プレビューが持つのはUTF-16位置ごとの索引表であり、**絶対位置で持つと前の行の長さが変わるだけで全要素が動く。**行内相対にすれば、その行の表は「その行が変わらない限り」生き残り、絶対位置は行ごとの走行合計で出る。

行の突き合わせは両端から行う。先頭からの一致と末尾からの一致を取り、間だけを作り直す。**行の挿入・削除でも前後の行は「ずれた」ではなく「同じ」と認識される。**キャレットが別の行へ移ったときに形が変わるのは2行だけ（前の活性行と新しい活性行）なので、それだけを作り直す。

残る0.41msは、行ごとの表の再構築ではなく**プレビュー本文の文字列連結**である。engineが`&str`を取る以上ここは避けられない。

#### 素朴な版を消さずに残す

この2つの差分化で共通の骨格ができた。**「文書全体を一度に処理する、素朴で明らかに正しい版」を`#[cfg(test)]`で残し、速い版をそれと突き合わせる。**`DocumentStats::from_source`と`visible_markdown_text_with_active_line`がそれにあたり、呼ぶのは検査だけである。

プレビューの引き当ての検査は、**内部表現に依存しない形に書き直した。**元は内部配列を直接読んでいたので、表現を変えるとテストも書き直すことになり、実装に引きずられる。プレビュー本文から線形に計算する形にしたので、次に表現を変えても正解の定義は変わらない。

そのうえで**差分経路そのものを回す。**編集を8回重ねながら活性行も動かし、毎回すべての位置で線形走査と一致することを確かめる。新規構築が正しいだけでは足りない。**「触らなかった行が、周りが動いたあとも正しい場所を指すか」がこの変更の本題である。**

### 6.13 IMEの候補ウィンドウ

2つの症状があり、**どちらも位置の計算では直らなかった。**

#### 縦書きなのに候補リストが横書きだった

症状は「全画面で画面右端の列を変換すると、候補ウィンドウが編集中の行に重なる」。候補リストは幅が可変で右へ伸びるので、編集列の右に開けば伸びる先が編集列から離れる——**普段はこれで正しく、実際そうなっていた。**右端に空きが無いときだけ、Windowsが折り返して編集列の上へ戻す。

**位置の問題として3回考えて、3回とも外した。**左へ400px寄せる案は、リストが収まる幅が数百pxで、**縦書きの数百pxは十数列にあたる**ため、編集中の文字を覆わなくなる代わりに十数列も離れた場所に出るようになった。さらに閾値をまたぐのは入力中のキャレット自身なので、1打鍵ごとに位置が変わった（213回中35回発火していた）。右固定へ戻した。

**原因はリストの向きだった。**一太郎とWordは候補を編集列の**左**に出すが、そのとき**リスト自体が縦書きで表示されている**。縦のリストは幅が狭く下へ伸びるので、左に置いても文字の上へ広がらない。MS-IMEもATOKも縦の候補リストを持っており、呼び出す手段がある。

**変換中文字列のフォント**がその手段である。`ImmSetCompositionFontW`へ、`@`で始まる書体名と`lfEscapement = 2700`を渡すと、日本語IMEは「この変換は縦書き」と解釈して候補リストを縦にする。フォント自体は描画には使われない——文字はDirectWriteが描く——**何を描くかではなく、どちら向きの文字列かを伝えるための値**である。

リストが縦になったので、位置の指定はやめた。`(x + width + 8, y + height + 8)`という横書き用のずらしを外し、**キャレットの矩形そのもの**を変換中の位置として伝える。どちら側に開くかはIMEが決める。一太郎とWordがしているのもこれである。

**位置は症状で、形が原因だった。**同じ層——座標の計算——の中だけで解こうとしている限り、答えは無かった。

#### クリックしたあと、1文字目の変換だけ位置がずれる

クリックでキャレットを置くと、**最初の変換だけ**候補が前の位置に出る。確定すると次からは正しい。ペインをまたいだときだけでなく、**同じペインの中でクリックしても起きた**ので、フォーカスの移動は関係が無い。

ログから、**アンカーの値はクリック直後から正しい**ことが分かった（キャレットが飛んだ再描画で、アンカーも同じだけ動いている）。問題はその先、Slintが「変換位置が変わった」とWindowsへ伝え直すタイミングにある。**変換位置が送られるのは入力欄自身のカーソルが動いたときで、欄が移動しただけでは送られない。**変換が始まる時点では前の位置のままで、変換中に文字が変わって初めて送り直される——だから1回目だけずれ、2回目から合う。

入力欄の`x`と`y`が変わったときに`set-selection-offsets(0, 0)`を呼び、**位置を送り直させる**ことで直った。欄は空なのでカーソルは動かないが、Slintが再報告する条件は満たされる。

### 6.14 ツールバーを押すと、そのあと編集できなくなる

本体実装で「開く」「保存」を足した直後に出た。**保存はできるが、そのあとキーが入らない。**ペインをクリックすると戻る。

ペインへ文字が入る先は`FocusScope`ではなく、その中のIME入力欄（`horizontal-ime-input` / `ime-input`）である。**この欄がフォーカスを取るのは、ペイン内のpointer-upのときだけ**で、`FocusScope`は`focus-on-click: false`である。Slintの`Button`はクリックでフォーカスを取るので、ツールバーを押した時点で欄はフォーカスを失い、次のクリックまで戻らない。ダイアログはさらに、ウィンドウごとフォーカスを外して返す。

**これは新しい不具合ではなく、既存のボタンすべてが持っていた性質である。**保存が「押したあとも打ち続ける操作」だったので表に出た。

`focus-generation`を1つ置き、ツールバーの各ボタンとダイアログを閉じたRust側が増やす。ペインはそれを見てIME入力欄へフォーカスを戻す。どちらのペインが応えるかは、最後にフォーカスを持っていた側（`focus-horizontal`）とSplitかどうかで決める。

**ここでも`changed`だけでは足りなかった（6.11と同じ）。**表示モードを切り替えるボタンは、押した時点ではまだ相手のペインが存在しない——ペインは可視になった時点で生成されるので、`focus-generation`は生まれたときの値として渡り、変化イベントは起きない。**`init`でも同じ判定をする。**6.11で「両ペイン・両軸のすべてに当てはまる」と書いたが、当てはまるのは寸法だけではなかった。

### 6.15 横書きの「整形後」で、下へ行くほど選択とキャレットがずれる

横書きペインを整形後表示にすると、クリックした位置と実際に置かれるキャレットが合わない。**上のほうは合っていて、下へ行くほどずれが大きくなる。**キャレットが動かせないので入力もできない。

`refresh_horizontal`だけが「原文か整形後か」を見ていた（3.12）。**ヒットテストとキャレット移動の4経路は`PaneText::Source`を直に書いていた。**

| 経路 | していたこと |
| --- | --- |
| `update_horizontal_selection` | クリック・ドラッグの位置を原文で引く |
| `edit_adjacent_grapheme_horizontal` | 「1文字」を原文の書記素で数える |
| `move_horizontal_caret` | 矢印移動を原文で引く |
| `move_horizontal_to_line_edge` | `Home`/`End`を原文で引く |

**ずれが下へ行くほど大きくなるのは、原因そのものである。**整形後表示はMarkdown記号を落とすので、2つの文字列の対応は上から下へ、記号を通過するたびに開いていく。「下部だけおかしい」ではなく「上ほど偶然合っている」だった。

**性能ログにも出ていた。**該当区間の`horizontal`行は、ポインタを動かすだけで毎回`measured=4`（全ブロック再計測）になっている。engineが1イベントごとに原文と整形後を往復していたからである。**症状は選択のずれで、ログに出ていたのは無駄な再計測で、原因は1つだった。**

3.12に「下流は`PaneText`に対して書かれていて、どちらを渡されたかを訊かない」と書いた。**それは正しかったが、`PaneText`を受け取っていない経路があることは見ていなかった。**どちらを組むかを決める場所を`horizontal_pane_text`の1つにまとめ、位置を引く経路は全部そこを通す。

### 6.16 横書きで、文書の最後の行に届かない

横書きペインで、ウィンドウの最下部に来る行を編集できない。クリックしてもキャレットが乗らず、スクロールしても戻される。

`horizontal_visible_flow`は**わざと過大に見積もる**。タイルをどこまで用意するかを決める値で、コメントのとおり「高く外すのはタイル1〜2枚の損、低く外すと白紙を見せる」からである。**その値がキャレットのスクロール計算にも渡っていた。**

`caret_visible_scroll`の下限は`visible - content`である。ログの実測で`reported=585`、`visible=760`、`content=861`だったので、

| | 下限 |
| --- | ---: |
| 本当の表示高さ 585 | **-276** |
| 過大見積もり 760 | -101 |

**-101までしか送れないので、最後の175pxは決して表示に入らない。**しかも`apply_horizontal_geometry`はリフレッシュのたびにこの値を書き戻すので、ホイールで下まで送っても、クリックや入力のたびに引き戻される。

**縦書きペインは最初から本物の値（`preview-visible-width`）を渡していた。**同じ量が横書きだけ過大な側から取られていた、という非対称である。

`horizontal_viewport_height`を分けた。**同じ「見えている大きさ」でも、外していい向きが逆の2つの量がある。**タイルは高く外す。スクロールは高く外してはならない。前者は損が有限で、後者は文書の末尾が消える。

### 6.17 診断ログを別に持つ

6.14・6.15・6.16はいずれも、画面の症状から原因まで何往復かかかった。**性能ログは費用の記録であって、出来事の記録ではない。**6.15で助かったのは`preview=`と`measured=`が偶然そこにあったからで、6.16の`reported`と`visible`は「一度食い違ったから」という理由で後から足された値だった。

`diag/Diag_<日付>_<時刻>_<番号>.log`を1実行につき1つ書く。性能ログが直近2回だけを残すのに対し、**こちらは全部残す**——読みたい実行は最後の実行とは限らない。行は`+経過ms 種別 key=value ...`で、種別で絞れば1種類の出来事だけを抜ける。

### 6.18 repeaterの中のボタンから、モーダルダイアログを開くと固まる

タブ列を実装したあと、**編集済みのタブを閉じようとすると、確認ダイアログが出ないまま固まる**。何も編集していないタブは、同じ経路で問題なく閉じる。

タブ列はrepeaterで、閉じるボタンは**その中の要素**である。そのボタンのクリックハンドラの中で、

1. `publish_tabs`が、そのボタンが載っているモデルを差し替え、
2. `MessageBoxW`が、ハンドラをスタックに残したままWindowsのメッセージを回す。

**ツールバーのボタンから同じダイアログを開いても起きない。**違いはrepeaterの中か外かで、中の要素のコールバックは繰り返しの内部状態を握ったまま走る。そこへメッセージループが入ると、Slintのレイアウトへ再入することになる。

`Timer::single_shot`でイベントループへ逃がし、クリックが終わってから作業する形にした。**「自分が載っているUIを、自分のハンドラの中で作り替えない」**というだけの話である。

**診断ログが、どこまで進んだかを直接示した。**`tab close at=`は`ask_before_closing`の**あと**に出るので、その行が無いこと自体が「ダイアログの前で止まった」証拠になる。成功した回のログには同じ行があったので、経路そのものは正しいと分かった。6.17で足したものが、最初にそれ自身の役目を果たした例である。

### 6.19 Splitを切り替えた直後の1回だけ、文書が飛ぶ

repeater化のあと、**ログを読んで見つけた**。画面では気づいていない。Split中の縦書きペインのrefresh 161回のうち2回、`shown`が**行の全幅**（1676）になっている。同じ瞬間の横書きペインは1165で、足すと行（1652）に収まらない——**ありえない値がプロパティに入っている。**

そのとき`geom.v`がスクロールを`-51564 → -50528`へ詰めている。6.16と同じ算術である。`caret_visible_scroll`の下限は`visible - content`なので、`visible`が実幅478ではなく1676だと下限が1,000px以上手前に来て、**文書がそこまで引き戻される。**

原因は6.11の**残りの半分**である。6.11は「ペインは可視になった時点で生成されるので`init`でも報告する」で直したが、**Splitの切り替えでは、すでに画面にあるペインは作り直されない。**幅が変わるだけなので`init`は走らず、`changed visible-width`が直す。ところが**切り替えのハンドラがその場で走らせるrefreshは、`changed`より前にある**——Slintのレイアウトは遅延評価で、変更ハンドラはイベントループの区切りで走るからである。だから切り替え直後の1回だけ、切り替え前の幅を読む。

出るのは**流れ軸が分割軸と一致するペインだけ**、つまり縦書きペインの幅である。Splitはウィンドウを左右に割るので、横書きペインの流れ（高さ）はSplitで動かない。6.16のとき`horizontal_visible_flow`をウィンドウの高さで抑えたのと、同じ非対称の裏返しである。

**報告された幅を、ウィンドウが与えうる上限で抑えた**（`width_share_bound`）。上限は「ウィンドウ幅 × そのペインの取り分」で、取り分はSplitのとき`split-ratio`とその補数、Split以外は1。仕切りの9pxも余白も引いていない——**上限であって代用ではない**ので、高く外れる分には害が無く、定常状態では決して効かない（報告値のほうが必ず小さい）。ウィンドウがまだ大きさを持たないうちは上限を置かない（0x0で抑えると初回のスクロールが死ぬ）。

**ペインが自分について報告する値は、レイアウトが走る前の一瞬だけ嘘になる。**その一瞬にRustが読む経路がある以上、値そのものを信じきってはならない。6.11・6.16・6.19は同じ話の3面である。

### 6.20 ごみ箱への移動は、他人のメッセージループに入る一箇所

要件5.2の削除は「完全削除せず、ごみ箱へ移動する」である。`SHFileOperationW`に`FO_DELETE`と`FOF_ALLOWUNDO`を渡す。確認は自前の質問（8.3・8.4と同じオーバーレイ）で済ませてあるので`FOF_NOCONFIRMATION`を添える——**が、`FOF_WANTNUKEWARNING`も必ず添える。**

これを落とすと、ごみ箱に入らない場合（ネットワークドライブ、ごみ箱の容量を超える大きさ）に**黙って完全削除される。**書き手が答えたのは「ごみ箱へ移動しますか」であって「消しますか」ではない。要件5.2は削除を取り消せることを求めているので、取り消せないと分かった時点で、それはもう答えられた質問ではない。

代償は、その稀な経路でだけWindowsのメッセージボックスが出る＝**6.18の構造に入ることである。**呼ぶのは左ペインのボタンでrepeaterの中ではないので6.18そのものではないが、「自分のハンドラの中で他人のメッセージループを回す」点は同じである。**そこで止まっても失うものが無い**（本文にも、そのUIにも触れていない）ことを確かめたうえで受け入れた。

**これは、質問を自前で描いている理由の裏返しでもある**（`file_dialog.rs`の冒頭）。避けられるものは避け、避けられないものは一箇所に閉じて、そこで何が起きうるかを書いておく。

「エクスプローラーで表示」は`SHOpenFolderAndSelectItems`に**そのファイル自身の項目**を渡す（親フォルダではない）。項目を1つ渡して選択リストを空にすると、シェルはそれを持つフォルダを開いて、それを選んだ状態にする。`explorer.exe /select,` でも同じことができるが、パスに空白やコンマが入ると引数の解釈が食い違う——プロセスを起こさない分も含めて、APIのほうが短い。

### 6.21 LineEditは矢印キーを食う

絞り込み欄と候補リストを1つの重ね物にした画面で、**`↑↓`がリストを動かさなかった。**Escは効いた。

`LineEdit`が`↑↓`を自分で扱ってしまい、上の`FocusScope`まで上がってこない。単一行の欄なので
上下に動く先は無いはずだが、キーは消費される。Escは消費しないので上がる——**だから「Escが効いて
いるのだから経路は通っている」という推論が成り立たない。**キーごとに違う。

候補リストを`↑↓`で選ばせたいなら、欄を`LineEdit`にしない（`TextInput`を自前で囲む）か、
`FocusScope`を欄より内側に置けない以上、欄そのものにキーを扱わせる必要がある。

この画面（クイックオープン）は要件から外したので直していない。**次に絞り込み欄つきの一覧を
作るときに、同じところで止まる。**

### 6.22 メニューとツールチップはSlintにある。ただし色は変えられない

**「Slintにメニューの部品もツールチップの部品も無い」は誤りだった。**アイコン主体のUIへ移る前提で
一度そう判断し、自前の重ね機構を作る計画まで立てたが、registryの
`i-slint-compiler-1.17.1/builtins.slint`を読んで取り消した（2026-08-22）。1.17にあるもの：

- `ContextMenuArea` ／ `Menu` ／ `MenuItem` ／ `MenuSeparator`——`show(position)`・`close()`・
  `checkable`・`icon`・入れ子メニュー・ショートカット表示・キーボード送りまで含む。
- `Tooltip`——要素の中へ1つ置くだけ。**既定で500ms待って8pxずらす**、画面端では向きを変える。
  `text`を書かず子要素を1つ置けば中身は自前にできる。
- `PopupWindow`——`close-policy`に`close-on-click`／`close-on-click-outside`／`no-auto-close`。
- `Timer`——Slint側で使える。

実験扱いの印はどれにも付いていない。

**色は上書きできない。**メニューもツールチップも選択中のwidget styleが描き、
`widgets/fluent/style-base.slint`の`global Palette`は**色が全部`out`プロパティ**で、`in-out`は
`color-scheme`だけ。アプリ側から差せる隙間が無い。`ToolTipImpl`は
`Palette.alternate-background`＋`Palette.border`、`MenuFrame`は`FluentPalette.background`＋
7px角丸＋最小幅280pxで固定。

**だからメニューは`PopupWindow`に自前の中身、ツールチップは組み込み、と分けた**（2026-08-22、
ユーザー判断）。`PopupWindow`は「外を押したら閉じる」と位置決めと最前面だけを担い、中身の
行は`Tok`で描くので紫になる。ツールチップは小さく短時間しか出ないので、Fluentの見た目のまま
機構をもらう。

**踏んだ罠：`string`は`styled-text`にならない。**`Tooltip.text`は`styled-text`で、
`text: root.tip;`はコンパイルエラーになる（`langtype.rs`の`is_convertible`が
`StyledText => matches!(other, Type::StyledText)`）。`text: @markdown("\{root.tip}");`と書く。
**副作用として、tipはmarkdownとして読まれる**——`*`や`_`を入れるとその周りが斜体になる。

**踏んだ罠2：`Tooltip`は書いた位置がそのまま重なり順になる。**`TouchArea`の**前**に書くと出ない。
`lower_tooltips.rs:258`が`elem.children.insert(tooltip_child_index, tooltip_area)`——
`Tooltip`を書いた索引にそのまま`TooltipArea`を差し込む。子は後ろから当たり判定されるので、
上に`TouchArea`があるとホバーがそこで止まり、下の`TooltipArea`まで届かない。
**`Tooltip`は要素の最後に書く。**上に載っても`ForwardAndObserve`（`items.rs`の
`input_event_filter_before_children`）で「見るだけで取らない」ので、下の`TouchArea`の
クリックは通る。**画面でしか出ない種類で、コンパイルは通る。**

**踏んだ罠3：ポップアップの中のrepeaterは、先に伝えてから閉じる。**タブ一覧のメニューを
`tabs-menu.close(); root.tab-selected(index);`の順で書いたら、**メニューは出るのに選んでも
何も起きなかった**（2026-08-22。診断ログにも何も残らない＝コールバックがRustまで来ていない）。
`close()`がポップアップを消すとき、**いまハンドラを走らせている当の要素も一緒に消える**ので、
次の行が実行されない。**Slint自身の`ComboBox`が逆順で書いている**——
`widgets/fluent/combobox.slint`の`clicked => { base.select(index); popup.close(); }`。
静的な行だけのメニュー（ペインの`⋮`）は`close()`が先でも動いていたので、**repeaterの中かどうか**が
分かれ目に見える。**閉じるのは最後**にすること。モデルの差し替えが心配な場合でも、Rust側が
`Timer::single_shot`で本体を次のターンへ逃がしていれば、開いたままのポップアップの下で
作り直しは起きない。

**踏んだ罠4：`PopupWindow`の`is-open`は、読むと生成コードが壊れることがある。**
`Esc`でメニューを閉じるために`is-open`（開いている間だけtrueになる組み込みの出力プロパティ）を
読んだところ、**`typography-panel`だけ**`cargo check`が生成コードで落ちた（2026-08-22）：

```
error: closure bodies that contain statements must be surrounded by braces
  --> target/.../out/app-window.rs
  ... . map (| x | x . set (value) ;
```

`is-open`を読むまでこのプロパティは実体化されず、setterも生成されない。読むと`show()`の側に
「親をたどって`is-open`に`true`を入れる」コードが生成され、**その`map`のクロージャに波括弧が
付かない**。壊れたのは`typography-panel`だけで、`app-menu`・`pane-menu`・`tabs-menu`は通った。
違いは**`show()`をどこから呼んでいるか**：後者は自分を宣言している部品の中からで、
`typography-panel`は`☰`のポップアップの中と、ペインのrepeaterの中（2段上）から呼んでいる。
`_self.parent.upgrade().and_then(|x| x.parent.upgrade())`という生成コードがその2段である。

**避け方：親をまたいで`show()`されるポップアップの`is-open`は読まない。**開いていない
ポップアップの`close()`は`window.rs`の`close_popup`が`if let Some(popup_index)`で弾くので、
**確かめずに閉じてよい**。失うのは「閉じたかどうか」の答えだけ。

**アイコンは`Path`の`commands`で描いている。**画像にすると(1)ディスク上のファイルが要り、
(2)`Tok`から色を取れないのでホバーで色が変わらない。`Path`は`viewbox-width`/`viewbox-height`と
SVGのパス文字列を取り、`stroke`にトークンを束ねられる。**ただし`Rectangle`の子は
x/yを書かないと左上に置かれる**ので、ボタンの中で中央に出すには自分で計算する。

### 6.22b 色の設定が「何も起きない」ように見えたのは、タイルの署名に色が無かったから

**要件9の文字色・背景色を足したが、打ち込んでも画面が変わらなかった**（2026-08-22）。値はエンジンまで
届いていて、再組版も走っている。変わらなかったのは**タイルが再描画されなかった**ためで、
`tile_signature`が混ぜていたのは`hash_typography`＝字送り・行間・本文サイズだけだった。
**色だけが「幾何を変えずに絵を変える」設定**で、それまでの設定は全部どこかの寸法を動かしていたため、
署名が寸法しか見ていなくても間に合っていた。

`hash_colours`を足して、**タイルの署名にだけ**混ぜた。レイアウトと計測の鍵には入れない——
色は幾何を変えないので、測り直す理由が無い。

**見出しごとの色は`SetDrawingEffect`で塗る**（D2Dの`DrawTextLayout`は描画効果としてブラシを渡されると
それを前景色に使う）。ブラシはレンダーターゲットのもので、**毎回の描画の前に色を入れ直し、
毎回の描画の前に効果を貼り直す**。理由は2つ：色は設定なのでターゲットより長生きし、
逆に**キャッシュされたレイアウトは、作り直されたターゲットの古いブラシを掴んだままになりうる**。

これは6.18の親戚である——**「キャッシュの鍵に、絵を決めるものが全部入っているか」**。
`hash_style_runs`が`marks`を混ぜていなかったとき（装飾が別の段落に付いた）と同じ形で、3例目になる。

### 6.23 Slintのレイアウトは、伸ばしもしなければ切りもしない

アイコン主体へ移す途中で2回はまった。**どちらもコンパイルは通り、画面でしか出ない。**

**`alignment: start`を書くと伸縮子が効かない。**タブ列の右端へ置きたいボタンの手前に
`horizontal-stretch: 1`のスペーサーを挟んでいたが、広がらなかった。`alignment`が`stretch`
以外のとき、レイアウトは各要素に**希望する幅を配って余りを行の末尾へ置く**だけで、
伸縮子を見ない。**右端に留めたいものは`x: parent.width - self.width - 8px`で直接留める。**
コードには「スペーサーが右へ押しやる」というコメントが以前から付いていたが、
実際にはそうなっていなかった。

**レイアウトは溢れても切らない。**タブが増えて合計幅が帯を超えると、縮みも隠れもせず、
**右のボタンの上に重ねて描かれる**。`clip: true`の箱に入れて、ボタンの手前で切るようにした。
これで「場所が無い」が「タブがそこで終わっている」に見える。

**——ここは半分まちがっていた（2026-08-22）。レイアウトは縮む。**縮まなかったのは中身の
ほうに理由がある。`i-slint-core-1.17.1/layout.rs`の`solve_box_layout`を読んだ：

```rust
let align = match data.alignment {
    LayoutAlignment::Stretch => { layout_items(...); None }
    _ if size_without_padding <= pref_size + spacings => { layout_items(...); None }
    LayoutAlignment::Start => Some((data.padding.begin, data.spacing)),
    ...
```

**希望する幅に足りないときは、`alignment`が何であれ`layout_items`が走る**（2番目の腕）。
その中の`adjust_items::<Shrink>`は各要素を`min`まで縮め、`can_grow = size - min`が0のものは
飛ばす。**伸縮子が0でも縮む**（`total_stretch <= 0`なら縮める要素で等分）。

つまり縮まなかったのは`Button`のせいで、**`Text`のmin幅は文字の幅そのもの**だから
（`builtins.slint`のText節：`overflow`が`elide`か`wrap`が`word-wrap`のときだけmin幅が0になる）。
`min`と`pref`が同じ要素からは1pxも取れない。

**これで一度タブを縮ませたが、画面で却下された（同じ日）。**全部のタブは残るものの、
**名前が読めなければタブが並んでいる意味がない**。縮めて残すより、読める幅のまま流すほうがいい。

**採ったのはスクロールのほう**（`editor-pane.slint`の`tabs-view`）。要点は1行で、
Slint自身の`TextEdit`が同じことをしている（`widgets/common/textedit-base.slint`の104行目）：

```slint
viewport-width: max(self.visible-width, strip.preferred-width);
```

**ビューポートに中身が欲しがる幅を渡せば、レイアウトは足りている＝何も縮めない。**
足りないぶんは帯が流れる。`ScrollView`のバーは`always-off`で消す（34pxの帯に狙う対象を
2つ置かない）。手が届くようにする道は2つで、どちらも要件6.3：右端の`⌄`が**全タブを名前で並べた
メニュー**、そして**前に出たタブが帯を自分のほうへ引く**（`reveal-tab`）。
**引くのはタブ自身**——タブが前に出る経路は帯・一覧・ツリー・隣を閉じる・起動時の復元と5つあり、
共通なのは「そのタブが前に出た」ことだけで、位置を知っているのはタブだけだから
（`Tab.report()`が`init`と`current`/`x`/`width`の変化で鳴らす）。
**`＋`と`⌄`は帯の外**に置いた。帯と一緒に流れるボタンは、タブが多くて必要になったときにいない。

**質問の選択肢を横1列に並べたのも、同じ形で破綻した**（2026-08-22）。「作業コピーを残して閉じる」は
12文字あり、4択だとどんなカードよりも横に長い。**溢れたぶんはカードの外へ描かれる**ので、ボタンが
はみ出して見えた。**縦1列にした**——言葉の長さがどうであれ、どの言語であれ収まる。横に並べられるのは
選択肢が短い言語だけで、それはキャンバスを英語の文言で描いていたから見えなかった。

**同じ理由で、レールやバーの中のアイコンは座標で置いている。**交差軸で固定幅の子がどう
揃うかを確かめていないため、`x: 7px`（40pxのレールに26pxのボタン）のように自分で計算する。
`(parent.width - self.width) / 2`も同じ用途で使える。

## 7. 未検証・未成立の項目

**この検証でまだ確かめていないことだけを置く。**済んだもの・判断が出たものは本文へ移し、7.3に参照先だけを残す。性能の検証はここで区切りとした（6.12まで）。

### 7.1 組版の未実装

- **段落間隔。**「段落と段落のあいだ」の量で、行の属性としてどこに属するかが自明でない。段落の末尾の改行が持つ高さとして表せる見込みはある——DirectWriteの行送りは行ごとの`height`の最大で決まるので、改行だけを大きくすれば、その行だけ送りが伸びる。**ただし字下げと同じ問いが先に立つ**（3.11）——それは書き手が空行で表すものではないか。
- **見出し以外の行属性。****判定・字下げ・行頭記号まで入った。残りは引用の縦棒と水平線。**`LineStyle`が`LineKind`（Body／Bullet／Ordered／Task／Rule／Fence／Code）と`quote_depth`を持つようになり、`document::line_styles`が文書を1回巡って全行を決める。**フェンスだけが「その行より前が決める」属性**で、それ以外は行だけで決まる——ブロックが自分の外に依存しないという前提はそのまま。フェンスの中の行は`is_literal`で、プレビューが素通しする（**コードの中には何もマーカーが無い**というインラインコードと同じ規則の、行版）。見出しもフェンスの中では見出しでない——`outline`も`line_style`を通すので、一覧とペインが別の意見を持たない。
  - コードブロックの見た目は**`Marks::code`の範囲属性1本**で出している。インラインコードとまったく同じ経路なので、**幾何は一切動いていない**。
  - **字下げは`IDWriteInlineObject`（4.12）。**`LineStyle`が持つマーカーの長さが`StyleRun.ornament`になり、`apply_marker_boxes`が箱を張る。箱の幅は本文2文字ぶんで、**マーカーの長さによらず同じ**——`-`／`10.`／`- [x]`が同じ字下げから始まるのはこのため。`leadingSpacing`（4.11）は採らなかった。
  - **箱は「測る側」と「描く側」の両方に張らなければならない。**レイアウトを作る場所は2つある（`update`＝測ってそのレイアウトをキャッシュする方と、`layout_for`＝キャッシュを外した方）。実装中に片方だけに張りかけた。**箱つきで組んで箱なしで測ると、リストのあるブロックから後ろが全部ずれる。**
  - **飾りのインクはタイル描画側**（`draw_marker_ink`）。`•`／`☐`／`☑`と、番号はその範囲の元テキスト（`10.`は`9.`と違うことを言っている）。**各飾りは自分の範囲が`HitTestTextRange`で返す矩形に描く**ので、行がどちら向きに走るかをここでは訊かない——矩形がもう答えている。`selection_rects`と同じ道具。
  - **箱に描かせない理由**：Direct2Dの`DrawTextLayout`はインラインオブジェクトにレンダラを渡すのであってレンダーターゲットは渡さない。箱がターゲットを抱えると、ターゲットが作り直されるたびに作り直しになり、箱を参照するレイアウトはまさにそこをまたいでキャッシュされている。
  - **記号の置き換えは要らなかった。**箱がマーカーの字形を隠すので、`-`を`•`へ**書き換える**必要がない。つまり**プレビューは元テキストから削るだけ**という`PreviewLine::build`の対応表の前提に触らずに済む。これがこの手段を選んだ2つ目の理由である。
  - **キャレットのある行には箱を張らない**（要件7.3.1）。箱の中に位置は無い（4.12）ので、張ると`-`と空白のあいだにキャレットが立てなくなる。マーカーを隠す条件とまったく同じ条件なので、`PreviewLine::build`の`active`分岐1箇所で足りる。
  - **引用の縦棒と水平線が入った**（2026-08-24）。**「範囲の矩形」ではなく「行そのものの矩形」**で、`text_blocks`の`line_runs`が論理行ごとに`LineOrnament::Quote{depth}`／`Rule`を返し、タイル描画側の`line_flow_range`が`BlockPlacement.lines`（視覚行）を**開始位置で**その範囲へ寄せ集める。行数を数えないのは、**論理行が何本の視覚行になるかは組版の産物**だからで、視覚行がどの論理行のものかを言えるのは開始位置だけである。**DirectWriteを一度も呼ばない**——測ったときの行表がもう答えを持っている。
    - **`HitTestTextRange`ではだめだった理由**：あれは文字数で答えるので、折り返した続きの行まで含めた「行の高さ」も、文字の無いところまで伸びる罫線も表せない。飾りのインク（`•`など）が今もあれを使っているのは、あちらが**範囲**の飾りだからである。
    - **字下げはブロックのものにした**（2026-08-24、画面を見て決め直した）。最初は`> `を残して`- `と同じ箱で隠したが、**箱は論理行の頭にしか置けないので、折り返した続きの行だけが左端へ戻った**。引用は段落全体が寄るものなので、これは字下げになっていない。いまは`BlockSpan`が`quote_depth`を持ち、**引用の深さが変わるところで必ずブロックを切って**、そのブロックだけを狭く組んで寄せている（`block_inset`／`block_extent`）。`> `はまたプレビューから消えるので、本文文字数が`> `を数える副作用も消えた。
      - **行軸を画面座標に変える箇所は全部この字下げを通る**——描画原点・キャレット・ヒットテスト・選択矩形・行移動の5つ。1つ忘れると、その操作だけが本文の左隣を指す。
      - **レイアウトと計測の鍵は`line_extent`ではなくブロック自身の`line_box`にした。**同じ本文でも字下げが違えば別のレイアウトで、それを1つの数で言えるのがこの箱の幅である。
      - **折り返し位置の問い合わせも幅を持つ。**`WrapPoints::line_starts`が`heading_level`ではなく`LineStyle`を受け取る形にした。切り口が線の頭であるのは**同じ幅で組んだときだけ**で、ペイン全幅で訊いた位置は引用ブロックの折り返し位置ではない。`ParagraphWraps`の鍵も`LineStyle`になった。
      - **原文には何も付けない**（2026-08-24、画面で出た）。字下げも縦棒も罫線も原文の面に出ていて、`---`の上に罫線が重なっていた。**判定はマーカーの有無**——`StyledText::is_preview`＝`markers`が空かどうかで、箱が前から使っていた signal と同じもの。原文では記法そのものが本文なので、その代わりに立つものは全部整形表示のものである。
      - **同じ仕組みで箇条書きのぶら下げ字下げもできる**（項目ごとにブロックを切り、記号を溝に描く）。ただしリストばかりの文書でブロック数が増えるので、先に測ること。
    - **水平線は`---`を箱で隠す**（`Ornament::Rule`）。`Ornament::Quote`とともに**インクを持たない箱**で、`draws_ink()`が偽。**行を`is_literal`にしたのが要点**——`***`をプレビューに通すと`push_marked`が斜体と読んで`*`1つに縮み、**原文で測った箱が表示行より長くなる**。「コードの中には何もマーカーが無い」の行版で、`---`もその行自身の印である。
    - **色は本文の墨の30%**（`ORNAMENT_ALPHA`、`SetOpacity`）。要件9が書き手に持たせるのは墨と紙で、棒も罫線も**文字ではなく文字への印**なので、選ばれた墨から作れば何を選んでも家族が揃う。既定の墨を既定の紙に置くとデザインの`rule-strong`になる。
    - **タイルの署名に`line_runs`を入れてある。**棒も罫線も幾何を動かさないので、入れないとキャッシュにある古いタイルがそのまま出る（6.22bと同じ罠の3例目）。ブロックの本文だけでは足りない——**フェンスの中の`---`はただのハイフン3つ**で、それを決めたのはブロックの外である。
  - **コードブロックの地が入った**（2026-08-24）。`LineOrnament::Code`が**フェンスの行も含めた1ブロックぶんを1つの走り**として返し、角を丸めた矩形を本文の下に敷く（`FillRoundedRectangle`、墨の8%）。行ごとに描くと切れ目ごとに継ぎ目が出て角も丸められないので、連続するコード行を束ねている。
    - **フェンスの行は箱で隠す。**`Ornament::Rule`を`Ornament::Hidden`へ改名して`LineKind::Fence`にも使う——「行が丸ごと印であるとき、その代わりに立つものは行の側にある」という同じ話。**行そのものは残る**ので、そのぶんが上下の余白になった（デザインの`padding:14px`のかわり）。
    - **原文の面には出ない**（`is_preview`）。原文には``` ``` ```がそこにあるので、地を敷いても隠すものが無い。
    - **継ぎ目が見えた**（2026-08-24、画面で出た。横書きだけ・縦書きでは出ない、というのが手がかりだった）。原因は2つあり、どちらも**「行の矩形を複数の図形で描くと、図形の境目が合成2回になる」**という同じ話。
      - **論理行ごとに描いていた引用の縦棒**が、境目で1本ぶん濃く見えた。**カーソルを動かすと濃い位置も動く**——アクティブ行は原文を出すので文字数が変わり、`ends_a_block`の判定が動いてブロックの切れ目が移るためである。**連続する行をまとめて1本の走りにした**（`line_runs`が`Gathered`で束ねる）。地も棒も1文書1図形が正しい形で、これは「行ごとに描く」を選んだのが誤りだった。
      - **ブロックをまたぐと図形が2つに割れる。**`place_blocks`はブロックの寸法を1つずつ丸めるので、**行の合計はブロックの置かれた幅より最大半画素短い**。その半画素に紙が見えるか、2度塗られるかで線になる。**ブロックの端に達した走りは、置かれた端へ寄せる**（`MarkExtent::at_edge`）——隣のブロックとぴったり合うのはそこだけである。角の丸めもその端では切る（継ぎ目で丸めると欠ける）。
      - **縦書きで出なかったのは、ペインの幅が違えば`cells_per_line`が違い、切れ目が別の場所に落ちるから。**同じ不具合が見えなかっただけで、無かったわけではない。
      - **切れ目がフェンスの直前に落ちると、閉じフェンス1行だけが次のブロックへ行く**（2026-08-24、最初のコードブロックだけ上側の角が残った）。その1行は「両端ともフェンス」に見えるので丸められ、上の大きいほうは角なしになる。**フェンスの中には普通の切れ目を落とさないことにした**（`may_end`）——`BLOCK_MAX_CELLS`は残るのでブロックは有界のまま。これで**閉じフェンスがブロックの先頭に来ることが無くなり**、「走りの端がフェンスなら、それがその走りの端」が成り立つ。**ブロックの境目が自分の行だけで決まらない唯一の場所**になるが、`line_cells`が既にstyleを読んでいるので、フェンスのある文書ではもともと完全にはローカルでなかった。
      - **閉じていないフェンスの地は下側が丸くない**（2026-08-24、意図）。角は「ここで終わる」と言うもので、閉じフェンスが無いブロックは終わっていない——文書の末尾まで続く。**閉じ忘れがそのまま見える**のは副産物として良い。丸めたいなら、描画側が「これが文書の最後のブロックか」を知ればよい（`line_runs`の側では「切れ目で割れた」と「閉じフェンスが無い」が区別できない）。
      - **角を切る条件を「ブロックの端に達したか」にしたのがもう一つの誤り**（2026-08-24、丸まっているのが3つのうち1つだけになった）。**ブロックの端に達したことと、隣のブロックが続きを持っていることは別**で、たまたま端で終わったコードブロックまで角を切っていた。**どこで切れ目が落ちたかは分割の性質であって文書の性質ではない**ので、丸めてよい端を決めるのはフェンスだけである（`LineRun::own_ends`）。寄せる（`at_edge`）ほうはブロックの幾何のままでよい——どちらでも正しいので。
      - **その寄せを「行の番号」で判定したのが次の誤り**（2026-08-24、縦書きで地が本文まで伸びた）。**縦書きは流れ軸に対して読み順が逆向き**なので、ブロックの先頭行は流れ軸の遠い端にいる。先頭行を含む走りを「近い端に達した」と見なして寄せると、**そのブロックの残り全部を覆う。****どこまで届いたかは座標の話で、座標は読み順を知らない**——行の`flow_start`／`flow_start+flow_size`の最小・最大と比べる形にした。横書きでは番号と座標が一致するので、この誤りは縦書きでしか出ない。
  - **箇条書きのぶら下げ字下げが入った**（2026-08-25）。折り返した続きの行が項目の頭に揃う。**`windows-0.62.2`のDirectWriteに段落字下げのAPIは1つも無い**（`IDWriteTextLayout`にも`IDWriteTextFormat3`にも「indent」は1つも無い）。`leadingSpacing`は字形だけを送る（4.11）。動かせるのは**ブロックの箱**だけで、引用で採った手がそのまま効く。
    - **項目ごとにブロックを切ったのではない。連なりごとに1ブロックである。**計測用文書`testdata/10_箇条書きの計測.md`（1749項目）を実測したところ`blocks=310`に対し`items=1749/503`（広いペイン）・`items=1749/1245`（狭いペイン）で、**項目ごとに切れば6倍、折り返す項目だけに絞っても狭いペインでは5倍**になる。**項目どうしは字下げを共有する**ので切る理由が無い——違うのはどの記号が溝に立つかだけで、それは行の話であり、行そのものの矩形から描ける（引用の縦棒と同じ道具）。
    - **「折り返す項目だけ切る」案も捨てた。**節約にならないうえ（狭いペインでは短い項目も折り返す）、**ブロックの切れ目が幅で動くようになる**。いまの切れ目は内容が決めるので、幅が変わってもタイルが生き残る（6.10）——リストだけそれを手放す値打ちが無い。
    - **`BlockSpan.quote_depth`は`indent_steps`になった。**引用も項目も字下げで、**足し合わさる**（引用の中の項目は2段）。数え方は`LineStyle::indent_steps`1箇所で、**分割と折り返し位置の探索が同じ規則を読む**——別々に持つと、ある幅で切ったブロックを別の幅で測ることになる。`LongLine`もこの数を運ぶ（**どのペインが訊いているかは分割だけが知っている**——要件7.3.1で原文の面は字下げしないので、styleから読み直す探索は原文を狭い幅で、プレビューを広い幅で測る）。
    - **マーカーの箱は幅を失った。**箱の仕事は2つあった——字形を隠すことと、1段ぶんの幅を取ること。後者はブロックへ移ったので、**箱も1段取ったら項目の1行目だけが2段入る**。`draws_ink()`がそのまま境目になる：インクを持つ箱（`•`／`☐`／`☑`／番号）は幅0、**行まるごとの箱（`---`・フェンス）は幅を保つ**——あちらの幅は次に来るものの字下げではなく、その行が残す余白だからである。
    - **記号は溝に描く。**箱が幅を失ったので`HitTestTextRange`が返すのは項目の本文の頭にある細片で、そこから**行軸に1段戻して**描く。戻す向きは`to_screen(0.0, -indent)`で訊く——**縦書きの溝は列の上側**で、横書きの左側と同じ話を軸が入れ替わって言っているだけである。
    - **DirectWriteに預けた仮定が1つある**：幅0のインラインオブジェクトを含む範囲でも`HitTestTextRange`が矩形を返すこと。返さなければ`count == 0`で素通りし、**文書中の記号が全部黙って消える**。`draw_marker_ink`のその分岐にそう書いてある。
    - **入れたあとの実測（2026-08-25）。**同じ計測用文書（1811項目・2199行）で`blocks=448〜657`——
**ブロック数はペインの幅で倍近く動く**（`line_cells`が`cells_per_line`を掛けるので、広いペインでは
1行が高くつき、768セルの上限に早く届く）ので、幅を揃えずに前後を比べてはいけない。
近い幅どうし（前 extent=705 で310、後 extent=640 で471）で+145、うち**約115は「間を空けた項目」の節**
（下記）で、**連なりが詰まっているリストの増分は1割ほど**である。設計どおり。
    - **費用は時間ではなく個数だけだった。**`layout=`は**文字数に付いていて、ブロック数には付いていない**——
642ブロックで110ms（前は310ブロックで100ms）、657ブロックで342ms（前は382ブロックで334ms）。
1画面のタイルも2〜12枚で前と同じ桁（タイルは画面の面積の話で、ブロック数の話ではない）。
**むしろ打鍵は安くなる**——リストの連なりが切り出されたぶん、1打鍵が測り直すブロックが小さい。
    - **費用が1つ残った：空行を挟むリスト（loose list）は連なりが切れる。**空行は字下げを持たない行なので、
項目・空行・項目でブロックが3つに割れる（60項目の節が**約120ブロック**、切らなければ5前後）。
**見た目は正しい**——空行に描くものは無い——ので個数だけの話で、上のとおりその個数は安い。
「空行はその連なりの字下げを引き継ぐ」規則は書けるが、**フェンスに次ぐ2つ目の「自分の行だけで決まらない
切れ目」**になり、**引用やリストの直後の空行でキャレットが字下げされる**という見える結果も伴う。
安い費用と引き換えにするものが大きいので、**いまは入れない**。
    - **`items=全部/折り返すもの`はこの判断のために足した数**。**折り返すかどうかは`line_cells`では訊けない**（`visual_lines`を分けた理由）——あの数は行送りを掛けたあとなので、190%で組まれた**1行の**項目がすでに1行ぶんのセルを超えている。
  - **既知の限界（引用の縦棒と共通）**：長大段落を折り返し位置で切ると、**どの断片も同じ論理行の属性を受け取る**ので、断片ごとに頭の箱が付く。箇条書きでも前からそうだった。
  - **折り返しの問い合わせが「組まれるとおり」になった**（2026-08-24。デザインが「行の矩形の前に直せ」と書いていたもの）。`WrapPoints::line_starts`が`LongLine`（本文・`LineStyle`・`Marks`・行頭の箱）を受け取る形になり、探索用のレイアウトにも`apply_typography`と`apply_marker_boxes`を通す。**切り口が行頭であるのは、断片が組まれるのと同じ指定で組んだときだけ**で、太字1つ・インラインコード1つで折り返しは動く。
    - **窓と再開は接尾辞を組む**ので、`LongLine::marks_from`が印を切り詰めて数え直す（またぐ印は、そこにある部分だけを言う）。行頭の箱は行頭のものなので、`from == 0`の窓にしか渡さない。
    - **再開できる長さは「印が変わっていないところまで」**（`reusable_prefix`／`marks_agree_until`）。本文が同じでも足りない——**閉じるマーカーは、その前にあったものを変える**。`**`の2つ目を打つと手前が太字になり、太字は別の場所で折り返す。**普通の打鍵は手前の印を動かさない**ので、6.10が守ろうとした場合の再利用はそのまま残る。印は閉じた順に記録されている（`push_marked`）ので、比べる前に並べ替える。
  - **残る近似**：等幅で組まれるコード行の折り返しを本文フォントで測っている（`Marks::code`は`line_starts`まで届くようになったが、`block_flow_bound`の見積りは1つのフォントで数えている）。長大な論理行がコードブロックの中にあるときだけの話。
- 和文の字間として`SetCharacterSpacing`を前後半分ずつ配るのが正しいか。画面では違和感が無かったが、自動テストが見ているのは幾何であって組版としての妥当性ではない。約物の前後や行頭行末では扱いを変える必要があるかもしれない。
- リンク、リスト、コードなどを含む完全なMarkdown原文位置と整形後表示位置の双方向マッピング。

### 7.2 IME

- 変換中文字列内の選択範囲・文節境界の描き分け。

### 7.3 タイル描画のテストが並列で止まる（解決）

`renders_only_the_requested_visible_tile`と`renders_a_horizontal_tile_across_the_pane`が、**スイート全体を並列で走らせたときだけ60秒以上返らない。**一度は「システムのメモリが不足しています」も出た。`run_check.bat`は1スレッドで走らせている（3秒が52秒になる）。

分かっていることだけ並べる。

| 条件 | 結果 |
| --- | --- |
| その2件だけを並列で | 2.0秒で成功 |
| スイート全体を並列で | **60秒超** |
| 4スレッド | 同じく停止 |
| COMをMTAへ変更 | 同じく停止 |
| 1スレッド | 52秒で成功 |

**並列そのものが駄目なのではない。**その2件だけなら並列で通る。多数のグラフィクステストと同時に走ったときに起きる。

**原因は共有DirectWriteファクトリだった**（2026-08-25）。`DWRITE_FACTORY_TYPE_SHARED`を`ISOLATED`へ変えたら**314件が3.16秒で全部通った**（1スレッドでは51.26秒）。止まっていた2件——`renders_only_the_requested_visible_tile`と`renders_a_horizontal_tile_across_the_pane`——も並んで通っている。

**見つけ方は消去法だった。**「複数スレッドが同時に組版したときだけ起きる」なら、原因はスレッドをまたいで共有されているものにしかない。数えると1つしか無かった——Direct2Dのファクトリは`SINGLE_THREADED`、WICのファクトリは`CoCreateInstance`でスレッドごと、`Graphics`そのものが`thread_local`。**共有ファクトリはプロセスに1つで、どのスレッドが呼んでも同じ相手を返す。**

**「何が起きているか」より先に「どこでなら起こりうるか」を数えたのが効いた。**アパートメントの種類を変えてみた1回目は、機序の当て推量から入って外し、そのうえアプリを起動しなくした。**共有されているものの数は数えられる。**

**製品に違いは無い**——編集器は1スレッドで組版するので、共有ファクトリの取り柄であるフォントキャッシュの共有が効く相手がいない。効いたのは`run_check.bat`のほうで、**1往復52秒が3秒になった**。

**残っている宿題は1つ**：STAをどちらのスレッドにも同じ判断で与えていること（下記）。これは並列組版（7.4）を本当に入れるときに答える。

**アパートメントの種類でもない。**組版するスレッドはメッセージポンプを回さないのにSTAを名乗っていたので、それが待ちの原因だと考えてMTAへ変えたが、テストは変わらなかった。**そのうえアプリが起動しなくなった。**winitはウィンドウを作るときに`OleInitialize`を呼び、これはMTAに対しては失敗する。この初期化はウィンドウができる前——起動時の組版の検証——で走るので、先にMTAにしてしまっていた。

STAへ戻した。**証拠の無い変更を「こちらが正しいはず」で残したのが誤りだった。**テストは何も言わず、アプリが誤りだと言った。なお、**ウィンドウのスレッドと、そうでないスレッドは要求が違う**——後者はMTAを欲しがる——のに、ここは1つの判断で両方に答えている。並列化（7.4）に進むなら、そこを分ける必要がある。

タイルの描画面は`TILE_TARGET_PIXELS`で面積一定（約53万画素・2.1MB）に保たれており、通る描画テストと同じ大きさである。**なぜこの2件だけなのかは説明が付いていない。**

**2026-08-25、絞り込んだ。プロセス全体で共有されるCOMオブジェクトは1つしかない**——`DWriteCreateFactory(DWRITE_FACTORY_TYPE_SHARED)`である。Direct2Dのファクトリは`SINGLE_THREADED`で作っており、WICのファクトリは`CoCreateInstance`でスレッドごと、`Graphics`そのものが`thread_local`。**「複数スレッドが同時に組版したときだけ起きる」障害が住める場所は、消去法でそこしか残らない。**

`ISOLATED`へ変えた（`directwrite_render.rs`と`directwrite_probe.rs`の3箇所）。**製品には今日なんの違いも無い**——編集器は1スレッドで組版するので、共有ファクトリの取り柄であるフォントキャッシュの共有が効く相手がいない。実験は`run_parallel.bat`。

**通れば7.3は説明が付き、7.4（組版の並列化）の前提が外れる。**止まるなら、あの2件は共有ファクトリとは別の理由で止まっていることになり、次に疑うのはSTAのアパートメント寿命である——テストのスレッドはメッセージを回さないまま`CoUninitialize`して消えるので、そこへ何かがマーシャリングされていれば戻ってこない。MTAで変わらなかったことがこの筋を弱めているが、**あのときの変更が本当に効いていたのかは確かめていない**（`RPC_E_CHANGED_MODE`なら黙って`None`を返す）。

**製品側の問題ではなかった。**エディタ本体は1スレッドで組版する。これで7.4の前提が外れた。

### 7.4 組版を並列にした

ブロックは自分の計測値だけで大きさを決める（3.4）。**N個のブロックの計測は、そのままN並列にできるはずである。**6.10で残った「段落の先頭付近で114ms」も、切った断片は互いに独立なので、並列化が効く形をしている。

**7.3が解けたので、前提は外れた**（2026-08-25）。ファクトリをスレッドごとに分ければ、314件のテストが並列で通る——そこには組版もタイル描画も入っている。**複数スレッドから同時にDirectWriteを使えることは、もう観測されている。**

残っているのはアパートメントの分け方である。いまはSTAを1つの判断で全スレッドに与えていて、**窓のスレッドはSTAでなければならない**（winitの`OleInitialize`）が、**メッセージを回さない組版スレッドはMTAを欲しがる**。テストが通ったのはSTAのままなので急ぎではないが、並列組版を本当に入れるならここを分ける。

通常の1打鍵は2.5msなので、並列化が効くのは重い場面に限られる。**その場面はもう分かっている**——幅が変わったときの全再計測で、実測110ms（642ブロック・11万字）から350ms。リサイズと表示設定の変更のたびに起きて、**これがいま画面で一番目に見える待ち**である。

**入れた**（2026-08-25、`MeasurePool`）。決めたことを並べる。

- **関門は`BlockMeasure.lines`が`Rc<[LineInfo]>`だったことだった。**ブロックは自分のテキストだけで測れる（3.4）ので測定は素直に分かれるのに、**非アトミックな参照カウントの後ろにある行表は、作ったスレッドから出られない**。`Arc`にした。アトミックの代償はクローン1回あたり数ナノ秒で、この共有がそもそも避けている「打鍵ごとの行表コピー」より桁が2つ小さい。
- **決めることと測ることを割った。**どのブロックがキャッシュに当たるか、どの範囲をどう組むか、箱の幅はいくつか——**全部テキストと算術で、グラフィクスを1つも触らない**。ここを分けたことが、測定を別の場所へ出せる理由そのものである。
- **スレッドは常設。**ファクトリを`ISOLATED`にした（7.3）ので、スレッドを毎回作るとそのたびにフォントキャッシュを建て直す。ドラッグのたびにそれを払っては、分けた意味が消える。
- **持ち帰るのは計測値だけで、レイアウトは持ち帰らない。**`IDWriteTextLayout`は作ったスレッドのもの。代償は画面に出ている数枚を`layout_for`が組み直すことで、**全ブロックを測り直すのに比べれば無い**。
- **48ブロック未満は分けない**（`PARALLEL_MEASURE_MIN`）。1打鍵は1ブロックしか測らない（6.9）ので、渡す費用のほうが高い。
- **待ち行列はスレッドごとに1本**。共有の`Receiver`は錠の内側に要り、**その錠を持ったまま待つスレッドは他の全員の問題**になる。ブロックは大きさが有界（`BLOCK_MAX_CELLS`）なので、順に配れば十分に揃う。
- **足りなかったぶんは呼び出し側が測る。**キューが受け取らなかった仕事も、返事の来なかった仕事も、**「まだ計測値の無いブロック」として1本の道に合流する**——スレッドが1つも無いときに通る道と同じところへ。だから落ち方が何通りあっても、後始末は1箇所にしかない。
- **5秒で待つのをやめる**（`MEASURE_WAIT`）。性能の数字ではなく、**バグが見えたままでいるための仕掛け**である。パニックしたワーカーは返事ごと消えるので、待ち続ければ編集器が止まる。
- **perfログに`measured=全部/分けたぶん`を出す。**`layout=`が速くなっただけでは分かれた証拠にならない——キャッシュがたまたま当たっても同じに見える。

**実測（2026-08-25、`10_箇条書きの計測.md`／11万字）。同じブロック数どうしで並べる。**

| ブロック | 前 | 後 | |
| --- | --- | --- | --- |
| 470 | 106.21ms | 18.71ms | 5.7倍 |
| 461 | 106.48ms | 19.49ms | 5.5倍 |
| 642 | 110.16ms | 18.66ms | 5.9倍 |
| 448 | 122.35ms | 19.77ms | 6.2倍 |
| 465 | 343.58ms | 52.36ms | 6.6倍 |
| 657 | 342.25ms | 50.72ms | 6.7倍 |
| 449 | 350.53ms | 53.57ms | 6.5倍 |
| 475 | 356.42ms | 62.56ms | 5.7倍 |
| 456 | 357.59ms | 60.98ms | 5.9倍 |

`measured=`はどの行も`N/N`で、**全ブロックがスレッドへ渡っている**。

**そして次の相手が見えた。**`09_段落長の計測.md`（6.9万字、`wrapped=63093`）では`layout=`が70〜245msのまま残る。**残っているのは折り返し位置の探索**（`LayoutWraps`）で、あれは`split_blocks`の中、計測より前、UIスレッドで走る。文書の6.9万字のうち6.3万字がそこを通っている——**長大段落ばかりの文書では、`layout=`のほとんどが計測ではなく探索である。**

**同じ文書・同じブロック数で、横書きが240ms、縦書きが70ms**という3.4倍の開きもそこにある。今回入れた並列化とは無関係で、前からあったもの。**次に測るならここ。**

**折り返し探索も並列にした**（2026-08-25）。`LayoutWraps`——分割の途中でDirectWriteを呼ぶ形——を**訊く／答える／使うの3つに割った**。

- **訊く**（`RecordedWraps`）：分割を1回、何も答えずに走らせて、どの行を訊かれるかだけ記録する。**訊くのが、規則の複製を持たずに知る唯一の方法**である——複製は「ブロックがどこで終わるか」についての第二の意見になる。**普通の文書は何も訊かないので、この1回がそのまま分割**になり、2回目は走らない。
- **答える**：再利用の判定（6.10）は**テキストと印の比較だけでグラフィクスが要らない**ので、キャッシュのある側で済ませる。スレッドへ渡るのは「何バイト目から組むか」だけ。
- **使う**（`PreparedWraps`）：表を引きながら分割し直す。**訊いた順に答えるのが厳密に正しい**のは、分割が訊く相手が「その行自身の大きさ」だけで決まり、返ってきた答えに依存しないからである。この2点はテストが持っていて、**どちらもDirectWriteを使わない**（`asking_and_then_answering_cuts_where_one_pass_would`）。
- プールは1つのまま（`LayoutPool`）。スレッドが持っているのはファクトリとフォントキャッシュで、**訊かれる中身によらず同じもの**である。閾値は2段落——ブロックと違って**長大段落は決して小さくない**。

**実測（`09_段落長の計測.md`、6.3万字が探索を通る）。**

| ペイン | 前 | 後 | |
| --- | --- | --- | --- |
| 横書き（extent 455〜1614） | 242ms | 144ms | 1.68倍 |
| 縦書き（extent 1246〜2804） | 71ms | 43ms | 1.64倍 |

`wrap=6/0/0/6`で6段落すべてが分かれている。**上限は1.97倍**で、32,000字の1段落が全体63,000字の約半分を占めるからである。**段落は途中で割れない**——次の断片がどこから始まるかは前の断片を組み終えるまで分からず、行頭でない位置から組めば、そこから下の切れ目が全部嘘になる。差の18%は配る費用と、費用が文字数に完全比例はしないぶん。

**この形は測る前に分かっていた。**作る前に段落長の分布を見て「上限2倍」と見積もり、それでも入れると決めた。1.65倍はその見積もりのとおりで、**次にここを速くしたいなら、分けるのではなく1段落の探索そのものを安くするしかない**。

**タイル描画も並列にした**（2026-08-27）。同じプールの3つ目の仕事（`PoolTask::Render`）である。スレッドが持っているのはファクトリとフォントキャッシュで、**訊かれる中身によらず同じもの**——組版・折り返し探索に続いて、描画もそれで足りる。

- **タイルは「1ブロックの絵」なので、そのまま parcel になる**（3.4）。`TileTask`が持つのはブロック自身の本文・印・配置（`BlockPlacement`）・spec・切り出す位置だけで、engineもDirectWriteも持たない。`MeasureTask`と同じ形で、同じ理由で成り立っている。
- **レイアウトは今度も持ち帰らないが、今度は要らない**——返るのは画素である。代わりに**ワーカーは自分でレイアウトを組む**。
- **それが余計な費用にならないのは、分ける場面ではUIスレッドにもレイアウトが無いからである。**全ブロックを測り直した回は測定がスレッドへ行っていて、レイアウトはそこに残っている（上の「代償は画面に出ている数枚を`layout_for`が組み直すこと」）。**その組み直しが、分けたぶんそのまま並列になっただけ**である。
- **閾値は3枚**（`PARALLEL_TILE_MIN`）。1打鍵が汚すのは打った1ブロックのタイルだけで、**そのブロックのレイアウトはUIスレッドにある**——あちらへ出せば組み直しになる。画面ごと作り直す場面（スクロール・リサイズ・spec変更）だけが、どちらにも無い側である。
- **描くコードは1つ。**`draw_tile`はengineを知らず、2つの道の違いは`cached: Option<IDWriteTextLayout>`——UIスレッドは`layout_for`が持っているものを渡し、スレッドは`None`を渡す。**第二の意見を作らない**ために、表のセルも`cell_layout_for`という同じ自由関数を通し、`draw_grid`もengineから外した。ブロックのレイアウトを作る場所も`build_block_layout`1つに畳んだ（測る・描く・当てるの3箇所が同じ指定で組む——違えばキャレットは文字の無いところに立つ）。
- **画素を余分に写すのは1回だけ。**ワーカーは`graphics.scratch`から`Vec`へ写して送る（所有していないとスレッドを越えられない）。UIスレッドで描いたぶんは今までどおり借りたまま渡すので、**1枚のときの費用は変わらない**。
- **足りなかったぶんはUIスレッドが描く**——渡らなかった仕事も、返事の来なかった仕事も、スレッドが1つも無いときに通る道と同じところへ合流する（7.4の他の2つと同じ）。

**検証は画素で行う**（`a_divided_tile_is_the_tile_drawn_here`）。同じタイル列を1枚ずつ（＝閾値未満なのでUIスレッド）とまとめて（＝プール）描き、**1バイトずつ比べる**。縦横の両方で回す——切り出すのは流れ軸で、流れ軸はモードごとに別の画面軸だからである。**parcelに欠けがあれば必ず出る**：`line_box`を40px狭めて実際に落ちることを確かめた（687バイト差）。テストが通るのに見ると違う、という4回の失敗（7.7）に対する備えがこれである。

**残っている宿題は7.3のまま。**STAを窓のスレッドと同じ判断でワーカーにも与えている。描画も同じ道を通るだけで、新しく増えてはいない——D2DのファクトリもWICのファクトリもスレッドごとで、スレッドを越える相手がいない。

**実測は未取得**（画面での確認待ち）。証拠はperfログの`tiles_new=描いた枚数/分けた枚数`で、`tiles=`が速くなっただけでは分かれた証拠にならない（キャッシュがたまたま当たっても同じに見える）。分ける前の数字は`perf_log.prev.txt`にあり、**1枚で3.57ms、12枚で11.13ms**——固定費が3.5msほどあって、1枚増えるごとに0.7msという形をしている。

**ファイル読み込みは分けない**（2026-08-27、測ってから決めた）。要件2の残りとしてREADMEに載っていたが、**測ると分けるだけの待ちが無かった**。

| 読むもの | 所要 |
| --- | --- |
| 上限いっぱい（75万字・1.77MB）、1回目 | 8.04ms |
| 同、2回目以降 | 3.12〜3.61ms |
| `技術検証.md`（229KB） | 0.39〜0.50ms |

**1打鍵が2.5ms、タイル1枚が3.5msである。**上限いっぱいの文書でその1〜3枚ぶんに収まり、実際の文書では1打鍵の6分の1で終わる。しかも読むのは**開く操作1回につき1度**で、その直前にはたいていモーダルのダイアログが立っている。**非同期の道を1本増やす**——世代番号、答えを受ける窓のコールバック、タブ・文書・ペインという最も状態の多いところでの継続——のは、この待ちに対して高すぎる。7.5の全文検索が216msだったのとは桁が2つ違う。

**分けるとしたら起動時である。**セッション復元はタブの数だけ原本を読む（`restore_tabs`）。そこは非同期にする必要が無く、`MeasurePool`と同じ「まとめて配って待つ」形が使える。**ただしそれも、起動時間に占める割合を測ってからにする**——読むのが10ファイルでも数msであり、フォントとSlintの初期化のほうが大きい見込みが強い。

### 7.5 作業フォルダ全文検索をスレッドへ出した

要件7.7の全文検索は、**編集中の1文書ではなく作業フォルダの全ファイルを読む**。これは編集器が一度に多数のファイルへ触る唯一の場所で、要件2に照らせばUIスレッドから外すべきものである。**2026-08-25に外した**（`src/searcher.rs`）。

**マルチスレッド化の1本目にこれを選んだのは、DirectWriteもCOMも関わらないからである**——7.3が未解決のままでも進められる唯一の重い仕事で、`writer.rs`（作業コピーの書き込み）に次ぐ2本目のスレッドになる。

決めたこと。

- **答えはイベントループ経由でしか戻らない。**編集器が持っているものは全部`Rc`で、どれもスレッドへ渡せない。渡せるのは`slint::Weak<AppWindow>`だけで、**あのスレッドに許してあるのは窓のコールバックを1つ鳴らすことだけ**である（`folder-search-finished`）。何を取り出してどう描くかはこちら側に残っている。`searcher.rs`はSlintを知らない——起こし方を引数でもらう。
- **ポーリングしない。**25msのタイマーで拾う手もあったが、それは「人が待っている仕事」に25msを足すうえ、スレッドが死んだとき永久に回る。`writer.rs`が既存のタイマーで拾っているのは、**書き込みの結果は誰も待っていないから**である。この2つは同じ問題ではない。
- **追い越しは画面に出ない。**捨てた答えは、捨てられたことが画面のどこにも現れない——ペインは結局いちばん新しい一覧を出すだけである。だから診断ログに`asked`／`found`／`stale`の3行を出す。**小さいフォルダでは速すぎて追い越しを起こせない**ので、確かめる道はそこしかない。
- **世代番号で古い答えを捨てる。**打っている途中でもう一度Enterを押せる以上、答えは質問より遅れて着く。編集器側に取り消す手立ては無いので、**答えのほうがどの質問に対するものかを言う**。
- **走っている検索は、新しいものが来たら捨てる。**ファイルとファイルの間で訊く（`Superseded`）。**中断した検索は何も報告しない**——半端な一覧は、答えに見えるぶん、無いより悪い。
- **上限は残した。**UIスレッドに居たときは上限が唯一の防御だったが、いまも「人が読める量」の答えとして要る（要件7.7）。1万行の一覧は、どのスレッドが作っても待つのと変わらない。
- **スレッドが起動できなかったときは、その場で検索する。**`writer.rs`と同じで、`dispatch`が仕事を返してくる。同じデータに対する同じ関数で、違うのは誰が待つかだけである。

**そして測れた**（2026-08-25、`50_Dev`＝5,432フォルダ・38,774ファイルを歩き、読むのは`.md`／`.txt`の880件）。**初回216.60ms、2回目以降83〜86ms。**冷えているときと温まっているときで2.5倍という、7.5がこの規模の小さいフォルダで見たのと同じ形である。

この216msが判断を裏づけている。**UIスレッドに居たら、フォルダを開いた直後の1回目の検索は必ず目に見えて止まっていた**（1打鍵2.5msに対して86倍）。いまは止まらない。

**イベントループ1往復の費用は0.2ms未満**——`asked`と`found`のログの差が216.81msで、検索自身が216.60ms。起こし方をポーリングにしなかった判断は、ここでは費用の面でも正しかった。

**追い越しは手では起こせない。**86msは、人が次の語を打ってEnterを押すより速い。`stale`が出るのはもっと大きいフォルダか、もっと遅いディスクに限られる。中断と世代の判定は単体テストが持っている（`an_abandoned_search_reports_nothing`／`the_newest_search_is_the_one_that_runs`）ので、**これは画面で確かめるものではない**と決めた。

上限は外す前から置いてある——読むファイル5,000、1ファイルから報告する行50、全体で500。**上限は速さのためではなく、答えなくなる状態を作らないためのもの**である。作業フォルダに誰かがリポジトリを落としても、結果が不親切になるだけで、窓は返ってくる。

実測（開発中の作業フォルダ、mdが数ファイル）：

| 検索 | 結果 | 所要 |
| --- | --- | --- |
| 1回目 | 5件 / 1ファイル | 31.93ms |
| 2回目 | 14件 / 2ファイル | 17.80ms |

**1回目のほうが遅く、件数は少ない。**差はファイルシステムのキャッシュで、読む量そのものではない。つまりこの規模では**費用は一致の数ではなく、触るファイルの数と、それが温まっているか**で決まる。数百ファイルの作業フォルダで初回が何ミリ秒になるかは測っていない——**そこが、スレッドへ出す判断を下す場所**である。1打鍵の2.5ms（6.9）に対して30msは、押した瞬間に1回だけなら気づかれないが、フォルダが10倍になっても同じとは言えない。

`find::hits_in`（1ファイル内の全一致）と`file_tree::files_under`（歩く）は純粋だったので、**スレッドへ出すときに書き換わったのは呼び出し側だけだった**——予想どおりで、純粋な部分を分けておくことの見返りがここに出た。`searcher.rs`が新しく持っているのは、待ち行列と、中断の判定と、世代番号だけである。

### 7.6 完了した項目

| 項目 | どこに書いたか |
| --- | --- |
| 横書きの向きでのブロック配置 | 3.4、5.1 |
| 横書きでのDirectWrite計測と描画 | 3.6、5.3 |
| 横書きペインをSlint `TextEdit`から自前描画へ置き換え | 3.7、6.6 |
| 横書きペインの幅変更（ウィンドウ・Split境界）に待ちを入れる | 3.7 |
| 任意の文字送りと列間 | 3.8、4.7 |
| フォントサイズ・見出しサイズが行ごとに異なる場合 | 3.8、4.8 |
| 改行を含まない長大な段落の分割 | 3.9、4.10、6.10 |
| 組版・倍率を変える操作に待ちを入れる | 6.8 |
| CRLF改行が改行2つになること | 6.7 |
| DirectWriteのレイアウト上限（262144px）に触れないこと | 3.9、7.4 |
| 横書き単独表示のフレームコスト | 6.6 |
| 文書全体の走査を差分にすること | 6.12 |
| 性能全般（分割・タイル・組版・走査） | 6.3〜6.12 |
| 段落先頭の字下げ（実装しない判断） | 3.11、4.11 |
| 横書きのライブプレビュー（3モード目） | 3.12 |
| 候補ウィンドウが編集行に重なること | 6.13 |
| クリック後1文字目の変換位置がずれること | 6.13 |
| Ctrl+Aによる全選択、Shift+クリックによる選択 | 6.13 |

CRLFについて補足する。`normalize_typed_input`が`\r\n`と単独の`\r`をどちらも`\n`へ畳んでおり、**文書へ文字が入る経路は入力とIME確定の2つだけで、どちらもここを通る。**したがって現状のアプリで`\r`を含む文書は作れない。`document.rs`が`str::lines()`を使っていることは残っているが、これが問題になるのは**ファイル読み込みを足したとき**であり、そのときは読み込み側で正規化すればよい。

## 8. 現時点の判断

- Slintはアプリ全体のUI、横書き・縦書きの表示切り替え、ソース編集、タブ、Split、設定画面に利用できる。
- Slint標準のテキスト部品だけで、本要件の縦書きライブ編集を完成させることはできない。
- DirectWriteは縦書きレイアウト、字形選択、実描画、ヒットテストに利用できる。
- DirectWriteの標準組版で、全角英数字の正立と半角英数字ランの90度回転を実現できる。
- DirectWriteのヒットテスト、縦書きキャレット、基本キー編集を同じMarkdown原文へ接続できる。
- 本番の縦書き編集面は、DirectWriteとSlint `TextInput`によるWindows IME入力ブリッジを組み合わせた独自コンポーネントとする。
- 文書を論理行境界でブロックへ分割し、表示領域と交差するブロックだけを扱う構造で、全画面・7万文字級の文書でも通常操作は10ms未満に収まる。この構造は本体実装でも維持する。
- タイルはブロックの分割単位とし、その識別子に座標を含めない。ブロックの位置と大きさは整数画素とし、大きさは自分の計測値だけから決める。この3つは一体であり、どれか1つを崩すと本文の変わらないブロックのタイルが無効になる。
- ブロックの大きさは行が占める空間で数え、境界は行の内容そのもので決める。文書先頭からの累積で決める量を分割規則に持ち込んではならない。
- 行の送り量は`DWRITE_LINE_METRICS.height`であり、ブロックの大きさはその合計である。単一のピッチを推定してはならない。回転した半角英数字ランを含む行は実際に広い。
- ウィンドウサイズの変更に追従でき、全画面でも1枚あたりの描画量は一定に保たれる。
- 残るコストは tiles / stats / layout / preview の4つにほぼ均等に散っている。入力側の次の一手は`stats`と`preview`の差分更新である。
- **描画そのものは問題ではない。**縦書きペインのフレームは0.29msで、文書長に依存しない。タイル画像のGPUへの転送も測定にかからない。ラスタライズをGPUへ移す動機は、少なくとも現在の構成には無い。GPU化が意味を持つとすれば、それはDirectWriteをレイアウト専用にしてグリフをアトラスへ焼く方式に踏み込むときであり、ビットマップをGPUで作る話ではない。
- **横書きペインをSlintのTextEditから自前描画へ移した結果、Splitの最速フレームは89.98msから0.33msになった（6.6）。**縦書き単独と区別がつかない。**ラスタライズを自前で持つことは負担ではなく、最も重い箇所を置き換える手段だった。**この判断は実測で裏付けられた。
- **改行を含まない長大な段落は、DirectWriteの折り返し位置で切れる（3.9、4.10、6.10）。**折り返し位置から組版し直しても同じ行が出る——禁則処理があるので自明ではなかったが、DirectWriteの行分割は貪欲で、ある位置から先の組版はそこから先の文字だけで決まる。**この性質がこの設計全体の前提になっている。**
- 切ったあとに残るのは、**切り口を探すコスト**である（6.10）。折り返し位置は組版の産物なので、探すには組版が要る。段落ごとに憶えること、編集より前は前回の結果を使うこと、の2つでほとんど消える。**素朴に実装すると全長大段落を毎打鍵組版することになり、切る前より遅くなる。**
- **段落の先頭付近での入力だけは軽くならない（6.10）。**5行目の折り返しは1〜4行目を組まないと分からない。貪欲な行分割の性質であって、実装の余地ではない。32,000字の段落で114ms残る——**これは残っている課題ではなく、この方式が持つ性質である。**減らすなら組版そのものを軽くする側で、`IDWriteTextAnalyzer`で行分割だけを取れないか、あるいは段落の途中に「ここから先は独立に組んでよい」と宣言できる位置を持てないか、という別の設計になる。
- **組版のコストは、測り直した文字数に比例する（6.9）。**縦書きで1文字3.26µs、横書きで0.96µs。200字から7万字まで超線形な項は無い。**そして比例するのは文字数であってブロック数ではない。**40ブロック合計7万字の再計測と、1ブロック3万2千字の再計測は、ちょうど文字数の比で並ぶ。
- **許容できる段落長は、いま使っているブロック予算そのものである（6.9）。**Split合計が1フレームに収まるのは1,000字までで、`BLOCK_MAX_CELLS`の768セルはほぼそこを指している。分割規則が狙う大きさは最初から正しく、欠けているのは論理行の内側でそれを適用する手段だけである。
- **長大な段落を分割しても、1打鍵がその段落全体を測り直すのなら1msも速くならない（6.9）。**コストが文字数に従う以上、得になるのは打鍵が分割後の一部だけを無効にできるときに限られる。**予想したとおりの非対称が実測で出た（6.10）**——段落末尾での入力は1,000字の段落で10ms、先頭付近では17.6ms、32,000字の段落の先頭付近では114msである。
- **原因の推定は、ボトルネックの推定と同じだけ外れる（6.10）。**`measured=1`を「編集は末尾寄りのはず」と読んで3周した。その段落はブロックが2つしかなく、その値は位置について何も言っていなかった。診断値（`wrap`と`miss`）を足したら1行で片が付いた。**推測で手を入れる前に、区別できる値を出すこと。**
- **行ごとに組版が変わっても、ブロック分割の構造は崩れない（3.8）。**字送り・列間・見出しサイズを3つとも既定から外し、見出しを混ぜた文書で、分割したブロックの合計が単一レイアウトと一致することを縦横どちらでも確認した。**ブロックが自分の行メトリクスだけから大きさを決めるという設計が、そのまま行ごとのサイズを許している。**行の送りが`DWRITE_LINE_METRICS.height`の和であるという4.2の結論は、行の高さが揃っていることに依存していなかった。
- 行ごとの属性は**行だけで決まる形**で運ぶ。見出しレベルは論理行ごとの配列として本文の横に並べ、ブロックはその連続部分列だけを見る。DirectWriteへ渡す範囲もブロックローカルにする。3.4の「文書先頭からの累積を分割規則に持ち込まない」は、幾何だけでなく属性にも同じように効く。
- 列間は`PROPORTIONAL`で指定する。`UNIFORM`は全行の送りを1つの数値に潰し、4.2で否定した「単一のピッチ」を裏口から持ち込む。**しかもそれは計算を合わせてしまうので、テストには出ない。**
- ブロックの流れ方向の上限だけは、見積もりを外すと壊れる（4.8）。他の見積もりは分割位置を決めるだけで、外れても遅くなるだけである。**この1つだけが、下回ると行が消える。**本文サイズ固定で見積もっていた従来の式は、見出しが本文の2.2倍を超えた時点で足りなくなっていた。
- **どちらの本文を組むかを決める場所は1つでなければならない（6.15）。**「下流は`PaneText`に対して書かれていて、どちらを渡されたかを訊かない」（3.12）は、`PaneText`を受け取る経路についてだけ正しかった。位置を引く4経路は原文を直に書いており、整形後表示では下へ行くほどキャレットがずれた。**抽象が正しいことと、全ての経路がその抽象を通っていることは別である。**
- **3モードすべてが同じengineの上で動いている（3.12）。**違いは、engineへ渡す文字列と書字方向、そして位置の対応表が恒等写像かどうかだけである。3モード目を足すのに要ったのは、その対応を`PaneText`という型にして、横書きペインにどちらを渡すかを選べるようにすることだけだった。
- **2ペイン構造そのものが、engineとは別の不具合の源になる（6.7）。**画面確認で見つかった4件のうち3件がそれで、いずれも自動テストは全て通ったままだった。本体実装では次の3つを最初から前提にする。**ペインが保持する位置は、他ペインの編集後に必ず文字境界へ丸める**（表示だけの経路は丸め、文書を変更する経路は厳格に）。**ペインは可視になった時点で生成されるので、生まれたときの寸法は変化イベントでは取れない。****入力の握り状態は自前のフラグではなくツールキットの状態を使う。**
- 組版の指定は文書全体にかかるので、変えれば全ブロックの計測とすべてのタイルが無効になる（6.8）。**これはズームとまったく同じ性質で、コストも同じ200msである。**細かくする余地は無いので、150msの待ちで連打を1回にまとめた。倍率も同じ経路に入れてある。
- **文書全体を走る処理は、行単位に分ければ差分にできる（6.12）。**統計もプレビューも見出しレベルも、論理行ごとに独立して決まる——ブロック分割が拠って立つ性質と同じものである。索引表は**行内相対**で持つこと。絶対で持つと前の行が伸びただけで全要素が動く。
- **速い版を入れるときは、素朴な版を消さずに残して突き合わせる（6.12）。**`DocumentStats::from_source`と`visible_markdown_text_with_active_line`は`#[cfg(test)]`で残してあり、呼ぶのは検査だけである。**そして検査は内部表現に依存しない形で書くこと。**表現を変えるたびに正解の定義を書き直すようでは、テストが実装を追認するだけになる。
- **製品化は100万字で進める。**文章Editorとして文庫本10冊ぶんにあたり、対象とする原稿には十分である。100MB級を扱うのは上限の数字ではなく構造の判断で、engineへ渡す本文を作り直さない形——engineが文書の一部だけを見る形——への置き換えを要する。9.1に分けて記録した。
- **IMEには、変換中の文字列がどちら向きかを伝えられる（6.13）。**`ImmSetCompositionFontW`に`@`書体と`lfEscapement = 2700`を渡すと、候補リストが縦になる。**縦のリストは幅が狭く下へ伸びるので、置き場所の問題そのものが消える。**位置を計算し直しても解けなかったのは、リストの形が違っていたからである。
- **同じ層の中で解こうとしている限り、答えが無いことがある（6.13）。**候補ウィンドウの位置を座標の計算として3回考えて3回とも外し、一段下——IMEへ何を伝えるか——に手を伸ばして初めて解けた。
- **行だけでは決まらない属性も、行ごとの枠に収まる（3.11）。**段落の先頭かどうかは前の行を見なければ決まらないが、文書全体で一度決めてしまえば、そのあとは行ごとの事実である。ブロックが自分の外に依存しないという性質は、**属性をどう決めるか**ではなく**ブロックがどう読むか**で保たれている。
- 診断ログは1回の実行ぶんだけを保持する。前回のぶんは`perf_log.prev.txt`へ退避する。**消してからでないと読めないログは、症状と原因の間に手順を1つ増やす。**行の種類は`session`・`refresh`・`horizontal`・`relayout`・`resize`の5つで、ビルドがdebugかreleaseかは`session`行にしか出ない——数値からは復元できず、しかも読み方が最も変わる値である。
- **ペインが可視になったときにすることを、`changed`だけに任せてはならない（6.11、6.14）。**寸法で1度、フォーカスでもう1度、同じ形で出た。**`init`と`changed`の両方で同じ判定をする**のが正しい形である。生まれたときの値は変化として観測できず、生まれる前に起きた要求は受け取れない。
- **画面で見つかる不具合は、直した場所の隣で再び起きる（6.11）。**「ペインは可視になった時点で生成されるので、生まれたときの寸法は変化イベントでは取れない」は6.7で結論し、8章にも書いた。それでも同じことが、もう一方のペインの、もう一方の軸で起きた。**結論を書くことと、それが当てはまる全ての場所に当てることは別である。**
- 性能ログは画面を見るより速く原因に届いた。下半分が描かれない不具合は、ブロック平均サイズとタイル枚数の割り算だけで可視高さの誤りへ辿り着いている。**内訳を数値で残しておくことは、性能のためだけではない。**
- 計測は決まった文書に対して行う（`testdata/09_段落長の計測.md`）。手近なMarkdownを貼り付けていたときは2つの実行を並べられず、長大な文書はそもそも用意しづらかった。
  **はじめは`samples.rs`が実行時に生成し、Typographyパネルの`Sample`ボタンが開いている文書に上書きしていた**が、
  2026-08-22に取り除いた——検証用の道具が本文を書き潰すのは事故と見分けがつかず、そもそも**文書はファイルであればよい**。
  ファイルなら他のテスト用ファイルと同じように開けて、内容が実行ごとに揺れる余地も無い。
  **その実害が同じ日に出た**：`Test1.md`（12バイト）の作業コピーが7万字の計測用文書になっていて、
  起動時に「タブ名は`Test1.md`、中身は計測用文書」で復元された。どの部品も仕様通りに動いた結果である
  ——ボタンが本文を置き換え、自動退避（要件8.1）がそれを未保存の作業として保存し、起動時に正しく戻した。
- 分割・配置・タイルの幾何は書字方向に依存しない形（流れ軸と行軸）で`text_blocks.rs`へ隔離した。3モード（横書きソース編集・横書きライブプレビュー・縦書きライブプレビュー）は、同じengineへ渡す文字列と方向の違いにすぎない。ソース編集ではMarkdown原文をそのまま渡し、位置の対応表が恒等写像になる。
- ~~次の技術検証では、横書きペインの自前描画と、任意の文字送り・行間・見出しサイズを確認する。~~ **両方とも完了した（3.7、3.8）。**次に残る最大の項目は、6.6の改行を含まない長大な段落である。

## 9. 将来的な課題

この検証の範囲外だが、構造の前提に関わるため記録する。**製品化は100万字（`MAX_DOCUMENT_CHARACTERS`）で進める。**

### 9.1 100MB級のファイル

100万字＝約3MBは、文章Editorとしては文庫本10冊ぶんにあたる。一方、汎用のテキストエディタは100MB級のファイルも開く。**どちらを名乗るかで設計が変わる**ので、上限の数字ではなく構造の判断として記録する。

文書は単一の`String`で、1打鍵ごとに全文が2回複製され1回動かされる。**このコストはどのログにも出ていなかった**——`refresh`行が測り始めるのは新しい本文が手に入ったあとだからである。上限を論じる前にそこを測る必要があったので、`edit`行を足した。

**測ったら、複製は問題ではなかった。**200KBの文書で`clone`が0.03ms、`store`が0.00msである。memcpyは秒あたり数GBなので当然といえば当然で、**「1打鍵で8〜12回コピーしている」という回数は正しかったが、それが上限を決めているという読みは誤りだった。**測る前に書いた見立てである。

効くのは走査の側で、しかも**6.12で差分化したあとの残り**である。プレビュー本文の連結と行の突き合わせは、engineが`&str`を取る以上どうしてもO(n)で残る。

| 100MB（約3,300万字）での1打鍵 | |
| --- | ---: |
| 全文複製 × 2 | 約30ms |
| **文書全体に比例する走査**（0.0062µs/字） | **約200ms** |
| `PreviewDocument`の索引表（メモリ） | 約400MB |

つまり100MBは「上限を上げる」では届かない。**engineへ渡す本文を作り直さずに済ませる**——engineが文書の一部だけを見る形にする——のが要点で、索引の遅延化とあわせて全モジュールに及ぶ。ロープやpiece tableは、そのとき同時に必要になる。

**いまはこの道を採らない。**現在の構造は「文書全体を1つの文字列として持ち、engineへ丸ごと渡す」ことを前提に組み上がっており、ブロック分割もタイルもその上に乗っている。100MBを扱うとは、その前提を置き換えるということである。対象とする原稿に対して100万字は十分であり、超える文書はそもそもこの製品の対象ではない（7.4のとおり、超えたぶんは受け取らずに断る）。

### 9.2 性能について残る選択肢

いずれも**現状の律速ではない**ので採らなかった。必要になったときの手がかりとして残す。

- **同じ行が連続する区間での境界の連鎖（4.5）。**内容による境界候補が現れないため、最大サイズだけで切られる。Enterを押し続けた直後の再計測が10ブロック程度になる。
- **ブロック1つ分ではなく、変更のあった行だけを再描画する方式。**描画コストの大半は`CreateWicBitmapRenderTarget`によるCPU描画なので、描く面積を減らすのが有効な手段である。ただし6.4のとおり、描画は現状の律速ではない。
- **段落の先頭付近での入力（6.10、114ms）。**貪欲な行分割の性質なので、この方式のままでは減らせない。`IDWriteTextAnalyzer`で行分割だけを取れないか、あるいは段落の途中に「ここから先は独立に組んでよい」と宣言できる位置を持てないか。

### 9.3 DLLに分けるか——分けない。分けるならクレートで

**問い（2026-08-22、ユーザー）**：いまは1つの実行ファイルに全部入っているが、DLLに分けたほうがよいか。
続けて、**将来Terminalをこのエディタの中へ入れたい。その場合Terminalの部分はDLLにすべきか。**

**答え：どちらもDLLにはしない。**理由は「Rustだから」だけではないので、根拠を残す。

#### DLLが向かない理由

1. **RustにはABIの安定が無い。**DLL境界を越えられるのは`extern "C"`だけで、`Rc<OpenDocument>`・
   `&str`・`Vec<Hit>`・`PaneId`はそのままでは渡せない。境界を作るとは、これらをCの形へ翻訳する層を
   書くということで、**型検査が助けてくれない層が1枚増える**。（`dylib`形式は両側のコンパイラの
   バージョンとフラグの完全一致を要求するので、配布には使えない。）
2. **この設計の中心が共有グラフである。**1つの文書を2つのペインが`Rc`で共有し、Undo履歴は文書が持ち
   （要件7.6）、`RenderCache`のCOMオブジェクトはスレッドに貼り付いている（`PaneGraphics`）。
   割れるのは呼び出しの形だけで、依存の形は割れない。
3. **分けたくなる動機が現状どれも無い。**ビルドは`cargo check`が5秒、テストのコンパイルが10秒
   （テスト実行の約50秒はDLLでは縮まない）。配布は1つのexeのほうが単純。プラグインは要件に無い。

#### 代わりに効くのはワークスペース（クレート分割）

`main.rs`は8,329行あり、そこは手を入れる価値がある。ただし答えはDLLではなく**クレート**である。
静的リンクなのでABIの問題が無く、Rustの型がそのまま通る。すでに**Windowsに触らない純粋な層**が
できている——`text_blocks.rs`（2,291行）・`document.rs`（1,675行）・`find.rs`・`pane_layout.rs`・
`file_tree.rs`・`app_data.rs`。これを別クレートにすると：

- **「純粋な部分にWindows APIを持ち込まない」がコンパイラに強制される**（いまは規約でしかない）
- **そのクレートはWindows以外でもテストできる**——現状は全部Windowsでしか回せない。ここが一番大きい
- 触っていないクレートは再コンパイルされない

**ただし急がない。**要件2（マルチスレッド化）は境界を動かすので、**その前に切ると切り直しになる**。
順番は「要件2 → クレート分割」。

#### Terminalを内包する場合

**DLLは、守ってほしいものを守らない。**Windowsのターミナルは ConPTY（`CreatePseudoConsole`）を使い、
**シェル本体は別プロセスで動く**。落ちうるもの・止まりうるもの・信用できない出力を出すものは最初から
向こう側にいる。こちら側に残るのはVT/ANSIの解析と画面グリッドで、これは純粋なRustのコードである。
DLLに入れてもパニックすればプロセスは落ちる（そもそもRustのアンワインドはDLL境界を越えられない）。

**Terminalペインは、Editorの内側を共有しないと成立しない。**分割ペイン（要件6.4）の中に置くとは
「もう1種類のペイン」を作るということで、同じペイン／タブのモデル（`PaneId`・`place_panes`・タブ列・
`⋮`・フォーカスの規則）、同じ描画（DirectWrite＋Direct2D＋タイル）、同じ表示設定（要件9はアプリ全体で
共通と決めている。等幅フォントの欄はすでに要件にある）を使う。**1つの窓に描画スタックを2つ持つと、
字送りも行間も倍率も二重管理になる。**

切るとよい線は：

| | |
| --- | --- |
| `terminal-core` | VT/ANSIの解析、画面グリッド、スクロールバック、選択。**Windows APIに触らない** |
| `terminal-win` | ConPTY、パイプ、読み取りスレッド。薄い |
| 描画 | Editorのエンジンをそのまま使う |

`terminal-core`は`text_blocks.rs`や`document.rs`と同じ種類のコードで、**テストが効く**。
VTの解析は仕様が細かいので、なおさらである。

**前提が1つある：要件2のマルチスレッド化。**PTYの出力は勝手に流れてくるので、UIスレッドで読むわけには
いかない。逆に言えば**Terminalは要件2をやらざるを得なくする機能**で、スレッドの形（どこで受けて、
どうUIへ渡すか）を決めてから着手しないと両方作り直しになる。

#### DLLが正解になる条件

3つだけである。**(1)** Terminalを C/C++ の既存ライブラリで作る、**(2)** Editor本体と別に配布・更新
したい、**(3)** 第三者が差し替えられるようにしたい。どれも当てはまらないならクレートで足りる。

**なお、Editorの中にTerminalを置くことは要件定義に入っていない。**作る前に「誰の何を助ける機能か」を
書くこと——§14へ移したクイックオープンの教訓（機能の名前だけの項目には着手しない）がそのまま当てはまる。

## 10. 参照した公式資料

- [Slint TextEdit](https://docs.slint.dev/latest/docs/slint/reference/std-widgets/views/textedit/)
- [Slint Window raw window handle](https://docs.slint.dev/latest/docs/rust/slint/struct.Window)
- [Microsoft DirectWrite 縦書きテキスト](https://learn.microsoft.com/ja-jp/windows/win32/directwrite/vertical-text)
- [Microsoft DirectWrite PadWrite TextEditor sample](https://github.com/microsoft/Windows-classic-samples/blob/main/Samples/Win7Samples/multimedia/DirectWrite/PadWrite/TextEditor.cpp)

### 7.7 表をどう組むか（升目に組み、セルの中で折り返す）

要件7.3.2の最後の1つ。**7.3.2でここだけ幾何が新しくなる。**

**確かめたこと：DirectWriteに列ごとのタブ位置は無い**（2026-08-25、`windows-0.62.2`の
`DirectWrite/mod.rs`を読んだ）。あるのは`SetIncrementalTabStop`だけで、**書式全体に1つの
等間隔タブ**である。列幅が列ごとに違う表は、これでは揃わない。

**残る道は1つ：`|`のところに幅を持ったインラインオブジェクトを置く。**箱はもともと
2つのことをする——**字形を隠すことと、幅を取ること**（`keeps_room`）。表はその両方を、
セルごとに違う幅で要求する。既にある仕組みの、まだ使っていない側である。

順に決めること。

1. **`Ornament`が幅を持つ。**いまは`apply_marker_boxes`が箱を2つだけ作って使い回している
   （幅0と1段）。表は`|`ごとに違う幅の箱が要るので、**runごとに作る**形になる。
   ここが今回いちばん広く触るところ。
2. **列幅は測ってから決まる。**各セルを組んで測り、列の最大を取る。**セルを測る指定は
   セルを描く指定と同じでなければならない**——`WrapPoints::line_starts`が`LongLine`を
   受け取る形になったのと同じ理由で、違えば列が揃わない。
3. **表は1ブロック。**列は表全体で決まるので、途中で切れてはいけない。フェンスに
   `may_end`で入れたのと同じ規則を表にも与える（7.1）。
4. **区切り行（`|---|---|`）は行まるごとの箱**＝`Ornament::Hidden`。既にある。
   行の残す余白がそのまま見出しの下の間になる（フェンスの行と同じ）。
5. **揃え（`:---`／`---:`／`:---:`）はセルの左右の箱の幅**で表す。
6. **見出しの下の罫線は`LineOrnament`**。引用の縦棒・水平線・コードの地と同じ、
   行そのものの矩形に描く仕組みがもうある。
7. **タイルの署名に列幅を入れる。**幾何を動かさずに絵を変えるものは署名に要る、を
   3回踏んでいる（6.18、6.22b、`line_runs`）。列幅は幾何を動かすが、**署名に無ければ
   幅の違う表が同じタイルを共有する**。
8. **縦書きペインでも同じ算術で組む。**§14が対象外にしているのは「縦書きライブプレビュー内で、
   表とコードブロックを**横書きブロックとして**表示すること」——縦書きの中で表だけ横に組む、
   という一点で、**縦書きの表そのものは対象外ではない**（2026-08-26に読み直して判明。
   要件7.3.2のリンクのときと同じ、一覧だけを見た誤り）。一度は測って取り下げたが、
   **規則が割れて戻した**——下の「縦書きで揃わなかった」と4.14を見ること。

**未決だった問いは決まった（2026-08-25、ユーザーの判断）：按分して縮める。**入るなら自然幅、
入らないときだけ列幅を按分して縮め、**列に入らないセルの末尾は箱で隠す**。セルの中では折り返さない
——折り返せば行の高さが列ごとに違い、**行そのものの矩形という前提**（7.1の引用の縦棒以来ずっと
使っている）が初めて崩れるので、そこは初期版では触らない。

**「切る」が箱でできるのは、プレビューが原文から削ることしかできない（4.12）ことの裏返し。**
省略記号は置けない——ページに無い語は置けない——が、**隠すのは削ることなので箱でできる**。
幅を持たない箱（`keeps_room`が偽のほう）が既にその形をしている。

#### 何が入ったか（2026-08-25、342テスト通過・fmt差分なし・warningなし）

**段階1：どの行が表か。**`LineKind::TableRow`と`TableRule`が入った。**表はフェンスに次いで、
行が自分だけでは決められない2つ目のもの**——バーの行が表になるのは、その次の行が区切り行のときだけで、
そのあとの行が行なのは区切り行がそう言ったから。`line_style`が1行だけ先を読む形になり、
`TablePlace`がその状態を持つ。**表は1ブロック**（`split_blocks`の`may_end`）——フェンスと同じ規則だが
理由は強い。列幅は表全体で決まるので、切ったら両半分が別々に測られ、**継ぎ目ではなく全部の行がずれる**。

**段階2：列を揃える。**`Ornament::Bar(BoxWidth)`——**幅を持つ唯一の箱**で、**runごとに作る唯一の箱**でもある。
`-`も`10.`も同じ段から始まるので1つの箱を使い回せるが、表のセルはどれも違う幅を要求する。

- **箱はバーとその周りの余白をまとめて覆う**（前のセルの後ろの空白＋`|`＋次のセルの前の空白）。
  だから**書き手が打った余白がまちまちでも、本文は列の頭ちょうどから始まる**。
- **箱の幅は「前の列の残り＋溝」**。`列幅[c-1] - この行のセル幅[c-1] + 溝`。
  **前の列だけで決まり、行のどこにあるかを知らない。**走る位置を積み上げないので、
  1つ間違えてもそこから先が全部ずれることが無い。
- **セルは描くときと同じ指定で測る**（`cell_along`が`apply_typography`を通す）。
  太字のセルを素で測れば、その行だけ次の列が右へずれる。`WrapPoints::line_starts`が
  `LongLine`を受け取る形になったのと同じ理由。
- **幅は`StyleRun`に入って運ばれる。**`Ornament`の中に`BoxWidth`（f32のビット）として持つので、
  **署名（`layout_key`）に自動的に入る**。別に持てば「幾何を動かすのに署名に無い」を4回目にする。
- **計測は`update`の一番前、1回だけ。**「測るものを決めるのにグラフィックスは要らない」という
  あの塊の性質は残してある——**表だけがその例外**で、列幅はDirectWriteにしか分からない。
  表を持たない文書はグラフィックスに一切触らない。

**段階3：見出しの下の罫線。**`Ornament::TableRule(BoxWidth)`——**区切り行の箱に表の幅を持たせ、
その箱の中に線を引く**。**`LineOrnament`では引けなかった**：あれが届くのはページ全体かブロック全体で、
**表の幅は表のセルにしか分からない**。`LineInfo`は行の高さしか持っていないので、描画時に幅を知る道は
「幅を持った箱をヒットテストする」以外に無い。

- **これが箱の3つ目の仕事。**字形を隠すことと幅を取ることは前からやっていて、**その幅の中に何かを
  描くのは初めて**。`draw_marker_ink`が箱の矩形を既にヒットテストしているので、
  文字を描く代わりに矩形を塗るだけで済んだ。
- **区切り行の箱を`line_marker`から外した。**幅は計測にしか分からないので、**箱を作る場所を1つにした**
  ——同じ行について2箇所が意見を持てば、その行は二度描かれる。`document.rs`はもう表の箱を1つも作らない。
- **線の太さは`rule_stroke`が1つで答える。**`---`の線と見出しの下の線は同じ印で、違うのは届く先だけ。
  重さに第二の意見があると、1ページに2種類の罫線が出る。
- **1回目は線が出なかった**（画面で出た。列は揃っているのに罫線だけが無い）。
  **runの一覧を作る場所が3つあった**——レイアウトを組むところ、箱の中に描くところ、タイルの署名を取るところ。
  表の箱を足したのはレイアウトのところだけで、**描くほうは`style_runs`しか見ていなかった**。
  箱は効くので列は揃い、区切り行の文字も消え、**行の高さぶんの空きだけが残る**——
  「箱は効いているが中に描くほうが動いていない」という、見ればすぐ分かるが
  テストからは見えない形になっていた。`TextEngine::block_runs`に1本化した。
- **テストは画素で取った**（`the_rule_under_a_header_reaches_the_pixels`）。
  `table_runs`の出力を見るテストはこのバグを通してしまう——**間違っていたのは出力ではなく、
  その出力を誰が受け取るか**だったので。1行の中で途切れずに続く墨の長さを見ており、
  直す前は17px（字の画）、直すと表の幅ぶん出る。**ページ幅の罫線でも落ちる**ように上限も置いた。
- **タイルの署名に列幅は要らない**（7.7の設計の項目7への答え）。列幅はブロック本文・表示設定・
  書字方向の関数で、その3つは既に署名に入っている。**署名に入れるとタイル1枚1フレームごとに
  DirectWriteを呼んで、既に分かっていることを測り直すことになる。**

**段階4：揃え。**`:---`／`---:`／`:---:`は**セルが列に対して持つ余りの、行き先を決めるだけ**。
余り（列幅−セル幅）を`cell_offset`が按分し、**その前の箱が運ぶ**——本文自身は自分の列の幅を知らないので、
運べるのは箱しかない。箱の幅は「前の列の残り − 前のセルの取り分 ＋ 溝 ＋ このセルの取り分」で、
**依然として前の列とこのセルだけで決まる**。

**段階5：按分して縮める＋入らないセルを切る。**表の届く先（`table_reach`）がブロックの箱を超えたら、
**溝と余白はそのままに列だけを比例で縮める**。溝と余白は「表が表であること」そのものなので、譲るのは列である。

- **フォントを縮める道は採らなかった。**幅の広い表だけが、書き手の指定した文字サイズが通らない
  唯一の場所になる。
- **セルの中で折り返す道も採らなかった**（ユーザーの判断）。行の高さがその行自身で決まらなくなり、
  7.1以来の全部の行の印（引用の縦棒・水平線・コードの地・入れ子の字下げ）がその上に乗っている。
- **切るのは箱でできる。**`Ornament::Cut`＝隠して幅を取らない箱。**プレビューは削ることしかできない**
  （4.12）ので省略記号は置けないが、**隠すことは削ることなのでできる**。
- **どこまで入るかはDirectWriteに訊く**（`cell_fits`）。列の幅の箱に組んで1行目の長さを読む。
  切れ目がどこに落ちるかは組版の性質で、字数で割って決めるものではない（欧文の語は途中で切れない）。
- **`table_reach`は1つの関数で、2回読まれる**——縮めるかを決めるときと、罫線の幅を言うとき。
  第二の意見があると、縮めた幅と罫線の幅が食い違う。

#### 段階6：升目にした（2026-08-26、361テスト通過・画面で確認済み）

**ユーザーの一言で決まった：「表になっていないですね」。**見出しの下に1本だけ引く形（本の組版の
慣習）は、この編集器の書き手には**表に見えなかった**。参考として置かれたObsidianのスクリーンショットは
**升目**——外枠があり、行と行のあいだにも列と列のあいだにも罫線があり、見出しの行が太字である。

**やったこと。**

1. **列の罫線**（`LineOrnament::TableColumn`）。溝のまん中に、表の長さぶん。**外側の2本も引く**
   （位置0と`edge`）。位置は`column_heads`の同じ1回の走りから取る。
2. **行の罫線**（`LineOrnament::TableRow { reach, far }`）。**各行が自分の上の1本だけを引き、
   最後の行だけが下の1本も引く。**両方が引くと、2つの図形が1本の縁に重なって**その画素だけ2回
   合成される**——`LineOrnament`が「まとめて1本の run にする」理由と同じ継ぎ目。
3. **区切り行を潰した。**`Ornament::TableRule`は`TableSeam`に改名し、**もう何も描かない**
   （罫線は行の境として引かれるので、そこに第二の意見を置かない）。
4. **見出しの行を太字**にした。**描くときだけでなく測るときも太字**——素で測れば、見出しの決めた
   列幅に見出し自身が入らない。
5. `draw_table_rule`は消えた（箱の中に描く唯一の飾りだった。もう無い）。

**区切り行が潰れなかった（そして直った）。**箱の`across`を罫線の太さにしても、行はほぼ1行ぶんの
高さを保った。**箱が覆っているのは行の本文だけで、行末の改行は覆っていない**——そして
**改行の文字は書式のフォント寸法を持つ**ので、行の高さはそこから決まっていた。
直しは`apply_typography`で、**区切り行の範囲を改行1つぶん伸ばしてフォントサイズを罫線の太さにする**こと。
行送りは`PROPORTIONAL`（4.3）なので、これで行が罫線の太さまで縮む。
**テストは画面の距離で見る**——見出しの行と最初の本文行の間隔が、本文行どうしの間隔の1.5倍未満であること。

**カーソルがその行に来れば全部戻る**（要件7.3.1）。箱が出なくなり、フォントサイズの指定も
箱の run に付いているので一緒に消え、**区切り行は自分の高さに立ち上がって`| --- | --- |`を見せる**。
これが無ければ区切り行は編集できない。

**テストは5本**：溝の中に罫線が立つこと（前の列の末尾と次の列の頭を画面から測る）、
行ごとに1本＋最後に1本で二重にならないこと、区切り行が高さを持たないこと、見出しが太字であること、
そして**画素**——横書きペインでは行の罫線が横に走り、縦書きペインでは列の罫線が横に走るので、
どちらも「1行の中で長く続く墨」で捕まえられる。

**閾値は見出しの罫線の頃より緩い**（<245）。1画素より少し太い線が画素の中心のあいだに落ちると
2画素に分かれ、どちらも薄くなる——墨の量は同じで、広がっただけ。
**罫線と字の縁を分けるのは相変わらず長さのほう。**

#### 升目にしたら3つ出た（2026-08-26、ユーザーが画面で見つけた。363テスト通過）

**1つ目：縦書きで境が二重になり、外の1本が消えた。**「各行が自分の手前の縁に1本引く」を
`flow.0`で書いていたが、**縦書きは読み順が流れ軸の小さいほうへ進む**（`FlowOrder::Descending`）。
手前の縁は横書きなら`flow.0`、縦書きなら`flow.1`である。二重に見えていたのは
**区切り行の薄い列の両側**に線が来ていたためで、表の外の1本はどこにも引かれていなかった。
**レイアウトの答えは全部正しく、画素だけが違う**——だからテストも画素で取った
（`a_table_rules_its_rows_evenly_in_both_directions`は、2行の表の罫線が3本あって等間隔であることを
両方向で見る）。

**2つ目：見出しの行だけ幅が狭かった（縦書きで目立つ）。**`mark_extent`は、ブロックの端まで届く印を
**置かれたブロックの縁**へ丸める（`place_blocks`の丸め分の半画素が継ぎ目に出ないように）。
表の罫線はそれを要らない——**引いているのは行と行の境**で、ブロックの縁は表ではなくブロックの事実。
引数で切れるようにした。

**3つ目：セルの中で字が上に寄っていた。**行送りは既定で1.0なので、余っていたのは**フォント自身の
下側の空き**である（22ptで行の高さ37、字は上の20px）。本文では見えないが、升目に囲むと一目で分かる。

- **罫線を持ち上げる道は使えない。**半分を上へ回すと**表の1本目がブロックの外に出る**——
  タイルはブロックの範囲で切られるので、その線は描かれずに消える。実際に消えて、画素のテストが捕まえた。
- **正しい直しは、セルに本当の余白を持たせること。**行の高さは「その行に載っている最も高いもの」で決まり、
  **箱は基準線の上下にどれだけ届くかを言える**。だから**バーの箱が表の行の高さを決める**。
  `TABLE_CELL_HEIGHT = 2.0`（全角の箱1つ＋上下に半角ずつの余白）、`TABLE_CELL_ASCENT = 1.38`
  （`0.88`＝全角の箱が基準線の上に持つ分＋余白`0.5`）。
- **これはブロックの高さごと変わる**ので、タイルもキャレットもヒットテストも同じものを見る。
  罫線だけを動かす道との違いはそこにある。
- **縦書きは直す前から中央にあった**（縦の行はグリフを列の中央に置く）。同じ変更で列が広くなり、
  こちらも余白が増えるだけで済んだ。

#### 段階7：セルごとのレイアウトへ作り直した（2026-08-26、365テスト通過・画面で確認済み）

**折り返しのために作り直した。**「長い文のときの折り返しも対応しましょう」（ユーザー）。

**なぜ箱では届かないか。**`1ブロック＝1つの`IDWriteTextLayout`で、位置の問いは全部そのレイアウトへの
ヒットテストである。ところが折り返した行は、**セルAの2行目とセルBの2行目が横に並ぶ**必要がある。
1つのレイアウトの中の文字順はA→B→Cなので、Aの続きは必ずAの直後に来る。**行を並べ替えることは
できない。**箱で寄せる、折り返し位置を自前で決める、`MUST_BREAK`でセルごとに改行させる——
どれも同じ壁に当たる。**2026-08-25に「切る」を選んだのは、これが理由だった。**

**通る道は1つ：セルそれぞれに自分のレイアウトを持たせること。**

- **`TableGrid`＝表の升目**（`text_blocks.rs`）。セルごとに、そのセルの文字範囲・箱・揃え・
  何が強調されているかを持つ。**座標はブロック自身の空間**（`LineInfo`と同じ）で、
  COMもDirectWriteも入っていないので`BlockMeasure`に乗って計測スレッドを渡れる。
- **表は必ず1ブロック**になった（`split_blocks`）。「途中で切れない」だけでなく**同居もしない**
  ——表と段落を1つのブロックに入れると、2通りの組み方を同時にすることになる。
- **`measure_table`が計測そのもの**。各セルを自然幅で測って列幅を決め、入らなければ按分で縮め、
  **列幅の箱で組み直して高さを得る**（ここが折り返し）。行の高さはその行のいちばん高いセル＋上下の余白。
  ブロックの行表（`LineInfo`）も自分で作る。
- **`draw_grid`が描画**。罫線と、セルごとのレイアウト。
- **位置を訊く5箇所が`layout_at`／`grid_hit_test`を通る**：キャレット、クリック、選択、上下移動、
  そして描画。**インクとキャレットが同じレイアウトを見ていること**が全部で、
  `a_click_in_a_table_lands_where_the_caret_is`が両方向でそれを見ている。

**消えたもの。**`Ornament::Bar`、`Ornament::TableSeam`、`BoxWidth`、`cell_fits`、`cell_offset`、
`LineOrnament::TableRow`／`TableColumn`、箱で行の高さを作る仕掛け、区切り行のフォントサイズの細工、
`table_marks`。**揃えは`SetTextAlignment`**——セルが自分の箱を持つので、余りを前の箱に運ばせる必要が無い。
**表の側は作り直して短くなった。**増えたのは engine の側の「1ブロック＝1レイアウトでないものが1つある」
という概念1つである。

**縦書きで2つ踏んだ。**どちらも横書きでは起きない。

1. **行の並ぶ向き。**縦書きは読み順が流れ軸の**小さいほう**へ進む（`FlowOrder::Descending`）。
   行を素直に0から積むと、見出しが左端に来て罫線が行の隙間に立つ。総高を先に出して逆から置く。
2. **縦書きのレイアウトは1行目を箱の遠い側に置く。**セルを「何でも入る広さ」の箱で組んで描くと、
   **箱の幅ぶん向こうに描かれる**（画面には升目だけが出て中身が1文字も見えなかった）。
   描くときは**測って分かった高さちょうど**の箱で組む。横書きでは流れ軸が下向きなので起きない。

**タイルの署名に升目を入れた。**7.7の初期版では「列幅はブロック本文・表示設定・書字方向の関数だから
署名に要らない」と書いたが、**ペインの幅が抜けていた**——高さの変わらないリサイズで列だけが動くと、
古いタイルがそのまま出る。いまは升目が計測の答えとして手元にあるので、**測り直さずに読むだけ**で
署名に入れられる。6.22bと同じ罠の4回目を、ここで塞いだ。

**Obsidianに追いついた。**残っている違いは無い。

#### カーソルのある行が原文に戻らなかった（2026-08-26、ユーザーが画面で見つけた）

要件7.3.1の「キャレットのある行は原文のまま出す」が、**表にだけ効いていなかった**。

**原因は、表の行だけ「原文に戻る」ことが見えなかったから。**他の記法では戻り方が2通りある——
記号を**消している**もの（強調・リンク）はプレビューの本文がその行だけ原文になるので自然に戻り、
**箱で隠している**もの（箇条書きのマーカー）は`PreviewLine::marker`が活動行では`None`になる。
**表のバーは箱で隠しているが、その箱を作るのは組版側**（`table_marks`）で、そこに活動行を伝える道が
無かった。**表の行は原文とプレビューで1文字も違わない**（バーはどちらにもある）ので、
本文からは見分けられない。

**道を1本足した：`StyledText::source_line`。**「原文のまま出す論理行」で、ブロック単位に切るときは
他の行別データと同じように付け替える。`PreviewDocument::active_line()`が答え、`PaneText`が運ぶ
（ソースペインは`None`——あそこは全部が原文で、何も被せていないので名指す1行が無い）。

- **活動行の箱は出さないが、計測はする。**落とすと、その行がいちばん広い列の幅がその行と一緒に消え、
  **カーソルがその行へ入るたびに表全体が動く**。
- **区切り行が活動行なら、見出しの下の罫線も消える**（罫線はその行の箱の中に引いているので）。
  これは正しい——罫線はそう言っている行の代わりに立つもので、その行がいま紙の上にある。
- **列の罫線は残す。**あれは行の印ではなく表の印で、引用の縦棒が活動行でも残るのと同じ。
- **`TextEngine`の再利用の判定に`source_line`を入れた。**表の行から行へカーソルが動くとき、
  **文字もマークもマーカーも1つも変わらない**——それだけを比べていた判定は、持っていた箱を
  そのまま答える。テストで外して落ちることを確かめてある。

#### 縦書きで揃わなかった（2026-08-26、画面で出た）→ 解決（2026-08-26）

横書きは揃っているのに**縦書きだけ列が揃わない**、とユーザーのスクリーンショットで出た。
**縦横で見え方が違うときは、まずここを疑う**の3例目。原因は2つ重なっていた。

**1つ目：セルの幅を測る軸が違っていた。**`cell_along`が`GetMetrics().width`を返していたが、
**本文が走るのは横書きならx、縦書きならy**。縦書きでは全セルが「行の太さ」というほぼ一定の値で
測られ、列幅の補正が効かず、次の列が前のセルの実際の長さのぶんだけずれていく。
`mode.to_axes(width, height).1`で直した。**軸を答えるための道具が既にあるのに、そこだけ生で読んでいた。**

**2つ目：箱の送りだったが、規則は箱の中身にあった。**当時は「縦書きでは箱の送りが幅と一致しない
ことがある」までしか分からず、**縦書きの表を組まないことにして一度取り下げた**。
規則は**4.14で確定した**——`supportsSideways: TRUE`の箱が正立する字の上に載ると、
`width`ではなく`height`が行に沿う。表の箱でそれに当たるのは**セルの尾を切ったときの箱だけ**で、
だから「縮めた表だけ」ずれていた。

**直しは`supportsSideways: false`の1語。**箱は何も描かないので寝た姿を持っておらず、
そう申告すると`width`の意味が両方向で1つになる。これで**縦書きの表は横書きと同じ算術で組める**
——列幅も、箱の幅も、揃えの余りも、罫線の届く先も、全部「行の軸に沿った長さ」のままでよい。

**取り下げを取り下げるときは、取り下げた理由を潰したことを示すこと。**
`a_vertical_pane_sets_a_table_the_way_the_horizontal_one_does`が2つのペインを
**互いに比べる**——数字を書き写すのではなく、「紙を回した書き手が同じ表を見る」を見ている。
**切ったセルのある表**を使うのは、そこが唯一ずれていた場所だから。
行が1行のままであることも同じテストが見る（送りが長すぎれば最後のセルが2行目へ落ちる）。

**副産物だった「行の途中の幅0の箱が縦書きで1行ぶん取る」も同じ規則だった**（4.14）。
`Ornament::Cut`を廃止して切った尾を次のバーの箱に飲ませた判断は、**結果として今も正しい**
——箱は「隠す」と「幅を取る」の2つをするので、1つで足りる。

**行の頭に立つ箱は初めから無事だった**（2026-08-26に測って確認）。箇条書きの箱（`Bullet`・
`Number`・`Indent`）はどれも幅0だが、**縦書きでも項目の本文は折り返した続きの行と同じ位置から
始まる**。これも4.14で説明が付く——マーカーは`- `や`10. `で、どれも寝かせる字から始まる。

**要件7.3.2の表は、横書きでも縦書きでも揃っている。**
