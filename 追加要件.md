# 追加要件

この文書は、未着手の要件・追加候補と確認待ちを整理する場所。
確定した仕様は[要件定義.md](要件定義.md)、操作説明は[README.md](README.md)へ記載する。
完了したものは下の一覧にまとめ、作業経過や同じ項目の途中版を追記しない。
実装上の注意は[技術検証.md](技術検証.md)へ記載する。

## 現在地（2026-09-15整理）

- **設定画面の改善は完了**（2026-09-15、書き手の確認済み）。設定のTAB化、Keysの全量表示と折り畳み、縦横シートの全量表示、文字サイズの直接入力とpt、面ごとのReset・Reset All、縦中横の既定を切、Code FontをTextへ、C5のOffice形式の色パレット。次は縦書きPDF（7.10）の1ページ試作。直近のコード検証は721件成功・11件除外。

- E17「長い段落の入力応答改善」と縦横切り替えの位置保持修正は完了。2026-09-14、書き手より体感速度向上、IMEの位置ずれなし、縦横切り替えの修正版に問題なしとの確認を得た。追加の確認手順はtestdata/40に残す。
- 承認済みの実装順0〜19（E15、U2・U3まで）と、その後のF1・diff／Merge・E7・Explorer改善は実装済み。
- 直近の「保存版と比較」、Explorerの整理3操作、行内での新規作成、削除時の保存確認とTAB終了は書き手の確認済み。
- diff／Mergeは、差分行のクリック・左右チェック・「本文（左）に反映」まで整理済み。通常操作は書き手の確認済み。取消・Undo・変更後の反映防止などは自動試験で確認。
- 直近のコード検証は718件成功・11件除外。E17の性能計測と画面試験は別途実行して成功。比較画面・Explorer・名前入力欄の描画とクリック／キー操作を確認済み。検索除外・閉じたTAB・当初のショートカット設定は書き手の実操作確認済み。
- 書き手の指定順でE5の検索除外、E6のTAB再表示、C1の一覧・検索・割り当て変更を実装。設定画面への移設、分類、キー実入力、修飾キーの誤発火修正も確認済み。拡張45操作すべての実操作確認を意味するものではない。

- **書き手の追加要件（2026-09-15）を手間の少ない順に進める。** Gitの版管理・文字色のセット・TAB毎・Pane毎の背景色は完了（書き手の確認済み）。残りはソース表示のReadOnlyモード → 背景の透明化・壁紙、ほかに表示の国際化対応とExplorerの書式設定（順は未定）。直近のコード検証は726件成功・11件除外。

## 残作業の一覧

| 区分        | 項目                    | 状態・範囲                                           |
| --------- | --------------------- | ----------------------------------------------- |
| 完了・実操作確認済み | E17 長い段落の入力応答改善       | 入力速度・IME・縦横切り替えの位置保持を確認済み。網羅的な追加確認はtestdata/40に残す |
| 採用済み・次に着手 | 縦書きPDF（7.10）          | 原稿を紙の形で確認する出力。設定画面の改善のあと、次のアクション1から  |
| 拡張候補      | C1 ショートカット            | 45操作の一覧・検索・変更は実装。Terminal・入力欄の設定、操作検索からの実行は残る候補 |
| 追加候補・P1   | T2 Terminalの貼り付け・終了確認 | 複数行貼り付けとシェル終了の確認設定                              |
| 追加候補・P2   | T1・T3・T4 Terminal改善   | 出力検索、シェル設定、履歴閲覧・コピー・リンク                         |
| 低優先・条件付き  | E8・E16、C2〜C4、T5       | 折り畳み等、大容量、プリセット、アイコン、描画高速化、TUI互換性           |
| 開発上の候補    | 部品化                   | 再利用候補の洗い出しから検討                                  |

候補のP1／P2／P3は優先度案であり、採用済みや実装順の確定を意味しない。

## 次のアクション（E17完了後の順序案）

**2026-09-15、書き手の選択でPDFより先に「設定画面の改善」を進め、書き手の確認を得て完了した。次は下の1。**

0. **E17と縦横切り替えの位置保持は完了。** 書き手の実操作確認済み。testdata/40の網羅的な操作は日常使用で追加確認し、問題が見つかった場合に追跡する。次の候補への着手は別途決める。
1. **縦書きPDF・印刷（7.10）の着手範囲を固める。** 最初に既存の組版から1ページを出す試作を作り、ルビ・傍点・縦中横と文字欠けを確認する。用紙・余白の既定値は試作を見て決める。その後、複数ページへの分割、ノンブル、保存操作、同じ出力経路からのWindows印刷へ進む。完了はPDFと印刷を含む7.10を満たした時点とし、1ページ試作だけで完了扱いにしない。
2. **日常使用と並行して、拡張ショートカットを確認する。** testdata/39のうち使う操作から確認し、不具合があれば先に直す。未確認項目があること自体でPDFへの着手を止めない。
3. **その後にTerminal改善の採否を判断する。** 日常使用で困っている場合はT2の貼り付け・終了確認を先に、次にT1の出力検索を候補とする。P1案という記載だけで採用済みとはしない。

選定理由：長い段落への入力で待ち時間が生じるという書き手の報告を受け、日常の執筆に直接影響するE17を先行する。その後の候補は採用済みの7.10。部品化・GPU化は現状の低優先を維持する。

## 完了したものと仕様の行き先

「実操作確認済み」は書き手から確認があった範囲。未確認の操作は次節に集約する。

| 項目 | 完了内容・確認状況 | 仕様・操作 |
| --- | --- | --- |
| TAB毎・Pane毎の背景色（2026-09-15） | 本文の右クリック「Background…」でTABの紙、Paneメニュー「Pane Background…」でPaneの紙。TAB > Pane > 全体、縦横は別々（メニューに向きを添える）。TABの右クリック「Tab Color…」でTAB（見出し）の色、既定は紙と同じ。Settings → Page「Random Background」Off／Light／Darkで新しく開いたTABにランダムな紙（同じ文書の別TABも新しいTABとして扱い、色は引き継がない）。Text・Layout・Pageの各面に縦書きの「横書きに合わせる」（縦中横は残す）。TAB・Paneが残る限り付き、セッションで戻る。書き手の確認済み | 要件定義9、README「設定」、testdata/43 |
| 文字色のセット（2026-09-15） | Textの面の先頭「COLOR SETS」。Body・H1〜H6の文字色を横書き・縦書きまとめて14色で10セット。一覧で選ぶと即適用、「Save to…」で保存先を選ぶ、Delete。一覧の表示は今の色と同じセット、なければCustom。Resetでは消さない。原文もBodyの文字色で一様に出すよう変更。書き手の確認済み | 要件定義9、README「設定」、testdata/42 |
| Gitの版管理（2026-09-15） | Paneメニュー「前回のCommitと比較」でHEADの内容を右に出す比較専用。Gitが無い・管理下にない・Commitが無い・ファイルがCommitに無い・持ち主が違う（safe.directory）を分けてステータスに出す。あわせてステータスバーの知らせを空き幅いっぱいまで出し、切れたときだけマウスオーバーで全文。書き手の確認済み | 要件定義7.11、README「前回のCommitとの比較」、testdata/41、技術検証6.39 |
| 設定画面の改善（2026-09-15） | 設定を「Settings」TABで開く（窓に1つ、群は左に縦）。Keysは分類ごとに畳める全量表示で、選んだ行の下に変更欄、キーを押して探す欄。Text・Layout・Pageは横書き・縦書きを上下に並べる。文字サイズはpx／%とptの欄に直接入力。Resetは面ごと、GeneralのReset Allは確認のうえ単語帳以外を戻しモードを「なし」に。縦中横の既定は切、Code FontはText。書き手の確認済み | 要件定義7.8・9・11.8、README「設定」 |
| C5 色パレット | Windowsの色ダイアログをやめ、全色見本がOffice形式のパレット（既定の色・テーマの色・標準の色・最近使用した色・その他の色…）を開く。その他の色は新規／現在を並べOKで反映。最近使用した色は設定ファイルに残す。書き手の確認済み | 要件定義9 |
| E5 検索除外 | 検索パネルに名前パターンの除外欄。フォルダを探索前に除外し、条件を保存。Explorerの表示は維持。書き手の実操作確認済み | 要件定義7.7、testdata/37 |
| 検索結果の表示 | 最大幅1200px、全文保持とマウスオーバー表示。書き手の実操作確認済み | 要件定義7.7、testdata/37 |
| E6 閉じたTAB | Paneメニュー／Ctrl+Shift+Tで直近20件を現在Paneへ戻す。文書・位置・モードを保持し、破棄・削除・Terminalは対象外。現在起動中の履歴のみ。書き手の実操作確認済み | 要件定義6.5、testdata/38 |
| C1 ショートカット設定 | Keysに45操作を分類し、実入力・重複拒否・解除・既定復帰・保存を実装。基本操作、配置・分類とCtrl+Alt誤発火修正は書き手の確認済み。拡張操作は自動試験済み、全操作の実機確認は未完了 | 要件定義11.8、testdata/39 |
| Explorer削除とTAB | 未保存なら保存・保存せず削除・取消の三択。削除成功後に同一ファイルの全TABと、フォルダ配下の対象TABを閉じる。失敗・取消は保持。保存・外部変更時の中止・複数TAB終了を自動確認。書き手の確認済み | 要件定義5.2、testdata/36 |
| Explorerの新規作成 | New File／New Folderで作成先に仮の行と名前入力欄を表示。Enterで作成・Escで取消。同名・不正名は入力を保持して修正可能。自動テスト705件成功・9件除外、入力欄描画とEnter／Escも確認済み。書き手の確認済み | 要件定義5.2、testdata/35 |
| E5 Explorerの整理 | 見出しに現在の文書を表示・すべて折り畳む・更新を追加。現在文書の親フォルダを展開し選択、作業フォルダ外では案内。全体705件成功・9件除外、描画とメニュー操作も確認済み。書き手の確認済み | 要件定義5.2、testdata/34 |
| E7 保存版との差分 | Paneメニュー「保存版と比較」で本文（左）とディスク上の保存内容を比較。比較専用で本文・Undo・保存基準・外部変更警告を保持。自動テスト705件成功・9件除外、Paneメニューのクリック試験成功。書き手の確認済み | 要件定義7.11、testdata/33 |
| Merge・差分選択 | 差分行のクリック、左右チェックとヘッダの採用ボタン、「本文（左）に反映」を統合。未選択は左、取消では本文不変、反映全体をUndo一回で戻せる。通常操作は書き手の確認済み、例外処理は自動確認済み | 要件定義7.11、testdata/32 |
| 参照先不明の警告 | 方針を簡素化し、移動先指定を削除。外部版を比較できないことを警告し、本文・Undo・TABを保持。「別名で保存」とキャンセルを案内。書き手から「期待通り」と実操作確認済み | 要件定義8、testdata/31 |
| Explorerの自動更新 | 表示中の作業フォルダ・展開中フォルダをバックグラウンドで定期確認。変更時だけ一覧を更新し、選択・展開・Active Paneを維持。ドラッグ・メニュー操作中は待機。実操作は未確認 | 要件定義5.2、testdata/30 |
| diff② 外部版 | 外部変更の問いから左右比較を表示。未保存本文・保存基準・警告・TABを保持し、終了で元の表示へ戻る。通常操作は書き手の確認済み。読み込み失敗は自動確認済み、実操作は未確認 | 要件定義8.3、testdata/29 |
| 全体の背景色 | メニュー・ツールバー・比較画面などの共通背景と、原稿・Terminalの初期背景を白に近づけた。設定済みの個別色は維持。書き手の実操作確認済み | ui/tokens.slint、Paper初期値 |
| diff・メニュー入口 | Paneメニューから別ファイルを選び、未保存本文と左右比較。差分の色分け・選択枠・前後移動と有効／無効表示・連動スクロール・全文コピーを実装。終了で元のTAB・配置・位置に戻る。Go to LineもPaneメニューへ配置。書き手の確認済み | 要件定義7.7・7.11、testdata/28 |
| F1 | 空白・タブ・実改行の表示切り替え。縦横の描画、文字位置の維持、設定保存と本文・Undoの不変を自動確認。書き手の実操作確認済み | 要件定義7.1、README「空白・改行の表示」、testdata/27 |
| 基本UI（〜9/8） | 分割、Explorer、Terminal、TAB、仮TAB、右クリック、倍率、選択開始、行番号、設定画面など | 要件定義6・9・11、README |
| E1〜E4 | 検索・置換、Ctrl+G、行移動・選択・インデント、文字コード・改行コード | 要件定義4.3・7.1・7.7・11.2 |
| 要件7.8、E9・E10 | ルビ・傍点・縦中横、記法のON/OFF、選択行の箇条書き | 要件定義7.1・7.3.2・7.8・9.1 |
| 単語チェック | モード・単語帳・色分け。効かない報告はモード「なし」が原因で、変更後の有効化を確認済み | 要件定義7.9、単語チェックモード要件.md、testdata/12 |
| 保存レビュー3件 | 保存先の衝突、確認待ちの状態保持、読み直し時の文字コードを修正 | 要件定義7.6・8.3 |
| S1・S2 | 新規メモの保持・三択・退避復元、外部変更通知・外部版比較・差分表示・手動取り込み。実装・自動確認済み | 要件定義8.3・8.4、README「保存と復元」「外部版との比較」 |
| U1、R1〜R3 | 「自動退避」の名称と説明、Ctrl+O、Undoの未保存表示、SettingsのEsc優先順を修正 | 要件定義7.1・8.1・11、README |
| R4 | 右ペイン閉鎖後の連続Ctrl+W。実操作で解消確認済み | 要件定義6.4、README「診断ログ」 |
| E11・設定画面 | 本文・見出しの装飾、背景をPaperに合わせる選択、色見本ボタンの統合、設定画面の移動。書き手の確認済み | 要件定義9、README「フォントの装飾」 |
| E12 | 未解決リンクの表示、パス指定リンクのCtrl+クリック。実操作確認済み | 要件定義7.3.2、testdata/22 |
| E13 | 本文だけをコピー。コピーされない問題も修正し、貼り付けまで実操作確認済み | 要件定義7.1、README、testdata/23 |
| E14① | 編集中の見出し書式・本文位置の維持、H1〜H6の設定から測る共通余白、設定変更への追随。実操作確認済み | 要件定義7.3.1、testdata/24 |
| E14②・TAB操作 | TAB単位のViewer、同一文書の別TAB、選択中のNew Tabを優先、Explorerのダブルクリックで別TAB。実操作確認済み | 要件定義7.3.1、README「Viewerモード」、testdata/25 |
| E15・Active Pane | 右下から分割、選択ペイン番号の表示。両方とも書き手の確認済み。Explorerの開き先も正常と確認 | 要件定義6.4、README「ステータスバーから分割」 |
| U2・U3 | 右クリック上段のUndo／Redo、履歴なし・Viewerでの無効表示、分割・倍率をペインメニューへ集約。実操作確認済み | 要件定義7.1、README、testdata/26 |
| マウスと移動履歴 | 戻る／進むボタンを接続。通常の動作は書き手の確認後、同一ファイルの別TABを区別するよう追加修正・自動確認済み | 要件定義7.7、README「本文の右クリックメニュー」 |
| その他の修正 | 選択解除・Shift状態、IME変換中の表示揺れ、本文のEnter後の字下げを修正 | 要件定義7.3.2・11.2・11.4、技術検証6.37・6.38 |

## 残る実操作確認

実装済みの項目について、確認済みと未確認を混同しないために残す。新たな不具合が判明したという意味ではない。

| 項目 | 確認する範囲 |
| --- | --- |
| C1 拡張操作 | Quick Draft内でのコピー・終了・転記、Viewer開始／終了、解除後と再起動後の保持など。修飾キーの誤発火修正は確認済み。testdata/39で必要な操作を順次確認 |
| S1 | 新規メモを閉じる三択、名前を付けて保存、破棄、再起動での退避復元。ネイティブ保存ダイアログを含む一連の操作 |
| R1〜R3 | Ctrl+Oとキャンセル、共有ペインの未保存表示、Settings・検索欄のEsc優先順 |
| 同一ファイルの別TABの履歴 | 編集TABとViewer TABを順に選び、戻る／進むでそれぞれのTABへ戻れること |
| Explorer自動更新 | 外部変更の定期反映と、選択・展開・スクロールの維持。自動試験済みだが、この項目単独の実操作完了報告は明示されていない |
| 例外条件 | 外部版読み込み失敗、保存・削除失敗などは自動試験の範囲。通常操作の確認と、例外を実機で再現した確認は区別する |

旧レビューの再現条件は[レビュー指摘_2026-09-11_実操作.md](レビュー指摘_2026-09-11_実操作.md)を参照。
行頭空白の最初の1文字と混在改行の報告2件は、書き手の判断で追跡終了済み。
混在改行の保存仕様は要件4.3、確認方法はREADME「診断ログ」を参照する。

## 直近の完了・採用済み未着手

### Quick Draftの選択が解除できない（2026-09-14・対策実装、実操作確認待ち）

Editorで以前修正した選択解除不能と同じ症状がQuick Draftにもあるとの報告。Quick DraftはSlint標準TextInputで選択を処理しており、Editor側のWindows実Shift状態による補正が適用されていなかった。Quick Draft専用のwinitイベントフィルターで、マウス・キー入力がSlintへ渡る前にGetKeyState／GetAsyncKeyStateを確認し、両方ともShift解放なら左右Shiftの解放をSlintへ通知する。どちらかが押下状態なら補正しない。文字入力・クリップボード・選択範囲自体は書き換えない。Slintの既存winitバックエンド用APIをfeatureで有効化し、新規ライブラリは追加していない。回帰確認は、左右Shiftの解放通知欠落後の矢印・クリックと、実Shift押下中の範囲選択。実操作では、発生していた操作の後に普通のクリックで選択解除できること、Shift+クリック／矢印で選択できること、IME変換が正常なことを確認する。

### 検索・置換欄の開閉時に本文位置と折り返しを維持（2026-09-14・完了）

検索欄が34px、置換欄が68pxの高さを使い、本文が下へ移動し、縦書きの折り返しも変わる問題に対応。書き手の指定に合わせて、本文領域の元の位置・大きさを維持し、検索・置換欄が占める上端だけをクリップする。横書きでは上部が隠れ、縦書きでは上側の余白から隠れる。余白より欄が大きければ本文の上端も隠れる。折り返しは変えず、ウィンドウのリサイズには追従する。透明な入力欄は検索欄の裏に隠れない位置へ補正し、フォーカス喪失を防ぐ。描画テストで横書き・縦書きとも本文の目印の画面座標が検索→置換→閉じるの間で不変であることを確認。実操作確認手順：Ctrl+F→Ctrl+H→Ctrl+Hで、本文の位置と列末が変わらず、上端だけが隠れること、検索キーとIME入力が正常なことを確認する。

書き手より「いいと思います。これで、一通り確認できたと思います」と確認を得た。今回の検索・置換欄の表示、本文位置・折り返し維持、検索ショートカットの一連の修正を確認済みとして完了。

### Pane切り替え後のCtrl+F（2026-09-14・完了）

最終確認：上記の一通りの確認報告を受け、完了。以下は原因特定までの調査経緯であり、その時点の未確認事項を含む。

検索欄開閉の接続修正後、マウスでPane／TABや縦横方向を切り替えた直後はCtrl+Fが効かず、本文クリック後に効くとの報告。切り替え後のフォーカス復帰時に、本文入力欄のフォーカスを一度解除して再取得する対策を追加した。Slintの同一要素へのfocus要求は再通知を省略するため、その省略を避ける。試験環境では本文クリック後もキー入力を再現できず、ネイティブ環境での解消は未確認。修正版で「切り替え→本文をクリックせずCtrl+F」を確認するまで、この項目は未完了とする。

追確認：一度Ctrl+Fが成功した後にもCtrl+F／Ctrl+Hが効かなくなる場合があるとの報告。`Diag_20260914_194559_001.log`で、起動後14.978秒→15.426秒、54.287秒→54.686秒に本文フォーカスの取得→喪失を確認。旧ログには検索キーの受信・検索欄の状態がなく、原因は未確定。診断ログに本文／検索欄でのCtrl+F・Ctrl+H受信、開閉処理の開始・終了、検索欄の開閉・対象Pane・置換モード・フォーカス要求状態を追加した。検索文字列・本文は追加ログに記録しない。次回発生時のログで、入力が届かないのか、開閉後にフォーカスを失うのかを確認する。

原因調査・修正：`Diag_20260914_195300_001.log`では、序盤3.376秒に検索欄を閉じ、3.384秒に本文フォーカス取得、3.788秒に検索欄が閉じたまま喪失。終盤も60.230秒のTAB切り替え後、60.362秒に本文フォーカスを喪失し、以降は検索キーの受信記録なし。Slint 1.17.1はキー配送前に入力要素が画面外へ完全に隠れていればフォーカスを解除する。本文用の透明なTextInputをキャレット座標へ直接配置していたため、フォーカス取得後の組版更新・スクロールによってこの条件に入る。入力欄の座標をScrollViewの実際の表示範囲内に収めるよう修正した。本文キャレットとスクロール位置はこの補正では変更しない。フォーカス取得後にアンカーを画面外へ移して実際のキーイベントを配送する回帰テストで、修正前のCtrl+F不発を再現。修正版では横書き・縦書きのCtrl+F／Ctrl+H連続開閉を検証する。ネイティブ実操作とIME候補位置の確認までは完了扱いにしない。

### 追加確認F：倍率変更後の表示切れ（2026-09-14・完了）

書き手のResultで、横書きの右側が切れ、スクロールでも見られないとの報告。150％時の操作ログでは行幅2343pxとなっており、画像の分割幅2048pxを超えていた。行に沿う方向のスクロール通知が描画更新へ接続されておらず、新しく見える画像が追加されていなかった。横書きの左右・縦書きの上下の両方で、実際のスクロール位置を記録して画像を更新するよう修正。書き手より「問題ないように見えます」と確認を得て完了。

testdata/40のResultを記録表へ反映した。A・B・D・G・Hは問題なし、C・Eは「問題なさそう」と用語の説明への指摘を記録。「印」は入力した確認用文字列を指すよう手順を具体化し、書き手のResult原文は保持した。

### Ctrl+Fで検索欄が開かない（2026-09-14・完了）

最終確認：検索欄の表示とフォーカスに関する後続修正を含め、書き手から一通りの確認報告を得たため完了。以下の実操作未完了の記載は、接続修正時点の経緯。

書き手の追加確認中に報告。設定可能なショートカットの処理が、検索欄の開閉ではなく一致箇所へ移動するコールバックを呼んでいた。Ctrl+FとCtrl+Hを、ペインの検索欄・置換欄を開閉する共通処理へ接続した。同じキーによる開閉、検索から置換への切り替え、本文から検索欄へのフォーカス、変更したキーの使用を実際のショートカットコールバック経路で自動確認済み。全体テストは718件成功・11件除外。書き手による修正版の実操作確認は未完了。

### E17 長い段落の入力応答改善（2026-09-14・完了、実操作確認済み）

P1相当。表示範囲の先行組版と画面外の非同期計算を実装した。2026-09-14、書き手より大幅な体感速度向上と、IMEでも位置ずれがないことを確認済み。その他の実操作確認は継続する。仕様は要件定義2.3、実装・計測結果は技術検証のE17節、確認手順はtestdata/40に移した。

- 変更前の確定済み折り返しを再利用し、表示範囲・入力位置の周辺を先に組む。長い段落の続きをUIスレッドで待たず、折り返しとブロック計測をワーカーで行う。
- 連続入力では古い仕事を中止し、待機仕事と結果は各1件に制限する。文書・書式・表示条件が一致する版だけを採用する。
- 未確定範囲はスクロール寸法の推定にだけ使用し、文字描画やヒットテストに使わない。スクロール先の追加計算と、確定時の表示位置保持を接続した。
- 縦横の先行表示と完了後のピクセル一致、IME下書き・本文復元、部分組版と同期組版の一致、ルビ・傍点・結合文字、古い結果・失敗からの復帰を自動確認した。
- 組版単体の比較では、5万文字の先頭・中央編集が従来の約150msから約20〜30msへ短縮。これは画面表示全体の時間ではない。短い段落の応答はほぼ同じ。
- 大量貼り付け・大幅な書式変更など、カーソルより前の組版が未知の場合も計算量を制限し、確定した部分から表示する。その場合のカーソルと遠方の未確定部分は計算完了後に表示する。折り返しなしの長大行、文書上限引き上げは別課題。

**残る確認・性能課題：** [testdata/40_長い段落の入力応答.md](testdata/40_長い段落の入力応答.md)に準備・操作・期待結果・記録表を記載した。優先項目はA「貼り付け・Undo／Redo・保存」、B「検索・末尾移動・スクロール」、C「TAB切り替え」（合計10〜15分程度）。D〜Hは余裕のある範囲で追加確認し、未実施はそのまま残してよい。オフスクリーン計測では組版を短縮した後のタイル画像化が待ち時間の大きな部分を占めたため、さらに体感を改善する場合はその計測を次の材料にする。GPU化の採用を意味しない。

**縦横切り替え時の表示位置（2026-09-14・完了）：** 書き手より、横書きから縦書きへ切り替えると同じキャレットが画面外になるとの報告。実操作ログではキャレットへ移動した直後、非同期組版完了時の表示位置復元がその移動を上書きしていた。切り替えに伴う組版が完了するまではキャレット表示を優先するよう修正した。

続くスクリーンショット確認では、画面内に入っても編集箇所が端へ寄り、前後を読めないことが判明。縦横両方向で、キャレット中心の相対位置（横書きは上端から、縦書きは右端からの割合）を引き継ぐよう追加修正した。表示領域のサイズ確定と非同期組版後も維持し、文書端ではスクロール可能範囲に収める。書き手より「問題なさそうです」と確認を得て、今回の修正は完了とした。

### 縦書きPDF（要件7.10）

自分の原稿を紙の形で確認するための出力。仕様は要件定義7.10にある。
READMEの未実装一覧に残っている。既存の優先度案はP2相当で、次の着手順は未決定。
ルビ・傍点・縦中横（要件7.8）は実装済みであり、未実装には含めない。

## 書き手からの追加候補

### E16 大きなファイルへの対応

優先度は低め。上限は要件2.3と`MAX_DOCUMENT_CHARACTERS`。
着手時は「何文字で何が遅いか」を1つ測り、読み込み・組版・タイル・全文検索のどこが先に問題になるかを確認する。
上限引き上げや方式変更は測定後に判断する。

### 部品化

右クリックメニュー、Paperの書式など、試行錯誤して調整した機能の再利用を検討する。
候補の洗い出しから始め、個別に最適化する。アプリの新機能とは別の開発上の課題。

# 背景の設定
## 背景の透明化・指定の壁紙設定
背景を透明化出来るようにする。透過度を調整できる。
また、指定の壁紙を設定できるようにする。
壁紙は、タイル表示、拡大表示(縦横比を維持して、縦に合わせるか横に合わせるか設定する)
壁紙の濃さを選択できる

# Readonlyモードの拡張
ソース表示にもReadOnlyモードを設ける
用途としては、ログファイルなど常に変更されるファイルを参照するとき。
このとき、自動スクロールされるが、マウスのホイールなどでスクロールを手動で動かすと、自動スクロールは止まる。
最下行に下ろせば、自動スクロールは再開する。

文字の検索は可能だが、編集は不可。
横書きのみで、縦書き変更・Markdownプレビューモードへの変更も不可

ファイルを別名保存することは可能だが、保存は断面となり、この時点で別ファイルになるため、更新は止まり、ReadOnlyモードは解除される

# 表示の国際化対応
メニューやメッセージの国際化対応を考えておきましょう
当面は日本語と英語でいいです
設定で選択。デフォルトはシステムに合わせるでいいと思います。
(システムの対応言語がない場合は、英語)

# ExploreのPaneの書式設定
左のExploreのPaneの文字サイズなどを変更可能にしておいた方がいいです
合わせて、背景色と文字色も。
フォルダとファイルで色を分ける機能もあった方がいいですね
フォントの変更までは不要です。


## 決定済み・追跡終了

- **F2 文書単位のロック案：終了。** TAB単位のViewer（E14）で目的を満たすため、文書全体のロックは追加しない。
- **外部移動先の推測・指定：採用しない。** 参照先がなければ警告し、本文・Undo・TABを保持する。比較は開始できず、必要なら別名で保存する。書き手の確認済み。
- **Editor内の削除と外部削除は区別する。** Explorerからの削除は未保存確認後、成功時にTABを閉じる。外部アプリの削除では本文を保持して警告する。
- **diff／Mergeの段階実装：完了。** 別ファイル比較・外部版比較・左右の採用・保存版比較の入口を実装済み。過去版の保存履歴や自動マージは今回の範囲に含めない。

## 追加候補（採否・着手順は未確定）

**レビューからの提案であり、採用済みの仕様を置き換えるものではない。**
優先度は、P1＝日常操作の手数を減らすもの、P2＝使う場面に応じて順次、P3＝後回し。
判断の基準は要件3の「縦書きで文章を書く人」「編集と校正が軽いこと」。
標準的なEditorの機能でも、クイックオープン、IDE機能、内部リンク解決、表記ゆれの自動判定は
既に対象外と決めているので再提案しない。

E1〜E7のEditor側項目は実装済み。E5の検索除外・E6の再表示は書き手の実操作確認済み。
Terminalは「残すが前に出さない」という方針を維持する。T2はP1案、T1はP2案。
C1は45操作の設定画面を実装し、Terminal・入力欄の設定と操作検索からの実行が残る。

## P1


### T2 Terminalの貼り付け・終了操作の整理

**困る場面：**改行を含む文字列を貼るとコマンドが実行され得る。タブを閉じる／シェルを替えると、
その中の作業も終わる。通常の文書タブとは結果が違うので、その違いを操作時に分かるようにする。

- Ctrl+Shift+Vを貼り付けの別入口として用意する。既存のCtrl+Vとbracketed paste対応は維持する。
- 複数行貼り付けの確認を設定で選べるようにする。確認する場合は行数と内容を見せ、キャンセル時は何も送らない。
- 下段の下書きから送る経路にも適用範囲を定める。現在の「末尾改行の有無で実行が決まる」規則を黙って変えない。
- 動作中のシェルを閉じる／切り替える前に確認できる設定を設ける。既に終了したシェルには確認を出さない。
- **完了の目安：**Ctrl+Cは常に割り込みとして届く。閉じる操作を取り消すとシェルと出力が保たれる。シェルの生存だけで子コマンド実行中と断定しない。

## P2

### T1 Terminalの出力検索

**困る場面：**出力が長いと、ホイールだけで以前のエラーを探す必要がある。
スクロールバックとコピーは既にあるので、その履歴を検索できるようにする。

- 対象はフォーカス中のTerminalの画面と保持している履歴。上段・下段のどちらにも同じ入口を設ける。
- 前／次、一致箇所の強調、大小文字の区別を用意する。ヒットした行までスクロールする。
- Ctrl+Shift+Fを候補とし、Ctrl+Fはシェルへ渡す。要件11.7の「編集器が先に取るキー」を採用時に追記する。
- **完了の目安：**画面外の過去の出力へ移動し、その箇所をコピーして本文へ戻れる。履歴上限から消えた出力は検索できないことを区別する。
- 参考：[Windows Terminalの検索](https://learn.microsoft.com/en-us/windows/terminal/search)。このEditorでの優先度はレビュー判断。

### T3 シェル設定の画面編集と開始フォルダ

**WSLのディストリビューションやシェルを足すとき、設定ファイルを探して編集しなくて済むようにする。**
名前・実行ファイル・引数・開始フォルダを設定画面で編集し、複製・並べ替え・既定指定ができるようにする。
「このフォルダでTerminalを開く」をExplorerに置き、現在の文書の親フォルダも選べるようにする。
空白・日本語を含むパスを扱い、既存のシェルの作業場所は変えない。WSLの開始位置はWindowsのパスと
同じ文字列を渡せばよいとは限らないので、シェルごとに確認する。
参考：[Windows Terminalのプロファイル設定](https://learn.microsoft.com/en-us/windows/terminal/customize-settings/profile-general)。

### T4 Terminalの履歴閲覧・コピー・リンク操作

**ログを読み返して原稿やメモへ持ち帰るため**に、「最新の出力へ戻る」、履歴のページ送り、保持行数の設定、
選択テキストのコピーを補強する。出力を遡っている間は勝手に最下部へ戻さず、新しい出力があることを示す。
自動折り返しで生じた行境界と実際の改行をコピー時に区別する。URLはCtrl+クリックで開けるようにし、
通常の選択操作と分ける。画面消去と履歴消去は名前・効果を区別し、シェルを再起動しない。

### C1 ショートカットの拡張（残り）

本文・Pane／TAB・Emacs・Quick Draftの45操作の一覧・検索・変更設定は実装済み。
今後の候補は変更対象の拡大、Terminal・検索欄の設定、操作名を検索してその場で実行する機能。
現段階では文字入力・OSの予約キーを守るため、一覧の既定キーまたはCtrl+Alt+A〜Zを指定する。

## P3：用途が固まってから

- **E8 見出しの折り畳み・ブックマーク：**長い章の一部分だけを見て書くため。折り畳みは表示だけに効かせ、保存内容を変えない。既存アウトラインで足りるかを先に試す。
- **T5 全画面TUIの互換性：**マウス追跡、フォーカス通知、代替画面、リサイズ、日本語・絵文字の幅、キー送信を代表的なTUIで確認する。現在はモードを記録していても入力送信まで結び付いていないものがある。TUIを日常利用するならP1へ上げる。
- **C2 表示設定のプリセット：**文字・余白・紙色等を名前を付けて保存し、読み返し用／執筆用に切り替える。要件13.2の具体化。本文や文書ごとの単語チェックモードを勝手に変更しない。
- **C3 実行ファイルのアイコン：**既存の`ui/icon.png`をWindowsリソースとして実行ファイルへ埋め込み、Windows Explorerでも表示する。日々の編集への効果は小さいが、配布前には必要な仕上げとして扱う。
- **C4 GPU化：**入力・スクロール・長文の初回表示を測り、組版・描画・画像転送のどこが待ち時間を作っているかで決める。GPUを使ったことではなく、文字欠けや位置ずれなしに待ち時間が減ったことを完了条件にする。
