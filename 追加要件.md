# 追加要件

## Split Pain（済 2026-09-06 `bc4e6d9`）
今、左右・上下の二分割にしか対応していませんが、複数分割に対応しましょう
分割は、Split Rightすると右にPainが増える
Split Downとすると、下にPainが増える
とします。

つまり、二分割状態で、右のPainからSplit Rightすると、右のPainが追加され三分割に。
右のPainからSplit Downすると、左のPainは維持したまま、右のPainが上下に分割する三分割に。

同様に、選択中のPainから、Split Right/Split Downすることで、そのPainが分割されるように変更しましょう

## Explorer Pain（済 2026-09-06）
このPainの幅を変更できないので、幅をマウスで変更できるようにしましょう
→ ドラッグで160〜640px、セッションへ保存。要件定義§6.2へ反映。

#### Search（未着手）
Explorer PainのSearchですが、現在検索フォルダ指定が出来ません
検索フォルダを指定できるようにしましょう
デフォルトはOpen Folderのフォルダで問題ないです。

# 設定機能の集約（済 2026-09-06）
設定ボタンを配置して、全体的な設定を機能集約する
→ レール最下段に Settings。☰ の `Typography…` はそこへ移した（⋮ のほうは残す＝ペイン側の入口）。要件定義§6.2へ反映。
ボタンの位置はExplorer, Serchのボタンが並んでいる列の一番下。ウィンドウの左下がよい


# アイコンのデザイン（一部済 2026-09-06）
アイコンを作る
「RE」の文字で紫背景
→ `ui/icon.png`（紫 `#572e9d` にアイボリーの RE＝RFN Edit）。タイトルバーとタスクバーには出る。
   **実行ファイル自体のアイコン（エクスプローラーで見えるもの）はまだ**——Windowsのリソースが要り、
   それにはこのリポジトリが持っていないビルド依存（`winres`等）が要るため。絵は同じものを使える。




# Terminal機能（最小の1タブが通った 2026-09-06）

**☰ → New Terminal** で、いま見ているペインにWSLのタブが開く。打鍵はシェルへ、出力は升目へ。
`src/pty.rs`（ConPTY）＋`src/terminal.rs`（VT解析と升目）＋`src/terminal_session.rs`（読み取り
スレッド）＋`directwrite_render::cells`（描画）。検証と罠は技術検証9.4。

**まだ無いもの**：接続先の選択（いまはWSL固定）、文字コードの切り替え、サブTerminal／サブEditor、
スクロールバックを遡る操作、選択とコピー、IMEの変換中表示（確定文字は届く）。

## 基本機能
基本はこれまでのEditorにおけるTABの一つが、Terminalとして表示され、汎用シェルとして動かせること。
TAB一つがTerminalのウィンドウになる。

#### 接続先（済 2026-09-06）
・　WSL2
・　Powershell
既定はWSL2とする。
WSL2が起動していない場合は、起動させる。
→ ☰ の New Terminal が2つの選択肢を出す（先頭がWSL＝既定）。タブ名はシェルの名前。
   WSL2の起動は`wsl.exe`自身がやる。

#### 文字コード（測って不要と判断 2026-09-06）
Terminalの表示文字コードを変更可能にする。
基本はUTF-8でよいが、Powershellでは日本語がShift-JISでないと困るかも。
→ **要らなかった。**ConPTYは出入りともUTF-8で、シェル側のコードページ（CP932）は
   conhostが吸収する。CP932のPowerShellで日本語の表示も入力も正しい（技術検証9.4）。
   設定を作れば、正しく動いているものを壊せる欄が増えるだけなので作らない。

## 追加機能

#### サブTerminal（済 2026-09-06）
EditorのTABの下部にコマンド実行用のTerminalを表示できる。
これは、TABのように移動できなくて良い。
→ ⋮ の「Terminal Below」または `Ctrl+\`` で開閉。境界をドラッグで高さ（60〜900px）。
   **下段はタブごと**——開閉も高さも下書きも、そのタブのもの。閉じても中のシェルは走り続ける。
   本文と下段の行き来は `Ctrl+Alt+↑↓`（要件11.3の隣のペインへ移る手つきと同じ。
   下段が無ければこれまでどおり隣のペインへ）。

#### サブEditor（済 2026-09-06）
TerminalのTABの下部にはQuick Draftの機能を備えたサブEditorを表示できる。
このEditorに入力した内容をショートカットでTerminal側に転送できる。
→ ⋮ の「Editor Below」または `Ctrl+\``（Terminalのタブでは下段がこちらになる）。
   `Ctrl+Enter` で上の端末へ送る。**打ったのと同じで、走らせない**——読んで直して、
   自分でEnterを押す。送った下書きは消える。

# ファイルエンコードの対応
複数の文字コードに対応する。
自動判別と、指定文字コードに対応する。
文字コードを変換して保存することが出来る。


# ファイル名の変更機能（済 2026-09-06）
今、TABにファイル名が表示されていますが、編集中のファイルのTAB名を少し長く押すと、ファイル名を変更可能になるようにする。
→ 0.5秒。**タブの中で直に書き換える**（書き手の指摘。最初はダイアログで作った）。
   Enterで確定、Escとフォーカス外しで取り消し。拡張子を除いた部分が選択された状態で始まる。
   実際の改名は要件5.2の`move_entry`を通るので、名前の検査も開いているタブの追従も1つ。
   運搬（4pxの移動）に入ったら鳴らない。保存先の無いバッファはステータスで断る。要件定義§6.5へ反映。

# 右クリックメニュー
#### ファイルの編集画面上
Editor TABの編集画面内で右クリックした場合

次のようなメニューで。ショートカットも合わせて表示する。(英語)
・　Cut
・　Copy
・　Paste
・　選択開始
・　矩形選択開始
セパレータ
・　Terminal　(Editorの下側にでるTerminal)
・　Quick Draft
セパレータ
・　Split Right
・　Split Down
・　Close Other Panes
セパレータ
・　Zoom In
・　Zoom Out
・　Reset Zoom

#### TABの上
・　Close
・　Close Others
・　Close Clean   : 未編集のTAB(保存待ちになっていない)を全て閉じる
セパレータ
・　Rename
・　Close TAB
・　Copy Path  : 開いているファイルのPathをクリップボードにCopy
セパレータ
・　Save
・　Save As

#### TABのない場所(TABの列)
・　New File
・　New Terminal
・　Open File
セパレータ
・　Close
・　Close All
・　Close Clean   : 未編集のTAB(保存待ちになっていない)を全て閉じる

#### File Explorer上
・　Rename
・　New File　: 無題の新規ファイルのTABが新しく作られる
・　New Folder  : 新しいフォルダを作成


# ＋文字のボタン
TABの右際の＋文字のボタンの内容を変更
これを押すと、New File, New Terminal, Open Fileの選択肢がでる
・　New File　: 無題の新規ファイルのTABが新しく作られる
・　New Terminal : 新しいTerminalのTABが開く
・　Open File : ファイルを開くダイアログが表示される。

Explorerの右にあるボタンは、以下のメニュー
・　New File　: 無題の新規ファイルのTABが新しく作られる
・　New Terminal : 新しいTerminalのTABが開く
・　New Folder  : 新しいフォルダを作成

Explorerの＋文字のボタンの右にあるフォルダボタンは、新規フォルダではなく
Open Folder (Explorerで開くフォルダ)に変更。

# 右下の倍率表示
ウィンドウの右下のステータスバーに、表示倍率が出ている
これをクリックするとスライドバーが表示され、倍率変更できるように。

# 行番号の表示
Editorでは、行番号の表示・非表示を可能にしておきたいです。

