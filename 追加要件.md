# 追加要件

書き手が気づいたことを書き留めるところ。**済んだものは要件定義.mdへ転記して、ここからは消す**
（2026-09-07にそう決めた）。ここに残っているものが、まだ手の付いていないものである。

済んだものの行き先：Split（要件定義§6.4）／Explorerの幅・設定ボタン（§6.2）／
Terminal（§6.8・§11.7）／タブ名の変更（§6.5）／仮のタブと戻る・進む（§6.5・§7.7・§11.3）／
右クリックメニューと`＋`（§6.7）／倍率のスライダー（§9）／選択開始（§11.4）／
アイコン（§2.4）／検索フォルダの指定（§7.7）／行番号（§9、縦横とも）。

---

# ファイルエンコードの対応（未着手）

複数の文字コードに対応する。
自動判別と、指定文字コードに対応する。
文字コードを変換して保存することが出来る。

# 実行ファイル自体のアイコン（残り）

絵は`ui/icon.png`（紫地にアイボリーの`RE`）で決まっていて、タイトルバーとタスクバーには
出ている。**エクスプローラーで見えるアイコンだけがまだ**——Windowsのリソースを実行ファイルへ
埋め込む必要があり、そのためにこのリポジトリが持っていないビルド依存（`winres`等）が要る。


# New Tab (実装済み)
今、新しいTABを開くとき、New FileかNew Terminalを選ぶ必要があります。
これをTABを作成してから、New File/New Terminalを選択する形にしたいと思います。
そうすると、十字アイコンの動作においては、New Tabをするだけにして
新しいTABが開いてから、New File/ New Terminalを選択するように促しましょう

New Tabには何もなく、New File/ New Terminal / Open Fileの選択肢がリンクのように中央に表示されている感じ。
このときに、ExplorerでFileを選択すると、そのTABの内容がFileの内容に切り替わります。

# 右クリックメニューの整理 (実装済み)
右クリックメニューについて、そのPaneや場所に置いて実行できないメニューは出さないように。
Editorの時は有効だが、Terminalだと無効のものなど。

→ 要件定義 6.7 へ。シェルが前に出ているときは Cut・選択開始・矩形選択開始・Zoom 3行を
編集面と`⋮`から、Save / Save As / Reveal in Explorer を`⋮`から、Find in File を☰から落とした。
**Copy は残して、写す先をRustが判じる**（升目にも選択はある）。タブの Rename / Copy Path は
ディスクの上に何かあるタブだけに出す。

# 設定画面の見直し (実装済み)
今、一枚にしているが、だんだん設定が増えているので、TAB化したほうがいいかも。

→ 要件定義 9 へ。General / Text / Layout / Page の4面。切れ目は「書字方向を持つかどうか」で、
シートを選ぶ釦は General には無い。

# 書き手の報告 2026-09-08（3件）
- **下段に右クリックメニューが無い** → 置いた（要件定義 6.7）。シェルの帯と下書きの帯で別の行。
- **設定の Page の`Paper`は`Paper Color`のほうが読める** → 直した。隣の`Code`も`Code Font`へ。
- **設定を開いていると背面がちらつく**（続報：**Generalの接続先を選び直すと、指定幅で
  折り返しているときの境界線が消える**）→ ちらついていたのは端末の升目ではなく、
  **紙の枠線**だった。`page`の`border-width`が`ime-input.has-focus`を見ていて、Slintは
  popupが開いた瞬間に焦点の要素を取り上げる（`FocusReason::PopupActivation`）ので、
  メニューを開くたびに枠が消えて戻っていた。**要件 6.3 と同じ旗**（`focused-pane`、Rustが
  決める値）へ替えた。最初に疑った折り返しの説明文は的外れだったが、`overflow: elide`は
  そのまま残す——popupの中の`word-wrap`は「開くときは`preferred`、その後は`min`」の食い違いを
  作るので、入れない側がよい。

## Default Shellについて (実装済み)
今、WSL/Powershell/Powershell7はこの環境の決め打ちだが、
汎用性を持たせるためにどうするか
環境別に設定するか

→ 要件定義 6.8 へ。設定ファイルに`名前 | コマンド行`の並びとして持つ（`terminal.shell.N`）。
3つはその初期値。既定は番号ではなく名前で覚える。**設定画面から足す道はまだ無い**
（ファイルを開いて書く）——足りなければそのときに。

## 自動保存の挙動 (実装済み)
自動保存のモードについて
設定でOn/Offできたほうがよい。
Offの場合は、状態を維持しない。

→ 要件定義 8.1 へ。設定の General に On/Off。**Offにした瞬間に、置いてある作業コピーを
全部消す**。セッション（どのファイルを開いていたか、ペインの分かれ方）は残す——それは
「未保存の中身」ではない。未保存のタブを閉じるときの問いからは「作業コピーを残して閉じる」が
消えて3択になる。

# Terminalの背景・文字色の設定画面を分ける (実装済み)
Terminalは背景が黒い人が多いので、設定画面を分けた方がいいです

→ 要件定義 6.8・9 へ。設定は **General / Terminal / Text / Layout / Page の5面**になり、
既定の接続先もTerminalの面へ移した（どちらも書字方向を持たない）。背景・文字色・書体・
大きさを持ち、**`Theme`の`Paper`／`Dark`は背景と文字を一度に置く**——片方だけ変えた画面は
読めないので、そこを通らせない。**SGRの16色は選ばせず、背景の明るさから2組を自動で選ぶ**
（選ばせれば16回選ぶことになり、しかも選び終えるまで読めない）。

# 単語の色分け機能 (要件定義 7.9 へ／本文に色が出るところまで実装済み)
これは欲しい機能なのですが
設定した単語帳に合わせて、その単語を指定した色に変える機能
単語帳は複数持てて、単語帳1=赤、単語帳2=青という形で色指定して、表示する

→ **要件定義 7.9「軽い校正」の中心に置いた**（2026-09-08）。実装はこれから。
「編集と校正が軽いこと」がこの編集器のいちばん力を入れるところ（要件3）で、
**これはその中身そのもの**。校正とは同じ語を何度も探すことなので、単語帳は
「毎回打ち込んでいた語を、置いておく場所」にあたる——新しい概念ではなく、
既にやっていること（7.7の検索）の手数を減らすもの、と書いた。
**編集器はどれが正しいかを判定しない**（表記ゆれの自動検出は持たない）。

→ **実装した**（2026-09-08、`src/word_marks.rs`）。**書き手の指摘で三度作り直した**：
(1)単語帳は大きくなるので語は1行1語の別ファイルへ、(2)**重複があるので「取り込む」形へ**
——IMEの辞書と同じく、読んだ結果を編集器の表に持ち、重複は数えて画面から参照できる。
(3)**モードという構造へ**——ソースの予約語の色分けと同じ考えで、**モードが語群を持ち、
語群が色を持つ**。モードは文書ごとで、ステータスバーから切り替える（コードエディタが
言語モードを出しているのと同じ場所）。同じモードの2つの語群にまたがる語には**色を付けない**
（どちらの色か編集器には決められない）。語は木に積んで本文を一度だけ歩くので、数千語でも
本文を歩く回数は増えない。

# 描画をGPU化
これはOptionalですが、GPUで高速描写という方向は考えたいです。

---

# Editor／Terminalの追加候補・優先順位（2026-09-08レビュー）

**以下はレビューからの提案。採用済みの仕様を置き換えるものではない。**
既存の「ファイルエンコード」「実行ファイルのアイコン」「GPU化」も順序に含める。
優先度は、P1＝日常操作の手数を減らすもの、P2＝使う場面に応じて順次、P3＝後回し。
同じ優先度の中では表の上から着手する想定。最初の追加は **E1「検索・置換の使い勝手」**を推す。

判断の基準は要件3の「縦書きで文章を書く人」「編集と校正が軽いこと」。
標準的なEditorの機能でも、クイックオープン、IDE機能、内部リンク解決、表記ゆれの自動判定は
既に対象外と決めているので再提案しない。Terminalの配色・書体設定と単語チェックモードは
最新の実装・要件に入っているため、未実装機能として数えない。

## 確認した範囲

- 実画面：既存ビルドで検証専用Markdownを開き、縦書き／横書き、検索、一件置換、Undo、保存、
  左右分割、メニューと設定の入口を確認した。元から開かれていた未保存文書の本文は変更していない。
- 画面確認に使ったexeは2026-09-08 13:42のビルド。その後のTerminal設定・単語チェックの追加は、
  最新ソースと要件定義で確認したもので、同じ画面で動作確認済みという意味ではない。
- 最新ソースの通常テスト：529件成功、0件失敗、4件除外。先のビルドでは別途、ConPTY経由で
  実コマンドの出力と終了後の表示保持を確認する既存テストも1件成功した。
- Terminalの対話画面、WSL／PowerShell双方の長時間運用、全画面TUIの操作は未確認。
  Terminalの追加候補は実装確認と公式資料との比較に基づく。

## 機能追加の前に直したい残り

前回の指摘について、終了時の退避と書き込み待ち、復元する版の記録、通常の作業コピー削除の
キュー化、退避失敗の通知・再試行、開く処理の共通化、全置換の文字数制限は実装に反映されている。
以下はそれとは別に残っている条件。機能追加のP1とは分けて扱う。

| 順 | 残り | 発生条件と確認方法 | 完了条件 |
|---|---|---|---|
| 1 | 自動退避Off時の削除も書き込みと順序をそろえる | `src/saving.rs`の`discard_all_work_copies`は今も直接削除。退避の書き込み中にOffにすると、削除後にコピーが完成し得る。Offのままでは復元されないが、再度Onにした後の起動で古いコピーが戻る可能性がある。コード上の確認 | 未完成・待機中のコピーも対象に含め、Off後に古いコピーが残らない。Onへ戻すと現在の未保存本文を退避する |
| 2 | 終了時の最後の退避失敗を扱う | `src/main.rs`の`writer.finish()`後は失敗件数をログに書いて終了する。書き込み不能時、最後の入力を失ったことに気づけない。コード上の確認 | 最終退避に失敗したら、終了前に再試行・別名保存・終了取り消しを選べる。成功時には確認を増やさない |
| 3 | 検索と一件置換の一致条件をそろえる | `Alpha alpha ALPHA`を`alpha`で検索すると3件。一件置換は`Alpha`を置換せず次へ飛ぶことを画面で再現。最新の`replace_in_pane`にも完全一致比較が残る | 現在の検索条件で一致している選択を、そのまま一件置換できる。全置換とも一致条件を共有する |

**この3件と、下の「0番」のCtrl+Wは、2026-09-09に済んだ**（540テスト成功、fmt差分なし）。
何をしたかは順に：

- **3（一致条件）：**`find.rs`に`is_match`を足し、`main.rs`の`replace_in_pane`が`==`で
  訊いていたところをそこへ通した。**一致かどうかを答える場所を1つにした**ので、
  `next_match`・`count`・`replace_all`・一件置換の4つが同じ畳み方を共有する。
  テスト2件（見つけたものは必ず置換できる／針でない範囲は一致ではない）。
- **Ctrl+W：**`ui/editor-pane.slint`のキー列に1行足して`close-active-tab()`を呼ぶ。
  **端末は上で先に返しているのでここへ来ない**——Ctrl+Wはシェルでは「直前の語を消す」で、
  そちらの意味は奪わない。
- **1（Off時の削除）：**`discard_all_work_copies`が`discard_work_copy`を通るようにし、
  **ディスクにあるものと開いている文書のぶんの両方**を並べる（行列で待っているだけの
  コピーはまだファイルが無いので、文書の側から名前を作らないと拾えない）。
  Onへ戻すときは`keep_work_copies_again`が未保存の文書の旗を立て直す。
  **打っているあいだは要らない**——編集のたびに旗は立ち、Offはそれを下ろさないので、
  Onへ戻せば次の時計で書かれる（書き手の指摘、2026-09-09）。塞いでいるのは
  「Offにして、**何も打たずに**、Onへ戻す」1つの並びだけで、そのとき旗は下りているのに
  コピーはOffが消したあと＝**未保存の文書にコピーが1つも無い**。
- **2（終了時の失敗）：**`FileWriter::settle`（`finish`の、送り口を落とさない版）を足し、
  `on_close_requested`が**窓を開けたまま**最後の退避を待つ。書けなかったら
  「もう一度試す／文書を保存する…／閉じない」を訊く。成功なら何も出ない。
  行列の畳み込みで捨てられる仕事も`superseded`として答えを返す（返さないと、渡した数と
  返った数が食い違って毎回3秒待つ窓になる）。テスト2件。

## 優先順一覧

| 順 | 優先度 | ID | 追加するもの | 効果・位置づけ |
|---|---|---|---|---|
| 1 | P1 | E1 | 文書内検索・置換の操作強化 | 語を探して直す往復を短くする。軽い校正に直結 |
| 2 | P1 | E2 | 文字コード・改行コードの表示と指定 | 既存要望の具体化。別の道具で書いた原稿を開ける |
| 3 | P1 | T1 | Terminalの出力検索 | 長い出力からエラーや以前の結果を取り出せる |
| 4 | P1 | E3 | 選択・行移動・インデントの基本操作 | 切り貼りの手数を減らし、段落や箇条書きを直しやすくする |
| 5 | P1 | T2 | Terminalの貼り付け・終了操作の整理 | 改行を含む貼り付けとシェル終了を意図した操作にする |
| 6 | P2 | E4 | 現在位置表示と指定行への移動 | 行番号で指摘された場所へすぐ行ける |
| 7 | P2 | E5 | Explorerの再表示・整理 | 既存の「ファイルへ行く道」を使いやすくする |
| 8 | P2 | E6 | 閉じた文書タブを開き直す | 誤って閉じても閲覧位置へ戻れる |
| 9 | P2 | T3 | シェル設定の画面編集と開始フォルダ | 設定ファイルの手編集や毎回のフォルダ移動を減らす |
| 10 | P2 | T4 | Terminalの履歴閲覧・コピー・リンク操作 | 出力を読んでEditorへ持ち帰る手数を減らす |
| 11 | P2 | E7 | 保存版との差分表示 | 推敲で何を変えたかを確認できる |
| 12 | P2 | C1 | ショートカット一覧と操作の検索 | アイコンや長押しを覚えなくても機能へ辿り着ける |
| 13 | P3 | E8 | 見出しの折り畳み・ブックマーク | 長い章の中で見る範囲と戻る場所を絞る |
| 14 | P3 | T5 | 全画面TUIとの互換性強化 | 汎用Terminalとしての用途を広げる |
| 15 | P3 | C2 | 表示設定のプリセット | 既存の要件13.2。作品や読み方に合わせた設定を再利用 |
| 16 | P3 | C3 | 実行ファイルのアイコン | 既存要望。配布する段階では優先度を上げる |
| 17 | P3 | C4 | GPU描画の検討 | 既存のOptional要望。測定したボトルネックに応じて判断 |

## P1：先に追加したい機能

### E1 文書内検索・置換の操作強化

**困る場面：**同じ語を追って何度もEnterを押すと、全体のどこを見ているか分かりにくい。
画面では総件数と現在の選択は分かるが、他の一致箇所や検索条件の切り替えがない。

- 入力中に件数と一致箇所を更新し、表示中の一致を薄く、現在の一致を強く示す。`3 / 12`のように現在位置も表示する。
- 本文に戻ってもF3／Shift+F3で次／前へ移動できる。検索欄ではEnter／Shift+Enterで同じ操作。
- 「大文字・小文字を区別」「単語単位」「選択範囲内」を追加する。既定の大小文字を区別しない検索は維持する。
- 選択範囲内の検索・置換では、検索結果へ移動しても最初の対象範囲を維持し、範囲外の本文を変えない。
- 検索語・置換語の履歴を再利用できる。正規表現と複数行検索は次の段階とし、同時に詰め込まない。
- **完了の目安：**横書き／縦書きの両方で、検索→次へ→一件置換→Undoが同じ条件で動く。IME入力中に本文へフォーカスを奪わない。

### E2 文字コード・改行コードの表示と指定

**既存の「ファイルエンコードの対応」を具体化する。**まずUTF-8（BOM有無）、UTF-16 LE／BE、
Windowsの日本語文書で使うCP932を対象とし、対応範囲を表示する。

- ステータスバーに現在の文字コードとLF／CRLF等を表示し、クリックで選べる。
- 「指定文字コードで開き直す」と「指定文字コードで保存する」を別の操作として示す。
- 自動判別が曖昧なら候補と読み取り例を出し、文字化けした内容を確定しない。未保存本文がある開き直しでは保存・取り消しの道を残す。
- 保存先の文字コードで表せない文字を`?`等に黙って置き換えない。該当文字を示し、UTF-8保存などを選べる。
- **完了の目安：**日本語・絵文字を含むファイルで、読み取り／変換／保存／再読み込みの結果を確認できる。無指定の保存では元の形式を維持する。

### T1 Terminalの出力検索

**困る場面：**出力が長いと、ホイールだけで以前のエラーを探す必要がある。
スクロールバックとコピーは既にあるので、その履歴を検索できるようにする。

- 対象はフォーカス中のTerminalの画面と保持している履歴。上段・下段のどちらにも同じ入口を設ける。
- 前／次、一致箇所の強調、大小文字の区別を用意する。ヒットした行までスクロールする。
- Ctrl+Shift+Fを候補とし、Ctrl+Fはシェルへ渡す。要件11.7の「編集器が先に取るキー」を採用時に追記する。
- **完了の目安：**画面外の過去の出力へ移動し、その箇所をコピーして本文へ戻れる。履歴上限から消えた出力は検索できないことを区別する。
- 参考：[Windows Terminalの検索](https://learn.microsoft.com/en-us/windows/terminal/search)。このEditorでの優先度はレビュー判断。

### E3 選択・行移動・インデントの基本操作

**困る場面：**段落や項目の順番を変えるたびに、範囲選択・切り取り・移動・貼り付けが必要になる。

- ダブルクリックで単語、行番号クリックで論理行を選択できる。日本語の単語境界は既存のAlt+F／Alt+Bの規則とそろえる。
- 選択した論理行の前後移動、複製、削除を用意する。縦書きでは画面の上下ではなく文書順の前／後として説明する。
- 複数行へのTab／Shift+Tabでインデント／解除。通常入力のTabとの違いが分かるようにする。
- 改行時のインデント維持、Markdownの箇条書き・引用の継続を追加する。空の項目でEnterを押すと継続を終える。
- **完了の目安：**一操作を一回のUndoで戻せる。ライブプレビューでも記号を壊さず、同じ文書を表示している別ペインの位置も追従する。

### T2 Terminalの貼り付け・終了操作の整理

**困る場面：**改行を含む文字列を貼るとコマンドが実行され得る。タブを閉じる／シェルを替えると、
その中の作業も終わる。通常の文書タブとは結果が違うので、その違いを操作時に分かるようにする。

- Ctrl+Shift+Vを貼り付けの別入口として用意する。既存のCtrl+Vとbracketed paste対応は維持する。
- 複数行貼り付けの確認を設定で選べるようにする。確認する場合は行数と内容を見せ、キャンセル時は何も送らない。
- 下段の下書きから送る経路にも適用範囲を定める。現在の「末尾改行の有無で実行が決まる」規則を黙って変えない。
- 動作中のシェルを閉じる／切り替える前に確認できる設定を設ける。既に終了したシェルには確認を出さない。
- **完了の目安：**Ctrl+Cは常に割り込みとして届く。閉じる操作を取り消すとシェルと出力が保たれる。シェルの生存だけで子コマンド実行中と断定しない。

## P2：次に追加したい機能

### E4 現在位置表示と指定行への移動

総行数とは別に、現在の論理行・行内位置を表示する。Ctrl+G等から行番号を指定して移動できる。
**他の人や別の道具から「何行目」と示された箇所へ行くため**の機能。横書きと縦書きで同じソース位置へ
着地し、サロゲートや結合文字の途中へ入らない。折り返しの表示行と論理行を混同しない。

### E5 Explorerの再表示・整理

**大きなツリーから近くの原稿へ行くため**に、現在の文書をツリー内で示す操作、すべて折り畳む操作、
明示的な更新を用意する。「Reveal in Explorer」はWindowsのExplorerなので別に置く。
まずこの3つを追加し、外部作成・名前変更の自動反映や検索対象の除外指定は次の段階とする。
更新しても選択・展開状態を可能な範囲で保持する。クイックオープンを復活させる案ではない。

### E6 閉じた文書タブを開き直す

**誤って閉じた直後に、同じ場所へ戻るため**に、Ctrl+Shift+T等で最後に閉じた文書を再表示する。
文書・キャレット・スクロール・表示モードを戻す。既存のファイル履歴やAlt+←の閲覧履歴とは区別する。
作業コピーを残した文書はそれを使い、明示的に破棄した未保存本文は復活させない。
Terminalのプロセス復元はこの機能に含めない。

### T3 シェル設定の画面編集と開始フォルダ

**WSLのディストリビューションやシェルを足すとき、設定ファイルを探して編集しなくて済むようにする。**
名前・実行ファイル・引数・開始フォルダを設定画面で編集し、複製・並べ替え・既定指定ができるようにする。
「このフォルダでTerminalを開く」をExplorerに置き、現在の文書の親フォルダも選べるようにする。
空白・日本語を含むパスを扱い、既存のシェルの作業場所は変えない。WSLの開始位置はWindowsのパスと
同じ文字列を渡せばよいとは限らないので、シェルごとに確認する。
参考：[Windows Terminalのプロファイル設定](https://learn.microsoft.com/en-us/windows/terminal/customize-settings/profile-general)。

### T4 Terminalの履歴閲覧・コピー・リンク操作

**ログを読み返して原稿やメモへ持ち帰るため**に、「最新の出力へ戻る」、履歴のページ送り、保持行数の設定、
選択テキストのコピーを補強する。出力を遡っている間は勝手に最下部へ戻さず、新しい出力があることを示す。
自動折り返しで生じた行境界と実際の改行をコピー時に区別する。URLはCtrl+クリックで開けるようにし、
通常の選択操作と分ける。画面消去と履歴消去は名前・効果を区別し、シェルを再起動しない。

### E7 保存版との差分表示

**推敲でどこを書き換えたかを確かめるため**に、編集中の本文と保存版の差分を読めるようにする。
最初は比較表示に絞り、部分的な取り込み・巻き戻しは後回し。外部変更の競合時にも比較へ入れるとよい。
比較しただけでは本文を変更せず、通常の保存・Undoに影響しない。Git連携や自動マージは前提にしない。

### C1 ショートカット一覧と操作の検索

**機能はあるのに入口を知らない状態を減らす。**まずメニューに書いたショートカットと実際の動作をそろえ、
Editor／Terminal／検索欄ごとの一覧をアプリ内で参照できるようにする。
次に、操作名から探して実行する小さな一覧を追加候補とする。ショートカットの変更はさらに後でよい。
既存のCtrl+D・Ctrl+K・Alt+Y等の割り当てを他のEditorに合わせて上書きしない。
**確認項目：**画面確認ではCtrl+Wでタブが閉じず、メニューにはCtrl+Wと表示されていた。
フォーカス位置・キーイベントを調べて、表示と動作の不一致かを最新ビルドで再確認する。

## P3：用途が固まってから

- **E8 見出しの折り畳み・ブックマーク：**長い章の一部分だけを見て書くため。折り畳みは表示だけに効かせ、保存内容を変えない。既存アウトラインで足りるかを先に試す。
- **T5 全画面TUIの互換性：**マウス追跡、フォーカス通知、代替画面、リサイズ、日本語・絵文字の幅、キー送信を代表的なTUIで確認する。現在はモードを記録していても入力送信まで結び付いていないものがある。TUIを日常利用するならP1へ上げる。
- **C2 表示設定のプリセット：**文字・余白・紙色等を名前を付けて保存し、読み返し用／執筆用に切り替える。要件13.2の具体化。本文や文書ごとの単語チェックモードを勝手に変更しない。
- **C3 実行ファイルのアイコン：**本ファイル冒頭の残りを完了させる。日々の編集への効果は小さいが、配布前には必要な仕上げとして扱う。
- **C4 GPU化：**入力・スクロール・長文の初回表示を測り、組版・描画・画像転送のどこが待ち時間を作っているかで決める。GPUを使ったことではなく、文字欠けや位置ずれなしに待ち時間が減ったことを完了条件にする。

## 既に採用済みの要件との順序

要件7.8のルビ・傍点・縦中横、要件7.10の縦書きPDFは、今回の候補とは別に採用済み。
最新ソースでは対応実装を確認できていない。**縦書き原稿を主用途にする現在の方針では、7.8を
上のP1と並行する優先課題、7.10をP2相当**として扱うのがよい。汎用Terminalの拡張だけが
先行して、原稿を書くために必要な既存要件が後ろへ押し出されないようにする。

一般的な編集操作の比較には[VS Codeの基本編集](https://code.visualstudio.com/docs/editing/codebasics)も参照した。
ここに書いた採否・優先順位・完了条件は、このEditorの用途と現在の実装に対するレビュー提案である。


# フォントの設定
今、見出しや本文のフォント設定は、フォントとサイズと色だが、太字・斜体・取り消し線にも対応した方が良い

# ルビなどのMarkdownではない記号対応
これは標準ではないので、ON/OFFが出来た方が良い


# 後から選択して箇条書き対応
数行を選択して、それを箇条書きとして数字や・を挿入する機能
また、数字の箇条書きの開始数字の変更、箇条書きの・の文字を変更する機能


# Markdownのリンク表示
リンクは対象外にしてきましたが、フルパスでリンク可能な物はリンクしてもいいと思いました。
また、Obsidian表記でファイルの所在が分からない場合は、リンク表示しますが赤字にしてリンクがきれているという示し方でもいいと思います


## リンク表示を末尾へ（2026-09-11、書き手の承認）

**E12。**上の「Markdownのリンク表示」は書き手が2026-09-11に足したもので、
**E10→E11→E12**の順に置いた（書き手の選択）。E11と同じで**書く手より読み返す目に
効く**——原稿を書いているあいだリンクは踏まない。切れたリンクを赤で出すことは、
**働いていない状態を画面に出す**という単語チェックモード要件 2.1.1 の2番と同じ根なので、
そこへ着くまでの道（所在を確かめる＝ディスクに触る）は**開いていないフォルダは読まない**
（要件 5.1）に当てて決めること。

## この3件を並びに入れた（2026-09-10、書き手の承認）

上の3つは書き手が後から書き足したもので、レビューの一覧（E1〜E8）には無い。
**E2のあと、この順**で進める（書き手が「3件を並びに入れてからE2の②」と選んだ）。
番号は空いている続きを取った——`E8`はP3の見出し折り畳みが既に使っている。

- **E9 ルビなどの記号のON/OFF。**要件 7.8 で入れた`｜漢字《かんじ》`と`《《傍点》》`は
  **Markdownの標準ではない**——書き手の言うとおり、切れなければ「この編集器でしか
  正しく見えない書き方」を強いることになる。3件の先頭に置くのは、**7.8の道がまだ
  新しいうちに切り口を付ける**のがいちばん安いからで、後から足すと「記号を読む場所」が
  増えたぶんだけ入口も増える。切ったときは**記号をそのまま字として出す**（原稿の
  バイト列は変えない）のが要点。
- **E10 後から選択して箇条書き。**E3の④（`Tab`の入れ子）と⑤に当たる`Enter`の継続で
  **箇条書きを「続ける」道はできた**ので、残りは**後から「する」道**である。
  E3のコード（`lines`の region、記号と番号の付け替え）がそのまま効くので、
  E9の次に置く。開始番号と記号の変更もここに入る。
- **E11 フォント設定の太字・斜体・取り消し線。**表示設定（要件 13）の拡充で、
  **書く手より読み返す目に効く**ぶん、3件のうちでは後ろになる。DirectWriteの
  字体は`DWRITE_FONT_WEIGHT`／`STYLE`で持てるが、**取り消し線は組版ではなく線を引く
  仕事**なので、ここだけ描く側（`directwrite_render`）の話が混ざる。

---

# 次に進める順（2026-09-08、書き手が承認）

上のレビュー（優先順一覧）を、**縦書きで小説を書く人**という方針（要件定義 3）に当てて
並べ直したもの。**書き手が「その方針で次回進めましょう」と承認している。**

```
0. 機能追加の前   一致条件のずれ / Ctrl+W / 退避2件   ← 2026-09-09に4件とも済み
1. 要件 7.8       ルビ・傍点・縦中横                  ← 2026-09-09に済み
2. E1             文書内検索・置換の操作強化          ← 2026-09-10に済み（①〜⑤、下）
3. E4の残り       Ctrl+G（現在位置の表示は既にある）    ← 2026-09-10に済み（585テスト）
4. E3             行移動・箇条書き継続・ダブルクリック  ← 2026-09-10に済み（①〜④）
5. E2             文字コード・改行コード                ← 2026-09-10に済み（①〜⑤、下）
6. E9             ルビなどの記号のON/OFF               ← 2026-09-10に済み（646テスト、下）
7. E10            後から選択して箇条書き               ← 2026-09-11に①（下）
8. E11            フォント設定の太字・斜体・取り消し線   ← 書き手の追記（2026-09-10）
9. E12            Markdownのリンク表示                  ← 書き手の追記（2026-09-11）
```

## いまの位置（2026-09-10、セッションの区切り・2回目）

**この日に済んだもの**（2つのセッション）：E1の④⑤（履歴・正規表現）→ E4の残り
（`Ctrl+G`）→ E3の①〜④（語と行を選ぶ・行を動かす・Enterの継続・`Tab`の入れ子）→
**E2の①②③**（読める文字コードを増やす・指定文字コードで開き直す・名前を付けて保存で
文字コードを選ぶ）。**書き手の画面確認を全部通っている**（報告と直しは各節に残してある）。
638テスト、fmt差分なし。

**次にすること**：E2の④——**自動判別が曖昧なときに候補と読み取り例を出す**。
①の判別は「印→UTF-8→CP932、どれでもなければ断る」で、いま断られたファイルには
**選び直す道が無い**（②の一覧は開いてからでないと押せない）。④はそこに候補を出す
仕事である。**E2が終わったらE9〜E11**（書き手が2026-09-10に足した3件、上の「並びに
入れた」）。

**E2で残っているもの**：④のほかに、**改行コードの切り替え**（要件 E2 の1つ目
「LF／CRLF等を表示し、クリックで選べる」の後半）。表示は出ているが押して変える道は
まだ無い。**③で保存の節をステータスバーから外した**ので、改行も「読み方の帯」ではなく
名前を付けて保存の側へ置くのが筋である（欄をもう1つ足す形）——**④のあとに書き手へ
訊くこと。**

**この日に学んだことで、次の人が使えるもの**：

- **知らせの場所を1つにするなら、そこへ書いている全部を数えてから**にする
  （`set_render_status`は65か所あり、1打鍵ごとの計測が同じ口から出ていた）。
- **「効かない」の半分は「効いたのが見えない」。**保存を断っていたのに、その言葉は
  縦書きの紙の上にしか出ていなかった——横書きの書き手には一言も届いていなかった。
- **知らせは畳み方まで決めて置く。**出す場所を決めただけで消す場所を決めていなかったので、
  「一度出ると出っぱなし」になった。**時間では消さない**（読んでいる最中に消えるのは、
  出ていないのと同じ）——**書き手が次へ動いたら**畳む：打つ・別のタブへ移る・別のものを
  開く・新しいタブを開く、の4つ。
- **診断ログに1行足しておくと往復が減る。**`enter`・`goto`・`lines`・`encoding`はそのために
  足した。**とくに「求めが届いた」行**（`file save asked … vertical=`）——書けたか断られたかの
  行しか無いと、**「効かなかった」と「鍵が届いていない」を見分けられない**。
- **`was=`と`as=`を一緒に書く。**「開き直したのに何も変わらない」は、同じ文字コードを
  選んでいただけだった——**答えが正しいときほど、言葉が足りないと効かなく見える。**
- **画面のことは画面で測れる。**`caret_geometry`と`render_tiles`を試験から呼べば、
  「入力中に右へズレる」も「入れ子の記号が本文にくっつく」も画素で決着する。
- **大文字で届く鍵は、修飾も見る。**`Ctrl+Shift+S`は`event.text == "S"`として届くので、
  `"s" || "S"`とだけ書いた行が名前を付けて保存を飲み込んでいた（`Ctrl+W`と同じ穴で2度目）。

**この日いちばん効いたのは、書き手が削ったこと。**③の問いは私の案では
「入らなかった字を全部並べて3つ選ばせる」だったが、**3度正されて「断って、UTF-8で保存
してくださいと言うだけ」**になった（数えない・替えない・訊かない）。**作りが正しくなると
コードは減る**——`save_replacing`も`encode_replacing`も`Unmappable`の向きも`Written`の
3状態も、書かずに済んだ。同じことが保存の場所でも起きた：ステータスバーの一覧は
**読み方**だけになり、書き方は名前を付けて保存の欄へ移った。

## 0番の中身（コードに当てて確かめてある。**4件とも2026-09-09に済んだ**）

**どれも「画面が言っていることと動作が違う」種類**で、単語チェックモード要件 2.1.1 の
2番（**働いていない状態は画面に出ていなければならない**）と同じ根である。

1. **検索と一件置換の一致条件がずれている。**`main.rs`の`replace_in_pane`が`==`の完全一致、
   `find.rs`は`eq_ignore_ascii_case`。`alpha`で`Alpha`が見つかるのに、置換は飛ばす。
   **単語チェックモード要件 7.7 に書いた規則**（「Ctrl+Fで見つかるものが色で出る」＝畳み方は
   1つ）が、置換の道で破れている。**0番の中でもこれが先。**
2. **Ctrl+Wを受ける処理が無い。**`ui/editor-pane.slint`の2か所（ペインの`⋮`とタブのメニュー）が
   `Ctrl+W`と表示しているが、それを受ける行がどこにも無い（`w`を見ているのはAlt+Wだけ）。
   **割り当てるか、表示を消すか。**
3. **自動退避Off時の削除がキューを通らない**（`saving.rs`の`discard_all_work_copies`）。
4. **終了時の最後の退避失敗が診断ログにしか出ない**（`main.rs`の`writer.finish()`のあと）。

## 要件 7.8 の結果（2026-09-09）

556テスト成功、fmt差分なし。**READMEの「現在できること」に何をどう作ったかがある**ので、
ここには決めたことだけを残す。

- **傍点の綴りは`《《…》》`＋青空文庫の注記形式**（§15の「実装時に決める」に対する書き手の決定）。
  なろうのルビ代用（`｜強調《・・》`）は採らない——ルビとして振りたい書き手の意図を編集器が
  奪うことになり、見分けがつかない。
- **ルビの位置は「行自身の箱の中」**。要件は「表示設定に入れる」としか言っておらず、
  どこを基準にするかは書いていなかった。行の外へ出すと縦書きの1列目が切れる。
- **ルビが親文字より長いときは、はみ出して組む**（幾何は動かさない）。折り返さないので
  2行にはならない。**親文字を広げる案は採っていない**——広げれば字送りが動き、
  ルビの有無で本文の位置が変わる。読みが極端に長い原稿でどう見えるかは、画面で見てから。
- **本文文字数の設定は紙のシートに置かない**（`Count ruby`は☰ の Settings → General の MARKUP）。
  シートが持つのは組み方で、これは数え方である——縦書きの原稿と横書きの原稿で、
  投稿サイトへ出す字数の決まりが変わるわけではない。
- 副産物の実測は技術検証 6.30：**同じ箱のインスタンスを隣り合う範囲に張ると、DirectWriteは
  1つの箱として扱う**。縦中横の`202`が3マスではなく1マスに収まっていた。

## E1の刻み方（2026-09-09）

**1回で全部を入れない。**E1は6つの箇条書きで、うち3つは画面に出るもの、2つは
探し方そのものである。**論理から**（書き手の選択）：

```
① 件数と現在位置・キー操作      ← 2026-09-09に済み
② 探し方の3つと、その入切        ← 2026-09-09に済み
③ 一致箇所の表示                ← 2026-09-09に済み（566テスト、書き手の求めで前倒し）
④ 検索語・置換語の履歴          ← 2026-09-10に済み（581テスト）
⑤ 正規表現                      ← 2026-09-10に済み（572テスト、書き手の求め）
```

正規表現は⑤として2026-09-10に入れた（書き手の求め）。**E1が「次の段階」と
言ったのは、詰め込まないためであって、要らないという意味ではない。**

### ①でしたこと

- **`find::tally`が件数と現在位置を一巡で答える。**別々に数えれば同じ本文を2度歩く。
  数え方は`replace_all`の置き換え方と同じ（重なる一致は二度数えない）——**ここが
  食い違うと、「12件」と言った直後の全置換が11件を報せる。**
- **打っているあいだはカーソルに触らない**（`count_in_pane`）。E1の完了の目安に
  ある「IME入力中に本文へフォーカスを奪わない」は、**変換中に本文が飛ばない**こと
  でもある。動かす鍵はEnter・Shift+Enter・F3。
- **`F3`／`Shift+F3`は本文からも効く。**語が無ければ帯を出す。帯が閉じているあいだ
  の答えは紙の上の報せへ出す——**効いたのが見えないのは、効かないのと見分けが
  つかない**（2026-08-28の`Ctrl+Tab`と同じ根）。診断ログに`find`の行を足したので、
  「効かない」と言われたら往復せずに切り分けられる。
- **帯が開いていれば、探すのは帯のあるペイン**（`find_target`）。4つの入口が
  `focused_pane`を見ていたので、帯を出したまま隣のペインを触ると別の文書を数えていた。

### 書き手の画面確認で出た3件（2026-09-09、直した）

1〜2と5（縦書き）は通った。残りは**どれも同じ傷**——「効いたのが見えない」。

1. **探す語が窓に1つしか無かった。**Pane1で探し、Pane2で探し、Pane1へ戻ると
   F3が**Pane2の語**を探していた（書き手の報告）。診断ログに残っていた：
   `find step pane=pane1 next needle=4 nothing`。**語・置換語・帯が言うことを
   `PaneScreen`の行へ移した**（面ごと）。帯はその文書を開いている面の中に出るの
   だから、語もそこにある。
2. **「下の報せ」が出なかった。**`render-status`は**縦書きペインの紙の上の帯**で、
   ステータスバーではない（`pane.vertical ? root.render-status : ""`）。置き場所を
   間違えていた。**ステータスバーに`count-find`を足し**、`「白猫」3 / 12`を出す。
   `update_status`で数え直すので、打鍵のたびに正しい。
3. **F3で帯が出なかった。**語が空のときだけ帯を出す作りだが、語が窓に1つで
   消えなかったので、空になることが無かった。1を直したことで届く。

### 書き手の画面確認で出た3件（2回目、2026-09-09）

1. **IMEが検索欄に重なる。**1度目の直しは効かなかった——`select-all`は空の欄で
   何もしない（Slintの`move_cursor`が`text.is_empty()`で戻る）ので、**開いた直後の
   空の帯では一度も送っていなかった**。`set-selection-offsets`へ替えた（空でも届く）。
   あわせて**プログラムから鍵盤を渡すときはIMEを横書きへ言い直す**——`changed
   has-focus`は生まれたときから焦点を持っている欄では起きない。診断ログに
   `ime settle`／`ime field`を足したので、次に「重なる」と言われたら往復せずに
   切り分けられる。理由は技術検証6.31。
2. **縦中横に入切を付けた**（書き手の決定：「気持ち悪く感じました」）。表示設定の
   Typography、縦書きのシートにだけ`Tate-chu-yoko`。既定は`Upright`のまま。
3. **タブをまたぐと前の語を引きずる**のは意図どおりで、書き手の確認も取れた
   ——語は「いま何をしているか」であって文書の性質ではない（そこが単語チェック
   モードとの違い）。

**この直しが壊したもの**（書き手の報告、同日）：名前の変更で「一度拡張子を除く
名前が選ばれるが、次の瞬間に拡張子まで広がる」。送り直しの時計が`select-all`を
掛けていた。**送り直しは選び直しではない**——時計は呼び手が頼んだ選択を出し直す。

**副産物**：`set-selection-offsets`はバイト位置なのに、`stem_length`は文字数を
渡していた。**日本語の名前の変更は頭の1文字しか選べていなかった**（`第一章.md`で
`第`だけ）。直した。

### ②でしたこと（2026-09-09）

- **`find::Rules`に集めた。**大小の別・単語単位・範囲の3つを1つの型にして、
  検索・件数・全置換・一件置換の4つが同じ規則を受け取る。0番で直した
  「一致の意味が2つあった」を、切り替えが増えても繰り返さないための形である。
- **単語単位は英数字の語のためのもの**と決めた（日本語には語の切れ目が無い）。
  字種の変わり目で切る案は採らない——理由は`Rules::whole_word`に書いた。
- **範囲は押した瞬間の選択**。選ばれていなければ入らず、帯がそう言う。
  輪も範囲の輪で、置換で長さが変われば範囲の終わりもずれる。
- 帯に`Aa`／`W`／`[ ]`の3つ（`ToggleChip`、`tip`で何のことかを言う）。
  **`Match case`と書ける幅は帯に無い**——半分の窓でも収まる必要がある。
- 試験3本（大小の別、単語単位の当たり方、範囲の輪と範囲外を触らないこと）。

### 書き手の画面確認で出た3件（②のあと、2026-09-09）

1. **見出しの中の数字が本文の大きさで立っていた。**縦中横の箱は見出しの字送りを
   取っているのに、中の数字だけが本文の書式で組まれていた。**ルビと傍点も同じ
   取りこぼし**だったので、3つとも「その行の字の大きさ」で組むようにした。
2. **ルビが本文に重なる／小さい。**画素で測って決着（技術検証6.32）。**縦書きの
   行間100%には、ルビの入る場所が無い**（本文へ5px食い込む）。`Ruby room`を入れ、
   行送りの下限を「本文1つ＋ルビ1つ」にした。**縦書きのシートだけ**——横書きは
   素の行送りがもう空きを持っており、足すと1行目のルビがブロックの外へ出て消える。
   **大きさは`Ruby size`で上げられる**（既定50%）。空きは比率から決まるので、
   上げれば行間も一緒に広がる。
2c. **`Ruby room`は外した**（3往復目、書き手の画面）。「行間が広すぎます」「ルビの横に
   まだ広いスペースがあります」——**行間の問題ではなかった。**真犯人は2つ：読みの行箱が
   帯より高いこと（横書きの重なり）と、帯を行の箱の端に置いていたこと（縦書きで字から
   離れる）。両方直したら、**行間を1画素も動かさずに**両方の面が正しくなった。
   設定と配管は全部外してある。技術検証6.32に3往復の記録がある。

2b. **画面を見て測り直した**（`Ruby_横書き.png`／`Ruby_縦書き.png`、同日）。
   横書きは重なり、縦書きは行間が広がりすぎだった。**真犯人は読みの行箱**で、
   DirectWriteの素の行箱は帯より3割高く、下へはみ出していた（横書きで7px）。
   帯の厚みに揃えたところ、**横書きは空きを足さずに直り**、縦書きの要る行間も
   `1.0＋比率`から`0.9＋比率`（ルビ50%で140%）まで減った。技術検証6.32。

3b. **範囲は選び直しに付いてくる**（書き手の指摘 2026-09-09、決着）。「検索の度に
   範囲を取得し直した方が自然ではないですか」——そのとおりだが、**素直に毎回
   取り直すと2回目で潰れる**（検索が一致を選ぶので、その一致が範囲になる。E1が
   「最初の対象範囲を維持」と書いているのはこれを避けるため）。**変えたのが誰かで
   見分ける**ことにした：検索が置いた選択を覚えておき、今の選択がそれと違えば
   書き手の手が入ったとみなして取り直す。

3. **`[ ]`の選び直しが不安。**いまは「押した瞬間の選択」を保つ（E1が「最初の対象
   範囲を維持」と言っている）。選び直しても範囲は動かず、切って入れ直せば新しい
   範囲になる。**不安の正体は範囲が見えないこと**だと思うので、③（一致箇所の表示）で
   範囲そのものも描くのが筋——それでよいか決めてほしい。

### ③（2026-09-09、書き手の求めで前倒し）

「ヒットした語がすべてハイライトされた上で、対象が動く方がいい」。**色は選択と
同じもので、弱いほう**——別の色を足すと「選ばれている」と「見つかっている」が
別のことに見える。組んだ本文の位置へ写して（`utf16_at_source_byte`）、選択と
同じ道で矩形にする。**範囲内検索では範囲の中しか光らない**ので、`[ ]`が何を
しているかがそのまま目に見える（書き手の「効いていないように見えます」への
答えでもある）。診断ログにも範囲を出した（`scope=120..480`、`scope … took …`）。

### ③の仕上げ（2026-09-09、書き手の求め4件）

1. **一致の矩形が語ではなく行を覆っていた**（`検索.png`）。`selection_rects`が
   取るのは**「始まりと長さ」**で、終わりではない（技術検証6.33）。
2. **色が消せなかった。**語は面に残る（F3のため）ので、**光るのは帯が出ている
   あいだだけ**にした——`Esc`が消す鍵でもある。
3. **範囲を薄く出す。**検索が選択を動かすので、範囲は選択では見えない。
   いちばん薄い帯として、一致と現在地の下に敷く。
4. **F3で帯が出る**（「何で検索しているのか分かります」）。**鍵盤は本文に
   置いたまま**——静かに出た帯に`Ctrl+F`を押せば、閉じずに鍵盤がそこへ移る。
5. **`Clear`**で語・置換語・3つの入切・範囲をまとめて捨てる。

### ⑤ 正規表現（2026-09-10、書き手の求め）

「ワイルドカードや正規表現は対応した方がいい」「正規表現は、ON/OFFでいい」。

- **切り替えは1つ**（帯の`.*`）。ワイルドカードは別に持たない——`*`は`.*`、
  `?`は`.`で書ける。**同じことを2つの切り替えで言うと、どちらが効いているかを
  画面に出す仕事が増える。**
- **`find.rs`を`Search`へ組み替えた。**正規表現を入れると「一致の長さは針の
  長さ」が崩れる。長さを前提にした関数を4つ並べたままにすると、次に規則が増えた
  ときどれかが取り残される——**0番で直したのがまさにそれだった。**いまは検索・
  件数・色・置換が同じ一巡（`walk`）を歩く。
- **副産物：見つかるものは必ず数の中にある。**以前は`next`だけが別の道を
  歩いていて、`あああ`の2文字目から`ああ`を探すと数に無い一致へ着けた。
- **書きかけの正規表現に「0件」と答えない**（`(`は誤りであって、見つからないのとは
  違う）。帯とステータスバーが「正規表現が正しくありません」と言う。
- `^`と`$`は行の端、`.`は改行に当たらない。置換の字はそのまま（`$1`は解さない）。
- 依存は`regex 1.13.1`——**Slintがすでに連れてきている**ので増えていない。

### ④ 検索語・置換語の履歴（2026-09-10）

**帯には何も足さない**（書き手の選択：「↑↓だけ（幅を足さない）」）。

- **↑と↓**。検索欄では探した語、置換欄では置き換えに使った語。一行の欄で
  この2つは何も起こさない鍵なので、割り当てても書き手から取り上げるものが無い。
  ②で「`Match case`と書ける幅は帯に無い」と書いたのと同じ理由——**釦を1つ足せば、
  半分の窓で最初にはみ出すのはこの帯である。**
- **打ちかけの字は返ってくる。**↑で遡った書き手が↓で戻ると、遡る前に打っていた
  字がそこにある。**履歴を覗くことと、打った字を捨てることは別である。**
- **列を歩いている途中かどうかは、最後に置いた字と欄の字が違うかで見分ける。**
  打鍵のたびに知らせてもらう必要が無く、③で範囲の取り直しに使ったのと同じ
  見分け方である（**変えたのが誰かで見分ける**）。
- **覚えるのは「次へ」と言った回の語**（Enter・Shift+Enter・F3・置換）。打っている
  あいだの1字ずつを覚えると、`白`『白猫』の途中の字が10件をすぐ埋める。
  **見つかったかどうかは問わない**——見つからなかった語こそ打ち直したくない
  ものである（次の一手は、たいていその語を少し変えてもう一度探すこと）。
- **読めない正規表現は覚えない。**`Search::new`が断った語は、探し方として
  成り立っていない。
- **窓に1つの列**（面ごとではない）。探し方の切り替えは面ごとだが、それは
  「いまこの文書で何をしているか」である。履歴は打ち直さないための列で、
  隣の面で探した語を持ってこられないなら列が2つある意味が無い。
- **10件、同じ語は1つ**（下書きの履歴と同じ規則、要件 12.4）。
- **次に起動しても残る**（書き手の選択）。セッションに`needle:`／`replacement:`で
  書く。**書き出すのはセッションを書くときだけ**——語を覚えるたびに書けば、
  F3のたびにファイルが1つ書かれる。**改行を含む語は書かない**（1行1事実が
  破れると、その1語で前のrunの並びが読めなくなる）。
- 試験9本（列の規則3つ、↑↓の往復、端で止まる、打ち直しで畳む、覚えると畳む、
  セッションの往復、改行を含む語）。

### 画面で確かめてほしいこと（④）

1. `Ctrl+F`で`白猫`を探しEnter。`Esc`で閉じ、もう一度`Ctrl+F`で**↑**
   ——`白猫`が戻るか。件数もその場で出るか。
2. 何か打ちかけて（`しろ`）**↑**、そのあと**↓**——`しろ`が返ってくるか。
3. **Pane1で探した語が、Pane2の帯の↑でも出るか**（列は窓に1つ）。
4. `Ctrl+H`の置換欄で↑——置き換えに使った語が出るか（検索欄の語とは別の列）。
5. **アプリを閉じて開き直し、`Ctrl+F`で↑**——前の run の語が残っているか。

## E3の刻み方（2026-09-10、書き手の選択）

**選ぶ→動かす→続く→Tab。**選ぶ手段が先に無いと、②の確かめ方がない。

```
① ダブルクリックで単語、行番号クリックで論理行   ← 2026-09-10に済み（588テスト）
② 選んだ行の前後移動・複製・削除                 ← 2026-09-10に済み（599テスト）
③ Enterでの字下げ維持と、箇条書き・引用の継続   ← 2026-09-10に済み（603テスト）
④ 複数行のTab／Shift+Tab・入れ子                ← 2026-09-10に済み（610テスト）
```

**②の鍵は書き手が決めた**：`Alt+↑`／`Alt+↓`で文書順の前／後へ、`Shift+Alt+↑↓`で複製、
`Ctrl+Shift+K`で行を消す。**縦書きでも`Alt+↑`が「文書順の前」**（書き手の選択）
——要件が「縦書きでは画面の上下ではなく文書順の前／後として説明する」と言っているとおりで、
画面の向きに合わせると`Alt+←`が閲覧履歴（要件 11.2）とぶつかる。

### ①でしたこと（2026-09-10）

- **語の切れ目は`Alt+F`／`Alt+B`の規則とそろえた**（E3がそう言っている）。
  `document::word_around`は`is_word`を使うので、日本語では句読点までのひと続きが語になる
  ——`黒猫が鳴いた。`で`黒猫が鳴いた`。**規則を2つ持たないことのほうが、語の切り方の
  精度より大事である**：`Alt+F`が止まらないところでダブルクリックが切れたら、書き手は
  どちらが本当かを覚えなければならない。
- **語・空白・記号の3つに分けた。**`。`を押して前後の空白まで選ばれるのは、押したものより多い。
- **行はまたがない**（改行は語の一部ではない）。**行末で押したら手前の語**——打ち終えた語の
  後ろを押すのは、その語を指しているのと同じことである。
- **行番号の欄を押すと、その論理行が改行ごと選ばれる。**改行まで持つのは、②で行を動かすとき
  改行を置いていくと2行が1行になるからである（`document::line_span`）。
- **押した先が欄かどうかは組版側でしか分からない**ので、`hit_test`と同じ一度の問いで答える
  （`TextEngine::in_number_column`、`PaneHit`）。欄の中の点でも返るバイトは本文の位置
  （その行の頭）なので、行を決めるのはそのバイトである。
- **そのまま押したまま動かせば行単位で伸びる**（`line_drag`）。1画素のぶれで行の選択が字の選択へ
  変わると、押しただけのつもりが選び直しになる。離した合図が来ないことがあるので、
  次に本文を押した回でも畳む。
- **選ぶ道を1つにまとめた**（`select_source_range`）。検索の一致も、語も、行も、選ばれて
  いる状態としては同じものである——別々に組み立てていると、どれか1つがIMEの下書きを
  消し忘れる（それが起きるのは選んだ直後に打った1文字が消えるときで、原因からいちばん
  遠いところで見つかる）。`Ctrl+G`の着地もこの道に乗せ替えた。
- 試験3本（語の切れ目、行をまたがないこと、行の範囲が改行を含むこと）。

### ②でしたこと（2026-09-10）

**鍵は書き手が決めたとおり**：`Alt+↑`／`Alt+↓`で文書順の前／後へ、`Shift+Alt+↑↓`で
写す、`Ctrl+Shift+K`で消す。**縦書きでも`Alt+↑`が「前の行」**（画面の上下ではない）。

- **選ばれている行ぜんぶが1つの塊**（`document::selected_lines`）。カーソルだけなら
  その1行、選択が3行をまたいでいれば3行が一緒に動く。**終わりがちょうど行頭なら、
  その行は入らない**——選択の終わりは「そこまで」であって、次の行を指してはいない。
- **改行は行と行のあいだにある**（`rejoin`）。組み直しは本文だけを並べてあいだに
  改行を1つ置き、最後の改行は元の範囲が持っていたときだけ付ける——**末尾に改行の
  無い文書で最後の行を動かしても、改行が増えたり減ったりしない。**ここを素直に
  「範囲を入れ替える」で書くと、末尾で1行が2行になる。
- **末尾の行を消すときは、その手前の改行も。**行だけ消して改行を置いていくと、
  空の行が1つ増える。
- **動いた行が選ばれたまま**なので、押し続ければさらに動く。写しも同じで、
  `Shift+Alt+↓`を3回押せば3つ増える（写したほうが選ばれる）。
- **先頭の行を前へ、末尾の行を後へは動かせない**（`line_edit`が`None`）。何も
  起きないことは画面に出ている——行がそこにある。
- **普通の編集の道**（`draw_edit`）を通るので、取り消しは1回で戻り、同じ文書を
  出している別の面も付いてくる（E3の完了の目安）。**入れ替えは「消して入れる」の
  形なので、打鍵のように繋がらない**——`Alt+↓`を3回押せば取り消しも3回である。
- **写しは文書を増やす**ので、打鍵や貼り付けと同じ上限を通る（要件 8.2）。
- `Ctrl+K`ではなく`Ctrl+Shift+K`。`Ctrl+K`だけなら要件 11.4 のEmacs系
  （行末まで消す）と読める鍵である。`Ctrl`付きの矢印にもしない——`Ctrl+Alt+矢印`は
  隣のペインへ移る鍵（要件 11.3）。
- 診断ログに`lines`（`pane=pane0 MoveAfter region=… chose=…`）。
- 試験7本（前へ・後へ・末尾の改行・複数行・行頭で終わる選択・写し・削除）。

### ③でしたこと（2026-09-10）

書き手の選択2つ：**番号は1つ進める**（下の行は振り直さない）・**中身の無い項目では
印だけ消して字下げは残す**。

- **行の見方は`line_styles`のものをそのまま使う**（`enter_continuation`は`LineStyle`を
  受け取る）。ここで箇条書きかどうかを決め直すと、**画面が箇条書きとして組んでいる行を
  Enterが本文として扱う**という食い違いが起きる——①の「語の切れ目は`Alt+F`と同じ規則」と
  同じ考え方である。
- 継ぐのは**字下げ・引用の`>`・箇条書きの印**。タスクは**空の箱**で継ぐ（済んだ印を
  写すのは、まだしていないことを済んだと言うことになる）。番号は1つ進める。
- **下の行の番号は振り直さない**（書き手の選択）。途中に行を挿したときに下を書き換えるのは、
  打っていないところが動くことである。
- **中身の無い項目でEnterは、印だけ消して改行を入れない。**書き手が終えたいのは箇条書きで
  あって、行ではない。**印を持たない行はここへ来ない**——字下げだけの行でEnterが何も
  しないと、効かない鍵に見える。
- **頭の中で押されたEnterは、ただの改行。**行を押し下げたいだけの書き手に、印を写して
  返さない。
- **編集の道は1本**（`apply_span_edit`）。②の行の入れ替えと、この「印を消す」が同じ
  関数を通る——編集の入口が増えても、取り消し・別の面の追従・上限の見方は1つである。
- **`New Tab`の上でも打鍵として扱う**（`answer_new_tab`）。`Enter`は`text-input`を
  通らなくなったので、これを忘れるとEnterだけが「何も起きない鍵」になる。
- 試験4本（印を継ぐ・字下げだけ継ぐ・中身の無い項目・頭の中のEnter）。

### 書き手の報告（③のあと、2026-09-10）

「改行は入らないですが、字下げは残っていないように見えます」。

**動作は正しく、私の確認手順が悪かった。**診断ログ（`lines … EndItem region=…
chose=…`）を見ると、試された行は`- `／`1. `／`> `／`- [ ]`で、**どれも行頭にあり、
残す字下げを元から持っていなかった**——`region=1307..1310 chose=1307..1307`は
「3字消して0字残した」と言っている。入れ子（`  - `）なら2字残る。文書の中の行として
`line_styles`を通す試験を足して確かめた（1行だけを見る試験では、入れ子の深さが出ない）。

**あわせて、選択肢の書き方が二通りに読めていた**ので決め直してもらった：
**残すのは「項目の本文が始まっていた列」まで**（書き手の選択）——字下げに**印の幅ぶんの
空白**を足す。印は消えるが、書き始める場所は動かない：その項目に属する段落として
書き続けられる位置であり、Markdownとしてもそう読まれる。`- `なら2つ、`10. `なら4つ。
**引用の`>`は埋めない**——引用に「本文の列」は無く、終えた書き手が戻るのは本文である。

### 書き手の報告3件（③のあと、2026-09-10、ScreenShot『箇条書き1〜4』）

「箇条書きを入力すると、キャレットと文字が右にズレ、Enterすると元の位置に戻ります」
「折り返しは機能していますが、画面がズレます」「入れ子にする方法が分かりませんでした」。

**1と2は同じ根で、直した。**組版を実測して分かったこと（横書き、本文の左端 x=33）：

- 組み上がりの項目は x=77（余白＋1段）から始まり、記号は溝（33〜77）に立つ。
  **ここは書き手が「今のままでいい」と言った部分。**
- ところが**カーソルのある行は原文で出る**（要件 7.3.1）ので、`- `や`1. `が幅を持つ。
  段下げは記号が隠れている前提のままなので、**その行だけ本文が記号の幅ぶん右にあり
  （`- `で+14、`1. `で+22、`- [ ] `で+43）、行を離れると左へ戻っていた。**
  スクリーンショット4がそれで、`1. aaa`は左、書きかけの`2.`だけ右にある。
- 直し方：**編集中の行の行頭の記号を、箱で覆って溝に描く**（`Ornament::Markup`）。
  **箱は字を隠すためではなく、幅を取らせないためにある**——覆った字はそのまま溝に
  描かれるので記号は見えたままで、`1.`が丸印の位置に立つ。`Number`が`10.`を
  読み出して描くのと同じ道である。引用の`>`も同じく覆う（組み上がりで消える印だから）。
  **区切り線とフェンスは覆わない**：あれは行そのものが記号で、溝に立てるものが無い。
- 試験は**位置そのもの**を見る：`- 項目`／`10. 項目`／`- [x] 項目`／`> 項目`／
  `  - 項目`／`項目`の6つで、**触っている行と離れている行で本文の x が変わらない**こと。
- **残る動き**は2つ。記号が成立した瞬間の1段（＝組み上がりの位置そのもの）と、
  空の項目でEnterしたときに印が消えること（③で入れた動き）。

**3（入れ子）は次の④**。いまは行頭に空白を足すしかなく、それが分からないのは
道具の落ち度である——`Tab`／`Shift+Tab`をその鍵にする。

### 画面で確かめてほしいこと（E3の③）

1. `- 一つめ`でEnter——`- `が出るか。`9. 九つめ`なら`10. `か。`- [x] 済んだ`なら`- [ ] `か。
2. `> 引用`でEnter——`> `が出るか。
3. **中身だけ消した項目**でEnter——印が消え、**カーソルが項目の本文の列に残る**か
   （改行は入らない）。`- `なら2桁、`  - `なら4桁、`10. `なら4桁。`> `は行頭へ戻る。
4. 字下げした本文（行頭に空白）でEnter——同じ字下げが続くか。
5. **行の頭**（`- `の前）でEnter——印が写らず、ただ改行するか。
6. `New Tab`を開いてすぐEnter——文書になって改行が入るか。

### 画面で確かめてほしいこと（E3の②）

1. `Alt+↑`／`Alt+↓`で行が入れ替わり、**動いた行が選ばれたまま**か（押し続けて動くか）。
2. 3行ほど選んで`Alt+↓`——**3行が一緒に**動くか。
3. **末尾の行**を`Alt+↑`で上げてから`Alt+↓`で戻す——空の行が増えないか。
4. `Shift+Alt+↓`で写し、`Ctrl+Shift+K`で消す。**それぞれ1回のUndoで戻るか。**
5. **縦書きの面**で`Alt+↑`が「前の行」か（画面では右へ動く）。
6. 同じ文書を2つの面に出しておいて、片方で動かすと**もう片方も追うか**。

### 書き手の画面確認で出た3件（①のあと、2026-09-10、直した）

「語のダブルクリックのところが不安定に感じました」「英語では単語選択にならない感じ」
「範囲選択が外れなくなったりもしました」——**2つの原因で、3つに見えていた。**

1. **訊いていた場所が「字」ではなく「境目」だった。**当たり判定が返すのは
   カーソルの置き場所で、点が字の後ろ半分にあれば**次の字の頭**になる。`cat`の
   `t`の右半分を押すと、語ではなく次の空白を指していたことになる——**英字は
   細いので、狙って避けるほうが難しい。**日本語は字が広いぶん起きにくく、それが
   「英語では」という形で出た。`HitTest::utf16_letter`（`textPosition`そのもの、
   後ろ半分でも送らない）を足し、**語を選ぶときだけそちらで訊く。**カーソルを
   置くのは今までどおり境目である。
2. **2回目を離した合図が、選んだ語を崩していた。**ダブルクリックのすぐ後ろから
   `selection-end`が来て、カーソルを押した点へ置く——**アンカーは語の頭のまま
   なので、語の途中までの選択が残る。**押した場所で残り方が変わるので「不安定」に
   見え、そのまま消えないので「外れない」にも見えた。`word_drag`（`line_drag`と
   同じ形の印）を置き、**押し直すまでこの選択を守る**。副産物として、
   **ダブルクリックしてから引くと語ごと伸びる**——行番号から引くと行ごと伸びるのと
   同じである。
3. 試験を2本足した：`utf16_letter`が字の後ろ半分でも動かないこと（組版側、
   実際にDirectWriteへ訊く）と、空白の扱い。

### 書き手の画面確認で出た2件（直しの直し、2026-09-10）

「white catは2語です」「契機がわからないのですが、選択がはずれなくなります」。
**私の直しが両方を作っていた。**

1. **離した合図で語を広げていた。**`word_drag`を置いたところまではよかったが、
   `End`でも「押した語と離した点の語の合併」を取っていた——離した点が隣の語へ
   1画素寄っていれば、`white cat`が丸ごと選ばれる。**離した合図では何もしない**
   （選んだ語は、選んだそのままでよい）。合併するのは**押したまま動かしている
   あいだ**だけである。
2. **`double-clicked`は2回目だけに来る合図ではなかった。**離した合図の前後
   どちらで来るかも決まっておらず、3回目・4回目にも来る——押すたびに語が選び
   直されるので、**単クリックで外れなくなる**。窓に数えさせるのをやめ、
   **押下の間隔と場所をRustで数える**（`EditorState::double_click`）。
   **2回目で区切るので、3回目は次の1回目**である。間隔は`GetDoubleClickTime`
   ——**書き手がWindowsで決めた速さ**であって、この編集器の秒数ではない。
   場所も見る（4画素）：離れたところを2回押したのは、同じものを2回押したのでは
   ない。試験2本（3回目は1回目に戻ること、離れた押下は組にならないこと）。

**言葉の直し**：この文書で「引く」と書いていたのは**ドラッグ（押したまま動かす）**
のこと。書き手に伝わらなかったので、以降は「押したまま動かす」と書く。

### 画面で確かめてほしいこと（E3の①）

1. 本文の語をダブルクリック——`黒猫が鳴いた。`で句読点の手前まで選ばれるか。
   **`white cat`で`white`だけ、`cat`だけが選ばれるか**（字の右端を押しても、
   2語にまたがらないか）。そのあと**単クリックで外れるか**（速く押しても）。
   ダブルクリックの2回目を**押したまま動かす**と、語ごと伸びるか。
2. **行番号を押す**——その行が選ばれるか（☰ の Settings で行番号を出しておく）。
   そのまま**押したまま上下へ動かす**と、行ごと増えていくか。
3. **縦書きの面**でも同じか（番号は列の頭に出る）。
4. 番号を出していない面で、余白を押しても今までどおりか。

### ④と、③の決め直し（2026-09-10、書き手の求め3件）

「カーソルの動きが直るなら直しましょう」「TABをすると内側に新しい箇条書きが出来て、
Shift+TABで元の箇条書きに戻る感じ」「空でEnterすると箇条書きの段が初期化されて元の素の
先頭に行き、Shift+Enterだと箇条書き記号が消えるが、キャレット位置は箇条書きの本文の
先頭位置というのが自然だと思いました」。

**1. 記号の中では、カーソルも溝を歩く**（`TextEngine::markup_offset`）。編集中の行の
記号は幅0の箱に覆われて溝に描かれるので、DirectWriteに訊いた位置をそのまま使うと
`1. `の中のどこにいてもカーソルは本文の頭に立っていた——書き手には「←が効かない」に
見える。**描いてある場所と、立つ場所を1つにする**：溝へ一段戻し、溝に描いた字を同じ
書式で組み直して、その中での位置を足す。**縦書きでも同じ道**（ずれは両軸で出る）。
測るのは箱の中にカーソルがあるときだけなので、普段は一度も組まない。

**2. `Tab`と`Shift+Tab`**（`document::shift_indent`、`tab_in_pane`）。分かれ目は3つで、
**どれも書き手が画面で見て言える**もの：

1. 選んだものが**2行以上にまたがっていれば**、行の字下げ（E3の④が言っているもの）。
2. **箇条書きの行にいれば**、その項目の入れ子の深さ。深さは書き手が下げた桁数から
   決まる（`ListLevels`）ので、**字下げを足すことが入れ子にすることである**。
3. それ以外の`Tab`は字を入れる（今までどおり）。`Shift+Tab`はいつでも字下げを外す
   ——字を消す鍵ではないので、外すものが無ければ何も起きない。

- **一段の幅は1つ**（`document::INDENT_STEP`、4桁）。本文へ入れる`Tab`と入れ子の一段が
  違っていたら、同じ鍵に2つの意味が付く。`main`の`TAB_INDENT`はこれを指している。
- **字下げは引用の`>`の後ろに入る。**前に入れると引用そのものが崩れる。
- **空の行は下げない。**選んだ範囲に挟まった空行まで下げると、そこで箇条書きが切れる。
- タブ文字1つは一段とみなして外す（他の道具で書かれた原稿のため）。

**3. 空の項目でのEnterとShift+Enter**（書き手の決定）。`Enter`は**段ごと捨てて素の行頭へ**
——箇条書きを終えて本文へ戻るのがいちばん多い用事で、字下げが残っていると次の行がそれを
継いでいく。`Shift+Enter`は**印だけ捨てる**ので、カーソルは項目の本文が始まっていた列に
残り、その項目に属する段落としてそのまま書き続けられる。

### 書き手の報告3件（④のあと、2026-09-10、直した）

1. **番号が階層ごとに数え直されていなかった**（「TABを打って入れ子になると、次の番号は
   1からです。Shift+TABで元の箇条書きに復帰すると、番号が元の箇条書きの番号を継続します。
   今はすべてが連番になっています」）。`renumber_around`を入れ、**深さごとに1から**
   数えるようにした（内側へ入れば積み、外へ戻れば捨てる——捨てた深さはもう一度入ったとき
   1から始まる）。**字下げと番号の直しは同じ一手**（`shift_indent`が文書ぜんぶを返し、
   `changed_span`で最小の範囲を取る）——2つに分けると、`Tab`1回の取り消しが2回に割れる。
   **連なりは空行で切れる**ので、上のリストの番号を継いでこない。
2. **入れ子の記号が本文にくっついて見えた**（「親側は3文字分くらい空いて本文ですが、
   入れ子側は1文字も空いていない印象」）。溝に描く墨に**字下げの空白まで入っていた**
   ので、その空白が番号を溝の中で右へ押していた。**前後の空白を落として描く**
   （字下げはブロックが持つもので、墨ではない）。カーソルの位置も同じ字を測るように
   直した（描いてある字と、立つ場所は1つ）。
3. **Shift+Enterは項目の続きの段落**（「`1. aaa`でShift Enterすると、2.とならずに、
   改行してaaaの下の位置のキャレットになると思います。ここで、続けてENTERとすると、
   2.となって、1の続きの箇条書きになるのが自然です」）。そのとおりに直した：
   **Shift+Enterは印を継がず、本文の列までの空白だけを継ぐ**。そして**空の継続行で
   Enterを押すと、改行して次の項目が出る**（`item_above`が同じ深さの項目を探して、
   その次の印を置く）——**空いた行はそのまま残るので、項目と項目のあいだが一行空く**
   （書き手の言い直し：「続けてEnterすると改行して2.になる感じ。つまり、一行空く感じ」。
   私は最初「その行が次の項目になる」と取り違えていた）。**リストの外までは遡らない。**

4. **入れ子の行頭の空白の中で、カーソルが止まって見えた**（「箇条書き文字の前に
   スペースがありますよね。あそこをカーソルが歩いているのだと思いますが、その間
   止まっているように見えます」）。そのとおりで、**その空白はどこにも描かれていない**
   ——記号の墨から落としたのがまさにその空白である（上の2）。**描かれない字は、
   カーソルの止まり場所ではない**ことにした：←と→はそこを一息に跨ぐ
   （`document::hidden_indent`、`move_pane_caret`）。**整形して見せている面だけ**——
   原文の面に箱は無く、空白はそのまま字である。引用の`>`は描かれるので跨がない。

### 書き手の言い直し（2026-09-10）：Enterはいつでも次の項目

「Shift-ENTERの間はその段落が続いている感じです。つまり、Shift+Enter、入力して
Shift+Enterとつづけて、Enterすると、次の箇条書きが始まる感じ。今は、2回Shift+Enterすると
箇条書きに復帰できません」。

**私は「空の継続行のときだけ」と読んでいた。**書きかけの段落（中身のある継続行）で
Enterを押すと、ただの改行になっていた——だから「復帰できない」。**Wordと同じ形へ**：

- **`Shift+Enter`は「まだこの項目」**と言う鍵。印は継がず、本文の列までの空白を継ぐ。
- **`Enter`はいつでも「次の項目へ」。**継続行に中身があってもなくても、次の項目が出る。
- 項目そのもの（`- `だけの行）でのEnterは、これまでどおり段を捨てて素の行頭へ。

**診断ログに`enter`を足した**（`pane=pane0 soft=0 kind=Body list=1 at=3 did=insert=4`）
——次に「Enterが効かない」と言われたら、どの行をどう読んで何を入れたかが残っている
（行の中身は書かない：原稿の言葉である）。**今回はそれが無くて、私は書き手の言葉から
場合を組み立て直すのに3往復かけた。**

### 画面で確かめてほしいこと（④と直し）

1. `1. ` と打って `←` を押す——**カーソルが `1`・`.`・空白の間を歩く**か（止まらないか）。
2. `- 親`／`- 子` と打って、子の行で **`Tab`**——内側の項目になるか。**`Shift+Tab`**で戻るか。
3. 3行選んで `Tab`／`Shift+Tab`——まとめて下がる／戻るか（空行は下がらないか）。
4. 箇条書きでない行で `Tab`——今までどおり4桁の字下げが入るか。
5. `- ` だけの行で **Enter**——素の行頭へ戻るか。**Shift+Enter**——印だけ消えて、
   カーソルが本文の列（2桁）に残るか。
7. `1. 甲` `2. 乙` `3. 丙` と並べ、`2.` の行で **`Tab`**——**`1.` になり、下が `2.` へ
   繰り上がる**か。**`Shift+Tab`** で元の連番に戻るか。**取り消しは1回**で戻るか。
8. 入れ子の項目で、**番号と本文のあいだが親と同じだけ空いている**か。
9. `1. aaa` で **Shift+Enter** → `bbb` と打つ → **Shift+Enter** → `ccc` と打つ
   → **Enter**。**次の項目 `2. ` が出る**か（段落の途中からでも戻れるか）。
   空の継続行から押しても同じか。
10. 入れ子の項目で、記号の前から **←** を押す——**一息に前の行へ移る**か（空白の中で
    止まって見えないか）。**→** で戻ると、記号の1字目の後ろへ着くか。
6. **縦書きの面**でも1と2が同じか。

## E2の刻み方（2026-09-10）

```
① 読めるものを増やし、何で読んだかを画面に出す   ← 2026-09-10に済み（624テスト）
② 指定文字コードで開き直す                       ← 2026-09-10に済み（632テスト、下）
③ 指定文字コードで保存する（表せない字を黙って落とさない）  ← 2026-09-10に済み（639テスト、下）
④ 判別が決まらなければ推定で開く（書き手が問いを削った）   ← 2026-09-10（640テスト、下）
⑤ 改行コードの切り替え（名前を付けて保存の欄）             ← 2026-09-10（641テスト、下）
```

### ①でしたこと（2026-09-10）

**始まりの位置**：UTF-8だけを読み、それ以外は開かずに断っていた（`LoadError::NotUtf8`）。

- **変換はWindows自身のもの**（`code_page.rs`、`MultiByteToWideChar`／`WideCharToMultiByte`）。
  **CP932はWindowsの表**であって、この編集器の表ではない——別の実装を持ち込めば、
  同じ原稿が道具によって違う字になる。依存も増えない。
- **判別の順が規則**（`read_bytes`）：①印があればそれ（UTF-16 LE/BE、UTF-8 BOM）
  ②UTF-8として読めればUTF-8 ③それからCP932 ④どれでもなければ断る。
  **印の無いUTF-16は読まない**——向きを決める当てが無く、日本語の原稿には偶然
  そう読める並びが珍しくない（曖昧なものを黙って決めない、が④の下敷きでもある）。
- **読めない字を`?`にして開かない**（`MB_ERR_INVALID_CHARS`）。文字化けを確定しない。
- **保存で似た字に寄せない**（`WC_NO_BEST_FIT_CHARS`）。ここは**絵文字より先に
  日本語の原稿が当たる**ところで、小説でよく使う`—`（U+2014）はCP932の表に無く、
  Windowsは黙って`―`（U+2015）へ寄せる——**寄せた時点で、原稿は書き手の書いた字ではない。**
  表せない字があれば**1バイトも書かず**、その字を名前にして断る（③でこれを画面の
  選択肢にする）。
- **ステータスバーに`CP932・CRLF`のように出る**（`count-encoding`）。混ざった改行は
  `（混在）`と言う——読んだときに1つへ揃えてあるので、黙っていると保存で揃ったことが
  画面のどこにも出ない。
- 見本を4つ置いた（`testdata/15〜18_文字コード_*.txt`）。**試験はその実ファイルを読む**
  ——バイト列を試験の中で組み立てるのと、書き手が開くファイルを読むのは別のことである。
- 試験11本（CP932の往復、代用の拒否、UTF-16の印、判別の順、上限、見本の読み書き）。

### 書き手の報告（①のあと、2026-09-10）：断ったことが画面に出ていなかった

「絵文字を入れると断られずに保存が出来ません。何も言わないので、何か出した方がいいと
思いました」。**断ってはいたが、その言葉が届いていなかった。**

- `set_render_status`が出す言葉（「保存できません」「開けません」「クリップボードへ
  渡せませんでした」……56か所）は、**縦書きのペインの紙の上にだけ**出ていた
  （`status: pane.vertical ? root.render-status : ""`）。**横書きで書いている書き手には
  一言も届かない**——保存が断られたことさえ、画面のどこにも無かった。
- **窓に1つの場所へ移した**：ステータスバーに、警告と同じ色で出す。紙の上には
  出さない（同じ知らせが2か所にあると、書き手はどちらが新しいかを考えることになる）。
- 言葉も直した：**どの字か**と、**ファイルは元のまま**であること
  ——「CP932では表せない字があります（🐈）。ファイルは元のままです」。次にすることは
  「その字を直す」か「別の文字コードで保存する」かで、どちらを選ぶにも原稿が
  壊れていないことが要る（③でその選択を画面に出す）。

**これは要件 7.7 の「働いていない状態は画面に出ていなければならない」の、いちばん
大きな取りこぼしだった**——効かなかったのではなく、効いたことが見えなかった。

**副作用の始末**（書き手の報告、同日）：「ステータスバーに性能ログが出るようになりました。
……画面確認することはないと思いますが、必要ですか」——**要らない。**測った値
（遅延スクロールとドラッグ選択の所要）は`perf_log.txt`と診断ログにもう出ている。
**ステータスバーは書き手への知らせの場所**であって、私の測りものの場所ではない
——3つとも診断ログへ移した。失敗（`整形: NG`など）は画面に残す。

**まだ出ていた**（書き手の再報告、同日）：「赤字で表示時間が出ています」。本命は
**組み直しのたびに書かれる行**（`縦書き 12.3ms / tiles 4.5ms 3枚1新 / 横 1.2ms`）で、
1打鍵ごとに書かれていた。検証の時代は縦書きの紙の上にだけ出ていたので目立たず、
知らせを1つの場所へ集めたときに前へ出てきた。**消した**——内訳はすぐ下の`log_perf`に
あり、画面より詳しく、ペインごとに1行、切り詰められもしない。

**教訓**：知らせの場所を1つにするなら、**そこへ書いている全部を数えてから**にする。
私は「失敗の言葉が届いていない」ことだけを見て移し、1打鍵ごとの計測が同じ口から
出ていることを見落とした（`set_render_status`は65か所ある）。

### ②でしたこと（2026-09-10）

**押せる場所は既にあった。**ステータスバーの単語モードと倍率が同じ帯で押せる
——文字コードもそこに並んでいるので、**同じ形にした**（押した言葉の真上に一覧が
開く。技術検証 6.30：popupは押した釦と同じ箱の中に置く）。

- **一覧は見出しを持つ**（`この文字コードで開き直す`）。E2は「開き直す」と「保存する」を
  **別の操作として示せ**と言っているので、見出しのほうが先にある——③の
  「この文字コードで保存する」はこの下に並ぶ。行はUTF-8／UTF-16 LE／UTF-16 BE／CP932の
  4つで、**いまの文字コードに印が付く**。**BOMの有無は行にしない**：読むときの印は
  ファイルが持っていて、書き手が選ぶのは表のほうである（BOMを選ぶのは保存＝③の話）。
- **判別を通らないのが開き直しの全部**（`file_io::decode_as`）。畳み方も上限もBOMの
  扱いも読み込みと同じ道（`fold`）を通る——別々の作りにすれば、**開き直したあとの
  保存だけが違う形を書く**。
- **印の無いUTF-16も、言われれば読む。**判別が自分では選ばないのは向きを決める当てが
  無いからで（①の規則）、書き手が「LEだ」と言ったのならその当てはもうある
  ——**②はそのための操作である。**
- **自分の印だけ脱がせる。**外さなかった印は本文の1字になる（LEのファイルをBEだと
  言えば先頭に`U+FFFE`）。それは間違いではなく「BEの表ではそういう字だ」という答えで、
  ファイルには触っていないので選び直せる。CP932は印を持たないので、UTF-8のBOMは
  字として読もうとして**断られる**（`EF BB BF`はCP932の表に無い）。
- **読めなければ何もしない。**`DocumentFile::reopen_as`は読めたときだけ入れ替わるので、
  断られた文書は読めていたときのままである——**開き直しは、失敗しても何も失わない
  操作**でなければならない。言葉も判別のものとは分けた：「CP932としては読めません。
  文書はそのままです」（判別の「どれとしても読めない」は、1つを選んだ書き手には嘘になる）。
- **未保存の本文があれば先に訊く**（要件 E2：「保存・取り消しの道を残す」）。
  保存して開き直す／破棄して開き直す／キャンセルの3つで、**保存が済まなかったときは
  開き直さない**——名前を付けるのを取り消した、書けなかった、外部変更で別の問いが
  立った。書き手が「保存して」と言ったのに保存されないまま本文が入れ替わるのが、
  いちばんしてはいけないことである。
- **まだ保存していない文書には行を出さない**（要件 7.7）。読み直す相手がいないので、
  押せて何も起きない行は置かず、一覧はその理由を1行で言う。③（指定文字コードで
  保存する）がその文書のための道になる。
- 開き直したあとは**本文とファイルが一致している**ので、退避も捨てる（外部変更の
  読み直しと同じ規則）。混ざった改行は畳んだことを言う。
- 診断ログに`encoding`（`reopened as=CP932 bytes=… mixed=0`／`refused as=UTF-8`）。
- 試験7本（印の無いUTF-16、自分の印だけ外す、CP932に印は無い、読めない組み合わせ、
  畳み方と上限が読み込みと同じ、見本ファイルの開き直し、失敗しても文書が動かない）。

### 書き手の報告2件（②のあと、2026-09-10、直した）

1. **「壊れて見えるはず」が壊れて見えなかった**（「3について、字が壊れて見えるはず　特に
   壊れて見えません」）。**診断ログが答えを持っていた**——`encoding reopened as=UTF-16 BE
   bytes=484`で、**開き直す前と後で本文の長さが1バイトも違わない**。前にあったのは
   `17_文字コード_UTF16BE.txt`（`tab peek`は同じ1枚を使い回すので、16を開いた次に17を
   開けば前にあるのは17である）で、**UTF-16 BEの見本をUTF-16 BEで開き直していた**。
   答えとしては正しい。**言葉が足りなかったのが直すところ**で、「開き直しました」としか
   言わないと、字が変わらないのが「効かなかったから」なのか「同じものだったから」なのか
   区別が付かない——**同じ文字コードなら「UTF-16 BEのまま読み直しました（字は変わりません）」**
   と言う（選んだ行には既に印が付いていたのだから、それが書き手の知りたいことである）。
   診断ログにも`was=`を足した（`reopened as=UTF-16 BE was=UTF-16 BE`）。
2. **知らせが出っぱなし**（「一度『UTF-8としては読めません』が出ると出っぱなしです」）。
   **消す道がどこにも無かった**——`set_render_status`は53か所から呼ばれるのに、空にする
   呼びは1つも無い。①で知らせを窓に1つの場所へ集めたとき、**畳み方を決めていなかった。**
   縦書きの紙の上にだけ出ていたころは目立たなかったが、帯に居座ると**いま見えているものに
   ついての話ではない言葉**が画面に残り続ける。
   - **消す場所は3つ**：**打つ**（`SharedText::borrow_mut`——編集はぜんぶそこを通るので、
     どこか1つの経路で消し忘れることが無い）、**別のタブへ移る**、**別のものを開く**。
     どれも「書き手が次へ動いた」ことである。
   - **時間では消さない。**読んでいる最中に消えるのは、出ていないのと同じである。
   - **失敗が続いていることは別の場所が言う**——保存できていない文書にはタブと題に`*`が
     付いたままで、要件 7.7 の「働いていない状態」はそちらが持つ。知らせは出来事の記録で、
     状態の表示ではない。

### ③でしたこと（2026-09-10）

**一覧に2つ目の節を足した**（`この文字コードで保存する`）。要件 E2 が「開き直す」と
「保存する」を**別の操作として示せ**と言っているのはこの2つで、向きが逆である
——上はファイルの字を読み直し、下は画面の本文をその文字コードで書く。

- **保存の節は5行**（UTF-8／UTF-8 (BOM)／UTF-16 LE／UTF-16 BE／CP932）。開き直しの
  4行と数が合わないのは、**BOMの有無は書くときにだけ決めごとになる**からである
  ——読むときの印はファイルが持っている。**番号の表も分けた**（`save_form_of_id`と
  `encoding_of_id`）：揃わないものに同じ番号を使うほうが危ない。試験で往復を見ている。
- **まだ保存していない文書でも押せる**（行き先を訊いてから書く）。開き直しと違って、
  保存には読み直す相手が要らない。
- **書けた時点から、その文書はその形のもの**（`save_to_as`）。ステータスバーもそう言い、
  次の`Ctrl+S`もその文字コードで書く。**断られたときは何も変えない**：形も、ファイルも。
- **外部変更があるときは書かない。**要件 8.3 の問いは4つあり、そこへ文字コードの選択まで
  持ち込むと答えが増えすぎる——先に`Ctrl+S`でそちらを片付けてもらう。

**表せない字があったときは、断る**（書き手の判断 2026-09-10）：

> 「CP932では表せない文字があるため保存できません。UTF-8で保存してください」

**どの字かは言わない**（書き手の指摘：「🐈があるためと言わなくてもいいと思います。
一文字に限らないので」）。1字だけ挙げれば**それを直せば済むように読める**が、実際には
次の字でまた断られる——書き手が次にすることはUTF-8で保存することであって、字を1つずつ
潰していくことではない。**見つけた字は診断ログに残す**（`first=`）：「なぜ保存できないのか」を
後から辿る手掛かりは要る——画面に出すかどうかとは別の話である。

- **ここまで3度、書き手に削ってもらった。**私は最初「入らなかった字を全部並べる問い」を
  出そうとし、書き手が**「対象の字が非常に多ければ崩壊しそうです」**と止めた。次に
  「無視して保存する／保存しない」の2択にしたが、私は`?`をファイルの中だけに入れる
  つもりで、書き手が**「無視して保存するというのは内容を替えるということなので、文字が
  ?になって保存され、そう見えることは正しい」**と正した。そして最後に——
  **「3万文字のうち1000文字が表せなかった場合、それを数えなくていいと思いますし、
  無視して保存できなくていいとも思います」。**
- **そのとおりだった。**1000という数は書き手の役に立たない（答えは1字目で出ている）し、
  1000字が`?`になった原稿は**もう書き手の原稿ではない**。要るのは断りと、次にすること
  だけである。**問いも、`?`に替える道も、まるごと消した。**
- **数えない＝最初の1字で止める。**`code_page::encode`はまずWindowsに1回だけ訊く
  ——**代用が要ったかどうかは1回の変換が旗で答える**ので、旗が立たなければそれ以上
  調べるものは無い（**普通に書ける原稿は問い合わせ1回で終わる**。以前は3万字の原稿なら
  3万回訊いていた）。旗が立った回だけ、どの字かを頭から探して**見つけたところで止める**。
- **UTF-8への道はすぐ隣にある**（同じ帯の一覧の下の節）ので、言葉でそこを指す。
- **保存はステータスバーから離した**（書き手の判断 2026-09-10）。私は帯の一覧に
  「この文字コードで保存する」の節を作ったが、書き手が2度に分けて正した——まず
  「いきなり保存されるので、原本が上書きされてしまいます。保存についてはSave Asのほうに
  まとめて、ここで保存しない方がいい」、次に**「そもそもステータスバーから保存できるのが
  おかしいのでは? コードを替えて保存したければ、Save Asからコード選択して保存するべきだと
  思います。ステータスバーで切り替えるのは、読み方だと思います」。**
  - **そのとおりだった。**帯に出ている`CP932・CRLF`は「この文書はこう読んだ」という
    **読み方の表示**で、押して選ぶのもその続き（開き直す）である。書き方は**行き先を
    決める操作**にくっついている——名前と場所と文字コードは、1つのダイアログで一度に
    決まるほうが筋が通る。
  - **`IFileDialogCustomize`で、名前を付けて保存に「文字コード」の欄を足した**
    （Windowsのメモ帳と同じ形）。並びは5つ（UTF-8／UTF-8 (BOM)／UTF-16 LE／UTF-16 BE／
    CP932 (Shift_JIS)）で、**初めからいまの形が選ばれている**。上書きの`Ctrl+S`は何も
    訊かない——そのときは決め直していないので、いまの形で書く（要件 E2 の「無指定の保存では
    元の形式を維持する」）。
  - **改行は欄の受け持ちではない。**ファイルが持ってきたものをそのまま書き戻す。
  - **欄を出せなかったときは、いまの形のまま保存する**——ダイアログが差し替えられている
    環境でも、保存が止まったり知らない文字コードで書かれたりしない。
  - **語群の書き出し（単語チェックモード）には欄を出さない。**読むほうがUTF-8しか
    受けないので、選べると言って選ばせないほうが悪い（要件 7.7）。
  - 帯の一覧は**読み方だけ**に戻り、`この文字コードで開き直す`の4行になった。
- **「全て保存」も同じ言葉**で、そちらは「保存できなかった1件」として数に入る。問いが
  無くなったので、1枚の道と何枚もの道が同じ作りに戻った（`Written`の3状態も要らなくなった）。
- 診断ログに`encoding`（`unmappable as=CP932 path=…`／`save asked as=CP932`）と、
  `file save ok`に`as=`。
- 試験（最初の1字だけが返る、代用に寄せない、形を受け取る、断られたら形も変わらない、
  番号の表の往復2本）。

### 書き手の報告3件（③のあと、2026-09-10、2件直した）

1. **`Ctrl+Shift+S`が効いていなかった**（「Ctrl+Sと同じ動き。メニューからSave Asを選ぶと
   CP932が選ばれています」）。**Shiftを押した`s`は大文字の`S`で届く**ので、
   `event.text == "s" || event.text == "S"`と書いてあった行が、修飾を見ずに名前を付けて
   保存を飲み込んでいた。**メニューが`Ctrl+Shift+S`と出しているのに受ける行が無い**のは
   2026-09-09の`Ctrl+W`と同じ穴で、**画面が言っていることと動作が違う**（要件 7.7）。
   Shiftのときは`save-as-requested`へ回した。**大文字で届く鍵は、修飾も見る。**
2. **「保存しました」がNew Tabのあとも残っていた**（「新規のファイルはまだ保存されて
   いないからです」）。**新しい紙に、前の紙の知らせは付いてこない**——`open_tab`で畳む。
   `add_tab`ではなくそちらに置いたのは、`add_tab`はファイルを開く道も通っていて、
   **開いた直後に言うことがある**（「改行コードが混在していました」）から。
   知らせを畳む場所はこれで4つ（打つ・タブを移る・開く・新しいタブ）。
3. **縦書きの警告は出ていた**（「私の勘違いだったかもしれません」）。**診断ログの
   `file save asked … vertical=`はそのまま残す**——「鍵が届いていない」と「効いたのが
   見えない」を見分ける行が無かったこと自体が、この日の往復の原因だった。

### 書き手の報告（3件のあと、2026-09-10）：ダイアログの絞り込み

「Save Asが開いたとき、ファイル名は拡張子txtのファイル名が入っていますが、ファイルの種類が
mdになっています。これは、Openのときもそうなのですが、mdでフィルタさせています。**mdに
限定する話ではなく、Editorで扱えるのは拡張子にかかわらずテキストファイル全般なので、
デフォルトは`*`でいい**と思います。もちろん、拡張子フィルタを外さず、選択可能にしておくのは
いいと思います」

- **そのとおり。**この編集器はMarkdownを読むが、**Markdownしか読まないわけではない**
  ——`.txt`も`.log`も`.csv`もただのテキストで、絞り込みが`*.md`だと**開けるはずのファイルが
  一覧に出てこない**。名前が`…txt`なのに種類が`Markdown`と出ているのは、**画面が言って
  いることと中身が食い違っている**形でもある（要件 7.7）。
- **初めから選ばれているのを`すべてのファイル`にした**（`SetFileTypeIndex`）。開く・語群の
  取り込み・名前を付けて保存の3つとも。**絞り込み自体は残す**（選べる、が書き手の求め）。
- **付け足す拡張子も、その文書のものにした。**`note.txt`を保存するのに`.md`が足されるのは
  種類の欄と同じ食い違いである——**名前が拡張子を持っているならそれ**、持っていないとき
  （`無題1`のような新しい文書）だけこの編集器がふだん書く`md`にする。試験1本。

### 画面で確かめてほしいこと（E2の③）

1. `testdata/15_文字コード_CP932.txt`を開き、末尾に`🐈`を打って`Ctrl+S`——帯が
   **「CP932では表せない文字があるため保存できません。UTF-8で保存してください」**と言うか
   （**どの字かは言わない**）。
2. **ディスクの中身は元のまま**か（別の道具で開いて`?`も`🐈`も無いか）。画面のほうは
   `🐈`が残ったまま・題に`*`が付いたままでよい——**断ったのは保存であって、打った字ではない。**
3. `🐈`を消して`—`（EM DASH）にして`Ctrl+S`——同じように断られるか。**小説でよく使う字で、
   CP932の表に無いのはこちら**である（`―`＝U+2015は表にあるので通る）。
4. `🐈`を入れたまま**`Ctrl+Shift+S`（名前を付けて保存）**——**鍵で開くか**（2026-09-10に
   直した：`Ctrl+S`と同じ動きになっていた）。ダイアログに**「文字コード」の欄**があり、
   **`CP932 (Shift_JIS)`が初めから選ばれている**か（いまの形だから）。
   `UTF-8`に替えて同じ名前で保存——上書きの確認が出て、そのあと帯が「UTF-8で保存しました」、
   ステータスバーが`UTF-8・CRLF`になるか。**次の`Ctrl+S`は黙って通る**か。
5. **ステータスバーの一覧に保存の行が無い**か（2026-09-10に外した——押せるのは
   `この文字コードで開き直す`の4行だけ）。
6. `New Tab`に何か書いて`Ctrl+S`——名前を訊かれ、欄は`UTF-8`から始まるか。`UTF-16 LE`に
   替えて保存すると、ステータスバーが`UTF-16 LE`と出るか。
7. 縦書きの面でも1が同じか（2026-09-10に確認済み——出ていた）。
8. **`New Tab`を押すと、前の知らせが消える**か（2026-09-10に直した）。保存した直後に
   新しいタブを開いて、「保存しました」が残っていないか。
9. **開くのも保存も、種類の欄が`すべてのファイル`から始まる**か（2026-09-10に直した）。
   `.txt`の文書を`Ctrl+Shift+S`——名前と種類が食い違っていないか。拡張子を書かずに
   `メモ`と打って保存すると、`.txt`が付く（`.md`ではなく）か。新しい文書なら`.md`か。

### 画面で確かめてほしいこと（E2の②）

1. `testdata/15_文字コード_CP932.txt`を開き、ステータスバーの`CP932・CRLF`を**押す**
   ——一覧が上へ開き、`CP932`に印が付いているか。
2. `UTF-8`を選ぶ——**断られて**（「UTF-8としては読めません。文書はそのままです」）、
   **本文が文字化けしないまま残っている**か。
3. `testdata/16_文字コード_UTF16LE.txt`を開いて`UTF-16 BE`を選ぶ——読めるが**字が壊れて
   見える**はず。もう一度押して`UTF-16 LE`に戻すと**元どおり**か（ファイルは無事か）。
   **タブの題が`16_…`であることを先に確かめる**（ツリーの一度押しは同じタブを使い回すので、
   16のあとに17を開くと前にあるのは17である）。**印の付いている行を選んだとき**は
   「UTF-16 BEのまま読み直しました（字は変わりません）」と出る（2026-09-10に足した）。
7. **知らせが畳まれるか**（2026-09-10に直した）：2で「UTF-8としては読めません」を出してから、
   **何か1字打つ**／**別のタブへ移る**／**別のファイルを開く**——そのどれでも消えるか。
   読んでいるあいだ（手を止めているあいだ）は消えないか。
4. 何か1字打ってから別の文字コードを選ぶ——**訊かれる**か（保存して開き直す／破棄して
   開き直す／キャンセル）。「キャンセル」で**打った字が残る**か。「破棄して開き直す」で
   ファイルの中身に戻るか。
5. `New Tab`の文書で押す——一覧が「まだ保存していない文書は開き直せません」と言い、
   **行が無い**か。
6. **縦書きの面**でも同じか。開き直したあと、そのまま打てるか。

### 画面で確かめてほしいこと（E2の①）

1. `testdata/15_文字コード_CP932.txt`を開く——**文字化けせずに開き**、ステータスバーが
   `CP932・CRLF`と言うか。
2. 16〜18も開く——`UTF-16 LE・CRLF`／`UTF-16 BE・LF`／`UTF-8 BOM・LF`か。
3. どれかを少し直して保存し、**開き直しても同じ文字コードのまま**か。
4. 新しい文書（`New Tab`）は`UTF-8・LF`か。
5. CP932のファイルに絵文字か`—`（EM DASH）を入れて保存——**断られる**か（その字が
   名前に出るか）。**ファイルが壊れていない**か。

## E4の残り——`Ctrl+G`（2026-09-10）

**現在位置の表示（`Ln 12, Col 5`）は既にあった**ので、残りは行の指定だけ。
書き手の選択3つ：**検索と同じ帯**・**越えた番号では動かずに言う**・**`12:5`も受ける**。

- **帯は検索と同じ場所・同じ作り。**一行、Enterで行き、`Esc`と`Ctrl+G`で閉じる。
  E1で出来上がっている帯の作りをそのまま使うので、IMEも鍵盤の行き先も同じ道を通る。
- **開いた瞬間から`1〜128行`と出ている。**「128行までです」を打ってから知るのでは
  遅く、この数は「この文書がどれだけあるか」の答えでもある。**数はステータスバーと
  同じところから訊く**（`DocumentCounts`）——2つの数え方があれば、いつか食い違う。
- **無い行では動かない**（書き手の選択）。E4は「他の人や別の道具から『何行目』と
  示された箇所へ行くため」の機能なので、**越えた番号で末尾へ着地すると「手元の原稿が
  違う」という知らせが消える**——行ったのに違う行、が起きる。
- **`12:5`も受ける**（書き手の選択）。ステータスバーが`Ln 12, Col 5`と出しているので、
  画面が言っている形をそのまま打てる。**全角の`１２：５`も受ける**——IMEを立てたまま
  帯へ来た書き手に「半角で打ち直してください」と言うために、この機能はない。
- **着いたら帯は畳む。**行の指定は1回の用事で、探す語のように次があるものではない。
  畳めば鍵盤は紙へ戻り、その行から打ち始められる。
- 桁は書記素で数え、**行の終わりで止まる**（次の行へこぼれない）。返す位置は必ず
  字の切れ目——サロゲートや結合文字の途中へは入らない（E4の完了の目安）。
- 逆写像は`document.rs`の`place_of`で、`caret_place`の対である。**試験は「行って、
  その場所を`caret_place`に訊くと同じ行と桁を言う」形**にしてあるので、片方だけが
  ずれることがない。
- 診断ログに`goto`（`line=128 of=340 column=- at=4821`、範囲外は`outside`）。
- 試験4本（番号の読み、行と桁の往復、無い行、桁の止まり方）。

### 書き手の画面確認で出た2件（E4のあと、2026-09-10、直した）

1. **☰の`Go to Line…`だけ効かなかった**（「6だけうまくいきませんでした」）。
   **技術検証 6.24 にそのまま嵌っていた**——条件付きのメニュー行は`repeater`なので、
   `app-menu.close()`を先に書くと、ハンドラが走っている当の要素ごと消えて次の行へ
   来ない。**頼んでから閉じる。**すぐ上の`Find in File…`がその順で書いてあるのは、
   同じ穴を一度踏んだからである。**条件を付けた行は繰り返し要素だと思って書くこと。**
2. **検索の帯が出ていると`Ctrl+G`が効かなかった**（「切り替わるといいと思いました」）。
   欄は`shortcut`で訊かれた鍵しか通さず、そこに`Ctrl+G`が無かった。足した。
   **閉じるのではなく移る**——`Ctrl+G`は`goto-requested(taking: true)`で来るので、
   帯が既に出ていれば鍵盤だけがそちらへ動く（**メニューからの求めも同じ`taking`**：
   押した書き手は「そちらへ移りたい」のであって、閉じてほしいのではない）。
   検索の帯は開いたままなので、**探している語の色も残る**（E1の③）。
   `Ctrl+F`が番号の帯から語の帯へ移れるのと同じ道を、逆向きにも通した形である。

### 画面で確かめてほしいこと（E4）

1. `Ctrl+G`——帯が出て`1〜◯行`と言うか。`128`＋Enterでその行へ行き、帯が畳まれて
   そのまま打てるか。ステータスバーが`Ln 128, Col 1`になるか。
2. **行数を越える番号**（`9999`）——打っている途中で`◯行までです`と出て、Enterでも
   **カーソルが動かない**か。
3. `12:5`と打つ——その行の5桁目に着くか（`Ln 12, Col 5`）。全角の`１２：５`でも同じか。
4. **縦書きの面**で同じことをして、同じ行に着くか。
5. `Ctrl+G`をもう一度押す／`Esc`——帯が閉じて、鍵盤が紙へ戻るか。
6. ☰ の`Go to Line… Ctrl+G`からも同じ帯が出るか（2026-09-10に直した）。
7. **`Ctrl+F`で検索の帯を出したまま`Ctrl+G`**——番号の帯へ鍵盤が移り、検索の帯と
   一致の色はそのまま残っているか（2026-09-10に直した）。逆に、番号の帯で`Ctrl+F`も。

### 画面で確かめてほしいこと（②）

1. `Aa`を入れて`alpha`を探す——`Alpha`が数から外れるか。**置換も同じ**か。
2. `W`を入れて`cat`を探す——`cats`が外れ、`猫`は`黒猫`の中でも当たり続けるか。
3. 範囲を選んで`[ ]`——件数がその範囲のぶんに変わるか。`Replace All`で
   **範囲の外が1字も変わらない**か。範囲を選ばずに`[ ]`を押すと帯が言うか。
4. 3つとも**面ごと**か（Pane1で`Aa`、Pane2は入っていない）。

### 画面で確かめてほしいこと（①）

1. `Ctrl+F`で語を打つ。**打っている最中に数が変わり、本文は動かない**か。IMEの変換中も。
2. Enterで次へ、Shift+Enterで前へ。帯が`3 / 12`と数えているか。
3. `Esc`で帯を閉じ、**本文でF3**。選択が次の一致へ動き、下の報せに`3 / 12`が出るか。
4. 何も探していないところで**F3**。帯が出るか。
5. 縦書きの面でも同じか（完了の目安：横書き／縦書きの両方）。
6. **Pane1とPane2で別の語を探し、行き来する**——それぞれの語が残っているか。
7. ステータスバーに`「白猫」3 / 12`が出て、**本文を打つと数が変わる**か。

## レビューから動かしたもの（理由つき）

- **要件 7.8 をP1の先頭へ。**レビューは「P1と並行」だが、**ルビが無いことは、縦書きで小説を
  書く人がこの編集器を選べない理由**である——原稿がなろう／青空文庫式のルビを含むので、
  開いた瞬間に破綻して見える。
- **T1（端末の出力検索）をP2へ。**P1に端末が2つあるのは前に出しすぎ（**端末は残すが前に
  出さない**）。T2（複数行貼り付けの確認）は安全側なので小さいままP1に残す。
- **E7（保存版との差分）をE5・E6より上へ。**「軽く編集・校正がしやすいこと」に直接効く。
- **C1を二つに割る。**表示と動作の不一致（上の2番）は0番、一覧そのものはP2。
- **E4は半分できている。**`Ln 12, Col 5`はステータスバーに出ている（`count-caret`、ソース面
  のみ）。残りはCtrl+Gだけ。

## レビューを読むときの注意

- 画面確認に使ったexeは**2026-09-08 13:42のビルド**で、同日の単語チェックモードの作業
  （除外語群・覚え書き・`Edit…`・**目的の言い直し**）は入っていない。単語まわりの記述は
  [単語チェックモード要件](単語チェックモード要件.md)が正。
- レビュー自身の最後の段落——**「汎用Terminalの拡張だけが先行して、原稿を書くために必要な
  既存要件が後ろへ押し出されないようにする」**——が、この並べ替えの基準そのものである。

### ④でしたこと（2026-09-10）

**書き手が削った。**私は最初、判別が断ったところに**候補と読み取り例を並べた問い**を
立てた（`UTF-16 LE　# 文字コードの見本…`が1行、`UTF-16 BE　…`が1行、それに「開かない」）。
書き手が止めた——**「ちょっと親切すぎる気がしますね。推定で開いて、おかしければユーザーが
読み直すのでいい気がします（少なくとも、私は普段そうしています）」。**

**そのとおりだった。読み直す道は、②でもう作ってある。**ステータスバーの帯を押せば
`この文字コードで開き直す`が開く。そこへ行き着く手前に問いを立てるのは、**同じ選択を
2か所に置いて、片方を必ず通らせること**でしかない。しかも問いのほうは、原稿が読めている
ときには出ない——**いちばん要らないときにだけ出る関所**である。

- **判別に段が1つ増えただけ**（`read_bytes`）。印→UTF-8→CP932ときて、**それでも
  読めなければ印の無いUTF-16 LEと見なす**。**LEが先**——Windowsの「Unicode」はこれで、
  印の無いBEは実際にはまず無い。違っていれば書き手が帯から選び直す。
- **`NUL`が混ざる読みは、その文字コードの平文ではない**（`holds_nul`）。**この1つの
  規則が残りを全部決めている**：`NUL`が並ぶ読みを飛ばすから印の無いUTF-16まで落ちて
  こられるし、**その推定にも`NUL`が並ぶなら平文ではない**——実行ファイルも画像も
  `00 00`を必ず持つので、そこで断られる。**推定して開く道と、開かない道を分けているのは
  ここだけ**である。
- **これは判別だけの規則。**②の開き直しは見ない——書き手が「CP932だ」と言ったのなら、
  `NUL`が並ぶのはその問いへの答えであって、編集器が代わりに考え直すところではない。
- **要件 E2 の「文字化けした内容を確定しない」は、保存が守っている。**開くことは確定では
  ない——ファイルは1バイトも動かず、帯は何として読んだかを言い、選び直せる。確定するのは
  書くときで、そこは③が断る。**この一行を取り違えていたのが、問いを立てた理由だった。**
- **見本を2つ足した。**`testdata/19_文字コード_UTF16LE印なし.txt`＝16番から印だけを
  外したもの（日本語なのでUTF-8として読めず、素直に落ちてくる）。
  `20_文字コード_UTF16LE印なし英字.txt`は**`NUL`の規則のほう**で、英字だけなので
  **UTF-8として正しく読めてしまう**——規則が無ければ`#· ·E·n·c·…`が配られていた。
  **どちらも、いまはただ正しく開く。**
- **捕まらない場合は残る**（正直に書いておく）：`春の海`だけの原稿のように、改行も
  ASCIIも1字も無いUTF-16は`NUL`を持たず、UTF-8として読める並びになりうる。そこは黙って
  文字化けする——**帯を見れば`UTF-8`と出ているので、押して選び直せる。**これ以上の判別は
  当てずっぽうになるので、規則を増やしていない。
- **対象外の文字コード（EUC-JP、Latin-1など）は、推定で開くと化けた字が出る。**以前は
  「開けません」と断っていた。**断りのほうが親切だが、正しくはない**——この編集器が
  読めないだけで、ファイルが壊れているわけではない。帯を見れば何として読んだかは分かる。
- 試験2本（見本2つが推定で開く・`NUL`の規則）と、断りの試験を書き換え（PNGの頭で、
  **平文でないものは推定の先でも開かない**ことを見る）。**640テスト、fmt差分なし。**
- **消したもの**：候補を数える`candidates`／`candidates_of`／`Candidate`／`sample_of`、
  問いの`Question::OpenAs`と`ask_which_encoding`、`open_path_in_pane_as`の`forced`、
  診断ログの`ambiguous`行、「UTF-16 LEとして開きました」の知らせ（**帯がもう言っている**）。
  `main.rs`は1行も変わっていない。**作りが正しくなるとコードは減る**——③に続いて2度目である。

### ⑤でしたこと（2026-09-10）

**改行コードの切り替えを、名前を付けて保存の欄に足した**（書き手の選択）。表示は①から
出ていたが、押して変える道が無かった。**帯が受け持つのは読み方だけ**という③の線を
そのまま引いてある——文字コードの隣に置くのは、どちらも「書くときに決めること」で、
決める時機が同じだからである。

- **欄は2つになり、作りは1つになった**（`file_dialog::add_field`）。③の`labels`と
  `chosen`は`SaveFields`（何を出すか）と`SaveChoice`（何が決まったか）の対になり、
  **入りと出が同じ形**で読める。語群の書き出しは`SaveFields::none()`——読むほうが
  UTF-8しか受けないので、選べると言って選ばせないほうが悪い（要件 7.7）。改行も
  同じで、あれは原稿ではなく道具のための表である。
- **並びは`LF`／`CRLF`／`CR`＝帯と同じ言葉**（`NEWLINE_LABELS`は`newline_name`を
  読んで作る定数なので、**言葉を決める場所は1つ**）。同じものを指す言葉が2か所で
  違えば、書き手はそのつど読み替えることになる。
- **2つの欄は別々に効く**（`save_form_of_id`）。`held`から始めて分かったぶんだけ
  置き換えるので、**片方が知らない番号を返してももう片方は効く**。表を分けているのは
  ③と同じ理由——文字コードの5行と改行の3行は数も中身も違うので、番号を兼ねれば
  行を足した日にずれる。
- **上書きの`Ctrl+S`は何も訊かない。**そのときは決め直していないので、いまの形で書く
  （要件 E2 の「無指定の保存では元の形式を維持する」）。欄に初めから選ばれているのも
  いまの形である。
- **知らせは帯と同じ言葉にした**（`CP932・LFで保存しました`）。③のままだと文字コード
  しか言わないので、**改行だけを選び直した書き手には何も答えていない**ことになる。
  診断ログも`save as=CP932・LF was=CP932・CRLF`＝**`was=`と`as=`を一緒に**（②の教訓）。

**ついでに1つ直した：書いた時点で、そのファイルの改行は1つである**（`DocumentFile::took`）。
本文が持っているのは`\n`だけで、`file_io::encode`はそれを`form.newline`に揃えて書く
——**混ざっていたのは読んだファイルのほうで、いま書いたファイルではない。**畳んでいな
かったので、帯が`（混在）`と言い続けていた（`Ctrl+S`でも同じで、⑤で改行を選べるように
したときに前へ出てきた）。③の「書けた時点から、その文書はその形のもの」と同じ一文である。

- 試験3本（改行の番号の往復・2つの欄が別々に効く・書いたら混在でなくなる）。
  **641テスト、fmt差分なし。**

### 書き手の報告（⑤のあと、2026-09-10、直した）：帯が替わっていなかった

「保存しましたは出ますが、帯が替わってないですね」。**診断ログが答えを持っていた**
——`encoding save as=UTF-16 LE・LF was=UTF-16 LE・CRLF`で、しかも次の保存の`was=`が
`UTF-16 LE・LF`になっている。**形は正しく替わって覚えられていた。帯が描き直されて
いなかっただけ**である。

- **帯は打鍵とは別の理由で変わる。**文字コードと改行が変わるのは**ファイルを読み直した
  ときと、書いたとき**で、どちらも本文を1字も動かさない。帯を書いていたのは
  `update_status`＝**組み直しに乗っている**ので、保存では呼ばれない
  ——②（開き直し）で気づかなかったのは、**あちらは本文を入れ替えるので組み直しが
  来ていた**からである。
- `publish_encoding`として括り出し、**保存の道からも呼ぶ**。`write_document_in`は
  `Ctrl+S`も名前を付けて保存も「全て保存」も通る1本なので、置く場所は1つで足りる。
- **帯が言うのは、いま前にある文書のこと**（`publish_active_encoding`）。「全て保存」は
  前にない文書も書くので、**書いた文書の形をそのまま帯へ入れてはいけない**。
- **これも「効かない」ではなく「効いたのが見えない」だった**（この日3度目）。
  ①では断りの言葉が縦書きの紙の上にしか出ていなかった。②では同じ文字コードで
  開き直したことを言っていなかった。**今度は、正しい答えが画面に届いていなかった。**

### 書き手の報告2件目（⑤のあと、2026-09-10）：混在の見本が無かった

「`08_CRLFとBOM.md` これを開いても混在と出ません。今、混在していないかも」。
**書き手のとおりで、私が確かめずに言っていた**——08番はCRLFが13行、混ざった行は
0で、名前のとおり「CRLFとBOMのファイル」である。**testdataに改行が混ざった見本は
1つも無かった。**

- **見本を足した**（`testdata/21_改行の混在.md`＝CRLF8・LF4・CR1）。混在は①から
  画面に出ていたのに、**画面で確かめる相手がいなかった**。試験もその実ファイルを
  読む（①からの決めごと）。
- **教訓：画面確認の手順を書くときは、そのファイルを開いて確かめてから書く。**
  「これで見てください」と言えるのは、中身を見たファイルだけである
  ——書き手の時間を使って私の推測を確かめてもらうことになる。
- **そしてこの見本は、それが示す操作そのもので消える**（書き手が`Ctrl+S`を押して
  失った）。混在を見せる見本を保存すれば、混在でなくなるのは正しい動作である。
  - **見本の頭に断りを入れた**（「この見本は保存すると消えます」）。
  - **試験が番人になる**：混在でなくなれば
    `the_mixed_sample_reads_as_mixed_and_keeps_the_first_kind`が落ちるので、
    消えたことは次の`run_check`で分かる。
  - **git に入れてあれば`git restore`で戻る。**この一件のときはまだ未コミット
    だったので、作り直しになった。**画面確認を頼む前にコミットする**ほうが、
    見本を使う要件では安い。

# E9 ルビなどの記号のON/OFF（2026-09-10）

**Settings → General の MARKUP に`Ruby markup`。**切ると`｜漢字《かんじ》`も`《《傍点》》`も
`［＃「本当に」に傍点］`も**そのまま字として出る**。原稿のバイト列は元から変えて
いない——読むのをやめるだけである。

- **ルビと傍点は1つの旗で切る。**同じ記法の一族（`《》`を使い、同じ道具が書き、
  同じ理由でMarkdownに無い）で、片方だけ読む書き手は考えにくい。**足りなければ
  書き手が2つに分けるよう言う**（案は少ない方から）。
- **縦中横は既に切れる**（Layoutの`Tate-chu-yoko`、2026-09-09）。E9で足したのは
  残りの2つである。
- **紙のシートには置かない。**`count.ruby`と同じ理由の一歩先で、シートが持つのは
  **組み方**、これは**読み方**である——`｜漢字《かんじ》`が記法なのか11字の本文なのかを
  決める。**シートに置けば、同じ文書が2つのペインで別の本文になる**（字数も食い違う）。
- **口は`wire_typography`にある**（設定そのものはFILESだが、切り替えると本文が変わるので
  表示設定と同じ組み直しが要る。`spec_timer`はそこにしか無い）。
- 既定は**On**——要件 7.8 がその記法で組めと言っているので、切りたい書き手が切る側である。
  設定ファイルは`ruby.marks`で、**`0`だけがOff**（読めない値は既定へ倒す）。
- 通し方は`read`／`read_as`と同じ名付け（`visible_markdown_text_as`、
  `PreviewDocument::from_source_as`、`preview_of_as`）にしたので、**既存の試験の
  呼び出しは1つも変わっていない。**
- 試験4本（切ると5つの記法がそのまま出る・入れていればいままでどおり・旗が変われば
  取っておいた行を捨てる・枠が旗を見る）。**646テスト、fmt差分なし。**

## 書き手の報告（E9のあと、2026-09-10、直した）：反映のタイミング

「設定しただけでは反映されず、プレビューのON/OFFでも反映されず、**縦書き横書きを
切り替えると反映されます**」。

- **枠が旗を見ていなかった。**`PreviewSlot`は「本文も活性行も同じなら`refresh`を
  呼ばない」ので、旗が中まで届かない——中の`PreviewDocument`に「旗が変われば行を
  捨てる」規則を入れてあったのに、**そこへ行き着かない。**字数の枠（`CountsSlot`）も
  同じ穴だった。
- **私のコメントが逆だった**（「古いかどうかを決めるのは中のほう」と書いていた）。
  決めるのは**外の枠**で、中は「捨てるものを捨てる」係である。
- **「向きを変えると効く」が答え合わせだった**——向きを変えたときだけ枠そのものが
  作り直されていた。**書き手の報告は、どこが壊れているかまで言っている**ことがある。
- **取っておく枠には、取っておく理由を全部書く。**本文・活性行・組み方までは
  数えてあったのに、読み方だけが漏れた。**新しい入力を1つ足したら、その入力を
  見ている枠を全部数える**（E2⑤の「帯が替わらない」と同じ形——あちらは更新の経路が
  無く、こちらは無効化の条件が足りなかった）。

# E10 後から選択して箇条書き（2026-09-11）

書き手が決めたこと（2026-09-11、4つの問い）：

- **入口は右クリックメニューと鍵の両方**（`Ctrl+Shift+8`＝印、`Ctrl+Shift+7`＝番号）。
  **もう一度頼めば外れる**（トグル）。
- **「・の文字」は両方**——画面に出る印（いまは`•`固定）も、原稿に入る字（`-`／`*`／`+`）も
  選べるようにする。
- **開始番号は打ち直して揃える**。付けるときはいつも1から。変えたいときは先頭を手で
  `5.`に打ち直し、`Renumber`（メニュー1行）で下を揃える——数を訊く画面は作らない。

## 刻み方（2026-09-11）

```
① 選んだ行を箇条書きにする／外す（右クリック2行＋鍵2つ）   ← 2026-09-11に済み（649テスト）
② Renumber——先頭の数字から下を振り直す                  ← 2026-09-11に済み（652テスト）
   ＋ Panicの直し（下、655テスト）
③ 印の字を選ぶ（画面の印はTypography、原稿の字はFILES）   ← 2026-09-11に済み（661テスト）
```

**E10は③で終わり。**

### ①でしたこと（2026-09-11）

- **`document::list_edit`が純粋な部分**（`source`・`line_styles`・選んだ範囲を受けて、
  書き換える範囲と入る字と選び直す範囲を返す）。**編集の道はE3と同じ1本**
  （`apply_span_edit`）なので、取り消しは1回で戻り、同じ文書を出している別の面も付いてくる。
- **行の見方は`line_styles`のもの**（E3の③と同じ理由）。ここで決め直すと、**画面が
  見出しとして組んでいる行に印が付く**。
- **触るのは本文の行と、既に箇条書きの行だけ**（`takes_marker`）。見出し・区切り線・表・
  コードは行そのものが別の意味を持っていて、行頭に印を足せばその意味が壊れる
  （`# 章`は`- # 章`になれば見出しでなくなる）。**空の行は触らない**（`shift_indent`が
  空の行を下げないのと同じ理由）。**タスク（`- [ ]`）も触らない**——印を付け替えれば箱が
  消える。済んだかどうかは書き手が付けた印であって、並べ方を変えた拍子に捨ててよいもの
  ではない（E3の③の「済んだ印を写さない」と同じ根）。
- **外すのは、選んだ行が全部そろってその印のときだけ。**1行でも印の無い行が混じって
  いれば、書き手が頼んでいるのは「そろえる」ほうである。
- **番号は選んだ範囲の中で1から**、字下げの桁ごとに数える（入れ子は入れ子で1から
  ——`renumber_around`と同じ数え方）。**範囲の外の番号は書き換えない**：打っていない
  ところは動かさない（E3の③で書き手が選んだこと）。範囲の外とそろえるのは②の仕事である。
- **印は引用の`>`の後ろに入る**（`shift_indent`が字下げを入れるのと同じ場所）。
- **`Ctrl+Shift`を押した8と7が何の字で届くかは鍵盤で違う**——JISでは`(`と`'`、USでは
  `*`と`&`。3つとも同じ意味に取る（`Ctrl+=`が`+`でも来るのと同じ扱い）。**修飾を
  押したまま打った字が本文へ入ることはない**ので、取り違える先が無い。
- **原稿に入る印は`LIST_BULLET`（いまは`-`）1か所**。③でここが設定から来る。
- 診断ログは`lines`（E3の②と同じ口）。**何も起きなかったときも1行出す**
  （`… Bullet region=… nothing`）——触れる行が無かったのか鍵が届いていないのかを、
  往復せずに切り分けられる（E1の`find`と同じ）。
- 試験4本（付けて外す・範囲の中で1から・触らない行・引用と印の置き換え）。
  **649テスト、fmt差分なし。**

**ついでに直した1件**：`directwrite_render.rs`で`#[test]`が2つ並び、その上の
`horizontal_hit_testing_agrees_with_caret_geometry`の説明だけが別の試験の上に
取り残されていた（E3の①で試験を挿した場所）。説明を自分の試験へ戻した。
`cargo check --all-targets`の警告はこれで0件。

### 画面で確かめてほしいこと（E10の①）

`target/release/editor_spike.exe`。**見本は`testdata/01_行属性.md`**——見出し・箇条書き・
番号・タスク・引用・コード・入れ子が1つの文書に入っていて、どれもgitに入っているので
保存してしまっても`git restore`で戻る。

1. **「## 箇条書きに見えて箇条書きでないもの」の下の4行**（`-1 は負の数です`から
   `snake_case_name …`まで）を選んで`Ctrl+Shift+8`——4行に`- `が付く。選んだままなので、
   続けて`Tab`を押せばそのまま入れ子になる。
2. **もう一度`Ctrl+Shift+8`**——印が外れて元へ戻る（`Ctrl+Z`1回でも戻る）。
3. **同じ4行で`Ctrl+Shift+7`**——`1.`〜`4.`が付く。**下の「## 引用」より先にある
   `1. ピリオドの番号`の番号は変わらない**（範囲の外は動かさない。揃えるのが②）。
4. **右クリック**——`Bullet List`／`Numbered List`の2行が出る（`Paste`の下）。端末の面では
   出ない。押しても鍵と同じことが起きる。
5. **「## タスクリスト」から「## 引用」までを丸ごと選んで`Ctrl+Shift+8`**——見出し・空行・
   `- [ ]`・`> `の行はそのままで、**本文の行にだけ**印が付く。コードブロック
   （「## コードブロック」の```の中）を選んでも何も起きない。
6. **カーソルだけでも1行に効く**（選択が無いときは、カーソルのいる行が選ばれているもの）。
7. **縦書きの面でも同じ**（印は縦書きでも行頭の箱に出る）。
8. 効かないところがあれば、`target/release/diag/`の最新ログの`lines`の行を見せてください
   （`pane=… Bullet region=… chose=…`、何も起きなければ`nothing`）。

### ②でしたこと（2026-09-11）

**右クリックの`Renumber`1行。**鍵は付けていない——開始番号を変えるのは原稿を書き
終えてからの用事で、打鍵の途中に出てくる頼みではない。

- **数え直すのはカーソルの行から下だけ**（`renumber_below`）。上は書き手が打った
  ところである——**打ち直した`5.`がそのまま開始番号**になり、その上にある番号は
  動かない（E3の③で書き手が選んだ「下の行は振り直さない」の裏返しで、**振り直す
  向きを書き手が選べる**ということ）。
- **連なりが切れるところで止まる。**箇条書きに属さない行——空行も本文の段落も——が
  リストの終わりで、そこを越えて遠くのリストを数えない（要件 7.3.2）。
- **深さごとに数える。**内側の連なりはその連なりの先頭が書いている番号から始まり、
  外側へ戻れば外側の続きになる（`renumber_around`と同じ数え方）。番号を持たない項目
  （`-`や`- [x]`）も1つと数え、**書き換えはしない**。
- 編集の道は①と同じ1本（`apply_span_edit`）＝取り消しは1回。入口も①と同じ
  `list-edit`（0=印、1=番号、2=数え直す）——**入口が増えても道は1本**である。
- 試験3本（打ち直した番号から下・深さごと・リストの外では何も起きない）。
  **652テスト、fmt差分なし。**

### 画面で確かめてほしいこと（E10の②）

同じ`testdata/01_行属性.md`の「## 番号付きリスト」で。

1. `1. ピリオドの番号`を**`5.`に打ち直して**、その行にカーソルを置いたまま
   **`Ctrl+Shift+7`**——下が`6)`・`7.`になる（`2)`の丸括弧はそのまま、数だけが
   変わる）。数え直した3行が選ばれたまま残り、**もう一度押すと番号が外れる**。
   右クリックの`Renumber`でも同じことが起きる（そちらは外さない）。
2. **上の行は動かない**——「## 入れ子のリスト」の`1. 番号付きも入れ子になります`で
   数え直しても、その上の`- 一段目`の連なりには何も起きない。
3. **番号の無い連なりでは何も起きない**——「## 箇条書き」の`- ハイフンの箇条書き`で
   頼んでも変化はない（診断ログに`lines … Renumber … nothing`と出る）。連なりは
   空行で切れるので、そこから下の「## 番号付きリスト」へは届かない。
4. `Ctrl+Z`1回で元へ戻る。

## 書き手の報告（②のあと、2026-09-11、直した）：空行を挟んだ番号が数え直せない

「**ちょっと動作がイメージと合いません。今の操作をログから分かりますか**」。

**ログに全部あった**（`Diag_20260911_132819_001.log`）：

```
10568 lines pane=pane0 Ordered  region=1422..1502 chose=1441..1517   ← 5行に番号が付いた
17350 lines pane=pane0 Renumber region=1441..1517 nothing
20666 lines pane=pane0 Renumber region=1441..1517 nothing
33007 edit  pane=pane0 bytes=1632                                    ← 数字を打ち直した
39582 lines pane=pane0 Renumber region=1442..1514 nothing            ← それでも「何もしない」
42859 lines pane=pane0 Ordered  region=1442..1517 chose=1442..1502   ← 外れる
57132 lines pane=pane0 Ordered  region=1422..1505 chose=1422..1516   ← 付く
（以下、付く・外れるの繰り返し）
```

**`Renumber`が一度も効いていない。**書き手が触っていたのは`TEST.md`の1380バイト目
あたりで、**`aaa`の行が空行で区切られて並んでいる**ところだった。

- **印を付ける側は空行を跨いで1から数え**（空行は飛ばす）、**数え直す側は空行で
  止まっていた。同じ文書について2つの数え方があった。**番号を振った直後に
  「何も変わらない」と言うのがそれである。
- 止まっていた理由は2つ重なっていた：**空行は連なりの終わりだと決めていた**ことと、
  **空行を深さ0として数えていた**こと（深さの積み木を空行が崩すので、空行の下の
  項目が自分の書いている番号から数え直され、何も変わらない）。
- **空行は連なりを切らないほうを正にした。**この決まりは要件のどこにも無く、私が
  E3の④で書いたものだった——**Markdown自身は逆を言う**（項目のあいだが空いていても
  1つのリスト＝緩いリスト）。`testdata/01_行属性.md`にも「空行をはさんでも深さは
  続きます」と書いてある。**切るのは、字の入った行がリストの外にいるとき**である。
- **数えるのは項目だけ**（空行も、項目の続きの段落も数えない）。
- E3の④の試験を1本書き直した（`1. 甲 / 2. 乙 / 空行 / 1. 丙`の丙は**3つめ**になる）。
  **要件に無い決まりを試験に書くと、その試験が決まりになってしまう**——直すときに
  「前はこうだった」としか言えない。
- 試験2本（空行を挟んだ並びが、付ける側でも数え直す側でも1つの連なり／E3の④の
  書き直し）。**657テスト、fmt差分なし。**

## 書き手の報告（②のあと、2026-09-11、直した）：開始数字の変更の入口

「**開始数字の変更、これはどうやって操作しますか?** 開始文字を変更して、CTL+Shift+8を
すると、一度箇条書きが消されて、そこで変更した数字も消えます」。

**動作は正しく、入口の置き場所が間違っていた。**`Ctrl+Shift+8`は「印の箇条書きにする」
鍵なので、番号が印に置き換わるのは筋が通っている——**問題は、書き手が「開始数字を
変える」ためにそこを押した**ことである。右クリックの`Renumber`は、書き手が探した場所に
無かった。

**`Ctrl+Shift+7`を「そろえる→外れる」の階段にした**（書き手の選択）：

1. 先頭の行を`5.`に打ち直して`Ctrl+Shift+7`——**先頭の数字から下がそろう**
   （`5. 6. 7.`）。数え直した連なりが**選ばれたまま**残るので、どこまで数え直したかが
   画面に出る。
2. もう一度`Ctrl+Shift+7`——**もう変わらないので、選ばれている行の印が外れる。**

**「もう一度頼めば外れる」は守られている**（書き手が①で選んだこと）——**変えるものが
無くなったときが「外す」である**。印の鍵（`Ctrl+Shift+8`）はこの道を通らない：印に
数え直すところは無い。右クリックの`Renumber`も残した（こちらは外さない）。

**入口を足したのではなく、書き手が押した鍵に仕事を足した。**新しい鍵を覚えるのは
書き手の負担で、C1（ショートカット一覧）が減らそうとしているものである。

## 書き手の報告（②のあと、2026-09-11、直した）：Panicした

**診断ログに全部あった**（`Diag_20260911_130415_001.log`の最後の1行）：

```
panic at=src\main.rs:14636 said=end byte index 809 is not a char boundary;
it is inside '今' (bytes 808..811 of string)
```

落ちた場所は`source_line_start`だが、**原因は数え方**である。

- **1行ごとに動かした位置を、次の行の閾値と比べていた。**選び直す範囲（`chosen`）は
  編集の前の位置を行ごとにずらして作るが、**ずらした結果を次の行の「ここから後ろ」と
  比べる**と、積んだぶんだけ位置が前へ出て**もう一度ずれる**——行末に立っていた端が、
  次の行の印の後ろまで送られる。送られた先が字の途中だと、そこを頭にした`&str`の
  借り方が落ちる。
- **書き手がしていたのは後ろ向きの選択**（ログの`phase=Begin … hit_byte=818`から
  `phase=End … hit_byte=698`）。選択の端が行末に来るのは、この選び方でいちばん起きる。
- **直し方は`renumber_around`が先に持っていた形。****比べるのは編集の前の座標、
  動かすのは最後に一度**（`note_shift`／`settle`）。**同じ傷が`shift_indent`
  （E3の④の`Tab`）にもあった**ので一緒に直した——`shift_positions`は誰も呼ばなく
  なったので消した（作りが正しくなるとコードは減る）。
- **落ちるくらいなら、1字手前に立つ。**`apply_span_edit`が選び直す範囲を字の切れ目へ
  丸めるようにした。**編集の入口はここ1つ**なので、これから足す編集も同じ守りの
  内側にいる。**丸めたことはログに出す**（`chose=… asked=…`）——黙って直すと、
  次に同じ数え違いをしたとき見つからない。
- 試験3本（行末の端が次の行へ送られない・`Tab`でも同じ・**本物の原稿の上で
  総当たり**：`testdata/01_行属性.md`を前向き・後ろ向き・カーソルだけの3通りで
  なぞり、選び直す範囲がいつも字の切れ目に立つこと）。**655テスト、fmt差分なし。**

**この日に学んだこと**：**位置を動かしながら数えない。**動かす前の座標で数え切って、
最後に一度動かす。同じ形の数え方が3か所（`renumber_around`・`shift_indent`・E10）に
あって、**正しかったのは最初の1つだけ**だった——**数え方を写すときは、写す先ではなく
写し元を読む。**

## 書き手の報告（2026-09-11、直した）：選んだところが塗られていなかった

「選択して、Numter/Renumberしたときに、**選択が外れますが、もう一度実行すると内部的に
選択範囲は維持されているように見えます。選択を外さない方がいいのでは?**」。

**書き手の読みがそのまま当たっていた。**選択は`EditorState`に入っていて（だから
もう一度押すと同じ範囲に効く）、**画面に一度も塗られていなかった。**

- **`draw_edit`はカーソルの位置だけを受け取り、選択は`PaneSelection::default()`で
  描いていた。**打鍵の道（選択は畳まれる）にはそれで足りるが、**選び直した範囲を持つ
  編集**——E10の印と数え直し、E3の②の行移動、E3の④の`Tab`——では、状態にあるものが
  画面に出ない。
- **`caret`を`Option`にして、`None`なら状態を見て描く**ようにした（`draw_both`の
  `None`は`schedule_catch_up`が既に通っている道で、新しい道ではない）。**編集の
  入口は`apply_span_edit`1つ**なので、直したのは1か所である。
- **E3の②と④も一緒に直った。**「動いた行が選ばれたまま」「下げた行が選ばれたまま」は
  状態としては正しく、**ずっと見えていなかった**——`chose=`がログに出ていたので
  合っているように見えていた。**ログが正しいことは、画面に出ていることの証しではない。**

## 書き手の報告（2026-09-11、直した）：先頭の行だけ印が選択から落ちる

「Number Listの場合もドットの場合も、**先頭行の先頭文字(Numberなら1)だけ選択範囲から
落ちます**。機能は効いています」。

- **書き換えた範囲の頭に立っていた端を、動かしていた。**`start..end`を書き換えたので
  `start`はその**外側の境目**であり、そこにいた端は足した印の**前**にいる——なのに
  「印を入れた点より後ろの位置はずらす」規則にそのまま乗せていたので、**先頭の行の
  印だけが選択の外**に出ていた（2行目以降は端が関わらないので中に入っていた）。
- **カーソル1つだけなら動く。**そこには「範囲の頭」は無く、あるのは書き手が立っていた
  字である——`Tab`で行頭のカーソルが字下げの後ろへ出るのは、書き手が画面で見て通した
  ことである（E3の④）。**選んでいるときだけ頭が動かない。**
- **`shift_indent`も同じ`settle`を通る**ので、`Tab`でも先頭の行の字下げが選択の内側に
  入るようになった（こちらは今日まで見えていなかった）。
- 試験1本（印・字下げのどちらでも頭が動かず、カーソル1つなら動く）。
  **658テスト、fmt差分なし。**

## 書き手の報告（2026-09-11、直した）：選択範囲より一行前から箇条書きになる

「CTRL+Shift+7で、**選択範囲より一行前から箇条書きになります**」。

**ログで決着した**（`Diag_20260911_140516_001.log`）：

```
pointer.p0 phase=Begin  hit_byte=1361 active=None      ← 押したところが1361
pointer.p0 phase=End    hit_byte=1416 active=Some(1361)
lines pane=pane0 Ordered region=1361..1416 chose=1361..1425
```

**1361は`\n`そのもの**（`TEST.md`の空行の改行）。別の回は`1396`＝`aaaaaaaaaaaaaaaaaa`の
行末で、そのときは**その行に`- `が付いていた**（`Bullet region=1378..1503`）。

- **行の頭の近くを押すと、当たり判定は前の行の末尾を返す。**それは普通の振る舞い
  （行より上を押せば上の行）だが、**`selected_lines`がそれを「その行も選ばれている」と
  読んでいた**——行末に立てた頭では、**その行の字は1つも選ばれていない。**
- **終わりの規則の裏側が抜けていた。**「終わりがちょうど行頭なら、その行は入らない」は
  最初から書いてあったのに、頭のほうを書いていなかった。**片側だけの規則は、反対側から
  同じ形で戻ってくる。**
- 直したのは`selected_lines`1か所なので、**E3の②（行移動）とE3の④（`Tab`）も同じだけ
  正しくなった。**
- **改行はバイトで見る**——`from`は字の切れ目とは限らず、`&source[from..]`はそこで
  落ちる（試験がそれを捕まえた）。
- **診断ログに`asked=`を足した**（`lines pane=pane0 Ordered asked=1396..1503
  region=1378..1503 chose=…`）。`region`は行へ伸ばしたあとのもので、書き手が選んだ
  ものではない——**2つ並べないと「一行前から効く」の原因が読めない**（`was=`と`as=`を
  一緒に書く、の3度目）。E3の②と④の行にも同じものを足した。
- 試験1本（行末から始まる選択・空行のとき・カーソル1つなら行末でもその行）。
  **659テスト、fmt差分なし。**

## 書き手の報告（2026-09-11、直した）：`Alt+↑`でカーソルが次の行の頭へ出る

「1行だけ選択して、ALT+↑を押すと、**選択範囲は正しいですが、キャレットが行末から
次の行の先頭に移動します**」。

- **選び直す範囲に、行の後ろの改行を入れていた。**`line_edit`の`chosen`は
  「動いた行＋その改行」だったので、**カーソル（範囲の終わり）が次の行の頭に立つ。**
  動かしたのは行であって、書き手を次の行へ連れて行ったのではない。
- **改行は行と行のあいだにあるもの**——この節の`rejoin`が最初からそう言っていた
  のに、選び直す範囲のほうがそれに従っていなかった。**4つの行編集（前へ・後へ・
  写す2つ）ぜんぶ**から外した。
- **E10の数え直しも同じにした**（`renumber_below`）。連なりの最後の改行を選びに
  入れると、そこでもカーソルが次の行へ出る。
- 試験は既にあった4本の`picked`を書き直した（`"二\n"`→`"二"`）。**選ばれるのは
  行の字まで**、が1つの決まりになった。**659テスト、fmt差分なし。**

### ③でしたこと（2026-09-11、書き手の言い直しで作り直した）

**はじめは2つの字を2つの場所に置いた**（「両方」）。**書き手が使ってみて作りを2度
言い直した**ので、下の「書き手の言い直し」が正である。以下はその前の記録。

- **画面に出る印は紙のシート**（Typography → `Bullet mark`、`•`／`・`／`○`）。
  これは**組み方**である——同じ原稿を別の紙で開けば別の丸に見えてよい。
  シートの数（`Setting`）に1つ足しただけなので、設定ファイルも初期値へ戻すも
  既存の仕組みに乗っている（`bullet-mark`）。
- **原稿に入る字はFILES**（`List bullet`、`-`／`*`／`+`）。これは**読み書きの字**で、
  Markdownが認める3つだけ。**読めない値は既定（`-`）へ倒す**——手で書いた設定ファイルが、
  Markdownでない字を原稿へ入れてはならない（`list_bullet_of`）。
- **タイルの署名に入れた**（`hash_typography`）。箱は幅0なので印を替えても幾何は
  1画素も動かない——**入れ忘れると、設定を替えても絵置き場の古い絵がそのまま出る**
  （6.18の罠、ルビの大きさと同じ側）。試験で確かめてある。
- **覆った字を読み出す道を1本に括った**（`covered`／`ruby_reading`）。`marker_ink`に
  紙の字を渡すにあたって、`Upright`とルビは**自分の欲しいものを直に取る**ようにした
  ——印の字を知らない関数に印の字を配らないため。
- 試験3本（紙が言った字が出る・印を替えるとタイルの署名が変わる・原稿へ入るのは
  Markdownの印だけ）。**661テスト、fmt差分なし。**

### 画面で確かめてほしいこと（E10の③）

1. **☰ → Settings → Layout**（縦書き／横書きそれぞれの紙）の下のほうに
   **`Bullet mark`**——`•`／`・`／`○`。押すと**その場で紙の印が替わる**（原稿は
   1バイトも変わらない）。**縦書きと横書きで別に持ちます**（紙ごとの組み方なので）。
2. **☰ → Settings → General**（MARKUPの節）に **`List bullet`**——`-`／`*`／`+`。ここを`*`にして
   本文を選び`Ctrl+Shift+8`を押すと、**原稿に`* `が入る**（画面の丸は1のままです）。
3. 両方とも**閉じて開き直しても残る**（設定ファイルへ書いています）。

## 書き手の報告（③のあと、2026-09-11、直した）：設定の名前と、印の替え方

「**Setting →FILES, Setting Typographyというものはない**と思います。なにか不整合が
起きているのであれば、それは修正するべきです。Setting → General/ Setting → Layoutの
前提で確認しました。Layoutのほうは問題ないと思います。**Generalのほうは、一度設定すると、
常にその文字を箇条書きとして認識している**ように思えます」。

**1. 名前が合っていなかった。**書き手の言い直しが正しい——「**ソースと画面があっていない
のであれば、合わせないと混乱の元です**」。私の書き方を直すだけでは、**次に読む人がまた
同じところで食い違う。**そこで**画面のほうを直した**：

- **`⋮`の`Typography…`を`Layout…`にした。**あの行が開くのは`SETTINGS`の面で、組み方が
  載っているタブの名前は`Layout`——**3つの名前が別々を指していた。**押した先のタブへ
  合わせて開くようにもした（`settings-tab = 4`）。
- **Generalの中に`MARKUP`の節を作った。**`FILES`の見出しの下に、ルビの記法も箇条書きの
  字も並んでいた——**どれもファイルの話ではない。**`FILES`は自動退避だけになり、記法の
  読み書き（`Count ruby`／`Ruby markup`／`List bullet`）は`MARKUP`の下にある。
- **ソースの名前もそろえた**（`typography-panel`→`settings-panel`、`typography-requested`
  →`layout-requested`、`typography-step/chose`→`sheet-step/chose`）。**画面が`SETTINGS`と
  言っているものを、ソースが`typography`と呼んでいた**のが、私が書き間違えた元である。
- README・この文書の`Settings → FILES`／`Typography`も全部直した。

**正しい道**：**Settings → General の MARKUP**（`List bullet`・`Ruby markup`・`Count ruby`）／
**Settings → Layout**（`Bullet mark`・`Tate-chu-yoko`・ルビの大きさと位置）。

**2. 印の鍵も「そろえる→外れる」の階段にした。**設定を`*`にしてから`- 項目`の上で
`Ctrl+Shift+8`を押した書き手が頼んでいるのは、**その字にそろえる**ことである。
いままでは「もう箇条書きだから」と**外していた**（そのあともう一度押すと`*`が付くので、
2手かかっていた）。`Ctrl+Shift+7`の階段と同じ形にそろえた：**字が違えば替え、同じなら外す。**

**3. 読むほうは変えていない。**`-`も`*`も`+`も、設定が何であれ**箇条書きのまま**である
——設定を`*`にしたからといって、`- 項目`と書かれた**他人の原稿が本文になってはならない**
（要件 7.4「未対応の記法が存在しても、保存時に内容を失わない」と同じ根）。**この設定は
書くときだけのもの**で、そう読めるように画面の説明文も直した（「Ctrl+Shift+8 が原稿へ
入れる字。読むときは - * + のどれも箇条書きです。」）。**もし「その字だけを箇条書きとして
読む」ほうが要るなら、言ってください**——そのときは要件 7.4 との兼ね合いを先に決める。

試験1本（設定と違う印はそろい、同じなら外れ、読むほうは変わらない）。**662テスト、
fmt差分なし。**

## 書き手の言い直し（③のあと、2026-09-11）：同格に認めているなら、同格に扱う

2度の言い直しでここへ着いた。

1. 「**-が標準的な箇条書き形式だと思いますが違いますか?** それであれば、**-としている
   ものを、プレビューでどう表示するのかという問題**で、それがカスタマイズ可能というのが
   正しいと思います。標準形式以外の箇条書き形式を入れるか入れないかというのは
   Optionalではないですか? また、標準形式が否定されるのは避けた方がいいと思います」
2. 「**CommonMarkの仕様が同格に認めているなら、同格に扱ってあげるべきでは?**」
   （私が「書く側の標準は`-`」と言ったのに対して）

**書き手が正しい。**私は1を受けて`Other bullets`（`*`と`+`を読むかの入切）を作ったが、
**それは同格に扱っていない。**2で言われたとおりに戻した——**入れた設定を消すのが直し**
である。

```
原稿の字をどう読むか    `-`・`*`・`+` は同格の印（CommonMarkのまま。設定は無い）
原稿へ入れる字          `-`（`Enter`が継ぐ字と同じ。どれか1つを選ぶしかないから）
画面に出る印            Settings → Layout の `Bullet mark`（`•`／`・`／`○`）
```

- **`List bullet`は消した。**替えても画面が1画素も変わらない設定だった——**押した
  手応えが画面に無い**のは、単語チェックモード要件 2.1.1 の2番と同じ穴である。
  「印の字を変える」で書き手が見たいのは**画面に出る丸**で、それは`Bullet mark`にある。
- **`Other bullets`も消した**（作って、言われて、消した）。同格に扱うとはそういうことである。
- **`Ctrl+Shift+8`は、どの印の行でも「もう箇条書き」と見て外す。**`* 項目`を選んで
  押せば外れる（`- `に替えてから外す、ではない）。入れるときは`-`。
- 消したぶんコードは減った（`list_bullet_of`・`same_mark`・設定の読み書き・画面の行）。

### 「読む方・書く方」という言い方をやめる

書き手の問い2つ：「**読む方・書く方という分解の意味が分かりません。それは、ファイルを
読むクラスと、書くクラスという意味ですか**」「**書く側というのが、またわからないのですが**」。

**私の言葉が悪い。**クラスの話ではない。言うべきだったのは、同じ原稿の同じ字についての
2つの場面である：

- **原稿の字をどう読むか**（`document::line_styles`。プレビューの組み方も、`Enter`の
  継続も、`Tab`の入れ子も、ここが決める）
- **`Ctrl+Shift+8`が原稿へどの字を入れるか**（`set_markers`）

**「読む側／書く側」と縮めると、ファイルI/Oの話に聞こえる。**縮めずに書くこと。

試験1本（`*`も`+`も「もう箇条書き」として外れ、入れるのは`-`、読みも同格）。
**661テスト、fmt差分なし。**

### 画面で確かめてほしいこと（③の作り直し）

1. **Settings → General の MARKUP** に`List bullet`も`Other bullets`も**無い**
   （`Count ruby`と`Ruby markup`だけ）。
2. `* 項目`を選んで`Ctrl+Shift+8`——**外れる**（`*`も印だから）。もう一度押すと`- 項目`。
3. `* 項目`も`+ 項目`も、プレビューでは`-`と同じ丸で出る（`Bullet mark`の字）。
4. **Settings → Layout の`Bullet mark`**で`•`／`・`／`○`——これが「印の字を変える」。

## 書き手の決定（2026-09-11、4つの問いへの答え）：記号に意味を与える

やり取りの末に、書き手がこう決めた。

> 「**仕様は正しくしておきたいので、三種類の文字に箇条書きの意味を与えましょう。そして、
> それぞれに、表現としてどう見せるかを設定できるようにしましょう。ただし、三種類のうち、
> どれを箇条書きとして採用するかはON/OFFできるようにしましょう。**そうすると、ユーザーが
> `-`しか選ばなければ、Obsidianと同じになります。**また、追加の箇条書き文字の可否は、
> 作っても利用機会が少なさそうなので、やめましょう**」

決まったこと（私の問い4つへの答え）：

1. **設定は2つのタブに割れる**（やむを得ない）。ただし**依存関係は必須**——読むと決めて
   いない記号は、見せ方も設定できない。
2. **`-`もOFFにできる**（3つとも同格なので扱いも同じ）。
3. **入れる記号は案C**——右クリックにONの記号ぶんの行、鍵は**先頭のON記号**。
4. **§5.3（記号を変えるとそこから別のリスト）も直す。**

### 刻み方（2026-09-11）

```
① どれを箇条書きとして読むか（General → MARKUP の3つの入切）  ← 2026-09-11に済み（664テスト）
② 記号ごとの画面の印（Layout の対応表。OFFの記号は設定できない）  ← 2026-09-11に済み（665テスト）
③ 箇条書きにするときの記号を選ぶ（右クリックにONの記号ぶんの行）      ← 2026-09-11に済み（665テスト）
④ 記号を変えるとそこから別のリストが始まる（§5.3）
```

### ①でしたこと（2026-09-11）

- **`BulletMarks`が「いま印として読む記号」**（`-`／`*`／`+`の入切）。設定は
  **General → MARKUP の`Bullet marks`**で、読む記号を並べた字（`-*+`）として
  設定ファイルへ書く（`list.marks`）。**知らない字は読み飛ばす。**
- **読み方は1つの値で運ぶ**（`Reading { ruby, bullets }`）。2つの旗は同じところ（窓）から
  来て同じところ（`DocumentCounts`と`PreviewDocument`）へ行く——**片方だけ渡し忘れる道が
  無い。**`line_styles_as`が受け、そこから下（プレビューの箱・`Enter`の継続・`Tab`の
  入れ子・数え直し）は全部ついてくる。
- **切った記号の行は本文の1行**になり、記号もそのまま字として出る。**原稿のバイト列は
  変えない**（ルビの旗と同じ作り）。
- **全部OFFにもできる**（書き手の決定2「扱いは同じ」の素直な帰結）。そのとき箇条書きの
  印は1つも無く、`Ctrl+Shift+8`は**何もしない**——診断ログに`mark=None`と出る。
- **入れるのは読む記号のいちばん前のもの**（案Cの半分。残りの「右クリックで選ぶ」は③）。
  **印として読まない字を原稿へ入れない**——入れたら、書いた行が箇条書きにならない。
- 試験3本（3つとも選べる・読まない印には箱も立たない・入れる字は読む記号の先頭）。
  **664テスト、fmt差分なし。**

### 画面で確かめてほしいこと（①）

1. **☰ → Settings → General の MARKUP** に **`Bullet marks`**——`-` `*` `+` の3つの釦。
   下の説明文が、いま読む記号を言う。
2. `- あ` `* い` `+ う` が並んだ原稿で、**`*`を押して切る**——`* い`だけがただの本文に
   なり、`*`が字として見える。`-`と`+`はそのまま。**もう一度押すと戻る**（原稿は
   1バイトも変わっていない）。
3. **`-`も切れる**（3つとも同じ扱い）。
4. 切った記号の行で`Enter`——継続しない（本文の行だから）。読む記号の行では継続する。
5. **`*`だけONにして`Ctrl+Shift+8`**——原稿に入るのは`* `（読む記号の先頭）。
   **3つとも切ると何も起きない**（`target/release/diag/`の最新ログに
   `lines … Bullet … mark=None`と出る）。
6. 閉じて開き直しても残る。

### ②でしたこと（2026-09-11）

**紙が、原稿の記号1つずつに字を与える**（`Typography::bullets`＝`-`／`*`／`+`の順に1つ）。

- **描くときに、原稿のどの記号かを読み出す**（`bullet_ink`）。箱が覆っている字を
  そのまま読む道は`Number`が先に通っていたので、**新しい道は作っていない**
  ——`Ornament`に記号を持たせる必要も無い（レイアウトの鍵に字を1つ増やさずに済む）。
- **タイルの署名に入っている**（`hash_typography`）。箱は幅0で幾何は動かないので、
  入れ忘れると**設定を替えても絵置き場の古い絵が出る**（6.18の罠）。
- **読むと決めていない記号は、見せ方も設定できない**（書き手の求めた依存関係）。
  Layoutのその行は釦を出さず、「General で切ってあります」と言う——**働かない
  切り替えを画面に置かない**（単語チェックモード要件 2.1.1）。
- 紙のシートの数が1つから3つに増えた（`Setting::BulletMark(0..2)`、設定ファイルは
  `bullet-mark-hyphen`／`-star`／`-plus`）。既定は3つとも`•`——**設定になったからと
  いって書き手の画面が動くいわれはない。**
- 試験1本（記号ごとに違う字が出る）。**665テスト、fmt差分なし。**

### 画面で確かめてほしいこと（②）

1. **☰ → Settings → Layout**の下のほうに **`Bullet marks`** が3行（`-` `*` `+`）。
   それぞれに`•`／`・`／`○`の釦。
2. `- あ`と`* い`が並んだ原稿で、**`-`の行を`・`、`*`の行を`○`**にする——
   **プレビューで別々の印になる**（原稿は1バイトも変わらない）。
3. **General で`*`を切る**——Layoutの`*`の行は釦が消えて「General で切ってあります」に
   なり、本文側でも`* い`はただの本文になる。
4. **縦書きと横書きで別々に持つ**（紙ごとの組み方なので）。

### ③でしたこと（2026-09-11）

**どの記号で書くかは書き手が選ぶ**（案C）。

- **右クリックに、読む記号ぶんの行**（`Bullet List  -`／`* `／`+`）。切ってある記号の
  行は出ない——**押せない行を出さない。**
- **鍵（`Ctrl+Shift+8`）はいちばん前の記号**で、その行にだけ鍵を書く。読む記号が
  1つも無ければ、鍵も行も何もしない（診断ログに`mark=None`）。
- **頼まれた記号にそろえる→外す。**`* 項目`の上で「`Bullet List  -`」を頼めば`- 項目`に
  なり、もう一度頼めば外れる——`Ctrl+Shift+7`の階段と同じ形である。**記号ごとに画面の印が
  違うのだから、記号は選ぶもの**であり、「もう箇条書きだから外す」では書き手の頼みに
  答えていない。
- **読むほうは3つとも同格のまま**（`*`の行は、`-`を頼まれても箇条書きである）。
- `for`の中に`if`は書けないので、メニューの3行は並べて書いた（技術検証の段階2b）。
- 試験1本（頼まれた記号にそろい、次の一押しで外れ、読みは変わらない）。
  **665テスト、fmt差分なし。**

### 書き手の求め（③のあと、2026-09-11）：`Bullet List`は階層に

「**右クリックメニューが長いので、BulletListは階層にした方がいい**と思いました。
**何も選ばれていなければ、空**」。

- **右クリックに出るのは`Bullet List`の1行だけ**になり、押すと記号の行がその場で開く
  （`▸`／`▾`）。**読む記号が1つも無ければ、開いても空である**（書き手の言葉のとおり）。
- **入れ子のポップアップにはしない。**popupを閉じるとその木ごと消え、消えた木から
  `root`へ登った生成コードは黙って何もしない（技術検証 6.24・6.30）——**その場で開く形
  なら木は1つのまま**で、この罠の外にいる。
- **開くたびに畳んでおく**——前に開いた形が残っていると、右クリックの長さが回ごとに違う。
- 鍵（`Ctrl+Shift+8`）は、開いた中の**いちばん前の記号の行**に書いてある。

### 画面で確かめてほしいこと（③）

1. 右クリック——**`Bullet List`は1行**。押すと`-` `*` `+`（読む記号だけ）が下に開く。
   もう一度押すと畳む。**閉じてから開き直すと畳まれている。**
2. General で`-`を切る——開いた中から`-`が消え、`Ctrl+Shift+8`が`*`の行に付く。
3. `* 項目`の上で「`Bullet List  -`」——**`- 項目`にそろう**。もう一度で外れる。
4. Layoutで`-`を`・`、`*`を`○`にしておくと、**3の前後で画面の印が替わる**のが見える。
5. **3つとも切ると**、`Bullet List`の行は1つも出ず、`Ctrl+Shift+8`も何も起きない。

## 次にすること

**E10の④**（§5.3：記号を変えるとそこから別のリストが始まる）。そのあと **E11** → **E12**。

## そのあと

**E11**（フォント設定の太字・斜体・取り消し線）→ **E12**（Markdownのリンク表示）。
どちらも書き手の追記で、中身は上の「書き手の追記」にある。
