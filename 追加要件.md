# 追加要件

**ここはディスカッションの場所である**（2026-09-07にそう決めた）。書き手が気づいたことを
書き留め、**要件として確定したら要件定義.mdへ転記して、ここからは消す。**
ここに残っているものが、まだ手の付いていないものである。

> **2026-09-11に整理した。**この日までの4日で、私（作り手）は刻み方・画面確認の手順・
> 書き手の報告と直しを**全部ここへ書き足していた**——2000行を超え、書き手に
> 「めちゃくちゃです」と言われた。**運用はこの冒頭に最初から書いてあった。**
> 済んだものの行き先は下の一覧にある。**作業の記録はgitのコミットに全文がある**ので、
> ここには残さない（`git log --oneline`で日付ごとに追える）。

## 済んだものの行き先

| いつ | 何 | 転記先 |
| --- | --- | --- |
| 〜2026-09-07 | Split／Explorerの幅・設定ボタン／Terminal／タブ名の変更／仮のタブと戻る・進む／右クリックメニューと`＋`／倍率のスライダー／選択開始／アイコン／検索フォルダの指定／行番号 | 要件定義 6.4・6.2・6.8・11.7・6.5・7.7・11.3・6.7・9・11.4・2.4 |
| 2026-09-08 | 単語の色分け（単語帳→単語チェックモード） | 要件定義 7.9、[単語チェックモード要件](単語チェックモード要件.md) |
| 2026-09-08 | New Tab／右クリックメニューの整理／設定画面の見直し／Default Shell／自動保存の挙動／Terminalの背景・文字色／書き手の報告3件 | 要件定義 6.5・6.7・9・6.8・8.1・6.3 |
| 2026-09-09 | 要件 7.8（ルビ・傍点・縦中横） | 要件定義 7.8 |
| 2026-09-10 | E1（検索・置換の操作強化）／E4（現在位置と行移動） | 要件定義 7.7・6.6・11.2 |
| 2026-09-10 | E3（選択・行移動・インデント） | 要件定義 7.1・11.2 |
| 2026-09-10 | E2（文字コード・改行コード） | 要件定義 4.3・6.6・11.2 |
| 2026-09-10 | E9（ルビなどの記法のON/OFF） | 要件定義 7.8・9.1 |
| 2026-09-11 | E10（後から選択して箇条書き、印の読み方・見せ方、CommonMark §5.3） | 要件定義 7.1・7.3.2・9.1・11.2 |
| 2026-09-11 | ファイルエンコードの対応（E2で具体化して実装） | 要件定義 4.3 |
| 2026-09-11 | 書き手のレビュー：保存まわり3件（保存先の衝突／答えを待つあいだの形／読み直しの文字コード） | 要件定義 7.6・8.3 |
| 2026-09-11 | 書き手のレビュー：保存方針の合意 S1・S2（**要件は確定、実装はこれから**） | 要件定義 8.4・8.3 |

**作り方の罠は技術検証.mdの6.xにある**（2026-09-11に6.34〜6.36を追加：数え方／状態と画面／
popupの寄せ方）。**現在できることはREADME.mdにある。**

---

# まだ手の付いていないもの

## 実行ファイル自体のアイコン（残り）

絵は`ui/icon.png`（紫地にアイボリーの`RE`）で決まっていて、タイトルバーとタスクバーには
出ている。**エクスプローラーで見えるアイコンだけがまだ**——Windowsのリソースを実行ファイルへ
埋め込む必要があり、そのためにこのリポジトリが持っていないビルド依存（`winres`等）が要る。

## 部品化

これはアプリの機能ではありませんが、今回試行錯誤しながら調整した機能は部品化して再利用可能に
しておくと、将来のために有効です。たとえば、右クリックメニューの方式やPaperの書式など。
部品化出来る要素を洗い出して、個別に最適化していくことも考えましょう。

## フォントの設定（E11）

今、見出しや本文のフォント設定は、フォントとサイズと色だが、太字・斜体・取り消し線にも
対応した方が良い。

→ 表示設定（要件 9）の拡充。**書く手より読み返す目に効く。**DirectWriteの字体は
`DWRITE_FONT_WEIGHT`／`STYLE`で持てるが、**取り消し線は組版ではなく線を引く仕事**なので、
ここだけ描く側（`directwrite_render`）の話が混ざる。

## Markdownのリンク表示（E12）

リンクは対象外にしてきましたが、フルパスでリンク可能な物はリンクしてもいいと思いました。
また、Obsidian表記でファイルの所在が分からない場合は、リンク表示しますが赤字にして
リンクがきれているという示し方でもいいと思います。

→ **E11と同じで、書く手より読み返す目に効く。**切れたリンクを赤で出すことは、
**働いていない状態を画面に出す**という単語チェックモード要件 2.1.1 の2番と同じ根である。
所在を確かめる道（ディスクに触る）は、**開いていないフォルダは読まない**（要件 5.1）に
当てて決めること。

---

# Editor／Terminalの追加候補（2026-09-08レビュー）

**レビューからの提案であり、採用済みの仕様を置き換えるものではない。**
優先度は、P1＝日常操作の手数を減らすもの、P2＝使う場面に応じて順次、P3＝後回し。
判断の基準は要件3の「縦書きで文章を書く人」「編集と校正が軽いこと」。
標準的なEditorの機能でも、クイックオープン、IDE機能、内部リンク解決、表記ゆれの自動判定は
既に対象外と決めているので再提案しない。

**P1とP2のEditor側（E1〜E4）は2026-09-09〜11に全部済んだ**ので、ここに残るのは
Terminal側（T1〜T5）と、Editorの後回し分（E5〜E8・C1〜C4）である。

**レビューのあとで動かしたもの**（理由つき）：

- **T1（端末の出力検索）はP2へ。**P1に端末が2つあるのは前に出しすぎ（**端末は残すが前に
  出さない**）。T2（複数行貼り付けの確認）は安全側なので小さいままP1に残す。
- **E7（保存版との差分）はE5・E6より上。**「軽く編集・校正がしやすいこと」に直接効く。
- **C1は二つに割った。**表示と動作の不一致は0番として2026-09-09に済み、**一覧そのものが
  ここに残っている。**

## P1


### T2 Terminalの貼り付け・終了操作の整理

**困る場面：**改行を含む文字列を貼るとコマンドが実行され得る。タブを閉じる／シェルを替えると、
その中の作業も終わる。通常の文書タブとは結果が違うので、その違いを操作時に分かるようにする。

- Ctrl+Shift+Vを貼り付けの別入口として用意する。既存のCtrl+Vとbracketed paste対応は維持する。
- 複数行貼り付けの確認を設定で選べるようにする。確認する場合は行数と内容を見せ、キャンセル時は何も送らない。
- 下段の下書きから送る経路にも適用範囲を定める。現在の「末尾改行の有無で実行が決まる」規則を黙って変えない。
- 動作中のシェルを閉じる／切り替える前に確認できる設定を設ける。既に終了したシェルには確認を出さない。
- **完了の目安：**Ctrl+Cは常に割り込みとして届く。閉じる操作を取り消すとシェルと出力が保たれる。シェルの生存だけで子コマンド実行中と断定しない。

## P2

### T1 Terminalの出力検索

**困る場面：**出力が長いと、ホイールだけで以前のエラーを探す必要がある。
スクロールバックとコピーは既にあるので、その履歴を検索できるようにする。

- 対象はフォーカス中のTerminalの画面と保持している履歴。上段・下段のどちらにも同じ入口を設ける。
- 前／次、一致箇所の強調、大小文字の区別を用意する。ヒットした行までスクロールする。
- Ctrl+Shift+Fを候補とし、Ctrl+Fはシェルへ渡す。要件11.7の「編集器が先に取るキー」を採用時に追記する。
- **完了の目安：**画面外の過去の出力へ移動し、その箇所をコピーして本文へ戻れる。履歴上限から消えた出力は検索できないことを区別する。
- 参考：[Windows Terminalの検索](https://learn.microsoft.com/en-us/windows/terminal/search)。このEditorでの優先度はレビュー判断。

### E5 Explorerの再表示・整理

**大きなツリーから近くの原稿へ行くため**に、現在の文書をツリー内で示す操作、すべて折り畳む操作、
明示的な更新を用意する。「Reveal in Explorer」はWindowsのExplorerなので別に置く。
まずこの3つを追加し、外部作成・名前変更の自動反映や検索対象の除外指定は次の段階とする。
更新しても選択・展開状態を可能な範囲で保持する。クイックオープンを復活させる案ではない。

### E6 閉じた文書タブを開き直す

**誤って閉じた直後に、同じ場所へ戻るため**に、Ctrl+Shift+T等で最後に閉じた文書を再表示する。
文書・キャレット・スクロール・表示モードを戻す。既存のファイル履歴やAlt+←の閲覧履歴とは区別する。
作業コピーを残した文書はそれを使い、明示的に破棄した未保存本文は復活させない。
Terminalのプロセス復元はこの機能に含めない。

### T3 シェル設定の画面編集と開始フォルダ

**WSLのディストリビューションやシェルを足すとき、設定ファイルを探して編集しなくて済むようにする。**
名前・実行ファイル・引数・開始フォルダを設定画面で編集し、複製・並べ替え・既定指定ができるようにする。
「このフォルダでTerminalを開く」をExplorerに置き、現在の文書の親フォルダも選べるようにする。
空白・日本語を含むパスを扱い、既存のシェルの作業場所は変えない。WSLの開始位置はWindowsのパスと
同じ文字列を渡せばよいとは限らないので、シェルごとに確認する。
参考：[Windows Terminalのプロファイル設定](https://learn.microsoft.com/en-us/windows/terminal/customize-settings/profile-general)。

### T4 Terminalの履歴閲覧・コピー・リンク操作

**ログを読み返して原稿やメモへ持ち帰るため**に、「最新の出力へ戻る」、履歴のページ送り、保持行数の設定、
選択テキストのコピーを補強する。出力を遡っている間は勝手に最下部へ戻さず、新しい出力があることを示す。
自動折り返しで生じた行境界と実際の改行をコピー時に区別する。URLはCtrl+クリックで開けるようにし、
通常の選択操作と分ける。画面消去と履歴消去は名前・効果を区別し、シェルを再起動しない。

### E7 保存版との差分表示

**推敲でどこを書き換えたかを確かめるため**に、編集中の本文と保存版の差分を読めるようにする。
最初は比較表示に絞り、部分的な取り込み・巻き戻しは後回し。外部変更の競合時にも比較へ入れるとよい。
比較しただけでは本文を変更せず、通常の保存・Undoに影響しない。Git連携や自動マージは前提にしない。

### C1 ショートカット一覧と操作の検索

**機能はあるのに入口を知らない状態を減らす。**まずメニューに書いたショートカットと実際の動作をそろえ、
Editor／Terminal／検索欄ごとの一覧をアプリ内で参照できるようにする。
次に、操作名から探して実行する小さな一覧を追加候補とする。ショートカットの変更はさらに後でよい。
既存のCtrl+D・Ctrl+K・Alt+Y等の割り当てを他のEditorに合わせて上書きしない。
**確認項目：**画面確認ではCtrl+Wでタブが閉じず、メニューにはCtrl+Wと表示されていた。
フォーカス位置・キーイベントを調べて、表示と動作の不一致かを最新ビルドで再確認する。

## P3：用途が固まってから

- **E8 見出しの折り畳み・ブックマーク：**長い章の一部分だけを見て書くため。折り畳みは表示だけに効かせ、保存内容を変えない。既存アウトラインで足りるかを先に試す。
- **T5 全画面TUIの互換性：**マウス追跡、フォーカス通知、代替画面、リサイズ、日本語・絵文字の幅、キー送信を代表的なTUIで確認する。現在はモードを記録していても入力送信まで結び付いていないものがある。TUIを日常利用するならP1へ上げる。
- **C2 表示設定のプリセット：**文字・余白・紙色等を名前を付けて保存し、読み返し用／執筆用に切り替える。要件13.2の具体化。本文や文書ごとの単語チェックモードを勝手に変更しない。
- **C3 実行ファイルのアイコン：**本ファイル冒頭の残りを完了させる。日々の編集への効果は小さいが、配布前には必要な仕上げとして扱う。
- **C4 GPU化：**入力・スクロール・長文の初回表示を測り、組版・描画・画像転送のどこが待ち時間を作っているかで決める。GPUを使ったことではなく、文字欠けや位置ずれなしに待ち時間が減ったことを完了条件にする。

## 既に採用済みの要件との順序

要件7.8のルビ・傍点・縦中横、要件7.10の縦書きPDFは、今回の候補とは別に採用済み。
最新ソースでは対応実装を確認できていない。**縦書き原稿を主用途にする現在の方針では、7.8を
上のP1と並行する優先課題、7.10をP2相当**として扱うのがよい。汎用Terminalの拡張だけが
先行して、原稿を書くために必要な既存要件が後ろへ押し出されないようにする。

一般的な編集操作の比較には[VS Codeの基本編集](https://code.visualstudio.com/docs/editing/codebasics)も参照した。
ここに書いた採否・優先順位・完了条件は、このEditorの用途と現在の実装に対するレビュー提案である。

---

# 書き手のレビューから（2026-09-11）

**元の記録は[レビュー指摘_2026-09-11_実操作.md](レビュー指摘_2026-09-11_実操作.md)にある**
（実操作の手順・確認環境・問題なく動いた範囲も、そちらが正）。ここに置くのは**まだ手の付いて
いないもの**だけである。

**済んだもの**：コードレビュー3件（保存先の衝突P1・答えを待つあいだの形P2・読み直しの
文字コードP2）は2026-09-11に直し、要件定義 7.6・8.3 へ転記した。

## 直すもの

### R1［P2］本文で`Ctrl+O`を押しても「開く」が出ない

メニューには`Ctrl+O`と書いてあるのに、本文からは効かない（`ui/editor-pane.slint`の本文キー
処理に`O`の分岐が無い）。**表示と動作の不一致**で、C1の0番と同じ根。
**完了の目安**：横書き・縦書きの本文から開くダイアログが出る。キャンセルで同じ本文へ戻る。
端末のキーの扱いは別に決める。

### R2［P2］Undoで元に戻っても未保存の印が消えない

保存した内容と同じところまで戻しても、タブのドットと下端の`*`が残る（Undoは
`document.text.borrow_mut()`を通り、無条件に編集済みになる）。**「試して戻しただけ」の原稿と
本当に保存すべき原稿を見分けられない。**
**完了の目安**：入力→Undo、保存→編集→Undo、全置換→Undoで保存済み表示に戻る。そこから
Redoすれば再び未保存になる。同じ文書の全ペインで一致する。

### R3［P2］Settings上のEscが、背後の検索バーを先に閉じる

`Esc`1回目でSettingsが残り、背後の検索バーが閉じる。2回目でSettingsが閉じる。
**閉じる対象が、見た目の重なりと逆である。**
**完了の目安**：検索バー＋Settings、置換バー＋Settingsで、Esc1回がSettingsだけを閉じる。

### R4［P2・原因未確定］右ペインを閉じた直後、続く`Ctrl+W`が効かない

ペインが消えたあと、本文をクリックするまで次の`Ctrl+W`が効かなかった。**見た目のアクティブ
ペインと、実際の入力フォーカスがずれている**可能性がある。**まず手で再現するところから。**
**完了の目安**：ペイン閉鎖後にクリックを挟まず、矢印移動・`Ctrl+F`・`Ctrl+W`が残ったペインで
効く。

## 画面の整理（提案、採用前）

- **U1 自動退避の名前と説明位置**：`FILES / Auto-save / Off`の説明が`MARKUP`の3つより下に
  あり、どの設定の説明か読み戻すことになる。**実態に合う名前**（自動退避）にして、説明を
  その直下へ。「元ファイルへの反映は`Ctrl+S`」も短く明示する。
- **U2 本文の右クリックにUndo／Redoの入口**：全置換や箇条書きをマウスで試したあと、同じ
  マウス操作で戻れる入口が無い。履歴が無いときは無効表示にする。
- **U3 右クリックメニューの情報量**：切り貼りから箇条書き・選択・Terminal・下書き・戻る／
  進む・分割・倍率まで縦に長い。U2を入れるとき、**本文編集を上段にまとめ、分割・倍率などは
  ペインのメニューへ寄せる**案を相談したい。

## 追加候補（採否これから）

- **F1 空白・タブ・実改行を必要なときだけ見せる**：字下げのずれ、不要な空白、折り返しと実改行
  の違いを調べるため。最初の範囲は半角空白・全角空白・タブ・実改行の表示切り替えで、通常はOff。
  **表示だけを変え、保存内容・コピー文字列・Undo履歴を変えない。**自動で空白を削除する機能とは
  別で、Markdownの意味を変える整形は同時に入れない。
- **F2 参照文書の読み取り専用**：資料を横に置いて書くときの誤編集防止。最初の範囲は文書単位の
  編集ロックで、選択・コピー・検索・スクロールは維持する。タブ等に状態を出し、同一文書の全
  ペインでそろえる。OSのファイル属性変更とは分ける。

## 合意済みだが未実装（要件は転記済み）

- **S1［P2］新規メモは開いているタブとして保持する**（要件定義 8.4）。閉じるときは
  「名前を付けて保存／破棄／キャンセル」で、**「作業コピーを保持して閉じる」は設けない。**
  アプリ終了時は退避して次回起動で復元する。
- **S2［P1］外部変更を早期に知らせ、双方の内容を確認して解決できるようにする**（要件定義 8.3）。
  保存を待たず常設表示で知らせ、**「外部版と比較する」入口**を設ける。最初の範囲は双方を
  保持した比較と手での取り込みで、自動マージは必須にしない。

---

# 次に進める順（2026-09-08に書き手が承認、以後の追記を反映）

```
0. 機能追加の前   一致条件のずれ / Ctrl+W / 退避2件   ← 2026-09-09に済み
1. 要件 7.8       ルビ・傍点・縦中横                  ← 2026-09-09に済み
2. E1             文書内検索・置換の操作強化          ← 2026-09-10に済み
3. E4の残り       Ctrl+G                              ← 2026-09-10に済み
4. E3             行移動・箇条書き継続・ダブルクリック  ← 2026-09-10に済み
5. E2             文字コード・改行コード                ← 2026-09-10に済み
6. E9             ルビなどの記号のON/OFF               ← 2026-09-10に済み
7. E10            後から選択して箇条書き               ← 2026-09-11に済み
8. レビューの保存3件  保存先の衝突・形が落ちる・読み方が戻る ← 2026-09-11に済み
9. S2             外部変更を早く知らせ、比べて解決する   ← 原稿保全（P1）。次はここ
10. S1            新規メモはタブとして保持する
11. U1            自動退避の名前と説明位置
12. R1〜R3        Ctrl+O／Undoの印／Escの順序          ← 小さく確実、いつ挟んでもよい
13. R4            右ペインを閉じた直後のCtrl+W（まず手で再現）
14. E11           フォント設定の太字・斜体・取り消し線
15. E12           Markdownのリンク表示
```

**順は書き手のレビューの推奨に合わせた**——「前回の原稿保全の指摘とS2を優先し、S1・U1で
保存方針と表示をそろえる。今回のR1〜R3、R4の手動再確認、U2も進める。F1・F2は必要性を判断
して追加する」。**U2・U3・F1・F2はまだ採否が決まっていない**ので、この並びに入れていない。

## いまの位置（2026-09-11、セッションの区切り）

**E10と、レビューの保存3件まで済んだ。668テスト、fmt差分なし、
`cargo check --all-targets`の警告0件。****次はS2**（外部変更の早期通知と比較）。

**進め方**（4日やって効いた形）：**要件の1項目を3〜5に刻み、刻みごとにreleaseを作って
画面確認を頼む。**決めどころは選択肢にして訊く——**見た目と代償を並べる**と書き手はすぐ決める。
**ただし訊く前に「その画面が要るか」を疑う。****報告の半分は「効いたのが見えない」**である。

**この4日で私が繰り返した間違い**（次の人が同じ穴に落ちないように）：

- **位置を動かしながら数えた**（技術検証 6.34）。数え方を写すときは、写す先ではなく写し元を読む。
- **ログが正しいことを、画面に出ている証しだと思った**（6.35）。
- **片側だけの規則を書いた**（「終わりが行頭なら入らない」だけ書いて、頭のほうを書いていなかった）。
- **画面とソースで名前が食い違っているのに、私の呼び方だけ直そうとした**（要件定義 9.2）。
- **替えても画面が1画素も変わらない設定を2つ作った**（言われて両方消した。265行減った）。
- **仕様が同格に認めているものに、勝手に順位を付けた**（`-`を「標準」と呼んだ）。
- **この文書に作業記録を書き足し続けた**（それがこの整理の理由である）。
