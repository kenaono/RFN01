# Tag機能（Obsidian互換）——仕様と実装計画

2026-09-23、書き手の求め：「TAGの機能を設けましょう。Obsidian互換で、検索においてもTag検索が出来るように。また、階層Tagにも対応し、Tag Viewも。」
同日の会話で下の仕様に合意した。Linear Issueは未登録（登録したらこの節に番号を書く）。

## 合意した仕様

### 1. タグとして読むもの（Obsidian互換）

- **本文の`#タグ`**：行頭か空白の後の`#`に、字（日本語を含む）・数字・`_`・`-`・`/`が続くもの。数字だけ（`#123`）はタグではない。`#`の直後が空白なら見出しで、タグではない。
- コードブロック・インラインコード・リンクの中の`#`、URLの`#`（前が空白でない）は読まない。全角`＃`は対象外。
- **フロントマター**（1行目の`---`から次の`---`まで）の`tags:`／`tag:`。`[a, b]`、`a, b`、`a b`、次の行からの`- a`のどれでもよく、`#`は付いていても無くてもよい。
- **階層**：`#小説/人物/主人公`。親のタグ（`#小説`、`#小説/人物`）も付いているとみなす。
- 大文字小文字は同じタグ。表示は最初に見つかった書き方。

### 2. 本文での見え方

- ライブプレビュー（横書き・縦書き）でタグを色付けする。ソース表示は記法のまま。字は消さない（`#`も見える）。

### 3. 検索

- 検索パネルの欄に`tag:#小説`（`tag:小説`も同じ）で、`#小説`と`#小説/…`を持つ文書のタグの位置を一覧する。
- `tag:#小説 主人公`：タグを持つ文書の中で、残りの語（`主人公`、句として）を探す（AND）。`tag:`を複数並べるとすべてを持つ文書。
- 範囲は**開いているWorkspaceの全フォルダ**（最後に見ていたのがExplorerでも。Tag Viewが数えたものと同じ所を探すため）。検索パネルでフォルダを絞り込んでいればそのフォルダ。Workspaceを開いていなければ作業フォルダ。

### 4. Tag View

- 入口：左のアイコン列、Bookmarksの下の「Tags」。**Workspaceを開いているときだけ**中身があり、開いていなければその旨を表示する（Bookmarksと同じ）。
- 中身：タグの階層の木（▸／▾で開閉、初期状態は子を畳んだ状態）、各行にそのタグ（子を含む）を持つ文書の数。名前順。
- 上部：Collapse All／Expand All、絞り込み（Filter Tags…、打った文字を含むタグとその親だけ出す）。
- **行を押すと検索パネルへ切り替わり、欄に`tag:#タグ`が入って検索する**（Obsidianと同じ）。
- Workspaceの索引が更新されると描き直す。未保存の編集も索引の優先更新を通じて反映する。

### 5. 本文からの操作

- **Ctrl+クリック**：タグの上なら、そのタグで検索する（リンクと同じ手つき。Ctrlを押している間はポインタが手になる）。
- **補完**：本文で`#`に続けて打つと、Workspaceにあるタグを候補に出す（`[[`の補完と同じ枠・操作：上下・Enter・Esc）。行頭の`#`だけでは出さない（見出しの`# `を打つ途中なので）——1字打てば出る。

### 6. 同じ作業単位に加えたもの（2026-09-23、書き手）

- **フロントマター**：ソース表示ではそのまま、ライブプレビューでは非表示（行ごと詰める、印刷も同じ）。カーソルが中に入ったときだけ全体を淡い原文で見せる。中の`# `は見出しにしない、`---`は区切り線にしない、文字数に数えない。
- **リンクのポインタ**：既に脚注と同じ仕組みで手の形になっていた（書き手が確認、変更なし）。

### 範囲外（必要になったら相談）

タグの一括改名、タグの色分け、件数順の並べ替え、フロントマターの色付け。

## 実装計画

| 部分 | 場所 | 内容 |
| --- | --- | --- |
| タグの読み取り | `src/tags.rs`（新規、純粋な計算） | `tag_here`（本文1か所）、`tags_in`（文書全体：フロントマター＋本文、コードを除く）、`tag_at`（Ctrl+クリック用）、`matches`（階層込みの一致）、`split_query`（`tag:`の分離）、`TagTree`（View用の木） |
| 本文の色 | `document.rs`・`text_blocks.rs`・`directwrite_render.rs` | `Marks::tag`を足し、`push_marked_recording`で`tags::tag_here`を読む。描画はリンクと同じく字の色だけ |
| 索引 | `workspace_index.rs`・`workspace_priority.rs` | `Entry::tags`。キャッシュの形式を2版へ（古いものは読まずに作り直す） |
| 検索 | `searcher.rs`・`find.rs`・`main.rs`（`open_result`） | `tag:`を分けて文書を絞る。一覧の行は一致の長さを持ち、開くときはそれで指し直す |
| Tag View | `ui/app-window.slint`・`main.rs` | 左の一覧（`left-rows`）を共有、`LeftTab::Tags`（6）。畳みは`RenderCache`、件数は`tag-counts` |
| Ctrl+クリック | `main.rs`（`open_link_at`・`link_hover_at`） | リンクより先にタグを訊く |
| 補完 | `link_completion.rs`・`main.rs` | `TriggerKind::Tag`。候補は索引のタグ＋今の文書のタグ |

再利用：左の一覧の行・▸／▾・Collapse/Expandのアイコン（Outline）、検索パネル（`search_work_folder`）、リンク補完の枠と確定処理、Ctrl+クリックの経路。本文の読み方は`document.rs`の1か所に足す。

## 完了条件

- 自動試験：`tags.rs`の読み取り（行頭・空白の後・数字だけ・コード・URL・フロントマターの各形・階層・大小文字）、検索の絞り込みとAND、キャッシュの読み書き、補完の検出、プレビューの印。
- 全体の`cargo test`・`fmt --check`・`check --all-targets`が通る。
- 実操作（書き手のAccept）：下の確認手順。

## 確認手順（Accept用）

1. Workspaceを開き、原稿に`#小説/人物 と #メモ`、別の原稿のフロントマターに`tags: [小説]`と書いて保存する。
2. ライブプレビューで`#小説/人物`と`#メモ`に色が付く（縦書きでも）。
3. 左の「Tags」（値札のアイコン、Bookmarksの下）を押す → `メモ`（右に1）、`▸ 小説`（右に2）。▸で`人物`（右に1）が出る。
4. `小説`を押す → 検索パネルに`tag:#小説`が入り、2つの文書が出る。行を押すとタグの位置へ行く。
5. 検索欄で`tag:#小説 人物`→ 1つ目の文書だけ。
6. 本文の`#メモ`をCtrl+クリック → `tag:#メモ`で検索。
7. 本文で空白の後に`#小`と打つ → 候補に`小説`・`小説/人物`。Enterで確定。
8. `testdata\51_タグ.md`（フロントマター付き）をライブプレビューで開く → 先頭は「タグの確認」の見出しから始まる。文書の先頭で↑ → `---`〜`---`が淡く出る。本文へ戻ると消える。ソース表示では常に見える。
