# メニューA案：画面仕様と実装計画

2026-09-20。A案の採用と既存機能のみのメニュー化、および本書の画面・操作は合意済み。実装と実画面検証を進めている。検証結果は「メニューA案_実装検証.md」に分け、ユーザーAcceptと区別する。

## 画面の配置

タイトルバーとメニューを同じ一段に置く。開閉で本文・TAB列・サイドバーの高さや位置を変えない。

```text
閉じた状態
┌─────────────────────────────────────────────────────────────┐
│ アプリアイコン  原稿.md * - RFN Edit          移動領域   ─ □ × │
├─────────────────────────────────────────────────────────────┤
│ ☰ │ 既存のTAB列・本文・サイドバー                            │
│   │                                                         │
└─────────────────────────────────────────────────────────────┘

開いた状態
┌─────────────────────────────────────────────────────────────┐
│ アイコン File Edit Insert Layout View Run   移動領域   ─ □ × │
├─────────────────────────────────────────────────────────────┤
│ ☰ │ 既存のTAB列・本文・サイドバー                            │
│   │                                                         │
└─────────────────────────────────────────────────────────────┘
```

- 左上の既存Menuアイコンを開閉入口にする。アイコンを増やさず、従来のアプリメニューPopupを新しいバーの開閉に置き換える。他の左アイコン・設定ボタンは維持する。
- バーを開くと、タイトル文字の表示場所に6分類を置く。右端の最小化・最大化／元に戻す・閉じるは同じ位置に残す。閉じると通常のタイトル表示へ戻る。
- OSへ渡すウィンドウタイトルは既存の `document-title + " - RFN Edit"` を維持する。バー表示中もAlt+Tab等の識別情報を消さない。未保存表示は既存のTAB・下部表示も維持する。
- タイトルバーの高さはDPIとウィンドウ状態に応じて一定とし、バー開閉では変えない。メニューの文字と背景は不透明。本文の透過処理と分離する。
- メニュー項目・アプリアイコン・右端ボタン以外の余白をウィンドウ移動領域にする。項目のクリック／ドラッグをウィンドウ移動として処理しない。
- 余白のドラッグで移動、ダブルクリックで最大化／元に戻す。右端ボタンとウィンドウ外周のリサイズ、Snap、最大化時の作業領域を維持する。最大化ボタンのホバー動作も検証対象。
- 最小幅640pxで右端ボタンを最優先に確保する。分類の幅は翻訳後の文字幅を測って決める。6分類と移動領域が収まらない場合だけ、末尾の分類を「…」へ移す。文字を重ねたり、本文上へ別の常設段を増やしたりしない。
- メニューは本文の上にPopupとして開く。左右端では子メニューの開く方向を反転し、高さ不足はPopup内スクロールで扱う。

## 開閉と操作

初回および再起動直後は閉じる。今回、開閉状態の設定保存は追加しない。

| 状態・入力 | 次の状態／操作結果 | フォーカス |
| --- | --- | --- |
| 閉じた状態でMenuクリック | バーを表示、ドロップダウンは未選択 | 本文等の元の入力先を保持 |
| バー表示中にMenuクリック | 子メニューも含め全て閉じる | 元の入力先へ |
| 分類をクリック | そのドロップダウンを開く | メニュー操作へ。本文の選択は保持 |
| 開いている同じ分類をクリック | ドロップダウンだけ閉じる | 元の入力先へ |
| ドロップダウン中に別分類へ移動 | その分類へ切替 | メニュー内 |
| 子メニューの行を選ぶ | 子メニューを開く | 子メニュー内 |
| 実行可能な末端項目を選ぶ | 一回実行しPopupを閉じる。バーは残す | 編集なら元対象、画面を開くならその画面、移動なら移動先 |
| 無効項目を選ぶ | 実行しない | メニュー内 |
| 外側をクリック | Popupを閉じる。バーは残す | クリック先。本文をクリックすれば通常のキャレット移動を許す |
| Esc | 子Popup→親Popup→バーの順に一段閉じる | 最後は保存した入力先へ |
| アプリが非アクティブになる | Popupを閉じる。バー表示状態は維持 | 再アクティブ時に元対象を復帰 |
| 元TAB／入力欄が消える | 保存した対象のコマンドを無効化しPopupを閉じる | 残っている通常の対象へ。別文書へコマンドは実行しない |
| 既存モーダル・印刷・比較の専用画面中 | 主メニュー操作を無効化 | 専用画面の操作を維持 |

キーボード案：本文・通常入力欄ではF10でバーを表示して先頭分類へ、左右で分類移動、下／Enterで開く。Popup内は上下で有効項目を巡回、右で子を開き左で親へ戻る。Home／Endは先頭／末尾、Enterで実行。無効項目と区切りは飛ばす。

Terminalには既存のF10送信があるため横取りしない。TerminalからはMenuクリックで入り、メニュー内の矢印等を使えるようにする。Alt+文字の既存Emacs操作も維持する。IME変換中はF10／Escをメニューが奪わず、変換操作へ渡す。

## 今回表示する項目と条件

[操作接続仕様](メニュー操作接続仕様.md)のIDを使う。各項目の処理・副作用は同書を参照し、この計画で機能を追加しない。

| 分類 | 今回の項目 | 対象の原則 |
| --- | --- | --- |
| File | F01〜23 | 既存の作成・開く・保存・閉じる・ファイル操作・印刷・終了。新しいPanel保存範囲は追加しない |
| Edit | E01〜26 | 元の本文／Panel／Terminal／入力欄へ接続。未対応の組合せは無効 |
| Insert | I41〜45のみ | 既存の箇条書き・番号付きリスト・振り直し |
| Layout | L01〜07 | 縦横、Pane背景、TAB本文背景、TAB見出し色、Terminal／Panel個別外観 |
| View | V01〜33 | 既存のパネル・表示・移動・倍率。行番号／空白表示は既存の方向別共有値を操作 |
| Run | R01〜13 | 既存の比較・出力保存・ログ・転送・シェル切替。主／下段の現行対応差を維持 |

- Helpと新規挿入機能は表示しない。New Tabは＋専用。
- 共通項目は位置を固定して無効表示、Terminal固有群は対象時のみ表示する。無効項目に必要なら短い理由を添える。
- Save Allは「すべて保存（Editor Panelを除く）」、Close Savedは「未編集のTABを閉じる」とし、実際の対象を説明する。
- 背景は「このTABの本文背景…」「このTAB見出しの色…」「このPaneの背景…」と区別する。Pane背景の説明に個別色の解除を明記する。
- 表示設定は「行番号（横書き共通／縦書き共通）」「空白・改行記号（横書き共通／縦書き共通）」と作用範囲を明示する。共有連動設定がある場合は実際に連動する方向も説明へ反映する。
- Previewの動作は既存のショートカット43と整合させる。Viewer中は「Viewerを終了してSourceへ」と結果を明示。ReadOnlyでは切替不可。単体preview-toggledだけを呼んでキーと異なる動作にしない。
- New File／New Terminalは直接作成する。Terminalの登録シェル選択は既存一覧を使う。新しい種類選択画面は作らない。

## ソースで確認した接続条件

- `ui/app-window.slint` は通常のWindowで、タイトルは130行付近、最小幅640px。現行にA案の独自タイトルバーはない。
- `src/main.rs` は `on_winit_window_event` を既に使い、移動・サイズ・DPI・Dropを処理する。メニュー追加で登録を上書きせず、既存ハンドラに必要な通知を統合する。
- 終了は同ファイルの `on_close_requested` にある。新しい×／File→Exitも同じ終了要求へ送る。強制終了で代用しない。
- `src/ime.rs:window_handle` でHWNDを取得できる。入力欄のIME位置・縦書き処理を維持する。
- vendored `i-slint-backend-winit/winitwindowadapter.rs` は装飾変更を持つが、それだけでWindowsタイトルバーへSlint要素を描けるわけではない。現行の `src/ui` では独自の非クライアント領域ヒットテストを確認できない。
- `ui/controls.slint:MenuRow` はenabled／checked／hoveredとマウス起動を持つ。メニュー全体のキー巡回、対象保持、子Popup管理は新設する。
- ソフトウェア描画とWindowsの透過処理に既存vendor差分がある。装飾を外すだけの実装を完成形とせず、本文透過・IME・Drop座標への影響を検証する。

## 実装の順序と変更境界

### 1. A案のウィンドウ枠を先に検証

新規 `src/window_chrome.rs` と `ui/title-menu-bar.slint` に責務を分ける案。前者はWindowsの枠・ヒットテスト・ウィンドウ操作、後者は表示とメニュー領域。具体的なWin32接続方式は試作で確定する。

アプリの既存HWNDへ枠処理を統合し、タイトル行を描く領域を確保する。Windowsの標準的な操作を維持できるかを先に試す。最大化・Snap・リサイズ・高DPI・本文透過・IMEのいずれかが壊れる方式は採らない。必要なvendor変更は該当Windows処理に限定し、既存差分を保持する。A案が成立しない場合もB案へ自動変更せず、原因と修正可能性を報告する。

完了条件：Menu開閉で本文の矩形が変わらず、通常／最大化／別DPIモニターでタイトル行とウィンドウ操作が機能する。空TAB・本文・Terminalの各画面で透過を含めて確認。

### 2. コマンド定義と対象保持

新規 `src/menu_commands.rs` に表示ID・パラメータ・対象・有効判定・実行を集約する案。文書／TAB／Panel／Terminalの識別子を保持し、位置indexだけには依存しない。登録シェル等は実行直前に生存を再確認する。

`main.rs`／`wiring.rs`／`saving.rs`／`diff_view.rs`等にある既存処理は、明示的な対象を渡せる入口へ必要最小限分離する。保存・閉じる・比較等の中身や対象範囲は変更しない。遅延処理には元の対象を渡す。

完了条件：二つの主Pane・Editor Panel・下段Terminalを区別でき、メニューへフォーカスが移っても別の対象へ実行しない。対象消失は安全に中止する。

### 3. メニューの表示と入力

`ui/title-menu-bar.slint`に6分類を表示し、WindowsのネイティブPopupへ既存コマンドを接続する。項目・子メニューのキー巡回はWindows、分類間の移動はメニューループのフックで扱う。バーの選択状態を描画してからPopupを開く。`app-window.slint`の旧左上Popupから入口を移す。

TextInput操作は入力欄の公開アダプタから実行する。既存のpaste経路を維持し、キーイベントの偽装送信は使わない。Undo/Redoの履歴も対象欄自身へ接続する。New Tabの既存キーIDは詰め直さず、＋専用の方針に合わせて互換処理を決める。

完了条件：マウス／キーで全表示項目に到達でき、Esc／外側クリック／実行後のフォーカスが上表どおり。TerminalのF10と既存Emacsキーを壊さない。

### 4. 既存操作の接続

File・Edit→Insert・Layout・View→Runの順で接続する。各群がまとまった段階で対象違いと取消を確認する。現在未対応のTerminalログやPanel機能を接続作業のついでに追加しない。新しい記法挿入は作らない。

完了条件：操作接続仕様の今回対象IDが、実行可能な既存操作または理由の明確な無効状態へ対応し、空の項目・無反応な項目がない。

### 5. 統合検証と確認用ビルド

- 対象解決・有効判定・対象消失・設定済みキーID互換に必要なテストを追加する。表示名だけを写したテストは増やさない。
- 関連する既存の保存／復旧／Panel／Terminal／印刷／比較テストを実行。その後一度、全体テストと確認用ビルドを作る。依存が揃っていればoffline実行を使う。
- 実画面で640px、通常サイズ、100/125/150/200% DPI、最大化、Snap、IME、透過、Drag & Drop、長いメニュー、英日表示を確認する。確認したものと未確認を記録する。
- 要件定義のメニュー配置・旧制約、技術検証、キー一覧の影響箇所を更新する。
- 検証したコミットと実行ファイルを一致させ、以下のAccept手順を添える。実装だけでIssueをCloseしない。

## Acceptで確認する操作

1. Menuを開閉し、タイトル行だけが変わり本文が動かない。
2. メニュー余白で移動・最大化、右端ボタン・Snap・リサイズを操作できる。
3. 二つの文書とEditor Panelで選択してCopy／Saveを実行し、選んだ対象へ作用する。Escで選択を失わない。
4. Insertで既存リスト操作だけが使え、未実装のルビ等は表示されない。
5. 背景3種、行番号／空白表示の作用範囲が表示と一致する。
6. 主／下段Terminalで出力保存・検索、主Terminalでログ開始／停止を実行する。片方のログ停止が他方を止めない。
7. 未保存のClose／Exitをキャンセルでき、印刷・比較は既存画面へ正しく進む。

R10（Panel側から親Terminalのログを新規Panelへ取り込む追加入口）は接続仕様に「採用要判断」として残り、今回のメニューには表示しない。既存の主Terminal側の開始・停止、取り込み先Panel側の停止を接続する。
