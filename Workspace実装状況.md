# Workspace・リンク補完の実装と検証

RFN01-11 / RFN01-5を一つのPRで扱う。ユーザーの指示で、SonnetからCodexへ実装を引き継いだ。現行の変更仕様は `Workspaceレビュー対応.md`。

## 2026-09-19 再レビュー：共通ツリーと編集中リンク

- Explorer・Workspace共通のフォルダ行にフォルダアイコンを追加。展開記号・階層・root区切りを維持し、ファイル行と区別した。
- 保存方式メニューは現在の状態を `AutoSave ON` / `AutoSave OFF` と明記。
- 短縮したWiki表示と元ソースの位置を混同していた編集行の着色を修正。元パス全体を着色し、明示別名と後続の装飾・通常Markdownの位置を維持した。
- Cloneの内容表示付きフォルダ選択で実フォルダ名を表示。空でない・確認不能な保存先を確定時に拒否し、選択画面内で説明する。Workspaceへの通常追加は非空フォルダを許可。
- 全体テスト1,012成功・0失敗・17除外、追加リンク操作テスト2成功。Unicode・別名・編集行の着色範囲、確定前のフォルダ内容変化・サブフォルダ・空ファイルも検証。
- releaseビルド成功。実画面でExplorerとWorkspaceのフォルダアイコン、AutoSave ON/OFFの表示と再起動後の維持、編集中Wikiパス全体の着色を確認。Cloneの非空フォルダは選択画面が閉じず、空フォルダは選択でき、実フォルダ名が表示されることを確認した。
- Workspace右クリックからClone画面を開く際のAccessKit再入による停止を修正。最終releaseで同じ操作から画面の表示・取消・続くExplorer操作まで確認した。
- 「リンク更新がうまくいかない」という追加報告は詳細操作を確認中。既存改名・移動テストは成功しているが、この報告を解決済みとはしない。
- 保存設定は既にフォルダ共通、親子は最深の登録を全Workspaceで適用。索引キャッシュのWorkspace間共有・親子root再利用は未実装の提案として分離。

## 2026-09-19 追加レビュー：リンク表示・移動時更新・Explorer

- RFN01-23を同じ作業単位に追加。Explorer上部のフォルダ名から既存Open Folderを開き、重複するアイコンを撤去。
- CloneのURL欄からフォーカスが離れた時点で非同期Git確認。入力変更・取消後の古い結果を棄却。保存先の参照・手入力確定時に空フォルダを検証し、各欄の下へ赤字で表示。参照ボタンは保存先欄の右側へ配置。
- 暗黙のWiki表示は拡張子なしのファイル名と見出し部分。明示したWiki別名とMarkdown表示名、ソースのリンク先は維持。省略したパスを含めた文字位置対応も検証。
- Editor内の改名・移動時に、現在のWorkspace（未使用時はExplorer）内の参照元Auto Save設定で更新を判定。OFF、閲覧専用、外部変更・競合は変更しない。開いた未保存本文は保持して通常の自動保存へ接続し、閉じた文書は再確認後に元の文字コード・改行で保存。
- 走査・計画は背景処理、書込みはUI側で分割。隠しファイル・添付ファイルも解決対象に含め、同名で曖昧なWikiは更新しない。不完全な走査・上限超過時は書込み前に中止。処理中の次の移動と終了を待機させ、途中破棄を防ぐ。外部アプリで行った移動の追跡は対象外。
- 全体テスト1,010成功・0失敗・17除外、追加リンクテスト2成功。ON/OFF・未保存本文・閲覧専用・外部変更・改行維持・フォルダ移動・大小文字だけの改名・連続移動を含む。
- releaseビルド成功。隔離プロファイルの実画面で、Wikiのファイル名表示・明示別名・Markdown表示名の維持、Explorer名からのフォルダ選択、重複アイコン撤去、URL欄のフォーカス離脱時と保存先選択直後の赤字警告、参照ボタンの横並び、Clone取消を確認。
- 実GUIのファイル改名により、開いたAuto Save文書のリンク表示・リンク先が更新され、通常の自動保存でディスクへ反映されることを確認。Auto SaveがOFFの別文書はディスク上も変更なし。キャレットも元の本文位置を維持した。ネットワーク認証付きClone、全IME組合せ、大規模走査時間は未確認。
- gpt-ossの追加ケース提案から連続移動と大小文字だけの改名を採用。Windowsで無効な制御文字を含む名前の提案は不採用。実行結果はCodexが確認（LocalLLM runs/20260919T031931Z-0bf1c10f.review.md）。
- Windowサイズ・フォルダラベルはユーザー確認済み。新たな修正は別途Acceptで確認する。RFN01-22（縦横LivePreview見出し編集中の位置ずれ）は未対応の別Issue。

## 2026-09-19 追加レビュー：ラベル・Cloneフォーム・候補位置

- Workspace開閉時にWindowの最大高さが再計算されて縮む問題を修正。
- 区切りにフォルダ名を表示し、フルパスのTooltipを維持。現在の編集ペインの文書に対応するラベルを強調する。ツリー選択とは独立し、タブ切替・保存先変更でも更新する。
- CloneのURLと保存先を同じフォームに配置。入力不備・Git確認失敗はフォーム内に表示し、入力を保持。Gitによるリポジトリ確認後に取得する。
- 内容を確認できるフォルダ選択で、確定ボタンを一つに統一。表示中のフォルダを返し、ファイルや仮の入力名を登録しない。
- リンク候補を各ペインのキャレット付近へ配置。縦横、スクロール、表示領域の端に対応する。
- 全体テスト995成功・0失敗・17除外、追加リンクテスト2成功。編集文書と右クリック対象の分離、同名ファイル間のタブ切替、Clone入力保持、Git実プロセスの成功・不正リポジトリ拒否を含む。
- releaseビルド成功。隔離プロファイルの実画面でWorkspace開閉前後とも1182×792を維持すること、Cloneの単一フォーム・入力保持・フォーム内警告・参照結果の反映・取消、空フォルダの単一ボタン確定を確認。選択画面にファイルが表示され、空フォルダに仮のファイルが作られていないことも確認した。
- 実画面でラベル名・フルパスTooltip、別フォルダの文書と上下分割ペインのフォーカス切替による強調を確認。リンク候補は横書きで入力行の下、縦書きで入力列の横、上下分割の下側ペインで表示領域内に配置され、横書きの下端では上へ回避し、スクロール後は入力位置へ追従した。
- ネットワーク認証を伴うClone、IME全組合せ、大量ファイルの所要時間は未確認。横書きLivePreviewの見出し編集位置ずれは別の未対応不具合として維持。ユーザーAccept前でありPRはマージしない。

## 2026-09-19 追加レビュー対応：Workspace内の直接操作

- 初期画面はWorkspace一覧・新規・Cloneとし、選択したら直接開く。利用中は名前による切替、右側同じ行の追加・Clone・フィルター、下部の閉じるを配置した。
- 登録フォルダの中身を最上位へ並べ、境界を区切り線に変更。区切り線にパスのツールチップと操作対象を保持し、空フォルダも管理できるようにした。
- コンテキストから所属する登録フォルダ全体のAuto Save、取り外し、順序変更、表示・索引の初期化へ接続。取り外し時も全TABの保存・破棄・取消を経由する。
- Clone開始時のWorkspaceを保持。初期画面では新規Workspaceを作り、利用中では開始時のWorkspaceへ追加する。登録処理と取得処理を分離して検証した。
- 表示済みファイル名のフィルターはExplorerと独立。Workspace切替・終了時には解除し、前の絞り込みを持ち込まない。
- 旧管理画面の撤去で失われた場所変更と未使用設定整理の入口を補修。初期一覧の右クリックと、未使用設定がある場合だけ表示する折りたたみ欄へ移した。
- 最終全体テスト995成功・0失敗・17除外、追加リンクテスト2成功。直接表示の深さ・所属判定、保存確認の取消、Cloneの二つの登録先、表示フィルターを含む。整形チェック成功。
- 最終releaseビルド成功。実画面で初期一覧、登録フォルダの中身と境界線、同じ行の操作アイコン、名前からの切替、表示フィルターと切替時解除、保存して全TAB切替、取消時保持、下部の閉じるを確認した。
- 実画面で余白の右クリック対象が短い問題を発見し、表示領域の残りを覆う高さへ修正。最終版で余白の追加・Clone・初期化メニューが開き、個別フォルダの保存方式・取り外しを誤操作できないことを確認。場所変更のメニューと未使用設定の展開・解除ボタンも確認した。実GUIでの設定削除自体は行わず、既存自動テストで検証した。
- gpt-ossのテスト観点提案は、既存検証の再提案と保存仕様に反する期待値があったため不採用。実行記録とコードでCodexが成否を確認した。評価記録はLocalLLM runs/20260919T013707Z-2b6fd6a6.review.md。
- 横書きLivePreviewで見出し編集中に記号と本文がずれる報告は、今回のWorkspace変更とは別の未対応不具合として保持する。

## 2026-09-19 前回レビュー対応（画面構成は上記で更新）

- Workspace専用アイコンとメインメニューの入口、左Pane内の管理・ファイル表示を追加。Explorerの位置・展開・選択を独立させた。
- Workspaceの開閉・切替を、未保存確認後の全TAB切替へ変更。取消・保存失敗・破棄失敗では本文とTABを保持する。復元した無題文書も一括保存の対象に含めた。
- 登録フォルダ内へのオープン・保存先制限、領域外起動・復元時の保護、全ペイン・履歴の切替を追加。
- ファイルを表示するフォルダ選択、明示したフォルダ順変更、共通保存方式の説明を追加。
- Git取得を独立した背景処理として追加。空の保存先・URLを検証し、取得成功後だけWorkspace登録する。
- 全体テスト：993成功・0失敗・17除外。切替・復元・境界・保存失敗・Git取得の回帰を含む。既定で除外されるリンク移動を含む追加確認：2成功。
- 実画面：左Pane内の一覧・構成、フォルダ選択でファイルを表示して登録、保存済みTABを閉じてWorkspace切替、未保存の切替取消後の本文・TAB・Workspace保持を確認した。
- 実画面で見出し領域が伸びて操作を押し下げる問題を発見・修正。狭いPaneで操作名を省略しない配置にし、「閉じる」と「登録解除」を区別した。
- 整形チェック・最終releaseビルド成功。機能テスト後の追加変更は上記の配置・文言・パスのツールチップ。最終版で日本語の操作名が省略されないことと、メインメニュー「Workspace…」から左Paneの管理へ移動できることを再確認した。
- Git取得はローカルリポジトリを使った実プロセスの成功・失敗を検証。ネットワーク認証を伴う取得、実機IMEの全組合せ、大量ファイルの所要時間は未確認。

新規画面・外観では入口、配置、初期表示、遷移、取消と既存操作への影響を先に確認する運用を親AGENTS.mdへ記録した。今回のClaude不在によるCodex実装は明示された例外として記録した。

## 初回実装の記録（2026-09-18）

以下は初回確認時点の記録。TAB維持や旧管理画面については、上記のレビュー対応で置き換えた。

## 実装

- 複数Workspace・複数フォルダ、共有保存方式、デフォルト起動、切替・管理・リセット・登録解除・未使用設定整理。
- 複数フォルダのExplorer・検索・監視。親子・別名の重複除外、循環回避、走査取消、件数上限。
- 文書の保存先による自動保存。入力停止2秒、設定有効化前の未保存本文を即時上書きしない。別名保存後の保存先更新、閉じたTABの除外、外部変更・読み取り専用・書込失敗時の停止。
- 保護用退避を後方互換のヘッダ属性で識別。通常の退避Offでも保持・復元し、原本が見つからない場合も本文と合意時点を復元。書込失敗を再試行し、明示的な保存・破棄で整理する。
- 一つの背景スレッドによる索引・キャッシュ管理、段階更新、世代による取消、未保存見出しの優先。
- Wiki／Markdownリンク補完、ファイル・見出し・重複候補の識別、確定・取消・Undo・IME保護、既存の未保存TAB再利用。
- Windowsの通常パスと拡張パスを同じドライブとして扱い、同一ドライブでは相対リンクを生成する。

## 実行確認

- Windows `cargo test --offline --quiet`：982成功・0失敗・17除外。
- 除外を含むリンク操作テスト：2成功。確定後のUndo、Esc、古い候補の拒否、IME、未保存TAB再利用、コード内抑止。
- 管理画面のハンドラ・状態保存失敗・退避保持／復元・連続した保存方式変更の回帰テストは全体テストに含む。
- `cargo fmt --all -- --check`：成功。
- 除外を含む既存の復元・Drop・起動・一括保存テスト：15成功。
- Windows `cargo build --release --offline --quiet`：成功。

実画面で自動保存、同名ファイルと同名見出しの候補、相対リンク生成、Undo、縦書きでの見出し選択・確定、Workspace切替後のTABと保存方式の維持を確認した。管理画面の作成操作も確認済み。

画面確認で発見した、管理画面の初期フォーカスおよび閉じる際の二重フォーカス更新による停止を修正した。最終リリース版でデフォルトWorkspace起動、管理画面の表示、Escで閉じた後の入力フォーカス復帰を再確認した。最終版とユーザー確認用ビルドの対応はPRとLinearに記録する。

## 確認範囲

ユーザー確認手順は `testdata/Workspace確認.md`。実装済みとユーザー確認済みを区別し、Acceptの了承前にマージやIssueのCloseを行わない。

管理データの削除・リセットや失敗時の保護は自動テストで確認し、実画面で破壊的な操作は行っていない。実機IMEの全組合せや大量ファイル環境での所要時間測定は未実施。

部品化、起動高速化全般、文書上限引上げ、WSL起動、任意のファイルステータスは今回の対象外。
