# Terminal作業単位（RFN01-13〜17・20・27）

2026-09-19に会話で仕様・着手合意。Codexが直接実装・テスト・レビューを担当する。Claude・ローカルLLMは使わない。途中成果をAcceptへ出さず、一つのPR・確認用ビルドにまとめる。

## 合意仕様

- 下段をEditor Panel／Terminal Panelと呼ぶ。上段TABごとに複数の下段TABを保持。非表示でも文書・シェル・取り込みを維持する。
- Editor Panelは保存できる作業用下書き。Quick Draftは一時メモ。
- 出力の選択範囲、保持履歴、開始後の継続ログは別操作。継続取り込み先は新規Editor Panel TABを既定として固定し、上段TAB切替で変えない。
- 取り込み先には既存ReadOnlyを適用。閲覧・選択・コピー・保存は可。停止で開始前の状態へ戻す。ReadOnly解除は先に取り込み停止。
- 閉じる確認のキャンセルでは、文書・シェル・取り込みを保持する。
- 複数行貼り付け・生存シェル終了確認は既定ON、設定でOFF可。Editor Panelからの送信にも適用。既存の末尾改行・bracketed paste・Ctrl+C割り込みを維持。
- 検索バーはTerminal内。右クリックとCtrl+Shift+Fから開き、前／次・一致強調・大小文字区別を用意。Ctrl+Fはシェルへ渡す。
- シェル設定はTerminal設定画面の一覧と編集欄。名前・実行ファイル・引数・開始フォルダ、追加・複製・並べ替え・既定指定。Explorerと文書の親フォルダから新しいTerminalを開ける。
- 履歴上限は既定10,000行で設定可。最新へ戻る、新着表示、ページ送り、折返しを尊重したコピー、Ctrl+クリックでURL、画面消去と履歴消去を分ける。
- ログをファイル保存でき、日時による既定ファイル名は変更可。出力本文のタイムスタンプは保留。プロンプト識別を実測し、別途ユーザー確認まで付与しない。
- 外観はEditor／Terminal／Editor Panel／Terminal Panel別の既定。設定画面とTAB右クリックを入口とする。
- 背景色はTAB > Pane > 種類別既定。Pane背景変更はTerminal・Panelも含め一括適用し個別背景を解除。透過・書式は維持。Random背景はTAB作成時に決定・固定し、明示TAB／Pane背景が優先。

## 接続と変更範囲

- `src/main.rs`：既存PaneTab/TabBelow、TerminalView、settings保存読込、終了確認、メニュー・コールバックへ接続。
- `src/terminal.rs`：既存Screen/Parserを使った保持履歴・検索・コピー・ログ採取。制御列を本文へ混入させず、折返しと実改行を区別。
- `src/terminal_session.rs` / `src/pty.rs`：非表示セッションも出力回収、開始フォルダ、ログ採取。シェル送信規則は維持。
- `ui/editor-pane.slint` / `ui/app-window.slint`：Panel TAB列、取り込み状態と操作、検索バー、設定画面、右クリック。
- `src/directwrite_render.rs` のcellsモジュール と透過経路：外観の適用範囲を整合。文字やUIまで透過させない。
- 文書の保存・ReadOnlyは既存のOpenDocument系を再利用する。無関係のMarkdown・Workspace処理は変更しない。

## 完了条件と検証

1. 上段切替・非表示・別Paneへの移動でPanelと取り込み先が混ざらない。複数TABに独立した内容・保存先・ReadOnly状態を保持。
2. 貼付キャンセルは0バイト送信。終了キャンセルでは停止しない。送信の末尾改行、Ctrl+C、UTF-8の分割受信を検証。
3. 保持履歴の検索・コピー・URL操作、履歴上限での表示位置維持、改行と折返し、クリアの区別を検証。
4. シェル設定は再起動後も復元。空白・日本語パス、WSLの開始フォルダ、起動失敗を検証。
5. ログの追記・停止・保存、ReadOnly復元、ファイル書込失敗を検証。機密の入力内容を診断ログへ記録しない。
6. 外観4種とTAB／Paneの優先順位、Random固定、透過と非透過文字の分離を検証。
7. 必要な単体・統合テスト→全体テスト→release→実画面確認→PR→Accept。Terminalへのコマンド実行はCLI/PTYテストで行い、Computer-useでは実行しない。

## 進捗

- 着手前：main `52eb1a8`、追跡ファイル変更なし。ユーザーの未追跡資料は維持。
- 作業ブランチ：`feature/terminal-workflow`。
- 既存下段の上段TABへの所属・非表示での保持を確認。複数下段TABと保存できる文書への拡張から開始。

- 実装：Panel TAB・保存・固定ログ取り込み、貼付／終了確認、履歴検索、シェルプロファイル、出力保存、独立外観を接続。
- 検証：全体テスト1,029件成功。実シェルを使う追加6件は直列実行で成功。詳細は `Terminal確認手順.md`。
- 追加修正：閉じる直前のログ末尾保存、下段検索バー、履歴上限の即時適用、メニューの呼び出し順序。実画面確認後にAcceptへ渡す。
