# Terminal作業単位の確認手順

対象：RFN01-13〜17・20・27。実装検証後のユーザーAccept用。PRのマージはこの確認の了承後。

1. **貼り付け・終了（13）**：Terminalで改行を含む内容を貼り付けると内容と行数の確認が出る。キャンセルで送られない。実行中のTerminalを閉じる確認でもキャンセルでシェルと取り込みが継続する。
2. **検索（14）**：Terminal本文を選びCtrl+Shift+Fで検索バー。検索語を入れEnter／矢印で履歴へ移動し一致部分が強調される。大小文字を切替できる。Terminal Panelでは下段にバーが出る。
3. **シェル設定（15）**：設定 → Terminalでプロファイルの追加・複製・並べ替え・編集後の適用・既定変更。日本語／空白を含む開始フォルダから新規Terminalを開く。既存Terminalの場所は変わらない。再起動後も設定が残る。
4. **履歴操作（16）**：履歴を上へスクロールしてから出力が増えると新着表示。「最新」で末尾へ戻る。Ctrl+クリックでHTTP(S)リンク。右クリックの「画面消去」と「履歴消去」は別動作。
5. **ログ（17）**：Terminal右クリックからファイルへのログ保存を開始。日時の既定ファイル名を変更できる。Editor Panelに取り込み状態とReadOnlyが表示され、停止後に編集可能。ファイルには制御列を除いた出力とプロンプトが残る。本文タイムスタンプは今回付けない。
6. **Panel（20）**：下段の「＋」で複数TABを作り、内容が独立していること。上段TAB切替・Panel非表示から戻して内容を維持。選択出力の挿入、履歴取り込み、継続ログ取り込みを使い分ける。継続取り込み中に上段TABを切り替えても元の取り込み先に追加される。「...」から保存できる。
7. **外観（27）**：設定の3種類の外観と、Terminal／Panel右クリックの個別外観を確認。Pane背景変更が上下へ揃って適用され、個別フォント・透過度は残る。Random背景はTAB作成時に決まり、操作のたびに変わらない。

## 検証記録

- `cargo test --offline`：1,029成功、0失敗、20 ignored。
- `cargo test --offline terminal_ -- --ignored --nocapture --test-threads=1`：追加6成功。実ConPTYでWindows/WSL開始フォルダ、シェル終了後の出力、取り込み先固定・キャンセル継続、ファイルログ末尾、書込失敗時の本文保持を確認。
- WSLテストの並列実行では一時フォルダ削除が使用中で失敗したため、実シェルテストは直列実行する。開始フォルダの内容比較は並列時も成功。
- 背景透過はピクセルテストで背景alphaと不透明文字を確認。UI操作は確認用の専用プロファイルで実施する。
- 実画面で追加メニューの呼び出し順序の問題を検出・修正し、メニュー操作の回帰テストを追加。
- 出力本文のタイムスタンプは合意どおり保留。Panel文書は明示保存する。Panelの複数TAB一式の再起動復元や異常終了復旧は今回の対応外（非表示・上段切替での保持は対応）。

最終の実行ファイルとコミットはPR／LinearのAccept記録へ記載する。
