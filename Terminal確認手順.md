# Terminal作業単位の確認手順

対象：RFN01-13〜17・20・27、PR #4。今回の修正版のAccept確認用。了承後にマージし、IssueのCloseはユーザーが行います。

## 今回の確認

1. **検索（14）**：出力のあるTerminalでCtrl+Shift+F。語を入力すると出力を残したまま一致部分が強調される。再度Ctrl+Shift+F、×、Escapeで閉じると検索状態が解除される。Terminal Panelでも同じ操作。
2. **Panel（20）**：「...」→ファイルを開く、編集→保存でTAB名が更新される。保存済みTABを長押しすると名前を編集できる。長い文書をホイールと右端のスクロールバーで上下へ移動し、選択後Escapeで解除できる。
3. **シェル（15・20）**：Terminal TAB／Terminal Panel TABを長押ししてシェルを選ぶ。同じシェルとキャンセルでは何も変わらない。別シェルは終了確認設定に従って新しいセッションとなり、TAB位置とPanel文書は残る。
4. **ログ（17・20）**：取り込み中は開始メニューが無効。ファイル保存中は保存先、Panelのみへの取り込みでは保存が必要な旨が表示される。末尾表示中は新しい出力に追従し、過去へスクロールすると追従が止まる。停止後は編集可能。
5. **設定（27）**：General／Editor／Terminal／Keysの4分類。TerminalにはEditor Panel、EditorにはTerminal Panelの既定外観がある。背景・透過率、字体・文字色・サイズ・装飾をその場で変更できる。透過率は数値とスライダーで変更。Random設定後に新規Terminal TAB／Terminal Panelを作ると背景が変わり、操作中は同じ色を維持する。Pane背景の変更は上下へ適用される。
6. **操作（13・20）**：改行を含む送信の確認をEnterで決定できる。ログを取っていないTABを閉じるとき、他のTABのログが終了するような文言は出ない。Editorへ戻った後もCtrl+Vで貼り付けられる。

上段TABの切り替え後にPanel内容が残ることはユーザー確認済み。非表示中の出力追記はCodexの自動テストで確認するため、手動で出力のタイミングを合わせる必要はありません。

## 検証記録

- 全体テスト1,034件成功。実ConPTYの追加8件成功。コミットと実行ファイルはLinearのAccept記録に記載します。
- 実画面確認：4分類の設定、横並びの外観編集、Random・透過率数値の保存、検索中の出力保持とCtrl+Shift+Fでの解除、Panelファイルを開く、150行のスクロール、Escapeでの選択解除、Panelから上段EditorへのCtrl+Vと保存。
- TAB長押しの実ジェスチャー、ログの長時間追従はユーザー確認待ちです。シェル選択後の同一・キャンセル・変更は実ConPTYテストで確認しています。
- 追加検証：同じシェルの無操作、別シェルへの変更とキャンセル、取り込み先の固定、非表示中の追記、検索前後の出力保持、PanelファイルのOpen／Rename、保存後のTAB名、未変更のUIモデル保持、RandomとPane背景の優先関係。
- 既存Panicの回帰テスト：Panel保存・再編集の通知と、3,000行の描画・クリック・Ctrl+End・選択。Slint software rendererの座標変換修正は維持しています。
- 本文タイムスタンプは保留。Panel一式の再起動復元・異常終了復旧は今回の範囲外です。保持したいPanel文書は明示保存してください。
- 外部ブラウザへのURL実遷移と長時間の大容量ログ取り込みは未実施です。
