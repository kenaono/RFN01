# 単語チェックモードの要件

**書き手が自分で作る「言語モード」。**語の一覧を作っておくと、本文の中のその語が指定した色で
出る。要件定義 7.9（軽い校正）から切り出した、この機能ひとつの要件である。2026-09-08。

## 1. この文書の位置づけ

**要件定義.mdの1章では収まらなくなったので切り出した。**この機能は色を塗るだけのものでは
なく、**辞書を持ち、それを保ち、出し入れし、文書ごとに切り替える**——その全部が噛み合って
いる。

- **ここが正**：単語チェックモードに関する決定はこの文書にある。要件定義 7.9 は校正の章として
  残り、この文書を指すだけにする。
- **要件定義 3（思想）と 4.2（IDE機能を持たない）は動かない。**この文書はその中で書かれている。
- **記法の色（Markdownの装飾文字を薄くする話）は隣の機能だが、同じ色の層に乗る**ので
  8.5 に置く。層の順位を2か所に書くと、片方だけ直るからである。
- 引用する形は「単語チェックモード要件 4.2」のように節番号で。

## 2. 機能の姿

### 2.1 何をするか

書き手が語の一覧を持ち、本文のその語に色が付く。**それだけである。**

**これは検索の言い換えである。**校正とは同じ語を何度も探すことで（要件 7.7）、
**単語帳は「毎回打ち込んでいた語を、置いておく場所」**にあたる。新しい概念ではなく、
既にやっていることの手数を減らす。

### 2.2 構造は、ソースの予約語の色分けと同じ

**書き手の指摘（2026-09-08）**：「考え方はソースの編集における予約語の色分けと同じ。
C言語モード、C#モードを作るつもりで、構造的に自分の単語チェックモードがあればいい」。

```
モード（小説A）
 ├ 語群（人物）   色：青   ├ 田中 / 佐藤 / 鈴木 …
 ├ 語群（敵役）   色：赤   ├ 田中 / 黒木 …
 └ 語群（言い回し）色：緑  └ そして / しかし …
```

C言語モードが予約語・型・前処理をそれぞれの色で持つように、**モードが語群を持ち、
語群が色を持つ**。

**これは3度目の形である。**最初は「単語帳ファイル1つ」、次が「語群を一冊ずつOn/Offする」で、
どちらも直感的でなかった——後者は**C言語モードを使うのに予約語グループを個別に有効化する**
ことに当たる。

### 2.3 しないこと

- **編集器はどれが正しいかを判定しない。**表記ゆれの自動検出は持たない——判定を始めた瞬間に、
  **当たらない判定を消す作業**が書き手に増える。
- 語に「正しい／誤り」の区別を持たせない。色が違うだけである。
- 語ごとの色は持たない（7.5）。

### 2.4 使い道は書き手が決める

固有名詞の表記ゆれ（「達也」と「たつや」を別の色に）、癖で多用してしまう語、伏線の語、
人物ごとの色。**開発向けの編集器として使いたければ、モードを1つ増やせばよい**——
それがこの構造の値打ちである。

## 3. データの構造

### 3.1 3つの層

| | 持つもの | 上限 |
|---|---|---|
| **モード** | 番号・名前・語群 | 16 |
| **語群** | 番号・名前・色・語 | 1モードに8 |
| **語** | 文字列（1行に収まるもの） | 1語群に10,000 |

**語は1行に収まるものだけ。**改行をまたぐ選択を足せてしまうと、その語は本文のどこにも
当たらないまま一覧を汚す。

### 3.2 番号は名前より重い

- **文書が指すのはモードの番号であって名前ではない。**名前は書き手が変えるもので、
  **変えた瞬間に文書のモードが切れる**のでは名前の仕事として重すぎる。
- **消した番号は二度と配らない。**表が「次に配る番号」を覚えている（`next_id`）。
  使い回すと、古いセッションが指していた番号が別のモードを指す——**「切れている」より悪い。**
- 語群も番号を持つ。設定画面が並び順で指しているうちは要らないが、**モードの複製（7.5）と
  モードごとの書き出し（5.3）が入ると要る。**

### 3.3 語は由来を持つ（これから）

**手で足したのか、取り込んだのか。**理由は2つ：

- **取り込み直しで、手で足した語を消さないため。**他の道具で作った一覧を更新して入れ直す
  とき、書きながら足した語まで消えるのは、書き手の仕事を捨てることである。
- **「なぜこの語があるのか」に答えられるため。**

### 3.4 上限は実測してから

いまはモード16・語群8・1語群1万語・1ブロック256印。**どれも「足りないと言われてから
増やす」側の数である。**語群の数は色を用意するぶんの上限、語数は木の大きさの上限、
印の数は「そのページが読めるか」の歯止めでもある。

## 4. データの保持

**書き手の問い（2026-09-08）**：「ファイルで維持すると壊れやすい印象ですがどう思いますか」。
挙げられた3点——**安定したID・遅延書き込み・壊れたときの強さ**——がこの章である。

### 4.1 置き場所

`%LOCALAPPDATA%\RFN Edit\words.rfnwords`。

**作業フォルダには置かない**（要件 5.1：作業フォルダ内へ独自の管理ファイルを作らない）。
単語帳は作品に属するのではなく**書き手に属する**——別の作品を開いても同じ辞書が要る。

### 4.2 形式

```
RFN-EDIT-WORDS 2
next: 12
mode: 1 | 小説A
group: 2 | 人物 | #3b6ea5
word: 田中
word: 佐藤
group: 3 | 敵役 | #a53b3b
word: 黒木
mode: 4 | 開発
...
```

- **1行に1つ。**壊れ方が行単位に閉じる——**1行読めなくても、その1語を失うだけである。**
  JSONやTOMLでは括弧が1つ落ちた瞬間に全部が読めなくなる。
- **先頭の行が版と身元。**合わなければ「この編集器の表ではない」と判じる。
- **色は`#rrggbb`の文字列のまま持つ。**この層は色を混ぜない。
- **キーの前の空白は捨てる**（`trim_start`）。整形の道具が字下げを足しても読める。

### 4.3 書き込みは作業コピーと同じスレッドへ

**1語足すたびに全部を書き直して`sync_all`まで待つのは、書く手を止めることである**
（要件 2）。書き込みは`writer`スレッドの列へ入れる。同じ列なので：

- **同じ道へ何度も積まれたら、最後の1つに畳まれる**（連続して語を足しても書き込みは1回）。
- **終了時に待つ仕組み（要件 8.1）がそのまま効く**——`finish()`が列を空にしてから終わる。

### 4.4 読めない表は上書きしない

**辞書は書き手が積み上げたもので、読めないからといって捨ててよいものではない。**
先頭の行が合わなければ、その実行は**色分けの無いまま進む**——そして書き込まない。

**読めなかった行は数えて言う。**黙って半分になった辞書は、いちばん気づきにくい失い方である。

### 4.5 起動のときに1世代だけ控える

`words.rfnwords.prev`。守りたいのは**「この実行が辞書を壊した」**で、そのとき直前の姿が要る
（`perf_log.prev.txt`が同じ理由で同じことをしている）。**実行中の変更ごとに控えても、
守れるものは増えない。**

### 4.6 ファイルで持つことは弱くない（求められて答えたもの）

書き手の「ファイルだと壊れやすい印象」への答え。**壊れやすさはファイルかどうかではなく、
書き方で決まる。**

- **書き込みは原子的に**（一時ファイルへ書いて置き換える、`file_io::write_atomically`）。
  途中で電源が落ちても、**古い版か新しい版のどちらかが在る**——半分書けた版は残らない。
- **形式が行単位**なので、それでも壊れたときに全部を失わない（4.2）。
- **控えが1世代ある**（4.5）。
- **上限が決まっている**（3.4）ので、際限なく大きくならない。

データベース（SQLite等）にすれば強くなるかというと、**この大きさでは変わらない。**
1万語×8語群×16モードでも数MBで、書き込みは1秒に何度もない。**足すのは依存と、
壊れたときに人が読めない形式である。**

## 5. 取り込みと書き出し

**辞書は編集器が持つ。書き手はファイルを管理しない**（2026-09-08、書き手の指摘）。
**これが基本の形である**——語は表にあり、画面から足して、画面から消す。ファイルは
**出し入れのためにだけ**あり、**無くても使える。**

理由は2つ。ひとつは重複で、ファイルを指しっぱなしにする形では**黙って先勝ちにするしか
なく**、「なぜかこの語だけ色が違う」を書き手が画面のどこからも辿れない。もうひとつは
**書き手に管理させないこと**——どこに置き、いつ更新し、どうバックアップするかは、
書き手の仕事ではない。

### 5.1 語群単位のファイル：1行1語

```
# 主要人物
田中
佐藤

# 脇役
鈴木
```

- **1行に1語。**単語帳は大きくなる——人物名だけで何百、表記ゆれを追い始めれば何千。
  設定ファイルの1行にカンマで並べる形では**探せないし直せない**（書き手の指摘）。
- **空行と`#`で始まる行は語ではない。**何百語の一覧には見出しが要る（「主要人物」「脇役」）
  ので、書き手が自分で区切れるようにする。
- 前後の空白は捨てる。
- **独自の形式にしない。**他の道具と行き来できることが値打ちである。

### 5.2 取り込みは足す。入れ替えない

- もう入っている語は**足さずにそう言う。**黙って二度目を入れると、「二重」と言われる語が
  取り込むたびに増える。
- 取り込んだ数を言う。
- 上限（3.4）を超えた分は捨て、そう言う。

### 5.3 辞書のファイルを直接編集する

**書き手の求め（2026-09-08）**：「設定ファイルは直接編集できてもいいかもしれない。
`Edit`とすると設定ファイルが開かれるというのはいかが」。

設定の`Words`に`Edit…`を置き、`words.rfnwords`を**この編集器のタブで開く**。
**行に1つの形（4.2）は、人が読んで直せるためでもある**——数百語の並べ替えや一括の
書き換えは、設定画面のボタンより本文の編集のほうが速い。

- **横書きのソースで開く**（要件 4.2、書き手の報告 2026-09-08）。縦書きのペインから開いても
  縦書きにしない——`word: 田中`の並びを縦に組んでも読めない。
- **開く前に、いまの表をその場で書く。**書き込みはふだん別のスレッドの行列を通る（4.3）
  ので、**行列に残っている仕事より先に読むと、書き手は一つ前の姿を編集することになる。**
- **保存すると読み直して、そのまま置き換える。**取り込み（5.2の「足す」）ではない——
  書き手が直したのは表そのもので、画面の表と食い違ったまま進むと、**次に語を1つ足した
  拍子に書き手の編集が消える。**
- **読めなければ、いまの表のまま止める**（4.4）。そして**書き込みを止める**——直している
  最中のファイルを上書きしないためで、次に読めたときに戻る。
- **読み直したあとに書き戻さない。**いま読んだものがファイルの中身なので、書けば開いて
  いるタブに外部変更として立つだけである。
- **タブを開いたまま設定画面から語を足すと、そのタブは外部変更になる。**これは要件 8.2・
  8.3 がすでに扱っている形（4つの答えを訊く）で、**この機能のために別の道を作らない。**

### 5.4 モードを丸ごと出し入れする（これから）

いまは語群単位だけである。**機械を移るとき、作品を引き継ぐとき、語群を4つ手で書き出して
手で作り直すのは、5章の冒頭と矛盾する**（5.3で辞書ごと持ち出せるが、それは1つの機械の中の
全部であって、モード1つではない）。

- モード1つを、語群・色・語ごと書き出す／取り込む。
- 形式はこの編集器自身のもの（4.2の一部）でよい——**行き先はこの編集器だけである。**
- 取り込んだモードは**新しい番号を配る**（3.2）。番号ごと持ち込むと、こちらの表の番号と
  衝突する。

## 6. モードの切り替え

### 6.1 モードは文書ごと

コードエディタの言語モードと同じ粒度。作品Aの章を開けば作品Aのモード。

- **決めるのはそのタブのメニュー**（要件 6.7）。**ステータスバー（要件 10）だけに置いていた
  ときは、どのタブに効くのかが画面から読めなかった**（書き手の報告 2026-09-08）。
- **ステータスバーは「いま書いている文書のモード」を見せる場所として残し、そこからも
  変えられる。**指す先が違うだけで、することは同じである。
- **ペインごとに別のモードが効く。**2つのペインが別の作品を開いていれば、別の色分けになる。

### 6.2 新しいタブは、開いた面のモードを継ぐ

**同じ作品の次の章を開いて選び直させるのは、書く手を止めることである**（要件 3）。

### 6.3 セッションに残す

要件 8.5。タブごとに**モードの番号**を書く（`mode: <id>`、「なし」のときは書かない）。
次の起動で同じ色分けに戻る。

### 6.4 「なし」が必ず選べる

コードエディタの`Plain Text`にあたる。**色分けを止めるのにモードを消す必要は無い。**

**無い番号を指しているタブも「なし」**として扱う——モードを消したあと、間違った色で出るより
よい。

### 6.5 記法の色は、この選択では消えない

**モードを「なし」にして消えるのは単語の色だけである**（8.5）。

## 7. 単語帳の管理

### 7.1 語は書きながら足す

本文で語を選んで右ボタン、`単語帳へ追加：人物`。**これが語を足すいちばん普通の道である**
——書いていて気づいた名前を、その場で、書く手を止めずに入れる（要件 3）。一覧を開いて
打ち込むのは、その次。

**足す先はいまその文書に効いているモードの語群だけ。**別のモードの語群に足せてしまうと、
足したのに色が付かない。

### 7.2 設定画面（Words）ですること

- モードを作る・消す・開く。語群を作る・消す・色を選ぶ。
- **モードと語群の名前を変える**（長押し、7.4）。
- **語群の行に置くのは、色見本・名前・語数だけ**（書き手の報告 2026-09-08：**名前が
  「…」に潰れて読めなかった**）。行に釦を5つ並べると、380pxの幅では**いちばん大事な
  名前から潰れる**。**名前は行に、することは開いた先に。**
  - 取り込み・書き出し・**語群を消す**は、開いた語群の語の下に置く。開いている語群には
    印を付ける——下の釦がどれに効くのかは、その印だけが言う。
  - **消すのを行の`×`にしない。**語を1万まで入れられるものが、一覧の行の釦ひとつで
    消えてよいはずがない。
- **語群の語の一覧を開いて、要らない語を消す。**アプリの中で見て、アプリの中で消せることが、
  **辞書を書き手に管理させない**ということである。
- **辞書のファイルそのものを開く**（`Edit…`、5.3）。
- 語群ごとの取り込み・書き出し（5章）。

### 7.3 重複は画面から参照できる

2種類あり、**同じ意味ではないので扱いも違う。**

| 種類 | 何が起きるか | 色 |
|---|---|---|
| **衝突**：同じモードの2つ以上の語群にまたがる語 | どちらの色で出すのか編集器には決められない | **付けない** |
| **二重**：同じ語群の中で二度 | 色は1つに決まるので迷いが無い | **付く**（言うだけ） |

- **黙って片方を選ぶのは、選んだことを書き手に隠すことである。**
- **重複はモードの中でだけ起きる。**別のモードの語群と重なっても、同時に出ることが無いの
  だから衝突ではない（C言語モードの`int`とC#モードの`int`が喧嘩しないのと同じ）。
- 一覧は語群の名前で言う——書き手が見るのは名前である。
- 一覧に出すのは先頭の50件まで。

### 7.3.1 起動したときも、名前と色が食い違わない

**書き手の報告（2026-09-08）**：「起動直後に前回のモード名が出ているが、機能していない」。

**名前はタブから、色は画面の行から来ていた。**セッションが戻すのはタブの側で（6.3）、
組版が読むのは画面の行——**行へ入れ直すのを忘れていたので、名前だけが出て色が付かなかった。**

**同じことを2か所が別々に持つなら、片方を書いたところで必ずもう片方も書く。**セッションを
開いたその場で、前に出ているタブのモードを行へ入れる。

### 7.4 設定は一枚で終える

**書き手の求め（2026-09-08）**：「設定画面がわかりにくいと混乱するので、出来るだけ一枚で
設定できるようにしましょう。決定すると次の画面という形で連続すると、何をしているのか
分かりにくくなる」。

- **名前は別の問いにしない。**モードや語群を作るとき、名前は**設定画面の中の欄**で受ける。
  問いは窓の中に描くもの（要件 8.3）で、設定画面はその上に浮いている別の窓だから、
  **訊くたびに設定画面が消えて戻る**ことになる。消えて戻る画面は、書き手から見れば
  「次の画面」であり、何をしているのか分からなくなる。
- **駄目だった理由も、その場で言う。**名前が使えない・もうある・上限に達した——**言う先は
  下の帯ではなく、名前を打った欄のすぐ下**である。目はいまそこにある。
- **できたときだけ欄を畳む。**駄目だったときに畳むと、打った名前も理由も消える。
- **窓を開くのは、編集器の外に頼むときだけ**——色を選ぶ（Windowsの色の画面）、ファイルを
  取り込む・書き出す（Windowsのファイルの画面）。**編集器が自分で訊けることを、窓に
  しない。**
- **名前を変えるのも同じ欄で**（書き手の求め 2026-09-08）。モードの名前を**長押しする**と、
  その行が名前の欄になる。語群の名前も同じ。**作るときと変えるときで手つきを変えない。**
  - **長押しはこの編集器の作法である**——タブの名前も半秒の長押しで変える（追加要件
    2026-09-06）。同じ意味の操作は同じ手つきであるべきなので、間隔も揃える。
  - **名前を変えても文書のモードは切れない**（3.2）。文書が指しているのは番号である。
  - 語群の名前は釦になっている（押せば語の一覧、長押しで名前）。**押せると見えることが、
    長押しに気づく道でもある。**

### 7.4.1 設定画面は「いまどうなっているか」を出す

書き手の報告3件（2026-09-08）は、どれも**状態が画面に出ていない**という同じ形をしていた。

- **モードを開いたら中身を出す。**語群も語も重複も編集器が作って窓へ渡すものなので、
  **開く先が変わったら作り直す。**押しても何も出ないのは「効かなかった」に見える。
- **語群が1つも無いモードでは、次にすることを書く。**作った直後は必ずここに来る。
- **作ったモードは開いた状態にする。**作った直後に中身が出ていなければ、何が起きたのか
  画面に無い。
- **いまこの文書が使っているモードに印を付ける。**「開いて中を見ている」ことと「いま色が
  付いている」ことは別のことで、印が1つしか無ければ見分けられない。

### 7.5 語群はモードが所有する。共有しない

共有したくなる実例ははっきりしている——「自分の癖の言い回し」「使わないと決めた語」は
全作品で共通である。**それでも所有のままにする。**

理由は7.3との噛み合わせである。**重複はモードの中でだけ起きる**としてあるところへ共有語群を
入れると、「同じ語群が、あるモードでは衝突しているが別のモードではしていない」が起きる。
正しく動くが、**画面から辿れない。**

**代わりに置くのはモードの複製**（`小説A`から`小説B`）。足りないと言われてから参照へ進む
——順序として安全な側である。

### 7.6 語ごとの色は無い

`int`だけ色を変えられるC言語モードは無い。**語に個別の色を許すと、語群という単位が意味を
失う**——語群とは「同じ色で見たい語の集まり」の別名である。

### 7.7 探し方の規則は増やさない

**ASCIIの大小だけを畳む**（`find.rs`と同じ探し方）。**Ctrl+Fで見つかるものが色で出る**、が
守るべきことである。

「ヶ所／箇所」を編集器が同じ語だと決め始めると、**2.3（正しさを判定しない）が崩れる**。
両方を登録すればよく、そのために語群がある。

### 7.8 語のメモは後回し

「3章で改名」のような覚え書きは欲しくなるが、無くても困らない。**軽さを先に取る。**
3.3で語が構造を持つので、後から足せる。

## 8. 色の当て方

### 8.1 どこに当てるか

**原文の面にも整形表示にも出す。**校正は原文でもするためで、これは要件 7.3.1（原文には飾りを
付けない）の例外になる——**色は記法の代わりに立つ飾りではなく、書き手が自分で置いた
しるし**だからである。

**長い語が勝つ。**`田中さん`が入っていれば、その4文字がひとつながりで色づき、中の`田中`は
別に色づかない。**文字の途中からは始まらない。**

### 8.2 当てない場所

- **コードの中**（`Marks::code`）：中身は文章ではない。人物名がコード例に出てきても校正の
  対象ではない。
- **リンク先**：URLは読むものではない。
- **記法文字そのもの**：`**田中**`の`**`まで色が付くと、語の切れ目が読めない。

**これは記法の色（8.5）と交わる唯一の点であり、層を1本にする実利でもある**——1本なら
「単語はcodeとlinkの上には置かない」と1行で書ける。

### 8.3 他の色との順位

色源は同じ範囲を取り合う。**順位は1か所に書く。**弱いほうから：

1. **記法**（`#` `**` `>` `|`、リンクの綴り、コードの綴り）
2. **見出し・コメント**（要件 9 の6段の色、`comment_ink`）
3. **単語**（この文書）
4. **検索の当たり**（要件 7.7、まだ無い）

**単語が記法より強いのは、書き手が自分でそこへ置いたしるしだからである。**記法から出た色は
文書の形から自動で出たもので、言いたいことが弱い。**検索の当たりが最も強いのは、いま探して
いるからである**——探している最中に見つからないのは、色分けの失敗として最も重い。

**選択とキャレットはこの順位に入らない。**それらは色ではなく形で、文字の色を取り合って
いない。混ぜると「選択中は単語の色が消える」のような、誰も要求していない振る舞いを作り込む
ことになる。

### 8.4 色は幾何を1画素も動かさない

**これは技術検証 9.3.1 の再掲であり、破ると静かに遅くなる。**単語の一覧は`Typography`の
**外**に置く。`TextEngine::update`は`Typography`が違えば`measures`／`layouts`／`wraps`を
捨てるので、そこへ入れると**色を1つ変えるたびに文書全体を測り直す**。一度そうしてしまい、
ON/OFFの反映に数秒かかったのがこの失敗である。

色が変わったときに古くなるのは**タイルだけ**（`tile_signature`が一覧の指紋を含む）。

**置く前に剥がす。**`SetDrawingEffect`はキャッシュされた組版の上に残るので、置く前にその
組版の効果を全範囲について剥がす——単語を1つ消したのにその語だけ色が残っていたのがこれで
ある。

### 8.5 隣の機能：記法の色

**書き手の意図（2026-09-08）**：ソース面でMarkdownの装飾文字を、コードの予約語のように
色分けする。**別の機能だが、同じ層（8.3の1番）に乗るのでここに書く。**

- **IDEの3層のうち、要るのは1つだけ。**字句解析（`text_blocks::style_runs`）は既にあり、
  意味解析（補完・定義へ移動・診断）は要件 4.2 が外している。**足りないのは8.3の順位だけ**
  であって、記法の色のために新しい解析器は要らない。
- **既定は「色を足す」ではなく「濃さを引く」。**IDEでキーワードを目立たせるのはキーワードが
  意味だからで、散文では`**`は意味ではない。色を付けると**文が虫食いに見える**——とくに
  縦書きソースでは行の途中に色の点が縦に並ぶ。濃さの引き方は`comment_ink`と同じ考えで、
  **書き手のインクを、書き手の紙へ寄せる**（誰も設定できない色は、誰かの紙に合わない）。
- **色を使うのは、それ自体が読まれるものだけ**：リンク先のURL、インラインコード、コードの
  中のコメント。**増える色は3つ。**
- **記法の判定は1か所から出す。**いま判定しているのは`document::push_visible_line`で、
  プレビューを組み立てながら決めている。ソース面（`PaneText::Source`）は`marks()`が空を
  返すので、記法の色は**新しい仕事**である。別の場所で判定すると「プレビューでは太字なのに
  ソースでは色が付かない」が起きる——**同じ判定から、プレビューには消す範囲を、ソースには
  薄くする範囲を返す。**
- **これは組み込みモードとして見せられる**（Markdownモード：語群の代わりに記法の種類を持ち、
  色だけが設定できる）。**ただし見せ方が同じでも、選び方は別**である——文書のモードを
  「なし」にして記法の色まで消えるのは間違いで、組み込みモードは**ファイルの種類と
  ソース／プレビューから決まる**（6.5）。
- **コードの中は言語別に色分けしない**（要件 4.2）。言うのはコメントかどうかだけ。

## 9. 性能

- **語を木（トライ）に積んで、本文を一度だけ歩く。**語ごとに本文を一周する形では
  語の数×本文の長さになり、数千語では1打鍵の費用に対して桁が合わない。
- 木は**モードごとに1本**、変わったときにだけ建て直す。
- 一覧には**指紋**を持たせ、タイルの署名に混ぜる（8.4）。
- **1ブロックに置く印は256まで**（3.4）。
- 探し方は`find.rs`と同じ畳み方（7.7）。

## 10. まだ決めていないこと

- 上限（3.4）——実測してから。
- **モード丸ごとの形式**（5.4）が、`words.rfnwords`そのものと同じでよいか。
- **語の由来（3.3）をどう見せるか。**持つことは決めたが、画面に出すかは決めていない。
- 記法の色で、薄くする度合いと、リンク先・コードに使う色（8.5）。**組んで、縦書きで目で見て
  から決める。**
- **検索の当たり（8.3の4番）を色にするか形にするか。**要件 7.7 を作るときに決める。

## 11. 実装の順番

済んでいるもの：2章の構造、3.1〜3.2、4.1〜4.5、5.1〜5.3、6.1〜6.4、7.1〜7.4.1、7.6〜7.7、
8.1・8.4、9章。

残り：

1. **8.3の順位を1か所に出す**（いまは`draw_tile`の中に手書きで埋まっている）
   ——これが無いと2も3も同じ場所を汚す
2. **8.2（当てない場所）**——1の直後に、1行で書ける
3. **記法の色**（8.5）
4. **3.3（語の由来）・5.4（モードごと出し入れ）・7.5（モードの複製）**
