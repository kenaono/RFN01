# 入れ子フォルダの中のファイル

左ペインのファイルツリーで、フォルダを開いたときだけ読むことを確かめます。
開いていないフォルダは読みません。

## 全文検索

このファイルは`testdata`の直下ではなく、その下のフォルダにあります。
フォルダ全文検索は、開いていないフォルダも歩きます。

- 検索の結果をクリックすると、開いているテキストの中で一致を探し直します
- 別のタブで編集していても、正しい位置へ行くはずです

## コードブロック

```python
def hello():
    print("**これは太字になりません**")
```
