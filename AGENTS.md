# RFN Editの作業ルール

このフォルダはWindows向けテキストエディタRFN Editのソースを扱う。親フォルダの `../AGENTS.md` にある会話・AI分担・検証のルールも適用する。

## 対応する管理先

- ローカルの作業フォルダ：`10_Editor`
- Linearチーム：`RFN Edit`（Issue識別子は `RFN01-…`）
- GitHubリポジトリ：https://github.com/kenaono/RFN01

LinearのRFN Editチームは、このフォルダのソースに関する議論・作業を扱う。GitHub MCPとLinear MCPを利用し、Issue・GitHubの差分やPR・ローカルのソースを照らし合わせる。接続状態は実際の読み取りで確認し、登録済みであることだけで接続成功と判断しない。

## 議論と記録

通常の相談はCodexデスクトップの会話で行う。ユーザーがLinearに記載したアイデアや課題を、必要に応じて取り上げる。Issueの登録だけで実装を開始しない。

- Issue本文には現在の目的、合意した仕様、完了条件を整理する。既存の意図や未解決点を消さない。
- コメントには議論の要点、決定理由、未解決点、実装・検証結果を記録する。会話全文を転記する必要はない。
- 継続して適用する仕様は、関連する `要件定義.md`、`追加要件.md`、`README.md` などにも必要に応じて反映する。Linear上の案と実装済みの動作を混同しない。
- ステータスは合意と作業の実態に合わせる。名称だけを根拠に着手承認や完了承認を推測せず、未合意のステータス運用を自動適用しない。

議論して着手に合意したIssueから実装する。範囲を広げる必要が出た場合は、その理由と必要な変更をユーザーに相談する。運用自体も実際に使いながら調整する。

## 実装と検証

作業前に関連仕様とGitの状態を確認し、既存の未コミット変更を保持する。ローカルの作業内容とGitHubにPush済みの内容を区別する。

Codexがユーザーと要件を確認し、実装方針・影響範囲・実装計画・完了条件を整理して、Claude Code（Sonnet）に実装を依頼する。実装・修正・テストコードの追加はSonnetが担当し、Codexは実装しない。具体的な委任方法と言語は親の `AGENTS.md` に従う。

Sonnetへ渡す実装計画・変更範囲・完了条件を対象Issueにひとまとまりで残し、Codexのレビューも同じ条件で照合する。計画変更時は現行の計画と変更理由を記録する。

gpt-ossにはログ解析やテストの網羅性確認などを依頼する。分担先の説明だけで完了とせず、Codexが仕様・差分・周辺への影響・実行結果を確認する。必要に応じてComputer-useで画面と実操作を確認する。修正が必要なら、Codexが修正計画と完了条件をSonnetへ戻す。
実装前はテスト観点・完了条件の抜けの確認、実装後は失敗ログなどの解析にgpt-ossを使い分ける。

報告では、実装済み・テストで確認済み・実操作で確認済み・未確認を区別する。PRを作成する場合は対応するLinear Issueを明記し、LinearにもPRへのリンクを残して経緯を追えるようにする。

## Acceptと完了確認

要件確認・実装方針の整理 → Sonnetへ実装計画と完了条件を渡す → Sonnetが実装 → Codexがレビュー・検証（必要に応じてComputer-useで確認） → Acceptでユーザー確認・了承 → CodexがPRをマージして結果を報告 → ユーザーがIssueをClose、の順に進める。

Codexのレビュー・検証で問題がなければ、LinearのIssueをAcceptにし、変更内容、検証結果、未確認事項と、「何を操作すると、どうなるはずか」という短い確認手順を記録してユーザーに引き渡す。検証したコミットと実行ファイルの場所・版を明記し、ユーザーが確認する版と揃える。確認で問題が出た場合は修正・再レビューに戻す。

Acceptでのユーザーの了承を対応PRのマージ承認として扱い、Codexがマージと結果報告を行う。同じ承認を重ねて求めない。マージ前には了承された内容とPRの差分の一致、必要なチェックの成功を確認する。了承後に実装内容が変わった場合は再レビュー・再確認に戻す。マージ失敗時は理由を報告し、完了扱いにしない。

IssueのCloseはマージ結果の報告後にユーザーが行う。Codexが独断でCloseやDoneへ進めない。PRマージでIssueが自動的にCloseされないよう、自動Close指定や連携設定を確認する。最初の1件でこの流れを試し、手間や不足をユーザーと振り返って運用を調整する。

## 接続設定

GitHub MCPとLinear MCPは、このデスクトップのCodexが利用する接続を使う。WSLのGit・GitHub CLI認証と、デスクトップのMCP接続は別に確認する。

認証トークンはAGENTS.md、ソース、Issue、PR、会話に記載しない。MCP設定を変更する必要がある場合は既存セクションを確認し、重複を避け、設定の読み込みを検証してから再起動を案内する。
