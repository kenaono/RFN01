# 編集ペイン分割の設計

要件 §6.4（編集ペインの分割）と §7.6（同一ファイルの複数表示）へ進むための設計。
**残っている項目の中で、これだけが構造の置き換えを要する。**着手前に形を決めるため
に書く。

## 1. なぜ今の構造では届かないか

タブを実装したとき、「編集コードにタブを知らせない」形を採った。ペインとキャッシュ
は常に**1つの生きた文書**を相手にし、タブ切り替えはその1つを入れ替える操作にした。
約30個のコールバックと全ての編集関数に手を入れずに済み、実際に安く済んだ。

**§6.4 はその前提を壊す。**「各編集ペインは独立したタブ列を持つ」ので、ペインが2つ
あれば、同時に生きた文書が2つある。1つを入れ替える形では表せない。

いま2つあるペイン（横書き・縦書き）は§6.4のペインではない。あれは**同じ文書の2つの
書字方向**で、要件でいえば §7.6 に近い。§6.4 のペインは**別のファイルを表示できる**。

## 2. 3つの層に分ける

現在 `RenderCache` に同居しているものを、属する層で分ける。**この分類が設計の全部**で、
残りはその帰結でしかない。

| 層 | 持つもの | いまどこにあるか |
| --- | --- | --- |
| **文書**（開いているファイルごとに1つ） | `DocumentFile`、本文、Undo履歴、`CountsSlot` | `OpenDocument`（`file` / `text` / `history` / `counts`）。**この層は揃った** |
| **ペイン表示**（ペインごとに1つ） | `TextEngine`、タイル、`PreviewSlot`、キャレット、選択、preedit、スクロール、モード、倍率 | `engine` / `horizontal` / `preview_slot` / `tiles` / `caret_utf16` / `selection_utf16` / `preedit_range` |
| **アプリ全体** | `FrameProbe`、`PerfLog`、`DiagLog`、表示設定 | `frames` / `perf_log` / `diag` |

`counts_slot`が文書側なのは重要である。統計も見出しレベルも**文書の性質**であって、
どう表示しているかに依らない。ペインごとに持つと同じ走査をN回やることになる。

`counts`は`OpenDocument`へ移した（2026-08-18）。共有の1スロットでも**正しさは保たれて
いた**——`CountsSlot::get`は本文を鍵にしていて、違う本文を渡されれば作り直すからで
ある。移した理由は費用のほうで、ペインが別々の文書を映すと、refreshが切り替わるたびに
文書全体を走査し直すことになる（0.006µs/字なので100万字で約6ms、それが打鍵ごとに乗る）。
そのために`document: &OpenDocument`を組版・ヒットテスト・状態表示の11関数へ通した。
`RenderCache::split`（ペインとカウントを同時に借りるためだけの関数）は要らなくなった。

逆に`PreviewSlot`はペイン側である。プレビューはキャレットのある行だけ原文を見せる
ので、キャレットが別ならプレビューも別になる（技術検証 3.12 で横書き用に別に持った
のと同じ理由）。

## 3. 文書はペインが共有する

要件 §7.6 は、同じファイルを複数ペインへ表示でき、**編集は全ての表示へ即時反映**し、
**Undo履歴はファイル単位で共有**すると定める。したがって文書は`Rc`で共有し、ペインは
それへの参照を持つ。

```
Documents : Vec<Document>          Document = { file, text, counts }
Pane      : { tabs: Vec<DocumentId>, active, view }
             view = { engine, tiles, preview_slot, caret, selection, scroll, mode, zoom }
Layout    : ペインの入れ子（下記）
```

キャレット・選択・スクロール・倍率は**ペイン内のタブ表示ごと**に保つ（§7.6 の末尾）
ので、`view`はタブごとに1つ、つまり`Pane`は`Vec<(DocumentId, View)>`に近い形になる。

## 3.5 書字方向はタブのもの（2026-08-18に判明）

**4つの表示モード（要件 7.2）はタブの属性である。**いま`TabView`が持っているのは
`preview`（原文か整形後か）だけで、横書きか縦書きかは**ペインの正体そのもの**に
なっている——`PaneId::Horizontal`／`Vertical`であり、`editor-mode`の0と1であり、
`RenderCache`の2つの`Pane`である。要件どおりにするなら、

```
TabView = { state, scroll, preview, vertical }   ← 4モードはこの2つのbool
Pane    = レイアウトの枠。前に出ているタブの向きで組む
```

となり、**ペインは向きを持たない**。「Splitか単独か」は枠が2つか1つかの話になり、
ツールバーの「横書き／縦書き」ボタンは消えて、タブごとの向きの切り替えになる。

**急所は`TextEngine`が1つの`WritingMode`で作られ、生涯変えないこと**
（`directwrite_render.rs`の冒頭がそう書いてある）。ペインの向きが変われば
engineを作り直す——ブロックの実測もタイルも捨てることになるので、向きの切り替えは
1回の全再組版（4万字で約200ms、6.8）を払う。**意図的な操作なので払ってよい費用**だが、
打鍵ごとに起きてはならないため、向きは「タブが前に出たとき」と「切り替えたとき」に
だけ変わる。

`PaneId`の35箇所の`is_vertical()`は、「このペインの正体」ではなく「このペインが
いま出している向き」を訊くことになる。行（`PaneScreen`）にはすでに`vertical`が
あるので、置き場所は増えない。純粋な関数（`tile`、`line_anchor`、
`reveals_while_moving`）だけが向きを引数で受け取る形になる。

**4aと4bは済んだ**（2026-08-18、200テスト通過・画面で確認済み）。

- `TabView`が`vertical`と`preview`を持ち、4モードがタブのものになった。
- `PaneId::is_vertical()`が2つに割れた——`is_right()`（**正体**。番号・ログ名・分割比）
  と`vertical(window)`（**いま出している向き**。流れ軸・スクロール軸・キャレットの形）。
  35箇所をどちらの意味かで振り分けた。`window`を持たない4つ（`tile`、`revealed_line`、
  `reveals_while_moving`、`line_anchor`）は向きを**引数で**受け取る関連関数になった。
- `RenderCache`は`panes: [Pane; 2]`。ペインは向きを名乗らない。
- 切り替えは各ペインのタブ列の右端（「横書き/縦書き」「原文/整形後」）。ツールバーの
  向きボタンは消え、「左ペイン/右ペイン」＝どのペインを画面に出すかになった。
- `focus-horizontal`は`focused-pane: int`へ。**ペインの持ち物（タブ列・その2つの
  ボタン）を触ったら、そのペインが焦点になる**——これが無いと、右ペインのタブを
  選んでから打つと左ペインに入る。

**向きを変えたときに消すもの**（実際に画面で出た不具合）：**両軸のスクロール**と
キャレットの行方向アンカー。古い流れ軸の値は、新しい向きでは**直交軸のずれ**になり、
紙が数万px横へ飛ぶ。読んでいた位置は直後のリフレッシュでキャレットが引き戻す。

次が段階4本体（`Layout`の木、利用者による分割）。

## 4. 分割の構造

§6.4 は「左右に分割した後で左側だけを上下に分割できる」と定める。したがって平坦な
リストでは足りず、**入れ子**が要る。

```
enum Layout {
    Pane(PaneId),
    Split { direction: Horizontal | Vertical, ratio: f32, first: Box<Layout>, second: Box<Layout> },
}
```

**`DocumentId`は`Rc<OpenDocument>`になった**（2026-08-18、段階3の前半）。文書は
`Rc`が持ち、タブはそれを1つ持つ。同じファイルを2つのペインで開くとは、同じ`Rc`を
2つのタブが持つことで、「同じ文書か？」は`Rc::ptr_eq`である。**表も要らず、消えた
ものを指すidも作れない**——最後のタブが手放したときに文書が消える。ペインが今どの
文書を見ているかは`PaneStates`が持ち、編集のコールバックは**自分のペインに聞いて
から**書く（`states.document(id)`）。タブの切り替えは、本文を出し入れするのを
やめて、ペインの向き先を変えるだけになった。

`ratio`が境界の位置で、これがそのままセッション情報になる（§6.4 末尾、§8.5）。
「あるペインのタブをすべて閉じるとそのペインの分割を解除する」は、木から`Pane`を
取り除き、親`Split`をもう一方の子で置き換える操作になる。

**この木は純Rustで、Windowsにも Slint にも依存しない。**分割・解除・比率変更・
ペイン入れ替えは全部この上の操作なので、テストで固められる。`text_blocks.rs`と同じ
扱いにする。

**実装済み**（2026-08-18、`src/pane_layout.rs`、テスト10件）。`Layout::divide` /
`remove` / `swap` / `move_boundary` と、木を矩形のリストへ平らにする `place`。
**Slintは深さの分からない木を描けない**（コンポーネントは自分自身を置けない）ので、
窓は矩形を絶対座標で並べる。**「ペインが画面にある」＝「面積を与えられている」**で、
木が名指ししないペインは幅0になり要素として存在しない。境界のドラッグは
「位置」ではなく「面積に対する割合の差分」で、比率が保存されるのでウィンドウを
広げても割合が保たれる（§8.5でそのまま復元できる）。

**境界のドラッグで踏んだ罠（6.18の3例目）**：`place_panes`が毎回`set_boundaries`で
モデルを差し替えていたため、repeaterがインスタンスを作り直し、**ドラッグを掴んでいる
TouchArea が消えて1pxごとに中断していた**。ログのイベント数（5回 vs 236回）で一発で
分かった。**数が変わったときだけモデルを差し替え、あとは行だけ書く。**

## 5. Slint 側

`vertical-*`と`horizontal-*`の2組のプロパティと、2つのハードコードされたペイン
ブロックがあった。**ペインのモデル（`PaneScreen`）に対する repeater 1つ**に
置き換え済みである（2026-08-18）。1行が1ペインで、行にはそのペインが画面に出して
いるものが全部入る——タイル列、選択矩形、組版済みの大きさ、キャレット、IMEの位置と
入力欄、スクロール、報告された寸法。

コールバックはすべてペイン番号を第1引数で受け取り、Rust側はその番号で行と`Pane`を
引く。**行の位置＝`PaneId::index`**なので、行に入っている`id`は自分の位置と一致して
いなければならない（テストがある）。Rust側の`PaneView`と名前が衝突するため、Slint
側の構造体は`PaneScreen`という名前にしてある。

### 5.1 成立性は確認した（Slint 1.17.1 のソースで確認）

2つ心配があり、どちらも成立する。

- **struct は配列フィールドを持てる。**`Type::Array`は`is_property_type`に含まれ、
  `type_struct_from_node`はメンバーの型を`type_from_node`で作るので`[PreviewTile]`
  を書ける。Rust側では`ModelRc<PreviewTile>`になる（`generator/rust.rs`）。
- **repeater のモデルデータへ双方向バインディングできる。**コンパイラに
  `TwoWayBinding::ModelData`がある。`viewport-y <=> pane.scroll`のような、いま
  2ペインで使っているのと同じ形が書ける。モデルは`VecModel`で持つこと。

**IMEもモデルへ入れた（当初案からの変更）。**当初は「変換は常にフォーカスのある
ペインで起きるのだから`ime-buffer`はroot直下に1組でよい」としていたが、**これは
やってはいけない**。1組を全ペインが`text <=> root.ime-buffer`で受けると、片方の
ペインへの入力でもう一方の`TextInput`の`text`も動き、そちらの`edited`が発火する。
1打鍵が2回入る。ペインごとに1つ持つのは、2ペインだったときと同じ形でもある。

### 5.2 順序を変える（当初案の誤り）

当初は「段階1（Rustの分解）→段階2（Slintのrepeater化）」としていたが、**逆である**。
`refresh_preview`と`refresh_horizontal`が別々なのは、大半が**書き込む先のSlint
プロパティが違う**ことに由来する。repeater 化してモデルの行へ書くようにすれば、
その違いは消える。先にRust側で統合しようとすると、消える予定の違いを吸収する
コードを書くことになる。

したがって段階2は次の順で行う。

- **2a：Slintをrepeater化する。**ペインのモデルを1つ置き、2つのペインブロックを
  1つの繰り返しに畳む。Rust側は`refresh_preview`が行0、`refresh_horizontal`が行1
  を書く。**画面は同一のまま**で、これが合格条件になる。
- **2b：2つの refresh をペイン番号を取る1つへ統合する。**2aのあとなら、両者は
  同じことをしているので短い作業になる。

**実際には、この順序もさらに入れ替えた。**1往復の値段が両者で違うからである。
`run_check.bat`は`build.rs`ごしにSlintも組むので、**構文と型は検査される**——
守られないのは**振る舞い**だけで、そこは画面を見るまで分からない。だから
「Slintの文面だけ変える回」と「画面ツリーを変える回」を分けると往復が減る。
そこで2aを「2つのペインブロックを`EditorPane`1つへ括り出して2回インスタンス化
する」までに留め、**Slintに触る前にRust側を縮めた**。
消える予定の違いを吸収するコードは書いていない——違いを`PaneId`の`impl`へ**集めた**
だけで、行数は増えていない。repeater化のとき、あの`impl`の中身が1行ぶんの読み書き
へ置き換わる。

順に済んでいるもの:

1. `EditorPane`の括り出し（Slint）と`lay_out_pane`の括り出し（Rust）。
2. `PaneId`に、流れがどちらの画面軸かと、どのSlintプロパティを読み書きするかを
   集約。組版・タイル・選択矩形・幾何・スクロール後の再描画が**それぞれ1本**に
   なった。`refresh_preview`/`refresh_horizontal`は引数を1つ埋めるだけの薄い包み
   で、呼び出し側がどちらのペインかを言うためにある。
3. ポインタ経路も1本にした。`pane_text`（4モードのうちどちらの本文か）、
   `hit_test_pane`（点→文書位置）、`update_pane_selection`。**6つのコールバックが
   すでにペインを引数で受け取っている**ので、repeater化のときは番号から`PaneId`を
   作って渡すだけになる。縦書きのドラッグ中の速い経路（`drag_caret_only`）だけは
   縦書きに限ったまま残した——実測に基づく最適化で、横書きへ広げるのは測ってから
   決めることであり、書き換えのついでにやることではない。
4. 編集経路も1本にした。入力（`insert_pane_text`）、削除（`delete_adjacent_grapheme`）、
   キャレット移動（`move_pane_caret`）、`Home`/`End`（`move_pane_to_line_edge`）、
   IME変換中の表示（`set_pane_preedit`）。縦書き側は`main()`の中に直書きされていた
   ので、括り出しとペイン化を同時にやっている。**14個のコールバックが`PaneId`を
   渡すだけになった**ので、Slintに残っている2ペイン分岐は表示のプロパティだけである。
   キャレット位置・表示中の行・行方向のアンカー・編集後の描画順は`PaneId`の
   `caret_byte`／`revealed_line`／`line_anchor`／`draw_edit`へ移した。移動キーが
   engineへ聞く前に組み直す部分は`lay_out_for_caret`が1つ持っている（`hit_test_pane`
   と同じ理由で組み直す）。
5. **コールバックがペイン番号を運ぶようにした。**Slintの`vertical-*`/`horizontal-*`
   26個を`pane-*` 14個へ畳み、第1引数を必ずペイン番号にした（0=横書き、1=縦書き、
   `editor-mode`と同じ番号）。Rust側は`PaneId::from_index`で受けるだけで、登録も
   28→14になった。`PaneStates`が番号から「そのペインの状態」と「もう一方」を返す。
   `refresh_preview_from_state`/`refresh_horizontal_from_state`は
   `refresh_pane_from_state`1本になり（表示中の行は`revealed_line`が答える）、
   `refresh_preview`/`refresh_horizontal`という薄い包みは消した。
   **ここまではSlintの画面ツリーを変えていない**——`build.rs`がSlintを組むので
   `cargo check`が両側を検査でき、画面でしか出ない不具合の入る余地がない。
6. **Slintのrepeater化**（2026-08-18、段階2の終わり）。`vertical-*`/`horizontal-*`
   のプロパティを`PaneScreen`1行へ移し、2つのインスタンス化を1つの繰り返しへ畳んだ。
   Rust側で変わったのは`PaneId`の`impl`だけで、読み書きの相手が名前つきプロパティ
   から行になった（`screen`／`update_screen`）。**行は必ず読んで、変えて、書き戻す**
   ——スクロール・報告された寸法・IME欄はペイン自身が同じ行へ書くので、先に読んで
   おいた行はもう画面の行ではない。予想どおり、変わったのはその`impl`と、寸法を
   読む4つの関数と、タブがスクロールを出し入れする2箇所だけだった。

   繰り返しの中身で確かめたことが3つある。**`for`の本体に`if`は書けない**
   （`parse_sub_element`は要素しか受けない）ので、1ペイン＝1つの`HorizontalLayout`
   のセルにし、その中でペインと仕切りを`if`で出す。**仕切りはセルの中に入れる**
   ほかない——repeaterのインスタンスは隣り合って並ぶので、2つの間には何も置けない。
   表示していないペインは`horizontal-stretch: 0`と`preferred-width: 0px`で幅0に
   なり、行から消える。**モデル行への双方向バインディングは行そのものへ書き戻る**
   （`link_two_way_to_model_data`が`model_set_row_data`を呼ぶ）ので、Rustが
   `row_data`で読むのはペインが最後に置いた値である。`changed`は値が実際に変わった
   ときだけ発火する（`ChangeTracker`は前回値と比較する）ので、行を書き戻しても
   同じ値のところからコールバックは出ない。

7. **次は段階3**：文書をペインが共有する形にし、ペインごとのタブ列にする。ここから
   先は画面が変わる。

技術検証 6.11・6.14 の教訓がそのまま効く：**ペインは可視になった時点で生成される**
ので、寸法報告もフォーカスも`init`と`changed`の両方で行うこと。ペインがN個になれば、
その落とし穴もN個になる。

### 5.3 方向で名前を付けたものが、全部の原因だった（2026-08-18）

4a で`PaneId`を「正体」と「いま出している向き」に割ったとき、**方向で名前が付いて
いた関数を4つ取り残した**。`horizontal_width`／`preview_height`／
`horizontal_visible_flow`／`push_source_to_horizontal`。どれも「横書きの幅」を
訊くとペイン0が答える形で、ペインが向きを持たなくなった世界では嘘になる。

画面で出た症状は全部これだった。

| 症状 | 実際に起きていたこと |
| --- | --- |
| 右ペインのキャレットがずれる | 右ペインが**左ペインの幅**で行を組んでいた |
| 下ペインの最下行に届かない | 下ペインが**上ペインの高さ**でスクロール範囲を決めていた（6.16の形） |
| 縦書き2ペインで左の編集が右に出ない | 「横書きペインへ押す」が**自分自身**を指していた |

**いまは全ペインが自分の面積だけを見る。**木がペインごとの矩形を持っているので、
報告値は自分の矩形で抑えられる（6.19の上限が幅と高さの両方に効く）。ステータス行を
書くのが右のペイン、という順序だけが「正体」で決まる。

**もうひとつ**：他のペインが持つ位置は、丸めるだけでなく**編集にあわせて運ぶ**
（`Change { at, removed, inserted }`）。丸めるだけだとバイト位置が据え置かれ、
前方を消したときに本文に対してキャレットが滑る。要件7.6の「即時反映」は本文だけの
話ではない。

## 6. 段階

| 段階 | 内容 | 画面で変わること | 状態 |
| --- | --- | --- | --- |
| 1 | `RenderCache`を§2の3層へ分ける。ペインは2つのまま、`Pane`という値にする。**§7.3のとおり engine とデータを分ける** | なし（内部のみ） | 済 |
| 2 | コールバックがペイン番号を運ぶ。Slintを repeater 化し、2ペインで通す | なし | 済 |
| 3 | 文書をペインが共有する形にし、ペインごとのタブ列にする | ペインごとにタブが出る | 済 |
| 4 | `Layout`の木、分割・解除・境界ドラッグ・入れ替え | 分割できる | 済 |
| 5 | 木と比率と各表示のセッション復元（§8.5） | 再起動で戻る | 済 |

段階1と2は**画面が何も変わらない**。それでも先にやるのは、3以降を今の形の上でやると、
毎回2ペイン分の特別扱いを書き足すことになるからである。

## 7. マルチスレッド化との関係

要件 §2 は「時間のかかる処理がUI操作を妨げないよう、マルチスレッドでの実装を基本と
する」と定める。着手時点でスレッドは1本もなかった（`Rc`が77箇所、`Arc`はゼロ）。

**最初に外したのは組版ではなくファイル書き込みである。**診断ログで、作業コピーの退避が
500〜700バイトに対し2.4〜5.9msかかっていた。ほぼ全部が`sync_all`と`rename`の固定費で、
バイト数に比例していない——つまり**文書が小さくても消えない停止**であり、しかも打鍵中に
最大5秒ごとに起きる（§8.1）。`writer.rs`がこれを別スレッドへ移す。

組版の並列化（技術検証 7.4）は別の話で、**7.3 が未解決のまま進めない**。1打鍵2.5msは
律速ではなく、効くのは長大段落の再計測のような場面に限られる。書き込みスレッドは
DirectWriteにもCOMにも触れないので、7.3 とは独立に入れられた。

### 7.1 「マルチスレッド化を先にやるべきでは」に対する答え

**2つある。性質が違う。**

- **(A) 並列組版**（7.4）。`engine.update()`の中でNブロックを同時に計測する。1回の
  関数呼び出しの中で始まって終わるスコープ付きの並列で、ブロックの本文を借用し、
  返る前に結果が揃う。**アプリ層は何も変わらない**——`Arc`も`Send`も要らない。
- **(B) バックグラウンド再レイアウト**。200msの再レイアウトを裏で走らせ、その間は
  古いタイルを見せる。所有権が変わる。**そのために要る「ペインの状態が動かせる値で
  あること」が段階1の成果物**なので、依存は「段階1 →(B)」であり逆ではない。

さらに、**Slint のUIオブジェクトは構造的に単一スレッド**である。コールバック・
プロパティ・モデル・`Rc`はUIスレッドに残る。「あとで全部`Arc`にする」手戻りは発生
しない。

### 7.2 規則（これを守れば手戻りが出ない）

> **文書・タブ・UIはUIスレッドに留まる。ワーカーは所有されたデータかスコープ付きの
> 借用だけを受け取り、所有された結果を返す。**

`writer.rs`が既にこの形である（所有された`Vec<u8>`を渡し、結果はタイマーで拾う）。

### 7.3 段階1への制約

`TextEngine`は DirectWrite / Direct2D の COM オブジェクトを持ち、**スレッド親和性が
ある**（技術検証 7.3 が未解決なのはまさにそこ）。したがって段階1では、ペインを

- **COMを持つ engine**（スレッドに貼り付く側）
- **ただのデータ**（ブロック配置、キャレット、選択、スクロール、モード、倍率）

に分けて括り出す。**スレッドを越えられるのは後者だけ**である。この分け方を知らずに
1つの構造体へ混ぜると、(B) に進むときに割り直すことになる。

## 8. 決めていなかったことと、その答え（2026-08-19）

段階1〜5がすべて済んだ時点で、この節が挙げていた3つはいずれも決着している。

- **どこまでやるか** → 段階5まで行った。要件 §6.4 と §8.5 を満たしている。
- **Undo履歴** → 予想どおり段階3で文書の層を作るときに一緒に入った。`OpenDocument.history`
  で、1エントリは`Edit { at, removed, inserted, made_at }`の差分だけ。スナップショットは
  取らない（100万字の文書で毎打鍵メガバイトになる）。キャレットは「戻した文字列の末尾」の
  1規則で決まるので別に覚えていない。**履歴が本文に合わなければ当てずに捨てる。**
- **書字方向がペインごとかタブごとか** → §3.5 のとおりタブのものになった。要件 §7.2 の4モードが
  `TabView { vertical, preview }`で、`PaneId::is_vertical()`は「正体」と「いま出している向き」の
  2つに割れた。

**残っている大きな未達は、この設計書の範囲の外にある**——組版・タイル描画・ファイル読み込みが
まだUIスレッドにいることで、§7 が書いたとおり、層を分けたのはそれを後からできるようにするため
である。分けた形はそのまま使える（`PaneGraphics`がCOMを持ち、`PaneView`はただのデータ）。

## 9. 段階6：ペインが2つでなくなった（2026-09-06）

段階1〜5で**木は最初から任意の入れ子**を扱えていたし、セッションの`layout:`行も`pane:`行の並びも
数を決めていなかった。塞いでいたのは**ペインという値のほう**である。

| 塞いでいたもの | いま |
| --- | --- |
| `enum PaneId { Vertical, Horizontal }` | `struct PaneId(u32)`。番号だけ |
| `PaneId::ALL`（`[PaneId; 2]`） | `PaneId::all(window)`——**窓のモデルの行数が唯一の勘定** |
| `PaneId::other()` | `others(window)`と`followers(window, states)`（＝同じ文書を映している全ペイン、要件 7.6） |
| `PaneId::is_right()` | 消えた。ログ名は番号、ステータス行の持ち主は**フォーカスのあるペイン** |
| `PaneStates`/`Tabs`/`RenderCache`/`EditPace`の`[_; 2]` | `Vec`。分割で1つ増え、ペインが消えると1つ減る |
| `editor-mode`／`split-view` | 消えた。**ペインの数はモデルの長さが言う**——第二の意見がまさに古びるところだった |

**名前で分けていたものが、全部の原因だった（5.3の再演）。**4aで「正体」と「向き」に割ったときに
`is_right()`が残り、それが「初期モード」「ログ名」「ステータス行の持ち主」「もう一方」の4つを
決めていた。番号にしたら4つとも別々の答えを持つべきものだと分かった。

### 決めたこと

- **ペインは、最後のタブが閉じたときに消える**（要件 6.4）。タブを抱えたまま閉じる道は無い——
  書き手の判断。`Close Other Panes`は**他のペインのタブをこのペインへ集めてから**畳むので、
  この約束を破らない。「ペインは消えない（画面外に留保する）」という旧仕様は、Nペインでは
  行き場のないタブが溜まるだけなので捨てた。
- **番号は詰める。**ペインの番号は窓のモデルの行で、モデルに穴は開けられない。`remove_pane`が
  木・状態・タブ列・レンダーキャッシュ・行・**進行中のまとめて閉じるの待ち行列**の6つを
  1箇所で詰める。`Layout::renumber_above`がその純粋な部分（テスト2件）。
- **分割は木を先に試す。**`divide`が偽を返すペイン番号に対して値だけ足すと、画面に居場所の無い
  行が残る。`MIN_PANE`を割る分割はステータス行に理由を出して断る——**何もしないメニュー行は、
  壊れているメニュー行と見分けがつかない。**
- **新しいペインへキーボードが移る。**`Split Right`をもう一度押すと3枚目が2枚目の隣に付く、
  という読み方になる。移さないと同じペインを何度も割ることになる。
- **入れ替えは方向で選ぶ**（`Swap Left/Right/Up/Down`、テスト1件）。最初は「描画順で次のペイン」に
  したが、書き手に「あまり意味がない」と言われた。**弱かったのは能力ではなく規則のほう**で、
  2ペインのときは候補が1つしかなかったから「もう一方と入れ替える」で言い切れていた。
  リストになった途端、私が置いた「次」は恣意的な規則になっていた。**隣を決める規則は
  `Ctrl+Alt+←→↑↓`のフォーカス移動が使っている`neighbour`と同じもの**で、新しい規則は増えていない。
  その向きに隣が無ければ**メニューに行を出さない**——何もしない行は、壊れている行と見分けがつかない。
  （タブ列ごと動かせるのは入れ替えだけである。要件6.5のドラッグは1枚ずつで、
  キャレットもスクロールも1枚ぶんずつしか運べない。）
- **セッションは、木とペイン一覧が食い違ったら1ペインで開く。**そのとき**全部のタブを
  そのペインへ集める**——復元できない配置でも、書き手の仕事は書き手のものである。

### 9.1 コンパイラが捕まえられなかった対（2026-09-06）

**3ペインを復元した起動が、必ず落ちた。**`index out of bounds: the len is 2 but the index is 2`。

```rust
let timers = [Rc::new(Timer::default()), Rc::new(Timer::default())];
...
let timer = &timers[id.index() as usize];   // ← ペイン2で落ちる
```

ウィンドウのリサイズ後の再レイアウトを待たせる**ペインごとのタイマー**である。`[Pane; 2]`や
`[PaneId; 2]`のように**型にペインが出てこない**ので、`PaneId`を番号に変えたときのコンパイルエラーに
1つも現れなかった。**ペインを数えているのに、ペインの名前がどこにも書かれていない対**が、
これ1つだけ残っていた。

- **書き手には「強制終了したら起動しなくなった」と見えた。**強制終了は無関係で、
  **3ペインのセッションを残したのがそれだった**というだけである。3分割した状態からの再起動は
  どれも同じところで落ちる。
- **探し方は`.index() as usize]`のgrep。**ペイン番号で添字を引いている場所はこれで全部出る
  （残りは`restore_session`の`strips`で、そちらは長さがペイン数と揃っている）。
- **ペイン番号で引く容器は、伸ばすか丸めるかのどちらかにする。**`Tabs::of`・`RenderCache::pane`・
  `PaneStates::of`は丸める（番号は打鍵と一緒に届くので、古い番号で落ちてはならない）。
  タイマーは**訊かれたときに伸ばす**。

### 9.2 番号を握ったまま消すと、自分を消す（2026-09-06）

**`Close Other Panes` で落ちた。**最初の版はこう書いていた。

```rust
while PaneId::count(window) > 1 {
    let other = PaneId::all(window).into_iter().find(|id| *id != here)?;  // ← here が古い
    ...
    remove_pane(window, live, other);
}
```

**`here` は番号である。**自分より小さい番号のペインが消えた瞬間に番号が詰まり、書き手のペインは
`here - 1` になる。ところがループは古い `here` で「自分でないペイン」を探し続けるので、
**2周目に選ばれるのは書き手自身のペイン**になる。しかもそれは、いま `Close Other Panes` の
ハンドラが走っている当のrepeater要素である——**ハンドラを走らせている要素を壊した**（6.18・6.24と同じ穴の、
これで4回目）。

**直しは「消す前に全部名前を付ける」。**`here.others(window)` を先に取り、**後ろから消す**
（残っているペインの番号が動かない）。動くのは `here` だけで、自分より下が1つ消えるたびに1つ下がる。
さらに**次のtickへ逃がす**（`Timer::single_shot`）——タブのコマンドが全部そうしているのと同じ理由で、
ペインを消すことはrepeaterのインスタンスを壊すことであり、頼んだメニュー行はその中にいる。

**規則**：*ペインの番号は、1つでもペインが消えた瞬間に別のペインを指す。番号を跨いで持ち回るな。*

### 9.3 パニックがログに出ていなかった

**落ちた原因を、ログの最後の行から推測することになった。**編集器は自前で窓を描くので既定のフックが
書き出す先のコンソールが無く、**ログはただ止まる**。止まる直前の行は「何をしていたか」しか言わない。

`DiagLog::catch_panics` を足した（テスト1件）。**この構造体を通さず、パスへ直接書く**——フックは
`Send + Sync` で全部より長生きしなければならず、ログはUIスレッドの `Rc<RefCell<…>>` にいる。
しかもパニックが**その借用の中**で起きることがあり、そこで2度目の借用をすれば2度目のパニックになる。
ファイルを開き直すのが、事態を悪くしようのない唯一の入り口である。

### 9.4 起動時に伸ばし忘れた2つが、黙って隠れていた（2026-09-06）

**書き手の報告は「`Close all Tabs` でPanicしたように見えたが再現しない」。**9.3で入れたフックが
ログに残していた。

```
+   6486.065 tab close pane=pane0 at=0
+   6488.297 layout pane gone=pane0 left=3
+   6488.353 panic at=src\main.rs:597 said=… the editing area always holds one pane (要件 6.3)
```

**引き金は`Close all Tabs`、原因は起動である。**`PaneStates`も`RenderCache`も**1ペインで作られ**、
セッションが4ペインを復元してもそのままだった。伸ばしているのは`divide_pane`だけで、復元の経路には
無かった。

**それでも動いて見えたのは、両方が「持っていない番号を、持っている番号で答える」から**である
（古い番号は打鍵と一緒に届くので、落ちてはならない）。結果として**4つのペインが1つの状態と
1つのengineを共有し**、3つのペインのキャレットはどこへも行かないまま、同じ文書を4回描いていた。
ログの`refresh.p0〜p3`が全部同じ`content=44709`だったのがその姿である。**気づけたのは、
4ペインぶんのタブを閉じて状態の一覧が0を割ったとき**だった。

- **直し**：起動時に`PaneId::count(&window)`まで両方を伸ばす。
- **同じ形を二度やらないために**、`after_layout_change`で**4つの一覧の長さが揃っているか**を見て、
  違えば`layout mismatch panes=… states=… strips=… engines=…`を出す。
  数を変える場所は3つ（起動・`divide_pane`・`remove_pane`）で、確かめる場所はここ1つ。
- **教訓**：*安全側に倒す既定値（丸める・先頭で代用する）は、構造の誤りを黙らせる。*
  丸めるのは正しいが、**丸めたことを数える場所が要る**。2026-08-22に「両ペインが同じ文書を
  表示していた」で一度通った道で、そのときも原因は`PaneStates::new`が1文書から作られることだった。

