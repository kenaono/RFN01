# Editor 実操作レビュー（2026-09-11）

## 位置づけと確認環境

追加要件の適用途中のrelease版を、Windowsの実ウィンドウで操作したレビュー。実操作の記録と提案を区別する。レビュー後に合意した保存方針は、下記「保存方針の合意事項」に記録する。それ以外の提案の採用を意味しない。

- 対象：`target/release/editor_spike.exe`（RFN Edit）
- 実行ファイル更新日時：2026-09-11 18:26:39
- SHA-256：`9A567C8EB4BCCE5BB4E74CB65CFCB29A680C2E0E95157ED0D92FA4756E2EB495`
- 照合したソース：HEAD `26b1dd8`。実行ファイルとHEADの厳密なビルド対応は未確認。
- 既存の設定を使用。本文倍率130%、Auto-saveはOff。設定値は変更していない。
- Windows操作はcomputer-useの`@oai/sky`を使用。クリック・文字入力・キー送信後の画面で結果を確認した。
- 編集・保存は `testdata/review-2026-09-11/操作確認.md` のみ。既存の原稿は編集・保存していない。
- 前回の保存関連3件は利用者が「追加要件.md」に記録済み。本書で実操作確認済みに格上げしていない。

優先度はP1＝原稿保全を優先、P2＝日常操作の支障、P3＝用途に応じた改善。本書の実操作指摘はすべてP2。原因を推定したものは明記する。

## 実操作で確認した指摘

### R1［P2］本文でCtrl+Oを押しても「開く」が出ない

**状態：実操作で2回確認、ソースも照合。**

手順：

1. 文書の本文をクリックして編集フォーカスを置く。
2. `Ctrl+O`を押す。
3. 左上メニューから`Open File…`をクリックする。

実際：キーではダイアログが出ず、メニューからは出る。メニューには`Ctrl+O`と書かれている。起動直後の文書と、テスト文書の右側横書きペインで確認した。後者では同じフォーカスで直前の`Ctrl+S`、直後の`Ctrl+H`は機能した。

期待：メニュー表示とキー操作を一致させる。`Ctrl+O`を実装済みの開く処理に接続する。

影響：キーが効かなかったのか、待ち時間なのか判断できず、結局マウスへ移る。既存候補C1の「表示と動作の一致」に該当する。

ソース：`ui/app-window.slint`のOpen File行（893行付近）はショートカット表示を持つ。一方、`ui/editor-pane.slint`の本文キー処理ではF/H/G等の分岐はあるが、Oから`open-file-requested`を呼ぶ分岐は見つからなかった。

完了確認：横書き・縦書きの本文から開くダイアログが出る。キャンセルで同じ本文へ戻る。Terminalのキーとの扱いは別に定義する。

### R2［P2］Undoで元に戻っても未保存の印が消えない

**状態：実操作で確認、ソースも照合。**

手順：

1. 未保存の印がないテスト文書を開く。
2. 「最後の行です。」の後ろに`X`を入力する。
3. `Ctrl+Z`で戻す。

実際：`X`が消え、本文カウントは103→102、ソースカウントは119→118へ戻るが、タブのドットと下端の`*`は残る。共有ペインで文字入力をUndoした場合、および保存後に全置換をUndoした場合にも印が残った。`Ctrl+S`では両ペインの印が消えた。

期待：保存時と同じ内容に戻れば未保存表示も解除する。

影響：「試して戻しただけ」の原稿と本当に保存すべき原稿を見分けられない。Auto-save Offでの不要な保存確認もコード上は懸念されるが、未保存のまま最後のタブを閉じる確認までは今回試していない。

原因の根拠：`src/main.rs:13943`付近のUndoは`document.text.borrow_mut()`を通り、`src/open_document.rs:70`付近で無条件に編集済みとなる。保存点へ戻ったかの判定がない。

完了確認：入力→Undo、保存→編集→Undo、全置換→Undoで保存済み表示に戻る。そこからRedoすれば再び未保存になる。同じ文書の全ペインで一致する。

### R3［P2］Settings上のEscが背後の検索バーを先に閉じる

**状態：実操作で確認。キー処理の優先順位はソースからも説明できる。**

手順：

1. 文書内検索を開いたままにする。今回は「春」の2件目を表示していた。
2. 左下のSettingsをクリックする。
3. `Esc`を押す。
4. もう一度`Esc`を押す。

実際：1回目はSettingsが残り、背後の検索バーが閉じて本文が移動する。2回目でSettingsが閉じる。

期待：最前面のSettingsを先に閉じ、背後の検索バーや検索状態を維持する。

影響：設定だけを見て検索へ戻りたいとき、別のUIが先に消える。閉じる対象が見た目の重なりと逆になっている。

ソース：`ui/editor-pane.slint:2211`付近は`root.dismiss-menus() || root.escaped()`の順で処理し、ウィンドウ側の設定を閉じる処理（`ui/app-window.slint:746`付近）よりペイン内UIの終了が先になる。

完了確認：検索バー＋Settings、置換バー＋Settingsで、Esc1回がSettingsだけを閉じる。本文や検索欄へフォーカスが戻ることも確認する。

### R4［P2・原因未確定］右ペインを閉じた直後、続くCtrl+Wが効かない

**状態：今回の一連の操作で発生。繰り返し再現と原因の確定は未実施。**

手順：

1. 左右に同じ文書を開く。左は縦書き、右は横書き。
2. 右本文にフォーカスを置き、保存済み状態で`Ctrl+W`を押す。
3. 右ペインが消え、左だけになったところで再び`Ctrl+W`を押す。
4. 左本文をクリックし、もう一度`Ctrl+W`を押す。

実際：最初のキーで右ペインは閉じた。次のキーでは左のテスト文書が閉じず、本文をクリックした後は閉じた。

期待：ペイン消滅後は残った編集ペインへキーボードフォーカスが移り、連続操作できる。

推定：見た目のアクティブペインと実際の入力フォーカスがずれている可能性がある。操作ツールのフォーカス復帰との関係も含め、手動で再確認したい。

完了確認：ペイン閉鎖後にクリックを挟まず、矢印移動・Ctrl+F・Ctrl+Wが残ったペインで機能する。別文書のペインでも試す。

## 画面を操作して感じた改善余地（採用前の提案）

### U1 自動退避の名称と説明位置

Settings→Generalで、`FILES / Auto-save / Off`を確認した。説明「退避しません。切り替えたときに作業コピーも消えます。」は、`MARKUP`のCount ruby、Ruby markup、Bullet marksより下にあり、どの設定の説明か読み戻す必要がある。

「自動退避」等の実態に合う名前にして、説明をその直下に置きたい。「元ファイルへの反映はCtrl+S」も短く明示する。Onに切り替える操作は今回していない。

レビュー後の合意：自動退避＋手動保存を維持する。元ファイルへの自動保存へ変更する指摘ではない。新規メモと外部変更の扱いは、次節のS1・S2を適用する。

### U2 本文の右クリックにUndo／Redoの入口

縦書き本文の右クリックメニューでCut、Copy、Paste、箇条書き、選択、分割等を確認したが、Undo／Redoがない。全置換や箇条書きをマウスで試した後、同じマウス操作で戻れる入口が欲しい。履歴がない場合は無効表示にする。

常設の大きなツールバーを増やす必要はなく、既存候補C1の一部として扱える。

### U3 右クリックメニューの情報量

今回の縦書き本文メニューは、切り貼りから箇条書き、選択、Terminal、下書き、戻る／進む、分割、倍率まで縦長に並んだ。今回の画面では収まるが、「文章を直す」操作を探すには長い。

Undo／Redoを加える際、本文編集を上段にまとめ、分割・倍率などは既存のペインメニューへ寄せる案を検討したい。操作の削除ではなく、入口の整理として利用者と相談する。

## 保存方針の合意事項（2026-09-11追記）

**状態：利用者と合意済み。実装・実操作による完了確認は未実施。**

作業内容はアプリ専用領域へ自動退避し、元ファイルへの反映は明示的な保存で行う。退避は最新の作業内容を守るためのもので、過去の版を残す履歴とは区別する。

### S1［P2］新規メモは開いているタブとして保持する

困る場面：新規メモを閉じるたびに見えない作業コピーが残ると、残した文書を把握・整理できなくなる。

合意した動作：

- 同じ文書の自動退避は同じ作業コピーを更新し、退避のたびに別のメモを作らない。
- 空の新規タブを閉じる場合は、確認なしで破棄する。
- 本文のある新規文書を明示的に閉じる場合は、「名前を付けて保存／破棄／キャンセル」を選ぶ。「作業コピーを保持して閉じる」は設けない。
- アプリ全体の終了時は、開いている新規文書を退避し、次回起動時に同じ文書として復元する（自動退避Onの場合）。
- 正式なファイルへの保存が成功したら、不要になった新規文書の退避コピーを削除する。保存失敗・キャンセルでは本文とタブを保持する。
- 本文のあるメモを、期限や件数だけで自動削除しない。書きかけを残す場合はタブとして見える状態にする。

効果：アプリ終了では作業を継続でき、文書を閉じる操作では保存か破棄かが明確になる。閉じた無題メモ専用の管理画面を必要としない。

既存要件との関係：要件定義8.4の「保存、作業コピーの保持、破棄」を上記へ変更する方針。自動退避Off時の終了確認は既存方針を維持する。同一文書を複数ペインで開いている場合は、最後の表示を閉じる際に文書を閉じる判断を行う。

完了確認：空の新規タブは再起動で復活しない。本文のある新規文書では保存・破棄・キャンセルがそれぞれ成立する。アプリ終了・再起動を繰り返しても同じメモが重複しない。保存済み・破棄済みのメモが退避コピーから復活しない。既存の閉じた無題メモがある場合も、移行時に黙って削除せず、タブとして確認・整理できる。

### S2［P1］外部変更を早期に知らせ、双方の内容を確認して解決できるようにする

困る場面：未保存のまま長く書き続ける間に外部でも原稿が変わると、保存時に上書きか再読み込みかを選ぶだけでは、双方の変更を残しにくい。

合意した動作：

- 未編集の文書は、既存要件どおり安全に再読み込みする。
- 編集中の文書で外部変更を検知したら、保存時まで待たず、邪魔にならない常設表示で知らせる。
- Editor側の本文と自動退避を保持し、執筆は続けられるようにする。外部版を無断で本文へ適用しない。
- 「外部版と比較する」入口を設け、双方の内容・差分を確認し、必要な変更を手動で取り込めるようにする。比較だけではどちらも上書きしない。
- 競合が解決するまで、元ファイルへの無確認の上書きを防ぐ。上書き・外部版の読み込み・別名保存・キャンセルの判断に、比較を利用できるようにする。

効果：保存直前に初めて競合へ気づく状態を避け、どちらかの変更を捨てる前に確認できる。

最初の範囲：双方を保持した比較と手動での取り込み。自動マージは必須にしない。編集開始時の本文・現在の本文・外部の本文を使う三者比較は将来の候補であり、今回の必須要件には含めない。

既存要件との関係：要件定義8.3の通知・競合時の選択を補強する。E7の「保存版との差分」と対象が異なるため、比較UIを共用する場合も、保存基準と現在の外部版を区別して表示する。

完了確認：編集中に外部変更すると保存前に通知され、本文と退避内容は保持される。比較して双方の変更を確認・転記できる。キャンセルで執筆へ戻れる。比較後に外部版が再変更された場合も、保存前に再確認し、古い比較結果を根拠に無確認で上書きしない。アプリ終了中の外部変更も、復元後に検知できる。

## Editor機能の追加候補（未採用）

### F1 空白・タブ・実改行を必要なときだけ見せる

テスト文書の半角／全角空白は間隔として見えるが、どの文字なのかまでは判別しづらい。ソース・UIを確認した範囲では不可視文字表示の切り替えを見つけられなかった。

- 用途：字下げのずれ、不要な空白、折り返しと実改行の違いを調べる。
- 最初の範囲：半角空白・全角空白・タブ・実改行の表示切り替え。通常はOff。
- 条件：表示だけを変え、保存内容・コピー文字列・Undo履歴を変えない。
- 自動で空白を削除する機能とは別。Markdownの意味を変える整形は同時に入れない。

### F2 参照文書の読み取り専用

分割表示で資料を横に置く使い方に合う。現行要件・実装を検索した範囲では、文書の編集ロックを確認できなかった。

- 用途：設定資料・旧稿を読みながら新稿を書くときの誤編集防止。
- 最初の範囲：文書単位の編集ロック。選択・コピー・検索・スクロールは維持する。
- 条件：タブ等に状態を表示。同一文書の全ペインで挙動をそろえる。OSのファイル属性変更とは分ける。

### 既存候補との関係

E5（現在の文書をツリーで示す・更新）、E6（閉じたタブを開き直す）、E7（保存版との差分）、C1（操作一覧）は既に追加候補なので重複して新設しない。深いツリーを実画面でも確認したため、E5・E6の有用性は高いと感じた。

E11・E12やPDF等の予定済み機能を、今回の未実装バグとして扱わない。クイックオープンなど、明示的に見送った機能も再提案しない。

## 問題なく確認できた範囲

| 操作 | 結果 |
| --- | --- |
| メニューからOpen File | 日本語パスのテスト文書を開けた |
| 横書き本文のクリック・文字入力 | 指定した行の末尾に入力できた |
| Ctrl+Z | 入力内容は元に戻った。未保存表示はR2 |
| Ctrl+Fと「春」の検索 | 3件を表示・強調。Enterで1/3へ移動 |
| Escで検索を閉じる | 本文に戻れた。Settingsを重ねた場合はR3 |
| 横→縦の切り替え | 選択位置を維持。日本語・半角英語・箇条書き・ルビを表示 |
| 縦書きでF3 | 次の一致へ移動し2/3と表示 |
| Split Right | 同じ文書を左右に表示。右だけ横書きへ切り替え可能 |
| 右で「共有確認」を入力・Undo | 左の縦書きにも反映し、Undoで両側とも戻った |
| Ctrl+H、alpha→betaのReplace All | Alpha/alpha/ALPHAの3件がbetaになり、左側にも反映 |
| 全置換後のCtrl+Z | 1回で3件とも元の大小文字へ戻った |
| Ctrl+S | 保存完了表示、両側の未保存印が消え、ファイル本文も確認できた |
| Ctrl+W | 右ペインの最後のタブを閉じるとペインが消えた。次のキーはR4 |

本文に場所を取り、縦横で同じ原稿を確認しながら編集できる構成は、この用途に合っている。短いテスト文書の今回の操作ではクラッシュや編集文字の欠落は観察しなかった。ただし性能測定ではなく、滑らかさや低遅延を保証するものではない。

## 未確認と後片付け

- IMEのローマ字入力→候補選択→確定／取消は未確認。日本語はツールの文字列入力であり、通常のIME操作試験とは区別する。
- 長文のスクロール性能、画面外までのドラッグ選択、DPI・小窓、再起動復元、障害時の退避、Terminalは今回未確認。
- 前回の保存関連3件はコードレビューのまま。Save Asの衝突・外部変更の再現試験は別途必要。
- テスト本文の変更はUndoで戻して保存済み。作成時に混在した改行はEditorの保存でLFへ統一された。
- テストタブと追加した右ペインを閉じ、起動時の既存2タブ・1ペインへ戻した。Editor自体は開いたまま。
- 再確認用にテスト文書を残す。履歴にテスト文書を開いた記録が残り得る。
- 本書とテスト文書のみを新規作成。既存の「追加要件.md」の利用者による追記は変更していない。

推奨順：前回の原稿保全の指摘とS2を優先し、S1・U1で保存方針と表示をそろえる。今回のR1〜R3、R4の手動再確認、U2も進める。F1・F2は必要性を判断して追加する。
