# メニュー操作・接続仕様（RFN01-34／12／18）

A案の具体的な配置・状態遷移・変更対象・検証順序は[画面仕様と実装計画](メニューA案_画面仕様と実装計画.md)を参照。

## 今回の確定範囲（2026-09-20、ユーザー合意）

**A案：タイトルバーへのメニュー統合を採用する。今回の作業は既存機能のメニュー化に限定し、不足機能の追加を含めない。** 以下の調査台帳に残る将来案より、この範囲を優先する。

- 含む：A案の表示・開閉、メニューのマウス／キーボード操作、対象と選択の保持、有効条件・チェック・既存キーの表示、既存処理への接続、取消・フォーカス復帰、Windowsのウィンドウ操作を維持するための実装と検証。メニュー接続に必要なアダプタは範囲内。
- 含む：既存の箇条書き・番号付きリスト・振り直し（I41〜45）、既存のファイル・編集・表示・比較・Terminal記録等。調査で漏れていた既存の別TAB表示、文書フォルダでTerminal起動、TAB本文背景も既存操作として配置する。
- 含めない：I01〜40のリンク・ルビ・装飾・見出し・字下げ・地付き・改ページ等の新規挿入コマンド。記法を解析・描画できることは、その挿入操作が既存であることを意味しない。RFN01-12の詳細案は将来の設計資料として保持する。
- 含めない：第11節の追加挿入機能、Helpの新規ガイド／About画面、ブックマーク、下段Terminalの新しいログ／転送対応、Save AllへのPanel追加、文書単位の新しい表示設定保存方式。
- 既存動作を変更しない：Save Allの対象範囲、Close Savedの対象、背景変更の副作用、既存の閲覧モード遷移・ログ処理を維持する。誤解を避ける項目名・説明はメニュー設計内で整える。
- 行番号・空白／改行表示は既存の方向別共有値を操作する接続のみが今回の範囲。現在文書だけの上書きを新設しない。既存の共有値を変えることが伝わる表示を用い、暗黙に対象文書だけの操作として説明しない。
- 実画面確認後の訂正：トップ分類はFile / Edit / Insert / Layout / View / Run / Help。I01〜40とH01〜02は階層ごと配置し、末端を「未実装」の無効項目として表示する。新しい機能を実装しないことと、メニューを表示しないことを区別する。
- A案の配置選択は確定済み。B案への変更を実装側で決めない。タイトルバーの移動・リサイズ・最大化・最小化・閉じる・Snap・DPIを維持する具体的な接続と画面仕様を詰める。

以下の「要判断」のうち、新機能の採否・機能拡張に関するものは今回の着手条件から外す。メニュー自身の操作・配置の詳細と、既存処理への接続検証に絞って進める。今回の合意は範囲とA案の採用であり、実装完了・実画面確認済みの記録ではない。

2026-09-20。現在の作業ツリーをソース調査した設計資料。実装・実操作確認・テスト実行はしていない。分類は合意済み、以下の追加配置・実行条件はレビュー案。コードで確認した現行動作を「現行」、新しい決め方を「案」、不足を「要技術確認」「要判断」で区別する。

本書を末端項目の接続台帳とする。[基本メニュー整理案](基本メニュー整理案.md)は分類・経緯、[挿入メニュー詳細案](挿入メニュー詳細案.md)は生成文字列を参照する。旧資料の「既存処理を使う」だけの記述は本書で具体化する。未決を残したまま実装可能とは判定しない。

## 1. 対象と実行の共通仕様案

- メニューを開く直前に、親Pane、TABの識別子、文書参照、Editor Panelの識別子／source_id、TerminalのFront／Belowとセッション、入力欄、選択の両端、文書の変更状態を保持する。配列indexだけを保存しない。
- 保存・比較等の現行コールバックは `focused_pane`／`live.active` に依存する。メニューへフォーカスが移った後、そのまま呼ぶだけでは接続完了にならない。対象を明示する内部入口へ分離するか、保存した対象を検証して既存入口に渡す。どちらの場合も実行中のTAB変更で対象をすり替えない。
- Editor Panelは独立した文書とsource_idを持つ。親Paneの本文への代用実行を禁止する。Terminalは文書の代わりにFront／Belowのセッションを渡す。
- メニューを閉じただけなら選択と本文を変えず元の入力先に戻る。ダイアログを開く操作はその入力欄、文書作成は作成した本文、移動操作は移動先へフォーカス。保存ダイアログのキャンセルは元の対象へ戻す。複数件処理のキャンセルは既に完了した操作を巻き戻さない。
- 書換えは `document.read_only()` と画面のViewer／ReadOnlyをともに検査。IME変換中の新規挿入は無効にする案。既存コールバックに検査があるかどうかに関係なくメニューでも対象を再検査する。
- 本文の新規編集は `apply_span_edit` へ接続し、変更範囲・置換文字列・実行後の選択を渡す。Undoを一回に区切り、後続入力とも分離。文字数上限、文字境界、他の表示面の更新を維持する。保存・ファイル名変更・ログ・シェル切替・表示操作は本文Undoの対象ではない。
- 表中の「本文」は通常の文書編集面。「Panel」はEditor Panel。「入力欄」は検索・設定等のTextInput。本文専用とした項目をPanelへ自動拡張しない。共通項目は無効表示、対象固有群は切替表示という案は要判断。
- 各行の確認は、対象Aと隣のBを異なる内容にしてAから実行し、Bが変わらないことも確認する。書換え項目はUndo一回・Redo、開く項目はキャンセルも確認する。以下の「確認」は実施済みではなく受入手順。

## 2. File

| ID／到達経路（File →） | 対象・有効条件／実行結果 | 現行の入口 → 処理、必要な接続 | 確認手順・残点 |
| --- | --- | --- | --- |
| F01 New File | 対象領域に無題文書を作成し本文へ。PanelからはPanel文書 | `main.rs:on_pane_new_file → new_file_tab → open_tab`。Panel分岐は `terminal_panels::action(1)` | 主Pane／空TAB／Panelで作成先を確認。on_pane_new_fileは空TABならanswer_new_tab、その他はnew_file_tabへ分岐する |
| F02 New Terminal → 登録シェル各項目 | 親Paneに選んだシェルのTAB。起動失敗時に成功扱いしない | `on_pane_new_terminal → new_terminal_tab`。Panelは親Paneへ変換 | 2種のシェルで起動。登録一覧が空なら無効 |
| F03 Open File… | 本文は対象Pane、PanelはそのPanel。キャンセルは無変更 | `wiring.rs:on_open_file_requested → open_document`。Panelは `terminal_panels::action(11) → open_file` | 同一ファイル・無題・別Pane・Panelを確認。主入口の重複ファイル処理は維持 |
| F04 Open Folder… | Explorerで選択フォルダを開く。文書の置換ではない | `wiring.rs:on_work_folder_requested` | open_work_folderは一時的な単一フォルダ表示へ切替え、検索範囲を解除する。既定Workspaceの登録は変えない。取消は無変更 |
| F05 Recent Folders → 各フォルダ | 登録済みパスを開く。存在しないパスは既存エラーへ | `on_recent_folder_chosen(index)` | 開いた後に並びが変わっても別パスを実行しない |
| F06 Workspaces… | Workspace管理画面へ | `on_workspace_manager_requested → workspace_manager_requested` | 開く・閉じる。個別の管理操作は管理画面内 |
| F07 Save | 本文／Panel。無題は保存先指定。通常ReadOnlyモードの上書き不可 | `wiring.rs:on_save_requested → saving::save_document(false)`。Panel source_id分岐 → `terminal_panels::save` | 文書内部のread_onlyと画面ReadOnlyを区別。外部版のread_onlyはSave Asも拒否する現行条件 |
| F08 Save As… | 本文／Panel。保存先・文字コード・改行形式を選ぶ | `saving::save_document(true)`／Panel `action(6)` | 無題、保存先衝突、キャンセル。画面ReadOnly解除とPanelの扱いはそれぞれ確認 |
| F09 Save All | 現行は全主PaneのTAB文書を重複排除して列挙。Editor Panelは列挙しない | `saving::save_all → save_all_native(false) → save_all_including_memos_with_choice`、`main.rs:open_documents` | 編集済みを保存。外部変更は保留。無題ダイアログを取消すと残りの無題を保留、既存の保存済み結果は維持。Panelを含める変更は要判断 |
| F10 Close Tab | 主領域はそのTAB、Panelはその文書／セッション | `on_pane_tab_closed → close_tab`／`terminal_panels::action(2) → request_close` | 未保存／実行中Terminalの確認、キャンセル後も対象を維持 |
| F11 Close Other Tabs | 現在の主Pane内、現在TAB以外。Panel中は無効案 | `on_pane_close_others` → CloseRun系 | 他Paneを閉じない。途中取消時に既に閉じたTABは復元しない |
| F12 Close All Tabs | 現在の主Pane内。Panel中は無効案 | `on_pane_close_all` → CloseRun系 | 最後のTAB後の空TAB、途中取消を確認 |
| F13 Close Saved Tabs | 現行判定は `terminal.is_none() && !document.text.edited()` | `on_pane_close_clean → close_clean_tabs → advance_close_run` | 保存先の有無を検査していない。「保存済みだけ」と説明しない。空TAB／設定TABを除外すべきか要判断 |
| F14 Reopen Closed Tab | 主TAB履歴。Panel履歴は追加しない | `on_reopen_closed_requested → reopen_closed_tab`（遅延実行） | 履歴なしを無効。閉じた未保存文書の復帰を確認 |
| F15 File Operations → Rename… | 保存先のある通常文書。TAB名入力へ移る | TABの `rename-token` → `on_pane_tab_renamed → rename_tab → move_entry` | 拡張子を含む名前、不正名、同名衝突、Esc。ファイル変更は本文Undo不可 |
| F16 File Operations → Rename Panel Tab… | 保存先のあるPanelの実ファイル名を変更。取り込み中は不可 | `on_panel_renamed → terminal_panels::rename` | renameはmove_entryで実ファイルを移しfollow_renameする。無題は先に保存が必要。単なる表示名変更という前案を訂正 |
| F17 File Operations → Copy Path | 保存先のある主文書。クリップボードへ | `on_pane_copy_path → copy_tab_path` | 無題で無効、選択本文をコピーしない |
| F18 File Operations → Reveal in Explorer | 保存先のある主文書 | `on_reveal_requested → saving::reveal_active_document → shell::reveal` | 別Paneのパスを開かない |
| F19 File Operations → Reopen with Encoding → UTF-8／UTF-16 LE／UTF-16 BE／CP932 | 保存先のある文書を選択した読取文字コードで再読込。保存形式変更とは別 | `on_reopen_encoding → encoding_of_id → reopen_as_asked` | IDはUTF-8=0、LE=1、BE=2、CP932=3。未編集は即再読込、編集済みは保存して／破棄して／取消の確認を通す |
| F20 Print… | 主文書の印刷プレビューへ | `on_print_requested → print_view::open`。既に印刷画面なら同コールバックは `print_now` | メニューが印刷画面でも押せると突然印刷になる。印刷画面中は主メニュー無効案 |
| F21 Exit | ウィンドウ終了要求へ。確認取消なら継続 | `main.rs` のウィンドウclose-requested処理を共通化して接続 | 強制終了を使わない。退避失敗・未保存・Terminalの各確認を通す |
| F22 File Operations → Open in Another Tab【漏れ補正・追加案】 | 同じ文書を別TABで表示。独立したファイル複製ではない | TAB右クリック → `on_pane_duplicate_tab` | 一方の編集が他方へ反映し、表示位置は独立することを確認 |
| F23 New Terminal in Document Folder【漏れ補正・追加案】 | 現在文書のフォルダで既定シェルを起動 | ペインメニュー → `app-window.slint:terminal-in-folder → shell-profile-action(8,0)` | terminal_shells::action(8) → open_in。保存先なしは通知して終了。既定シェルのdirectoryだけ文書の親フォルダにして起動。F02との表示順は案 |

## 3. Edit

| ID／到達経路（Edit →） | 対象・有効条件／結果 | 既存接続・新設点 | 確認手順・残点 |
| --- | --- | --- | --- |
| E01 Undo／E02 Redo | 編集可能な文書、対応履歴あり | `on_pane_undo(pane,false/true) → undo_in_pane` | Panelはsource_id、入力欄は欄自身の履歴へ。入力欄用のメニュー入口は新設が必要 |
| E03 Cut | 編集可能で選択あり | `on_pane_copy(pane,true) → copy_selection` | Terminalは不可。入力欄から背後の本文へ送らない |
| E04 Copy | 選択あり。本文・Panel・Terminal・入力欄 | 本文 `copy_selection(false)`、主Terminal `copy_terminal_selection(Front)`、下段 `on_pane_below_copy` | TerminalはCtrl+Shift+C表示。入力欄はUIアダプタを要技術確認 |
| E05 Paste | 編集可能な対象／稼働中Terminal、対応クリップボードあり | 本文・主Terminalはime-input.paste()、下段Terminalはbelow-ime.paste()。UI内の公開メソッドを新設してこの経路を呼ぶ | キーイベント偽装を使わない。Panelも同じEditorPaneのime-input。検索／設定欄は各TextInputのpaste()へ。複数行のTerminal確認を迂回しない |
| E06 Select All | 本文／Panel／入力欄 | `on_pane_select_all → select_whole_document`、入力欄は欄自身 | Terminalは今回は無効案。本文を変えず対象の選択だけを変更 |
| E07 Copy Text Only | 本文。選択なしでも有効 | `on_pane_copy_body`：Markdownは `plain_body_text`、非Markdownは選択範囲または全文 | 選択なしで全文。Copyと同じ「選択必須」条件にしない |
| E08 Find… | 本文→検索欄、Terminal→出力検索、設定→設定検索 | 本文 `toggle_find(pane,false)`、Terminal `terminal_workflow::action(spot,0)` | Panelはpanel_source::syncが登録したsource_idをtoggle_findへ渡す。find_targetがfind-paneを優先しPanel自身を検索する |
| E09 Replace… | 書換え可能な本文 | `toggle_find(pane,true)` → `wiring.rs:wire_find` の置換処理 | Viewer／ReadOnly無効。Panelもsource_idをfind-paneに保持しreplace_in_paneへ。親Paneに変換しない |
| E10 Go to Line… | 本文 | `on_goto_requested → toggle_goto`、`on_goto_accepted` | goto_targetは保持したgoto-paneを優先。Panelもsource_id。メニューからtaking=trueで既に開いていても入力へ、キーはfalseで開閉する差を維持 |
| E11 Find in Folder… | フォルダ検索パネル | `on_folder_search_requested`／`on_search_folder_requested` | メニュー入口は既存左上と同じtree-open=true、left-tab=1、left-tab-chosen(1)。検索実行のfolder_search_requestedや範囲選択search_folder_requestedとは別 |
| E12 Terminal → Clear Screen | 対象TerminalのFront／Below | `terminal_workflow::action(spot,5)` | 保存済みログ・Panel文書は削除しない。本文Undoなし |
| E13 Terminal → Clear History | 同上 | `terminal_workflow::action(spot,6)` | 保持履歴のクリア。ログ停止とは別 |
| E14 Lines → Delete Line | 編集可能な本文、現在行／選択行 | `pane_line_edit(4) → edit_lines → document::line_edit(Drop) → apply_span_edit` | 終端行、最終改行、複数行を確認 |
| E15／16 Lines → Move Line Up／Down | 同上、移動先あり | 同入口の0／1 → MoveBefore／MoveAfter | 先頭／末尾は無変更。選択行を維持 |
| E17／18 Lines → Duplicate Line Up／Down | 同上、文字数上限内 | 同入口の2／3 → CopyBefore／CopyAfter | 選択・Undo一回、別Pane表示も更新 |
| E19 Selection → Start／Cancel Selection | 本文の現在選択状態 | `pane_mark_toggled(false) → toggle_mark` | 本文変更なし、現在状態でラベル切替 |
| E20 Selection → Start／Cancel Rectangle Selection | 本文 | `pane_mark_toggled(true) → toggle_mark` | 矩形状態の解除まで確認。Insertでの矩形変換は別 |
| E21 Kill Ring → Cut to End of Line | 書換え可能な本文、行末までの文字／改行あり | `pane_kill(0) → kill_ring_action(ToLineEnd)` | 行末では改行を切る。文書末尾は無変更 |
| E22 Kill Ring → Copy to Kill Ring | 本文、選択あり | `pane_kill(1)` | クリップボードCopyと区別。本文Undoなし |
| E23 Kill Ring → Cut to Kill Ring | 書換え可能、選択あり | `pane_kill(2)` | 選択をリングへ入れ削除、Undo |
| E24 Kill Ring → Paste Latest Kill | 書換え可能、リングに内容あり | `pane_kill(3) → yank_into_pane` | 空リング無効、貼付範囲を記録 |
| E25 Kill Ring → Replace with Previous Kill | 書換え可能、直前のYank範囲が有効 | `pane_kill(4) → standing_yank → yank_into_pane` | 間に編集／移動／Undoしたら古い位置を上書きしない |
| E26 Word Check → Mode → None／登録モード | 対象の主文書。現在モードに印 | `on_pane_word_mode_chosen` | 共有語群は変更しない。モードIDを保持し並替に耐える |

## 4. Insert

全項目は本文書換えであり、通常本文の編集可能状態が対象。新規ひな形は初回Panel・入力欄・Terminal・矩形選択で無効とする案。既存箇条書きは現行範囲を維持する。生成文字列の詳細は別紙を参照するが、処理の接続は下記で管理する。

| ID／到達経路（Insert →） | 生成・選択・キャレット | 接続／現在の状態 | 確認 |
| --- | --- | --- | --- |
| I01 Link → Markdown Link | 未選択 `[│]()`、選択 `[本文](│)` | 新規変換 → `apply_span_edit`。読取は `document::link_here` | 選択反転、日本語・絵文字、Undo |
| I02 Link → Wiki Link | 未選択 `[[│]]`、選択 `[[本文│]]`案 | 同上。`link_completion.rs` の既存更新経路へ | 候補確定で閉じ括弧が重複しない |
| I03 Link → Wiki Link with Alias | 未選択のリンク先欄、選択文字は別名欄へ（別紙） | 新規変換。採用は要判断 | 別名を壊さず候補確定、区切り文字入りを確認 |
| I04 Ruby | 選択 `｜本文《│》`、未選択 `｜│《》` | 新規変換。解析 `ruby_here` | 親文字を失わず読み欄へ。解釈OFFでも設定を変えない |
| I05 Annotation → Left-side Annotation | 本文を選択必須、参照文字列を複製して左注記の空欄へ | 新規変換、解析 `side_note_here` | 未選択無効案。参照する親文字一致 |
| I06 Annotation → Ruby and Left-side Annotation | 選択必須、両方の記法を作りルビ欄へ | 同上 | 自動Tab移動・別ダイアログなし |
| I07 Emphasis Marks → 傍点 | `《《│》》`、選択時は包み末尾へ | 新規変換 | 空ひな形と選択の両方 |
| I08〜12 Emphasis Marks → ゴマ／丸／白丸／二重丸／× | 各々の名称で参照型注記。選択必須案 | 新規変換、解析 `dots_note_here` | 5種類を別々に生成・表示照合 |
| I13〜17 Emphasis Lines → 傍線／二重傍線／波線／鎖線／破線 | 各々の名称で参照型注記。選択必須案 | 新規変換、既存注記解析 | 5種類、引用括弧を含む選択の扱いを確定 |
| I18〜21 Text Annotation → 縦中横／割り注／小さな文字／大きな文字 | 対応する開始／終了注記で包む。空なら間、選択ありなら末尾へ | 新規変換、解析 `RANGE_NOTES` | 4種類、空ひな形、Undo |
| I22〜27 Heading → H1〜H6／I28 解除 | 選択論理行／現在行の先頭記号を置換・除去 | 新規行変換 → `apply_span_edit`。解析 `heading_level` | 6レベルと解除。コード／引用／既存別注記の混在は要判断 |
| I29〜32 Paragraph Annotation → 字下げ → 1〜4字／I33 解除 | 各対象行頭に `［＃N字下げ］`、同種を置換／除去 | 新規行変換。`note_indent_head`／`note_indent_of` | 範囲字下げ内は初回無効案。空行・選択末尾の次行先頭 |
| I34 地付き／I35〜38 地から1〜4字上げ／I39 解除 | 各対象行頭の同種注記を置換・除去 | 新規行変換。`note_tail_head` | 地付きの範囲記法は生成しない。字下げとの併用は要判断 |
| I40 Page Break | 独立行 `［＃改ページ］` を一つ追加。選択本文は消さない | 新規変換。解析 `line_style`、印刷 `page_ranges` | 行途中の分割か行前かは要判断。先頭／末尾／連続を確認 |
| I41〜43 Bullet List → -／*／+ | 選択論理行。対象が全て同じ記号なら解除、混在ならその記号へ揃える | `pane_list_edit(0,mark) → edit_list → document::list_edit` | 解釈OFFの記号は無効にする案。現行処理は別の有効記号へフォールバックするので直接呼ぶだけでは不適切 |
| I44 Numbered List | 対象行へ番号。既に番号付きならまず振り直し、変化なしなら解除 | `pane_list_edit(1,mark) → ListEdit::Ordered` | 連続番号／欠番／入れ子、再実行を確認 |
| I45 Renumber | 現在位置／選択から続く番号を振り直す | `pane_list_edit(2,mark) → renumber_below` | 選択開始行からリストの連なり終端まで。空白／継続段落を跨ぎ、基準深さでリスト種類が変われば停止。最初に書かれた番号から継続。空行開始は無変更 |

新規変換関数の戻り値案は「置換範囲、置換文字列、選択両端、実行不可理由」。解析関数を挿入コマンドと呼ばない。`apply_span_edit` は文字数上限・read_only・viewerを検査するが、ひな形の構築、適用可否、Undo分離、補完呼出しは新設が必要。I41〜45は現行の箇条書き解釈設定に依存し、新規ひな形の「解釈OFFでも挿入」と混同しない。

## 5. Layout

| ID／到達経路（Layout →） | 対象・結果 | 接続 | 確認・残点 |
| --- | --- | --- | --- |
| L01 Horizontal／L02 Vertical | 主文書。選んだ方向にする。同値なら無変更 | `on_pane_direction_toggled → toggle_pane_direction` の前に現在値と要求値を比較する新規アダプタ | ReadOnlyは横固定。Terminal／Panelに文書方向を適用しない |
| L03 Pane Background… | 親Paneの背景色を変更し、そのPane内のTAB本文背景・見出し色・Terminal個別背景の上書きを解除する現行動作 | `editor-pane.slint:open-paper-palette(1,…)`、paper-chosen/default/more | paper target=1 → colour-set(kind=4)。即時反映。既定も同じ解除経路。パレットを閉じるだけなら未選択の色は適用しない |
| L04 Tab Color… | 対象主TABの見出しの色。本文背景とは別 | 同パレットのtarget=2 | 別TABへ切替後も元対象の検査。既定へ戻すの対象はTAB |
| L07 Tab Background…【漏れ補正・追加案】 | 現在TABの本文背景。見出し色と別 | 本文右クリックBackground → paper target=0 → colour-set(kind=3) | 縦横双方のtab.paperを変更。L04のtab_colourとは別 |
| L05 Terminal Appearance… | 現在の主Terminalのみ | `terminal_appearance::on_appearance_requested(pane,1)` → `Target::Front(identity)` | default(scope=0)を呼ばない。適用・閉じる・リセットを確認 |
| L06 Panel Appearance… | 現在の下段TABのみ | 同入口scope=2 → `Target::Panel(entry)` | Editor／Terminal Panel両方。選択中の別Panelにすり替わらない |

## 6. View

| ID／到達経路（View →） | 対象・結果 | 接続 | 確認・残点 |
| --- | --- | --- | --- |
| V01〜05 Sidebar → Workspace／Explorer／Search／History／Outline | 指定サイドパネル。同項目再選択で閉じる案 | `app-window.slint` の左アイコンと `wiring.rs:on_left_tab_chosen` | IDはWorkspace=4、Explorer=0、Search=1、History=2、Outline=3。tree-openを同項目再選択で反転しleft-tab-chosen(id)で再描画 |
| V06 Live Preview | 主文書のSource／Preview、チェックで表示 | `on_pane_preview_toggled` | Viewer時のショートカット43はViewer解除後切替、コールバック単体と同じではない。メニュー動作とキー併記を要判断 |
| V07 Viewer／ReadOnly／Exit Viewer／Exit ReadOnly | 主文書。SourceからReadOnly、PreviewからViewer | `on_pane_viewer_toggled → toggle_viewer` | 入口モード・解除後の方向と表示、書換え禁止を確認 |
| V08 ReadOnly（Panel） | 対象Panelの閲覧状態 | `terminal_panels::action(4)` | 取り込み中なら取り込み停止して解除する。単なる表示トグルではない |
| V09 Show Line Numbers | 主文書、チェック表示 | `Setting::LineNumbers` → Typography。既存 `on_sheet_chose` は `shown_sheet` の共有値を書き換える | 現在文書への上書き新設か共有設定か要判断。設定TABで最後に見た方向を誤操作しない |
| V10 Show Spaces and Line Breaks | 主文書、チェック表示 | Setting::Whitespace（ID54）→ Typography.whitespace。現行は空白・改行を一括ON/OFF | V09と同じ保存単位の判断が必要 |
| V11／12 Panes → Split Right／Split Down | 親Paneを分割。上限時無効 | `on_divide_requested(true/false)` | 分割後の文書・フォーカス、最大数を現行判定から取得 |
| V13 Panes → Close Other Panes | 他PaneのTABを現在Paneへ集める。文書を閉じる意味ではない | `on_close_others_requested → close_other_panes` | TAB・Terminalを失わず一つにする |
| V14〜17 Panes → Swap Left／Right／Up／Down | 指定方向に隣接Paneがある場合 | `on_swap_requested(0/1/2/3)` | 隣なし無効、TAB内順序を変えない |
| V18／19 Go To → Next／Previous Tab | 親主PaneのTAB列 | `on_pane_tab_stepped(false/true)` | Panel独自の巡回を新設しない案 |
| V20〜22 Go To → Left／Right／Upper Pane | 隣接Paneへ | `on_pane_focus_moved(0/1/2)` | 存在しない方向は無効 |
| V23 Go To → Panel Below／Lower Pane | 下段があれば下段、それ以外は下Pane | `shortcuts.rs:command16` → below_focus(true)／focus_moved(3) | below_kindは非表示の下段を0とする。したがって隠れている場合は下のPaneへ移る |
| V24／25 Go To → Back／Forward | 文書移動履歴 | `on_pane_navigate(false/true)` | 履歴なし無効、本文Undoと分離 |
| V26 Terminal Panel／Editor Panel | 親TABの下段を開閉 | `on_pane_below_toggled → toggle_below` | 非表示にしてもシェル終了・ログ停止しない |
| V27 Latest Terminal Output | 対象Terminal Front／Belowの最新位置 | `terminal_workflow::action(spot,4)` | 文書キャレットへ流用しない |
| V28 Focus Editor Above／Focus Terminal Above | 下段から親へ | `on_pane_below_focus(parent,false)` | 下段TABやログは保持 |
| V29 Quick Draft… | 独立した既存窓を開く | `wiring.rs:on_quick_draft_requested` | 既存窓がある時の再表示、閉じる際の元対象 |
| V30 Zoom → Zoom In／V31 Zoom Out | 主文書の倍率、本文文字列不変 | `on_pane_zoom(pane,+1/-1) → zoom_pane` | Terminalは無効。倍率上限下限 |
| V32 Zoom → Reset Zoom | 主文書 | `on_pane_zoom_reset` | 現行の既定倍率は150%。元の書体サイズ設定は変更しない |
| V33 Zoom → 現行倍率の各値 | 主文書、現在値に印 | `on_pane_zoom_set(pane,percent)` | UIの倍率モデルと同じ値を使い二重定義しない |

## 7. Run／Help

| ID／到達経路 | 対象・結果 | 接続 | 確認・残点 |
| --- | --- | --- | --- |
| R01 Run → Compare → With Saved | 保存先のある主文書と取得時点のディスク内容 | `diff_view::on_compare_saved_requested → show_saved` | 文書のファイル情報はcloneして再読込。外部変更の承認・保存を兼ねない |
| R02 Run → Compare → With Git Last Commit | 保存先のある主文書とGit HEAD版 | `on_compare_head_requested → show_head → git_version::head_version` | 非Git／未追跡／取得失敗を表示。Git操作の追加ではない |
| R03 Run → Compare → With Another File… | 主文書（無題可）、選択ファイルとの比較画面へ | `on_compare_files_requested → show_merge` | キャンセル無変更。R01/R02はshow、これはshow_mergeなので「全比較が同じ操作」と説明しない |
| R04 Run → Terminal Output → Save Output… | Front／Belowの保持中出力を一度保存 | `terminal_workflow::action(spot,7)` | 既存文書・ログ先との衝突拒否、保存取消、失敗。全過去ログを保証しない |
| R05 Capture Log to File… | 主Terminalの以後の出力 | `terminal_panels::action(10) → start_file` | 下段は無効案。保存先取消で開始しない。Panelを作らない |
| R06 Stop File Log | 主Terminalでファイル記録中 | `action(14) → stop_source(...,true)` | Panelログは継続、ファイルは残す |
| R07 Capture Log to Editor Panel | 主Terminal、新規取り込み先Panelを作る | `action(7) → import` | 同じTerminalの重複取り込みを拒否。読み取り専用で取り込む |
| R08 Stop Panel Log（主Terminal側） | 主Terminalの取り込み停止 | `action(13) → stop_source(...,false)` | ファイルログを止めない |
| R09 Stop Panel Log（取り込み先Panel側） | そのPanelが受けている取り込みだけ停止 | `action(5)` → current entry.stop | 親Terminalの別Panelを誤停止しない。R08と分ける |
| R10 Capture Parent Terminal Log to New Panel | Panelから親Terminalの取り込みを新規開始する案 | 親Paneを指定した `action(7)` | 現在のPanelへ追記すると誤解させない表示。採用要判断 |
| R11 Run → Send to Editor Panel → Selection | 主Terminalに選択あり、現在Panelへ挿入 | `action(9) → import` | 下段Terminalは無効案、ReadOnly先へ誤挿入しない |
| R12 Run → Send to Editor Panel → Retained History | 主Terminalの保持履歴を新規Panelへ | `action(8) → import` | 既存Panel文書を上書きしない |
| R13 Run → Switch Shell → 登録シェル各項目 | 現在Terminalのシェル切替 | 主 `on_pane_switch_shell`、下段 `terminal_panels::action(12,index)` | 実行中セッション終了確認、同じシェルは無変更、キャンセル維持 |
| H01 Help → User Guide… | 今回未提供なら非表示案 | 新規内容・表示先が必要 | READMEを自動的に流用しない。採否要判断 |
| H02 Help → About RFN Edit… | 名前・版・ビルド識別子を表示する案 | 新規画面・版情報の取得が必要 | Close／Esc／×で戻る。採否要判断 |

## 8. 既存機能からの逆引き

| 現行の入口・機能群 | 行先／扱い | 根拠と確認結果 |
| --- | --- | --- |
| 左上の開く／フォルダ／最近／Workspace／一括保存／Terminal／Draft | F02〜09、V29 | `ui/app-window.slint`、`wiring.rs` |
| 空TABのNew File／New Terminal／Open File | F01〜03。空TAB内の入口も維持 | `editor-pane.slint` 3442付近 |
| ＋、New Tab、設定済みNew Tabキー | ＋専用。キーID18の互換性は要技術確認 | `shortcuts.rs`。キー配列を詰めて後続IDをずらさない |
| TABの閉じる／他／未編集／保存／名前／パス／色 | F07〜18、L04 | TAB右クリック3931付近。Close Savedの条件差をF13へ記録 |
| Open in Another Tab | F22追加案 | TAB右クリック4049、`on_pane_duplicate_tab`。前案から漏れ |
| Terminal in Document Folder | F23追加案 | ペインメニュー。前案から漏れ |
| 分割／入替／他Paneを閉じる | V11〜17 | 他Paneを閉じてもTABを保持する意味を明記 |
| 本文右クリック編集／選択／Kill Ring／行操作 | E01〜25 | `editor-pane.slint` と `shortcuts.rs` のdispatch |
| コピー本文のみ | E07 | 選択なし全文の例外を反映 |
| 箇条書き／番号／振り直し | I41〜45 | `document::list_edit`。解析OFF時の条件差あり |
| 解析済みリンク／ルビ／傍点／傍線／範囲注記／行注記／改ページ | I01〜40 | 解析対応だけで挿入実装済みとはしない |
| 強調／脚注／画像／表の記法・操作 | 今回の挿入メニューは採否未決 | 読取機能の全記法と挿入対象の対応監査は別途必要。「対応記法を全件列挙済み」とはしない |
| Terminal主領域／下段右クリック | E04/05/08/12/13、L05/06、V27/28、R04〜13 | `terminal_workflow.rs` はspot対応。`terminal_panels.rs:import/start_file`は主Terminal依存 |
| Editor Panelの新規／開く／保存／名前／閉じる／ReadOnly／外観／取り込み | F01/03/07/08/10/16、V08、L06、R09/10 | `terminal_panels.rs:action`の数値を各行に記載 |
| コピー／貼付／検索入力欄の固定キー | E01〜06はその欄へ接続を新設 | `shortcuts.rs:FIXED`。本文キーの再送では代用しない |
| 次文字削除、単語移動・選択、通常カーソル移動 | キー操作に残す。メニューへ全移動を列挙しない案 | shortcuts command21〜23、31〜32 |
| Quick Draft内コピー／クリア／コピーして閉じる／TABへ貼付 | Draft内に残す | shortcuts38〜41。メイン側V29は入口のみ |
| Explorerの作成／複製／削除／名前変更／Terminal／検索 | Explorer内に残す | `wiring.rs:on_explorer_command/on_tree_command`。主文書のF15と対象が違う |
| Workspace切替／管理／フォルダ移転等 | Workspace内に残す。F06は管理入口 | `wiring.rs:wire_workspace` |
| 検索条件／履歴／前後／置換／全置換 | 検索画面内に残す | `wiring.rs:wire_find` |
| 比較の差分移動／採用／反映／終了 | 比較画面内に残す | `diff_view.rs`。R03は反映可能な経路 |
| 用紙／印刷方向／余白／倍率／実印刷 | 印刷画面内に残す | `print_view.rs`。F20はプレビュー入口 |
| 全体フォント／組版／背景／言語／シェル登録／キー割当／語群管理 | 設定ボタンからの設定画面のみ | `wire_typography/wire_colours/wire_terminal_look/wire_word_modes` |
| 個別右クリックのLayout…（全体設定へ） | メインへ移さない。既存入口を削除するかは要判断 | ペインメニューには現存。全体設定を置かない合意と既存入口の整理は別 |
| 外部変更／ファイル消失の通知 | 通知から解決画面へ。メニュー追加なし案 | `on_outside_asked`、保存時確認 |
| リンク候補／リンクを開く／画像リサイズ | 本文内の直接操作に残す | メニュー挿入と別。新規ダイアログを追加しない |
| ブックマーク | 今回のメニューには置かない、RFN01-28 | 未実装の入口を有効に見せない |

## 9. メニュー本体の操作仕様案

1. 左上Menuでバー開閉。初回は閉じる。バーを開いた時点では本文の選択を変えない。
2. File/Edit/Insert/Layout/View/Run/Helpを順に表示。採用項目がないHelpは見せない案。英日ラベルは同じコマンドIDから取得する。
3. 項目実行・外側クリックはドロップダウンのみを閉じ、バーを維持。Menuでバーを閉じる。Escは子→親ドロップダウン→バーを順に閉じる案。
4. キー入口はF10案。上下で有効項目、左右でトップ項目／子メニュー、Enterで実行、Escで戻る。既存Alt+文字は奪わない。IME変換中は入口キーを奪わない。ソース上TerminalはF10をシェルへ渡す（editor-pane.slintのkey_number）。本文ではF10案、Terminalで横取りするかは要判断。
5. 開いた子メニューは画面外にはみ出すなら反対側へ。縦方向に収まらなければスクロール。項目数の多いFileと動的シェル一覧で確認する。
6. メニュー表示中にTAB削除・文書変更・Terminal終了があれば状態を再評価。保存した位置に対する編集は中止し、再選択を促す。ファイルダイアログ・既存モーダル表示中は主メニューを無効にする案。
7. 配置はタイトルバー統合か直下か要判断・要技術確認。標準タイトルバーのSnap／移動／最大化・DPIを保つ実現性調査と画面案が未完了。実装側で直下案へ勝手に変更しない。
8. 開閉の永続保存は要判断。現案は起動ごとに閉じる。設定保存単位を未合意で増やさない。

## 10. 調査で訂正した点／判断事項

### コードで判明した訂正

- Save AllはPanelを含まない。対象説明を確定せず「すべて」とだけ説明していた。
- Close Savedは保存先ありを条件にしていない。Terminal以外かつ未編集という条件を記録した。
- Copy Text Onlyは選択なしなら全文。Copyと同条件にはしない。
- Live Previewのコールバックとキー43はViewer時の手順が異なる。単純なキー併記は誤りになる。
- 印刷コールバックは印刷画面中には即印刷を実行する。入口の再利用に状態条件が必要。
- 比較はSaved／GitとAnother Fileでshow／show_mergeが異なる。
- 箇条書きの無効記号指定は別記号にフォールバックする。指定記号の表示と結果が食い違わない条件が必要。
- Open in Another Tab、Terminal in Document Folderが漏れていた。追加案としてF22/23に配置した。

### 利用者の判断が必要な点（推奨案、未合意）

| 論点 | 具体案／影響 |
| --- | --- |
| Save Allの範囲 | Panelも含めるなら列挙・保存失敗集計の変更が必要。現行維持なら「Panelを除く」範囲を説明する。名前と期待が合うようPanelを含める方向を推奨するが追加実装となる |
| Close Savedの対象 | 「未編集のTABを閉じる」と表示し、設定／空TABを含めるかを確定。名前だけから推測して既存挙動を変えない |
| 行番号・空白表示 | ユーザーの対象文書という原則なら、現在文書の表示だけ切替える案を推奨。共有設定の変更で済ませず、TAB側の上書き状態が必要 |
| 改ページ | `前│後`を `前\n［＃改ページ］\n│後`にする案を推奨。選択ありは先頭の位置へ一つ挿入し選択本文は保持する案。現在行の前に置く案との選択が必要 |
| 閲覧中のLive Preview | Viewer中は無効とする旧案か、現行キーと同じくViewer解除して切替か。後者なら編集可能になることを表示名で明示する |
| 新規挿入の初回範囲 | 別紙の全種類を採るか、別名Wiki／左注記等を後回しにするか。入力画面なしは維持 |
| Help | 内容を用意して採用するか、今回はトップ項目ごと保留するか |
| 外観・開閉 | タイトルバー統合、開閉保存、無効表示方針の画面案を確認する |

### Codex側に残る技術作業（ユーザーに調査を委ねない）

- 入力欄・Panelの編集、検索、Goto、貼付に対する既存の経路と、メニューから呼ぶためのUIアダプタを確定する。
- F01/F04/F16/F19/F23、V01〜05/V10/V23の追加追跡を完了し、空TAB分岐・Workspace解除・実ファイル改名・文字コードID・シェル起動先・Sidebar ID・Whitespace ID・非表示下段の扱いを各行へ反映した。
- 表示倍率、文字コード、シェル等の動的値をコマンドIDへ結ぶ。MenuRowの無効・フォーカス・スクロール実装とタイトルバー方式を検証する。
- ui/*.slintのMenuRow宣言149件を抽出した[現行入口索引](メニュー現行入口索引.md)を照合用に追加。各宣言に配置IDまたは既存画面に残す扱いを記録した。動的な項目は宣言単位。ボタン・キーは第8節、追加の対応記法は第11節に別記する。
- 各既存処理で、取消後のフォーカス、保存／閉じるの途中失敗、非同期完了後の対象再検査を確定する。

実装へ進める条件：上記技術確認を完了し、要判断の項目を合意／除外として固定する。全末端項目について対象・有効条件・実行先・結果・取消・確認手順が定まり、メニュー本体の画面案も確認できた時点で実装計画を作る。

## 11. 対応記法からの逆引き補足

メニューの不足を「新機能がないから」で済ませないため、`document.rs` のインライン記号定義・行解析を照合した。下記は対応済みの解析に対して挿入項目の採否が未確定なもの。自動的に今回の実装範囲へ加えないが、漏れのない採否判断の対象として明示する。

| 記法 | 解析の根拠 | メニューの扱い案 |
| --- | --- | --- |
| 太字 `**…**`／`__…__` | `markers()` | Insert → Emphasis → Boldを追加候補。生成する標準記号を選ぶ必要がある |
| 斜体 `*…*`／`_…_` | 同上 | Insert → Emphasis → Italicを追加候補 |
| 取消線 `~~…~~` | 同上 | Insert → Emphasis → Strikethroughを追加候補 |
| インラインコード | 同上 | Insert → Code → Inline Codeを追加候補。選択にバッククォートを含む場合の長さを決める |
| コードフェンス | `fence_marker`／`line_style` | Insert → Code → Code Blockを追加候補。独立行と選択複数行への生成規則が必要 |
| 引用 | `quote_content`／`outside_fence` | Insert → Quoteを追加候補。行単位の付加／解除・多重引用を設計する |
| Callout | `callout_label` | 引用内のラベルを解釈。採用する種類・生成文を未選定、保留候補 |
| タスクリスト | `task_kind`／LineKind::Task | Insert → Task Listを追加候補。チェック切替の専用操作とは分ける |
| 水平罫線 | `is_rule`／LineKind::Rule | Insert → Horizontal Ruleを追加候補。改ページと別。独立行の扱いを共通化できる |
| 脚注参照 | `footnote_here` | 参照と定義をどこへ置くかが必要。単純な空ひな形だけでよいか要判断 |
| 表 | `table_line` | 行列数を決めるか最小ひな形を置くか未決。入力画面を無断追加しない |
| Markdown画像／Wiki画像 | `image_of_line` | `![説明](画像.png)`／`![[画像.png]]`等。通常リンクと別、幅指定対応。画像ファイル選択とひな形挿入の採否は別に決める |

通常の改行・空白・エスケープ文字は手入力に残す。画像リサイズや既存リンク補完など本文内の直接操作は維持する。既存の記法を全てメニューに載せること自体はまだ合意していない。

## 12. 接続実装の境界と確認単位

実装時の内部構造案（未実装）：

1. 項目定義はコマンドID、表示名、親ID、表示順、動的値を持つ。画面に同名のコピーを作って別々の処理を持たせない。
2. 実行対象を保持する構造と、コマンドごとの有効判定をRust側に設ける。UIはその状態を表示する。スナップショット後のTAB・文書の生存確認を実行直前に行う。
3. 本文／Panelの編集は明示したPaneIdへ、Terminalはセッションとspotへ、入力欄はSlint側の対象TextInputへ振り分ける。入力欄は検索語、置換語、Goto、設定検索・編集、名前入力を列挙してアダプタを作る。欄が閉じたら無効化し、本文へフォールバックしない。
4. 貼付は既存TextInput.pasteを公開メソッドから呼ぶ。カット・コピー・全選択も欄自身へ。Undo/Redoは各欄の履歴操作を公開できることの確認が必要で、本文Undoで代用しない。これは未調査の業務仕様ではなく、新しいメニューと既存入力部品を接続する実装上の検証点。
5. ひな形／行変換は文字列計算と適用を分け、同じapply_span_editへ。既存キーへの追加は安定したIDを使い、New TabのIDを削って後ろをずらさない。
6. MenuRowにはenabled／checked／hoveredとマウス起動はあるが、バー全体のキー巡回管理はない。F10・矢印・子メニュー・フォーカス復帰を持つホストが新設範囲。TerminalのF10送信との競合は要判断。
7. 現行src/uiの探索では独自タイトルバーのヒットテスト処理を確認できない。タイトルバー統合案は既存MenuRowを並べるだけでは実現済みにならない。ウィンドウ移動・リサイズ・Snap・DPIを含めた小規模試作を別工程とし、その結果を見て配置を確定する。

| 確認単位 | 操作と期待結果 |
| --- | --- |
| 対象固定 | 左右PaneにA/B、下段にCを開く。各々の選択からCopy／Save／挿入を実行して他の文書が変わらない。メニュー中に元TABが消えたら実行しない |
| 入力欄 | 検索欄の語を選びCut／Paste／Undo。検索語だけが変わり本文のUndo履歴は変わらない |
| 作成・開く | 空TAB／文書／PanelからF01〜03。作成先とフォーカスを確認し、キャンセルでTABを増やさない |
| 保存・閉じる | 主文書とPanelを未保存にしてF09の合意した範囲を確認。保存失敗・無題取消・一括閉じるの途中取消で残りを失わない |
| 記法 | 各I項目を未選択／順向き／逆向き選択、日本語・絵文字で実行。生成文字列・キャレットを別紙と比較、Undo一回。文字数上限超過は無変更 |
| 行操作 | 最初／最後の行、末尾改行ありなし、選択末尾が次行先頭、空行、既存指定、混在指定。不適用の場合に一部だけ書き換えない（新規行変換案） |
| 改ページ | 行途中／先頭／末尾／連続、縦横、解釈ON/OFF、コード内。画面破線と印刷分割を区別して確認 |
| Terminal | 主／下段で出力保存・検索・クリア。主Terminalのファイル／Panelログを同時開始し一方だけ停止。選択転送と履歴転送の作成先を確認 |
| 背景 | L03でPane配下の個別背景・見出し色が解除される。L04は見出しのみ、L07は本文のみ。Terminal個別背景にも現行の解除が及ぶことを確認 |
| 閲覧・表示 | Source／Preview／Viewer／ReadOnlyを遷移。閲覧解除の有無、行番号の適用範囲、倍率の保存先、書換え不可を確認 |
| 比較・印刷 | 無題／保存先あり／非Git／GitでR01〜03。取消で原文不変。印刷プレビュー中にFile→Printが意図せず印刷しない |
| メニュー本体 | キーのみ、マウス、IME中、狭い幅、125/150/200% DPI、子メニューの端、長い一覧、Esc、外側クリック、再起動後を確認 |

以上は仕様レビュー用の確認手順であり、テスト成功・UI確認済みという記録ではない。
