# 色分けの要件

記法の色・単語チェックモード・そして**誰の色が勝つか**。2026-09-08に書き手と話して決めた。

## 1. この文書の位置づけ

**要件定義.mdから切り出した1章である。**要件定義は1,000行を超えていて、色分けは
これから記法・単語・検索の3方向へ伸びる——同じファイルへ足し続けると、どちらも読めなくなる。

- **ここが正**：色に関する決定はこの文書にある。要件定義 7.9（軽い校正）と 7.3.2（整形表示する
  要素）は、**この文書を指すだけ**にして中身を二重に持たない。
- **要件定義 4.2 は動かない**：プログラムコード向けの補完・デバッグ・高度なシンタックス
  ハイライトは提供しない。この文書はその制約の中で書かれている。
- 引用する形は「色分け要件 3.2」のように節番号で。

## 2. 前提：IDEから何を借りて、何を借りないか

**書き手の問い（2026-09-08）**：「ソースモードのときにMarkdownの装飾文字をコードのように
色を変えるつもり。そうするとIDEに近い機能が必要になりませんか」。

### 2.1 色分けは3つの層でできている

| 層 | IDEでの中身 | この編集器 |
|----|-------------|-----------|
| ① 字句 | tree-sitter／TextMate文法。差分で解析し直す | **もうある** |
| ② 意味 | 記号表、定義へ移動、補完、診断 | **取らない**（要件 4.2） |
| ③ 決定 | 複数の色源が同じ範囲を取り合ったとき誰が勝つか | **足りない。ここだけを作る** |

### 2.2 ①はもうある

`text_blocks::style_runs`がブロック単位で`StyleRun`を吐く。1つの`StyleRun`は
UTF-16の範囲と、`heading_level`、`Marks{bold, italic, strike, code, link, comment}`、
`Ornament`を持つ。**ブロックが不変の単位**なので、1打鍵で組み直るのは触ったブロックだけ
（技術検証 5.5・6.9）。これは字句解析器そのものであって、記法の色のために新しい解析器を
建てる必要は無い。

### 2.3 ②は取らない

補完・定義へ移動・診断は、記号表と横断索引を要求する。要件 4.2 が外しているのはその先で、
**①があるからといって②へ進む理由にはならない。**買う人は縦書きで文章を書く人であって、
開発者ではない（要件定義 3）。

### 2.4 ③だけを作る

いまの色の順番は`directwrite_render.rs`の`draw_tile`の中に手書きで埋まっている——
「全部剥がす → コメント → 見出し → 単語（単語が勝つ）」。ここへ記法が入り、いずれ検索の
当たりが入ると、色源が4つ5つになる。**4つの取り合いを描画関数の途中で解くのは無理がある。**

**ただしこれは新しいモジュールを建てる話ではない。**層は多くて4つで、表を持つほどの数では
ない。**あの順番を名前のついた関数へ出し、順位を1か所に書く**——それだけが要件である。
ここを大きく作ると、要らない②まで引きずり込む。

## 3. 色の層（③）

### 3.1 順位は1か所に書く

弱いほうから強いほうへ：

1. **記法**（`#` `**` `>` `|`、リンクの綴り、コードの綴り）
2. **見出し・コメント**（要件 9 の6段の色、`comment_ink`）
3. **単語**（書き手が単語チェックモードへ置いた語）
4. **検索の当たり**（要件 7.7、まだ無い）

**単語が記法より強いのは、書き手が自分でそこへ置いたしるしだからである。**記法から出た色は
文書の形から自動で出たもので、言いたいことが弱い。**検索の当たりが最も強いのは、いま探して
いるからである**——探している最中に見つからないのは、色分けの失敗として最も重い。

### 3.2 選択とキャレットは層に入れない

**それらは色ではなく形である。**選択は`SelectionRect`として文字の下に敷かれ、キャレットは
線として引かれる。文字の色を取り合っていない以上、順位の話には入らない。混ぜると、
「選択中は単語の色が消える」のような、誰も要求していない振る舞いを作り込むことになる。

### 3.3 層は幾何を1画素も動かさない

**これは技術検証 9.3.1 の再掲であり、破ると静かに遅くなる。**色は`Typography`の外に置く。
`TextEngine::update`は`Typography`が違えば`measures`／`layouts`／`wraps`を捨てるので、
色をそこへ入れると**色を1つ変えるたびに文書全体を測り直す**。単語セットを一度そこへ入れて
しまい、ON/OFFの反映に数秒かかったのがこの失敗である。

層が変わったときに古くなるのは**タイルだけ**（`tile_signature`）。

### 3.4 置く前に剥がす

`SetDrawingEffect`はキャッシュされた組版の上に残る。**層を置く前に、その組版の効果を
全範囲について剥がす。**単語を1つ消したのにその語だけ色が残っていたのがこれである。

### 3.5 上限

1ブロックに置く単語の印は`MAX_MARKS_PER_BLOCK`（256）まで。**印が多すぎるページは、
色分けとして既に失敗している**——数を絞るのは性能のためでもあるが、読めなくなることの
歯止めでもある。

## 4. 記法の色

### 4.1 既定は「薄くする」であって「色を足す」ではない

**IDEでキーワードを目立たせるのは、キーワードが意味だからである。**散文では`**`は意味では
ない。書き手が読みたいのは文であって、アスタリスクではない。記法文字に色を付けると
**文が虫食いに見える**——とくに縦書きソースでは、行の途中に色の点が縦に並ぶ。

だから既定は：

- **記法文字（`#` `**` `>` `-` `|` `[` `]` `(` `)`）は薄くする。**中身は普通の濃さのまま
- 濃さの引き方は`Typography::comment_ink`と同じ考え——**書き手のインクを、書き手の紙へ寄せる**
  （いまは45%）。**誰も設定できない色は、誰かの紙に合わない**（要件 9 でリンクに下線を引いて
  色を使わなかったのと同じ理由）
- 紙を暗くしても自動で追随する。書き手が設定する項目は増えない

### 4.2 色を使うのは、中身の一部であるものだけ

薄くするのでは足りないもの＝**それ自体が読まれる内容**であるもの：

- リンク先のURL（`](ここ)`）
- インラインコードとコードブロックの中身
- コードの中のコメント（既にある）

**これで色は3つしか増えない。**小説を書く人にテーマエディタを渡さない、というのが
軽さの思想（要件定義 3）に沿う。

### 4.3 記法の判定は1か所から出す

**いま記法を判定しているのは`document::push_visible_line`で、プレビューを組み立てながら
決めている。**そしてソース面は`PaneText::Source`であり、**`marks()`は空を返す**——
ソース面はいま、行の種類（見出し段・コード段・引用の深さ）しか持っていない。

だから記法の色は**新しい仕事である**。それを別の場所で判定してはいけない：

- **プレビューが「これは太字の記号だ」と言い、ソースが別に判定すると、片方だけ直る。**
  「プレビューでは太字なのにソースでは色が付かない」が起きる
- 同じ判定から、**プレビューには「消す範囲」を、ソースには「薄くする範囲」を**返す。
  出口が2つで、判定は1つ

### 4.4 プレビュー面ではこの層は何もしない

プレビューは記法を消しているので、薄くする対象が無い。**層は在るが空である**——面ごとに
別の道を作らない。

### 4.5 コードの中は色分けしない

要件 4.2。フェンスの中で言うのは**コメントかどうか**だけで、これは既にある
（`Marks::comment`）。言語ごとの予約語には手を出さない。

## 5. モードの構造

### 5.1 組み込みモードと、書き手のモード

書き手の言い換え（2026-09-08）「C言語モード、C#モードを作るつもりで、構造的に自分の
単語チェックモードがあればいい」を、記法の側まで通す。

| | 中身 | 出どころ |
|---|---|---|
| **Markdownモード**（組み込み） | 記法の種類：見出し記号・強調記号・引用記号・リンク・コード・コメント | 字句解析（`StyleRun`） |
| **書き手のモード**（小説A、など） | 語群：人物・敵役・言い回し | 語の木（トライ） |

**設定画面は同じ形で見せられる**——モードがあり、その中に色を持つ項目が並ぶ。
概念が1つも増えない。共通なのは「範囲に色を置く」ところだけで、そこが第3章である。

### 5.2 見せ方は同じ、選び方は別

**これは最初の案の訂正である。**文書のモードを`なし`にしたときに記法の色まで消えるのは
間違いだった。

- **組み込みモードは、ファイルの種類＋ソース／プレビューから決まる。**書き手が文書ごとに
  選ぶものではない
- **書き手のモードは文書ごと**（要件定義 7.9）。タブのメニューとステータスバーから選ぶ
- したがって`なし`が消すのは**単語の色だけ**である

### 5.3 組み込みモードにできないこと

消せない。名前を変えられない。語を足せない。**色だけが設定できる。**
`New Mode`で作れるモードと同じ表に置かない——書き手の表は書き手のもので、
編集器が勝手に1行増やしてよい場所ではない。

## 6. 単語の管理

「ただの単語の色づけというより、その単語の管理機能は丁寧に考えておく方がよさそう」
（書き手、2026-09-08）に答える章。要件定義 7.9 の決定はすべて生きていて、以下はその上に足す。

### 6.1 語群はモードが所有する。共有しない

共有したくなる実例ははっきりしている——「自分の癖の言い回し」「使わないと決めた語」は
全作品で共通である。**それでも所有のままにする。**

理由は既に決めた規則との噛み合わせである。**重複はモードの中でだけ起きる**（要件 7.9）と
してあるところへ共有語群を入れると、「同じ語群が、あるモードでは衝突しているが別のモード
ではしていない」が起きる。正しく動くが、**画面から辿れない**。

**代わりに置くのはモードの複製**（6.5）。足りないと言われてから参照へ進む——順序として
安全な側である。

### 6.2 色は語群が決める。語ごとの色は無い

`int`だけ色を変えられるC言語モードは無い。**語に個別の色を許すと、語群という単位が
意味を失う**（語群とは「同じ色で見たい語の集まり」の別名である）。

### 6.3 色を付けない場所

いま単語の色は、コードブロックの中でもリンク先URLの中でも当たる。**当てない。**

- **コード**（`Marks::code`）：中身は文章ではない。人物名がコード例に出てきても、それは
  校正の対象ではない
- **リンク先**（`Marks::link`が示す「見せる側」の外）：URLは読むものではない
- **記法文字そのもの**：`**田中**`の`**`まで色が付くと、語の切れ目が読めない

**これが記法の層と単語の層が初めて交わる点であり、層を1本にする実利でもある**——
1本なら「単語はcodeとlinkの上には置かない」と1行で書ける。

### 6.4 語は由来を持つ

**手で足したのか、取り込んだのか。**`StoredGroup.words`は文字列の並びだが、ここへ1つ
足す。

- **取り込み直しで、手で足した語を消さないため。**他の道具で作った一覧を更新して入れ直す
  とき、書きながら足した語まで消えるのは、書き手の仕事を捨てることである
- **「なぜこの語があるのか」に答えられるため**

### 6.5 モードの複製と、モードごとの出し入れ

- **モードを複製する**（`小説A`から`小説B`）。6.1で共有を採らない代わりに要る
- **モードを丸ごと書き出す／取り込む。**いまは語群単位だけである。機械を移るとき、作品を
  引き継ぐとき、語群を4つ手で書き出して手で作り直すのは、**辞書は編集器が持ち、書き手は
  ファイルを管理しない**（要件 7.9）と矛盾する
- 語群1つの出し入れは1行1語のテキストのまま（他の道具と行き来できることが値打ち）。
  モード丸ごとは編集器自身の形式でよい——行き先はこの編集器だけである

### 6.6 正規化の規則は増やさない

ASCIIの大小だけを畳む（`find.rs`と同じ探し方）。**「ヶ所／箇所」を編集器が同じ語だと決め
始めると、編集器は正しさを判定しないという決定が崩れる**（要件定義 7.9）。両方を登録すれば
よく、そのために語群がある。

### 6.7 語のメモは後回し

「3章で改名」のような覚え書きは欲しくなるが、無くても困らない。**軽さを先に取る。**
6.4で語が構造を持つので、後から足せる。

### 6.8 上限は実測してから

いまはモードが16、1モードに語群8つ、語群あたり1万語、1ブロック256印。**どれも「足りないと言われて
から増やす」側の数である。**語群の数は色を用意するぶんの上限、語数は木の大きさの上限。

## 7. 取らないもの

IDEにあり、①があれば手が届いて見えるが、入れない：

- 補完、定義へ移動、参照検索
- 診断（波線）、綴りの検査
- **コードブロック内の言語別ハイライト**（要件 4.2）
- テーマの配布・読み込み。色は要件 9 の設定と、そこから混ぜたものだけ
- 表記ゆれの自動検出（要件定義 7.9：**編集器はどれが正しいかを判定しない**）

## 8. まだ決めていないこと

- **記法を薄くする度合い。**`comment_ink`の45%をそのまま使うか、別にするか。
  **実際に組んで、縦書きで目で見てから決める**（要件 7.10 の紙の既定値と同じ決め方）
- **リンク先とインラインコードに使う色。**紙の明るさから選ぶ（端末の16色と同じ考え）か、
  設定に出すか
- **検索の当たりの見せ方。**色（第4層）か、形（選択と同じ敷き方）か。7.7 を作るときに決める
- 6.8 の上限

## 9. 実装の順番

1. **③を1本にする**（`draw_tile`の順番を名前のある関数へ、順位を3.1として1か所に）
   ——これが無いと2も3も同じ場所を汚す
2. **記法の色**（4章）。判定は`push_visible_line`と同じ場所から出す（4.3）
3. **単語の管理**（6.1・6.3・6.5）

**1が先なのは、2と3が同じ場所を触るからである。**順番を変えると、同じ50行を3回書き直す
ことになる。
