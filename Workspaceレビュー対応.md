# Workspaceレビュー対応（2026-09-19）

RFN01-11のユーザーレビューを受けた合意。旧Workspace設計.mdと食い違う箇所はこの変更仕様を優先する。RFN01-5のリンク確認は別途維持し、PR #2は再確認前にマージしない。

## 追加レビューで確定した現行仕様

2026-09-19の再レビュー合意：CloneのURLは入力欄からフォーカスが離れた時に非同期でGitリポジトリ確認を行う。入力変更後の古い検証結果は表示しない。保存先は参照選択直後と手入力のフォーカス離脱時に確認する。参照ボタンは保存先欄の右側へ配置し、各欄の警告は赤字にする。取得実行時にも再検証する。

RFN01-23：Explorer上部のフォルダ名をクリックするとOpen Folderへ進む。フォルダ未選択時の見出しも同じ入口とする。重複するOpen Folderアイコンを取り除き、メインメニューと検索用のフォルダ操作は維持する。

リンクのPreview表示は明示した表示名を維持し、別名のないWikiリンクは拡張子なしのファイル名を表示する。見出し指定は残し、ソースとリンク先は変えない。Editor内のファイル／フォルダ移動・改名時に参照元文書のAuto SaveがONならリンク先を更新し、OFFなら本文を変更しない。現在のWorkspace（WorkspaceなしではExplorerのフォルダ）の範囲を対象にする。開いている文書は未保存本文・Undoを維持し通常の自動保存へ渡す。閉じた文書は文字コード・改行形式を維持し、外部変更・読み取り専用なら上書きしない。外部アプリでの移動追跡は今回対象外。

2026-09-19の追加指摘：Workspaceの開閉でメインWindowのサイズを変えない。登録フォルダの区切りにフォルダ名を表示し、マウスオーバーでフルパスを表示する。現在編集中のファイルが属する登録フォルダを強調する（ツリーの選択とは独立）。分割時はフォーカス中のペインを基準にする。

CloneはURLと空の保存先を一つのフォームで指定する。参照ボタンは保存先欄へ選択結果を戻す。URL形式に加えてGitによるリポジトリ確認を行い、入力不備・取得失敗は入力を保持したフォーム内で知らせる。取得中はフォームから取消可能。フォルダ選択の確定ボタンは「フォルダを追加」一つとし、表示中のフォルダを追加する。

RFN01-5：リンク候補は入力位置の近くに表示する。縦書き・横書きとスクロール・分割ペインに追従し、画面端では収まる側へ配置する。

初期一覧のWorkspace名を右クリックすると、閉じたWorkspaceの登録フォルダの場所変更へ進める。未使用の共通フォルダ設定がある場合だけ、初期一覧に折りたたみ式の整理欄を表示する。管理画面をなくしても、従来合意した場所変更とAppDataの整理手段を維持する。

以下を先の画面仕様より優先する。独立した管理画面と「ファイル一覧へ」は廃止する。未選択時は上部にWorkspace一覧、その下にNew Tabと同じリンク表示で新規Workspace・Clone。閉じるは置かない。選択すると保存確認付きでそのWorkspaceを開く。

使用中はWorkspace名から切替と名前変更・複製・既定指定・初期化・登録解除を行う。名前の右にフォルダ追加・Clone・表示フィルタの操作を置く。閉じるはPane最下段とし、初期画面へ戻る。Explorerの位置と展開状態は引き続き独立。

登録フォルダの中身を最上位に直接並べ、フォルダの境界を細い区切り線で示す。通常の追加とCloneで同じ表示にする。区切り線は所属フォルダの確認・右クリック・移動先指定にも使い、登録フォルダ自体を折り畳む操作は不要。下位フォルダは従来どおり開閉する。

余白右クリックはフォルダ追加・Clone。ファイル・フォルダ・区切り上の右クリックは、所属する登録フォルダ全体のAuto Save切替、取り外し、上下移動、Workspace表示・索引初期化に対応する。ファイル単位の保存設定は追加しない。取り外しで領域が変わる際も保存確認を先に行い、取消ではTAB・構成を維持する。

Cloneは開始時の対象を保持する。未選択時は新しいWorkspaceを作成し、使用中はそのWorkspaceへ取得先を追加する。取得中に別Workspaceへ切り替えても登録先を取り違えない。登録後のファイルは中身を直接表示する。表示フィルタは表示中のファイル名を絞り込み、フォルダの移動経路と区切りを保持する。ExplorerとWorkspaceの入力値を分ける。

横書きLivePreviewの見出し編集位置のずれは別不具合として扱う。

## 操作・画面

- 左のアイコン列のExplorerより上に、独立したWorkspaceアイコンを置く。メインメニューにも「Workspace…」を置く。Explorerの開閉アイコンをWorkspace操作へ流用しない。
- Workspace管理を左Pane内に表示する。初期状態は登録済みWorkspace一覧と「新規…」。選択後に「開く」、フォルダ構成、フォルダ単位の保存方式、複製・既定指定・リセット・登録解除を表示する。
- Workspaceを開いた後は同Paneに登録フォルダのツリーと「管理」「Workspaceを閉じる」を表示する。管理に戻るだけではTABを閉じない。
- Explorerのフォルダ位置・展開・選択とWorkspaceの表示状態を分離する。アイコンによる表示切替は文書やアクティブWorkspaceを変えない。
- リセットはWorkspace管理の初期状態へ戻す。フォルダ構成・保存方式・原稿を保持する。登録解除でExplorerの位置を変更しない。
- フォルダは登録順を基本とし、明示した上下移動で並べ替える。フォルダ選択では内容のファイルを確認して追加できる。
- 「共有」は同じフォルダを複数Workspaceへ登録する意味。ネットワーク共有の作成や設定は扱わない。保存方式は同一フォルダの共通設定。

## ファイル範囲とTAB

- Workspace利用中に開けるファイルは登録フォルダ配下に限定する。Explorer・履歴・Drop・リンク経由でも同じ制約を使う。保存先の変更で領域外へ移ることも防ぐ。
- Workspaceを開く／切り替える／閉じる時は、全ペインのTABを閉じる。未保存があれば先に保存・破棄・キャンセルを確認する。キャンセルまたは保存失敗ではTABを閉じず、現在のWorkspaceを維持する。破棄は追加確認する。
- 無題文書の作成・編集は可能。保存先は現在のWorkspace内にする。保存先未決定の本文は既存の退避規則で保護する。
- 起動引数にWorkspace外のファイルや明示フォルダがあれば、その起動はWorkspaceなし。登録済みのデフォルトは変更しない。
- 起動時、領域外の未保存文書の復元が必要な場合は、復元を優先してWorkspaceなしにし、理由を表示する。領域外の保存済みTABをデフォルトWorkspaceへ持ち込まない。

## 別の実装単位

Git Clone：取得元URLと空の保存先を指定し、成功した保存先をWorkspaceへ登録する。画面・範囲制限の修正とは独立した処理として実装する。取得中は編集を止めず、失敗時は登録しない。途中のファイルを自動削除しない。URL・保存先の入力は既存の名前入力と内容確認可能なフォルダ選択を用いる。登録後は一覧で選択され、「開く」で利用を開始する。

## レビュー結果と今回の検証

ユーザーが問題なしとした既存動作：複数フォルダ登録、切替時のExplorer範囲変更、既定起動、自動保存、自動退避、外部変更時の停止と保護。旧手順の「共有」およびリセット・解除全体は了承済み扱いにしない。

今回の完了条件：入口から管理への到達、狭いPaneでの操作、Explorer状態保持、切替取消・保存失敗・破棄確認、全ペインと履歴の範囲、複数rootとパス別名の境界、領域外起動と未保存復元、既存保存とリンクの回帰を確認する。確認結果は実行後に追記する。

## 2026-09-19 追加レビュー：表示の区別と選択時の検証

- ExplorerとWorkspaceの通常フォルダ行に、共通のフォルダアイコンと展開記号を表示する。ファイル行はフォルダアイコンなし。登録rootの区切りと階層は維持する。
- 保存方式のコンテキストは現在の状態を `AutoSave ON` / `AutoSave OFF` と明記する。
- Wikiの非編集行は短いファイル名表示、編集行は元のリンクパス全体を着色する。明示別名と通常Markdownの位置を維持する。
- 内容が見えるフォルダ選択の仮名「このフォルダ」を実際の表示中フォルダ名に変更する。Cloneでは確定時にも空を検証し、非空・確認不能なら選択画面を閉じない。通常のWorkspace追加では非空を許可する。
- 同じフォルダの保存設定は既に共有済み。親子登録は最も深い登録の設定が全Workspace共通で適用される。索引キャッシュのWorkspace間・親子root間共有は提案として保持し、今回の表示変更で実装済みとはしない。

## 2026-09-19 最終合意：リンクの判定色

未判定を既定の通常リンク色とする。Workspaceを開いていない文書、現在のWorkspace外のリンク、走査中・走査失敗・判定不能を赤くしない。有効なリンクも通常色。現在のWorkspace内でリンク先がない、または曖昧と確定した場合だけ赤くする。編集中のパス全体と非編集時の表示名で同じ判定を使用し、本文・別名・リンク先は変えない。

ユーザーは既存Workspace・Explorerの確認待ち部分を了承し、リンク色の実装・検証後にPR #2をマージすることを承認した。IssueのCloseはユーザーが行う。

## 別Issueへ分離した検討事項

- RFN01-24：Workspace間の索引共有と親子フォルダの重複走査の整理。保存方式の共通設定は既存仕様として維持。共有方式・寿命・整理・移行は未決定。
- RFN01-25：任意ファイルステータスの用途・操作。未定義の候補として別途検討。
- RFN01-26：起動時の縦横・ソース／プレビュー指定。RFN01-9で保留していた候補を分離。
- 既存RFN01-6/7/8/10/22はそれぞれ大文書・部品化・起動高速化・WSL起動・見出し位置ずれを扱い、重複起票しない。
