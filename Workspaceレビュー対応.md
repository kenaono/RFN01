# Workspaceレビュー対応（2026-09-19）

RFN01-11のユーザーレビューを受けた合意。旧Workspace設計.mdと食い違う箇所はこの変更仕様を優先する。RFN01-5のリンク確認は別途維持し、PR #2は再確認前にマージしない。

## 操作・画面

- 左のアイコン列のExplorerより上に、独立したWorkspaceアイコンを置く。メインメニューにも「Workspace…」を置く。Explorerの開閉アイコンをWorkspace操作へ流用しない。
- Workspace管理を左Pane内に表示する。初期状態は登録済みWorkspace一覧と「新規…」。選択後に「開く」、フォルダ構成、フォルダ単位の保存方式、複製・既定指定・リセット・登録解除を表示する。
- Workspaceを開いた後は同Paneに登録フォルダのツリーと「管理」「Workspaceを閉じる」を表示する。管理に戻るだけではTABを閉じない。
- Explorerのフォルダ位置・展開・選択とWorkspaceの表示状態を分離する。アイコンによる表示切替は文書やアクティブWorkspaceを変えない。
- リセットはWorkspace管理の初期状態へ戻す。フォルダ構成・保存方式・原稿を保持する。登録解除でExplorerの位置を変更しない。
- フォルダは登録順を基本とし、明示した上下移動で並べ替える。フォルダ選択では内容のファイルを確認して追加できる。
- 「共有」は同じフォルダを複数Workspaceへ登録する意味。ネットワーク共有の作成や設定は扱わない。保存方式は同一フォルダの共通設定。

## ファイル範囲とTAB

- Workspace利用中に開けるファイルは登録フォルダ配下に限定する。Explorer・履歴・Drop・リンク経由でも同じ制約を使う。保存先の変更で領域外へ移ることも防ぐ。
- Workspaceを開く／切り替える／閉じる時は、全ペインのTABを閉じる。未保存があれば先に保存・破棄・キャンセルを確認する。キャンセルまたは保存失敗ではTABを閉じず、現在のWorkspaceを維持する。破棄は追加確認する。
- 無題文書の作成・編集は可能。保存先は現在のWorkspace内にする。保存先未決定の本文は既存の退避規則で保護する。
- 起動引数にWorkspace外のファイルや明示フォルダがあれば、その起動はWorkspaceなし。登録済みのデフォルトは変更しない。
- 起動時、領域外の未保存文書の復元が必要な場合は、復元を優先してWorkspaceなしにし、理由を表示する。領域外の保存済みTABをデフォルトWorkspaceへ持ち込まない。

## 別の実装単位

Git Clone：取得元URLと空の保存先を指定し、成功した保存先をWorkspaceへ登録する。画面・範囲制限の修正とは独立した処理として実装する。取得中は編集を止めず、失敗時は登録しない。途中のファイルを自動削除しない。URL・保存先の入力は既存の名前入力と内容確認可能なフォルダ選択を用いる。登録後は一覧で選択され、「開く」で利用を開始する。

## レビュー結果と今回の検証

ユーザーが問題なしとした既存動作：複数フォルダ登録、切替時のExplorer範囲変更、既定起動、自動保存、自動退避、外部変更時の停止と保護。旧手順の「共有」およびリセット・解除全体は了承済み扱いにしない。

今回の完了条件：入口から管理への到達、狭いPaneでの操作、Explorer状態保持、切替取消・保存失敗・破棄確認、全ペインと履歴の範囲、複数rootとパス別名の境界、領域外起動と未保存復元、既存保存とリンクの回帰を確認する。確認結果は実行後に追記する。
