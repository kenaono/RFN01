# RFN01-24 Workspace索引 検証結果

2026-09-20。実装・自動検証済み、ユーザーAccept待ち。既存の未コミット変更を保持した作業ツリーで検証。マージ・Issue Closeは未実施。

2026-09-21のコードレビューを受けた修正を反映し、確認用ビルドを作り直した。変更点と再検証は末尾の「レビュー対応」を参照。コミットは整形のみの `12cb104` と作業単位の `8056184`（ローカル、未push）。

## 変更

- 索引対象をmdと対応画像に限定。txt/log等の単独編集・明示パスの利用は維持。
- 相対パスを優先し、索引で拡張子省略mdと画像名を補完。同名で一意に決まらない場合は勝手に選ばない。列挙未完了時の非ローカル候補も確定しない。
- 共有ディスクキャッシュ、親子範囲への再投影、短期間の列挙・完成済み索引再利用。旧キャッシュの読み込みにも対応し、移行元は保守的に保持。
- 背景走査・見出し解析は最大4ワーカー。別の優先スレッドで開いているmdと直接のリンク先を更新し、未保存見出しを反映。世代と背景リビジョンで古い結果の上書きを防止。
- 走査の待機・初回結果・列挙・解析・保存・総時間、件数、再利用数、優先更新の待機・処理時間を診断ログへ記録。本文とファイル名は記録しない。UI描画完了までの時間はこの計測には含まない。

## 自動検証

- `cargo test --offline -- --test-threads=1 --quiet`: 1,054成功、0失敗、25 ignored。
- 2026-09-21のレビュー対応後: `cargo test --offline -- --test-threads=1`: 1,055成功、0失敗、26 ignored（ignoredの増分1件は追加した `view_rebuild_benchmark`）。`cargo fmt --all -- --check` 成功（作業ツリー全体が整形済み）。`cargo build --offline --release` はベンチマーク実行時に成功。
- ignoredのリンク操作描画テスト `link_ui_tests::local_links_open_without_losing_the_source` を個別実行: 1成功。
- `cargo build --offline --release`: 成功（未使用コードの警告あり）。
- 実行ファイルを分離設定で起動。診断ログで背景走査完了、md2件解析、優先更新3件反映を確認。起動時の小規模fixture走査は1.272ms。これは下記ベンチマークとは別。
- 実画面操作はComputer Useのアプリ操作承認タイムアウトにより未確認。自動描画・画像デコードテストの成功を実画面確認として扱わない。

## 性能比較

32ディレクトリ、各64件のmd/png/txt、計6,144ファイル。mdは各100見出し。旧方式は6,144件、新方式は4,096件を索引へ登録。OSのファイルキャッシュは消去していない。「初回」は索引キャッシュが空の意味。

| 条件 | 初回 ms | 変更なし ms | 1ファイル変更 ms |
|---|---:|---:|---:|
| 旧方式・単回 | 200.547 | 91.797 | 81.114 |
| 新方式1ワーカー・3回中央値 | 143.954 | 79.930 | 79.251 |
| 新方式2ワーカー・3回中央値 | 98.299 | 74.635 | 76.969 |
| 新方式4ワーカー・3回中央値 | 78.374 | 72.925 | 73.735 |

合成データでの測定であり、実原稿・異なるストレージの所要時間を保証するものではない。旧方式との比較には対象種類の削減効果も含む。新方式1対4ワーカーの比較で並列化の効果を区別できる。

## 確認用の版と手順

- 基点: コミット `8056184`（作業単位。親に整形のみの `12cb104`、その基点HEADは `bf666f5581faa71899cb56ff5cf1f094d6bc03b5`）。この内容から作成した。
- 実行ファイル: `target/rfn24-review/editor_spike.exe`（`cargo build --offline --release` の `target/release/editor_spike.exe` を配置）
- SHA256: `42DEE4E909C815D8BBB7F7CB7007ABE145163227A5AF1D4AE9D5D1B901A646CA`（48,805,376バイト、2026-09-21 10:10作成。`target/release` 側と同一）
- 分離した確認用Workspace: `testdata/rfn24-review`。アプリ設定は `testdata/rfn24-appdata`。通常のユーザー設定とは別。

1. 分離起動スクリプト `testdata/rfn24-review/確認起動.ps1` を実行。
2. Review.mdの `[[Target]]` と `[[Target#Target heading]]` を開き、notes/Target.mdと指定見出しへ移動できること。
3. Review.mdの画像名だけの埋込みと相対パスの画像がともに表示されること。
4. Target.mdの見出しを未保存のまま変更し、リンク補完に最新見出しが反映されること。
5. RFN24 OneとRFN24 Twoを切り替え、同じリンク・画像が解決すること。索引候補にignore.txt/ignore.logが含まれないこと。
6. 実原稿のWorkspaceでも開いてすぐの操作感、画像、同名候補、親子フォルダの切替を確認すること。

ユーザーAccept後に統合を判断し、Issue Closeはユーザーが行う。

## レビュー対応（2026-09-21）

コードレビューの5件について、ユーザーの判断を受けて次のように直した。

1. **走査キャッシュの破棄条件**（`workspace_index.rs` の `run_worker`）: 「切替時の重複再利用を優先し、同スコープの再走査ではどうせ期限切れなので捨てる」が意図であると確定した。比較の向きは変更せず、その理由と非対称であることをコードに記録した。あわせて `index_work` に、同じディレクトリの列挙が寿命内では再利用され、破棄後は読み直されることを固定するテストを追加した。
2. **索引全体の複製**（`main.rs` / `workspace_links.rs` / `pictures.rs`）: 共有ビューを `Arc<Vec<Entry>>` とし、`entries_shared()` で複製せずに渡すようにした。妥当性確認・優先要求・画像索引は同じ Arc を共有する。`rebuild_visible` は内容が変わりうるイベントのときだけ動き、`Complete` などの進捗通知では作り直さない。UI側の費用として `index_view`（rebuilds / rebuild_ms / compare_ms / entries）と `priority_submit_ms` を診断・性能ログに追加した。件数と時間のみで、本文とファイル名は記録しない。
   - 実測（2026-09-21、release、合成10,000ファイル×30見出し、`view_rebuild_benchmark`）: 初回公開 rebuild 54.8ms / compare 0.0ms / 全体 79.9ms。1ファイル変更のイベント1回あたり rebuild 52.7〜54.2ms / compare 0.0〜1.2ms / 全体 59ms（中央値 53.9ms、5回）。
   - この結果から、UIスレッド側の残る主要な費用は**ビューの作り直しそのもの**（スナップショット全件の複製＋整列）であり、呼び出し側の複製は解消済みと分かる。件数にほぼ比例するため、原稿規模が小さいうちは軽く、1万件規模では1回あたり約54msになる。次に軽くするなら、Entry を `Arc<Entry>` にして作り直しの複製をポインタ操作に落とすか、ビューを差分更新にする方向がある（未着手・要相談）。
3. **整形**: `cargo fmt --all` を適用し、`cargo fmt --all -- --check` が通る状態にした。対象は今回の作業ファイルに限らず、以前から未整形だったファイル（`saving.rs` 約5,100行、`terminal.rs`、`panel_source.rs`、`directwrite_render.rs` など）にも及ぶ。差分が広いため、作業単位を狭めたい場合は整形分だけ戻せる。
4. **削除漏れと重複**: `ValidityChecker::submit` を削除して `submit_scoped` 一本に統一（テストも `submit_scoped(..., true)` へ）。共有キャッシュの `*.lock` を整理対象に追加（`PruneShared` がキャッシュ本体を消したあと、ロックを解放してからロックファイルも消す）。`Entry::headings` と `ScanOptions::max_heading_bytes` の説明を実装（mdのみ解析、txt/logは索引対象外）に合わせた。
5. **移動時のリンク書き換え**: `link_move` の `is_indexable` 判定と `resolve_indexed_file` の型フィルタを外し、索引の名前解決対象外である `.pdf`/`.txt` へのリンクも移動に追従して書き換わるように戻した。確認は裸名（`[[old.pdf]]`）のテストで行う。索引側は生成時点（走査・共有キャッシュ・優先更新・可視ビュー）で型を絞っているため、ビューに他形式が混じることはない。

残件と未確認:

- 実画面操作は未確認（Computer Use のアプリ操作承認タイムアウトが続いている）。今回の修正も同じく未確認。
- 実原稿での `index_view` / `priority_submit_ms` は未取得。上記の性能比較は索引走査のみで、UIスレッド側の費用を含まない。
- 確認用ビルドは 2026-09-21 に作り直し済み（上記SHA256）。実画面での確認はこれから。
- `cargo check` の未使用警告のうち `Indexer::clear` / `Command::Clear` / `clear_cache` / `overlay_dirty_headings` / `Snapshot::generation` は今回の変更以前からあるもの（テスト専用または未使用）で、今回は触っていない。
